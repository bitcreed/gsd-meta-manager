---
phase: quick
plan: 260403-p84
subsystem: ui/detail
tags: [keyboard, scrolling, ux]
dependency_graph:
  requires: []
  provides: [page-scrolling]
  affects: [detail-screen]
tech_stack:
  added: []
  patterns: [PAGE_SCROLL_LINES constant for page-size scrolling]
key_files:
  created: []
  modified:
    - src/ui/screens/detail.rs
decisions: []
metrics:
  duration: 2min
  completed: "2026-04-04T01:14:02Z"
---

# Quick Task 260403-p84: Add PageUp/PageDown Scrolling Summary

PageUp/PageDown key handlers added to all scrollable detail views, scrolling by 20 lines/items per page press.

## What Was Done

### Task 1: Add PageUp/PageDown key handlers to detail screen
- **Commit:** 3d8d720
- **Files modified:** src/ui/screens/detail.rs

Added `PAGE_SCROLL_LINES` constant (20) near the top of the file alongside `TAB_TITLES`.

Added two new `KeyCode` match arms (`PageDown` and `PageUp`) in `handle_input`, placed between the existing `Up`/`k` handler and the tab-switching number key handlers. Both arms mirror the exact structure of the existing `Down`/`Up` single-line handlers:

- **List-based views** (GitHistory, Backlog, Pipeline, Queue, Sessions): scroll selected index by `PAGE_SCROLL_LINES as usize`, clamped to list bounds
- **Archive view**: handles all depth levels (MilestoneList, PhaseList, FileList with page-size index jumps; FileView with `archive_scroll_offset` page-size scroll)
- **Catch-all** (Overview, Roadmap, State, etc.): scrolls `self.scroll_offset` by `PAGE_SCROLL_LINES`

All branches set `ctx.needs_redraw = true` and return `ScreenAction::None`.

## Verification

- `cargo check` passes cleanly
- `cargo test` passes (25 tests, 0 failures)
- `KeyCode::PageDown` and `KeyCode::PageUp` confirmed present in detail.rs

## Deviations from Plan

None -- plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED
