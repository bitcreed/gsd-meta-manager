---
phase: quick
plan: 260403-p84
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/screens/detail.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "PageUp/PageDown scrolls content by a page in all scrollable detail views"
    - "Archive FileView scrolls by a page with PageUp/PageDown"
    - "Existing j/k/Up/Down single-line scroll still works unchanged"
  artifacts:
    - path: "src/ui/screens/detail.rs"
      provides: "PageUp/PageDown key handlers"
      contains: "KeyCode::PageDown"
  key_links:
    - from: "KeyCode::PageDown handler"
      to: "scroll_offset / archive_scroll_offset"
      via: "saturating_add with page size"
      pattern: "PageDown.*saturating_add"
---

<objective>
Add PageUp/PageDown keyboard scrolling to the detail screen's markdown viewer and all scrollable content views.

Purpose: Currently scrolling through long content (markdown files in archive FileView, roadmap, state info) requires holding j/k one line at a time. PageUp/PageDown enables fast navigation through long documents.

Output: Updated detail.rs with PageUp/PageDown handlers that scroll by a page (~20 lines) in all views that currently support single-line scrolling via j/k.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/screens/detail.rs
</context>

<interfaces>
<!-- Key patterns from detail.rs the executor needs -->

Scroll state fields:
- `self.scroll_offset: u16` -- used by most tabs (Overview, Roadmap, State, etc.) for general content scrolling
- `cache.archive_scroll_offset: u16` -- used by Archive FileView for file content scrolling

Existing single-line scroll pattern (KeyCode::Down / 'j', lines 339-436):
- For list-based views (GitHistory, Backlog, Pipeline, Queue, Sessions): increments selected index
- For Archive FileView: `cache.archive_scroll_offset.saturating_add(1)`
- For all other views (catch-all `_ =>`): `self.scroll_offset.saturating_add(1)`

Same pattern inverted for KeyCode::Up / 'k' (lines 438-497).

The `current_view: DetailSubView` determines which branch executes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add PageUp/PageDown key handlers to detail screen</name>
  <files>src/ui/screens/detail.rs</files>
  <action>
Add a constant for page scroll amount near the top of the file (after existing constants like TAB_TITLES):

```rust
const PAGE_SCROLL_LINES: u16 = 20;
```

Add two new KeyCode match arms in the `handle_input` method, after the existing `KeyCode::Char('k') | KeyCode::Up` block (after line ~497) and before the tab-switching number key handlers (line ~499). The new arms should follow the exact same structure as the existing Down/Up handlers but use the page size:

**KeyCode::PageDown:**
- For list-based views (GitHistory, Backlog, Pipeline, Queue, Sessions): scroll the selected index by `PAGE_SCROLL_LINES as usize`, clamped to max
- For Archive FileView: `cache.archive_scroll_offset.saturating_add(PAGE_SCROLL_LINES)`
- For catch-all `_ =>`: `self.scroll_offset.saturating_add(PAGE_SCROLL_LINES)`

**KeyCode::PageUp:**
- For list-based views: `selected.saturating_sub(PAGE_SCROLL_LINES as usize)`
- For Archive FileView: `cache.archive_scroll_offset.saturating_sub(PAGE_SCROLL_LINES)`
- For catch-all `_ =>`: `self.scroll_offset.saturating_sub(PAGE_SCROLL_LINES)`

All branches must set `ctx.needs_redraw = true` and return `ScreenAction::None`.

Mirror the existing Down/Up handler structure exactly -- same view matching, same cache entry patterns, same clamping logic -- just replace `1` / `saturating_sub(1)` with `PAGE_SCROLL_LINES` / `saturating_sub(PAGE_SCROLL_LINES)`.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>PageUp and PageDown KeyCode arms exist in detail.rs handle_input, cargo check passes with no errors, scrolling uses PAGE_SCROLL_LINES constant for all scrollable views</done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo test` passes (no regressions)
- grep confirms `KeyCode::PageDown` and `KeyCode::PageUp` in detail.rs
</verification>

<success_criteria>
- PageDown scrolls all detail views forward by 20 lines (or items for list views)
- PageUp scrolls all detail views backward by 20 lines (or items for list views)
- Existing j/k/Up/Down single-line scrolling is unmodified
- Code compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260403-p84-add-pageup-pagedown-scrolling-to-markdow/260403-p84-SUMMARY.md`
</output>
