---
phase: quick-260917-r4c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-conformance-oracle.sh
  - scripts/pre-tag-check.Dockerfile
  - scripts/pre-tag-check.sh
  - .github/workflows/release.yml
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260917-r4c]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/install-conformance-oracle.sh, run in a HOME with no GSD install and with node+npm present, leaves $HOME/.claude/gsd-core/bin/gsd-tools.cjs resolvable and exits 0."
    - "The same script, run where $HOME/.claude/gsd-core already exists, does NOT modify it: it verifies it and exits 0, or exits non-zero if it exists but cannot answer a query."
    - "The script exits non-zero if the oracle does not actually answer after provisioning. It never reports success while leaving the oracle unresolvable."
    - "No GSD version literal appears anywhere in the new script, the Dockerfile or the workflow; the version is read out of GSD_CORE_SYNCED_VERSION in src/state_reader/config_json.rs at run time, and an unextractable constant is a hard error."
    - "A bare ./scripts/pre-tag-check.sh invocation behaves exactly as it does today: same gate order, same reconciliation banner, same EXIT-trap summary, same exit-code semantics."
    - "./scripts/pre-tag-check.sh --container builds scripts/pre-tag-check.Dockerfile, provisions the oracle inside the container with the SAME script the publish job calls, and re-runs this same script's gates in there."
    - "When no container runtime is usable, --container fails loudly and exits non-zero; it never silently falls back to a local run."
    - "The inner, in-container invocation cannot re-enter container mode."
    - "release.yml's publish job provisions the oracle BEFORE its Test step, and its msrv job is byte-identical to the previous commit."
    - "Nothing anywhere in the repo sets the conformance test's missing-oracle opt-out variable."
    - "CLAUDE.md Release Process step 3 tells the operator to run the container dry run before tagging and says why a bare local run cannot certify the publish job."
  artifacts:
    - "scripts/install-conformance-oracle.sh (new, mode 0755)"
    - "scripts/pre-tag-check.Dockerfile (new)"
    - "scripts/pre-tag-check.sh (gains --container; everything else untouched)"
    - ".github/workflows/release.yml (publish job only)"
    - "CLAUDE.md (Release Process step 3)"
  key_links:
    - "GSD_CORE_SYNCED_VERSION (src/state_reader/config_json.rs:21) -> runtime grep inside scripts/install-conformance-oracle.sh -> the exact npm version installed"
    - "scripts/install-conformance-oracle.sh -> $HOME/.claude/gsd-core symlink -> Oracle::resolve()'s FIRST branch (tests/driver_router_conformance.rs:91-115)"
    - "scripts/install-conformance-oracle.sh -> called by BOTH .github/workflows/release.yml (publish) and scripts/pre-tag-check.sh --container, so the dry run provisions byte-identically to CI"
    - "scripts/pre-tag-check.Dockerfile gh apt line -> gh_grammar_probe tests in src/envelope/policy.rs (they need the runner's gh, not a missing one)"
    - "scripts/pre-tag-check.Dockerfile git-core PPA line -> CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION witness in src/envelope/policy.rs"
---

<objective>
Give the release publish job the one dependency it is missing — GSD's own `gsd-tools`
conformance oracle — and give the operator a container dry run that provisions the same way,
so a green pre-tag check actually certifies the publish job.

Purpose: the crate has failed to publish three times. Each tag exposed exactly one
environment-dependent test, because `.github/workflows/release.yml`'s publish job runs a
fail-fast `cargo test` on a runner that has neither GSD installed nor any local rehearsal that
could notice. v1.7.1 died on
`tests/driver_router_conformance.rs::the_rust_rule_table_agrees_with_gsd_s_own_router_over_a_fixture_per_state`,
which fails BY DESIGN when its oracle is absent. The fix is to PROVIDE the dependency, never to
switch the check off.

Output: one new provisioning script called by both CI and the dry run, one container definition
that reproduces `ubuntu-latest`, a `--container` mode on the existing pre-tag script, the
publish-job wiring, and the CLAUDE.md release-step update.

NOT in scope, hard: any behaviour change under `src/`, any `#[ignore]`, any `Cargo.toml` bump,
any tag or release, any new file under `.github/workflows/`, and the known 1-in-800 BrokenPipe
race in `tests/envelope_tracer.rs`'s `run_stub` (deliberately left open — do not touch it).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@scripts/pre-tag-check.sh
@.github/workflows/release.yml
@tests/driver_router_conformance.rs
@src/state_reader/config_json.rs
</context>

<measured_facts>
These were measured before planning. Do NOT re-derive them; they cost hours of container work.

- The complete list of environment-dependent failures, from an exhaustive
  `cargo test --no-fail-fast` in a clean ubuntu-latest-lookalike (45 integration binaries + lib +
  bin + doc-tests, 2118 passed / 3 failed):
    1. the router-conformance test, because the oracle is absent. THE ONLY GENUINE RUNNER FAILURE.
    2+3. the two `gh_grammar_probe` tests in `src/envelope/policy.rs` — NOT runner failures.
       `ubuntu-latest` ships `gh` preinstalled; they failed only because the first container build
       omitted it. Re-measured with gh 2.101.0 from the same `cli.github.com` apt repo: BOTH PASS.
       There is nothing to fix in code; the container just has to carry `gh`.
  The git-version witness PASSES in the container, because its git is 2.55.0 (from
  `ppa:git-core/ppa`) — the version the constant records and the runner carries.
- The provisioning sequence below is PROVEN end to end in that container. It lands the oracle on
  `Oracle::resolve()`'s FIRST branch, so no PATH surgery is needed — which matters, because a PATH
  exported in one GitHub Actions `run:` step does not survive into the next without `$GITHUB_PATH`.
    npm install -g --prefix "$HOME/.npm-global" "@opengsd/gsd-core@<version>"
    mkdir -p "$HOME/.claude"
    ln -s "$HOME/.npm-global/lib/node_modules/@opengsd/gsd-core/gsd-core" "$HOME/.claude/gsd-core"
- The npm package's `gsd-core/` directory has NO `VERSION` file. Do not verify by reading one.
- `gsd-tools --version` is NOT a valid probe: it exits non-zero with "Unknown flag: --version".
- With the oracle in place the conformance test does REAL work and prints:
  "conformance: 5 command comparisons, 2 declared divergences, 2 upstream-silent states, over 9 fixtures".
- On THIS machine, verified at planning time: `docker` and `podman` are both on PATH and the docker
  daemon is reachable; node v24.15.0 and npm 11.12.1 are present; `$HOME/.claude/gsd-core` exists as
  a REAL DIRECTORY (a developer install, not a symlink) — so task 1's don't-clobber rule is live here.
  `shellcheck`, `shfmt` and `yq` are NOT installed; `python3` with `pyyaml` IS.
- `ubuntu:24.04` already has uid 1000 (user `ubuntu`); `useradd -u 1000` fails with
  "UID 1000 is not unique" unless you `userdel -r ubuntu` first.
- Named docker volumes are created root-owned and must be chowned before a non-root run.
</measured_facts>

<tasks>

<task type="tracer">
  <name>Task 1: scripts/install-conformance-oracle.sh — provision the oracle, end to end, in a HOME that has none</name>
  <files>scripts/install-conformance-oracle.sh</files>
  <precondition>`node` and `npm` are on PATH and the network can reach the npm registry (measured present here: node v24.15.0, npm 11.12.1).</precondition>
  <action>
Create `scripts/install-conformance-oracle.sh`, mode 0755, `#!/usr/bin/env bash`. It is the SINGLE
SOURCE OF TRUTH for "what the publish job needs on top of a bare ubuntu-latest so that `cargo test`
actually tests something", and it is called by BOTH `.github/workflows/release.yml` and
`scripts/pre-tag-check.sh --container` so the dry run provisions byte-identically to CI. Say that in
the header comment, in this repo's house voice: WHY each rule is there, not just what it does.

Write it in the style of `scripts/pre-tag-check.sh` — tab indentation, a `PROG="${0##*/}"`, loud
`HARD ERROR:` blocks framed by a `RULE` line, prose that explains the reasoning rather than
restating the code. Unlike that script this one may use `set -euo pipefail`: it has no
run-all-gates-and-report obligation, so an early hard stop is the correct shape. State that choice
in a comment so the divergence from its sibling reads as deliberate.

Behaviour, in order:

1. CONSTANT EXTRACTION. Read the GSD version out of `src/state_reader/config_json.rs` at run time by
   matching the `pub const GSD_CORE_SYNCED_VERSION : &str =` DECLARATION line only, then extracting
   the quoted literal. Mirror the existing CONSTANT EXTRACTION block at `scripts/pre-tag-check.sh`
   lines 155-202 — that is the house style and its reasoning applies verbatim here: the version
   string must NEVER be spelled in this script, because a copy goes stale silently. Match the
   declaration only, because the constant is also named in the doc comments above it and in
   `GSD_CORE_SYNCED_COMMIT`'s doc comment, any of which would extract the wrong literal. If the file
   is missing, or no declaration matches, or the line carries no quoted literal, print a loud
   HARD ERROR naming what it was looking for and exit non-zero. Resolve the source file relative to
   the script's own location (derive the repo root from `${BASH_SOURCE[0]}`), so the script works
   from any cwd — CI runs it from the workspace root, the container runs it from `/work`.

2. TOOL REQUIREMENTS. Require `node` and `npm` on PATH. If either is absent, HARD ERROR naming
   which one and saying that the publish job must install Node before calling this script.

3. THE DON'T-CLOBBER RULE — decide it here and DOCUMENT it at the top of the script, because this
   will be run by hand on developer machines that already have a real GSD install (this one does:
   `$HOME/.claude/gsd-core` is a real directory, not a symlink). The rule is: if
   `$HOME/.claude/gsd-core` already exists — as a directory, a symlink, or anything else — the
   script NEVER touches it. It skips installation entirely, goes straight to the verification in
   step 5, exits 0 if the existing install answers, and exits NON-ZERO with a loud message if it
   exists but does not answer. It does not repair, move, back up or overwrite it; the message tells
   the operator to fix or remove it themselves. Removing a developer's GSD install as a side effect
   of a release rehearsal is not this script's decision to make.

4. INSTALL (only reached when nothing is there). Run the measured sequence from `<measured_facts>`
   with the extracted version substituted for `<version>`: `npm install -g --prefix
   "$HOME/.npm-global"` the exact pinned `@opengsd/gsd-core@<version>`, then `mkdir -p
   "$HOME/.claude"`, then symlink the package's `gsd-core` subdirectory to `$HOME/.claude/gsd-core`.
   Pin the EXACT version, never a range and never `@latest`: this runs in the job that holds the
   crates.io token, and an unpinned install is an unreviewed code path in that job. Note in a
   comment that the target of the symlink is the package's inner `gsd-core/` directory, not the
   package root, and that this is what puts `bin/gsd-tools.cjs` at the path
   `Oracle::resolve()` probes first.

5. VERIFY THE ORACLE ACTUALLY ANSWERS, and exit non-zero if it does not. A provisioning step that
   "succeeds" while leaving the oracle unresolvable hands the fail-open shape straight back to the
   test, which is the whole failure this task exists to close — say exactly that in the comment.
   The probe must exercise the SAME call path the test uses, so a green probe means a green test:
   assert `$HOME/.claude/gsd-core/bin/gsd-tools.cjs` is a file, then build a throwaway fixture in a
   `mktemp -d` that mirrors `skeleton()` at `tests/driver_router_conformance.rs:232-246` — a
   `.planning/ROADMAP.md` carrying a `Phase 01` entry, a minimal `.planning/STATE.md` with
   `status: in_progress` front matter, and an empty `.planning/phases/` — then run
   `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query init.manager` with that fixture as cwd.
   Require exit 0 AND stdout that parses as JSON (pipe it through a `node -e` that JSON.parses
   stdin). On failure, print the exit status and the first ~200 bytes of stdout/stderr and exit
   non-zero. Do NOT probe with a `--version` flag — it is not a valid flag on this tool and exits
   non-zero. Clean the fixture up on exit. Note in a comment that `init.manager` is the verb
   `tests/driver_router_conformance.rs:54` names, so the probe and the test ask the same question.

6. VERSION ADVISORY, never a failure. After a successful verification of a PRE-EXISTING install,
   make a best-effort read of that install's version (look for a `package.json` carrying a
   `version` adjacent to the resolved `gsd-core` directory) and, if it is findable and differs from
   the extracted constant, print a loud ADVISORY — never change the exit code. This mirrors the
   git-version advisory at `scripts/pre-tag-check.sh` lines 207-235 and its reasoning is the same:
   a developer's install legitimately runs ahead of the synced constant (this machine's is 52
   commits past v1.14.0), and hard-failing there would paint the script red on a good tree and
   train the operator to ignore it. If the version cannot be determined, say "unknown" and move on;
   the package's `gsd-core/` directory has no `VERSION` file, so absence is expected, not a fault.

7. Report what it did in one clear closing block: installed-vs-already-present, the resolved oracle
   path, and the probe result.

Make it idempotent: a second run on a HOME the first run provisioned takes the step-3 path, verifies,
and exits 0.
  </action>
  <verify>
    <automated>bash -n scripts/install-conformance-oracle.sh &amp;&amp; test -x scripts/install-conformance-oracle.sh &amp;&amp; ! grep -nE '1\.14\.0|@opengsd/gsd-core@[0-9]' scripts/install-conformance-oracle.sh &amp;&amp; bash -c 'set -e; RCH="${CARGO_HOME:-$HOME/.cargo}"; RRH="${RUSTUP_HOME:-$HOME/.rustup}"; FAKE=$(mktemp -d); trap "rm -rf \"$FAKE\"" EXIT; echo "== negative control: no oracle in a fake HOME =="; if env HOME="$FAKE" CARGO_HOME="$RCH" RUSTUP_HOME="$RRH" cargo test --test driver_router_conformance the_rust_rule_table >/dev/null 2>&amp;1; then echo "NEGATIVE CONTROL FAILED: the test passed with no oracle"; exit 1; fi; echo "ok: fails without the oracle"; echo "== provision =="; env HOME="$FAKE" ./scripts/install-conformance-oracle.sh; echo "== idempotent re-run =="; env HOME="$FAKE" ./scripts/install-conformance-oracle.sh; echo "== positive: the test now does REAL work =="; env HOME="$FAKE" CARGO_HOME="$RCH" RUSTUP_HOME="$RRH" cargo test --test driver_router_conformance -- --nocapture 2>&amp;1 | grep -F "conformance: 5 command comparisons"'</automated>
    <automated>bash -c 'set -e; BEFORE=$(stat -c "%i %Y" "$HOME/.claude/gsd-core"); ./scripts/install-conformance-oracle.sh; AFTER=$(stat -c "%i %Y" "$HOME/.claude/gsd-core"); [ "$BEFORE" = "$AFTER" ] || { echo "CLOBBERED the developer install: $BEFORE -> $AFTER"; exit 1; }; echo "ok: pre-existing install verified and untouched"'</automated>
  </verify>
  <done>Running the script in a HOME with no GSD install exits 0 and flips the conformance test from failing to printing its 5/2/2-over-9 conformance line. Running it against this machine's real `$HOME/.claude/gsd-core` exits 0 and leaves that path byte-for-byte untouched (same inode, same mtime). No version literal appears in the script.</done>
</task>

<task type="auto">
  <name>Task 2: the container definition and `pre-tag-check.sh --container`</name>
  <files>scripts/pre-tag-check.Dockerfile, scripts/pre-tag-check.sh</files>
  <precondition>A container runtime is usable — `docker info` succeeds (verified reachable at planning time) — and the network can reach the ubuntu, nodesource, cli.github.com, git-core PPA, npm and crates.io endpoints the image build and the in-container gates need.</precondition>
  <action>
PART A — create `scripts/pre-tag-check.Dockerfile`, an ubuntu-latest lookalike. Comment it the way
this repo comments things: each line says WHY it is there and WHICH RUNNER FACT it mirrors. It must
reproduce exactly the definition already validated in `<measured_facts>`:

  - `FROM ubuntu:24.04`.
  - `software-properties-common curl ca-certificates gnupg` first, then `add-apt-repository -y
    ppa:git-core/ppa` and install `git` — this is what yields git 2.55.0. Comment that this line
    exists so the container's git matches
    `CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION` in `src/envelope/policy.rs` and the
    runner's ambient git, i.e. so the version-witness test is validated here the way CI validates it.
  - `build-essential pkg-config libssl-dev jq unzip xz-utils procps coreutils tar`.
  - Node 24 via the nodesource setup script, then `nodejs`.
  - `gh` from the `cli.github.com` apt repo with the keyring at
    `/usr/share/keyrings/githubcli-archive-keyring.gpg`, which yields gh 2.101.0. Comment LOUDLY
    that this line exists so the two `gh_grammar_probe` tests in `src/envelope/policy.rs` probe the
    version the runner actually carries — `ubuntu-latest` ships `gh` preinstalled, so an image
    without it manufactures two failures that are not runner failures and sends the next
    investigation down a false trail. It already did once.
  - rustup with `--default-toolchain stable --profile minimal --component clippy`, plus
    `rustup toolchain install 1.88.0 --profile minimal` so the container can also run gate 2, the
    MSRV gate. `RUSTUP_HOME=/usr/local/rustup`, `CARGO_HOME=/usr/local/cargo`, both `chmod a+w`,
    `PATH` includes `/usr/local/cargo/bin`. Comment that the 1.88.0 pin mirrors the
    `dtolnay/rust-toolchain@1.88.0` ref in release.yml's msrv job, and that if the manifest floor
    moves, both move.
  - The uid-1000 user: `RUN (userdel -r ubuntu 2>/dev/null || true) && useradd -m -u 1000 -s
    /bin/bash runner`, with a comment that `ubuntu:24.04` already ships uid 1000 as user `ubuntu`
    and that `useradd -u 1000` otherwise dies with "UID 1000 is not unique". Follow it with
    `chmod 0777 /home/runner` and a comment saying why: the run passes the INVOKING user's uid
    through so bind-mounted repo files keep their ownership, and that uid is not guaranteed to be
    1000 on every developer machine.
  - HOME must be deliberately free of `.claude`. Add an explicit comment stating that the image
    must NOT bake GSD in, because the whole point is that the oracle is genuinely absent until the
    publish job's own provisioning step puts it there — an image with GSD pre-installed would make
    the dry run green while telling you nothing about CI.

PART B — add `--container` to `scripts/pre-tag-check.sh`.

A BARE invocation must behave EXACTLY as today. The existing gate order and its inverted-relative-
to-CI justification, the git reconciliation banner, the EXIT-trap summary, the `set -u`-only choice
and its stated reasons, the `NOT REACHED` initial statuses — all stay, unmodified.

Thread the flag through the existing arg loop at lines 47-68: that loop rejects ANY `-*` argument as
an unknown option, so add a `--container` case BEFORE the `-*` catch-all, setting a `CONTAINER=1`
flag. `--container` composes with the optional TAG argument, so `--container v1.7.2` must work in
either order. Update `usage()` so `--help` stays accurate: document the flag, what it does, and the
loud-failure rule below.

PLACE THE CONTAINER DISPATCH after the `RULE`/`THIN` definitions (lines 70-71) and BEFORE the
`trap ... EXIT` installation at line 153, and comment that placement: the EXIT trap prints a
five-gate summary describing gates that never ran in THIS process, so a container dispatch must
never reach it. The inner run prints its own banner and its own summary, streamed through to the
operator's terminal, and that is the only summary a `--container` invocation should show.

The dispatch does, in order:

  1. RECURSION GUARD. Pick one env var name — use `PRE_TAG_CHECK_IN_CONTAINER` — and hard-error
     with exit 2 if `--container` is requested while it is already set. The inner command never
     passes `--container` inward, so this is belt-and-braces, but an accidental recursion in a
     script that builds images and runs full test suites is expensive enough to guard explicitly.
  2. RUNTIME RESOLUTION. Resolve `docker`, else `podman`, and confirm the daemon actually answers
     (`<runtime> info` exits 0) — a runtime binary whose daemon is unreachable is not a usable
     runtime. Honour an override env var `PRE_TAG_CONTAINER_RUNTIME` when set, and document in the
     header that it exists for two reasons: unusual rootless setups, and so the loud-failure path
     below is TESTABLE. If nothing is usable, FAIL LOUDLY and exit non-zero. The error text must say
     plainly that this run has certified NOTHING, that there is deliberately no fallback to a local
     run, and WHY: a silent fallback would recreate the exact blind spot this whole change exists
     to close — a green local gate on the one thing CI is red on.
  3. BUILD the image from `scripts/pre-tag-check.Dockerfile`, tagged `gsdmm-pre-tag-check:latest`,
     with `scripts/` as the build context (the Dockerfile copies nothing from the repo; the repo is
     bind-mounted at run time). Build every invocation — the layer cache makes an unchanged
     Dockerfile near-instant, and a stale image is a lie about what was tested.
  4. VOLUME BOOTSTRAP. Use two stable named volumes, `gsdmm-pretag-target` and
     `gsdmm-pretag-cargo-home`, so the cargo cache survives between runs. Named volumes are created
     root-owned, so before the unprivileged run, run the SAME freshly-built image once as root
     (`-u 0:0`) with both volumes mounted and `chown -R` them to the invoking uid:gid. Comment that
     the same image is reused deliberately rather than pulling a second one.
  5. RUN. `--rm`, `-u "$(id -u):$(id -g)"`, `--cpuset-cpus 0-3` (so `nproc` reports 4 like the
     runner — comment that), `-e HOME=/home/runner`, `-e CARGO_TARGET_DIR=/cargo-target`,
     `-e CARGO_HOME=/cargo-home`, `-e PRE_TAG_CHECK_IN_CONTAINER=1`, the repo bind-mounted at
     `/work` with `-w /work`, and the two named volumes at `/cargo-target` and `/cargo-home`.
     A DEDICATED `CARGO_TARGET_DIR` IS REQUIRED — comment that sharing the host `target/` would
     thrash the developer's build cache between two different environments. The inner command is
     `bash -c './scripts/install-conformance-oracle.sh && ./scripts/pre-tag-check.sh [TAG]'`,
     forwarding the TAG argument when one was given: the container run mirrors the publish job
     INCLUDING its provisioning step, which is what makes a green container run mean a green
     publish job. Propagate the inner exit status as this script's exit status.
     Note in a comment that the bind mount is read-write and that gates 3-5 can therefore touch
     `Cargo.lock` if it is stale — visible in `git status`, and arguably what you want to find out
     before tagging, since CI would fail on it too.

Do NOT reorder, reword or "tidy" anything outside the arg loop, `usage()`, and the new block.
  </action>
  <verify>
    <automated>bash -n scripts/pre-tag-check.sh &amp;&amp; ./scripts/pre-tag-check.sh --help | grep -F -- '--container' &amp;&amp; bash -c 'set -e; ./scripts/pre-tag-check.sh --bogus >/dev/null 2>&amp;1; [ $? -eq 2 ] || { echo "arg parser regressed: --bogus did not exit 2"; exit 1; }; ./scripts/pre-tag-check.sh v0.0.0-not-the-crate-version >/dev/null 2>&amp;1; [ $? -eq 1 ] || { echo "gate 1 hard stop regressed"; exit 1; }; PRE_TAG_CHECK_IN_CONTAINER=1 ./scripts/pre-tag-check.sh --container >/dev/null 2>&amp;1; [ $? -eq 2 ] || { echo "recursion guard did not fire"; exit 1; }; OUT=$(PRE_TAG_CONTAINER_RUNTIME=definitely-not-a-container-runtime ./scripts/pre-tag-check.sh --container 2>&amp;1); [ $? -ne 0 ] || { echo "missing runtime did not fail"; exit 1; }; printf "%s" "$OUT" | grep -qiF "fallback" || { echo "the no-runtime error does not state the no-fallback rule"; exit 1; }; echo "ok: bare behaviour preserved, guard and loud-failure both fire"'</automated>
    <automated>./scripts/pre-tag-check.sh --container "v$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"</automated>
    <human-check>The container run's own PRE-TAG SUMMARY shows gates 1-5 all PASS and reports a MATCH (not a MISMATCH) on the git reconciliation banner, and the gate-4 log contains the conformance line "conformance: 5 command comparisons".</human-check>
  </verify>
  <done>A bare invocation is indistinguishable from today's: `--bogus` still exits 2, a wrong tag still hard-stops at gate 1 with exit 1, `--help` is accurate. `--container` with no usable runtime exits non-zero saying there is no fallback; with the guard set it exits 2. A full `--container` run builds the image, provisions the oracle inside it, and exits 0 with all five gates PASS.</done>
  <timing_note>
The second automated check is the expensive one. The first image build is roughly 5-10 minutes (apt,
nodesource, two rust toolchains) and the first in-container gate run is a COLD cargo cache across
three profiles — release build, test build, clippy — plus ~2100 tests: budget up to 90 minutes.
Subsequent runs on the warm volumes are roughly 10 minutes. The Bash tool caps a single call at 10
minutes, so RUN THIS IN THE BACKGROUND (`run_in_background: true`) and poll; do not try to fit it in
one foreground call and do not shorten the run to fit a timeout.
  </timing_note>
</task>

<task type="auto">
  <name>Task 3: wire the publish job and CLAUDE.md release step 3</name>
  <files>.github/workflows/release.yml, CLAUDE.md</files>
  <action>
PART A — `.github/workflows/release.yml`, the `publish` job ONLY.

Do NOT create any new workflow file: a push/PR CI workflow was explicitly declined. Do NOT touch the
`msrv` job — it only runs `cargo check` and needs nothing from this change; it must come out of this
task byte-identical.

In `publish`, between the `Swatinem/rust-cache@v2` step and the `Check tag matches Cargo.toml
version` step, add:

  - `actions/setup-node@v4` with `node-version: 24`, matching the Node major the container carries.
  - A step named so it is obvious in the job log — "Provision the GSD conformance oracle" — running
    `./scripts/install-conformance-oracle.sh`. It MUST sit before the existing `Test` step. Add a
    short YAML comment above it saying what it is for: `tests/driver_router_conformance.rs` compares
    this crate's rule table against GSD's own router and fails by design when the oracle is absent,
    and an `ubuntu-latest` runner has Node but no GSD. The comment should say the point is to
    PROVIDE the dependency.

Do NOT set the conformance test's missing-oracle opt-out variable anywhere — not in `env:`, not in
a `run:`, and not in a comment. That variable makes the test vacuous, which is the fail-open shape
this codebase repeatedly documents as unacceptable; the whole point is to supply the dependency, not
to switch the check off. <!-- planner-discipline-allow: ALLOW_MISSING_ORACLE -->

Change the `Test` step from `cargo test` to `cargo test --no-fail-fast`, with a YAML comment giving
the justification: fail-fast stops at the first failing test BINARY and hides every later suite
behind it, which is precisely how this crate burned three tags discovering one environment-dependent
failure at a time. `--no-fail-fast` surfaces a future breakage as a complete list in one run. This is
also what makes the publish job and `scripts/pre-tag-check.sh` gate 4 finally agree — today the
script deliberately diverges from CI on this exact point (see its gate 4 comment). It is safe to
make now and not before: the exhaustive audit recorded in `<measured_facts>` already ran the whole
suite with `--no-fail-fast` in the lookalike container and the complete failure set is the three
explained entries, all of which this change resolves. Record this in the SUMMARY as an INFERRED
DECISION for audit — the task brief asked for a recommendation, and it is being implemented on the
strength of that measurement rather than deferred.

Leave `CARGO_REGISTRY_TOKEN` exactly where it is — step-scoped `env:` on the `Publish` step. Do not
hoist it to job or workflow level. The new steps run third-party code (an npm install), and keeping
the token out of their environment is the mitigation for T-r4c-SC below.

PART B — `CLAUDE.md`, Release Process step 3 (currently one paragraph at line 133).

Rewrite it so the container dry run is the instruction, not an aside. It must say: run
`./scripts/pre-tag-check.sh --container vX.Y.Z` before tagging; that mode builds
`scripts/pre-tag-check.Dockerfile`, provisions the conformance oracle with the same
`scripts/install-conformance-oracle.sh` the publish job calls, and re-runs the gates in that
environment, so a green container run certifies the publish job in a way a bare local run cannot.
Say plainly WHY a bare local run cannot: the developer machine has GSD installed and a different
ambient git, so the two tests that have blocked publication — the router-conformance oracle test and
the git-version witness — both pass locally on precisely the trees CI rejects. Keep the existing
git-reconciliation-advisory explanation, but reframe it as what the BARE mode tells you; in
container mode the git versions agree and the advisory should read MATCH. Say that `--container`
requires a usable container runtime and fails loudly rather than falling back. Keep the rest of the
Release Process steps untouched.
  </action>
  <verify>
    <automated>bash -c 'set -e; python3 -c "
import yaml
d = yaml.safe_load(open(\".github/workflows/release.yml\"))
jobs = list(d[\"jobs\"])
assert jobs == [\"msrv\", \"publish\"], jobs
steps = d[\"jobs\"][\"publish\"][\"steps\"]
names = [s.get(\"name\") or s.get(\"uses\") for s in steps]
oracle = next(i for i, n in enumerate(names) if n and \"oracle\" in n.lower())
node = next(i for i, n in enumerate(names) if n and \"setup-node\" in n)
test = names.index(\"Test\")
assert node < oracle < test, names
test_step = steps[test]
assert test_step[\"run\"].strip() == \"cargo test --no-fail-fast\", test_step
pub = steps[names.index(\"Publish\")]
assert \"CARGO_REGISTRY_TOKEN\" in pub.get(\"env\", {}), pub
assert \"env\" not in d[\"jobs\"][\"publish\"] or \"CARGO_REGISTRY_TOKEN\" not in d[\"jobs\"][\"publish\"][\"env\"]
print(\"workflow OK:\", names)
"; grep -q "ALLOW_MISSING_ORACLE" .github/workflows/release.yml && { echo "the fail-open opt-out leaked into the workflow"; exit 1; }; [ "$(ls .github/workflows | wc -l)" -eq 1 ] || { echo "a new workflow file was added"; exit 1; }; HEAD_YML=$(git show HEAD:.github/workflows/release.yml) || { echo "could not read release.yml at HEAD"; exit 1; }; rng() { awk "/^  msrv:/{f=1} /^  publish:/{f=0} f" ; }; diff <(printf "%s\n" "$HEAD_YML" | rng) <(rng < .github/workflows/release.yml) || { echo "the msrv job was modified"; exit 1; }; echo "ok: msrv job byte-identical, publish job wired, one workflow file"'</automated>
    <automated>grep -n -- '--container' CLAUDE.md &amp;&amp; grep -n 'install-conformance-oracle' CLAUDE.md &amp;&amp; grep -c 'pre-tag-check' CLAUDE.md</automated>
    <automated>bash -c 'set -e; CHANGED=$(git diff --stat -- src/ Cargo.toml Cargo.lock); if [ -n "$CHANGED" ]; then printf "%s\n" "$CHANGED"; echo "FORBIDDEN: src/, Cargo.toml or Cargo.lock changed"; exit 1; fi; echo "ok: no src/, Cargo.toml or Cargo.lock change"'</automated>
  </verify>
  <done>`release.yml` parses, still has exactly two jobs, its `msrv` job is byte-identical to HEAD, and its `publish` job runs setup-node then the oracle provisioning step then `cargo test --no-fail-fast`, with `CARGO_REGISTRY_TOKEN` still scoped to the `Publish` step alone. No new workflow file exists and the fail-open opt-out appears nowhere in the workflow. CLAUDE.md step 3 names `--container` and the provisioning script. `src/`, `Cargo.toml` and `Cargo.lock` are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> release publish job | Third-party JavaScript is downloaded and EXECUTED inside the job that holds the crates.io publishing token. |
| apt/nodesource/cli.github.com -> container image | Third-party packages executed inside a container that bind-mounts the developer's working tree read-write. |
| container -> host working tree | `/work` is a read-write bind mount of the repo, running as the invoking uid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r4c-SC | Tampering | `npm install -g @opengsd/gsd-core` in `.github/workflows/release.yml` publish job | high | mitigate | Install an EXACT version pinned from `GSD_CORE_SYNCED_VERSION` in `src/state_reader/config_json.rs` — never a range, never `@latest`. Keep `CARGO_REGISTRY_TOKEN` scoped to the `Publish` step's own `env:` so the install and probe steps never see it. The package is GSD itself, the tool this repo exists to manage and already a measured dependency of the test suite; it is not a newly introduced third party. |
| T-r4c-01 | Elevation of Privilege | `scripts/pre-tag-check.sh --container` volume chown bootstrap | medium | mitigate | The root-privileged container is the freshly built local image, runs only `chown -R` on two dedicated named volumes, and never mounts the repo. The gate run itself is `-u $(id -u):$(id -g)`, never root. |
| T-r4c-02 | Tampering | `/work` read-write bind mount | low | accept | The container runs as the invoking uid, so it can only write what the developer could already write, and any change (e.g. a refreshed `Cargo.lock`) surfaces in `git status`. Mounting read-only would break gates 3-5, which legitimately compile. |
| T-r4c-03 | Spoofing | `gh` apt repo keyring in `scripts/pre-tag-check.Dockerfile` | low | mitigate | Install from `cli.github.com` with the pinned keyring at `/usr/share/keyrings/githubcli-archive-keyring.gpg`, the same repo and key path GitHub's own runner image uses. |
| T-r4c-04 | Repudiation | conformance test fail-open | high | mitigate | The provisioning script exits non-zero when the oracle does not answer, and nothing in the repo sets the test's missing-oracle opt-out. A run that checked nothing can no longer report success. |
</threat_model>

<verification>
1. `bash -n` clean on both shell scripts; `scripts/install-conformance-oracle.sh` is mode 0755.
2. No GSD version literal in any new or edited file — the version is read from
   `src/state_reader/config_json.rs` at run time.
3. The conformance test flips from FAIL to a real 5/2/2-over-9 comparison in a HOME the new script
   provisioned, and the developer's own `$HOME/.claude/gsd-core` survives a run untouched.
4. A bare `./scripts/pre-tag-check.sh` is behaviourally identical to HEAD.
5. A full `--container` run exits 0 with gates 1-5 PASS and a MATCH on the git banner.
6. `release.yml` parses, has two jobs, an unmodified `msrv` job, and a publish job that provisions
   before testing with `--no-fail-fast`.
7. `git diff -- src/ Cargo.toml Cargo.lock` is empty.
</verification>

<success_criteria>
- Five files land: two new scripts (one of them a Dockerfile), one script extended, one workflow job
  wired, one CLAUDE.md step rewritten.
- The publish job would now pass on `ubuntu-latest`: the one genuine runner failure is provisioned
  away, and the two `gh` failures were never runner failures.
- The dry run can PROVE that claim locally, byte-identically to CI, or fail loudly saying it cannot.
- Nothing under `src/` changed, no test was ignored, no version was bumped, no tag was created, and
  no new workflow file exists.
</success_criteria>

<output>
Create `.planning/quick/260917-r4c-provision-the-conformance-oracle-on-the-/260917-r4c-SUMMARY.md` when done.
Record as INFERRED DECISIONS FOR AUDIT: (a) the publish job's `Test` step moved to
`--no-fail-fast`; (b) the don't-clobber rule chosen for an existing `$HOME/.claude/gsd-core`
(verify-only, never modify, non-zero if present-but-broken); (c) the `PRE_TAG_CONTAINER_RUNTIME`
override added so the no-runtime failure path is testable; (d) a version mismatch on a
pre-existing developer install is an advisory, never a failure.
</output>
