---
phase: quick-260509-zh2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state_reader/config_json.rs
  - src/state_reader/disk_status.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/detail.rs
autonomous: true
---

<objective>
1. Layer ~/.gsd/defaults.json under project config in the Defaults tab.
   Show '*' next to values that came from the global defaults.
2. Add a [d] shortcut to toggle the Defaults tab between editing the
   project's .planning/config.json and editing ~/.gsd/defaults.json.
3. Reorganise the Defaults tab into the six sections used by GSD's
   /gsd-settings command (Planning / Execution / Docs & Output /
   Features / Model & Pipeline / Misc).
4. Extend the Pipeline tab with sub-stage drill-down — detect each
   per-toggle artifact (RESEARCH, PATTERNS, PLAN-CHECK, VALIDATION,
   UI-SPEC, AI-SPEC, UI-CHECK, REVIEW, UI-REVIEW) and surface
   per-toggle status under the parent Plan / Execute stages.
</objective>

<context>
GSD's /gsd-settings groups workflow toggles into six sections that
double as a hint for which artifact each toggle gates on disk:

  Planning   → workflow.research / pattern_mapper / plan_check /
               nyquist_validation / ui_phase / ui_safety_gate /
               ai_integration_phase
  Execution  → workflow.verifier / tdd_mode / code_review /
               code_review_depth / ui_review

Existing GsdConfig is missing several keys that /gsd-settings exposes;
we need to extend it. The TUI must keep working even when
~/.gsd/defaults.json doesn't exist.

The CLAUDE.md state-reading constraint stands: no agent invocations,
files only.
</context>

<tasks>
  <task id="T1">Layer ~/.gsd/defaults.json beneath project config.
    - Add load_user_defaults() in state_reader/config_json.rs.
    - Extend GsdConfig with the missing /gsd-settings keys
      (pattern_mapper, ai_integration_phase, tdd_mode, code_review,
      code_review_depth, ui_review).
    - Extend ConfigEntry with `from_defaults: bool`.
    - When project value is None, use the defaults value if present
      and mark from_defaults=true.
    - Render '*' marker next to default-sourced values.
  </task>
  <task id="T2">Reorganise build_defaults_entries into six sections.
    Match /gsd-settings: Planning / Execution / Docs & Output /
    Features / Model & Pipeline / Misc. Wire set_config_value,
    set_string_value, clear_config_value, mutate_config_entry for the
    new fields.
  </task>
  <task id="T3">[d] toggle to edit defaults.
    - DefaultsEditTarget { Project, Global } cache field.
    - 'd' key swaps the displayed/edited config and reloads.
    - Title bar shows whichever target is active.
    - All edits/clears apply to whichever file is active.
    - Save respects target file.
  </task>
  <task id="T4">Pipeline sub-stage drill-down.
    - Extend DiskInference with has_patterns, has_plan_check,
      has_validation, has_ui_spec, has_ui_check, has_ai_spec,
      has_review, has_ui_review.
    - Detect each artifact in infer_disk_status.
    - In render_pipeline_tab, after the parent stage line, render
      indented sub-stage lines for Plan and Execute showing
      Done/Current/Skipped using the same StageStatus model.
  </task>
  <task id="T5">cargo build / clippy / test all clean.</task>
</tasks>
