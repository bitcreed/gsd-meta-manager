---
phase: quick-260926-gd5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/GETTING-STARTED.md
  - docs/CONFIGURATION.md
autonomous: true
requirements: [QUICK-260926-gd5]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README's dashboard key table names `c` (not `n`) as Create new project, matching the dashboard handler at src/ui/screens/normal.rs:525"
    - "Every dashboard key the handler binds without the experimental flag (normal.rs:508-715) and the global Ctrl+C (src/app.rs:2387) has a README row: j/Down, k/Up, Enter, b, /, s, a, c, d, Tab, ?, M, q, Ctrl+C"
    - "README's detail-view navigation rows match src/ui/screens/detail.rs:2976-3015 and 3820-3875: tab bar descends on Down/j/Enter/Space, Up/k climbs from the first row, arrows stop at the first/last tab, Phases Right focuses the Waves pane, Backlog Left closes an open pane"
    - "README has one Tab-specific keys table covering Roadmap, Phases, Waves pane, Backlog, Git, Queue, Sessions, Sessions > Agents, Config, Docs > Files, Docs > Milestones and the generic `e`, each row traceable to a detail.rs line cited in this plan"
    - "docs/GETTING-STARTED.md describes the `a` dialog as alias-then-path, lists `c`, mentions sub-tab arrows, and no longer claims README documents a different config format"
    - "docs/CONFIGURATION.md states the TUI has eight tabs with the experimental flag off (visible_tab_count, detail.rs:437-449)"
    - "No file under src/, tests/, Cargo.toml or Cargo.lock changes (doc-only)"
    - "Help-overlay and footer disagreements with the handlers (F1-F8 below) are recorded in the SUMMARY as findings, not fixed"
  artifacts:
    - path: "README.md"
      provides: "Corrected dashboard key table, corrected detail-view navigation table, new Tab-specific keys table, corrected Quick start step 3"
      contains: "Create a new project"
    - path: "docs/GETTING-STARTED.md"
      provides: "Corrected first-keystrokes list, alias-then-path registration, stale config-format callout replaced"
      contains: "The config file is JSON"
    - path: "docs/CONFIGURATION.md"
      provides: "Correct tab count in the GSDMM_EXPERIMENTAL_FEATURES section"
      contains: "the TUI has **eight** tabs"
  key_links:
    - from: "README.md Key bindings tables"
      to: "src/ui/screens/normal.rs NormalScreen::handle_key and src/ui/screens/detail.rs DetailScreen::handle_key"
      via: "every row cites a handler arm surveyed below"
      pattern: "Create a new project"
    - from: "docs/GETTING-STARTED.md Next steps"
      to: "README.md Key bindings"
      via: "'the full key-binding table' pointer (GETTING-STARTED.md:259-260) stays true"
      pattern: "full key-binding table"
---

<objective>
Make the user docs' key tables match the keymap the code actually binds. Source code key handlers are the
source of truth; this is a doc-only change and no code behaviour changes.

Purpose: README's dashboard table tells users `n` creates a project. It does nothing on the dashboard; the key
is `c` (src/ui/screens/normal.rs:525). The README tables also omit several bound keys (`s`, `b`, `Tab`,
`Ctrl+C`, every per-tab key) and misstate a few navigation details. GETTING-STARTED has the `a` flow backwards
(it asks for an alias first), and CONFIGURATION miscounts the tabs.

Output: README.md, docs/GETTING-STARTED.md and docs/CONFIGURATION.md corrected. The SUMMARY records every place
where the help overlay or a footer hint disagrees with the handlers (F1-F8 below), as findings only.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@README.md
@docs/GETTING-STARTED.md

The keymap survey was done during planning. The citations below are the verified source of truth, so the
executor does NOT need to re-survey detail.rs (21,971 lines). Re-open a cited line only if an edit depends on
a detail this plan does not state.

## Verified keymap (file:line)

Global: `Ctrl+C` quits from any screen (src/app.rs:2387-2390). No other key is global.

Dashboard, NormalScreen::handle_key (src/ui/screens/normal.rs):
- `q` quit :508 | `j`/Down :509 | `k`/Up :514
- `a` :519 pushes AddProjectScreen::new_alias(). The dialog asks for an ALIAS first (required, no whitespace,
  must be unique), then the PATH (add_project.rs:24-27, 111-136, 165-168). `Esc` cancels (:137, :170).
- `c` :525 pushes CreateProjectScreen. It asks for a name, then a path (`Tab` completes, create_project.rs:161),
  then confirmation with `y`/`Enter` or `n`/`Esc` (create_project.rs:210, 245).
- `d` :531 pushes DeleteConfirmScreen. `y` confirms, `n`/`Esc` cancels (delete_confirm.rs:41-45). The prompt
  says it "only unregisters it — project files are not deleted" (delete_confirm.rs:94).
- `r`/`x`/`o` :560/:574/:588 are bound only when `ctx.experimental` is on (driver start/stop/opt-in).
- `s` :618 toggles sort between alphabetical and attention-first. `M` :635 toggles mouse capture.
  `Enter` :636 opens the detail view. `b` :667 opens the detail view on the Backlog tab.
- `/` :680 enters the filter. While filtering, `Enter` keeps the filter (:823) and `Esc` clears it (:811).
- `?` :688 opens help. The overlay closes on `?`/`Esc` and scrolls with `j`/`k`/`PgUp`/`PgDn`
  (help.rs:409-447).
- `Tab` :692 switches the terminal to the selected project's session.
- Any other key, including `n` and `Esc`, does nothing (:715).

Detail view, DetailScreen::handle_key (src/ui/screens/detail.rs):
- `M` toggles mouse capture at every level, after the Config text intercepts (:2968).
- TAB BAR level (:2976-3015):
  - Left/Right step tabs and stop at the first/last tab, with no wrap (stepped_tab_index :1889-1895).
  - Down, `j`, `Enter` and `Space` all descend without taking an action (:2992).
  - Up and `k` do nothing (:2997). `Esc` and `q` go back to the dashboard (:2998).
  - `?` and `Tab` act and leave the tab bar focused (:3005).
  - Any other key drops focus into content and then runs there (:3010).
- CONTENT level:
  - `q` leaves the detail view from any level (:3035).
  - `Esc` closes one inner level first (:3040-3165): an Archive depth (:3042), an open Backlog pane (:3089),
    the Git commit pane (:3099), a Browse file or subdirectory (:3109), a Config dropdown and then a Config
    filter (:3142-3159). With nothing left to close, it goes to the tab bar (:3163).
  - `k`/Up on the first row climbs to the tab bar (:3344-3351).
  - Digits `1`-`8` jump into that tab's content (:3820). Sessions and Docs reopen the sub-tab last used there
    (switch_to_tab :2663-2676, switch_to_sub_view :2694-2704).
  - `Shift+D` is bound only in experimental mode (:3835).
  - Left/Right (:3844-3875) are handled in this order:
    1. With a Backlog pane open, Left closes it and Right does nothing (:3846-3857).
    2. On Sessions or Docs, the arrows step the sub-tab and stop at the ends (:3859, step_sub_tab :1165-1179).
    3. On Phases, Right focuses the Waves pane (:3864).
    4. Otherwise the arrows switch tab and land focus on the tab bar (:3867-3874).
  - `Enter`/`Space` (:3877-4347) do different things per tab:
    - Phases: focus the Waves pane (:3881).
    - Queue: mark the item done, which removes it (:3882-3921).
    - Backlog: `Enter` opens or closes the item's pane; `Space` does nothing (:3922-3965).
    - Git: `Enter` loads the commit's message and diff stat (:3966-4010).
    - Agents: jump to the agent's plan (:4016).
    - Sessions: resume the selected session (:4017).
    - Archive: descend milestone, then phase, then file (:4141).
    - Config: edit the value, or apply the choice in an open dropdown (:4250).
    - Browse: `Enter` opens a directory or file; `Space` does nothing (:4314-4343).
    - Roadmap list: see roadmap_activate below (:4344).
  - Browse only: `g` jumps to the `.planning/` root (:4349). `p` jumps back to the entry (active) phase
    directory (:4364).
  - `[`/`]` step the sub-tab on Sessions and Docs (:4384-4389). `m` is an alias for switching the sub-tab on
    Docs (:4398) and on Sessions (:4417).
  - Config: `/` filters (:4429), `x` clears the value (:4440), `r` reloads (:4488), and `d` switches between the
    project config and ~/.gsd/defaults.json (:4549).
  - `Tab` (:4510) switches to the highlighted session on the Sessions tab, and to the project's first session on
    any other tab.
  - Sessions: `n` launches a new Claude session (:4581).
  - Git: `p` toggles planning-only commits (:4638).
  - Queue: `a` adds an item (:4675), `d`/`x` delete with confirmation (:4696), and `J`/`K` move the item down/up
    (:4716, :4743).
  - Driver-only keys `f`/`G`/`i`/`s`/`x`/`o` (:4770-4906) exist only in experimental mode.
- `e` (:4907-5089) depends on where it is pressed:
  - Archive file view: refuses every file under `/milestones/` with "Archived files are read-only"
    (:4936-4942). Every Milestones file is under `.planning/milestones/` (archive.rs:119, 161-190), so on
    Milestones `e` always refuses.
  - Browse: opens the file in $EDITOR (:4952-4962).
  - Queue: pre-fills the editor and removes the old item. `Esc` in the editor leaves the item removed; the status
    line says "Esc cancels and removes" (:4979-5020, enqueue.rs:79-83).
  - Backlog: with the pane open, edits the item where it lives; with it closed, enqueues it (:5024-5073).
  - Anywhere else: enqueues the suggested next command (:5075-5088).
- Roadmap list view only (roadmap_list):
  - `v` switches between the graph list and the box view (:5093).
  - `g`/`G` jump to top/bottom (:5106-5107). `PgUp`/`PgDn` page (:3647, :3795).
  - `h`/`l` jump along dependency edges and cycle when repeated (:5108-5113, RoadmapNav::Edge :1908).
  - `[`/`]` move to the previous/next phase in the same wave (:5114-5115).
  - `Space` folds the milestone (roadmap_activate :1403-1430).
  - `Enter` (roadmap_activate :1398-1480) opens the phase in Phases. On a named milestone row it folds that row.
    On the shipped-milestones summary row it opens Docs > Milestones. On a planned-placeholder or shipped phase
    it only shows a status message.
- WAVES PANE (handle_waves_pane_key :1758-1797):
  - Left/`Esc` go back to the phase list (:1764). `q` leaves the detail view (:1769).
  - `j`/`k`/Down/Up/`PgUp`/`PgDn`/`g`/`G` move the cursor (:1775-1784).
  - `Enter`/`Space` fold a wave or jump to the plan's agent (:1785, waves_activate :1544-1620).
  - `e` edits PLAN.md at its objective (:1786, waves_edit :1700-1720).
  - Right does nothing (:1788). `?` and `Tab` pass through (:1789).
  - A digit, `D`, `[` or `]` leaves the pane and then acts (:1790-1794).
- Tab count: TAB_COUNT = 9 with Driver last. Eight tabs are visible with the flag off (:437-449, labels
  :460-477).

## Help-overlay / footer vs handler disagreements (FINDINGS — record in SUMMARY, do NOT fix code)

- F1: help.rs:248 row `q / Esc` reads "Quit / Back" at the dashboard level. On the dashboard `Esc` is unbound
  outside the `/` filter (normal.rs:715, 811).
- F2: the dashboard block of help.rs (:202-217) omits `Tab` (normal.rs:692), although the dashboard footer shows
  `[Tab]session` (normal.rs:1225). Help lists `Tab` only as a detail-view key (help.rs:268).
- F3: several bound keys appear only in footer hints (detail.rs:9157-9249) and never in the help overlay:
  - Sessions `n` (detail.rs:4581)
  - Queue `a`/`e`/`d`/`x`/`Enter`/`Space`/`J`/`K` (:4675-4770, :3882)
  - Git `Enter`/`p` (:3966, :4638)
  - Config `Enter`/`x`/`d`/`r`/`/` (:4250, :4440, :4549, :4488, :4429)
  - Docs Files `g`/`p`/`e` (:4349, :4364, :4952)
  - Roadmap `PgUp`/`PgDn` (:3647, :3795)

  This contradicts help.rs:1-2 ("the only place keys are documented").
- F4: help.rs:228 documents `e` as "Enqueue next action (detail view)". On Queue, Docs > Files and
  Docs > Milestones, `e` edits or opens instead (detail.rs:4907-4979). Help documents only the Backlog and
  Waves-pane exceptions.
- F5: help omits some key aliases the handlers accept:
  - The tab bar also descends on `j`/`Space` (detail.rs:2992).
  - `k` climbs to the tab bar (:3344).
  - `Space` also focuses the Waves pane (:3877/:3881).
  - `Tab` on the Sessions tab targets the highlighted session (:4516).
- F6: the Waves-pane footer shows `[q]uit` (detail.rs:9272-9273), but `q` there returns to the dashboard
  (detail.rs:1769-1772) rather than quitting the app.
- F7: the Docs > Milestones footer shows `[e]dit` (detail.rs:9207-9208), but `e` refuses every archived file
  (detail.rs:4936-4942, archive.rs:161-190).
- F8: help.rs:216 "Delete project" and the dashboard footer `[d]el` (normal.rs:1233) are misleading. The key only
  unregisters the project (delete_confirm.rs:94).

## Test coverage of these docs

No test reads README.md or any docs/*.md file. During planning, a grep of src/ and tests/ for README,
GETTING-STARTED, CONFIGURATION and include_str! of a .md file found only tests/fixtures READMEs
(tests/driver_rate_limit.rs:77-79, the src/state_reader fixtures) and the envelope tests' synthetic
"README.md" paths. CI (.github/workflows/release.yml) has no markdown check. `cargo test` therefore cannot
observe this change, so it is NOT run. The doc-only guard in <verification> replaces it. If an executor runs
the suite anyway, the one expected failure is the git-version witness in src/envelope/policy.rs.

## Inferred decisions (human unavailable — audit these)

- [inferred] README is the home for the per-tab keys, because GETTING-STARTED.md:259-260 calls README "the full
  key-binding table". The per-tab keys therefore go in one new README table rather than a new doc.
- [inferred] The Phases, Waves-pane and Sessions > Agents rows move out of the navigation table into the new
  Tab-specific table. Their wording is preserved, with a Tab column added. The navigation table keeps only
  level and tab movement.
- [inferred] Experimental-only keys (dashboard `r`/`x`/`o`, detail `Shift+D`, the Driver-tab keys) are NOT added
  as table rows. The help overlay hides them with the flag off (help.rs:193-199, 221-225, 308), and README
  gets one sentence pointing to docs/CONFIGURATION.md instead.
- [inferred] The stale GETTING-STARTED.md:112-113 blockquote is fixed here even though it is not a key row. It
  sits in a file this plan already edits, and it misdirects readers about README.
- [inferred] The CONFIGURATION.md tab count is fixed because it describes the tab surface the digit keys walk.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: README dashboard key table and Quick start `a` flow (the known `n` -> `c` bug)</name>
  <files>README.md</files>
  <action>
Rewrite the dashboard table under "### Key bindings" (README.md:183-193). It stays a two-column `| Key | Action |`
table in this row order, with these exact key cells:
1. `j` / `↓`: Move the selection down (normal.rs:509).
2. `k` / `↑`: Move the selection up (:514).
3. `Enter`: Open project detail (:636).
4. `b`: Open project detail on the Backlog tab (:667).
5. `/`: Filter projects. Type to filter, `Enter` keeps the filter, `Esc` clears it, and `?` shows the filter
   syntax (:680, :811, :823).
6. `s`: Toggle sort, alphabetical / attention first (:618).
7. `a`: Add an existing project. Type an alias, `Enter`, then its path, `Enter`; `Esc` cancels (add_project.rs).
8. `c`: Create a new project. Enter a name, then a path (`Tab` completes it), then confirm with `y` / `Enter`
   (:525, create_project.rs). The action text MUST begin with the words "Create a new project".
9. `d`: Remove the selected project from the registry after a `y` / `n` confirm. The action text MUST include the
   phrase "project files are not deleted" (delete_confirm.rs:94).
10. `Tab`: Switch the terminal to the selected project's Claude / Codex session (:692).
11. `?`: Show / hide the help overlay. `j` / `k` and `PgUp` / `PgDn` scroll it, and `Esc` also closes it
    (help.rs:409-447).
12. `M`: Toggle mouse capture (:635).
13. `q`: Keep the existing text, "Quit (dashboard); back to the dashboard (detail view)".
14. `Ctrl+C`: Force quit from any screen (app.rs:2387).

The old row that bound the letter n to project creation is DELETED. No dashboard row uses that key, because the
dashboard does not bind it (normal.rs:715).

Directly under the table, add one sentence. It says that with `GSDMM_EXPERIMENTAL_FEATURES` set, the dashboard's
`r` / `x` / `o` start a driver run, stop it and toggle opt-in, and `Shift+D` opens the experimental Driver tab.
Link it to [Configuration](docs/CONFIGURATION.md). This follows the [inferred] experimental-keys decision.

In Quick start step 3 (README.md:144-145), replace "pressing `a` and entering the path to any directory" with
wording that keeps the step's structure and CLI example:
- press `a`, type an alias for the project and press `Enter`,
- then enter the path to any directory that contains a `.planning/` folder.

Keep the `<!-- generated-by: gsd-doc-writer -->` header. Do not touch any other README section in this task.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-meta-manager && test "$(rtk proxy grep -cE '^\| *`c` *\| *Create a new project' README.md)" -eq 1 && test "$(rtk proxy grep -cE '^\| *`n` *\|' README.md)" -eq 0 && for k in '`b`' '`s`' '`Tab`' '`M`' '`Ctrl\+C`' '`a`' '`d`'; do rtk proxy grep -qE "^\| *$k" README.md || { echo "missing row $k"; exit 1; }; done && rtk proxy grep -qF 'project files are not deleted' README.md && rtk proxy grep -A3 'press `a`' README.md | rtk proxy grep -qi alias && rtk proxy grep -qF 'GSDMM_EXPERIMENTAL_FEATURES' README.md && git diff --quiet ab92327 -- src tests Cargo.toml Cargo.lock && echo OK</automated>
  </verify>
  <done>README's dashboard table has exactly one `c` row reading "Create a new project…" and no row whose key cell
is `n`. Rows exist for b, s, Tab, M, Ctrl+C, a and d, and the d row says project files are not deleted. Quick
start step 3 describes alias-then-path. The experimental-keys note is present. src/ and tests/ are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: README detail-view navigation table and new Tab-specific keys table</name>
  <files>README.md</files>
  <action>
Edit the detail-view table (README.md:200-216). Keep its `| Key (detail view) | Action |` header, the two
focus-level paragraphs above it, and the "`6` and `8` re-open…" sentence below it.

Correct these rows:
- The Left/Right row keeps the key cell `←` `→`. Its action becomes:
  - Tab bar: previous / next tab. The text MUST contain the phrase "stops at the first / last tab"
    (detail.rs:1889-1895).
  - Inside Sessions or Docs: previous / next sub-tab (Sessions / Agents, Files / Milestones), stopping at the
    ends (:3859, :1165-1179).
  - On the Phases tab, `→` focuses the Waves pane (:3864). With a Backlog item open, `←` closes it (:3846-3857).
  - Inside any other tab: switch tab and return to the tab bar (:3867).
- The Down/Enter row's key cell becomes exactly `↓` / `j` / `Enter` / `Space`. Its action stays "Tab bar: enter
  the tab's content (no action is taken)" (:2992).
- The Up row's key cell becomes `↑` / `k`. Its action stays "From the first row of the content: back to the tab
  bar" (:3344-3351).
- The `[` `]` row: change "on the Roadmap" to "on the Roadmap list" (:5114, list view only).
- The `Tab` row: append "(on the Sessions tab, the highlighted session)" (:4516).
- Keep the `1`-`8`, `Esc`, `q` and `m` rows as they are. They match the handlers (:3820, :3040-3165, :3035,
  :4398/:4417).

Remove the Phases, Waves-pane and Sessions > Agents rows (README.md:211-216) from this table. Their content
moves to the new table below, per the [inferred] decision.

After the "`6` and `8` re-open…" sentence and BEFORE "#### Mouse", add a paragraph "Tab-specific keys:" and a
three-column table `| Tab | Key | Action |`. The Tab column comes first, so no row's first cell is a key. Include
these rows in this order. The action text is guidance; keep each row short.
- Roadmap:
  - `j` `k` `g` `G` `PgUp` `PgDn`: on the list view, move the phase cursor, jump to top / bottom, or page.
  - `h` / `l`: jump to a dependency / a phase it unblocks; repeat to cycle.
  - `[` / `]`: previous / next phase in the same wave.
  - `Space`: fold / unfold the milestone.
  - `Enter`: open the phase in Phases. On a milestone row, fold / unfold it. On the shipped-milestones row, open
    Docs › Milestones.
  - `v`: switch between the graph list and the box view.
- Phases:
  - `→` / `Enter` / `Space`: focus the Waves pane (on top of the right side, under the ladder).
- Waves pane:
  - `j` `k` `g` `G` `PgUp` `PgDn`: move the row cursor, top / bottom, page.
  - `Enter` / `Space`: fold / unfold a wave (or a merged `w1–w10 ✓` row). On a plan, jump to its agent in
    Sessions › Agents.
  - `e`: edit the plan's PLAN.md in `$EDITOR` at its objective.
  - `←` / `Esc`: back to the phase list. `q` still leaves the detail view. A digit, `[` or `]` leaves the pane and
    acts as usual.
- Backlog:
  - `Enter`: open / close the item's content pane.
  - `j` `k` `PgUp` `PgDn`: scroll the open content pane.
  - `e`: with the pane open, edit the item where it lives (e.g. its ROADMAP.md section) in `$EDITOR`. Otherwise,
    enqueue it.
  - `←` / `Esc`: close the content pane.
- Git:
  - `Enter`: show the selected commit's message and diff stat.
  - `p`: toggle planning-only commits.
  - `PgUp` / `PgDn`: scroll by a page.
  - `Esc`: close the commit pane.
- Queue:
  - `a`: add an item.
  - `e`: edit the selected item. The item is removed while you edit, so `Esc` leaves it removed (:4979-5020).
  - `d` / `x`: delete the selected item (asks first).
  - `Enter` / `Space`: mark the selected item done, which removes it from the queue.
  - `J` / `K`: move the selected item down / up.
- Sessions:
  - `Enter`: resume the selected Claude session. Codex sessions cannot be resumed.
  - `n`: launch a new Claude session in this project.
- Sessions › Agents:
  - `Enter`: jump to the agent's plan in the Phases Waves pane (navigation only; nothing is sent to the agent).
- Config:
  - `Enter`: edit the selected value. In an open dropdown, apply the choice.
  - `x`: clear (unset) the selected value.
  - `d`: switch between the project's config and `~/.gsd/defaults.json`.
  - `r`: reload the config from disk.
  - `/`: filter the rows. `Esc` clears the filter.
- Docs › Files:
  - `Enter`: open the selected directory or file.
  - `Esc`: close the file, then go up one directory.
  - `g` / `p`: jump to the `.planning/` root / back to the active phase's directory.
  - `e`: open the selected file in `$EDITOR`.
- Docs › Milestones:
  - `Enter`: open the milestone, phase or file.
  - `Esc`: back up one level.
  - `e`: archived files are read-only, so this only shows a status message.
- Any other tab:
  - `e`: enqueue the suggested next GSD command.

Use the same `›` separator README already uses (README.md:38, 216). Leave the Mouse subsection unchanged. It was
verified against normal.rs:749-783 and detail.rs:5235-5297 during planning.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-meta-manager && rtk proxy grep -qF '`↓` / `j` / `Enter` / `Space`' README.md && rtk proxy grep -qF 'stops at the first / last tab' README.md && rtk proxy grep -qE '^\| *`↑` */ *`k`' README.md && for r in 'Roadmap *\| *`v`' 'Roadmap *\| *`h` / `l`' 'Phases *\| *`→` / `Enter` / `Space`' 'Waves pane *\| *`e`' 'Backlog *\| *`Enter`' 'Git *\| *`p`' 'Queue *\| *`J` / `K`' 'Sessions *\| *`n`' 'Sessions *\| *`Enter`' 'Sessions › Agents *\| *`Enter`' 'Config *\| *`/`' 'Config *\| *`d`' 'Docs › Files *\| *`g` / `p`' 'Docs › Milestones *\| *`e`' 'Any other tab *\| *`e`'; do rtk proxy grep -qE "^\| *$r" README.md || { echo "missing: $r"; exit 1; }; done && test "$(rtk proxy grep -cE '^#### Mouse' README.md)" -eq 1 && git diff --quiet ab92327 -- src tests Cargo.toml Cargo.lock && echo OK</automated>
  </verify>
  <done>The navigation table's rows match detail.rs's tab-bar, content and arrow handling: j/Space descend, k
climbs, arrows stop at the ends, and the Phases and Backlog arrow exceptions are stated. A Tab-specific keys table
covers every tab and level listed, with the Tab column first. The Mouse subsection is intact. src/ and tests/ are
unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: GETTING-STARTED and CONFIGURATION fixes, doc-only guard, findings into SUMMARY</name>
  <files>docs/GETTING-STARTED.md, docs/CONFIGURATION.md</files>
  <action>
In docs/GETTING-STARTED.md:
(a) In "From the TUI" (line 87-88), replace "press `a`, then enter the absolute path to any directory that
contains a `.planning/` subfolder". The new text says: press `a`, type an alias (no spaces) and press `Enter`,
then enter the absolute path to a directory that contains a `.planning/` subfolder and press `Enter`; `Esc`
cancels (add_project.rs:111-176).

(b) Delete the two-line blockquote at lines 112-113. It is a stale claim about README and the config format;
README.md:259-265 already says `config.json`. Replace it with the single sentence "The config file is JSON; see
[CONFIGURATION.md](CONFIGURATION.md) for the schema." This follows the [inferred] decision.

(c) In the "Useful first keystrokes" list (lines 140-157):
- The `j` / `k` bullet becomes "`j` / `k` (or `↓` / `↑`) — move the selection down / up" (normal.rs:509-514).
- In the `1`-`8` bullet, add that inside the Sessions and Docs tabs `←` / `→` switch sub-tabs (Sessions / Agents,
  Files / Milestones) instead of tabs (detail.rs:3859).
- After the `/` bullet, add "`c` — create a new project (name, path, confirm)" (normal.rs:525).
- Leave every other bullet unchanged. They match the handlers.

(d) In "Reading the Waves pane" (line 172), change "`Enter` on a header or merged row" to "`Enter` or `Space` on a
header or merged row" (detail.rs:1785).

In docs/CONFIGURATION.md line 115, replace the bolded tab count in "with the variable unset the TUI has … tabs"
with **eight**, so the sentence reads "the TUI has **eight** tabs". Leave the rest of that paragraph unchanged:
`Shift+D`, the `D` hint and the `r`/`x`/`o` keys were verified at detail.rs:3835, 9151 and normal.rs:560-598.

Do not edit docs/ARCHITECTURE.md, DEVELOPMENT.md, TESTING.md, GSD-CORE-SYNC.md or CONTRIBUTING.md. Planning
found no key rows in them, only a file listing ("help.rs Key-binding help overlay") that is accurate.

When writing the SUMMARY, copy the "Help-overlay / footer vs handler disagreements" list (F1-F8) from this plan's
context verbatim under a "Findings (not fixed — code)" heading, and list the [inferred] decisions under
"Inferred decisions".
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-meta-manager && rtk proxy grep -A4 'press `a`' docs/GETTING-STARTED.md | rtk proxy grep -qi alias && rtk proxy grep -qF 'The config file is JSON' docs/GETTING-STARTED.md && ! rtk proxy grep -qF 'Despite what' docs/GETTING-STARTED.md && rtk proxy grep -qE '^- `c` ' docs/GETTING-STARTED.md && rtk proxy grep -qi 'sub-tab' docs/GETTING-STARTED.md && rtk proxy grep -qF '`Enter` or `Space` on a' docs/GETTING-STARTED.md && rtk proxy grep -qF 'the TUI has **eight** tabs' docs/CONFIGURATION.md && git diff --quiet ab92327 -- src tests Cargo.toml Cargo.lock && CH=$(git diff --name-only ab92327 -- . ':!.planning') && test -z "$(printf '%s\n' "$CH" | rtk proxy grep -vxE 'README.md|docs/GETTING-STARTED.md|docs/CONFIGURATION.md')" && echo OK</automated>
  </verify>
  <done>GETTING-STARTED describes alias-then-path, lists `c`, mentions sub-tab arrows and Enter-or-Space on Waves
rows. The stale config-format callout is replaced. CONFIGURATION says eight tabs. The only tracked files changed
are README.md, docs/GETTING-STARTED.md and docs/CONFIGURATION.md. The SUMMARY carries F1-F8 and the inferred
decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> user | Users act on what the key tables say. A wrong row can lead a user into an unintended destructive action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gd5-01 | Tampering | README Queue rows (`Enter`/`Space`, `e`) | low | mitigate | Task 2 states that `Enter`/`Space` removes the item and that `Esc` during `e` leaves it removed (detail.rs:3882-3921, 4979-5020), so no keypress silently loses queue data |
| T-gd5-02 | Repudiation | README dashboard `d` row | low | mitigate | Task 1 states `d` only unregisters after a y/n confirm and that project files are not deleted (delete_confirm.rs:41-45, 94) |
| T-gd5-03 | Information disclosure | Experimental keys documented while hidden | low | mitigate | Per the [inferred] decision, experimental keys get one gated note pointing to CONFIGURATION.md, mirroring help.rs:193-199, rather than rows that do nothing on a default install |
| T-gd5-04 | Tampering | src/ accidentally edited | low | mitigate | Every task's verify runs `git diff --quiet ab92327 -- src tests Cargo.toml Cargo.lock`, pinned to the base so earlier task commits are still covered; Task 3 also asserts that the changed-file set since ab92327, excluding .planning, is exactly the three docs |
</threat_model>

<verification>
- All three task verify commands print OK.
- Doc-only guard: the diffs are pinned to the planning-time base `ab92327`, so they cover committed AND uncommitted
  work. `git diff --quiet ab92327 -- src tests Cargo.toml Cargo.lock` exits 0, and
  `git diff --name-only ab92327 -- . ':!.planning'` lists only README.md, docs/GETTING-STARTED.md and
  docs/CONFIGURATION.md.
- Spot re-check of the headline bug. Run `rtk proxy grep -n "KeyCode::Char('c')" src/ui/screens/normal.rs`; it
  must show line 525 pushing CreateProjectScreen, and README's `c` row must say Create a new project.
- `cargo test` is intentionally NOT run. No test reads these docs (see "Test coverage of these docs"). Use
  `rtk proxy` for every grep gate, because plain grep output is rewritten by the rtk hook.
</verification>

<success_criteria>
- The `n` -> `c` bug is fixed, and every non-experimental dashboard key plus Ctrl+C has a README row.
- The README detail-view navigation and Tab-specific tables match detail.rs, row for row, with the citations
  above.
- GETTING-STARTED and CONFIGURATION contain no key or tab statement that the handlers contradict.
- F1-F8 are recorded in the SUMMARY as code findings; no code is changed.
</success_criteria>

<output>
Create `.planning/quick/260926-gd5-fix-readme-getting-started-key-tables-ag/260926-gd5-SUMMARY.md` when done.
</output>
