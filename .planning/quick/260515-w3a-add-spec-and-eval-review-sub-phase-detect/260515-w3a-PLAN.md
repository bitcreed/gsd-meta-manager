---
phase: quick-260515-w3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state_reader/disk_status.rs
  - src/ui/screens/detail.rs
autonomous: true
---

# Detect SPEC.md and EVAL-REVIEW.md sub-phase artifacts

Mirror the SECURITY/UAT pattern.

- `SPEC.md` (from `/gsd:spec-phase`) — Plan sub-stage, first row (before Patterns)
- `EVAL-REVIEW.md` (from `/gsd:eval-review`) — Execute sub-stage, after UI Review, before UAT

## Changes

1. `disk_status.rs`: add `has_spec` and `has_eval_review` fields, detection branches, return-literal entries, and 4 new tests (2 per artifact).
2. `detail.rs`: include both flags in their group's `*_touched` predicate; add Spec row at top of Plan sub-stages; add Eval Review row in Execute sub-stages before UAT.
