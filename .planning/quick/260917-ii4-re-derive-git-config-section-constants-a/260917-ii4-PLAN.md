---
quick_id: 260917-ii4
phase: quick-260917-ii4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/envelope/policy.rs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: `CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION` reads `\"git version 2.55.0\"`, and BOTH arrays are byte-identical to what they were — `INDIRECTION_SECTIONS` stays `&[\"include\", \"includeIf\"]`, `REPARSED_COMMAND_SECTIONS` stays `&[\"alias\"]`."
    - "D-01: the version assertion is still LIVE and still FIRES. Run locally it fails, and its own message names `\"git version 2.55.0\"` as derived-against and `\"git version 2.53.0\"` as installed. That failing message is the proof the assertion was not skipped, ignored, gated, softened or deleted."
    - "D-02: both constants' doc comments carry a re-derivation record naming the exact release-note range read (`Documentation/RelNotes/2.44.0.adoc` through `2.55.0.adoc`), the cross-check sources, the per-class verdict, and the named near-misses that were examined and excluded WITH the reason each fails its class test."
    - "D-03: the record states plainly that the 2.44→2.55 re-derivation was DOCUMENTARY — release notes plus git's own source — and was NOT a re-run of the `/ENV_WINS` vs `/INCLUDE_WINS` measurement harness, because no git 2.55.0 binary exists on the machine that did the work."
    - "D-04: the four `Measured against \\`git version 2.43.0\\`` probe tables and every one of their rows are byte-identical. They are true records of a measurement on 2.43.0 and editing them to say 2.55.0 would be fabricating a measurement."
    - "D-05: `config_key_names_a_reparsed_command_section`'s subsection justification is TRUE on git 2.54+ — it no longer claims `alias` has no subsection, and instead says the subsection is unread because the family is open in two dimensions, which is what makes `alias.<name>.command` covered with no code change."
    - "D-06: the schedule-versus-control distinction the file already makes is preserved. The reverse-direction drift pins may be cited as genuine two-sided live measurement, and the forward (fail-open) direction is still stated as UNCONTROLLED."
    - "D-07: the K2 list acknowledges the `hook.<friendly-name>.command` family as a NEW, examined-and-excluded K2 member that did not exist at 2.43.0, and explicitly does NOT claim inheritance for it — it was not exercised in the child-environment-dump harness."
    - "The ONLY non-doc-comment line changed in the whole diff is the version constant. Every other changed line starts with `///`."
  artifacts:
    - "src/envelope/policy.rs — the only file this plan touches. No Cargo.toml, no tag, no release."
  key_links:
    - "`the_config_section_constants_record_the_git_version_they_were_derived_against` (policy.rs:10476) shells out to the real `git --version` and compares it with the constant at policy.rs:1021. That pair is the entire mechanism this task exists to satisfy, and the assertion side of it is untouchable."
    - "`config_key_section` reads only the text before the FIRST `.`, so `alias.<name>.command` resolves to section `alias` and is already refused. That is why git 2.54's new spelling needs no array entry — D-05's rewrite is what records the reason."
    - "The reverse-direction drift pins at policy.rs:9910 (`INDIRECTION_SECTIONS.len() >= 2`) and policy.rs:10188 (`!REPARSED_COMMAND_SECTIONS.is_empty()`) iterate these arrays against a real git binary. Neither array shrinks, so both floors are unaffected."
---

<objective>
Re-derive the two git config-section constants against git 2.55.0, update the recorded
version, and write the re-derivation evidence into the constants' own doc comments.

`.github/workflows/release.yml` runs `cargo test` in the publish job. The GitHub runner has
`git version 2.55.0`; `CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION`
(src/envelope/policy.rs:1021) still reads `"git version 2.43.0"`, so
`the_config_section_constants_record_the_git_version_they_were_derived_against` fails and the
v1.7.0 release CI fails with it. The test's own failure message prescribes the fix, and this
plan does exactly what it prescribes and nothing else.

**THE HARD CONSTRAINT, STATED FIRST BECAUSE IT IS THE MOST IMPORTANT RULE IN THIS TASK.**
The assertion must NOT be deleted, skipped, marked with the ignore attribute, feature-gated,
made conditional on git being present, softened, or turned into a warning. The doc comments in
that file say in so many words that a skipped pin is a fail-open pin and that this phase has
been punished for that shape six times. Any change that weakens the assertion is wrong, and the
verification below is built so that the assertion FIRING is the pass condition.

**The re-derivation itself is already done and is authoritative — do not re-do it.** All twelve
release-note files for 2.44.0 through 2.55.0 were fetched from
`Documentation/RelNotes/2.XX.0.adoc` on git/git master (the `.txt` variants 404 across this
whole range; the file was renamed to `.adoc`), and the conclusions were cross-checked against
master's `Documentation/config.adoc`, `Documentation/config/alias.adoc`,
`Documentation/config/hook.adoc`, `config.c` and `alias.c`. Task 2 of this plan carries the
findings verbatim. Your job is to record them, not to re-derive them.

**D-01 — NEITHER ARRAY CHANGES.** `INDIRECTION_SECTIONS` stays `&["include", "includeIf"]`.
`REPARSED_COMMAND_SECTIONS` stays `&["alias"]`. Only the version string moves. A no-change
outcome is a legitimate result of a re-derivation: the point is that the release notes were
actually read.

Purpose: unblock the release CI by doing the re-audit the failing test schedules, and leave
behind a record honest enough that the next reader can tell what was measured from what was
read.
Output: one string literal, three rewritten or extended doc comments, in one file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/envelope/policy.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Move the recorded version to 2.55.0 and record the CLASS (a) re-derivation on INDIRECTION_SECTIONS</name>
  <files>src/envelope/policy.rs</files>
  <read_first>
    src/envelope/policy.rs lines 933-1031 — the `INDIRECTION_SECTIONS` doc comment, its
    `Measured against` probe table, its residue and revisit-condition sections, the constant
    itself, and the `CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION` doc and constant.

    src/envelope/policy.rs lines 10476-10541 — the assertion this task exists to satisfy. Read
    it so you can see it, and then do not edit it. The banner comment at 10477-10511 states the
    schedule-versus-control framing D-06 requires you to preserve, and states the no-skip rule.
  </read_first>
  <action>
    This is the tracer slice: it touches the constant, the doc register around it, and the
    assertion's observable behaviour end to end, and after it the release-CI failure is
    resolved even before tasks 2 and 3 land.

    **Step 1 (D-01).** At src/envelope/policy.rs:1021 change the string literal to
    `"git version 2.55.0"`. Change nothing else on that line and nothing else in the file's
    executable text. Leave `INDIRECTION_SECTIONS` at line 1007 and `REPARSED_COMMAND_SECTIONS`
    at line 1265 byte-identical.

    **Step 2 (D-04) — the edit you must NOT make, named explicitly so you do not make it by
    tidiness.** The `Measured against \`git version 2.43.0\`` line at 943 and the seven-row
    probe table at 947-955 stay exactly as they are, as do the three other occurrences of that
    phrase elsewhere in the file. They record a real measurement performed on a real 2.43.0
    binary. Rewriting them to say 2.55.0 would be fabricating a measurement that was never
    taken. If your new prose needs to refer to them, phrase it differently — "the 2.43.0 probe
    table above" — so the four existing occurrences of that exact phrase are preserved and
    countable.

    **Step 3 (D-02, D-03, D-06).** Add ONE new section to the `INDIRECTION_SECTIONS` doc
    comment, in the file's own existing register (a bold heading in the style of the sections
    already there, placed after the revisit-condition section and before the constant). It must
    state, as `///` lines only:

    - The re-derivation performed for this version bump: `Documentation/RelNotes/2.44.0.adoc`
      through `2.55.0.adoc` on git/git master, twelve files, cross-checked against master's
      `Documentation/config.adoc` and `config.c`. Note that the `.txt` variants 404 across this
      range because the release notes were renamed to `.adoc`, so a future re-auditor does not
      conclude the notes are missing.
    - **The verdict: NOTHING NEW. No third indirection section exists at 2.55.0.** `include`
      and `includeIf` remain the complete set. `Documentation/config.adoc` on master still says
      the `include` and `includeIf` sections are the ones that allow including config
      directives from another source, and `config.c`'s `git_config_include` splices on exactly
      two keys — `include.path`, and `includeif` with a true condition and a `path` key — and
      no others.
    - The near-misses that were examined and EXCLUDED, each with the reason it fails the class
      test: 2.46's fix for a repository whose configuration has `includeif.onbranch` and its
      `hasconfig:remote.*.url` straightening, and 2.47's crash fix for `includeIf.onbranch`
      outside a repository, are all the EXISTING `includeIf` section, not a new one. 2.52's
      `:(optional)` prefix on pathname-valued variables such as `blame.ignorerevsfile` is a
      tolerate-a-missing-file marker on existing variables; git reads no CONFIG from that file
      and it creates no section. 2.47's settable default object hash and ref backend format are
      configuration of git's own storage, not a splice of config from a named file.
    - The forward-looking note, flagged as NOT IN RANGE: `Documentation/RelNotes/2.56.0.adoc` is
      in development and not released; it teaches the conditional-inclusion facility a condition
      keyed on the worktree location. That is a new CONDITION inside the EXISTING `includeIf`
      section, so it would not qualify even once released, and the guard covers it by
      construction because it never reads the subsection.
    - **The honesty clause (D-03), which is not optional.** State plainly that this
      re-derivation was DOCUMENTARY — release notes plus git's own source — and was NOT a re-run
      of the `/ENV_WINS` vs `/INCLUDE_WINS` measurement harness, because no git 2.55.0 binary
      exists on the machine that performed it. Do not let the new text imply the 2.43.0 probe
      table was re-measured.
    - **The control clause (D-06), also not optional.** State what IS live measurement and
      exactly how far it reaches: the reverse-direction drift pin below iterates these entries
      and probes a REAL git binary, so it re-measures against whatever git is installed wherever
      the suite runs — it passes on the installed `git version 2.53.0` locally and the release
      CI runs it on the runner's `git version 2.55.0`, which is genuine two-sided confirmation
      of the REVERSE direction. Then say, in the same breath and without softening it, that this
      is STILL NOT a control over the forward fail-open direction, which remains uncontrolled.
      The residue and revisit-condition sections already in this doc are correct as written and
      must not be weakened, deleted or hedged by the new text; the new evidence must not read as
      closing the residue. Note also that the pin's floor (`INDIRECTION_SECTIONS.len() >= 2`) is
      unaffected because the array does not shrink.

    **Formatting constraint that is a real gate, not a style note.** Keep every angle-bracket
    placeholder you write inside backticks. Rustdoc HTML-parses unbackticked doc-comment text,
    and an unbackticked placeholder becomes a new `invalid_html_tags` warning that the
    verification below counts. policy.rs currently has 58 rustdoc warning locations and none of
    them is an HTML-tag warning; keep it that way.
  </action>
  <verify>
    <automated>rtk proxy cargo build && rtk proxy sh -c 'mkdir -p target/rederive; grep -c "CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION: &str = \"git version 2.55.0\"" src/envelope/policy.rs; grep -c "Measured against .git version 2\.43\.0." src/envelope/policy.rs; grep -c "INDIRECTION_SECTIONS: &\[&str\] = &\[\"include\", \"includeIf\"\];" src/envelope/policy.rs; grep -c "REPARSED_COMMAND_SECTIONS: &\[&str\] = &\[\"alias\"\];" src/envelope/policy.rs'</automated>
    <automated>rtk proxy sh -c 'mkdir -p target/rederive; cargo test --lib -- --exact envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against --nocapture > target/rederive/pin.txt 2>&1; grep -q "derived against : \"git version 2.55.0\"" target/rederive/pin.txt && grep -q "\"git version 2.53.0\"" target/rederive/pin.txt && echo PIN_LIVE_AND_FIRING'</automated>
  </verify>
  <done>
    `cargo build` exits 0. The four grep counts are `1`, `4`, `1`, `1` in that order — the
    constant moved to 2.55.0, all four 2.43.0 measurement-table headings survive, and both
    arrays are byte-identical.

    The second command prints `PIN_LIVE_AND_FIRING`. That is the load-bearing acceptance
    criterion of the whole plan: the assertion ran, reached its comparison, and reported
    `derived against : "git version 2.55.0"` against the locally installed
    `"git version 2.53.0"`. **The test FAILING here is the CORRECT and REQUIRED outcome.** It
    means the constant now points at the CI runner's git and the pin is still live. Do NOT
    "fix" it — not by reverting the constant to 2.53.0, not by making the assertion tolerant of
    a version mismatch, not by skipping it when git is absent. The local machine is simply two
    releases behind the runner the pin is now aimed at.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the CLASS (b) re-derivation and the three-level alias command finding on REPARSED_COMMAND_SECTIONS</name>
  <files>src/envelope/policy.rs</files>
  <read_first>
    src/envelope/policy.rs lines 1142-1265 — the `REPARSED_COMMAND_SECTIONS` doc comment: the
    three-kind K1/K2/K3 enumeration with its 2.43.0 probe table, the three-direction residue,
    the revisit condition and the provenance paragraph.
  </read_first>
  <action>
    Add ONE new section to the `REPARSED_COMMAND_SECTIONS` doc comment, in the same register and
    positioned after the revisit-condition section, as `///` lines only. Do not touch the
    `Measured against \`git version 2.43.0\`` line at 1155 or its three-row table at 1159-1163
    (D-04), and do not touch the constant at 1265 (D-01).

    The section must state:

    - The same re-derivation provenance as task 1 — `Documentation/RelNotes/2.44.0.adoc` through
      `2.55.0.adoc`, cross-checked against master's `Documentation/config/alias.adoc`,
      `Documentation/config/hook.adoc` and `alias.c`.
    - **The verdict: no new section. `alias` remains the only K1 member.** `alias.c` on master
      has a single `parse_config_key` call site for the `alias` section and no second section.
    - **THE LOAD-BEARING FINDING, which must be written out rather than summarised into a
      one-liner.** Git **2.54** extended the alias grammar: the release notes record that the
      alias configuration syntax was extended to allow aliases using characters outside ASCII
      alphanumeric plus `-`, and `Documentation/config/alias.adoc` on master documents a second
      spelling — an alias defined with a subsection, `alias.<name>.command`, whose name may be
      any bytes except newline and NUL including UTF-8, and which is equivalent to the
      two-level `alias.<name>` form. 2.55's further update to the i18n alias support is a fixup
      to that 2.54 work, not a new section. State the consequence explicitly: this is a
      genuinely NEW three-level spelling whose value git re-parses as a git command line, and it
      adds NO member to this constant, because its SECTION is still `alias`. The guard reads
      only the text before the first `.` via `config_key_section`, so a carrier spelled
      `-c alias.co.command='…'` is ALREADY refused by the existing rule with no code change. Say
      that this is a vindication of the decision not to read the subsection rather than a gap —
      and say the counterfactual, because it is the part that carries the lesson: a guard that
      had enumerated the two-level `alias.<name>` shape would have FAILED OPEN on 2.54's
      `alias.<name>.command`.
    - The CLASS (b) near-misses examined and EXCLUDED, each with the reason it fails the class
      test: 2.48's `remote.<name>.serverOption` injects one fixed option value into one
      subsystem, is not a value re-parsed as a git command line, and `remote` is not a new
      section. 2.44's `fetch.all`, which pretends `--all` was given on the command line, is the
      same shape and is excluded for the same reason. 2.51's tightening, so that alias expansion
      is reported only when `-h` is the sole option, is a reporting and behaviour change inside
      the existing section. 2.46's logging of alias-expanded command lines to the trace output
      is logging only. 2.49's `help.autocorrect = 1` running the plausible typofix immediately
      is not a config value re-parsed as a command line.
    - **The new K2 family (D-07).** 2.54 added centrally-configurable hook commands —
      `hook.<friendly-name>.command`, `.event` and `.enabled`, extended in 2.55 by
      `hook.<friendly-name>.parallel`, `hook.<event>.jobs` and `hook.jobs`.
      `Documentation/config/hook.adoc` says the value can be an executable path or a shell
      oneliner. That is squarely this doc's own **K2** class — re-parsed as a SHELL command line
      and run as a CHILD that inherits the envelope's `GIT_CONFIG_COUNT`/`KEY_0`/`VALUE_0`
      triplet — and NOT K1, so it needs no entry here. Record it as a NEW and sizeable K2 family
      that did not exist at 2.43.0, and extend the K2 enumeration at lines 1172-1190 to mention
      it. **Then state the limit of the claim in the same terms the existing text uses for
      `core.pager` and `core.editor`: this family was NOT exercised in the child-environment-dump
      harness, so inheritance is NOT claimed for it, and a K2 member that does not inherit would
      be a FINDING.** Do not write a sentence that implies it was measured.
    - **The honesty clause (D-03)** in the same terms as task 1: documentary, not a re-run of the
      measurement harness, no 2.55.0 binary on the machine that did the work.
    - **The control clause (D-06)** in the same terms as task 1, scoped to this constant's pin:
      the reverse-direction drift pin probes a real git binary and re-measures on 2.53.0 locally
      and on 2.55.0 in release CI, which confirms the reverse direction two-sidedly; it is still
      not a control over any of the THREE fail-open directions this doc already names, all three
      of which remain uncontrolled and must survive the edit unweakened. The pin's floor
      (`!REPARSED_COMMAND_SECTIONS.is_empty()`) is unaffected because the array does not shrink.

    Same formatting constraint as task 1: every angle-bracket placeholder stays inside backticks.
  </action>
  <verify>
    <automated>rtk proxy cargo build && rtk proxy sh -c 'grep -c "REPARSED_COMMAND_SECTIONS: &\[&str\] = &\[\"alias\"\];" src/envelope/policy.rs; grep -c "alias[.]<name>[.]command" src/envelope/policy.rs; grep -c "hook[.]<friendly-name>[.]command" src/envelope/policy.rs; grep -c "2[.]44[.]0[.]adoc" src/envelope/policy.rs; grep -c "Measured against .git version 2[.]43[.]0." src/envelope/policy.rs'</automated>
  </verify>
  <done>
    `cargo build` exits 0. The array line is still present exactly once and unchanged. The new
    three-level alias spelling is named at least once, the new hook K2 family is named at least
    once, the release-note range start is cited at least twice (once per constant, from tasks 1
    and 2), and the four 2.43.0 measurement-table headings still count 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Correct the subsection justification that git 2.54 made false</name>
  <files>src/envelope/policy.rs</files>
  <read_first>
    src/envelope/policy.rs lines 1267-1311 — `config_key_names_a_reparsed_command_section`'s doc
    comment and body. The bullet to repair is the first one under the heading about the
    subsection and the variable never being read, at lines 1277-1279.
  </read_first>
  <action>
    **D-05.** The bullet beginning `**The SUBSECTION is not read**` gives a justification that was true at
    2.43.0 and is FALSE on git 2.54 and later: it argues from the premise that the alias section
    has no subsection form. Task 2 establishes that it does. The rule's BEHAVIOUR is unchanged
    and still correct; only its stated REASON is wrong. Leaving a false justification inside a
    control's own doc is precisely the `T-19-107` failure mode this file names repeatedly, so it
    gets repaired rather than left.

    Rewrite that bullet in place — keep the `**The SUBSECTION is not read**` opening so the
    heading structure and the sibling VARIABLE bullet below it still read as a pair — so that it
    says what is now true:

    - The subsection is not read because reading it would have to enumerate an OPEN family.
    - Since git 2.54 that family is open in TWO dimensions at once: the two-level
      `alias.<name>` and the three-level `alias.<name>.command`, with names that may be
      arbitrary UTF-8 — a raw-byte, case-sensitive subsection, unlike the section half, which
      git folds and which this function therefore compares with `eq_ignore_ascii_case`.
    - Not reading it is exactly what makes the 2.54 spelling covered with no code change, and a
      guard that had enumerated the two-level shape would have failed open on the three-level
      one.
    - Cross-reference `REPARSED_COMMAND_SECTIONS`' re-derivation record from task 2 as the
      source, rather than restating the whole finding here.

    Do NOT change the function body at lines 1303-1311, the VARIABLE bullet, the no-section
    paragraph, or the section-comparison paragraph. This is a doc-comment repair and nothing
    else. Keep angle-bracket placeholders inside backticks.

    **Record in the SUMMARY, under a "Coordinator inferred decisions (for audit)" heading, one
    correction made without the human.** The task brief asked for confirmation that the total
    pass count has not regressed from a 1345 baseline. That figure does not match the project's
    own most recent measured gate: `.planning/STATE.md` records `48 suites / 2120 passed /
    1 failed / 15 ignored` on the v1.7.0 tagged tree, which is this plan's base revision. The
    verification below therefore pins the MEASURED pre-edit baseline against the MEASURED
    post-edit result instead of against the quoted number, which is strictly stronger; note in
    the SUMMARY that 1345 was treated as stale.
  </action>
  <verify>
    <automated>rtk proxy cargo build && rtk proxy sh -c 'grep -c "no condition between them" src/envelope/policy.rs; grep -c "The SUBSECTION is not read" src/envelope/policy.rs'</automated>
    <automated>rtk proxy sh -c 'mkdir -p target/rederive; touch src/envelope/policy.rs; cargo doc --no-deps --document-private-items > target/rederive/doc-after.txt 2>&1; grep -c "^ *--> src/envelope/policy.rs" target/rederive/doc-after.txt'</automated>
  </verify>
  <done>
    `cargo build` exits 0. The first grep count is `0` — the falsified justification clause is
    gone. The second is `1` — the bullet is repaired in place, not deleted.

    The rustdoc warning-location count for `src/envelope/policy.rs` is **at most 58**, the
    measured pre-edit baseline. If it exceeds 58 the new doc text introduced a rustdoc warning;
    the cause is almost certainly an angle-bracket placeholder written without backticks, or a
    new intra-doc link to a private item. Fix it by backticking the placeholder or dropping the
    link — never by deleting the evidence the placeholder is part of.
  </done>
</task>

</tasks>

<verification>

Run these on the finished tree, in order.

**1. The project gates.**

```
rtk proxy cargo build
rtk proxy cargo clippy -- -D warnings
```

Both exit 0.

**2. The test suite — the command shape is itself load-bearing.**

```
rtk proxy cargo test --no-fail-fast
```

- **`--no-fail-fast` is mandatory.** A plain `cargo test` stops at the first failing test
  BINARY. `driver_reattach` fails (a documented pre-existing pair) and `envelope_*` sorts after
  `driver_*`, so a plain run never executes any envelope binary and reports a stale count no
  matter what changed.
- **`rtk proxy` is mandatory.** rtk's output filtering strips `test result:` lines, so any run
  not wrapped in `rtk proxy` lies about the counts.
- **Do not pipe the output through `grep` or `awk`.** rtk filters downstream of the pipe and
  fakes count regressions. Redirect to a file inside `target/` first and read the file.

**3. The expected result, stated exactly.**

The pre-edit baseline recorded for this base revision is `48 suites / 2120 passed / 1 failed /
15 ignored`, the one failure being
`envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`.
If you have any doubt, measure the baseline yourself on a clean checkout of the base revision
first — that is cheap and settles it.

**The post-edit failing set must be IDENTICAL, one test, the same test.** This is a stronger
statement than "no regression" and it is the right one here: that test fails BEFORE the change
(installed 2.53.0 vs derived 2.43.0) and AFTER it (installed 2.53.0 vs derived 2.55.0). Only
the assertion's message differs. Nothing else in the diff is executable, so no other count may
move.

**That one failure is CORRECT and must NOT be "fixed."** Not by reverting the constant to
2.53.0, not by softening, skipping or gating the assertion, not by making it tolerant of a
version mismatch. It is the local machine being two releases behind the CI runner the pin is
now aimed at, and on the runner — `git version 2.55.0` — the test PASSES, which is the entire
point of the change.

**4. The structural gate: the diff is doc comments plus one string literal.**

```
rtk proxy sh -c 'git diff -U0 -- src/envelope/policy.rs > target/rederive/diff.txt; echo -n "files changed: "; git diff --name-only | wc -l; echo -n "non-doc removals: "; grep "^-" target/rederive/diff.txt | grep -v "^---" | grep -v "^-[[:space:]]*///" | grep -v "^-pub const CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION" | wc -l; echo -n "non-doc additions: "; grep "^+" target/rederive/diff.txt | grep -v "^+++" | grep -v "^+[[:space:]]*///" | grep -v "^+pub const CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION" | wc -l; echo -n "probe rows removed: "; grep "^-" target/rederive/diff.txt | grep -c "ENV_WINS\|INCLUDE_WINS\|Measured against" || true'
```

Required: `files changed: 1`, `non-doc removals: 0`, `non-doc additions: 0`, `probe rows
removed: 0`.

This is the gate that enforces the hard constraint mechanically rather than by care. The test
module is not doc-comment text, so any weakening of the assertion — deleting it, adding an
attribute above it, wrapping the comparison in a conditional — shows up as a non-doc change and
turns this red. `probe rows removed: 0` is D-04: no row of any 2.43.0 measurement table was
deleted or edited.

**5. Scope.**

`files changed: 1` also enforces the out-of-scope rules: `Cargo.toml` is not bumped, no tag is
created, no release is run. A separate step handles the v1.7.1 release.

</verification>

<success_criteria>

- [ ] `CONFIG_SECTION_CONSTANTS_DERIVED_AGAINST_GIT_VERSION` reads `"git version 2.55.0"` (D-01)
- [ ] `INDIRECTION_SECTIONS` and `REPARSED_COMMAND_SECTIONS` are byte-identical (D-01)
- [ ] The version assertion fires locally and its message names 2.55.0 derived-against and
      2.53.0 installed — `PIN_LIVE_AND_FIRING` printed (D-01)
- [ ] Both constants carry a re-derivation record with range, verdict, and named-and-excluded
      near-misses with reasons (D-02)
- [ ] Both records say plainly that the re-derivation was documentary, not measured (D-03)
- [ ] All four `Measured against \`git version 2.43.0\`` tables and rows untouched (D-04)
- [ ] The subsection justification is true on git 2.54+ and names the `alias.<name>.command`
      spelling (D-05)
- [ ] The schedule-versus-control distinction survives; the forward direction is still stated
      as uncontrolled (D-06)
- [ ] The `hook.<friendly-name>.command` K2 family is recorded as examined-and-excluded with
      inheritance explicitly NOT claimed (D-07)
- [ ] `cargo build` 0, `cargo clippy -- -D warnings` 0
- [ ] `rtk proxy cargo test --no-fail-fast`: failing set identical to baseline — one test, the
      version pin
- [ ] Structural gate: 1 file changed, 0 non-doc removals, 0 non-doc additions, 0 probe rows
      removed
- [ ] rustdoc warning locations for policy.rs at most 58

</success_criteria>

<output>
Create `.planning/quick/260917-ii4-re-derive-git-config-section-constants-a/260917-ii4-SUMMARY.md` when done.

The SUMMARY must include a **Coordinator inferred decisions (for audit)** section covering:
- The 1345-vs-2120 baseline correction described in task 3.
- The verdict that NEITHER array changes, and that this is the re-derivation's genuine result
  rather than a decision to skip work.
- Any place where the wording of the honesty or control clauses required a judgement call, so a
  later reader can re-check the claim rather than inherit it.
</output>
