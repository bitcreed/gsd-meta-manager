---
quick_id: 260722-emn
plan: 10
item: I
type: execute
wave: 5
depends_on: [260722-emn-1, 260722-emn-2, 260722-emn-3, 260722-emn-4, 260722-emn-5, 260722-emn-6, 260722-emn-7, 260722-emn-8, 260722-emn-9]
files_modified: [Cargo.toml, Cargo.lock]
autonomous: true
---

<objective>
Release prep for the GSD 1.8.0 catch-up: bump the crate version to 1.6.0, refresh the
lockfile, record dependencies still behind their latest major, and prove the fully
integrated tree is clean under build, test, and clippy `-D warnings`.

Purpose: This is the final integration gate — it runs after every code wave merges, so
it catches any clippy warning or test regression introduced across plans 1–9. Per
CLAUDE.md the crate version must track the milestone and `gsd-meta-manager --version`
reads it from `Cargo.toml`.

Output: `Cargo.toml` at `version = "1.6.0"`, refreshed `Cargo.lock`, and a deps-behind
note in this plan's SUMMARY / commit body.

Scope guard: edit ONLY `Cargo.toml` and `Cargo.lock`. Do NOT create the annotated git
tag, update `.planning/STATE.md`, or perform the rest of the CLAUDE.md "Release Process"
milestone ritual — that is a separate `/gsd:complete-milestone` step and is out of scope
for this quick task. This plan runs strictly last (wave 5).
</objective>

<context>
@/home/blk/projects/rust/gsd-meta-manager/Cargo.toml
@/home/blk/projects/rust/gsd-meta-manager/CLAUDE.md (Release Process — version/lock steps only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 1.6.0 and refresh the lockfile</name>
  <files>Cargo.toml, Cargo.lock</files>
  <action>
    In `Cargo.toml` change `version = "1.5.0"` to `version = "1.6.0"` (edit only that line
    in `[package]`; leave dependency versions untouched). Run `cargo update` to refresh
    `Cargo.lock` to the latest within the current semver constraints. Determine which DIRECT
    dependencies are still behind their latest major (compare the resolved versions against
    crates.io latest — e.g. `notify` 8.x vs 9.x, and any others such as `serde_yml`,
    `ratatui`, `dirs`, `notify-debouncer-full`, `tui-textarea`); collect that list to
    itemize in the SUMMARY/commit body. Do NOT force any semver-breaking major bump here —
    just record the gap (deferring the major bump is the documented policy in CLAUDE.md).
  </action>
  <verify>
    <automated>grep -q '^version = "1.6.0"' Cargo.toml &amp;&amp; cargo build 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    `Cargo.toml` reads `version = "1.6.0"`, `Cargo.lock` is refreshed by `cargo update`, and
    the list of deps still behind latest major is captured for the summary. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full clean verification (build + test + clippy -D warnings)</name>
  <files>Cargo.toml, Cargo.lock</files>
  <action>
    Run the integration gate over the fully merged tree: `cargo build`, then `cargo test`,
    then `cargo clippy -- -D warnings`. All three must pass with zero warnings/errors. If
    clippy flags anything introduced by an earlier plan's merge, fix it minimally IN THIS
    PLAN only if the fix is confined to `Cargo.toml`/`Cargo.lock`; otherwise STOP and report
    which file/plan needs the fix (do not edit source files here — that would violate this
    plan's file scope and the wave-4 UI-only ownership). Record the deps-behind-latest-major
    list (from Task 1) in the plan SUMMARY and the release commit body.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -3 &amp;&amp; cargo test 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `cargo build`, `cargo test`, and `cargo clippy -- -D warnings` all pass clean on the
    integrated tree. Deps-behind list recorded in the summary.
  </done>
</task>

</tasks>

<verification>
- `Cargo.toml` version is `1.6.0`; `Cargo.lock` refreshed.
- `cargo build`, `cargo test`, and `cargo clippy -- -D warnings` all pass.
- Dependencies still behind their latest major are itemized in the summary/commit body.
</verification>

<commit>
Single atomic commit:
`chore(260722-emn): bump version to 1.6.0 and refresh Cargo.lock`
Body itemizes the deps still behind their latest major (deferred bumps).
</commit>
