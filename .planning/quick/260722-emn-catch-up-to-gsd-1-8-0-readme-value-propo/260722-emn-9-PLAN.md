---
quick_id: 260722-emn
plan: 9
item: H
type: execute
wave: 4
depends_on: [260722-emn-2, 260722-emn-4, 260722-emn-6, 260722-emn-8]
files_modified: [src/ui/screens/detail.rs, src/ui/screens/normal.rs]
autonomous: false
---

<objective>
Wire the wave-1–3 data-layer additions into the UI: display the `waves.json` parallelism
manifest in the Pipe tab, expose the new config keys in the Cfg tab, and surface
workstreams, `external_job_waiting`, and the new artifact flags where the UI already
shows phase status.

Purpose: All the reader work is inert until the TUI renders it. This is the sole UI plan
(last code wave), so every UI edit lives here.

Output: Updated `src/ui/screens/detail.rs` and `src/ui/screens/normal.rs`.

Scope guard: edit ONLY these two files. This plan runs alone in wave 4 (all data layers
merged). Keep additions minimal and consistent with existing widgets/patterns. Do NOT
edit `project_list.rs` — surface project-row indicators in `normal.rs::render_main`
(which renders the live project table) instead.
</objective>

<context>
@/home/blk/projects/rust/gsd-meta-manager/src/ui/screens/normal.rs

Key sites in `src/ui/screens/detail.rs` (read these regions before editing):
- `render_pipeline_tab` (~line 2377) and `build_substage_lines` (~line 3364) / `push_substage` (~line 3413) — the Pipe tab and its artifact drill-down.
- `build_defaults_entries` (~line 3582) with its `push(cat, key, value, kind, first, from_defaults)` closure and the layered accessors `bool_l`/`u32_l`/`str_l`/`enum_l` — the Cfg tab field enumeration.
- The Cfg-tab toggle/edit setter match arms keyed by field-name string (~lines 3857 and ~3970).

Canonical reference (read-only) for waves.json:
- /home/blk/projects/node/gsd-core/gsd-core/docs/how-to/enable-claude-orchestration-workflow-backend.md — manifest path `.planning/phases/<phase-dir>/waves.json`, shape `{ "waves": [ { wave/plans with files_modified } ] }`.
- /home/blk/projects/node/gsd-core/tests/fix-2285-claude-orchestration-wiring.test.cjs — a concrete waves manifest.

Data available on `ProjectState` after wave 3: `workstreams: Vec<WorkstreamState>`,
`external_job_waiting: bool`, `current_phase_name`, `current_plan`, `last_activity`; and
`DiskInference` now has `has_coverage`/`has_windows`/`has_deferred_items`/`has_skeleton`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Display the waves.json parallelism manifest in the Pipe tab</name>
  <files>src/ui/screens/detail.rs</files>
  <action>
    Add a pure helper `fn parse_waves_manifest(raw: &str) -> Option<WavesManifest>` (local
    `#[derive(Deserialize)]` structs) that deserializes `{ "waves": [ ... ] }`; be lenient
    about inner fields — capture at least the wave number/index and each wave's plan
    identifiers (and `files_modified` if present). In `render_pipeline_tab`, resolve the
    active phase directory for the selected project (project path from
    `ctx.config.projects.get(&self.alias)`, planning dir = `<path>/.planning`, phase dir via
    `crate::state_reader::disk_status::find_phase_dir(planning_dir, &phase_num)` using the
    current phase number). If `<phase_dir>/waves.json` exists and parses, render a compact
    "Waves" section — one line per wave listing its plans (and a small parallelism hint,
    e.g. plan count) — placed near the existing pipeline content. When the file is absent or
    unparsable, render nothing extra (no error noise). Add a unit test for
    `parse_waves_manifest` covering a two-wave manifest and a garbage-input `None`.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -3 &amp;&amp; cargo test --lib 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    The Pipe tab shows the phase's wave/plan manifest when `waves.json` is present and
    silently omits it otherwise; `parse_waves_manifest` is unit-tested. Build + tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose the new config keys in the Cfg tab</name>
  <files>src/ui/screens/detail.rs</files>
  <action>
    In `build_defaults_entries`, add display rows (via the existing `push(...)` closure and
    `bool_l`/`u32_l`/`str_l`/`enum_l` accessors) for the plan-4 config keys: a new
    "Orchestration" category for `claude_orchestration.enabled` / `execution_backend` /
    `min_agent_sdk_version`; extend the Execution/Planning categories with the new
    `workflow.*` gates (`api_coverage_gate`, `windows_enforce`, `specless_probe_fallback`,
    `assumption_delta`, `test_gate_timeout`, `context_guard_mode`, `plan_drift_precheck`,
    `mvp_mode`, `code_review_command`, `plan_chunked`, and the ASVS keys); add rows for
    `statusline.*`, `dynamic_routing.*`, `external_job.*`, `capabilities.*`,
    `graphify.graph_path`, and the top-level `phase_id_convention` / `claude_md_path`. For
    shape-varying keys (`security_asvs_level`, `security_block_on`, `review.reviewer_instances`,
    `sub_repos`), render them read-only as a formatted string of the JSON value (a
    `ConfigValueKind` display-only variant is acceptable) — do not attempt to make them
    editable. For the plain bool/string/u32 keys, ADD matching toggle/edit setter arms in
    the Cfg-tab setter match blocks (~lines 3857 and ~3970) so they persist via
    `get_or_insert_with(...)`, mirroring the existing arms. Keep category grouping and the
    `from_defaults` layering behavior consistent with the current rows.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -3 &amp;&amp; cargo test 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    The Cfg tab lists the new 1.4–1.8 config keys grouped sensibly; editable scalar/bool
    keys toggle and persist; shape-varying keys display read-only. Build + tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface workstreams, external_job_waiting, and new artifacts</name>
  <files>src/ui/screens/detail.rs, src/ui/screens/normal.rs</files>
  <action>
    In `detail.rs::build_substage_lines`, add the four new artifact sub-stages using the
    existing `push_substage` helper: extend the `plan_touched`/`exec_touched` gates to also
    consider `inf.has_coverage`/`inf.has_windows`/`inf.has_deferred_items`/`inf.has_skeleton`,
    then `push_substage(&mut lines, "Coverage", inf.has_coverage)`, `"Windows"`,
    `"Deferred"` (has_deferred_items), and `"Skeleton"` (has_skeleton), placing each in the
    Plan or Execute group as appropriate (Skeleton/Windows/Deferred under Plan, Coverage
    under Execute). In the pipeline tab (and/or the phase-status header), when
    `state.external_job_waiting` is true, render a distinct "external job waiting" indicator
    so the phase reads as legitimately blocked rather than stuck. In `normal.rs::render_main`,
    add two minimal project-row cues consistent with the existing paused (`⏸`) / session
    (`▶`) badges: (a) an `external_job_waiting` badge on the alias cell (a distinct glyph/
    color, lower priority than the pause badge), and (b) when `state.workstreams` is
    non-empty, a compact indicator (e.g. the active workstream name or a `N ws` count) in
    an existing column so multi-workstream projects are visible. Keep the 80/60/40-column
    layout branches working. Prefer reusing existing spans/styles over new widgets.
  </action>
  <verify>
    <automated>cargo build 2>&amp;1 | tail -3 &amp;&amp; cargo test 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    Coverage/Windows/Deferred/Skeleton appear as sub-stages when present;
    `external_job_waiting` is visibly distinct from a stuck phase; multi-workstream projects
    show a workstream cue in the dashboard. Build + tests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Pipe tab now shows the `waves.json` manifest; Cfg tab lists/edits the new GSD 1.8.0
    config keys; the dashboard surfaces workstreams, external-job-waiting, and the new
    artifact sub-stages (Coverage/Windows/Deferred/Skeleton).
  </what-built>
  <how-to-verify>
    Run `cargo run` against a registered project. (1) Open a project with a planned phase
    and check the Pipe tab renders the wave/plan manifest when a `waves.json` exists. (2)
    Open the Cfg tab and confirm the new keys appear under sensible categories and that a
    bool key toggles + persists. (3) On the dashboard, confirm a project with
    `.planning/workstreams/*` shows a workstream cue and one with a
    `.planning/async-jobs/*.json` reads as external-job-waiting, not stuck. Confirm the
    layout is intact at 80/60/40 columns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering/layout issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `cargo build` succeeds; `cargo test` passes.
- `parse_waves_manifest` unit-tested.
- Human-verify checkpoint confirms the three UI surfaces render correctly and the layout
  holds at 80/60/40 columns.
</verification>

<commit>
Commit atomically per task, e.g.
`feat(260722-emn): show waves.json parallelism manifest in Pipe tab`,
`feat(260722-emn): expose GSD 1.8.0 config keys in Cfg tab`,
`feat(260722-emn): surface workstreams, external-job-waiting, and new artifacts in UI`.
</commit>
