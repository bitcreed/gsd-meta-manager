---
quick_id: 260722-emn
plan: 1
item: A
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
---

<objective>
Rewrite/extend README.md to clarify the project's value proposition in light of GSD
1.8.0's experimental `claude-orchestration` backend, and state a compatibility target
of "GSD 1.8.0 state formats".

Purpose: GSD 1.8.0 added an experimental Claude-orchestration execution backend that
parallelizes plan execution *inside one Claude session on one project* and writes no
new on-disk state. A reader could mistake that for overlap with gsd-meta-manager.
This README makes the distinction explicit: gsd-meta-manager is the *cross-project*
dashboard (zero-token state reading, session detection/launch/resume, tmux focus,
archive browsing). Do NOT invent features that do not exist.

Output: Updated README.md.
</objective>

<context>
@/home/blk/projects/rust/gsd-meta-manager/README.md
@/home/blk/projects/rust/gsd-meta-manager/CLAUDE.md

Reference (read-only) for what claude-orchestration actually is — do NOT overstate it:
/home/blk/projects/node/gsd-core/gsd-core/docs/how-to/enable-claude-orchestration-workflow-backend.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add value-proposition + compatibility framing to README</name>
  <files>README.md</files>
  <action>
    Keep the existing accurate "Features", "Installation", "Quick start", "Usage",
    "How it works" sections intact (they describe real capabilities). Make three
    edits:
    (1) Refine the "What is it?" section (or add a short "Why GSD Meta Manager?"
    subsection right after it) that positions the tool as the CROSS-PROJECT dashboard:
    zero-token state reading from `.planning/` on disk (no Claude/GSD run required),
    Claude session detection + launch/resume, tmux focus (Tab-to-switch), and milestone
    archive browsing — spanning ALL registered projects at once.
    (2) Add a concise paragraph distinguishing it from GSD 1.8.0's experimental
    `claude-orchestration` execution backend: that backend parallelizes plan execution
    inside ONE Claude session on ONE project and writes no new on-disk state, whereas
    gsd-meta-manager observes and acts across MANY projects from a single terminal.
    Frame them as complementary, not competing.
    (3) Add a one-line "Compatibility" note (in Requirements or a new short section)
    stating the tool reads GSD 1.8.0 `.planning/` state formats.
    Do NOT claim any feature the binary does not have (no "controls orchestration",
    no "edits config across projects" beyond the existing Cfg tab, etc.). Preserve the
    leading `<!-- generated-by: gsd-doc-writer -->` comment. Keep prose wrapping
    consistent with the existing file (~80 cols).
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -3 &amp;&amp; grep -qi "1\.8\.0" README.md &amp;&amp; grep -qi "claude-orchestration\|orchestration" README.md &amp;&amp; grep -qi "cross-project\|across all\|every.*project\|across.*projects" README.md</automated>
  </verify>
  <done>
    README.md mentions GSD 1.8.0 state-format compatibility, contrasts the tool with
    the single-session claude-orchestration backend, and frames the cross-project
    value proposition. No fabricated features. Existing accurate sections preserved.
    `cargo build` still succeeds (README change is docs-only; build is a sanity gate).
  </done>
</task>

</tasks>

<verification>
- `cargo build` succeeds (no code changed).
- README states compatibility with "GSD 1.8.0" state formats.
- README distinguishes the cross-project dashboard from the single-session
  claude-orchestration backend without inventing features.
</verification>

<commit>
Commit atomically: `docs(260722-emn): clarify value proposition vs GSD 1.8.0 claude-orchestration`
</commit>
