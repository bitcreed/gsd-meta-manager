---
phase: 05-state-reader-accuracy
plan: 05
type: execute
wave: 1
depends_on: ["05-04"]
files_modified:
  - src/ui/screens/normal.rs
  - src/ui/screens/detail.rs
autonomous: true
gap_closure: true
requirements: [STATE-03]

must_haves:
  truths:
    - "Selected dashboard row shows compact pipeline (same as unselected rows), not expanded text"
    - "Detail view phase list has a legend line explaining bracket notation"
  artifacts:
    - path: "src/ui/screens/normal.rs"
      provides: "Compact pipeline for all rows, no expanded_status function"
    - path: "src/ui/screens/detail.rs"
      provides: "Legend line above phase list explaining disk status brackets"
  key_links:
    - from: "src/ui/screens/normal.rs"
      to: "compact_pipeline()"
      via: "all rows use compact_pipeline regardless of selection"
      pattern: "compact_pipeline"
---

<objective>
Close two UAT gaps from 05-HUMAN-UAT.md: (1) remove the expanded_status() function and is_selected branching so all dashboard rows show the compact D-R-P-E-V pipeline uniformly, and (2) add a legend line in the detail view explaining what the bracket notation means.

Purpose: Fix user-reported inaccurate expanded status text and missing context for bracket notation.
Output: Updated normal.rs and detail.rs with UAT issues resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-reader-accuracy/05-HUMAN-UAT.md
@src/ui/screens/normal.rs
@src/ui/screens/detail.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove expanded_status and unify dashboard rows to compact pipeline</name>
  <files>src/ui/screens/normal.rs</files>
  <read_first>src/ui/screens/normal.rs</read_first>
  <action>
1. Delete the `expanded_status()` function (lines 76-89) entirely.
2. In the `render_main` method, find the `status_cell` construction block (around lines 339-372). Remove the `is_selected` variable (line 309) and the `if is_selected { ... } else { ... }` branching. Replace the entire `status_cell` block with a single branch that applies to ALL rows (selected or not):
   - If `is_milestone_complete`: show milestone complete text (keep as-is)
   - Else: show `compact_pipeline(&inference.status)` if disk inference exists, otherwise show status string

   The resulting code should look like:
   ```rust
   let status_cell: Line = if is_milestone_complete {
       // ... existing milestone complete code unchanged ...
   } else {
       match state.and_then(|s| s.current_phase_status.as_ref()) {
           Some(inference) => compact_pipeline(&inference.status),
           None => Line::from(Span::styled(
               status_str.clone(),
               Style::default().fg(row_color),
           )),
       }
   };
   ```
3. Remove the `let is_selected = selected_idx == Some(idx);` line since it is no longer used.
4. The `expanded_status` function import of `DiskInference` may become unused — check and remove the `DiskInference` import from the `use crate::state_reader::disk_status::{DiskInference, DiskStatus}` line if nothing else references it. Keep `DiskStatus` which is still used by `compact_pipeline` and `prev_status`.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expanded_status" src/ui/screens/normal.rs` returns 0
    - `grep -c "is_selected" src/ui/screens/normal.rs` returns 0
    - `grep -c "compact_pipeline" src/ui/screens/normal.rs` returns at least 2 (definition + usage)
    - `cargo check` succeeds with no errors
  </acceptance_criteria>
  <done>All dashboard rows (selected or not) show the compact D-R-P-E-V pipeline. The expanded_status function and is_selected branching are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add legend line for disk status brackets in detail view</name>
  <files>src/ui/screens/detail.rs</files>
  <read_first>src/ui/screens/detail.rs</read_first>
  <action>
In the detail view's phase list rendering (the `else` branch of `if show_roadmap`), add a legend line immediately after the "Phases:" header line. This applies in TWO places: the `render` method (around line 265-268) and the `render_main_only` method (around line 493-496).

After the line that pushes `"  Phases:"`, and before the `if state.phases.is_empty()` check, insert:

```rust
lines.push(Line::from(Span::styled(
    "  Legend: + done  * current  o future  [stage] = disk-inferred  (N plans) = plan count",
    Style::default().fg(Color::DarkGray),
)));
```

This must be added in BOTH the `render` method and the `render_main_only` method since both contain duplicated phase list rendering logic.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Legend:" src/ui/screens/detail.rs` returns 2 (one in render, one in render_main_only)
    - `grep "disk-inferred" src/ui/screens/detail.rs` matches the legend text
    - `cargo check` succeeds with no errors
  </acceptance_criteria>
  <done>Detail view shows a DarkGray legend line below "Phases:" header explaining the icon meanings and bracket notation. Legend appears in both render paths.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes — no compile errors
2. `grep -c "expanded_status" src/ui/screens/normal.rs` returns 0
3. `grep -c "Legend:" src/ui/screens/detail.rs` returns 2
</verification>

<success_criteria>
- Dashboard rows uniformly show compact D-R-P-E-V pipeline regardless of selection state
- Detail view phase list includes a legend explaining bracket notation
- Project compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-state-reader-accuracy/05-05-SUMMARY.md`
</output>
