---
phase: 13-queue-execution-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md
autonomous: true
requirements:
  - QRES-01
  - QRES-02

must_haves:
  truths:
    - "Design document covers GSD autonomous mode lifecycle with phase-by-phase execution loop"
    - "Design document documents all CLI capabilities (claude -p, --continue, --resume, --output-format)"
    - "Design document maps hook points and WAITING.json signal mechanism"
    - "Design document presents two integration strategies with trigger, lifecycle, artifact flow, and error handling for each"
    - "Design document includes safety requirements with timeouts, retries, escalation triggers"
    - "Design document states confidence levels (HIGH/MEDIUM/LOW) on each design element"
  artifacts:
    - path: ".planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md"
      provides: "Complete queue execution design document"
      contains: "Executive Summary"
  key_links: []
---

<objective>
Write the QUEUE-EXECUTION-DESIGN.md document that captures all research findings into a structured design enabling v1.3 queue execution implementation.

Purpose: This document is the sole deliverable of Phase 13. It must be self-contained and actionable enough that a v1.3 implementer needs no further research.
Output: `.planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-queue-execution-research/13-CONTEXT.md
@.planning/phases/13-queue-execution-research/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GSD analysis sections (QRES-01)</name>
  <files>.planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md</files>
  <read_first>
    - .planning/phases/13-queue-execution-research/13-RESEARCH.md
    - .planning/phases/13-queue-execution-research/13-CONTEXT.md
  </read_first>
  <action>
Create QUEUE-EXECUTION-DESIGN.md with these sections, synthesizing from 13-RESEARCH.md:

1. **Executive Summary** (3-5 paragraphs): What this document covers, why it exists, the recommended approach (Strategy A per-item isolation), and key constraints (LLM-agnostic per user decision). State that this enables v1.3 implementation without further research.

2. **GSD Autonomous Mode Lifecycle** (per QRES-01): Document the 6-step lifecycle (Initialize, Discover, Execute Each Phase with sub-steps, Iterate, Lifecycle completion, Handle Blocker). Include the key observation that autonomous mode uses Skill() invocations internally which the TUI cannot call -- it must use claude -p. State confidence: HIGH.

3. **Hook Points and Integration Surfaces**: Document the 3 GSD hooks (context-monitor, workflow-guard, statusline) with their types and purposes. Document WAITING.json signal mechanism in detail -- the signal-waiting/signal-resume commands, the JSON structure, the bidirectional communication gap. Map the GSD CLI tools (gsd-tools.cjs) that the TUI can call directly without a Claude session. State confidence: HIGH.

4. **CLI Capabilities Matrix** (per QRES-01): Full table of claude CLI flags relevant to queue execution (from research: -p, -c, --resume, --output-format, --allowedTools, --permission-mode, --model, --bare, --append-system-prompt, --max-budget-usd, --worktree, --no-session-persistence, --name). Include the session ID chaining example. Note the critical finding about --bare mode. State confidence: HIGH.

5. **GSD Workflow Entry Points**: Table of GSD commands that map to QUEUE.md items (/gsd:discuss-phase, /gsd:plan-phase, /gsd:execute-phase, /gsd:verify-work, /gsd:autonomous, /gsd:quick, /gsd:fast, /gsd:do, /gsd:next). Highlight /gsd:next as particularly interesting for simple queue execution.

Each section must include an explicit **Confidence:** line (HIGH/MEDIUM/LOW) per QRES-02.
  </action>
  <verify>
    <automated>grep -c "^## " .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md
    - Contains "Executive Summary" section
    - Contains "Autonomous Mode Lifecycle" section covering the 6-step lifecycle
    - Contains "Hook Points" section with WAITING.json documentation
    - Contains "CLI Capabilities" section with flag table
    - Contains "Workflow Entry Points" section
    - Each section has a Confidence level stated
    - Document is LLM-agnostic (no hardcoded assumptions about Claude as the only backend)
  </acceptance_criteria>
  <done>Sections 1-5 of QUEUE-EXECUTION-DESIGN.md exist with all research findings for QRES-01 organized and confidence-labeled</done>
</task>

<task type="auto">
  <name>Task 2: Write design and safety sections (QRES-02)</name>
  <files>.planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md</files>
  <read_first>
    - .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md
    - .planning/phases/13-queue-execution-research/13-RESEARCH.md
  </read_first>
  <action>
Append these sections to QUEUE-EXECUTION-DESIGN.md, completing the design document:

6. **Strategy A: Per-Item Isolated Execution** (per QRES-02): Full strategy writeup with four subsections: Trigger Mechanism, Session Lifecycle, Artifact Flow, Error Handling. Include pros/cons. State confidence: HIGH.

7. **Strategy B: Persistent Session with Chaining** (per QRES-02): Same four subsections. Include the session_id chaining example. Include pros/cons. State confidence: HIGH.

8. **Strategy Comparison and Recommendation**: Side-by-side comparison table. Recommend Strategy A for v1.3 with the 5 rationale points from research. Recommend Strategy B as future enhancement. State confidence: HIGH.

9. **Safety Requirements** (per QRES-02): Four subsections from research:
   - Timeout Limits table (per-item 30min, per-session 2hr, cost $5)
   - Error Handling table (non-zero exit, hangs, WAITING.json, disk full, API errors)
   - Human Escalation Triggers (checkpoint:human-action, checkpoint:decision, verification failures, consecutive failures, budget exceeded)
   - Max Retry Counts table (per-item 1, per-checkpoint 0, queue-wide 3)
   - Runaway Loop Prevention rules
   State confidence: MEDIUM.

10. **TUI Integration Points**: Map existing codebase components to queue execution needs. Table from research: QueuedAction struct, Queue tab CRUD, suggest_next_commands(), ScreenAction::DispatchAction, Action enum, File watcher, ProjectState. Describe what new Action variants and state fields would be needed. State confidence: HIGH.

11. **LLM-Agnostic Abstraction Layer**: Define the Executor, ExecutionHandle, and ExecutionOptions interfaces from research. Explain how this interface can be implemented for claude -p, future codex, aider, or any LLM CLI. State confidence: MEDIUM.

12. **Open Questions and Future Work**: The 4 open questions from research (bidirectional checkpoint communication, process management across TUI restarts, GSD skill resolution in headless mode, cost visibility). Each with what we know, what's unclear, and recommendation.

13. **Sources**: Organized as Primary (HIGH) and Secondary (MEDIUM) with full paths.

Ensure the document reads as a cohesive design document, not a research dump. An implementer should be able to read it top-to-bottom and understand what to build.
  </action>
  <verify>
    <automated>grep -c "^## " .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md | xargs test 10 -le && grep -c "Confidence:" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md | xargs test 5 -le && grep -q "Strategy A" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md && grep -q "Strategy B" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md && grep -q "Safety Requirements" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md && grep -q "LLM-Agnostic" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - Document has at least 10 top-level sections (## headings)
    - Contains at least 5 Confidence level annotations
    - Contains "Strategy A" with trigger mechanism, session lifecycle, artifact flow, error handling subsections
    - Contains "Strategy B" with the same four subsections
    - Contains strategy comparison with recommendation for Strategy A
    - Contains "Safety Requirements" with timeout, error handling, escalation, retry subsections
    - Contains "TUI Integration Points" mapping to existing codebase
    - Contains "LLM-Agnostic" abstraction layer with Executor interface
    - Contains "Open Questions" section
    - Contains "Sources" section
    - No deferred ideas implemented (no actual code, no container support, no remote execution)
  </acceptance_criteria>
  <done>Complete QUEUE-EXECUTION-DESIGN.md with all 13 sections, covering both QRES-01 and QRES-02 requirements, with confidence levels throughout</done>
</task>

</tasks>

<verification>
1. `wc -l .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md` shows 200+ lines (substantive document)
2. `grep -c "Confidence:" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md` shows 5+ confidence annotations
3. `grep "Strategy A\|Strategy B\|Safety Requirements\|LLM-Agnostic\|Open Questions" .planning/phases/13-queue-execution-research/QUEUE-EXECUTION-DESIGN.md` matches all 5 key sections
4. Document contains no code implementations (this is research/design only)
</verification>

<success_criteria>
- QUEUE-EXECUTION-DESIGN.md exists and is a complete, self-contained design document
- QRES-01 covered: GSD autonomous mode lifecycle, hook points, CLI capabilities all documented with confidence levels
- QRES-02 covered: Two integration strategies with trigger/lifecycle/artifact/error subsections, trade-offs compared, safety requirements enumerated
- Document is LLM-agnostic per user decision
- An implementer can read it and begin v1.3 work without further research
</success_criteria>

<output>
After completion, create `.planning/phases/13-queue-execution-research/13-01-SUMMARY.md`
</output>
