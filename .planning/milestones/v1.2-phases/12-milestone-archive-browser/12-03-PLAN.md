---
phase: 12-milestone-archive-browser
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: []
autonomous: false
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "User sees Archive as the 8th tab in the detail view"
    - "User can browse milestones, drill into phases and files, and view styled markdown"
    - "Esc navigates back through levels without closing the detail view"
    - "Archive data appears after a brief Loading... indicator"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Milestone Archive Browser works end-to-end in the running TUI.

Purpose: Visual confirmation that all four requirements (ARCH-01 through ARCH-04) work correctly before marking the phase complete.

Output: User approval of the archive browsing experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-milestone-archive-browser/12-CONTEXT.md
@.planning/phases/12-milestone-archive-browser/12-UI-SPEC.md
@.planning/phases/12-milestone-archive-browser/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run smoke test</name>
  <files></files>
  <read_first>
    - src/ui/screens/detail.rs (verify Archive tab exists)
    - src/archive.rs (verify render_markdown_lines exists)
  </read_first>
  <action>
Build the project in debug mode and verify it compiles cleanly:
```bash
cargo build 2>&1
```

Run existing tests to ensure no regressions:
```bash
cargo nextest run 2>&1
```

If any compilation errors or test failures, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo build 2>&1 | tail -3 && cargo nextest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo build completes with zero errors
    - cargo nextest run shows all tests passing
  </acceptance_criteria>
  <done>
    Project builds and all tests pass. Ready for visual verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Archive tab</name>
  <files></files>
  <action>
Present the running TUI to the user for interactive verification of all ARCH requirements.

What was built: Complete Milestone Archive Browser tab in the TUI detail view. Users can:
1. See an "8:Archive" tab (8th tab) in the detail view tab bar
2. Browse completed milestones (v1.0, v1.1)
3. Drill into a milestone to see top-level docs and phases
4. Drill into a phase to see artifact files
5. View a file with styled markdown (bold headers, code blocks in DarkGray, bold text)
6. Navigate back with Esc at each level

How to verify — run the TUI:
```bash
cd /home/blk/projects/rust/gsd-manager && cargo run
```

**Test ARCH-01 (Archive tab visible):**
1. Select any project from the dashboard (Enter)
2. Press `8` or Right-arrow to the Archive tab
3. Verify "8:Archive" appears in the tab bar
4. Verify milestone list shows (e.g., "v1.0", "v1.1")

**Test ARCH-02 (Drill-down navigation):**
5. Select v1.0, press Enter
6. Verify breadcrumb shows "Archive > v1.0"
7. Verify list shows top-level docs (ROADMAP.md, etc.) and phases
8. Select a phase, press Enter
9. Verify breadcrumb updates (e.g., "Archive > v1.0 > Phase 01: Core Infrastructure")
10. Verify artifact files listed
11. Press Esc, verify return to phase list
12. Press Esc again, verify return to milestone list

**Test ARCH-03 (Styled markdown):**
13. Drill into a phase, select a PLAN.md or SUMMARY.md file, press Enter
14. Verify file content appears with styled rendering:
    - `#` headers in bold cyan
    - `##` headers in bold
    - `###` headers in bold dim
    - Code blocks in DarkGray
    - `**bold**` text rendered bold
15. Use j/k or Down/Up to scroll through the file
16. Press Esc to go back to file list

**Test ARCH-04 (Async loading and caching):**
17. Switch away from Archive tab (press 1 for Phases)
18. Switch back to Archive tab (press 8)
19. Verify milestones appear immediately (cached, no Loading... flash)
20. Drill into a previously visited milestone
21. Verify phases appear immediately (cached)

**Test edge cases:**
22. At the milestone list, press Esc — should close the detail view (pop to dashboard)
23. Press 8 again on a different project — verify it loads that project's milestones
24. Verify tab bar fits within 80 columns (check for "5:Pipe" and "7:Sess" abbreviations)
  </action>
  <verify>User confirms all tests pass or describes issues</verify>
  <acceptance_criteria>
    - User approves ARCH-01: Archive tab visible as 8th tab
    - User approves ARCH-02: Drill-down navigation works at all levels
    - User approves ARCH-03: Styled markdown rendering is visually correct
    - User approves ARCH-04: Async loading with caching works
  </acceptance_criteria>
  <done>
    User types "approved" confirming all ARCH requirements work correctly in the running TUI.
  </done>
</task>

</tasks>

<verification>
All four ARCH requirements verified through interactive testing.
</verification>

<success_criteria>
- User confirms all ARCH-01 through ARCH-04 requirements work correctly
- No visual glitches in tab bar, breadcrumb, or content rendering
- Navigation (Esc/Enter/j/k) works at all depth levels
- Caching works (no re-loading on tab switch)
</success_criteria>

<output>
After completion, create `.planning/phases/12-milestone-archive-browser/12-03-SUMMARY.md`
</output>
