---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 20
type: execute
wave: 2
gap_closure: true
depends_on: ["21-19"]
files_modified:
  - tests/spawn_seam_guard.rs
  - tests/driver_injection_corpus.rs
  - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md
  - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-18-SUMMARY.md
autonomous: true
requirements: [DRIVE-04, SAFE-07]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Guard nine sees a field declared with NO visibility modifier: `is_field_opener` recognises a bare identifier before `:` as an opener, the scan and the floor both judge it (they share the fn, so they cannot disagree), and a planted bare private `goal_hint: String,` in the synthetic control body is REPORTED — the plant that reproduces pass 6's exact silent signature (`field_lines=12 protected=6 offenders=[]`) committed red against the unfixed scanner first. The limits block names the thirteen-fixture struct-literal bound as what USED to stop this spelling, states it was a coincidence and is no longer load-bearing, and claims nothing beyond the committed controls (pass-7 WR-01; 21-18 truths 1/2 made true)."
    - "An allowlist entry can no longer be silently repurposed, and the pin that certifies it is an EQUALITY: `judge_declaration` judges `names_string_payload` inside the OsString branch instead of returning early, so `claude_args: (Vec<OsString>, String),` is REPORTED (planted red-first); and the `OSSTRING_ALLOWED` integrity pin compares the parsed declared type text for equality against the expected type, not `contains` — a compound type inheriting the suppression is the planted control (pass-7 WR-02; 21-18 truth 1's allowlist half made true)."
    - "The `DEGENERATE` uniqueness guard scans for at least TWO witnesses drawn from different members (including one of the four outside-the-old-ranges members 21-19 added), and its limits block names the under-detection direction in words: a hand copy that avoids every witness literal is invisible to this scan — the message no longer claims tree-wide coverage its scan does not perform (pass-7 WR-04; 21-18 truth 5's message half made true; prohibition-3's own prohibition honoured)."
    - "Guard ten's census rows cannot go stale silently: an assertion checks each `ARGV_ALIAS_ENTRY_POINTS` row's variant token appears in src/cli.rs, so renaming or swapping a variant while keeping the count at 8 breaks the guard loudly, and the limits block names this third approximation alongside the existing two (pass-7 guard-ten Warning)."
    - "The SAFE-07 record's arithmetic is a mechanism, not prose: an ACTIVE (non-ignored) census test in tests/driver_injection_corpus.rs scans its own source and asserts exactly SEVEN `corpus_*_arrives_and_leaves_the_command_unchanged` arms, exactly TEN `#[ignore]`-marked tests, and that `both_arms_of_every_class_comparison_were_really_executed` is among them — so no SUMMARY can miscount the ignored set again without a red test (pass-7 WR-05's arithmetic half)."
    - "What cannot be asserted without a Claude subscription is tracked where it is read, not where it is read once: `deferred-items.md` gains a standing item stating SAFE-07's boundary (all seven class arms, both suppression controls, and the arms' own non-vacuity meta-check) has never executed under any verification pass of this phase, with the exact command and expected output for the human run — and 21-18-SUMMARY's SAFE-07 qualification carries an appended, dated round-7 correction of its eight-arms miscount and its omission of the meta-check (append-only; the original lines stay legible as what round 6 believed)."
    - "The record is corrected where round 6 shipped falsehoods: this plan's SUMMARY carries a Record-corrections table naming 21-17 truths 1, 2, 6 and 8 and 21-18 truths 1, 2 and 5 FALSE AS SHIPPED, each with pass 7's measurement and the statement that is true after round 7 — a reader of round 8 inherits measurements, not certifications (the practice pass 7 called the best thing round 6 did, continued)."
    - "A `--max-escalations` value at or above the run's RESOLVED step cap is refused at the seam and a value below it is accepted — both directions pinned and green (`driver_escalation_cap` 8/8), reconfirmed against this plan's final tree (probe: DRIVE-04 boundary)."
    - "The escalation counter's precision holds: the decomposition consultation is counted against the same cap as every other consultation — no consultation path is exempt — reconfirmed by the existing cap pins passing unmodified against this plan's final tree (probe: DRIVE-04 precision)."
    - "Third-party text from `.planning/` files reaches a prompt only inside the labelled untrusted boundary, and arrival is asserted before influence: `driver_injection_corpus`'s structural arrival-before-influence pins pass unmodified against this plan's final tree; the behavioural half remains the tracked human item above, stated as unverified rather than certified (probe: SAFE-07 boundary)."
    - "The untrusted boundary's precision holds: only the enumerated strings cross — never a whole file — reconfirmed by guard seven passing inside this plan's own spawn_seam_guard run (probe: SAFE-07 precision)."
  prohibitions:
    - statement: "MUST NOT certify any guard or mechanism claim with a grep for a sentence of prose; every claim this plan makes about what a scan reports is certified by a planted-defect control committed red-first against the unfixed scanner, calling the SAME extracted fns the live assertion consumes."
      category: transparency
    - statement: "MUST NOT leave any guard doc, limits block, or plan truth claiming a bound no committed control goes red for; where a scan cannot deliver a bound, the residual is named in the doc WITH its failure direction — this is the fifth consecutive round's defect and this plan exists to end it, so it applies to this plan's own diff most of all."
      category: transparency
    - statement: "MUST NOT rewrite or delete any line of a prior SUMMARY: corrections to 21-18-SUMMARY.md are append-only, dated, and quote the text they correct — the record's history stays legible."
      category: transparency
    - statement: "MUST NOT paste a raw invisible, bidi, tag, or variation-selector character into any file; every such character appears exclusively as a \\u{...} escape."
      category: process
    - statement: "MUST NOT flip any .planning/REQUIREMENTS.md requirement to [x]/Complete in any commit of this plan; requirement status is decided exclusively by a passed verification (828d7cc, 0c4f712 — twice reverted, four rounds held)."
      category: process
  artifacts:
    - path: "tests/spawn_seam_guard.rs"
      provides: "is_field_opener seeing bare fields; judge_declaration judging payloads inside the OsString branch; equality-based allowlist integrity pin; two-witness DEGENERATE scan with named under-detection direction; guard-ten row-existence assertion; honest limits blocks"
      contains: "degenerate_witnesses"
    - path: "tests/driver_injection_corpus.rs"
      provides: "the active ignored-set census: seven arms, ten ignored, meta-check named"
      contains: "seven"
    - path: ".planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md"
      provides: "the standing SAFE-07 boundary-never-executed human-verification item with exact command and expected output"
      contains: "driver_injection_corpus"
  key_links:
    - from: "tests/spawn_seam_guard.rs (planted bare-field and compound-OsString controls)"
      to: "tests/spawn_seam_guard.rs (is_field_opener / judge_declaration)"
      via: "every planted spelling exercises the SAME extracted fns the live assertion consumes; each committed red before its fix"
      pattern: "is_field_opener"
    - from: "tests/driver_injection_corpus.rs (ignored-set census)"
      to: "tests/driver_injection_corpus.rs (its own source text)"
      via: "an active test counting the arms and ignore attributes the SUMMARY's arithmetic describes"
      pattern: "both_arms_of_every_class_comparison_were_really_executed"
    - from: "this plan's SUMMARY (Record corrections)"
      to: "21-17/21-18 truths pass 7 measured false"
      via: "measurement + now-true statement per truth, so round 8 inherits no certification round 7 falsified"
      pattern: "Record corrections"
---

<objective>
Close pass-7's gap 3: the anti-recurrence machinery still claims more than its scans perform —
the fifth consecutive round in which the overclaim lives in the artifact written to end
overclaiming. Every fix here follows the one method that has survived attack (plant the
defect, feed it to the same extracted fn, commit the red, fix, watch it go green), and every
doc change moves a claim DOWN to what a committed control measures.

Measured at pass 7, each confirmed by the verifier reading control flow, none taken on the
reviewer's word:

- **WR-01:** `is_field_opener` accepts only `pub `/`pub(`, so a field with NO visibility
  modifier is skipped by both the scan and the floor — `field_lines=12 protected=6
  offenders=[]`, pass 6's exact signature, in a spelling in neither the SEEN nor the SILENT
  list, bounded only by thirteen struct-literal fixture crates the SUMMARY reports as retired.
- **WR-02:** `judge_declaration`'s OsString branch returns before `names_string_payload` runs,
  and the allowlist integrity pin is a `contains` — so `claude_args: (Vec<OsString>, String),`
  is silent AND passes the pin that exists to catch exactly that.
- **WR-04:** the `DEGENERATE` uniqueness scan detects a hand copy only via ONE witness
  literal while its message claims every blank-shape pin consumes the const.
- **WR-05:** 21-18-SUMMARY's SAFE-07 qualification says eight class arms; there are seven,
  and the tenth ignored test — the arms' own non-vacuity meta-check — is omitted from the
  list. Criterion 4 remains behaviour-unverified; what CAN be asserted without a subscription
  is the composition of the ignored set, so that becomes an active census test, and the
  boundary-never-executed fact moves into deferred-items.md where it is tracked rather than
  read once.

This plan runs in wave 2 because 21-19 grows `DEGENERATE` to ten members (the two-witness
scan draws one witness from the new members) and because both plans' whole-suite gates should
not race in the same worktree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-PREMISES.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-REVIEW.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md
</context>

<tooling_note>
**`rtk` filters build/test/grep output.** Every count-bearing or presence-bearing check runs
`rtk proxy <cmd>`. The clippy gate is `rtk proxy cargo clippy --lib -- -D warnings`. The
whole-suite gate is `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2`
(documented flakes: `driver_reattach` ×2, `envelope_tracer` relocated-stub — re-run the binary
alone before reporting a failure). Raw invisible characters never appear in any file — escapes
only.
</tooling_note>

<edge_probe_audit>
Spec-less probe fallback is ACTIVE; the 7-row disposition table lives in 21-19-PLAN.md's
`<edge_probe_audit>` (7 surfaced = 4 authored + 3 flagged, no silent drops). This plan
authors the four resolvable rows as explicit re-confirmation truths — DRIVE-04 boundary,
DRIVE-04 precision, SAFE-07 boundary, SAFE-07 precision (truths 8-11 above) — each verified
by named existing suites passing against this plan's final tree, exactly as round 6 authored
them, with SAFE-07 boundary's behavioural half explicitly carried as the tracked human item
rather than certified. The three unclassified rows (DRIVE-01, DRIVE-03, SAFE-08) are flagged
assumptions in 21-19. Recalled prohibitions are authored descriptor-less in this plan's
`must_haves.prohibitions` block.
</edge_probe_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future code changes → guard scans | the guards exist to catch a seventh argv field, a repurposed allowlist entry, a re-copied blank list |
| guard docs / SUMMARYs → future readers | five rounds inherited exactly one wrong assumption each from an overclaiming artifact |
| `#[ignore]`d suite → verification passes | SAFE-07's behavioural proof cannot run without a subscription; the record must say so where it is read |

## STRIDE Threat Register (ASVS L1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-20-01 | Tampering | a seventh `DriveArgs` field declared bare (no `pub`) carrying a raw payload, silent to scan and floor | high | mitigate | Task 1 widens `is_field_opener` to bare identifiers; the plant is committed red against the unfixed scanner; the floor shares the fn so it cannot diverge. |
| T-21-20-02 | Tampering | an `OSSTRING_ALLOWED` name repurposed to smuggle a raw `String` payload past the deny-by-default rule | high | mitigate | Task 1 judges payload types inside the OsString branch and converts the integrity pin to an equality; the compound-type plant is committed red. |
| T-21-20-03 | Repudiation | a future hand-copied `DEGENERATE` subset invisible to a one-witness scan while the message claims tree-wide coverage | medium | mitigate + disclose | Task 2: two witnesses from different members; the under-detection direction named in the limits block — the message shrinks to what the scan performs. |
| T-21-20-04 | Repudiation | the phase record certifying counts and bounds pass 7 measured false | medium | mitigate | Task 3: active ignored-set census; append-only dated correction to 21-18-SUMMARY; Record-corrections table; deferred-items tracking for the human-only item. |

**Package legitimacy:** no `cargo add`, no `Cargo.toml` change.
</threat_model>

<decisions>
## Decisions and reversibility (REVERSIBILITY_GATES=true)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| D-20-1 | `is_field_opener` widens to bare identifiers before `:` | **reversible** | Test-only scanner change; over-detection direction (a non-field line shaped like `word:` inside the struct body) is bounded by the scan running only between the struct's braces and by the planted-control suite staying green. |
| D-20-2 | `judge_declaration` judges payload types inside the OsString branch; integrity pin becomes equality on parsed type text | **reversible** | Test-only; the false-positive direction keeps its existing NOT-reported controls (`Vec<OsString>` alone, `Option<PathBuf>`). |
| D-20-3 | Corrections to 21-18-SUMMARY are append-only and dated | **reversible** | The alternative — editing the original lines — would make the record's history illegible, which is this phase's own named failure mode. Prohibition 3 pins it. |
| D-20-4 | SAFE-07's boundary-never-executed fact moves to deferred-items.md as a standing item | **reversible** | The verifier's explicit instruction: tracked where it is read every round, not stated once in a SUMMARY qualification. Does NOT close criterion 4 — the item says so in its own text. |

No decision is `one-way`; no `checkpoint:decision` gate is required.
</decisions>

<named_shape_audit>
## Named-shape audit (this plan's slice)

| # | Named shape (source) | Owner | Covered by |
|---|---|---|---|
| 1 | Bare (no-`pub`) field silent to scan AND floor (pass-7 WR-01) | 21-20 T1 | opener widening + red-committed plant + limits rewrite naming the retired coincidence |
| 2 | Compound `OsString` declaration suppressed wholesale; `contains` pin passes (pass-7 WR-02) | 21-20 T1 | in-branch payload judgment + equality pin + red-committed plant |
| 3 | One-witness `DEGENERATE` scan under an every-pin message (pass-7 WR-04) | 21-20 T2 | two witnesses + named under-detection direction |
| 4 | Guard-ten rows can describe removed/renamed variants at a stable count (pass-7 Warning) | 21-20 T2 | row-token existence assertion + third limits entry |
| 5 | SAFE-07 qualification miscounts arms and omits the meta-check (pass-7 WR-05) | 21-20 T3 | active ignored-set census + append-only dated correction |
| 6 | SAFE-07 boundary never executed under verification (pass-7 behavior_unverified) | 21-20 T3 | deferred-items standing item with exact command; NOT claimed closed |
| 7 | 21-17 truths 1/2/6/8 and 21-18 truths 1/2/5 false as shipped | 21-20 T3 | Record-corrections table in the SUMMARY |
| 8 | The class subset and sampling defects themselves | 21-19 | delegated — see 21-19's audit |
</named_shape_audit>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): guard nine's two blind spots — plant both, commit red, then make the scanner see</name>
  <read_first>
    - tests/spawn_seam_guard.rs (lines 2740-3000: the guard-nine header, raw_string_argv_fields, judge_declaration, names_token, OSSTRING_ALLOWED, is_field_opener; lines 3260-3380: the floor and the allowlist integrity pin; the existing planted-control suite between them — the plants join it and must call the SAME fns)
    - src/driver/mod.rs (the real DriveArgs declaration — the body the synthetic controls mirror)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md (gap 3's control-flow traces: the exact skip at the pending==None branch and the early return)
  </read_first>
  <action>
    RED FIRST, committed before the fix, both plants in one commit marked
    `#[ignore = "red: pass-7 WR-01/WR-02 plants; un-ignored in the fix commit"]` with verbatim
    red output in doc comments and the commit message:

    Plant 1 (WR-01): `the_scanner_reports_a_bare_private_field` — feed the extracted
    `raw_string_argv_fields` a synthetic DriveArgs body holding the real declarations plus
    `goal_hint: String,` with NO visibility modifier, assert the offender list names
    `goal_hint`. At HEAD this FAILS: `is_field_opener` requires `pub `/`pub(`, so the line is
    skipped — record the exact silent signature (offenders=[]).

    Plant 2 (WR-02): `an_allowlisted_name_cannot_carry_a_raw_payload_beside_its_osstring` —
    feed a synthetic body where the `claude_args` declaration reads
    `pub claude_args: (Vec<OsString>, String),` and assert it IS reported. At HEAD this FAILS:
    the OsString branch returns before names_string_payload runs.

    THEN FIX in the next commit, un-ignoring both:

    (a) Widen `is_field_opener`: an opener is `pub `/`pub(` as today, OR a bare identifier
    immediately followed by `:` — first token matches an identifier pattern
    ([A-Za-z_][A-Za-z0-9_]* — implement with chars(), no regex dependency) and the line
    contains `:` before any `//`. Because the scan runs only on lines between the struct's
    braces, match arms and statements are out of scope by construction; say so in the fn doc
    with the over-detection direction (loud — a reported non-field would fail visibly in the
    offender list, not pass silently).

    (b) In `judge_declaration`, remove the early `return` semantics: inside the
    `names_token(type_text, "OsString")` branch, after the allowlist check, ALSO evaluate
    `names_string_payload(type_text)` and report a declaration that names both `OsString` and
    a raw payload type regardless of allowlist membership (an allowlisted name suppresses the
    OsString-presence report only, never a payload-type report).

    (c) Convert the allowlist integrity pin from `contains` to EQUALITY: parse the declared
    type text for each `OSSTRING_ALLOWED` name from the REAL src/driver/mod.rs body
    (trimmed, trailing comma/comment stripped) and assert it equals the expected type exactly
    — `claude_args` → `Vec<OsString>`, `claude_program` → `Option<PathBuf>` — with a failure
    message naming the repurposing harm.

    (d) Update guard nine's SEEN/limits lists: add the bare-field spelling to the SEEN list
    with its plant's name; REWRITE the limits entry that leaned on the thirteen-fixture
    struct-literal bound — state that bound was a coincidence, that pass 7 measured it
    load-bearing for exactly this spelling, and that it is no longer relied on because the
    opener now sees the spelling; keep the genuinely-remaining silents (macro-expanded
    declarations, type aliases) each with direction and bounding assertion. Verify the floor's
    numbers still hold with the widened opener against the real body (field_lines should now
    count identically since all real fields carry pub — assert that expectation explicitly so
    a drift is loud).

    (e) Keep every existing NOT-reported control green (`Vec<OsString>` alone,
    `Option<PathBuf>`, the false-positive direction) — the widening must not flip them.
  </action>
  <acceptance_criteria>
    - The red commit precedes the fix commit in `git log`, both plants' verbatim red output
      quoted in doc comments, commit message, and the SUMMARY.
    - `the_scanner_reports_a_bare_private_field` and
      `an_allowlisted_name_cannot_carry_a_raw_payload_beside_its_osstring` pass un-ignored,
      calling `raw_string_argv_fields`/`judge_declaration` — not a re-implemented loop
      (`rtk proxy grep -n "raw_string_argv_fields\|judge_declaration" tests/spawn_seam_guard.rs`
      shows the plants among the callers).
    - The integrity pin asserts equality: `rtk proxy grep -n "contains(expected_type\|== expected"
      tests/spawn_seam_guard.rs` shows the equality form and no `contains` on the declared
      type text in that pin.
    - The SEEN list names the bare-field spelling; the limits block no longer cites the
      thirteen-fixture bound as a bound (`rtk proxy grep -n "coincidence" tests/spawn_seam_guard.rs`
      ≥ 1 in guard nine's header region).
    - All pre-existing guard-nine controls pass unchanged
      (`rtk proxy cargo test --test spawn_seam_guard` → 0 failures).
    - `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat); `rtk proxy cargo clippy --lib -- -D warnings` clean.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: the uniqueness guard's honest message and guard ten's stale-row assertion</name>
  <read_first>
    - tests/spawn_seam_guard.rs (lines 3440-3560: degenerate_witness, executable_hits, the_degenerate_payload_set_is_spelled_in_exactly_one_place and its message; lines 3590-3760: guard ten's table, scan, and limits block)
    - src/test_support.rs (as grown by 21-19 — ten members; the witnesses draw from here BY VALUE, spelled as escapes, never by importing the const, so the scan cannot agree with a truncated const)
    - src/cli.rs (the eight variant declarations guard ten's rows must name)
  </read_first>
  <action>
    (a) Replace `degenerate_witness()` with `degenerate_witnesses() -> [String; 3]` returning
    three witness literals assembled at runtime from halves (the existing anti-self-match
    idiom), drawn from DIFFERENT members of the ten: the "\u{200b}" member, the "\n  \n"
    member, and one of 21-19's outside-the-old-ranges members ("\u{202e}"). The uniqueness
    test scans for EACH witness and fails if any appears in executable code outside
    src/text.rs's additive list and the allowed sites it names today.
    (b) Rewrite the guard's failure message and header to claim exactly what it performs:
    the scan detects a hand copy that carries ANY of three named witnesses; a copy avoiding
    all three is invisible — under-detection, silent, named with that direction in the limits
    block (this sentence is the one prohibition-3 requires and pass 7 found missing). Keep
    the message's instruction (consume `test_support::DEGENERATE`) — it is the fix, not the
    claim.
    (c) Guard ten: add `every_census_row_names_a_variant_that_still_exists` — for each row in
    `ARGV_ALIAS_ENTRY_POINTS`, take the variant token after `::` (e.g. `Add`, `PrePush`) and
    assert it appears as a variant declaration in the scanned src/cli.rs lines; add the third
    approximation to guard ten's limits block (the row check matches variant NAMES, so a
    renamed-and-re-added variant with the same name but different meaning is invisible —
    under-detection, silent, bounded by the judge strings' own tests).
    (d) Confirm no standing violation: `rtk proxy grep -rn "for blank in \[" src/ tests/`
    still returns only src/text.rs's additive list (which after 21-19 must itself be checked:
    if 21-19's diff altered it, re-verify it remains additive, not a DEGENERATE subset).
  </action>
  <acceptance_criteria>
    - `degenerate_witnesses` returns three witnesses from three different members including
      one outside-the-old-ranges member; the uniqueness test iterates all three
      (`rtk proxy grep -n "degenerate_witnesses" tests/spawn_seam_guard.rs` ≥ 2 hits:
      definition + consumer).
    - The limits block names the under-detection direction in words
      (`rtk proxy grep -n "avoid" tests/spawn_seam_guard.rs` ≥ 1 in the uniqueness guard's
      header region, or equivalent wording — verified by reading, quoted in the SUMMARY).
    - `every_census_row_names_a_variant_that_still_exists` passes and goes red when fed a
      synthetic table row naming a nonexistent variant (planted control via the same
      extracted row-checking fn).
    - `rtk proxy cargo test --test spawn_seam_guard` → 0 failures;
      `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: the record — the ignored-set census, the dated correction, the deferred item, and the audits</name>
  <read_first>
    - tests/driver_injection_corpus.rs (whole file — the seven arms, two suppression controls, the meta-check, and the twelve structural pins; the census test lands here and scans this file)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-18-SUMMARY.md (lines 380-405: the SAFE-07 qualification whose arithmetic the correction quotes)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md (the register the standing item joins; follow its established format)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md (frontmatter behavior_unverified_items — the exact command and expected output the deferred item must carry)
  </read_first>
  <action>
    (a) In tests/driver_injection_corpus.rs add an ACTIVE test
    `the_ignored_set_is_seven_arms_two_controls_and_their_own_meta_check`: read this file's
    own source (env CARGO_MANIFEST_DIR join the test path), count fn names matching the
    corpus_ prefix with the _arrives_and_leaves_the_command_unchanged suffix → assert exactly
    7; count `#[ignore` attribute occurrences → assert exactly 10; assert the source contains
    `both_arms_of_every_class_comparison_were_really_executed` and that an `#[ignore`
    attribute precedes it. Failure message: the SUMMARY arithmetic for SAFE-07 derives from
    THIS test's counts — update both together. Doc: this census is what a subscription-less
    verification CAN assert; the behaviour itself is the deferred human item, by name.
    (b) Append to 21-18-SUMMARY.md — never editing existing lines — a dated section
    `## Correction (round 7, 2026-08-23)`: quote the eight-arms sentence, state the measured
    truth (SEVEN class arms; the tenth ignored test is
    `both_arms_of_every_class_comparison_were_really_executed`, the arms' own non-vacuity
    check, omitted from the original list), and point at the new census test and the
    deferred item.
    (c) Append to deferred-items.md a round-7 section with a standing item: "SAFE-07's
    boundary has never executed under any verification pass of this phase" — all ten
    behavioural tests `#[ignore]`d (seven arms, two suppression controls, the meta-check);
    the human run is `cargo test --test driver_injection_corpus -- --ignored --nocapture`
    with an authenticated `claude` CLI, expected 10 passed with arrival asserted before
    influence and the CLI version recorded beside the result; last executor-claimed live run
    21-05-SUMMARY.md:514 (claude 2.1.238), never reproduced under verification. State
    explicitly this item does NOT close criterion 4 and must be re-surfaced every round until
    a human run is recorded.
    (d) This plan's SUMMARY carries: the Record-corrections table — 21-17 truths 1, 2, 6, 8
    and 21-18 truths 1, 2, 5, each with FALSE-AS-SHIPPED, pass 7's measurement, and the
    statement true after round 7 (21-17's rows point at 21-19's mechanisms; 21-18's rows at
    this plan's) — the prohibition audit table (all five prohibitions, command + output, no
    prose greps), and the named-shape audit table (rows 1-7 resolved with covering test
    names/locations, row 8 delegated to 21-19).
  </action>
  <acceptance_criteria>
    - `the_ignored_set_is_seven_arms_two_controls_and_their_own_meta_check` is ACTIVE (not
      ignored) and passes under `rtk proxy cargo test --test driver_injection_corpus`
      (12 + 1 active, 10 ignored: `rtk proxy cargo test --test driver_injection_corpus -- --list`
      counts quoted in the SUMMARY).
    - 21-18-SUMMARY.md ends with the dated correction section; `rtk proxy git diff` for that
      file shows additions only, zero deletions.
    - deferred-items.md carries the standing SAFE-07 item with the exact command, expected
      output, and the does-NOT-close-criterion-4 sentence.
    - The SUMMARY carries all three tables (Record corrections, prohibition audit,
      named-shape audit) with quoted command outputs.
    - `rtk proxy git log --format=%H -- .planning/REQUIREMENTS.md` shows no commit of this
      plan.
    - `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat); `rtk proxy cargo clippy --lib -- -D warnings` clean.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `is_field_opener` widened to bare identifiers (test scanner); plants
  `the_scanner_reports_a_bare_private_field`,
  `an_allowlisted_name_cannot_carry_a_raw_payload_beside_its_osstring`
- `judge_declaration` judging payload types inside the OsString branch; equality-based
  `OSSTRING_ALLOWED` integrity pin
- `degenerate_witnesses()` (three witnesses, three members); rewritten uniqueness-guard
  message and limits block
- `every_census_row_names_a_variant_that_still_exists` (guard ten) + third limits entry
- `the_ignored_set_is_seven_arms_two_controls_and_their_own_meta_check` (active census in
  tests/driver_injection_corpus.rs)
- 21-18-SUMMARY.md dated append-only correction; deferred-items.md standing SAFE-07 item
</artifacts_produced>

<verification>
1. `rtk proxy cargo test --test spawn_seam_guard` and
   `rtk proxy cargo test --test driver_injection_corpus` → 0 failures;
   `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures (flakes
   excepted); `rtk proxy cargo clippy --lib -- -D warnings` clean.
2. Red-arm evidence: the plants' red commit precedes the fix commit; verbatim red output in
   the SUMMARY.
3. Pass 7's two guard reproductions re-run against the fixed scanner and quoted: the bare
   field is reported; the compound OsString declaration is reported and the equality pin
   refuses the repurposed type.
4. The record chain is complete: census test active and green; 21-18-SUMMARY correction
   appended (additions only); deferred-items standing item present; Record-corrections table
   in this plan's SUMMARY covering all seven falsified truths.
5. No commit touches .planning/REQUIREMENTS.md.
</verification>
