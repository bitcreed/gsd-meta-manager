---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 28
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/ui/screens/driver.rs
  - src/ui/screens/driver_confirm.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/render_escape_guard.rs
autonomous: true
requirements: [SAFE-07, DRIVE-01]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CR-02 is closed AT THE TYPE on the surface it matters most: `DriverOutputLine::text` stops being a `String` and becomes `crate::text::Untrusted`, so `output_line`'s `Span::styled(line.text.clone(), ..)` stops compiling until it is `shown()`. This is the pane that displays the LLM's own prose, in a phase titled Prompt-Injection Hardening, and it was reachable by `U+E0000..U+E007F` — the tag block this phase itself names as the ASCII-smuggling carrier — because `push_record` composed only `sanitize_render_line`, which answers the CONTROL class alone. By this tree's own per-widget measurement the tag block SURVIVES a `Paragraph`, so it reached a cell; it was not a theoretical risk."
    - "The composition is proven EQUAL to the one it replaces rather than assumed to be: `Untrusted::from_untrusted_source(sanitize_render_line(raw)).shown().to_string()` equals `shown_capped(raw)` for every fixture in `test_support::LOOK_ALIKE_PAIRS` and for every control-class fixture the tree already carries, pinned by a committed test. Without it the retype would silently change what the pane shows — a second control-strip pass over an already-stripped string, and a cap applied before rather than after the escape — and nothing would go red for the difference."
    - "The two remaining sites on the same path are closed at the render, and the difference between a call and a type is DISCLOSED rather than smoothed. The injection rows (`src/ui/screens/driver.rs:1684`, over `InboxMessage::text` read from `inbox.jsonl`) and the dry-run preview (`:1088`, over report text the function's own doc says interpolates paths and branch names read from the project) compose both classes through `shown_capped`. Their carriers are NOT retyped, so those two are held by a call plus a probe rather than by the compiler, and the limits block says so with that failure direction."
    - "The probe hole that hid all three for nine rounds is CLOSED, not re-disclosed. `probe_ctx` populates `ctx.driver_output`, `cache.driver_journal` and `cache.driver_inbox` with the hostile identity, and a `DETAIL_SUB_STATES` entry drives `driver_dry_run`, so the Driver tab renders its output pane, its injection rows and its dry-run preview under probe on every run instead of `no_runs_lines` — which was the ONE already-composed site on the path and is exactly why nine rounds of green meant nothing here. Arrival is recorded per state in `DETAIL_TAB_ARRIVAL` and asserted as a set equality against the `chrome_ctx` baseline, so a populated cache the render never reads is reported by name rather than counted as coverage."
    - "`sanitize_render_line`'s doc stops asserting something false in the same commit that makes it true. Its justification sentence claims that `driver.rs` and `driver_confirm.rs` compose both classes; that sentence was false for four sites and is what let the gap survive a round whose whole subject was this composition. It is corrected with the falsified text quoted verbatim and dated, and what replaces it states what a reader can now check rather than what the author intended."
    - "WR-06 is closed with the same composition and a fixture that can go red for it: `render_disclosure` (`src/ui/screens/driver_confirm.rs:162-167`) renders `input.path` and `digest` — both read out of the recorded opt-in block in the user's `config.json`, a file this build does not exclusively own — through both classes, and the probe's fixture gains a `PromptInput` whose `path` and `digest` carry the probe identity. Today the two opt-in prompts are probed only with the shipped `DISCLOSED_PROMPT_INPUTS` defaults, every one of which is an authored `&'static str`, so no committed control could go red there."
    - "IN-01 is closed by making `shown_capped`'s completeness claim CHECKABLE instead of restating it. Its doc says every other site on this path was doing the same thing wrong; three sites in the same file were not converted, which is precisely the sentence that stops a reader re-checking. It is replaced by a committed census asserting that an executable call to `sanitize_render_line` in `src/ui/screens/driver.rs` and `src/ui/screens/driver_confirm.rs` appears only inside a composition — `shown_capped`'s own body, or a logical line that also names `display_identity` or `render_for_terminal`. Observed red by planting an unconverted call."
    - statement: "SAFE-07 boundary (edge-probe row 1), reconfirmed with explicit evidence rather than by inference: `.planning/` content crosses into a prompt only through the enumerated, labelled strings, and the structural half is pinned by `tests/spawn_seam_guard.rs` passing unmodified at its HEAD count. This plan does not touch that file; the reconfirmation is a re-run showing this plan's diff did not move the boundary, and the behavioural half remains ROADMAP criterion 4, which is permanently agent-unclosable and against which this plan does no work."
      verification: backstop
    - statement: "SAFE-07 precision (edge-probe row 2), reconfirmed with explicit evidence: only the enumerated strings cross the boundary, never a whole file, and this plan's diff adds no new channel — every value it touches moves from a `String` to an `Untrusted` in the same struct or gains an escape at a render site, changing what a RENDER may do with it and changing nothing about what crosses into a prompt. Certified by `tests/spawn_seam_guard.rs` and by `tests/driver_injection_corpus.rs`'s active structural pins passing at their HEAD counts, both quoted."
      verification: backstop
    - "Every mechanism this plan introduces was observed RED before green and the red is committed verbatim: (1) populating `ctx.driver_output` drives the probe red for the Driver tab's output pane before the retype lands; (2) the injection-row and dry-run-preview fixtures each drive their own red, separately where they separate; (3) the WR-06 fixture drives `DriverConfirmScreen` red before `render_disclosure` composes; (4) the IN-01 census goes red against a planted unconverted call. Four plants, four reds, `rtk proxy git status --porcelain` clean after each."
    - "The four PRE-EXISTING clippy lints are untouched and that is MEASURED: this plan edits neither `src/browser.rs` nor `src/project_creator.rs`, `rtk proxy git diff --stat` names neither, and `rtk proxy cargo clippy --all-targets -- -D warnings` still reports exactly FOUR lints of the same two kinds in the same two files. Three would mean one was silently fixed and is a failure of this plan just as five would be."
  prohibitions:
    - statement: "MUST NOT let the hardening silently delete the capability. The driver output pane exists to show the operator what the agent is doing; a composition that truncates it further, drops its markers, or turns ordinary prose into a wall of `U+XXXX` markers has removed the feature while passing the escape assertion. The composition-equality pin over the existing fixtures is what makes this checkable, and any visible change to what a CLEAN line renders as is a failure of this plan rather than an acceptable cost."
      category: values
    - statement: "MUST NOT count a populated cache as coverage. A cache this plan fills whose render path never reads it makes every new assertion pass by silence — the sharpest failure shape this phase has, found one level up in round 9. Every state added here records arrival against the `chrome_ctx` baseline, and a state that does not arrive is REPORTED by name with which of the two causes applies, never quietly left in the fixture."
      category: transparency
    - statement: "MUST NOT edit `src/ui/screens/detail.rs`, `src/text.rs`, `src/ui/screens/normal.rs`, `src/ui/screens/add_project.rs`, `src/ui/screens/create_project.rs`, `src/ui/screens/delete_confirm.rs`, `src/ui/screens/driver_start.rs`, `src/ui/screens/driver_inject.rs`, `src/ui/screens/enqueue.rs`, `src/ui/screens/queue_delete_confirm.rs`, `src/ui/mod.rs`, `src/ui/roadmap_widget.rs`, `src/error.rs`, `src/state_reader/backlog.rs` or `src/test_support.rs`. Those belong to 21-27 and 21-29, which run in PARALLEL worktrees in this same wave. `src/test_support.rs` in particular is READ-AND-IMPORT ONLY here: hostile fixtures are drawn from `LOOK_ALIKE_PAIRS` by import and never respelled, and 21-29 adds a member to that list in this same wave. If a change here forces a change in any of them, STOP and report it as a wave-conflict finding rather than editing across the fence."
      category: process
    - statement: "MUST NOT leave a bare `crate::text::display_identity` call in `src/ui/screens/driver.rs` or `src/ui/screens/driver_confirm.rs`. 21-29 lands a census in this same wave asserting that an executable call to that function under `src/ui/` either composes with `sanitize_render_line` on the same logical line or has become `render_for_terminal`. `driver_confirm.rs:275` is bare at HEAD and this plan converts it; every other call in these two files must be checked against that rule before this plan reports done, because after the worktree merge 21-29 cannot edit these files to fix them."
      category: process
    - statement: "MUST NOT retype `InboxMessage::text` or `DryRunPreview::report`. Those carriers reach `src/journal/inbox.rs` and `src/app.rs`, which belong to no plan in this wave; retyping them would widen this diff across the fence and would trade a bounded residual for a wave conflict. The two sites are closed by a CALL, that difference from the type-level closure is disclosed in the limits block with its failure direction, and the carriers are named in `deferred-items.md` by 21-30."
      category: process
    - statement: "MUST NOT re-disclose the Driver tab's probe hole in place of closing it. LIMIT 1 currently names `driver_dry_run` as a remaining unprobed STATE and does not name the output pane, the journal or the inbox as unescaped SITES. The rewrite must be strictly NARROWER than the wording it replaces and must quote that wording verbatim beside it, or it is not a closure."
      category: transparency
    - statement: "MUST NOT fix, reformat or otherwise disturb the four pre-existing clippy lints in `src/browser.rs` and `src/project_creator.rs`; `rtk proxy git diff --stat` for this plan must name neither file, and `cargo clippy --all-targets` must still report exactly four lints of the same two kinds in the same two files."
      category: process
    - statement: "MUST NOT plan, execute or claim any work against ROADMAP success criterion 4. `tests/driver_injection_corpus.rs` and `tests/spawn_seam_guard.rs` are RUN by this plan — they are its two SAFE-07 backstops — and EDITED by it under no circumstance."
      category: process
    - statement: "MUST NOT paste a raw invisible, bidi, tag, variation-selector or control character into any file; every such character appears exclusively as a `\\u{...}` escape, and hostile fixtures are drawn BY IMPORT from `test_support::LOOK_ALIKE_PAIRS` rather than respelled."
      category: process
    - statement: "MUST NOT flip any `.planning/REQUIREMENTS.md` requirement to `[x]`/Complete in any commit of this plan; requirement status is decided exclusively by a passed verification."
      category: process
    - statement: "MUST NOT certify any claim in this plan's SUMMARY with a grep for a sentence of prose, and MUST NOT inherit any count from this plan's own text, from `21-REVIEW.md` or from `21-VERIFICATION.md`; every number is re-measured under `rtk proxy` against the final tree."
      category: transparency
  artifacts:
    - path: "src/ui/screens/mod.rs"
      provides: "DriverOutputLine::text typed as crate::text::Untrusted, wrapped once in push_record; sanitize_render_line's false composition claim corrected with the falsified text quoted"
      contains: "Untrusted"
    - path: "src/ui/screens/driver.rs"
      provides: "output_line rendering line.text.shown(); the injection rows and the dry-run preview composed through shown_capped; shown_capped's completeness claim replaced by the census that checks it"
      contains: "shown()"
    - path: "src/ui/screens/driver_confirm.rs"
      provides: "render_disclosure composing both classes over input.path and digest; the bare display_identity call converted to render_for_terminal"
      contains: "render_for_terminal"
    - path: "src/ui/screens/render_escape_guard.rs"
      provides: "probe_ctx populating driver_output, driver_journal and driver_inbox; a driver_dry_run DETAIL_SUB_STATES entry; a PromptInput fixture carrying the identity; DETAIL_TAB_ARRIVAL rows for each new state; LIMIT 1 rewritten narrower with the wording it replaces quoted"
      contains: "driver_output"
  key_links:
    - from: "a run's journal on disk (exec_event text — the agent's own prose, read back by crate::app::driver_line_for_record)"
      to: "the Driver tab's output pane cells"
      via: "DriverOutputLine::text is crate::text::Untrusted, which has no Display and no Into<Cow<str>>, so Span::styled is a compile error until it is shown()"
      pattern: "shown()"
    - from: "inbox.jsonl's InboxMessage::text and the dry-run report's interpolated paths and branch names"
      to: "the injection rows and the dry-run preview"
      via: "shown_capped at the render site — a CALL, not a type, disclosed as such with its failure direction"
      pattern: "shown_capped"
    - from: "src/ui/screens/render_escape_guard.rs (probe_ctx and DETAIL_SUB_STATES)"
      to: "the Driver tab's output pane, injection rows and dry-run preview"
      via: "driver_output, driver_journal, driver_inbox and driver_dry_run populated with the hostile identity, with arrival recorded per state against the chrome_ctx baseline"
      pattern: "driver_output"
---

<objective>
**Round 10, wave 1. The pane that shows the model's own words was the one pane that was not
escaped.**

`shown_capped` was introduced in round 9 for exactly this defect, with exactly this argument
written in its doc: `sanitize_render_line` alone answers only the CONTROL class, so `U+202E`,
`U+00AD` and the `U+E0000..U+E007F` tag block pass through untouched into a `Paragraph`. It
was applied to six sites. **Three sites on the same path were left behind, and the largest of
them is the live driver output pane** — the surface that displays the LLM's own prose, in a
phase named for prompt-injection hardening, carrying the exact tag-block character this phase
names as the reason the render surface needed hardening at all.

Verification pass 10 confirmed it by reading the code rather than by trusting the reviewer,
and confirmed the second half too: this was invisible because `probe_ctx` populates
`cache.driver_runs` and leaves `ctx.driver_output`, `cache.driver_journal` and
`cache.driver_inbox` empty, so the Driver tab renders `no_runs_lines` under probe — the ONE
site on the path that was already composed. **Nine rounds of green on this tab meant nothing,
because the probe was looking at the one line that was right.**

And `DriverOutputLine::text` is absent from `deferred-items.md`'s own "six carrier types round
9 did NOT retype" table. So the residual was not merely open, it was **undisclosed**, on the
single most relevant carrier to this phase's own threat model.

**This plan closes the output pane at the TYPE and the other two at the CALL, and says which
is which.** `DriverOutputLine::text` becomes `crate::text::Untrusted`, so `output_line`'s
`Span::styled` stops compiling until it is `shown()`. `InboxMessage::text` and
`DryRunPreview::report` reach files no plan in this wave owns, so retyping them would be a
wave conflict; those two get `shown_capped` at the render site plus a probe fixture that goes
red for them, and the limits block states the difference with its failure direction rather
than letting a reader assume the whole path is compiler-held.

| Site | Value | Source of the bytes | Closed by |
|---|---|---|---|
| `driver.rs:1732` output pane | `DriverOutputLine::text` | `exec_event` text — the agent's own prose, read back off disk | **the type** (`Untrusted`) |
| `driver.rs:1684` injection rows | `InboxMessage::text` | `inbox.jsonl` on disk | a call + a probe fixture |
| `driver.rs:1088` dry-run preview | `DryRunPreview::report` | a report interpolating paths and branch names read from the project | a call + a probe fixture |
| `driver_confirm.rs:162-167` opt-in disclosure | `input.path`, `digest` | the recorded opt-in block in the user's `config.json` | a call + a probe fixture (WR-06) |

**And one sentence corrected in the same commit that makes it true.**
`sanitize_render_line`'s doc asserts, as its own justification for answering only one class,
that `driver.rs` and `driver_confirm.rs` compose both. That sentence was false for four sites
and is what let the gap survive a round whose entire subject was this composition.

**What this plan does NOT touch, stated so the wave stays parallel.** `detail.rs` and
`text.rs` belong to **21-27** (CR-01, parallel worktree, this wave). The nine render-site
files, `src/ui/mod.rs`, `src/error.rs` and `src/state_reader/backlog.rs` belong to **21-29**
(likewise parallel). Note the fence between `src/ui/screens/mod.rs` — **this plan's file** —
and `src/ui/mod.rs`, which is **21-29's**: they are different files and confusing them is a
wave conflict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-REVIEW.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-23-SUMMARY.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-25-SUMMARY.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md
</context>

<tooling_note>
**`rtk` filters build/test/grep output and has produced FALSE NEGATIVES four times in this
phase.** It strips `warning:` and `test result:` lines, so a grep for either succeeds
vacuously. Every count-, presence- or grep-bearing check in this plan runs `rtk proxy <cmd>`.

Gates re-measured by the planner at `b1d0478` (round-10 HEAD), all under `rtk proxy`:

| Gate | Result at `b1d0478` |
|---|---|
| `cargo build` | exit 0 |
| `cargo test --workspace --no-fail-fast` | **1387 passed / 0 failed / 13 ignored** |
| `cargo clippy -- -D warnings` | exit 0 |
| `cargo clippy --all-targets -- -D warnings` | fails with exactly **4** pre-existing lints: `bool_assert_comparison` ×3 (`src/browser.rs:131,132,133`), `cmp_owned` ×1 (`src/project_creator.rs:146`) |
| `cargo test --test spawn_seam_guard` | run and quote its HEAD count; the file is NOT edited |
| `cargo test --test driver_injection_corpus` | 13 passed / 0 failed / 10 ignored |

Documented flakes under parallel execution: `driver_reattach`, `envelope_tracer`. The
planner's own `--workspace` run saw `driver_reattach` fail once and pass on re-run; treat a
single failure in that binary as the documented flake and say so, do not absorb it silently.

Planner's reference sites at `b1d0478`, under `rtk proxy` — **re-measure before quoting**:
`DriverOutputLine` is declared at `src/ui/screens/mod.rs:328-334`; `push_record` at `:425`;
`sanitize_render_line` at `:527` with its composition claim at `:515-519`;
`sanitize_record_lines` at `:542`. In `src/ui/screens/driver.rs`: `shown_capped` at `:614`,
the dry-run preview render at `:1088`, the injection row at `:1684`, `output_line` at
`:1712-1734`, the `Paragraph` sink at `:1964`. `sanitize_render_line` has **ten** occurrences
in `driver.rs` at HEAD (two are executable non-composition calls, the rest are the import,
the composition and doc mentions) and **six** in `driver_confirm.rs` (two executable
non-composition calls at `:164`/`:166`). Only `driver.rs:1732` reads
`DriverOutputLine::text` in production; `:2394` reads it in a test.
</tooling_note>

<assumption_delta_decision>
## Assumption delta — FIRED, and this plan's disposition is `promote`

`gsd-tools query assumption-delta scan 21 --json` returned `detected: true`; the full
disposition is recorded once in `21-27`'s `<assumption_delta_decision>` and is not repeated
here. This plan's slice of it is a **`chosen`** signal of its own and the answer is
`promote`, not `add-alongside`:

- **The noun that is now primary:** *the buffered driver output line*. At HEAD it is a
  `String` whose doc says it is "already through `sanitize_render_line`" — a value whose
  safety is a claim in a comment. After this plan it is a `crate::text::Untrusted`, the
  general carrier round 9 promoted, and the comment's claim becomes the type's property.
- **The decision: `promote`**, with the old `String` demoted to the two sites whose carriers
  reach files outside this wave's fence (`InboxMessage::text`, `DryRunPreview::report`).
- **That demotion is ACCEPTED DEBT and it is named as such.** Those two sites are held by a
  call plus a probe fixture rather than by the compiler. **What would force a later promote:**
  a third render of either value, or any change to `src/journal/inbox.rs` or `src/app.rs`
  that is already opening those files for another reason — at which point retyping the
  carrier costs almost nothing and removes the last call-held site on this path. `21-30`
  records both carriers in `deferred-items.md` with this direction.
- **Suggested, not required:** an invariant test encoding the generalised intent — that no
  field of `DriverOutputLine` is a bare `String` — would make the promote hold for a field
  added tomorrow. Not taken in this plan, because the struct has exactly two fields and the
  other is an enum; noted so a later round can take it rather than rediscover it.
</assumption_delta_decision>

<edge_probe_audit>
This plan discharges rows 1 and 2 of the spec-less probe fallback's coverage report as
**backstop** truths (SAFE-07 boundary and SAFE-07 precision — see `must_haves.truths`, each
carrying a flat-scalar `verification: backstop` marker). Rows 6 and 7 (DRIVE-04 boundary and
precision) are authored in **21-30**; rows 3, 4 and 5 (the three `unclassified` rows for
SAFE-08, DRIVE-01 and DRIVE-03) stay `unresolved` and are surfaced as flagged planner
assumptions in **21-27**, which is the single audit location for the round.

**No-silent-drop equality for round 10: 7 surfaced = 2 (here) + 2 (21-30) + 3 flagged
(21-27).**

Both rows here are **reconfirmations by re-run, not new work**, and that is the honest
framing. SAFE-07's structural half was measured by executed plans 21-01, 21-05, 21-06 and
21-21 and is re-run here to show this plan's diff did not move it. SAFE-07's behavioural half
is ROADMAP criterion 4, which is permanently agent-unclosable and against which this plan
does no work (prohibition 8). Writing a *new* boundary predicate for SAFE-07 in a
render-honesty round would be the manufactured-predicate overclaim this phase exists to end.

The round's four kept prohibitions are recorded in `21-27`'s audit block; the two that ride
here are the capability-preservation one and the do-not-count-a-populated-cache one, both of
which this plan can falsify. All are authored descriptor-less, so each disposes
flagged-unverified.
</edge_probe_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a run's journal on disk → the driver output pane | `exec_event` text is the agent's own prose, written by the model and read back off disk by `crate::app::driver_line_for_record`. This is the phase's own central threat surface and the one pane a prompt-injection payload would most want to control |
| `inbox.jsonl` → the injection rows | `InboxMessage::text` is stored verbatim and deliberately un-redacted; the file is on disk and this build does not exclusively own it |
| the project's git state → the dry-run preview | the report interpolates paths and branch names read from a repository the operator cloned |
| the recorded opt-in block in `config.json` → the disclosure screen | `input.path` and `digest` are read back from a file on disk, and the screen they are drawn on is the one an operator reads before an irreversible act |
| a fixture that renders an empty branch → the reader who trusts its green | a probe whose fixture never reaches the code it claims to cover reports green while covering nothing; that is what happened here for nine rounds |

## STRIDE Threat Register (ASVS L1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-28-01 | Spoofing | `src/ui/screens/driver.rs:1732` via `mod.rs:425` — the live output pane composes only the control class, so the tag block, `U+202E` and `U+00AD` reach a `Paragraph` cell in the pane that displays the LLM's own prose | **high** | mitigate | Task 1: `DriverOutputLine::text` becomes `crate::text::Untrusted`; the render is a compile error until it is `shown()`. Composition equality pinned against `shown_capped`. |
| T-21-28-02 | Spoofing | `src/ui/screens/driver.rs:1684` — injection rows drawn from `inbox.jsonl` with only the control class applied | high | mitigate | Task 2: `shown_capped` at the render site, plus a `driver_inbox` fixture that goes red for it. |
| T-21-28-03 | Spoofing | `src/ui/screens/driver.rs:1088` — the dry-run preview, over a report whose own doc says it interpolates paths and branch names read from the project | high | mitigate | Task 2: `shown_capped` at the render site, plus a `driver_dry_run` probe state. |
| T-21-28-04 | Repudiation | `probe_ctx` leaving `driver_output`, `driver_journal` and `driver_inbox` empty, so the Driver tab renders the one already-composed line under probe and nine rounds of green covered nothing | **high** | mitigate | Task 2: every one populated with the hostile identity, arrival recorded per state against the `chrome_ctx` baseline. |
| T-21-28-05 | Repudiation | `mod.rs:515-519` — `sanitize_render_line`'s doc asserting as justification that these very files compose both classes, which was false for four sites | high | mitigate | Task 2 corrects it in the same commit that makes it true, with the falsified sentence quoted verbatim. |
| T-21-28-06 | Spoofing | `src/ui/screens/driver_confirm.rs:162-167` — the opt-in disclosure escaping only the control class over values read from the user's `config.json`, on the screen shown before an irreversible act (WR-06) | high | mitigate | Task 3: both classes composed, plus a `PromptInput` fixture carrying the probe identity so a committed control can go red. |
| T-21-28-07 | Repudiation | `shown_capped`'s doc claiming a completeness it does not have, which is the reason a reader would not re-check the file (IN-01) | medium | mitigate | Task 3: the claim replaced by a census over the two files, observed red by planting an unconverted call. |

**Package legitimacy:** no `cargo add`, no new dependency, no `Cargo.toml` change. The
`## Package Legitimacy Audit` gate does not fire.
</threat_model>

<decisions>
## Decisions and reversibility (REVERSIBILITY_GATES=true)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| D-21-37 | The output pane is closed at the TYPE (`DriverOutputLine::text: Untrusted`) rather than by a call at the render site | **costly** | The review offered both and preferred the render site so the buffered value keeps one documented meaning. The type gives the same single meaning AND makes the render a compile error, which is the property this phase spent nine rounds establishing is the only one that survives a new site. **Costly:** `DriverOutputLine` is a `pub struct` with `pub` fields on a crate published to crates.io, so the retype is semver-breaking and lands in the next minor. Revertible from git; no on-disk format and no parse behaviour changes. |
| D-21-38 | The escape stays at APPEND time for the control class and moves to RENDER time for the invisible class, rather than composing both at append | **reversible** | The cap is a display cap over agent prose and must be applied where the ring is bounded, or the ring stores unbounded lines. The invisible-class escape expands characters into six-character markers, so applying it before the cap would make the cap mean a different number of source characters per line. The composition-equality pin is what proves the split is behaviour-preserving. |
| D-21-39 | `InboxMessage::text` and `DryRunPreview::report` are NOT retyped in this plan | **reversible** | Their carriers reach `src/journal/inbox.rs` and `src/app.rs`, which no plan in this wave owns; retyping them would be a wave conflict, not a closure. Accepted debt, disclosed with its direction, recorded in `deferred-items.md` by 21-30, with a third render of either value named as what would force the promote. |
| D-21-40 | The probe hole is closed by populating every driver cache in one task rather than tab-state by tab-state across rounds | **reversible** | State-by-state is how nine rounds happened. The cost is one fixture extension; the benefit is that the Driver tab's largest render surface stops being invisible to every future round as well as to this one. |
| D-21-41 | IN-01's completeness claim is replaced by a CENSUS rather than by a softer sentence | **reversible** | The review offered the softer sentence. It is honest and weaker: this phase's own history is that a disclosed-but-unclosed residual returns as a Critical two rounds later, and this exact sentence is why three sites in one file went unre-checked for a round. |

No decision in this plan is `one-way`; no `checkpoint:decision` gate is required. D-21-37 is
the one `costly` rating and it is flagged, not gated: it is revertible from git with no data
migration and no on-disk format change.
</decisions>

<named_shape_audit>
## Named-shape audit (this plan's slice)

| # | Named shape (source) | Owner | Covered by |
|---|---|---|---|
| 1 | The live output pane composing only the control class, on the surface that shows the LLM's own prose (CR-02, confirmed independently by pass 10) | 21-28 T1 | `DriverOutputLine::text` retyped; the render is a compile error until `shown()` |
| 2 | The injection rows and the dry-run preview, same path, same half-composition (CR-02) | 21-28 T2 | `shown_capped` at both render sites, each with its own probe fixture and its own red |
| 3 | `probe_ctx` leaving `driver_output`, `driver_journal`, `driver_inbox` empty and `driver_dry_run` unreachable (CR-02's durable half) | 21-28 T2 | all four populated or driven, arrival recorded per state |
| 4 | `sanitize_render_line`'s doc asserting a composition that was false for four sites | 21-28 T2 | corrected in the same commit, falsified text quoted verbatim |
| 5 | The opt-in disclosure escaping only the control class, probed only with authored defaults (WR-06) | 21-28 T3 | both classes composed, plus a `PromptInput` fixture carrying the identity |
| 6 | `shown_capped`'s doc claiming a completeness it does not have (IN-01) | 21-28 T3 | replaced by a census over the two files, observed red by planting |
| 7 | `InboxMessage::text` and `DryRunPreview::report` as untyped carriers | **disclosed remainder** | call-held plus probe-held, with the failure direction stated; recorded by 21-30 in `deferred-items.md` |
| 8 | The shell-injection path and the accessor vocabulary (CR-01, WR-01) | **21-27** | delegated — parallel worktree, this wave |
| 9 | The remaining bare `display_identity` sites, the backlog sort, the concatenated-expected trap (WR-05, WR-07, WR-08) | **21-29** | delegated — parallel worktree, this wave |
| 10 | The Defaults edit buffer, the sealed doc, `adjudication_reason`, the popup width (CR-03, WR-02, WR-03, IN-02) | **21-30** | delegated — wave 2 |
| 11 | ROADMAP criterion 4's behavioural half | **nobody, deliberately** | permanently agent-unclosable; prohibition 8 forbids chasing it |
</named_shape_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: one hostile journal record, end to end — disk to terminal cell — with the render held by the compiler</name>
  <files>src/ui/screens/mod.rs, src/ui/screens/driver.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - src/ui/screens/mod.rs lines 320-345 — `DriverLineKind` and `DriverOutputLine`, including the field doc claiming the text is already through `sanitize_render_line`. That claim is what becomes a type property.
    - src/ui/screens/mod.rs lines 415-460 — `DriverOutput::retarget`, `push_record` and the ring's drop accounting. `push_record` is the ONE place a `DriverOutputLine` is constructed and therefore the one place the wrap belongs.
    - src/ui/screens/mod.rs lines 495-565 — `sanitize_render_line` in full, including its four numbered rules, the behaviour-preservation paragraph, the composition claim at `:515-519` that Task 2 corrects, and `sanitize_record_lines`.
    - src/ui/screens/driver.rs lines 585-625 — `shown_capped` and its doc (the composition, the cap rationale, and the completeness sentence IN-01 names).
    - src/ui/screens/driver.rs lines 1705-1740 — `output_line`: the five `DriverLineKind` arms, the marker span, and `Span::styled(line.text.clone(), text_style)` — the site that must stop compiling.
    - src/ui/screens/driver.rs lines 1930-1970 — `output_body` and the `Paragraph` sink at `:1964`, so the widget family is confirmed by reading rather than assumed.
    - src/ui/screens/driver.rs around line 2394 — the test that reads `DriverOutputLine::text`; it must keep working and it tells you what shape the test-side accessor needs.
    - src/text.rs lines 490-575 — `Untrusted`, `from_untrusted_source`, `as_raw_for_logic_only`, `shown`, and the hand-written `Debug`. **Do NOT edit this file** (prohibition 3); 21-27 owns it in this wave.
    - src/ui/screens/render_escape_guard.rs lines 700-900 — `probe_ctx` in full: what it populates today, why `driver_runs` alone leaves the tab on `no_runs_lines`, and the doc convention for recording a red beside the fixture that produced it.
    - src/ui/screens/render_escape_guard.rs lines 1023-1150 — `ALL_SUB_VIEWS`, `DETAIL_TAB_ARRIVAL` and `detail_chrome_baseline`, so the new arrival rows are written against the structure rather than against a memory of it.
    - src/test_support.rs lines 95-110 — `LOOK_ALIKE_PAIRS`, imported and never respelled.
  </read_first>
  <behavior>
    - A journal record whose `exec_event` text carries a tag-block character, a `U+202E` and a `U+00AD` reaches the Driver tab's output pane as escaped markers, and no character satisfying `is_invisible_formatting_char` reaches any terminal cell of that pane.
    - `Span::styled(line.text.clone(), ..)` does not compile: `DriverOutputLine::text` is `crate::text::Untrusted`, which has no `Display` and no `Into<Cow<'static, str>>`.
    - `Untrusted::from_untrusted_source(sanitize_render_line(raw)).shown().to_string()` equals `shown_capped(raw)` for every `LOOK_ALIKE_PAIRS` fixture and for every control-class fixture the tree already carries, in both directions.
    - A CLEAN line renders identically before and after this task: same characters, same marker, same length. The cap still cuts at the same source-character count and still appends the same ellipsis.
    - With `ctx.driver_output` populated and the escape NOT applied, the probe goes red naming the Driver tab and listing the offending characters.
    - The ring's drop accounting and the record-truncation flag are unchanged: the same input produces the same `dropped` count and the same `record_truncated` value as before the retype.
  </behavior>
  <action>
    (a) **Re-measure first**, every command under `rtk proxy`, and record each result beside
    this plan's figure in the SUMMARY: the five gates in `<tooling_note>`; the occurrence
    counts of `sanitize_render_line` in `driver.rs` and `driver_confirm.rs`, split into
    executable calls and doc mentions; the readers of `DriverOutputLine::text`.

    (b) **Capture the RED first, and do it by populating the fixture rather than by
    reasoning.** Extend `probe_ctx` to populate `ctx.driver_output` for the identity with one
    `DriverOutput` whose buffered lines are pushed through `push_record` from a record text
    built out of `LOOK_ALIKE_PAIRS`' hostile member. Run
    `the_screen_renders_identity_escaped`. It goes red for the Driver tab, listing the
    invisible-class characters that reached cells. **Capture that panic verbatim.** This red is
    the evidence CR-02 is real in this tree and not merely reported; without it the fix is
    asserted rather than certified.

    (c) **Retype the carrier.** `DriverOutputLine::text` becomes `crate::text::Untrusted`.
    Wrap ONCE, in `push_record`, around the value `sanitize_record_lines` produces. Rewrite the
    field's doc to say what the type now guarantees rather than what a caller promised: the
    control class is applied and capped at append time, and the invisible class is applied at
    the render because the type offers no other way out.

    (d) **Resolve only the sites the compiler names.** Expected from reading, but the compiler
    is the authority: `output_line` becomes `Span::styled(line.text.shown(), text_style)`, and
    the test at `:2394` uses `shown()` or `as_raw_for_logic_only()` with a one-line reason
    stating which question it answers. Count the sites the compiler named and state that count
    in the SUMMARY.

    (e) **Pin the composition equality, in both directions.** Write the test described in
    `<behavior>` before trusting the retype: over `LOOK_ALIKE_PAIRS` and over the
    control-class fixtures the tree already has, assert the wrapped-then-shown value equals
    `shown_capped` of the same input. Also assert the NON-vacuity direction — that at least
    one fixture is CHANGED by the composition — or an equality between two identity functions
    would pass forever. This is what makes prohibition 1 checkable: if a clean line rendered
    differently after this task, this test says so.

    (f) **Confirm the ring is unmoved.** Push enough records to force drops and to trigger the
    record-truncation flag, and assert `dropped` and `record_truncated` match the pre-retype
    values. The retype must not change what the buffer accounts for, only what a render may do
    with what it holds.

    (g) **Add the arrival row.** `DETAIL_TAB_ARRIVAL` gains a row for the output-pane state
    naming what it draws and where the bytes come from — `exec_event` text, the agent's own
    prose, read back off disk — and never the words escaped or safe. Then re-run the probe:
    green.

    (h) **Restore the red once to prove the control still has teeth**: revert the single
    `shown()` at `output_line`, re-run the probe, capture the second verbatim red, restore,
    and show `rtk proxy git status --porcelain` clean.
  </action>
  <acceptance_criteria>
    - Two verbatim REDs for the Driver tab's output pane are quoted in the SUMMARY and in the commit message — one from step (b) before the retype, one from step (h) after — each followed by `rtk proxy git status --porcelain` showing a clean tree.
    - `rtk proxy grep -c "pub text: String" src/ui/screens/mod.rs` returns 0.
    - `rtk proxy grep -n "Untrusted" src/ui/screens/mod.rs` shows the buffered line's text field carrying the carrier type; the struct definition is quoted in full in the SUMMARY.
    - `rtk proxy grep -c "line.text.shown()" src/ui/screens/driver.rs` returns at least 1, and the SUMMARY quotes the count of sites the compiler named.
    - The composition-equality test passes in both directions and its non-vacuity arm is quoted: the SUMMARY names at least one fixture the composition changes and states what the test reports if the two functions ever diverge.
    - The ring-accounting test passes with `dropped` and `record_truncated` matching their pre-retype values; both numbers are quoted.
    - The new `DETAIL_TAB_ARRIVAL` row names values and sources and contains neither of the two words its own doc forbids; the row's text is quoted in the SUMMARY.
    - `rtk proxy cargo test --lib -- ui::screens::render_escape_guard::tests::the_screen_renders_identity_escaped --exact --nocapture` exits 0.
    - `rtk proxy cargo test --workspace --no-fail-fast` → 0 failures (or `driver_reattach` alone, named as the documented flake and shown green on re-run), totals quoted against the 1387/0/13 baseline with every delta attributed.
    - `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings` reports exactly four lints of the same two kinds in the same two files.
    - `rtk proxy git diff --stat` names none of prohibition 3's files, and neither `src/browser.rs` nor `src/project_creator.rs`.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::screens::render_escape_guard::tests::the_screen_renders_identity_escaped --exact --nocapture</automated>
  </verify>
  <done>`DriverOutputLine::text` is `crate::text::Untrusted`, `output_line` renders it through `shown()` because nothing else compiles, the composition is pinned equal to `shown_capped` in both directions with a non-vacuity arm, the ring's accounting is measurably unmoved, and the probe was observed red for this pane before the fix and again on a single-site revert.</done>
  <reversibility rating="costly">`DriverOutputLine` is a `pub struct` with `pub` fields on a crate published to crates.io; retyping a field is semver-breaking and lands in the next minor. Revertible from git; no on-disk format, no parse behaviour, no data migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the other two sites on the same path, the fixture that makes them visible, and the doc sentence that was false</name>
  <files>src/ui/screens/driver.rs, src/ui/screens/mod.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - src/ui/screens/driver.rs lines 1055-1105 — `render_dry_run_preview` in full, including its own doc saying the report interpolates paths and branch names read from the project, and the `sanitize_render_line(raw)` mapping at `:1088`.
    - src/ui/screens/driver.rs lines 1640-1706 — `injection_rows` and `injection_block`: the head spans, the `INJECTION_INDENT` row that renders `message.text` at `:1684`, and the missed-reason row below it.
    - src/ui/screens/driver.rs lines 1850-1930 — the Driver tab's render entry, `driver_output` lookup, `driver_journal` read and `derive_injection_states(&cache.driver_inbox, records)`. This is the dispatch that decides whether the injection rows are drawn at all; a fixture that populates the inbox without the journal proves nothing.
    - src/ui/screens/mod.rs lines 495-545 — `sanitize_render_line`'s doc, especially the composition claim naming `driver.rs` and `driver_confirm.rs`. That sentence is the artifact this task corrects.
    - src/ui/screens/mod.rs lines 655-740 — `ProjectViewCache::driver_inbox`, `driver_journal` and `driver_dry_run`, plus `DryRunPreview`'s shape, so the fixtures are written against the structs.
    - src/journal/inbox.rs lines 80-110 — `InboxMessage`, especially the `text` field's doc saying it is stored verbatim and deliberately not redacted. **Do NOT edit this file** (prohibition 5); read it to state the carrier's residual accurately.
    - src/ui/screens/render_escape_guard.rs lines 1260-1310 — `DETAIL_SUB_STATES` and `SubStateArrange`: the four existing within-tab states and the doc explaining why a tab is not one picture. The `driver_dry_run` state follows this shape exactly.
    - src/ui/screens/render_escape_guard.rs lines 96-145 — the LIMITS block, especially LIMIT 1's current wording naming `driver_dry_run` as a remaining unprobed STATE while naming neither the output pane, the journal nor the inbox as unescaped SITES. That wording is quoted and replaced.
  </read_first>
  <behavior>
    - An `InboxMessage` whose `text` carries the hostile identity renders into the injection rows with both classes composed and no invisible-class character in any cell.
    - A dry-run report whose body carries the hostile identity renders into the preview with both classes composed.
    - The injection rows are genuinely REACHED under probe: the fixture populates `driver_inbox` AND the journal records `derive_injection_states` consumes, and the probe asserts a token only the injection-row branch can produce.
    - The dry-run preview is genuinely REACHED: a `DETAIL_SUB_STATES` entry sets `driver_dry_run` to a preview with a report body, and the probe asserts a token only that branch can produce.
    - Each new state records arrival in `DETAIL_TAB_ARRIVAL` and the set equality against the `chrome_ctx` baseline holds in both directions; a state that does not arrive is reported by name.
    - Reverting the escape at either new site drives the probe red naming that state, separately.
    - `sanitize_render_line`'s doc no longer asserts a composition that is not true of every site in the two files it names.
  </behavior>
  <action>
    (a) **Capture the reds first, one per site, by populating before escaping.** Extend
    `probe_ctx` to populate `cache.driver_inbox` with one `InboxMessage` carrying the hostile
    identity AND `cache.driver_journal` with the records `derive_injection_states` needs, then
    run the probe and capture the verbatim red for the injection rows. Then add the
    `driver_dry_run` `DETAIL_SUB_STATES` entry with a report body carrying the identity, run
    the probe again, and capture the verbatim red for the preview. **Two separate reds, one per
    site.** One red standing for two sites proves one site.

    (b) **Compose both classes at the two render sites**, through `shown_capped` — the capped
    composition that already exists in this file and whose cap is the right one for agent prose
    and for a report body. Write one line at each site naming the value, its source on disk,
    and the fact that this site is held by a CALL rather than by the type, with a pointer to
    the limits block entry that states the direction.

    (c) **Prove the branches are reached, not merely populated.** For each of the two states,
    assert a token that only that branch can produce — the injection indent marker for the
    rows, the preview's own loading/rule chrome for the dry run. This is the
    `STATUS_BRANCH_TOKEN` discipline the module already uses for `NormalScreen`'s status
    footer, applied here. A state that sets a field the render never dispatches on is a state
    that proves nothing, and this module records one such near-miss already.

    (d) **Add the `DETAIL_TAB_ARRIVAL` rows** for both new states, each naming what it draws
    and where the bytes come from, never the words escaped or safe. Run the set equality in
    both directions and record the result per state.

    (e) **Correct `sanitize_render_line`'s doc claim in `src/ui/screens/mod.rs`, in this same
    commit.** Quote the falsified sentence verbatim and dated, then state what is true after
    this plan: the output pane is held by the carrier's type, the other three sites are held by
    a composition at the render, and what the function itself answers is one class. Do not
    write a replacement that merely asserts the files are fine — prohibition 6's narrowing test
    applies to this sentence as much as to LIMIT 1.

    (f) **Rewrite LIMIT 1 strictly narrower and quote both wordings side by side in the
    SUMMARY.** It may no longer name `driver_dry_run` as an unprobed state, because this task
    probes it. What it may now say is which sites are held by the compiler and which by a call
    plus a fixture, and that the residual is carriers this wave's fence prevented retyping —
    `InboxMessage::text` and `DryRunPreview::report` — named with **under-protection, silent**
    as the direction and with what would force the promote.

    (g) Re-run both SAFE-07 backstops and record their results as evidence rather than
    inference: `rtk proxy cargo test --test spawn_seam_guard` at its HEAD count and
    `rtk proxy cargo test --test driver_injection_corpus` at its HEAD count of active pins with
    its ignored count unchanged. Both files are RUN and neither is EDITED; quote both counts,
    show `rtk proxy git diff --stat` naming neither, and state that this plan's diff did not
    move SAFE-07's structural boundary and did no work against its behavioural half.
  </action>
  <acceptance_criteria>
    - Two separate verbatim REDs — one for the injection rows, one for the dry-run preview — are quoted in the SUMMARY and in the commit message, each captured before its escape landed and each followed by a clean `rtk proxy git status --porcelain`.
    - `rtk proxy grep -c "shown_capped" src/ui/screens/driver.rs` is re-measured and quoted, and is strictly greater than its HEAD value; the SUMMARY names the two sites added.
    - The branch-reached assertions pass: the SUMMARY quotes the token used for each of the two states and states what the probe reports if the dispatching field is left at its default.
    - `rtk proxy grep -c "driver_output\|driver_journal\|driver_inbox\|driver_dry_run" src/ui/screens/render_escape_guard.rs` returns a non-zero count, quoted; at HEAD it is 0 for the first three.
    - Both new `DETAIL_TAB_ARRIVAL` rows are quoted in the SUMMARY, name values and sources, and contain neither of the two words the row's own doc forbids.
    - `sanitize_render_line`'s corrected doc is quoted in the SUMMARY beside the falsified sentence it replaces, verbatim, with the correction dated.
    - LIMIT 1's old and new wordings are quoted side by side and the new one is strictly narrower: it no longer names `driver_dry_run` as unprobed, and it names the two un-retyped carriers with the direction `under-protection, silent` and with what would force the promote.
    - `rtk proxy cargo test --test spawn_seam_guard` and `rtk proxy cargo test --test driver_injection_corpus` both pass at their re-measured HEAD counts, and `rtk proxy git diff --stat` names neither file.
    - `rtk proxy cargo test --workspace --no-fail-fast` → 0 failures, totals quoted; `rtk proxy cargo clippy -- -D warnings` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::screens::render_escape_guard::tests::the_screen_renders_identity_escaped --exact --nocapture</automated>
  </verify>
  <done>The injection rows and the dry-run preview compose both classes, each was observed red separately against a fixture that provably reaches its branch, both new states record arrival, `sanitize_render_line`'s false composition claim is corrected with its own text quoted, LIMIT 1 is strictly narrower, and both SAFE-07 backstops re-run green with their files unchanged.</done>
  <reversibility rating="reversible">Two render-site calls, `#[cfg(test)]` fixture states, arrival rows and doc text. No public API, no on-disk format, no data migration.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-06 — the disclosure an operator reads before an irreversible act; and IN-01 — a completeness claim replaced by the census that checks it</name>
  <files>src/ui/screens/driver_confirm.rs, src/ui/screens/driver.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - src/ui/screens/driver_confirm.rs lines 120-200 — `render_disclosure` in full: where `input.path`, `profile` and `digest` come from, the `DISCLOSED_PROMPT_INPUTS` defaults, and the two `sanitize_render_line` calls at `:164` and `:166` that answer one class.
    - src/ui/screens/driver_confirm.rs lines 260-345 — the two sites 100 lines below that DO compose both classes (`:279`, `:319`), the bare `crate::text::display_identity(alias)` at `:275`, and the screen's `adjudicate_screen!` reason at `:335-345` which already acknowledges the class split without closing it. The two composed sites are the shape `render_disclosure` must match; the bare call at `:275` is the cross-plan contract with 21-29.
    - src/registry.rs — `PromptProfile` and the recorded opt-in block's shape, so the fixture builds a real `PromptInput` rather than an approximation. **Do NOT edit this file.**
    - src/ui/screens/driver.rs lines 585-625 — `shown_capped`'s doc, especially the sentence claiming every other site on this path was doing the same thing wrong. That sentence is IN-01 and is what this task replaces.
    - src/text.rs lines 765-925 — the alphabet-spelling census: the runtime-assembled needle, the walk, the comment-dropping, the equality on a count and the committed RED. **This is the idiom the new census copies. Do NOT edit this file** (prohibition 3); 21-27 owns it in this wave.
    - src/ui/screens/render_escape_guard.rs — `fixture_for`'s `DriverConfirmScreen` arm as Task 2 left it, and `one_state`.
  </read_first>
  <behavior>
    - `render_disclosure` composes both classes over `input.path` and over `digest`, matching the two sites in the same file that already do.
    - The probe's `DriverConfirmScreen` fixture supplies a `PromptInput` whose `path` and `digest` carry the probe identity, so the disclosure state draws an attacker-influenced value rather than only authored `&'static str` defaults.
    - With the fixture in place and `render_disclosure` still composing one class, the probe goes red naming `DriverConfirmScreen` and its disclosure state.
    - No executable call to `sanitize_render_line` in `src/ui/screens/driver.rs` or `src/ui/screens/driver_confirm.rs` stands outside a composition: it is either inside `shown_capped`'s own body, or on a logical line that also names `display_identity` or `render_for_terminal`.
    - The census reports a NON-ZERO count, naming file and line, when one such call is planted outside a composition.
    - No bare `crate::text::display_identity` call remains in either of this plan's two render files — every one either composes with `sanitize_render_line` on the same logical line or has become `render_for_terminal`.
  </behavior>
  <action>
    (a) **Capture the RED first.** Extend the probe's `DriverConfirmScreen` fixture with a
    `PromptInput` whose `path` and `digest` carry the hostile identity, leaving
    `render_disclosure` as it is. Run the probe, capture the verbatim red naming the screen and
    the state. Today the two opt-in prompts are probed only with the shipped
    `DISCLOSED_PROMPT_INPUTS` defaults, every one of which is an authored `&'static str`, so no
    committed control could ever have gone red here — say that in the SUMMARY, because it is
    the reason WR-06 survived.

    (b) **Compose both classes** at the two `render_disclosure` sites, matching the shape the
    two sites 100 lines below already use. Re-run: green. Then revert one of the two, re-run,
    capture the second red, and restore — so both sites are individually covered rather than
    one standing for the pair.

    (c) **Convert the bare `display_identity` call at `driver_confirm.rs:275` to
    `render_for_terminal`**, and re-measure every executable call to `display_identity` in both
    of this plan's render files. This is the cross-plan contract in prohibition 4: 21-29 lands
    a census in this same wave over `src/ui/`, and after the worktree merge it cannot edit
    these two files to fix a violation. Report the per-file counts before and after.

    (d) **Replace IN-01's sentence with the census that checks it.** Delete the completeness
    claim from `shown_capped`'s doc and put in its place a statement of what a reader can
    check, naming the census. Add the census as a `#[cfg(test)]` function plus its test,
    following the alphabet census's idiom exactly: walk the two files, drop comment lines, join
    wrapped logical lines while keeping the first physical line's number for the report, and
    assert an EQUALITY on a count of zero over executable calls standing outside a composition.
    Assemble the needle at runtime from halves that are meaningless apart so the census cannot
    report itself, and assert that self-match property directly.

    (e) **Observe the census RED by planting**: add one unconverted call in a file this task
    does not otherwise edit within the census's scope, run it, capture the verbatim panic
    naming file and line, remove the plant, and show `rtk proxy git status --porcelain` clean.

    (f) State the census's residual WITH its direction, in the LIMITS style this tree uses: it
    is a source scan over two named files, so a composition assembled across statements, or a
    third file added to this path tomorrow, is invisible to it. **Under-detection, silent.**
    What bounds THAT is the probe — which now renders all four sites — not this census, and
    the doc must say so rather than implying the census closes the class.
  </action>
  <acceptance_criteria>
    - Two verbatim REDs for `DriverConfirmScreen` are quoted in the SUMMARY — one from step (a) before either site composed, one from step (b)'s single-site revert — each followed by a clean `rtk proxy git status --porcelain`.
    - The SUMMARY states in one sentence why no committed control could previously go red at this screen, naming `DISCLOSED_PROMPT_INPUTS` and the fact that its members are authored `&'static str`.
    - `rtk proxy grep -n "render_disclosure" -A 30 src/ui/screens/driver_confirm.rs` shows both classes composed at both sites; the two lines are quoted in the SUMMARY.
    - `rtk proxy grep -c "display_identity(" src/ui/screens/driver_confirm.rs` and the same for `src/ui/screens/driver.rs` are re-measured before and after and both quoted; the SUMMARY states explicitly that no bare call remains in either file and names the rule 21-29's census will apply.
    - The census RED from step (e) is quoted verbatim naming the planted file and line, followed by a clean tree.
    - The census asserts an EQUALITY on a count, its self-match control passes, and the SUMMARY states in one sentence why the zero is not vacuous.
    - IN-01's sentence is gone from `shown_capped`'s doc and the replacement names the census; both the removed sentence and its replacement are quoted in the SUMMARY.
    - The census's residual is stated with the word `Under-detection` and its direction.
    - `rtk proxy cargo test --workspace --no-fail-fast` → 0 failures, totals quoted; `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings` still reports exactly four lints of the same two kinds in the same two files.
    - `rtk proxy git diff --stat` names only this plan's four files and none of prohibition 3's.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::screens --nocapture</automated>
  </verify>
  <done>`render_disclosure` composes both classes over both values with a fixture that goes red for each, no bare `display_identity` call remains in either of this plan's render files, and `shown_capped`'s completeness claim is replaced by a committed census that was observed red by planting and that cannot report itself.</done>
  <reversibility rating="reversible">Two render-site compositions, one call conversion, a `#[cfg(test)]` census and a fixture. No public API, no on-disk format, no data migration.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
**Created by 21-28** (21-27's, 21-29's and 21-30's symbols are listed in those plans):

| Kind | Symbol | File |
|---|---|---|
| fixture | `probe_ctx`'s populated `ctx.driver_output`, `cache.driver_journal` and `cache.driver_inbox` | `src/ui/screens/render_escape_guard.rs` |
| fixture state | the `driver_dry_run` `DETAIL_SUB_STATES` entry | `src/ui/screens/render_escape_guard.rs` |
| fixture | the `PromptInput` carrying the probe identity in `DriverConfirmScreen`'s arm | `src/ui/screens/render_escape_guard.rs` |
| arrival rows | `DETAIL_TAB_ARRIVAL` rows for the output pane, the injection rows and the dry-run preview | `src/ui/screens/render_escape_guard.rs` |
| test | the composition-equality pin (wrapped-then-`shown()` equals `shown_capped`), with its non-vacuity arm | `src/ui/screens/driver.rs` |
| test | the ring-accounting pin (`dropped` and `record_truncated` unmoved by the retype) | `src/ui/screens/mod.rs` |
| test | the branch-reached token assertions for the injection rows and the dry-run preview | `src/ui/screens/render_escape_guard.rs` |
| fn (test) | the composition census over `driver.rs` and `driver_confirm.rs`, with its runtime-assembled needle halves | `src/ui/screens/driver.rs` |
| test | the census's zero-count assertion and its self-match control | `src/ui/screens/driver.rs` |

**Changed field types:** `ui::screens::DriverOutputLine::text` becomes
`crate::text::Untrusted` (wrapped once in `push_record`).

**Changed:** `output_line` renders through `shown()`; `driver.rs:1088` and `driver.rs:1684`
compose through `shown_capped`; `driver_confirm.rs`'s `render_disclosure` composes both
classes and its bare `display_identity` call becomes `render_for_terminal`.
**Rewritten narrower:** LIMIT 1 in `render_escape_guard.rs`. **Corrected with the falsified
text quoted:** `sanitize_render_line`'s composition claim in `mod.rs`; `shown_capped`'s
completeness claim in `driver.rs`.

**Deliberately NOT changed, and disclosed:** `journal::inbox::InboxMessage::text` and
`ui::screens::DryRunPreview::report` stay `String` (prohibition 5, D-21-39).

**Unchanged and RUN, never edited:** `tests/spawn_seam_guard.rs`,
`tests/driver_injection_corpus.rs`, `src/browser.rs`, `src/project_creator.rs`,
`.planning/REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<verification>
- `rtk proxy cargo build` exit 0; `rtk proxy cargo test --workspace --no-fail-fast` → 0
  failures, totals quoted against the 1387/0/13 baseline with every delta attributed, and any
  `driver_reattach` failure named as the documented flake and shown green on re-run.
- `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings`
  reports exactly four lints, same two kinds, same two files, before and after — both quoted.
- Six verbatim REDs, each with a clean `rtk proxy git status --porcelain` after: the output
  pane before the retype and again on a single-site revert; the injection rows; the dry-run
  preview; `DriverConfirmScreen` before and on a single-site revert; the planted census call.
- A per-state table for every new probe state: cache populated, branch-reached token, arrival,
  and the cause where arrival is absent.
- `sanitize_render_line`'s and `shown_capped`'s corrected docs quoted beside the sentences
  they replace; LIMIT 1's two wordings quoted side by side with the new one strictly narrower.
- `tests/spawn_seam_guard.rs` and `tests/driver_injection_corpus.rs` run at their HEAD counts
  and unchanged in `git diff --stat`; no work claimed against criterion 4.
- `rtk proxy git diff --stat` names only this plan's four files.
- `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
- The pane that displays the LLM's own prose cannot render an unescaped invisible-class
  character, and the render site is held by the compiler rather than by a call.
- The other three sites on the same path compose both classes, each observed red separately
  against a fixture that provably reaches its branch.
- The Driver tab's output pane, injection rows and dry-run preview render under probe on every
  run, with arrival recorded per state.
- The two sentences that asserted this composition while it was false are corrected in the
  commits that make them true, each quoting what it corrects.
- The difference between a type-held site and a call-held site is disclosed with its failure
  direction and with what would force the promote — not smoothed into one claim.
- Both SAFE-07 backstops re-run green at their HEAD counts with their files unchanged.
</success_criteria>

<output>
Create `.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-28-SUMMARY.md` when done.
</output>
