---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 34
type: execute
wave: 2
gap_closure: true
depends_on: [21-31, 21-32, 21-33]
files_modified:
  - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md
  - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/COVERAGE.md
  - tests/driver_reattach.rs
autonomous: true
requirements: [DRIVE-01, DRIVE-03, DRIVE-04, SAFE-07, SAFE-08]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `driver_reattach` flake record is corrected EVERYWHERE the wrong claim stands, and the completeness of that correction is established by a committed grep inventory rather than by an author's memory. `deferred-items.md` still opens, at its first flake section, with *\"the same binary with `-- --test-threads=1` returned ok three times out of three\"* — a reader who stops at the first section reads a mitigation that round 10 proved does not work. Two later updates in the same file qualify it and a third supersedes it, which is three layers of correction a reader must find. The correction is placed AT the stale sentence, not only after it. Round 10's proven understanding is the one that stands: pre-existing and not a regression (the untouched base flakes at the same rate); `--test-threads=1` does NOT reliably fix it; and the mechanism is that these tests discover runs via a **system-wide `/proc` scan that does not stop at the process-group or worktree boundary**."
    - "The correction's completeness is CHECKED, because correcting only some locations recreates the species this round is about. The executor greps `test-threads` and `driver_reattach` across `.planning/` and across `tests/` and `src/`, enumerates EVERY hit in the SUMMARY, and classifies each as one of exactly two kinds: a STANDING claim about how to run or mitigate the binary — which is corrected in place — or a DATED observation of one specific run, which is left unedited because it is a true record of what that run did, and is named in the corrections table so the record is complete rather than selective. No hit may be left unclassified, and the SUMMARY states the total so the classification can be checked against it."
    - "The correction is placed where a reader of the FAILING TEST finds it, not only in `.planning/`. `tests/driver_reattach.rs` carries no note about the flake at all today, so the person who hits it and reaches for `--test-threads=1` learns nothing from the file in front of them. A module-level comment records the three facts and points at the standing item. This is the ONE file under `tests/` this round touches and its diff is COMMENT-ONLY, which is asserted rather than promised — so the next verifier's `no file under tests/ in the diff` shortcut is replaced by a narrower and checkable statement, and `tests/driver_injection_corpus.rs` remains untouched."
    - "Round 11's four gap closures are recorded with their residuals and DIRECTIONS, not merely announced. gaps[0]: the id is fused to its option, and the residual is that this is a DEPENDENCY-BEHAVIOUR property of the `claude` CLI's option parser — a new standing staleness obligation is opened, mirroring the ratatui grapheme-filtering obligation this file already carries, with the exact probe commands and the version they were measured against, so a future CLI that changes `--resume`'s arity is re-measured rather than assumed. gaps[1] and gaps[2]: mechanism fixes with narrowed claims, each carrying its window or its over-detection direction. gaps[3]: a narrowed claim with a pinned reach, whose bookkeeping red direction is stated."
    - "The three items round 11 deliberately did NOT close are recorded as standing items with a promote condition and a direction each, rather than dropped: (1) `src/executor/claude.rs`'s three `--option value` pairs — `--resume`, `--model`, `--name` — which are inert today because all three read `Option` fields that are `None` at `ExecutionOptions::default()` and are set by no caller, and whose promote condition is the FIRST caller that sets one from a value not authored in this crate, with direction under-detection, SILENT until that caller appears; (2) `IN-01`, `IN-02`, `IN-03` and `IN-04` from pass 11's anti-pattern table, each with the one-line reason it is not chased this round; (3) `WR-06`, the missing per-emulator working-directory column, with the reason it is a capability question rather than a security one and the direction its failure takes."
    - "**ROADMAP success criterion 4 is re-surfaced VERBATIM for the fourth consecutive round with no work claimed against it, and this plan states in words that it is PERMANENTLY AGENT-UNCLOSABLE so the next verifier does not re-litigate it.** It requires a human with a live authenticated Claude subscription to run the ten `#[ignore]`d arms of `tests/driver_injection_corpus.rs`; it is a standing item by explicit user decision; `tests/driver_injection_corpus.rs` is RUN at its unchanged counts (13 passed / 0 failed / 10 ignored) and EDITED under no circumstance. **4/5 is the expected and correct ceiling for this phase and is not a failure.**"
    - "A Record-corrections table names every round-10 truth pass 11 measured FALSE as shipped, and says where each is now closed: `21-27` truth 1 (\"no parser left in the path\" — false, `claude`'s option parser remained; closed by `21-31` Task 2's fusion and doc correction), `21-27` truth 5 (the census reports \"any\" such line — false at twelve comment lines; closed by `21-32` Tasks 1 and 2), `21-28` truth 7 (an executable call appears only inside a composition — false, sibling arms launder; closed by `21-32` Task 3), and `21-29` truth 2 (the claim becomes a committed control over `src/ui/` — the completeness is not checked; closed by `21-33` Task 1's narrowing and pin). Each row states what was claimed, what was measured, and the plan and task that closed it — append-only, in the commit that records it."
    - statement: "DRIVE-04 boundary — the per-run model-escalation cap is reconfirmed at the threshold and one step either side by re-running `cargo test --test driver_escalation_cap` at its unchanged counts, with `git diff --stat` showing the file absent from round 11's diff, so the reconfirmation is explicit evidence rather than an inference from the round's scope."
      verification: backstop
    - statement: "DRIVE-04 precision — the escalation counter is an integer count compared by equality against an integer cap, so there is no rounding, tie-breaking or precision-loss contract to state; reconfirmed by the same unchanged suite in the same run."
      verification: backstop
    - "`COVERAGE.md` is AMENDED append-only rather than rewritten or regressed. Its existing reasoned declaration — no external API integration, with the `claude` subprocess seam deliberately not a matrix row because it is a program this phase HARDENS rather than an interface this round INTEGRATES — remains accurate for round 11 and is left byte-identical. A dated round-11 paragraph is appended stating that round 11's diff likewise adds no dependency (`git diff Cargo.toml Cargo.lock` empty over the round, measured) and naming what it changed instead: one argv element's shape, two census algorithms, one comparator clause, and a set of controls and doc corrections."
    - "The final gate is measured on the MERGED tree, not inherited from any wave-1 worktree: `cargo build` exit 0, `cargo test --workspace --no-fail-fast` with every delta attributed to a named test one of the four plans added, `cargo clippy -- -D warnings` exit 0, and `cargo clippy --all-targets -- -D warnings` reporting exactly FOUR pre-existing lints of the same two kinds in `src/browser.rs` and `src/project_creator.rs`. A count of three is a failure of this round exactly as five would be. `.planning/REQUIREMENTS.md` is untouched for the ninth consecutive round."
  prohibitions:
    - statement: "MUST NOT correct the flake record in some locations and not others. The grep inventory is the completeness check and every hit must be classified as a standing claim (corrected) or a dated observation (named, unedited, with the reason). A partial correction recreates the exact species — a claim wider than the evidence certifying it — that this round exists to close, one level up in the record itself."
      category: transparency
    - statement: "MUST NOT rewrite or shorten any existing section of `deferred-items.md` or `COVERAGE.md`. Both are append-only records; corrections are added beside what they correct, dated, quoting the superseded text verbatim. `COVERAGE.md`'s existing no-external-API declaration is accurate for round 11 and must come out byte-identical."
      category: process
    - statement: "MUST NOT edit any file under `src/`, and MUST NOT edit any file under `tests/` other than `tests/driver_reattach.rs`, whose diff must be comment-only and asserted so. `tests/driver_injection_corpus.rs` is RUN and never edited. Round 11's source changes are complete when this plan starts; this plan's job is the record and the merged-tree gate."
      category: process
    - statement: "MUST NOT attempt to FIX the `driver_reattach` flake. The record is corrected; the fix — scoping the run discovery to the test's own process group or worktree — stays a standing item with its promote condition, because its direction is false-red under parallelism, which is LOUD and therefore the safe direction, and because a synchronisation change to a live-process probe is not a record correction."
      category: process
    - statement: "MUST NOT plan, execute or claim any work against ROADMAP success criterion 4, and MUST NOT edit `.planning/REQUIREMENTS.md`. Criterion 4 needs a human with an authenticated Claude subscription and no agent can close it; requirement status is decided exclusively by a passed verification. The plan must SAY that 4/5 is the expected ceiling, so the next verifier does not re-litigate it."
      category: process
    - statement: "MUST NOT inherit any gate number from a wave-1 worktree's SUMMARY. Every figure in this plan's SUMMARY is re-measured on the MERGED tree under `rtk proxy`, and any difference from a wave-1 figure is REPORTED with both numbers rather than smoothed."
      category: transparency
  artifacts:
    - path: ".planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md"
      provides: "the flake record corrected at the stale sentence with the grep inventory behind it; round 11's four closures with their residuals and directions; the new claude-CLI staleness obligation; the three deliberate non-closures with promote conditions; criterion 4 verbatim; and the Record-corrections table for the four round-10 truths measured false"
      contains: "driver_reattach"
    - path: ".planning/phases/21-llm-goal-layer-prompt-injection-hardening/COVERAGE.md"
      provides: "an append-only dated round-11 paragraph confirming the existing no-external-API declaration still holds and naming what round 11 changed instead"
      contains: "No external API integration"
    - path: "tests/driver_reattach.rs"
      provides: "a comment-only module note recording the three corrected flake facts and pointing at the standing item, where a reader of the failing test will find it"
      contains: "driver_reattach"
  key_links:
    - from: "the stale mitigation sentence at the top of the flake record"
      to: "the reader who runs the failing binary"
      via: "a correction placed at the stale sentence and mirrored into the test file's own module comment, with a grep inventory establishing that no location was missed"
      pattern: "driver_reattach"
    - from: "round 11's argv fusion"
      to: "a future claude CLI whose --resume arity changes"
      via: "a standing staleness obligation carrying the exact probe commands and the version they were measured against"
      pattern: "deferred-items"
---

<objective>
**Round 11, wave 2. The record — corrected completely, or not corrected at all.**

Three of round 11's four gaps were mechanism defects; the fourth was a claim defect. This plan
is where the round's own claims are made to match what it delivered, and where one carried
falsehood is finally removed from the committed record.

**The falsehood.** `deferred-items.md`'s first flake section still reads *"the same binary with
`-- --test-threads=1` returned ok three times out of three"*. Round 4's update says the
mitigation no longer works; round 5's says the note is stale; round 10's says the real mechanism
is a **system-wide `/proc` scan that does not stop at the process-group or worktree boundary**.
So the file contains the correct understanding **and** the wrong one, and the wrong one is
first. A reader who stops at the first section reaches for a mitigation that does not work.

The correction goes **at** the stale sentence, and — because correcting only some locations
recreates this round's own species one level up — its completeness is established by a
**committed grep inventory**: every hit for `test-threads` and `driver_reattach` under
`.planning/`, `tests/` and `src/`, each classified as a STANDING claim (corrected in place) or a
DATED observation of one run (left unedited, named in the table with the reason). No hit
unclassified, and the total stated so the classification can be checked.

And it goes where the person who hits the flake actually looks: `tests/driver_reattach.rs`
carries no note about it at all today. This is the one file under `tests/` round 11 touches, its
diff is comment-only and that is **asserted** rather than promised, so the next verifier's
convenient "no file under `tests/` in the diff" shortcut is replaced by something narrower and
checkable.

**The rest of the record.** Round 11's four closures with their residuals and directions; a new
standing staleness obligation for the `claude` CLI's option parser, mirroring the ratatui
grapheme obligation this file already carries, carrying the exact probe commands and the version
they were measured against; the three items round 11 deliberately did not close, each with a
promote condition and a direction; a Record-corrections table naming the four round-10 truths
pass 11 measured FALSE as shipped and where each is now closed; and **ROADMAP criterion 4
re-surfaced verbatim for the fourth consecutive round with no work claimed against it and an
explicit statement that it is permanently agent-unclosable, so the next verifier does not
re-litigate it. 4/5 is the expected and correct ceiling.**

**Wave 2, because it depends on all three.** `21-31`, `21-32` and `21-33` run three-wide in
parallel worktrees in wave 1 with a verified-empty `files_modified` intersection; this plan runs
after they merge, because its numbers must be measured on the MERGED tree and its residual
statements must quote what the three plans actually delivered rather than what they intended.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-31-SUMMARY.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-32-SUMMARY.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-33-SUMMARY.md
</context>

<tooling_note>
**A global RTK hook rewrites and filters `cargo` output**, stripping `warning:` and
`test result:` lines. A grep for either against filtered output **succeeds vacuously**. Every
count-, presence- or grep-bearing check in this plan runs `rtk proxy <cmd>`.

Gates re-measured by the planner at pre-round-11 HEAD **`343c408`**, all under `rtk proxy`.
**These are the BASE, not the target — this plan re-measures every one on the merged tree:**

| Gate | Result at `343c408` |
|---|---|
| `cargo clippy -- -D warnings` (lib, the project gate) | **exit 0** |
| `cargo clippy --all-targets -- -D warnings` | fails with exactly **4** pre-existing lints: `bool_assert_comparison` x3 at `src/browser.rs:155,156,157`, `cmp_owned` x1 at `src/project_creator.rs:146` |
| `cargo test --workspace --no-fail-fast` | **35 binaries, 1416 passed / 1 failed / 13 ignored** — the single failure is `driver_reattach::a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`, the documented flake, in the planner's own baseline run **before any file was edited**. A fully green run is 1417 / 0 / 13. This baseline observation is itself evidence for Task 1: the flake fired at HEAD with nothing edited and with no `--test-threads` setting applied. |
| `cargo test --test driver_injection_corpus` | 13 passed / 0 failed / **10 ignored** |

Planner's grep reference, to be re-run rather than trusted: `test-threads` matches across
`.planning/` in `21-04`, `21-10`, `21-12`, `21-13`, `21-15`, `21-16`, `21-17`, `21-19`, `21-20`,
`21-21`, `21-23`, `21-24`, `21-26`, `21-28` artifacts and in `deferred-items.md`; the STANDING
mitigation claim is at `deferred-items.md` around lines 19-21 with its later qualifications
around lines 43-50 and 52-64 and the superseding entry from `21-30` around lines 972-1000.
`tests/driver_reattach.rs` matched neither term in a doc comment — it carries no flake note at
all, which is why Task 1 adds one.
</tooling_note>

<wave_parallelism>
## Wave 2, and the file-ownership intersection — COMPUTED, not asserted

| Plan | Wave | `files_modified` |
|---|---|---|
| 21-31 | 1 | `src/ui/screens/detail.rs`, `src/session_detector.rs` |
| 21-32 | 1 | `src/text.rs`, `src/ui/screens/driver.rs` |
| 21-33 | 1 | `src/ui/mod.rs`, `src/ui/screens/mod.rs`, `src/ui/screens/render_escape_guard.rs`, `src/state_reader/backlog.rs` |
| **21-34** (this plan) | 2 | `deferred-items.md`, `COVERAGE.md`, `tests/driver_reattach.rs` |

Wave 1's three pairwise intersections are all empty and its union is 8 distinct files with none
owned twice; **`21-34 ∩ (21-31 ∪ 21-32 ∪ 21-33) = ∅`** as well. This plan is nevertheless in wave
2 rather than wave 1, and NOT because of a file conflict: its `depends_on` is a genuine ordering
dependency. Every residual it records must be quoted from what the three plans actually
delivered, and every gate figure it commits must be measured on the MERGED tree — prohibition 6
forbids inheriting a wave-1 worktree's number.
</wave_parallelism>

<edge_probe_audit>
The spec-less probe fallback's full seven-row disposition and the three flagged planner
assumptions are recorded once, in **`21-31`'s `<edge_probe_audit>`**, which is round 11's single
audit location. This plan carries two of those rows: **DRIVE-04 boundary** and **DRIVE-04
precision**, both resolved as **backstop** structured markers in `must_haves.truths`, each
reconfirmed by an explicit re-run of `tests/driver_escalation_cap.rs` at its unchanged counts
with the file shown absent from round 11's diff — explicit evidence rather than inference from
the round's scope. Nothing is restated here, so there is one place to check the no-silent-drop
equality.
</edge_probe_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a committed record → the next reader | the boundary this whole plan guards: a superseded mitigation standing first in a file is what a reader acts on |
| a dependency's measured behaviour → a claim resting on it | the `claude` CLI's `--resume` arity and ratatui's grapheme filtering are both properties of a VERSION, and nothing in this tree goes red when either changes |
| a wave-1 worktree's measurement → the merged tree | three plans measured in three trees; only the merged tree's numbers are the round's numbers |
| a partial correction → the appearance of a complete one | correcting some locations and not others produces a record that reads as corrected and is not |

## STRIDE Threat Register (ASVS L1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-34-01 | Repudiation | `deferred-items.md` lines 19-21 — a mitigation claim measured false by round 10 still stands first in the file, above three later corrections, so a reader acts on the wrong one | **high** | mitigate | Task 1: the correction is placed AT the stale sentence, its completeness established by a committed grep inventory classifying every hit, and mirrored into the test file's own module comment. |
| T-21-34-02 | Repudiation | a partial correction reading as a complete one — the exact species this round closes, applied to the record itself | **high** | mitigate | Task 1: every grep hit enumerated and classified as standing (corrected) or dated observation (named, unedited, with the reason); the total stated so the classification is checkable; prohibition 1 forbids selectivity. |
| T-21-34-03 | Tampering | a future `claude` CLI changing `--resume`'s arity or its `=`-form binding, silently invalidating gaps[0]'s fix with every test in this repository green | **high** | mitigate | Task 2: a standing staleness obligation carrying the exact probe commands and the measured version, mirroring the ratatui obligation already in this file. Direction: under-detection, silent, so the obligation is the only thing that catches it. |
| T-21-34-04 | Elevation of privilege | `src/executor/claude.rs`'s three `--option value` pairs acquiring a live untrusted value later, re-opening CWE-88 at a second seam | medium | accept | Task 2: recorded as a standing item with its measurement (all three fields `None` at `ExecutionOptions::default()`, set by no caller), its promote condition (the first caller that sets one from a value not authored in this crate) and its direction (under-detection, SILENT until that caller appears). Accepted, not closed, and the reason is stated. |
| T-21-34-05 | Denial of service | the `driver_reattach` flake being chased as a regression, or masking a real one | medium | accept | Task 1 corrects the record; the fix stays a standing item with its promote condition because its direction is false-red under parallelism, which is LOUD and therefore the safe direction. Prohibition 4 forbids attempting the fix here. |
| T-21-34-06 | Repudiation | `COVERAGE.md` being rewritten rather than amended, losing a prior round's reasoned declaration | low | mitigate | Task 3: append-only; the existing declaration is asserted byte-identical and only a dated round-11 paragraph is added. |

**Package legitimacy:** no `cargo add`, no new dependency, no `Cargo.toml` or `Cargo.lock`
change in any of round 11's four plans; Task 3 measures `git diff Cargo.toml Cargo.lock` over
the whole round and asserts it empty. The `## Package Legitimacy Audit` gate does not fire.
</threat_model>

<decisions>
## Decisions and reversibility (REVERSIBILITY_GATES=true)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| D-21-61 | The flake correction's completeness is established by a committed grep inventory with every hit classified, not by an author's sweep | **reversible** | Correcting some locations and not others is this round's own species applied to the record. An enumeration with a stated total is checkable by the next verifier in one command. Documentation only. |
| D-21-62 | Dated per-run observations in executed SUMMARYs are NAMED and left unedited; only STANDING claims are corrected in place | **reversible** | A SUMMARY sentence saying a particular run passed 3/3 under a particular setting is a true record of that run. Editing it would falsify a historical observation to make a general point. Naming it in the corrections table keeps the record complete without rewriting history. |
| D-21-63 | A comment-only note is added to `tests/driver_reattach.rs`, the one file under `tests/` this round touches | **reversible** | The reader who hits the flake reads the test file, not `.planning/`. The cost — the next verifier can no longer use "no file under `tests/` in the diff" as a shortcut — is paid for by asserting the diff is comment-only, which is narrower and checkable. `tests/driver_injection_corpus.rs` stays untouched. |
| D-21-64 | A new standing staleness obligation is opened for the `claude` CLI's option parser | **reversible** | gaps[0]'s fix rests on a measured dependency behaviour exactly as the invisible-character work rests on ratatui's. This tree already carries one such obligation; a second beside it costs one entry and closes the same class of silent invalidation. |
| D-21-65 | `COVERAGE.md` is amended append-only and its existing declaration is left byte-identical | **reversible** | It is accurate for round 11's scope — no external API, the `claude` subprocess seam hardened rather than integrated — and regressing a reasoned declaration to re-derive it would lose the reasoning. |

No decision in this plan is `one-way`; no `checkpoint:decision` gate is required. This plan
changes no executable line anywhere in the tree — its entire diff is documentation and one
comment-only test-file note — so every change is revertible from git with no data migration and
no on-disk format change. There is no `costly` rating either.
</decisions>

<named_shape_audit>
## Named-shape audit (this plan's slice)

| # | Named shape (source) | Owner | Covered by |
|---|---|---|---|
| 1 | A superseded mitigation standing first in the record above three later corrections (phase constraint 4) | 21-34 T1 | corrected at the stale sentence, completeness established by a committed grep inventory |
| 2 | The failing test file carrying no note about its own documented flake | 21-34 T1 | a comment-only module note, with the diff asserted comment-only |
| 3 | gaps[0]'s fix resting on an unrecorded dependency behaviour (the `claude` CLI's option parser) | 21-34 T2 | a standing staleness obligation with the probe commands and the measured version |
| 4 | `src/executor/claude.rs`'s three `--option value` pairs (T-21-31-05) | 21-34 T2 | a standing item with its measurement, promote condition and direction |
| 5 | Pass 11's `IN-01`..`IN-04` and `WR-06` | 21-34 T2 | recorded as deliberate non-closures, one line of reason each |
| 6 | Four round-10 truths measured FALSE as shipped (21-27 t1, 21-27 t5, 21-28 t7, 21-29 t2) | 21-34 T2 | a Record-corrections table naming what was claimed, what was measured, and the plan and task that closed each |
| 7 | ROADMAP criterion 4's behavioural half | 21-34 T2 | re-surfaced VERBATIM, no work claimed, and stated in words to be permanently agent-unclosable so it is not re-litigated |
| 8 | The argument injection and its complicit corpus (gaps[0]) | **21-31** | delivered in wave 1; this plan records its residual |
| 9 | The two census mechanism defects (gaps[1], gaps[2]) | **21-32** | delivered in wave 1; this plan records their narrowed claims |
| 10 | The false completeness claim, WR-04, WR-05 and 21-29's S1/F3 (gaps[3]) | **21-33** | delivered in wave 1; this plan records the pin's direction and the F3 decline |
</named_shape_audit>

<tasks>

<task type="auto">
  <name>Task 1: correct the flake record everywhere it is wrong, and prove the "everywhere" with an inventory</name>
  <files>.planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md, tests/driver_reattach.rs</files>
  <read_first>
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md lines 1-70 — the original flake section with its two-binary table, the STANDING mitigation sentence, and the two later updates that qualify it. Read all three before writing, so the correction quotes what it corrects rather than restating it.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md lines 960-1005 — the `21-30` entry with the corrected mechanism (a system-wide `/proc` scan that does not stop at the process-group or worktree boundary), its promote condition and its direction. That entry is the understanding that stands; the correction points at it rather than duplicating it.
    - tests/driver_reattach.rs lines 1-40 — the module header comment block, which explains what the file proves and what it deliberately does not attempt. The new note goes in that block's idiom.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-30-SUMMARY.md around line 504 — round 10's measurement of the flake against the untouched base, which is the evidence the correction rests on.
  </read_first>
  <action>
    (a) **Build the inventory first, and commit it to the SUMMARY.** Run, under `rtk proxy`,
    a recursive grep for `test-threads` and a recursive grep for `driver_reattach` over
    `.planning/`, `tests/` and `src/`. Enumerate EVERY hit in the SUMMARY as a table of
    `file:line` plus the quoted sentence, and classify each as exactly one of:
    **STANDING** — a claim about how the binary should be run or mitigated, which a future reader
    would act on; or **DATED OBSERVATION** — a record of what one specific run did on one
    specific day. State the total number of hits and the count in each class, so the
    classification can be checked against the inventory rather than trusted. No hit may be left
    unclassified (prohibition 1).

    (b) **Correct every STANDING hit in place.** For each, add the correction beside it, dated,
    quoting the superseded sentence verbatim, and stating the three facts round 10 proved:
    pre-existing and not a regression, measured against the untouched base at the same rate;
    `--test-threads=1` does NOT reliably fix it; and the mechanism is a system-wide `/proc` scan
    that does not stop at the process-group or worktree boundary. The correction at the FIRST
    flake section must be positioned so a reader who stops there reads it — beside the stale
    sentence, not only in a later section — and must point at the `21-30` entry for the promote
    condition rather than duplicating it.

    (c) **Leave every DATED OBSERVATION unedited, and say why.** Add one paragraph to the
    corrections table stating that a SUMMARY sentence recording what a particular run did is a
    true record of that run, that editing it would falsify a historical observation to make a
    general point, and that they are named here so the inventory is complete rather than
    selective (D-21-62). List them by `file:line`.

    (d) **Add the note where the reader of the failing test will find it.** In
    `tests/driver_reattach.rs`'s module header comment block, in that block's existing idiom, add
    a short section recording the same three facts, naming the two tests that flake, saying
    explicitly that reaching for `--test-threads=1` will not help, and pointing at the standing
    item in `deferred-items.md` by file name. Add the round-11 measurement as one more data
    point: the planner's own single `--workspace` run at pre-round-11 HEAD, with no
    `--test-threads` setting and before any file was edited, reported 1416 passed / 1 failed /
    13 ignored with that failure being one of these two tests.

    (e) **Assert the test file's diff is comment-only.** Every added and removed line in
    `rtk proxy git diff -- tests/driver_reattach.rs` must begin with a comment marker after
    trimming. State in the SUMMARY that this is the ONE file under `tests/` round 11 touches,
    that its diff is comment-only and asserted so, and that `tests/driver_injection_corpus.rs`
    is untouched — so the next verifier replaces the "no file under `tests/` in the diff"
    shortcut with this narrower, checkable statement.

    (f) **Do not attempt the fix** (prohibition 4). The promote condition — scoping the run
    discovery to the test's own process group or worktree — stays a standing item, and its
    direction (false-red under parallelism, LOUD) is what makes deferring it safe.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries the full grep inventory as a table of `file:line` with the quoted sentence and a STANDING / DATED OBSERVATION classification for every hit, plus the total and the per-class counts; no row is unclassified.
    - Every hit classified STANDING has a dated correction beside it quoting the superseded sentence verbatim; the SUMMARY quotes each correction.
    - `rtk proxy grep -n "test-threads" .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md` output is quoted in the SUMMARY, with each line's classification named, and the first flake section's stale sentence is shown to be immediately followed by its correction.
    - `rtk proxy git diff -- tests/driver_reattach.rs` shows only comment lines added or removed, and the SUMMARY states that explicitly alongside `rtk proxy git diff --stat -- tests/` naming that file and no other.
    - The test file's new note names both flaking tests, states that `--test-threads=1` does not fix it, names the `/proc`-scan mechanism, and points at `deferred-items.md`; the SUMMARY quotes the note in full.
    - `rtk proxy cargo test --test driver_reattach` is run and its result reported — green or the documented flake, named as such — and the SUMMARY states that the note is comment-only so the binary's behaviour is unchanged either way.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --test driver_reattach</automated>
  </verify>
  <done>The `driver_reattach` flake record says the same true thing in every place a reader will look: the standing mitigation claim is corrected at the sentence itself, every grep hit is enumerated and classified so the correction's completeness is checkable rather than asserted, and the test file itself carries the three corrected facts with a comment-only diff.</done>
  <reversibility rating="reversible">Documentation and one comment-only test-file note. No executable line changes anywhere; revertible from git.</reversibility>
</task>

<task type="auto">
  <name>Task 2: the standing record — round 11's residuals with directions, the deliberate non-closures, the four corrected truths, and criterion 4 verbatim</name>
  <files>.planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md</files>
  <read_first>
    - The three wave-1 SUMMARYs — `21-31-SUMMARY.md`, `21-32-SUMMARY.md`, `21-33-SUMMARY.md` — in full. Every residual recorded here must be one those plans actually delivered, quoted from their own words, not one this plan's text predicted.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md — the existing standing-obligations section, especially the ratatui grapheme-filtering obligation, whose shape the new `claude` CLI obligation mirrors; and the existing criterion-4 entry, which is re-surfaced verbatim.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md lines 337-386 and 426-444 — the round-10 must-have table with its four FAILED rows, and pass 11's anti-pattern table with `IN-01`..`IN-05` and `WR-04`..`WR-06`. Every row of both must be accounted for here: closed by a round-11 plan, or recorded as a deliberate non-closure with a reason.
  </read_first>
  <action>
    (a) **Record round 11's four closures with their residuals and DIRECTIONS**, each quoted from
    the delivering SUMMARY rather than from this plan's text: gaps[0] (the fusion, with the
    dependency-behaviour residual); gaps[1] (the comment-budget fix, with the executable-line
    window as measured and the five directed residuals); gaps[2] (the innermost verdict, with
    its new over-detection LOUD direction); gaps[3] (the narrowed claim, with the pin's
    bookkeeping-red direction and the shrinking-coverage property).

    (b) **Open the `claude` CLI staleness obligation**, beside the ratatui one and in its shape.
    It must carry: the exact probe commands (the `--help` spec line and the five `--resume`
    probes), the CLI version they were measured against as reported by `claude --version`, the
    property they establish (`-r, --resume [value]` is an optional-value option; the `=` form
    binds the value regardless of its first byte; the `--` form does NOT bind and deletes the
    capability), the direction (**under-detection, silent** — nothing in this tree goes red if a
    future CLI changes this), and what to do (re-run the five probes and re-measure before
    trusting the fusion).

    (c) **Record the three deliberate non-closures**, each with a promote condition and a
    direction: `src/executor/claude.rs`'s three `--option value` pairs with the measurement that
    makes them inert today and the first-caller promote condition; pass 11's `IN-01`, `IN-02`,
    `IN-03` and `IN-04` with one line of reason each; and `WR-06` (the missing per-emulator
    working-directory column) with the reason it is a capability question rather than a security
    one and the direction its failure takes. `IN-05` is CLOSED by `21-32` Task 2 — say so and
    cite it rather than listing it as outstanding.

    (d) **Write the Record-corrections table.** One row each for `21-27` truth 1, `21-27` truth
    5, `21-28` truth 7 and `21-29` truth 2, with four columns: what was claimed, what pass 11
    MEASURED, the plan and task that closed it in round 11, and the evidence the closing plan
    committed. Append-only, dated, quoting the falsified claim verbatim.

    (e) **Re-surface ROADMAP criterion 4 VERBATIM**, unchanged, for the fourth consecutive
    round, with no work claimed against it — and add, in this round's entry, the sentence that it
    is **PERMANENTLY AGENT-UNCLOSABLE by construction**: it needs a human with a live
    authenticated Claude subscription to run the ten `#[ignore]`d arms of
    `tests/driver_injection_corpus.rs`, it is a standing item by explicit user decision, and
    **4/5 is the expected and correct ceiling for this phase, not a failure**. State that
    sentence plainly so the next verifier does not re-litigate it. Re-run
    `cargo test --test driver_injection_corpus` and record its unchanged counts with
    `git diff --stat` showing the file untouched by round 11.

    (f) **Reconfirm DRIVE-04 explicitly, not by inference.** Run
    `rtk proxy cargo test --test driver_escalation_cap`, record the counts and both cap
    directions, and show the file absent from round 11's `git diff --stat`. Record both the
    boundary and the precision backstop rows with that evidence.
  </action>
  <acceptance_criteria>
    - Every one of pass 11's four FAILED round-10 truths appears as a row in the Record-corrections table with all four columns filled, and the SUMMARY quotes the table.
    - Every row of pass 11's anti-pattern table (`IN-01`..`IN-05`, `WR-04`..`WR-06`) is accounted for in `deferred-items.md` as either closed-with-citation or a deliberate non-closure with a reason; the SUMMARY lists all of them with their disposition and states the total so none can be silently dropped.
    - The `claude` CLI staleness obligation contains the six probe commands verbatim, the measured `claude --version`, the property established, an explicit direction word, and the re-measure instruction; the SUMMARY quotes it in full.
    - `rtk proxy cargo test --test driver_injection_corpus` reports 13 passed / 0 failed / 10 ignored (or the measured values, with any difference reported), and `rtk proxy git diff --stat` over round 11 shows that file unchanged.
    - `rtk proxy cargo test --test driver_escalation_cap` is run, its counts quoted, both cap directions named, and the file shown absent from round 11's diff.
    - The criterion-4 entry is byte-identical to the prior round's and the new round-11 sentence states permanently-agent-unclosable and the 4/5 ceiling in words; the SUMMARY quotes both.
    - `rtk proxy git diff -- .planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md` shows additions only — no line removed from any existing section.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --test driver_escalation_cap</automated>
  </verify>
  <done>The standing record carries round 11's four closures with their residuals and directions, a new `claude` CLI staleness obligation in the shape of the ratatui one, the three deliberate non-closures with promote conditions, a Record-corrections table for the four round-10 truths pass 11 measured false, every anti-pattern row dispositioned, and criterion 4 verbatim with an explicit statement that it is permanently agent-unclosable and that 4/5 is the correct ceiling.</done>
  <reversibility rating="reversible">Append-only documentation. No executable line changes; revertible from git.</reversibility>
</task>

<task type="auto">
  <name>Task 3: amend COVERAGE.md append-only, and measure the round's gate on the merged tree</name>
  <files>.planning/phases/21-llm-goal-layer-prompt-injection-hardening/COVERAGE.md</files>
  <read_first>
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/COVERAGE.md in full — the existing reasoned no-external-API declaration, its enumeration of what round 10's diff changed, its explanation of why both detector signals were false positives, and its argument that the `claude` subprocess seam is a program this phase HARDENS rather than an interface a round INTEGRATES. Confirm for yourself that the argument still holds for round 11's scope before appending anything.
    - The three wave-1 SUMMARYs' `git diff --stat` sections, so the round-11 paragraph names what round 11 actually changed rather than what it planned to.
  </read_first>
  <action>
    (a) **Confirm the existing declaration still holds, and say so.** Round 11 integrates no
    external API, SDK or service. Its whole diff changes: one argv element's shape; two census
    algorithms and their claims; one sort comparator clause; a set of controls, fixtures and doc
    corrections; and three documentation files. The `claude` binary is still a spawned program
    this round HARDENS — round 11 changes how an untrusted value is bound to an option name, not
    whether an interface is integrated — so it is still deliberately not a matrix row, for the
    reason the existing file already gives. Append a dated round-11 paragraph saying this;
    change no existing line (prohibition 2).

    (b) **Measure the no-dependency claim rather than asserting it.** Run
    `rtk proxy git diff <pre-round-11-base>..HEAD -- Cargo.toml Cargo.lock` over the whole round
    and assert it empty, quoting the command and its empty output. That is a stronger check than
    naming any single crate and it is what the round's own no-new-dependency prohibition
    requires.

    (c) **Measure the round's gate on the MERGED tree** and quote every figure, re-measured, with
    any difference from a wave-1 worktree's figure REPORTED with both numbers (prohibition 6):
    `cargo build` exit 0; `cargo test --workspace --no-fail-fast` with the total attributed —
    every test added by `21-31`, `21-32` and `21-33` named and counted so the delta from the
    1417/0/13 green base is fully explained; `cargo clippy -- -D warnings` exit 0; and
    `cargo clippy --all-targets -- -D warnings` reporting exactly FOUR lints of the same two
    kinds in `src/browser.rs` and `src/project_creator.rs`, with a count of three treated as a
    failure exactly as five would be.

    (d) **Confirm the round's file fence held.** `rtk proxy git diff --stat` over the whole round
    must name exactly the ten files the four plans own — `src/ui/screens/detail.rs`,
    `src/session_detector.rs`, `src/text.rs`, `src/ui/screens/driver.rs`, `src/ui/mod.rs`,
    `src/ui/screens/mod.rs`, `src/ui/screens/render_escape_guard.rs`,
    `src/state_reader/backlog.rs`, `tests/driver_reattach.rs`, plus the two `.planning/` records
    — and nothing else. Any extra file is REPORTED as a fence breach with the plan that produced
    it, never absorbed.

    (e) **Confirm `.planning/REQUIREMENTS.md` is untouched** for the ninth consecutive round, by
    `git diff --stat` over the whole round.
  </action>
  <acceptance_criteria>
    - `rtk proxy git diff -- .planning/phases/21-llm-goal-layer-prompt-injection-hardening/COVERAGE.md` shows additions only; the SUMMARY states that the existing declaration came out byte-identical.
    - The appended round-11 paragraph names what round 11 changed instead of an API integration, and the SUMMARY quotes it.
    - `rtk proxy git diff <base>..HEAD -- Cargo.toml Cargo.lock` is quoted with its empty output.
    - The merged-tree gate figures are all quoted: build exit code, workspace totals with every delta attributed to a named test, lib clippy exit code, and the `--all-targets` lint count with kinds and files.
    - `rtk proxy git diff --stat` over the whole round is quoted and every named file is one of the ten the four plans own; any extra is reported as a fence breach with its owning plan.
    - `.planning/REQUIREMENTS.md` is shown absent from the round's `git diff --stat`.
    - Any `driver_reattach` failure in the merged-tree run is named as the documented flake, shown green on an isolated re-run, and cross-referenced to Task 1's corrected record.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --workspace --no-fail-fast</automated>
  </verify>
  <done>`COVERAGE.md` carries a dated round-11 amendment confirming its existing declaration still holds, with no existing line changed; the no-dependency claim is measured rather than asserted; and the round's gate is re-measured on the merged tree with every delta attributed, the file fence confirmed, and REQUIREMENTS.md shown untouched.</done>
  <reversibility rating="reversible">Append-only documentation plus measurement. No executable line changes; revertible from git.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
**Created by 21-34** (`21-31`'s, `21-32`'s and `21-33`'s symbols are listed in those plans):

| Kind | Symbol | File |
|---|---|---|
| record | the grep inventory of every `test-threads` / `driver_reattach` hit, classified STANDING or DATED OBSERVATION | `21-34-SUMMARY.md` |
| record | the flake correction placed at the stale mitigation sentence, dated, quoting what it supersedes | `deferred-items.md` |
| comment | the module note in the failing test file naming both tests, the non-working mitigation and the `/proc`-scan mechanism | `tests/driver_reattach.rs` |
| record | the `claude` CLI option-parser staleness obligation — six probe commands, the measured version, the property, the direction | `deferred-items.md` |
| record | three deliberate non-closures with promote conditions and directions (the executor argv pairs, `IN-01`..`IN-04`, `WR-06`) | `deferred-items.md` |
| record | the Record-corrections table for `21-27` t1, `21-27` t5, `21-28` t7 and `21-29` t2 | `deferred-items.md` |
| record | ROADMAP criterion 4 re-surfaced verbatim with the permanently-agent-unclosable statement and the 4/5 ceiling | `deferred-items.md` |
| record | the dated round-11 amendment to the no-external-API declaration | `COVERAGE.md` |

**Changed:** nothing executable anywhere. `deferred-items.md` and `COVERAGE.md` gain append-only
sections; `tests/driver_reattach.rs` gains comment lines only, asserted so.

**Removed:** nothing. Both records are append-only and the existing `COVERAGE.md` declaration is
byte-identical.

**Unchanged and RUN, never edited:** `tests/driver_injection_corpus.rs` (13/0/10),
`tests/driver_escalation_cap.rs`, `tests/spawn_seam_guard.rs`, `src/browser.rs`,
`src/project_creator.rs`, `.planning/REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<verification>
- `rtk proxy cargo build` exit 0 on the merged tree; `rtk proxy cargo test --workspace --no-fail-fast`
  with every delta from the 1417/0/13 green base attributed to a named test added by `21-31`,
  `21-32` or `21-33`; any `driver_reattach` failure named as the documented flake and shown green
  on an isolated re-run.
- `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings`
  reports exactly four lints, same two kinds, same two files — quoted.
- The grep inventory is committed to the SUMMARY with every hit classified and the totals stated.
- `rtk proxy git diff -- tests/driver_reattach.rs` shows comment lines only.
- `rtk proxy git diff` over both `.planning/` records shows additions only.
- `rtk proxy git diff <base>..HEAD -- Cargo.toml Cargo.lock` is empty.
- `rtk proxy git diff --stat` over the whole round names exactly the ten owned files.
- `rtk proxy cargo test --test driver_injection_corpus` at 13/0/10 with the file unchanged; no
  work claimed against criterion 4.
- `rtk proxy cargo test --test driver_escalation_cap` run and quoted, both cap directions named.
- `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
- The `driver_reattach` record says the same true thing in every place a reader will look, and
  the completeness of that is checkable from a committed inventory rather than trusted.
- Every residual round 11 leaves is recorded with a direction, and every item round 11 declined
  is recorded with a promote condition and a reason.
- The dependency behaviour gaps[0]'s fix rests on has a standing obligation, so a future CLI
  change is re-measured rather than silently assumed.
- Every round-10 truth pass 11 measured false is named, with what was claimed, what was measured,
  and where it is now closed.
- ROADMAP criterion 4 is re-surfaced verbatim, no work is claimed against it, and the record says
  in words that it is permanently agent-unclosable and that 4/5 is the correct ceiling.
- The round's gate is measured on the merged tree, its file fence is confirmed, and no number in
  the SUMMARY is inherited from a wave-1 worktree.
</success_criteria>

<output>
Create `.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-34-SUMMARY.md` when done.
</output>
