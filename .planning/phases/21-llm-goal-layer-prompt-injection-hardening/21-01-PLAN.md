---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/executor/mod.rs
  - src/executor/claude.rs
  - src/executor/stream_json.rs
  - src/driver/goal.rs
  - src/driver/untrusted.rs
  - src/driver/mod.rs
  - tests/driver_model_seam.rs
  - tests/spawn_seam_guard.rs
autonomous: true
requirements: [DRIVE-01, DRIVE-03, SAFE-07, SAFE-08]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A plain-language goal handed to the model seam comes back as a schema-conformant structured payload whose named command re-parses to a `router::RouterAction`, proven against the real `claude` binary end to end (ROADMAP criterion 1)."
    - "The OQ1 question — does goal decomposition need file bodies? — is answered empirically by two live arms recorded in-tree, and the answer decides whether the empty-tool seam survives; the plan halts rather than assuming (A2, research Open Question 1)."
    - "Untrusted content containing the boundary's own closing form does not terminate the boundary: the body is JSON-encoded so a literal closing tag is escaped, and the tag carries a per-call CSPRNG nonce so the closing form is unguessable (SAFE-07 boundary probe, research Pitfall 5)."
    - "A third-party string rendered into a record is length-bounded and control-character-stripped, truncation lands on a UTF-8 character boundary rather than mid-codepoint, and a truncated value is marked as truncated (SAFE-07 precision probe)."
    - "The model seam's spawn carries an empty tool set, an explicit suppression of `CLAUDE.md` auto-discovery, and a pinned structured-output retry count, and never the hook-disabling safe-mode flag — asserted against the value `build_argv` and the env closure actually produce, never against a constant those functions also read."
    - "Every step of a decomposed plan names a command that is a member of `router::SAFE_COMMAND_ALPHABET`, a target phase that is a plain path component, and a terminal state that reduces to `router::is_goal_met`; a goal that cannot be so reduced is refused at start, naming the part that could not be reduced (DRIVE-03, OQ6)."
  prohibitions:
    - statement: "MUST NOT report an injection control as satisfied on the strength of an assertion that never proved the hostile content reached the model — a negative-only corpus assertion is not evidence."
      category: transparency
    - statement: "MUST NOT weaken or bypass the Phase 19 PreToolUse envelope in the name of hardening; no profile may pass a flag that disables hooks."
      category: safety
  artifacts:
    - path: "src/driver/goal.rs"
      provides: "The structured plan type, its JSON Schema built from SAFE_COMMAND_ALPHABET, the RouterAction re-parse, the legality predicate, and the goal_ refusal taxonomy"
      contains: "SAFE_COMMAND_ALPHABET"
    - path: "src/driver/untrusted.rs"
      provides: "The nonce-suffixed, JSON-encoded untrusted-content boundary and the enumerated third-party string set"
    - path: "tests/driver_model_seam.rs"
      provides: "The live end-to-end seam proof and the two OQ1 arms"
  key_links:
    - from: "src/driver/goal.rs"
      to: "src/driver/router.rs"
      via: "the JSON Schema enum is populated from router::SAFE_COMMAND_ALPHABET and the re-parse walks router::RouterAction::ALL"
      pattern: "SAFE_COMMAND_ALPHABET|RouterAction::ALL"
    - from: "src/executor/claude.rs"
      to: "src/executor/mod.rs"
      via: "build_argv and the spawn closure match exhaustively on ExecutionOptions::profile"
      pattern: "SpawnProfile::"
---

<objective>
Build the model seam end to end — one plain-language goal in, one validated `RouterAction`
out — through the single existing spawn seam, and answer the phase's largest design risk
before anything else is committed to.

Purpose: Phase 20 proved the deterministic router. This plan proves the only place a model is
allowed to influence what the driver does, and proves it against the real binary rather than
against a mock, because research found the officially-recommended defence fails **silently**
on this transport (C-3). It also settles OQ1 — whether goal decomposition needs file bodies —
because if it does, the empty-tool seam is untenable and SAFE-07's surface grows substantially.

Output: `src/driver/goal.rs`, `src/driver/untrusted.rs`, a second argv/env profile on the one
audited spawn seam, `structured_output` on the wire model, a live end-to-end seam test carrying
the recorded OQ1 answer, and the source-scanning guards that keep the profile's controls coupled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-CONTEXT.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-RESEARCH.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The model seam, wired through every layer, and the OQ1 gate</name>
  <reversibility rating="costly">The seam profile discriminant becomes an exhaustive match arm at
  `build_argv` and at the spawn closure; a third profile later is a compile error at both, which is
  the intended cost, but reshaping the discriminant itself touches every match site.</reversibility>
  <files>src/executor/mod.rs, src/executor/claude.rs, src/executor/stream_json.rs, src/driver/goal.rs, src/driver/untrusted.rs, src/driver/mod.rs, tests/driver_model_seam.rs</files>
  <read_first>
    - `src/executor/claude.rs` — `build_argv` (:239-296, the `match options.target { ExecutionTarget::Host => {} }` exhaustive-discriminant shape at :245, `--strict-mcp-config` at :261) and the spawn closure (:448-497, the `CLAUDE`-prefix scrub loop at :472 and the `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` re-set at :476 — the exact precedent for a scrubbed-then-re-set variable, and the `envelope_env` application at :485-496).
    - `src/executor/mod.rs:278-345` — `ExecutionOptions`, where the profile discriminant is declared beside `target`.
    - `src/executor/stream_json.rs:284-341` — `ResultMessage`, and `permission_denials` as the precedent for an unmodelled wire field; `:14-18` for the tolerant-parse posture.
    - `src/driver/router.rs:290-384` — `COMMAND_DISCUSS_PHASE`/`COMMAND_PLAN_PHASE`/`COMMAND_EXECUTE_PHASE`, `SAFE_COMMAND_ALPHABET`, `RouterAction`, `RouterAction::verb`, `RouterAction::ALL`, and the private `command_for` whose doc records WR-09.
    - `src/driver/rate_limit.rs:1-33` and `:126-135` — the module-doc register for an untrusted payload and `MAX_OBSERVED_WINDOW_CHARS`, the "type carries it verbatim, the rendering is bounded" split.
    - `21-RESEARCH.md` § "Pattern 1: The structured-output call", § "Pattern 2: The untrusted-content envelope", § "Pattern 3: The seam argv profile", and the § "Code Examples" verified invocation.
    - `21-PATTERNS.md` § `src/executor/claude.rs (MOD)` and § `src/driver/untrusted.rs (NEW)`.
  </read_first>
  <action>
Wire ONE goal through every layer the phase touches, and make the walk answer OQ1.

Add `SpawnProfile` to `src/executor/mod.rs` as a field on `ExecutionOptions`, with two variants:
`Executor` (skills, hooks, `CLAUDE.md` and the Phase 19 envelope all live — the behaviour every
existing caller gets today, so `ExecutionOptions::default()` selects it and no existing run
changes) and `ModelSeam { json_schema: String }`. Match it exhaustively with no wildcard at every
site, in the same register as the existing `ExecutionTarget::Host` arm, so a third profile is a
compile error rather than a silently host-shaped spawn. Its doc must name the declined
alternative — a hook-disabling safe-mode flag — and state that the Phase 19 envelope is enforced
by a PreToolUse hook, so choosing that flag would disarm the git boundary in the name of
hardening. Do not add a second spawn site: `tests/spawn_seam_guard.rs` fails in both directions
on one.

In `build_argv`, under `ModelSeam`, push an empty-string value for the tool flag (`--tools` with
`""`) and `--json-schema` with the schema string inline. `--json-schema` takes inline JSON only —
a file path is rejected at startup with exit 1 and nothing on stdout, so never write the schema
to a temp file. Leave `--strict-mcp-config` exactly where it is at :261, unconditional for both
profiles, and push no `--mcp-config` under either: with none supplied the permitted MCP set is
empty and supplying one would widen it from empty to whatever that file names (C-2).

In the spawn closure, after the `CLAUDE`-prefix scrub loop and beside the existing
`CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` assignment, set two variables under `ModelSeam` only:
`CLAUDE_CODE_DISABLE_CLAUDE_MDS` to `1`, and `MAX_STRUCTURED_OUTPUT_RETRIES` to `1`. Comment both
with the reason they are distinct instructions: the first is a `CLAUDE`-prefixed name the scrub
loop removes and must therefore be re-set (the `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` precedent
one line above); the second does **not** carry that prefix, survives the scrub, and must be set
explicitly to deny an ambient operator value — an inherited large value would make one
driver-side escalation up to that many model turns and turn the DRIVE-04 count into a lie
(research Pitfall 2).

Add `structured_output: Option<serde_json::Value>` to `ResultMessage` with `#[serde(default)]`,
following `permission_denials` exactly: an unmodelled wire shape carried as `Value`, no strict
unknown-field rejection, no panicking accessor, no `unwrap` on a wire field. Read it off the
**terminal** result envelope the existing outcome derivation already selects with `last`, never
by indexing a turns vector — the CLI populates it from the last `StructuredOutput` call, and this
repo has already proved a result envelope is a turn boundary rather than a run terminator
(research Pitfall 4).

Create `src/driver/untrusted.rs` and declare it from `src/driver/mod.rs`. It exposes one
constructor that takes a source label and a `serde_json::Value` of third-party fields and returns
the boundary string: a tag suffixed with a `Uuid::new_v4()` nonce (`uuid` is already a dependency
with the `v4` feature, so no new crate), wrapping a JSON document carrying the source label, an
explicit untrusted marker, and the fields. Both halves are required and the module doc must say
why neither alone suffices: JSON escaping means a literal closing tag inside the content cannot
form one, and the CSPRNG nonce means the closing form cannot be guessed by a payload that only
knows the tag's shape. A module-level fixed boundary constant is forbidden here — state that in
the doc, and contrast it with `dry_run.rs`'s pinned constants, which are user-facing output and
correctly pinned; the two are not the same thing. Also export the enumerated third-party string
set the seam may be shown — `RoadmapPhase::name`, `RoadmapPhase::description`, and
`ProjectState`'s `status`, `current_phase_name`, `milestone` and `pause_context` — as data rather
than as a comment, plus `MAX_UNTRUSTED_FIELD_CHARS`, the bound applied where a third-party string
is rendered into a record line. Truncation must slice on a character boundary rather than a byte
index, and a truncated value must be marked as truncated, following
`rate_limit.rs::MAX_OBSERVED_WINDOW_CHARS`: the type carries the value whole, the rendering is
bounded and control-character-stripped.

Create `src/driver/goal.rs` and declare it from `src/driver/mod.rs`. For this task it needs only
two things: a function returning the escalation schema as a `serde_json::Value` whose `command`
property's `enum` is populated **from** `router::SAFE_COMMAND_ALPHABET` rather than restated as a
literal list, so widening the alphabet without widening the schema is impossible; and a re-parse
that takes the payload's named command as a string and finds the matching `router::RouterAction`
by walking `RouterAction::ALL` and comparing `verb()`. Anything that does not match is an error
carrying the named string. Its doc states the control relationship plainly: the wire schema is
defence in depth, the Rust re-parse is the control, because strict-schema derivation in the CLI
is best-effort and can fall back. The named-but-unmatched string is returned for the caller to
record and is never interpolated into a command string, a log format or a preview — the private
`command_for` in `router.rs` and the WR-09 it records are the precedent.

Create `tests/driver_model_seam.rs`. It carries the argv/env shape assertions, which are pure and
run everywhere, plus the two live OQ1 arms, which spawn the real binary and are `#[ignore]`d.
The shape assertions call `build_argv` with a `ModelSeam` options value and inspect the returned
vector; they must never assert about a constant that `build_argv` also reads.

The two live arms are the OQ1 gate. Both are handed the same plain-language goal and the same
target project fixture. **Arm A** is shown typed state only — phase numbers, disk-status tokens,
verification-status tokens, and phase names as short labels. **Arm B** is shown the same typed
state plus this project's own ROADMAP prose passed through `untrusted::` as one boundary block.
Each arm asserts the returned payload is schema-conformant and its command re-parses to a
`RouterAction`, and records whether the chosen target phase is the one a human would choose for
that goal. Record the observed outcome of both arms verbatim in a doc comment at the top of
`src/driver/goal.rs`, naming the binary version they were run against, in the register
`21-RESEARCH.md` uses for its spikes. Both arms must also assert the live seam reports an empty
MCP server list and a tool set containing only the structured-output tool, so the OQ1 measurement
cannot be confounded by a seam that quietly had file access.

**The gate.** If Arm A succeeds, the empty-tool seam survives and the rest of the phase proceeds
as planned. If Arm A fails and Arm B succeeds, the untrusted block is load-bearing for
decomposition — record that, and the rest of the phase proceeds unchanged, since the block is
already built. If **neither** arm succeeds, STOP: do not start Task 2, do not commit a design
that assumes an answer it does not have, and return `## OQ1 FAILED` naming both arms' observed
outputs. A seam that needs a real file-reading tool is a materially larger phase — a scoped
read-only tool with a directory allowlist and a correspondingly larger corpus — and it needs
replanning, not improvisation.
  </action>
  <acceptance_criteria>
    - `rtk proxy cargo build` and `rtk proxy cargo clippy -- -D warnings` both succeed; `rtk proxy cargo clippy --all-targets -- -D warnings` still reports exactly the 5 known pre-existing lints named in `.planning/codebase/TESTING.md` — the count and their locations are unchanged.
    - `rtk proxy cargo test --test driver_model_seam` passes with the pure shape assertions. Against the unfixed behaviour (before the argv/env delta lands) the seam-profile assertions FAIL, because `build_argv` returns a vector carrying neither the empty tool value nor a schema flag and the env closure sets neither variable.
    - `rtk proxy cargo test --test driver_model_seam -- --ignored` runs both OQ1 arms against the installed `claude` binary and both report a schema-conformant payload whose command re-parses to a `RouterAction`. If the binary or its auth is unavailable the ignored tests FAIL loudly with a diagnostic naming what was missing — they never pass and never skip silently.
    - `src/driver/goal.rs`'s head doc comment names the binary version and quotes both OQ1 arms' observed target-phase choices verbatim, so a later reader can tell which arm the design rests on.
    - The retry pin is verified rather than assumed (research assumption A3, which was read out of the binary's registry and never exercised): an `#[ignore]`d arm supplies a schema the model's answer will violate and counts the structured-output tool-use blocks in the drained events, asserting the count is 1. If the variable is not honoured, that count exceeds 1 and the arm FAILS — which is the finding, and it must be recorded rather than worked around, because an unhonoured pin makes the DRIVE-04 count under-report by up to fivefold.
    - The live arms assert an empty MCP server list and a single-entry tool set on the seam's init envelope; removing the empty-tool argument from `build_argv` makes them FAIL rather than pass with a wider tool set.
    - `src/driver/untrusted.rs` has an in-source test proving that a field value containing the literal closing form of the boundary tag does not terminate the block: the produced string still parses as one boundary whose JSON body round-trips to the original field value.
    - `src/driver/untrusted.rs` has an in-source test proving two successive calls with identical inputs produce different nonces, so the closing form is not reproducible from a build constant.
    - `src/driver/untrusted.rs` has an in-source test proving a field value whose truncation point falls inside a multi-byte character truncates on the character boundary without panicking, and that the result is marked truncated.
    - `src/driver/goal.rs` has an in-source test asserting the schema's command enum has the same membership as `router::SAFE_COMMAND_ALPHABET` in BOTH directions — an enum member the alphabet does not contain fails, and an alphabet member the enum does not contain also fails ("an allowlist wider than the truth it describes is the failure that shape exists to catch").
    - `src/driver/goal.rs` has an in-source test asserting a named command outside the alphabet returns the error carrying the named string verbatim, and that the returned error's rendering contains no space-separated command line assembled from it.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --test driver_model_seam &amp;&amp; rtk proxy cargo test driver::untrusted &amp;&amp; rtk proxy cargo test driver::goal</automated>
  </verify>
  <done>One plain-language goal reaches the model through the seam profile and comes back as a
  validated `RouterAction`, proven live; the OQ1 answer is recorded in-tree with both arms named;
  and the boundary refuses to be closed by its own content.</done>
</task>

<task type="auto">
  <name>Task 2: The plan type, its legality predicate, and the refusal taxonomy</name>
  <files>src/driver/goal.rs</files>
  <read_first>
    - `src/driver/goal.rs` as Task 1 left it.
    - `src/driver/router.rs:70-190` — `RouterReason`, its `REASON_*` constants and the `as_str` with one arm per variant and no wildcard; `:518-520` — `is_goal_met` and the `DiskInference` it reads.
    - `src/driver/bounds.rs:36-73` and `:165-172` — `BoundsReason` and `BoundsRefusal`, the refusal-enum-to-seam-error shape and the "one arm per variant, no wildcard: a new detector is a compile error here rather than a halt that borrows somebody else's reason string" doc.
    - `src/driver/mod.rs:134-144` — `DriveArgs::target_phase` and its `journal::is_plain_path_component` validation at the seam, the precedent that closed WR-02.
    - `21-CONTEXT.md` § "The Goal → Plan Decomposition" — the ordered-list-over-the-alphabet definition and the OQ6 refusal rule.
  </read_first>
  <action>
Complete `src/driver/goal.rs` with the plan the user reviews.

Declare the plan type: an ordered list of steps, each carrying the command as a `RouterAction`
(never a string), a target phase, and the terminal state that would satisfy the step. Say in the
doc why the command is the typed action and not the wire string: there is exactly one path from
an action to a command string and it runs through `RouterAction::verb`, so a plan holding strings
would be a second path.

Declare the legality predicate as a fallible function from the raw wire payload to the plan type.
It refuses, never repairs. Three checks, each with its own refusal reason so a failure names the
part that could not be reduced: every step's named command must re-parse to a `RouterAction` via
the Task 1 re-parse; every step's target phase must pass `journal::is_plain_path_component` and
must name a phase present in the project's roadmap; and the plan's terminal state must reduce to
`router::is_goal_met` — the target phase's verification status reading as passed — because a goal
with no machine-checkable stopping condition has no terminal state, and a run with no terminal
state is the unclassified "loop ended" outcome Phase 20's criterion 5 forbids. Also refuse a plan
with zero steps and a plan whose step count exceeds the run's resolved step cap, since a plan the
run provably cannot finish is not a plan the user can meaningfully approve.

Declare the refusal taxonomy in the shape all four existing sibling taxonomies use: one
`pub const REASON_*: &str` per arm with the `goal_` prefix, a `#[derive(Debug, Clone, Copy,
PartialEq, Eq)]` enum, and an `as_str` with one arm per variant and no wildcard. At minimum:
a step naming a command outside the alphabet; a step naming a phase that is not a plain path
component or is absent from the roadmap; a terminal state that does not reduce to the goal-met
predicate; an empty plan; and a plan longer than the resolved step cap. The refusal carries the
offending value so the message names it, and the value is bounded and control-character-stripped
through `untrusted::MAX_UNTRUSTED_FIELD_CHARS` before it reaches any record, since it originates
in model output that itself consumed third-party repository content.

Add a plan digest function returning a stable identity string for a plan value, in the
`format!("{prefix}:{hex}")` shape `journal::argv_digest` establishes. Plan 21-03 binds an approval
to it; declare it here so the type and its identity are defined in one place.
  </action>
  <acceptance_criteria>
    - `src/driver/goal.rs` has an in-source test per refusal arm, each constructing a wire payload that trips exactly that arm and asserting the returned reason's `as_str` matches the corresponding `REASON_*` constant — never a string literal typed into the test.
    - A test asserts every `GoalRefusal` arm's `as_str` starts with the `goal_` prefix and that the set of constants and the set of enum arms match in both directions; adding an arm without its constant, or a constant without an arm, FAILS.
    - A test asserts a step whose target phase is a path-traversal shape is refused, and names `journal::is_plain_path_component` as the checker rather than re-implementing the check.
    - A test asserts a plan whose terminal state cannot be reduced to `router::is_goal_met` is refused, and that the refusal message names the unreducible part. Against the unfixed behaviour — a predicate that accepts any terminal state — this test FAILS because the payload is accepted and no refusal is produced.
    - A test asserts a plan with more steps than the resolved step cap is refused, constructing the cap by calling `bounds::resolve` and reading `max_steps` off the result, NOT by referencing `bounds::DEFAULT_MAX_STEPS`. Against the unfixed behaviour — a comparison against the default constant — the test with `bounds::resolve(Some(2), None)` FAILS because a 3-step plan is accepted.
    - A test asserts the plan digest is stable across two constructions of an equal plan and differs for a plan whose step order is swapped.
    - `rtk proxy cargo clippy -- -D warnings` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test driver::goal</automated>
  </verify>
  <done>A wire payload becomes a legal plan or a named refusal, and no third path exists.</done>
</task>

<task type="auto">
  <name>Task 3: The coupling guards — a comment is not a guard, the test is</name>
  <files>tests/spawn_seam_guard.rs, src/executor/claude.rs</files>
  <read_first>
    - `tests/spawn_seam_guard.rs:1-120` — the file's own standard, `executable_lines`, and `calls_marker`'s word-boundary matcher with its regression story; `:189-197` — the two-string-halves trick that keeps the file's own source from matching its own grep; `:210-214` and `:514-521` — the non-vacuity assertions; `:388-449` — the private-fields guard, the both-directions allowlist diff, and the guard-of-the-guard control arm.
    - `src/executor/claude.rs:220-296` — `build_argv`'s doc, in particular the argv-versus-settings-file argument ("argv cannot be silently dropped, because it is argv rather than a file") and the `every_pattern_the_settings_file_denies_is_also_carried_on_argv` cross-reference.
    - `21-RESEARCH.md` § "Common Pitfalls" Pitfall 1, and the § "Code Examples" note that there is NO on-the-wire init signal that `CLAUDE.md` suppression took effect.
  </read_first>
  <action>
Add four source-scanning guards to `tests/spawn_seam_guard.rs`, each in the house style: scan
`executable_lines` so a doc comment naming a declined alternative does not trip the scan, assert
non-vacuity (the scan found something to examine), and diff observed against declared in **both**
directions where a set is involved.

Guard one — the coupling. Assert that under the seam profile the produced argv carries the
empty-tool value and the schema flag together, by calling `build_argv` on a `ModelSeam` options
value and inspecting the returned vector. Not by asserting about a constant `build_argv` also
reads: a test that compares a constant with itself cannot detect its return, which is precisely
the Critical Phase 20's review found. The guard must also assert the executor profile's argv is
byte-identical to what it produces today, so the new discriminant cannot silently change an
existing run.

Guard two — the hook-disabling flag. Assert that no executable line under `src/` passes the CLI's
safe-mode flag on any profile. Build the searched literal at runtime from two string halves so
this test file's own source does not match its own scan, exactly as the strict-unknown-field
guard at `:189-197` does. The failure message must name the reason rather than the rule: that
flag also disables hooks, and the Phase 19 envelope is enforced by a PreToolUse hook, so a seam
spawned with it runs with the git boundary off and nothing says so. Pair it with a
guard-of-the-guard control arm proving the scanner fires on a synthetic executable line carrying
the flag and does not fire on a synthetic doc-comment line mentioning it — the declined
alternative must remain documentable in a comment, which is what the rationale-in-code convention
requires.

Guard three — the MCP set. Assert `--strict-mcp-config` is present on the argv both profiles
produce, and that no executable line under `src/` supplies an MCP config path. Both directions:
losing the strict flag fails, and gaining a config path fails. State the reason in the message —
with no config supplied the permitted set is empty, and supplying one moves it from empty to
whatever that file names.

Guard four — the enumerated untrusted set. Parse the free-string field declarations out of
`src/state_reader/mod.rs`'s `ProjectState` and `src/state_reader/roadmap_md.rs`'s `RoadmapPhase`
struct bodies and diff them against the set `src/driver/untrusted.rs` enumerates, failing in both
directions: a `String`-typed field on either struct that the enumeration does not name is a field
that could reach a prompt unlabelled, and an enumerated entry that no longer corresponds to a
field means the enumeration is wider than the truth it describes. Give it a guard-of-the-guard
control arm over synthetic struct text so the parser keeps being proved correct once the tree is
correct.

Then correct the falsified prose. `src/executor/claude.rs`'s spawn-closure comment currently
states that the closure sets the one variable it means to set; it now sets three under one
profile. Rewrite it in the same commit as the code that falsified it, per the precedent
`src/driver/dry_run.rs:78-83` records, and state explicitly that there is **no** on-the-wire
signal in the init envelope that `CLAUDE.md` suppression took effect — the guard is the argv and
env shape asserted here plus the end-to-end corpus fixture that lands in plan 21-05, and claiming
otherwise would be the kind of unearned assurance this codebase's honesty conventions exist to
prevent.
  </action>
  <acceptance_criteria>
    - `rtk proxy cargo test --test spawn_seam_guard` passes, and the run reports strictly more tests than it did before this task; the four new guards are named in the output.
    - Guard one: temporarily removing the empty-tool argument from `build_argv`'s seam arm makes it FAIL; re-adding it makes it pass. Temporarily changing the executor arm's argv makes the byte-identical assertion FAIL.
    - Guard two: its control arm asserts the scanner reports a synthetic executable line carrying the flag AND asserts the scanner does not report a synthetic doc-comment line carrying the same text — both directions in one test, so the guard cannot be satisfied by a scanner that reports nothing.
    - Guard two's searched literal is assembled at runtime from two halves; `rtk proxy grep -c 'safe-mode' tests/spawn_seam_guard.rs` finds no single-token occurrence of the assembled literal in the file's own source. <!-- planner-discipline-allow: safe-mode -->
    - Guard three: temporarily adding an MCP config path to any executable line under `src/` makes it FAIL naming that line; temporarily removing `--strict-mcp-config` from `build_argv` also makes it FAIL.
    - Guard three additionally asserts the seam profile's argv carries no session-resume flag, so each consultation is a fresh single-turn spawn. Research assumption A5 notes that the empty-tool and schema flags were never exercised in composition with a resumed session; the seams are single-turn by construction and this guard is what keeps that true rather than incidental.
    - Guard four: temporarily adding a new `String` field to `ProjectState` without adding it to the enumeration makes it FAIL naming that field; temporarily removing a field while leaving its enumeration entry also makes it FAIL.
    - Every new guard asserts non-vacuity — that its scan examined a non-empty set — so a rename that silently empties the scan is a failure rather than a pass.
    - `src/executor/claude.rs`'s spawn-closure comment no longer claims a single variable is set, and states that no init-envelope field reports whether `CLAUDE.md` suppression took effect.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --test spawn_seam_guard</automated>
  </verify>
  <done>The seam profile's controls are coupled by tests rather than by convention, each guard has
  been seen red against the unfixed behaviour, and the comment the code falsified has been
  rewritten in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party repo files → model context | `CLAUDE.md` and `.planning/` in a cloned repo are attacker-controlled and reach the model both through the driver's prompt and, before this plan, through the CLI's own auto-discovery |
| model output → driver action | the seam's returned payload is untrusted wire content that decides what command runs next |
| operator shell env → driven child | ambient variables inherited by the spawned CLI change its behaviour |
| project-local `.mcp.json` → seam tool set | a file in the driven repo could introduce tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Elevation | CLI `CLAUDE.md` auto-discovery | high | mitigate | `CLAUDE_CODE_DISABLE_CLAUDE_MDS=1` set under `ModelSeam` in the one spawn closure, after the `CLAUDE`-prefix scrub; asserted by the Task 3 argv/env guard, proven end-to-end by plan 21-05's corpus |
| T-21-02 | Elevation | the hook-disabling safe-mode flag | high | mitigate | Task 3 guard two: no executable line under `src/` passes it, with a control arm proving the scanner fires; the reason is stated in the failure message so a future author reads it at the moment of the change |
| T-21-03 | Tampering | ambient `MAX_STRUCTURED_OUTPUT_RETRIES` | high | mitigate | set explicitly to `1` in the spawn closure; it does not carry the `CLAUDE` prefix and so survives the scrub, which is exactly why assignment rather than removal is the control |
| T-21-04 | Tampering | delimiter escape in the untrusted boundary | high | mitigate | JSON-encoded body (a literal closing tag cannot form one) plus a per-call CSPRNG nonce (the closing form is unguessable); both asserted by in-source tests, neither claimed sufficient alone |
| T-21-05 | Elevation | project-local `.mcp.json` | medium | mitigate | `--strict-mcp-config` unconditional with no config path supplied; Task 3 guard three fails in both directions |
| T-21-06 | Tampering | model names an action outside the alphabet | high | mitigate | the schema enum is populated from `SAFE_COMMAND_ALPHABET` and the Rust re-parse walks `RouterAction::ALL`; the schema is defence in depth because strict derivation in the CLI is best-effort |
| T-21-07 | Repudiation | the seam measurement confounded by a seam that quietly had file access | high | mitigate | both OQ1 arms assert an empty MCP list and a single-entry tool set on the live init envelope before recording their result |
| T-21-08 | Information disclosure | an over-long or control-character-bearing third-party string breaking the one-line-per-entry record shape | medium | mitigate | `MAX_UNTRUSTED_FIELD_CHARS` bounds the rendering, truncation lands on a character boundary and is marked; the type still carries the value whole |
| T-21-09 | Repudiation | a `[SLOP]`/`[ASSUMED]` package entering the tree | low | accept | this plan installs nothing; the one candidate dependency (`sha2`) is verified and installed in plan 21-03, where its legitimacy evidence is recorded |
</threat_model>

## Artifacts this plan produces

| Kind | Symbol / path | Notes |
|---|---|---|
| enum | `executor::SpawnProfile` | two variants, `Executor` and `ModelSeam { json_schema: String }`; matched exhaustively, no wildcard |
| field | `executor::ExecutionOptions::profile` | defaults to `Executor`, so no existing caller changes |
| field | `executor::stream_json::ResultMessage::structured_output` | `Option<serde_json::Value>`, `#[serde(default)]` |
| env var | `CLAUDE_CODE_DISABLE_CLAUDE_MDS` | set to `1` under `ModelSeam`, after the `CLAUDE`-prefix scrub |
| env var | `MAX_STRUCTURED_OUTPUT_RETRIES` | set to `1` under `ModelSeam`; not scrubbed, so assignment is the control |
| CLI flag (child argv) | `--tools ""` | seam profile only |
| CLI flag (child argv) | `--json-schema <inline>` | seam profile only; inline JSON, never a file path |
| file | `src/driver/untrusted.rs` | the boundary constructor, the enumerated third-party string set, `MAX_UNTRUSTED_FIELD_CHARS` |
| file | `src/driver/goal.rs` | the schema builder, the `RouterAction` re-parse, the plan type, the legality predicate, `GoalRefusal` + its `goal_`-prefixed `REASON_*` constants, and the plan digest |
| file | `tests/driver_model_seam.rs` | pure argv/env shape assertions plus the two `#[ignore]`d live OQ1 arms |
| tests | four new guards in `tests/spawn_seam_guard.rs` | profile coupling, the hook-disabling flag, the MCP set, the enumerated untrusted set — each with a control arm |

## Flagged assumptions — probe fallback (unresolved)

These three edge-coverage rows classified as `unclassified` and are carried forward as explicit
assumptions rather than auto-resolved or dropped. 7 applicable == 4 authored into `must_haves` +
3 flagged here.

- **DRIVE-01 (unclassified).** The probe could not classify "a user states a goal once and the
  driver pursues it." Assumed reading for this phase: the pursued-without-further-input property
  is proven by plan 21-04's multi-iteration run reaching a terminal outcome, and the
  stated-once property by the self-goal prohibition guard in the same plan. Neither is a
  probe-derived predicate; both are goal-backward truths. Review if that reading is wrong.
- **DRIVE-03 (unclassified).** Assumed reading: "machine-checkable" is discharged by Task 2's
  legality predicate refusing rather than repairing, and "reviewable before the run starts" by
  plan 21-03's explicit recorded approval. The probe named no boundary or precision facet.
- **SAFE-08 (unclassified).** Assumed reading: the fixed-enum constraint is discharged
  constructively (the one path from an action to a string) rather than by a boundary condition,
  so no boundary/precision predicate was authored for it. Plan 21-06 carries its behavioural
  proof.

<verification>
- Both OQ1 arms ran live against the installed binary and their outputs are recorded verbatim in
  `src/driver/goal.rs`'s head doc comment.
- Every guard added in Task 3 has been observed failing against the unfixed behaviour, and the
  observation is recorded in the SUMMARY per guard.
- `rtk proxy cargo build && rtk proxy cargo test && rtk proxy cargo clippy -- -D warnings` is clean.
- `rtk proxy cargo clippy --all-targets -- -D warnings` reports the same 5 pre-existing lints, in
  the same locations, as `.planning/codebase/TESTING.md` records.
</verification>

<success_criteria>
- A plain-language goal produces a schema-conformant payload whose command re-parses to a
  `RouterAction`, proven live.
- OQ1 is answered with both arms recorded; if neither succeeded, the plan halted and returned
  `## OQ1 FAILED` instead of proceeding.
- The boundary survives content containing its own closing form, and its nonce is not reproducible.
- The seam profile's four controls are coupled by tests that fail in both directions.
</success_criteria>

<output>
Create `.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-01-SUMMARY.md` when done.
Record, per guard, what it did when run against the unfixed behaviour.
</output>
