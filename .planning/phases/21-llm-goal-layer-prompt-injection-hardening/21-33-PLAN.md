---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 33
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/ui/mod.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/render_escape_guard.rs
  - src/state_reader/backlog.rs
autonomous: true
requirements: [SAFE-07, DRIVE-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE PATTERN CALL FOR gaps[3] — narrower claim plus explicit disclosure, NOT a widened mechanism.** The reasoning, in one line: a needle-driven census structurally CANNOT carry the completeness its name claims, because its coverage SHRINKS as the conversion succeeds — every site successfully converted to the composed form removes a needle, and a render site that escapes nothing never had one — so a version that made the current name true would have to enumerate render SITES rather than escape CALLS, which is a structurally different mechanism, and that mechanism ALREADY EXISTS in this tree. Building a fourth census here would move the over-claim rather than end it. The test is therefore renamed to what it checks, its measured reach is disclosed file by file, the shrinking property is stated with its direction, and the completeness claim is handed to the two mechanisms that actually carry it, by name."
    - "The mechanisms that DO carry the completeness claim are named in the narrowed doc rather than left implicit, so the reader who loses the census's reach knows where the real guarantee lives: (1) the sealed `RenderAdjudicated` supertrait on `Screen` (21-26), which makes `impl Screen for X` on an unadjudicated `X` a compile error — a trait bound has no spelling to be short of, so it cannot be evaded by a rename or missed by a needle; and (2) the per-screen behavioural probes in `src/ui/screens/render_escape_guard.rs`, which render each screen through its real `Screen::render` into a ratatui `Buffer` over hostile and clean twins and assert arrival before property. Neither is duplicated here; both are pointed at, so there is one place to keep current."
    - "The disclosed reach is a CHECKED NUMBER rather than a sentence, which is the one bounded mechanism addition this plan makes and the reason the narrowing is not merely prose. A committed per-file distribution of the census needle is asserted by equality in both directions, so a change to the census's reach — in EITHER direction, including the shrink the disclosure describes — fails loudly and forces the number and the disclosure sentence to be re-measured together. The planner measured the distribution at HEAD `343c408` with a script over all sixteen `.rs` files under `src/ui/`: SEVEN executable occurrences, in THREE files — `src/ui/screens/driver_confirm.rs` 4, `src/ui/screens/driver.rs` 2, `src/ui/screens/render_escape_guard.rs` 1 (the exempt probe module) — so SIX non-exempt occurrences in TWO of sixteen files drive the assertion, reproducing pass 11's measurement exactly and independently."
    - "The pin's failure direction is stated rather than discovered: it is a BOOKKEEPING red with a one-line repair — re-measure the distribution, update the constant, update the disclosed reach sentence in the same commit — and it will fire whenever any plan in any round adds or removes an executable occurrence of the needle under `src/ui/`, including a sibling plan in this same wave. That is deliberate. `21-31` and `21-32` both carry a matching prohibition against adding one, and a red here after the merge means one of them did and the disclosure is stale, which is exactly what the pin exists to say out loud."
    - "The shrinking-coverage property is disclosed in words with its direction, because it is the finding and not a footnote: a render site that applies NEITHER class carries no needle and is invisible to this census; and every successful conversion of a site to the single composed call REMOVES a needle, so the census's reach shrinks monotonically as the work it certifies succeeds. **Under-detection, silent, and growing over time.** No previous version of this doc said this, and pass 11 named its absence as the residual that made the name a false completeness claim."
    - "Pass 11's own negative finding is recorded with attribution rather than quietly relied on: the verifier sampled 63 `Span::raw`/`Span::styled` sites under `src/ui/` whose argument is neither a literal nor an obvious escape, hand-traced the risky ones — `normal.rs:734`, `:800`, `:808`, `driver.rs:841`, `:883`, `detail.rs:2887`, `:3064`, `:2957`, `:4297`, `:4346-4347`, `driver_confirm.rs:441` — and found every one either an authored `&'static str`/const or escaped one to three lines upstream. So gaps[3] is a FALSE COMPLETENESS CLAIM WITH AN UNDISCLOSED RESIDUAL, **not a live leak**, and the doc says so in those words. Inflating it into a leak would be the same overclaim pointing the other way."
    - "WR-04 is closed by giving `EditBuffer` the certificate `Untrusted` got in the same round, not by trusting its prose. Its doc asserts no `Display`, no `AsRef<str>` and no `Into<Cow<'static, str>>`, and `rtk proxy grep -c \"implements_\" src/ui/screens/mod.rs` returns 0 — the claim has no committed control, and adding `impl Display for EditBuffer` tomorrow restores the laundering path CR-03 closed with every test in this repository green. An autoref-specialization probe is built LOCALLY in this file, replicating the pattern at `src/text.rs:1985-2150` (which is READ-ONLY here, owned by `21-32` in this wave), and each absence is asserted with a matching PRESENCE control arm over `String`, so a broken probe answering `false` to everything cannot pass. Each absence is observed RED by planting its impl, one at a time, with a clean tree after each."
    - "The same doc's factually wrong example is corrected in the same commit: it argues from `Span::styled(buffer.clone(), ..)` not compiling, and `EditBuffer` derives no `Clone` at all, so the example never described a reachable call. It is replaced with an example the executor MEASURED by planting — the actual construction that fails to compile and the actual verbatim compiler error — and the accessor that is the one legitimate route to a cell is named beside it."
    - "WR-05 is closed at the comparator rather than described: `backlog_number_ordering` is total over KEYS, and every unusable suffix maps to the same key, so tied ELEMENTS keep `read_dir`'s arbitrary order — which is the very defect the function's own failure message describes. A string tiebreak is appended so the order is total over ELEMENTS, and the existing permutation-independence control is re-run and its result quoted. The fix is display-ordering only and touches no persisted artifact; if any committed test asserts an order that the tiebreak changes, that difference is REPORTED with the old and new orders rather than absorbed."
    - "**21-29's carried S1 is DELIVERED, whole, and F3 is DECLINED with a measurement rather than deferred a second time.** S1 asked for a two-direction spot-check on an INPUT-ECHO screen and both delivered spot-checks landed on `.planning/`-derived values. `21-30` recorded that closing it needed the fixture's module widened, the fixture widened, and the spot-check housed in `src/ui/mod.rs`. **That path is unnecessary and the planner measured it:** `src/ui/screens/render_escape_guard.rs` is a CHILD module of `ui::screens`, so it can already reach the private `ui::screens::tests` and its `pub(super) ctx_with_aliases` — it does so today at `render_escape_guard.rs:982` and `:1191`. Housing the spot-check there delivers S1 with ZERO visibility changes, so `mod tests` stays private, `ctx_with_aliases` stays `pub(super)`, and `src/ui/screens/mod.rs`'s visibility is untouched. F3 is closed as UNNECESSARY, with the two line numbers as the evidence — not carried into a third round."
    - "The S1 spot-check is two-direction and asserts arrival BEFORE property, on a screen whose rendered value is what the OPERATOR TYPED rather than what a `.planning/` file said. The hostile member and the clean member of one `LOOK_ALIKE_PAIRS` entry are each driven through the screen's real `Screen::render` into a ratatui `Buffer`: the clean render must produce the clean value byte-identical (the no-over-escaping direction — a clean value must not be rewritten), and the hostile render must produce zero characters satisfying `text::is_invisible_formatting_char` (the property direction). The widget family the screen actually uses is NAMED in the assertion's doc, because ratatui 0.30's `Paragraph` drops zero-width graphemes while `Block::title` and `ListItem` preserve them — a distinction this tree re-derived in round 9 — so an assertion on the raw form's absence is non-vacuous only for the preserving families. It is observed RED by temporarily reverting one escape at a converted input-echo site, with the tree confirmed clean afterwards."
    - "The four PRE-EXISTING clippy lints are protected by a CHECKED property rather than by avoidance, because this plan edits `src/state_reader/backlog.rs`: exactly FOUR lints, the same two kinds, in the same two files — `bool_assert_comparison` x3 at `src/browser.rs:155,156,157` and `cmp_owned` x1 at `src/project_creator.rs:146`, re-measured by the planner at HEAD `343c408`. A count of three is a failure of this plan exactly as five would be. ROADMAP criterion 4 is permanently agent-unclosable and no task here claims work against it."
  prohibitions:
    - statement: "MUST NOT build a widened or replacement census over `src/ui/`. gaps[3] is closed by narrowing the claim to what the mechanism checks and disclosing the remainder; the ONLY mechanism addition permitted is the per-file reach pin, whose entire content is the measurement the disclosure states. If closing gaps[3] appears to require a new walk, a new needle or a new file set, STOP and report it — that is the signal the disclosure is the answer."
      category: process
    - statement: "MUST NOT widen the census's EXEMPTIONS list to make anything pass, and MUST NOT remove the exempt probe module's un-composed occurrence: `stale_exemptions` is asserted empty, so an exemption that no longer shields anything is a red, and the probe module must keep at least one un-composed occurrence for its exemption to remain live. Task 3 adds to that file and removes nothing from it."
      category: process
    - statement: "MUST NOT claim the narrowed census carries any completeness. Its doc must state, in words, that a render site applying neither class is invisible to it and that its reach shrinks as the conversion succeeds, and must hand the completeness claim by name to the sealed `RenderAdjudicated` bound and to the behavioural probes. A doc that softens the name while keeping the implication is the same defect with a shorter sentence."
      category: transparency
    - statement: "MUST NOT deliver S1 by widening `mod tests` or `ctx_with_aliases` in `src/ui/screens/mod.rs`. The planner MEASURED that the widening is unnecessary — `render_escape_guard.rs:982` and `:1191` already call `super::tests::ctx_with_aliases` — and a visibility widening made anyway would be an unnecessary loosening shipped under a carried item's name. If the spot-check genuinely cannot be housed in the probe module, STOP and report why rather than widening."
      category: process
    - statement: "MUST NOT assert the absence of a raw hostile form in a rendered buffer without naming the widget family the screen uses. ratatui 0.30's `Paragraph` DROPS zero-width graphemes while `Block::title` and `ListItem` preserve them; an absence assertion over a dropping family is vacuous forever and would be a control that cannot fail — this round's own subject."
      category: transparency
    - statement: "MUST NOT edit `src/ui/screens/detail.rs`, `src/session_detector.rs`, `src/text.rs`, `src/ui/screens/driver.rs`, `deferred-items.md`, `COVERAGE.md` or `tests/driver_reattach.rs`. Those belong to `21-31`, `21-32` and `21-34`. `src/text.rs` is READ-ONLY here and its trait-probe pattern is REPLICATED locally rather than imported. A temporary PLANT in an unowned screen file is permitted for the red captures, provided it is reverted and `rtk proxy git status --porcelain` is clean and the file is absent from the final `git diff --stat`."
      category: process
    - statement: "MUST NOT plan, execute or claim any work against ROADMAP success criterion 4, and MUST NOT edit `tests/driver_injection_corpus.rs` or `.planning/REQUIREMENTS.md`. Criterion 4 is permanently agent-unclosable by explicit user decision and 4/5 is the expected ceiling."
      category: process
    - statement: "MUST NOT inherit any count from this plan's own text, from `21-VERIFICATION.md` or from `21-REVIEW.md`. Every number is re-measured under `rtk proxy` against the tree the executor actually has, and any difference from the planner's figure is REPORTED rather than smoothed."
      category: transparency
  artifacts:
    - path: "src/ui/mod.rs"
      provides: "the census renamed to what it checks, its measured per-file reach pinned as a checked equality, the shrinking-coverage property disclosed with its direction, and the completeness claim handed by name to the sealed trait bound and the behavioural probes"
      contains: "no_display_identity_call_under_ui_stands_outside_a_composition"
    - path: "src/ui/screens/mod.rs"
      provides: "EditBuffer's trait-absence claim certified by a local autoref-specialization probe with presence control arms, and its non-compiling-example sentence corrected with a measured compiler error"
      contains: "EditBuffer"
    - path: "src/ui/screens/render_escape_guard.rs"
      provides: "21-29's carried S1 delivered — a two-direction spot-check on an input-echo screen, rendered through the real Screen::render, reusing the existing pub(super) fixture with no visibility change"
      contains: "ctx_with_aliases"
    - path: "src/state_reader/backlog.rs"
      provides: "WR-05 closed — a string tiebreak making the comparator total over ELEMENTS rather than only over keys"
      contains: "backlog_number_ordering"
  key_links:
    - from: "the census's disclosed reach sentence"
      to: "the census's actual reach"
      via: "a committed per-file distribution asserted by equality in both directions, so the sentence and the measurement cannot drift"
      pattern: "no_display_identity_call_under_ui_stands_outside_a_composition"
    - from: "EditBuffer's trait-absence claim"
      to: "the compiler"
      via: "a local autoref-specialization probe with a presence control arm per absence, each observed red by planting its impl"
      pattern: "EditBuffer"
    - from: "what the operator types into an input-echo screen"
      to: "a ratatui Buffer cell"
      via: "the screen's real Screen::render, asserted in both directions over one LOOK_ALIKE_PAIRS entry"
      pattern: "ctx_with_aliases"
---

<objective>
**Round 11, wave 1. The census whose name promises everything and whose reach shrinks as it
succeeds — narrowed, disclosed, and its number pinned. Plus the three carried items, delivered
rather than carried a third time.**

`ui::tests::every_render_site_under_ui_composes_both_classes` claims **every render site under
`src/ui/`**. The planner measured what actually drives it, with a script over all sixteen `.rs`
files: **7 executable occurrences of the escape-call needle, in 3 files** —
`driver_confirm.rs` 4, `driver.rs` 2, `render_escape_guard.rs` 1 (exempt) — so **6 non-exempt
occurrences in 2 of 16 files**. `detail.rs` (7 463 lines) and `normal.rs` (2 167 lines)
contribute zero. And the property that makes this structural rather than incidental: **a site
that escapes NOTHING carries no needle, and every successful conversion REMOVES one — the
census's coverage shrinks as the conversion succeeds.**

**The pattern call, made deliberately and recorded: narrower claim plus explicit disclosure.**

Rounds 9, 10 and 11 each produced a fresh crop of over-claiming censuses; each new mechanism
needed certifying by the next round and failed. A mechanism change is admissible only where it
is bounded and terminal. gaps[3] does not clear that bar, and the reason is specific to it: a
needle census is the wrong SHAPE for this claim. Making the current name true needs an
enumeration of render SITES rather than of escape CALLS — a structurally different mechanism —
and **that mechanism already exists in this tree**: the sealed `RenderAdjudicated` supertrait
(21-26) makes `impl Screen for X` on an unadjudicated `X` a compile error, and the behavioural
probes in `render_escape_guard.rs` render every screen over hostile and clean twins. A fourth
census here would move the over-claim, not end it.

So the census is renamed to what it checks, its reach is disclosed file by file, the shrinking
property is stated with its direction, and the completeness claim is handed by name to the two
mechanisms that carry it. **The one bounded mechanism addition is a coverage PIN** — the
disclosed reach as a checked equality in both directions — because a disclosure that is only
prose is precisely what this phase has spent eleven passes learning not to trust, and because
the shrink the disclosure describes must be able to go red when it happens.

**And pass 11's negative finding is recorded, not quietly used:** the verifier hand-traced 63
`Span::raw`/`Span::styled` sites outside the needle and found **no live leak**. This is a false
completeness claim with an undisclosed residual, not a vulnerability, and the doc says so in
those words.

**The three carried items, delivered.**

- **WR-04.** `EditBuffer`'s trait-absence claim is prose-only — the exact standard 21-27 applied
  to `Untrusted` in the same round, not applied here. `impl Display for EditBuffer` tomorrow
  restores the laundering path CR-03 closed, with every test green. It gets the same certificate,
  built locally because `src/text.rs`'s probe module is private and belongs to `21-32` this wave.
  Its doc's `buffer.clone()` example is also simply wrong — `EditBuffer` derives no `Clone` — and
  is replaced by a construction the executor measures.
- **WR-05.** The backlog comparator is total over KEYS; every unusable suffix maps to the same
  key, so tied ELEMENTS keep `read_dir`'s order — the defect the function's own failure message
  describes. One appended string tiebreak.
- **21-29's S1, and F3 declined by measurement.** S1 wanted a two-direction spot-check on an
  **input-echo** screen and never got one. `21-30` recorded that closing it required widening
  `mod tests`, widening `ctx_with_aliases`, and housing the check in `src/ui/mod.rs`. **The
  planner measured that this is unnecessary:** `render_escape_guard.rs` is a CHILD of
  `ui::screens` and already calls `super::tests::ctx_with_aliases` at `:982` and `:1191`. So S1
  is delivered in the probe module with **zero visibility changes**, and F3 is closed as
  UNNECESSARY with those two line numbers as the evidence — not a half-move, and not a third
  carry.

**What this plan does NOT touch, stated so the wave stays parallel.**
`src/ui/screens/detail.rs` and `src/session_detector.rs` belong to **21-31** (gaps[0], the
blocker, parallel worktree, this wave). `src/text.rs` and `src/ui/screens/driver.rs` belong to
**21-32** (gaps[1] and gaps[2], likewise parallel) — `src/text.rs` is read here for its probe
pattern and never edited. `deferred-items.md`, `COVERAGE.md` and `tests/driver_reattach.rs`
belong to **21-34**, wave 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-29-SUMMARY.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-30-SUMMARY.md
</context>

<tooling_note>
**A global RTK hook rewrites and filters `cargo` output**, stripping `warning:` and
`test result:` lines. A grep for either against filtered output **succeeds vacuously**. Every
count-, presence- or grep-bearing check in this plan runs `rtk proxy <cmd>`.

Gates re-measured by the planner at HEAD **`343c408`**, all under `rtk proxy`:

| Gate | Result at `343c408` |
|---|---|
| `cargo clippy -- -D warnings` (lib, the project gate) | **exit 0** |
| `cargo clippy --all-targets -- -D warnings` | fails with exactly **4** pre-existing lints: `bool_assert_comparison` x3 at `src/browser.rs:155,156,157`, `cmp_owned` x1 at `src/project_creator.rs:146` |
| `cargo test --workspace --no-fail-fast` | **35 binaries, 1416 passed / 1 failed / 13 ignored** — the single failure is `driver_reattach::a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`, the documented flake, in the planner's own baseline run **before any file was edited**. A fully green run is 1417 / 0 / 13. |

**The `driver_reattach` flake — the corrected record.** Pre-existing, not a regression;
`--test-threads=1` does **NOT** reliably fix it (the stale note saying otherwise is corrected by
`21-34`); the mechanism is a **system-wide `/proc` scan that does not stop at the process-group
or worktree boundary**. Re-run the binary alone before treating a failure there as a regression,
and REPORT it rather than absorbing it.

**Planner measurements this plan is built on — re-measure each before quoting it.**

Executable occurrences of the census needle under `src/ui/`, comment lines excluded, at
`343c408`, over all sixteen `.rs` files:

| File | Executable occurrences |
|---|---|
| `src/ui/screens/driver_confirm.rs` | 4 |
| `src/ui/screens/driver.rs` | 2 |
| `src/ui/screens/render_escape_guard.rs` | 1 (the exempt probe module) |
| every other file under `src/ui/` (13 of them, including `detail.rs` and `normal.rs`) | 0 |
| **total** | **7** — 6 non-exempt, in 2 of 16 files |

Other reference points at `343c408`: `CALL_HEAD`/`CALL_TAIL` at `src/ui/mod.rs:41-43`;
`COMPOSED_HEAD`/`COMPOSED_TAIL` at `:46-49`; `CALL_JOIN_LINES = 4` at `:52`; `EXEMPTIONS`
(2 entries) at `:74`; `WAVE_PENDING` (empty) at `:113`; `ui_sources` at `:166`; `census` at
`:196`; `stale_exemptions` at `:238` and asserted empty at `:508`; the census test at `:357+`.
`EditBuffer` at `src/ui/screens/mod.rs:787`, its doc from `:740`, its hand-written `Default` at
`:790`; `mod tests` at `:1685`; `ctx_with_aliases` at `:1870` (`pub(super)`).
`super::tests::ctx_with_aliases` is CALLED from `src/ui/screens/render_escape_guard.rs:982` and
`:1191` — the measurement that makes F3 unnecessary. The five input-echo screens are named in
the probe's own table at `render_escape_guard.rs:368-375`.
`backlog_sort_key` at `src/state_reader/backlog.rs:112`; `backlog_number_ordering` at `:155-157`.
The trait-probe pattern to replicate (READ-ONLY, owned by `21-32`): `mod trait_probe` at
`src/text.rs:1985` and the `implements_*` macros at `:2094-2148`.
</tooling_note>

<wave_parallelism>
## Wave 1 file-ownership intersection — COMPUTED, not asserted

The planner listed each wave-1 plan's `files_modified` and intersected the sets mechanically:

| Plan | `files_modified` |
|---|---|
| 21-31 | `src/ui/screens/detail.rs`, `src/session_detector.rs` |
| 21-32 | `src/text.rs`, `src/ui/screens/driver.rs` |
| **21-33** (this plan) | `src/ui/mod.rs`, `src/ui/screens/mod.rs`, `src/ui/screens/render_escape_guard.rs`, `src/state_reader/backlog.rs` |

`21-31 ∩ 21-32 = ∅` · `21-31 ∩ 21-33 = ∅` · `21-32 ∩ 21-33 = ∅` · union = **8 distinct files, no
file owned twice**. `21-34` (wave 2) owns three further files and intersects none of them.

**The one cross-worktree coupling, named rather than left implicit, because it belongs to this
plan.** The reach pin Task 1 adds asserts the per-file needle distribution across ALL of
`src/ui/`, which includes `21-31`'s `detail.rs` (0 occurrences at HEAD) and `21-32`'s `driver.rs`
(2 occurrences at HEAD). Both sibling plans carry a matching prohibition against changing their
counts, and this plan's pin is measured in THIS worktree, where both files are at the pre-round
base. **A red in the pin after the merge is therefore informative rather than an accident:** it
says a sibling changed the census's reach and the disclosure sentence is stale. The pin's failure
message says exactly that, and the repair is to re-measure, update the constant and update the
disclosed reach in one commit — never to relax the assertion.
</wave_parallelism>

<edge_probe_audit>
The spec-less probe fallback's full seven-row disposition and the three flagged planner
assumptions are recorded once, in **`21-31`'s `<edge_probe_audit>`**, which is round 11's single
audit location. This plan carries none of the seven rows; nothing is restated here, so there is
one place to check the no-silent-drop equality.
</edge_probe_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a `.planning/`-derived or operator-typed value → a ratatui `Buffer` cell | the render boundary this whole class of work guards |
| a control's NAME → the property it checks | the boundary that failed: a name claiming every render site, driven by six executable lines in two of sixteen files |
| a control's disclosed reach → its actual reach | prose and measurement can drift silently; only a checked number cannot |
| `EditBuffer`'s doc → the compiler | three absences claimed, none certified; one trait addition restores the laundering path with every test green |
| a comparator's key space → its element space | total over keys, not over elements: tied elements inherit the filesystem's order |

## STRIDE Threat Register (ASVS L1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-33-01 | Repudiation | `src/ui/mod.rs:196-231, 357-408` — a control named for a property it does not check, driven by 6 executable lines in 2 of 16 files, whose coverage shrinks as the conversion succeeds, with the residual undisclosed | **high** | mitigate | Task 1: renamed to what it checks; reach disclosed file by file and PINNED as a checked equality; shrinking property stated with its direction; completeness handed by name to the sealed trait bound and the behavioural probes. Pass 11 hand-traced 63 sites outside the needle and found no live leak, so this is a false claim rather than a vulnerability, and the doc says so. |
| T-21-33-02 | Tampering | `src/ui/screens/mod.rs:750-757` — `EditBuffer`'s trait-absence claim is prose-only. `impl Display for EditBuffer` added tomorrow restores the round-trip laundering path CR-03 closed, invisibly in a diff and green in every existing test | **high** | mitigate | Task 2: a local autoref-specialization probe asserting each absence with a matching `String` presence arm, each observed red by planting its impl, with a clean tree after each. |
| T-21-33-03 | Repudiation | the same doc arguing from a construction that does not exist — `buffer.clone()` where the type derives no `Clone` — so a maintainer testing the claim finds the example wrong and may conclude the claim is too | medium | mitigate | Task 2: the example replaced with a construction the executor measures, and the verbatim compiler error committed beside it. |
| T-21-33-04 | Tampering | `src/state_reader/backlog.rs:112-157` — every unusable suffix maps to the same key, so tied elements keep `read_dir`'s arbitrary order; the failure message already describes this defect while the comparator still has it | medium | mitigate | Task 2: a string tiebreak appended, making the order total over elements; the existing permutation-independence control re-run and quoted. Display-ordering only; no persisted artifact is affected. |
| T-21-33-05 | Information disclosure | an operator-typed value reaching a terminal cell unescaped on an input-echo screen — the half of the render surface no committed two-direction spot-check had ever exercised (21-29's S1) | medium | mitigate | Task 3: a two-direction spot-check on an input-echo screen through the real `Screen::render`, arrival asserted before property, widget family named, observed red by a temporary reverted plant. |
| T-21-33-06 | Elevation of privilege | an unnecessary visibility widening shipped under a carried item's name — `mod tests` made `pub(crate)` when the child module could already reach it | low | mitigate | Prohibition 4 forbids it by name; the measurement (`render_escape_guard.rs:982` and `:1191`) is what closes F3 as unnecessary rather than deferring it again. |

**Package legitimacy:** no `cargo add`, no new dependency, no `Cargo.toml` or `Cargo.lock`
change. The `## Package Legitimacy Audit` gate does not fire.
</threat_model>

<decisions>
## Decisions and reversibility (REVERSIBILITY_GATES=true)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| D-21-56 | gaps[3] is closed by NARROWING the claim and disclosing the remainder, not by a widened or replacement census | **reversible** | A needle census's coverage shrinks as the conversion succeeds, so it is the wrong shape for a completeness claim; the structurally different mechanism that carries it already exists (`RenderAdjudicated` + the behavioural probes). A fourth census would move the over-claim. Doc and test-name change; revertible from git. |
| D-21-57 | The disclosed reach is PINNED as a checked per-file equality — the one bounded mechanism addition | **reversible** | A disclosure that is only prose is what eleven passes have taught this phase not to trust, and the shrink the disclosure describes must be able to go red. The pin's content is exactly the measurement the sentence states, so it cannot over-claim. Its red is a bookkeeping red with a one-line repair, stated in its failure message. |
| D-21-58 | WR-04's probe is REPLICATED locally in `src/ui/screens/mod.rs` rather than imported from `src/text.rs` | **reversible** | `src/text.rs`'s `mod trait_probe` is private and the file belongs to `21-32` in this same wave. Replication costs about forty test-only lines and keeps the wave parallel; importing would need a visibility change in a fenced file. |
| D-21-59 | WR-05 is closed by appending a string tiebreak rather than by changing the key function | **reversible** | The key function is correct — it is total over `f64` including `NaN` by `total_cmp` — and the defect is that many elements share one key. A tiebreak on the element makes the order total over elements without touching the numeric semantics any existing test asserts. Display-ordering only. |
| D-21-60 | S1 is housed in `src/ui/screens/render_escape_guard.rs` and F3's visibility widening is DECLINED as unnecessary | **reversible** | Measured: the probe module is a child of `ui::screens` and already calls `super::tests::ctx_with_aliases` at two sites, so the fixture is reachable with no visibility change. Declining the widening keeps `mod tests` private, which is the tighter state. |

No decision in this plan is `one-way`; no `checkpoint:decision` gate is required. One production
line changes (the comparator's tiebreak) and it affects display ordering only; everything else is
`#[cfg(test)]` code or doc comments. Every change is revertible from git with no data migration
and no on-disk format change. There is no `costly` rating either.
</decisions>

<named_shape_audit>
## Named-shape audit (this plan's slice)

| # | Named shape (source) | Owner | Covered by |
|---|---|---|---|
| 1 | A control named for a property it does not check, whose coverage shrinks as it succeeds (gaps[3]; 21-29 truth 2 scored FAILED) | 21-33 T1 | renamed, reach disclosed and pinned, shrinking property stated, completeness handed to the mechanisms that carry it |
| 2 | A site applying NEITHER class carrying no needle and being invisible (gaps[3]'s undisclosed residual) | 21-33 T1 | disclosed in words with its direction |
| 3 | `EditBuffer`'s prose-only trait-absence claim (WR-04) | 21-33 T2 | a local autoref-specialization probe with presence arms, each absence observed red by planting |
| 4 | The same doc's `buffer.clone()` example over a type with no `Clone` (WR-04's second half) | 21-33 T2 | replaced with a measured construction and its verbatim compiler error |
| 5 | The comparator total over keys but not over elements (WR-05) | 21-33 T2 | a string tiebreak, with the permutation control re-run |
| 6 | 21-29's S1 — no two-direction spot-check on an input-echo screen | 21-33 T3 | delivered in the probe module, two directions, arrival before property, observed red by a reverted plant |
| 7 | 21-29's F3 — widen `ctx_with_aliases` to `pub(crate)` | 21-33 T3 | **DECLINED as unnecessary, with the measurement**: `render_escape_guard.rs:982` and `:1191` already call it. Not deferred; closed. |
| 8 | Argument injection at the resume argv and its complicit corpus (gaps[0]) | **21-31** | delegated — parallel worktree, this wave |
| 9 | The interpreter census's comment-budget blind spot (gaps[1]) and the composition census's over-joining (gaps[2]) | **21-32** | delegated — parallel worktree, this wave |
| 10 | The stale `--test-threads=1` claim in the flake record | **21-34** | delegated — wave 2 |
| 11 | ROADMAP criterion 4's behavioural half | **nobody, deliberately** | permanently agent-unclosable by construction |
</named_shape_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rename the census to what it checks, pin its measured reach, and disclose the shrink</name>
  <files>src/ui/mod.rs</files>
  <read_first>
    - src/ui/mod.rs lines 35-115 — `CALL_HEAD`/`CALL_TAIL`, `COMPOSED_HEAD`/`COMPOSED_TAIL`, `CALL_JOIN_LINES`, the `Exemption` struct, the two `EXEMPTIONS` entries with their reasons and `inside_walk` flags, and the empty `WAVE_PENDING` with its doc. The exemptions must not be widened and the empty pending list must stay empty.
    - src/ui/mod.rs lines 120-235 — `collect_rs`, `ui_sources`, `continues_onto_the_next_line` and `census`. This is the mechanism whose reach is being measured and disclosed; read it so the reach is stated in terms of what the code does.
    - src/ui/mod.rs lines 236-290 — `stale_exemptions` and `satisfied_pending`, and lines 500-515 where `stale_exemptions(&files, &EXEMPTIONS)` is asserted empty. That assertion is why the exempt probe module must keep an un-composed occurrence.
    - src/ui/mod.rs lines 291-420 — the census test's doc in full: the equality rationale, the three committed REDs, the "What this census does NOT claim" block, and the two existing residuals. This whole doc block is what Task 1 rewrites; read every line of it before changing any.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md lines 316-336 and 435 — pass 11's measurement of this census's needle coverage and its hand-trace of 63 sites outside it, which are what the disclosure records.
  </read_first>
  <behavior>
    - A script-free, in-test count of executable needle occurrences per file under `src/ui/` produces a distribution that equals a committed constant, and the equality is asserted in both directions so a file appearing or disappearing from the distribution is a failure.
    - The walk still reads real source: the number of `.rs` files collected under `src/ui/` is at or above the measured floor, with the floor's value in the failure message.
    - The census itself still reports zero non-exempt un-composed sites, and `src/ui/mod.rs` is not among its raw sites after the rename.
    - `stale_exemptions` over the real exemption list is still empty.
  </behavior>
  <action>
    (a) **Re-measure first**, under `rtk proxy`, recording each result beside the planner's
    figure and REPORTING any difference: the three gates in `<tooling_note>`; the per-file
    executable needle distribution under `src/ui/`; the total number of `.rs` files under
    `src/ui/`; the census's current raw and outstanding site counts.

    (b) **Rename the test** to `no_display_identity_call_under_ui_stands_outside_a_composition`.
    That name states the mechanism's actual subject: it is about CALLS to the escape, not about
    render SITES. Update every reference to the old name in this file. Then confirm the rename
    did not create a hit: re-run the census and assert `src/ui/mod.rs` is absent from its raw
    site list, quoting that list.

    (c) **Add the reach pin.** Add `fn needle_distribution(files: &[(String, Vec<(usize, String)>)]) -> Vec<(String, usize)>`
    returning, for every file carrying at least one executable occurrence of the assembled call
    needle, the path and the occurrence count, sorted by path — comment lines excluded by the
    same rule the census uses. Add a committed constant holding the measured distribution and
    assert equality in BOTH directions inside the census test, before the outstanding-sites
    assertion. Its failure message must say, in words: the census's reach changed; re-measure the
    distribution, update the constant, and update the disclosed reach sentence in the same
    commit; a shrink here is the property the doc describes actually happening, and a growth is
    a new escape call somewhere. Add beside it a floor assertion on the number of files
    collected, so a walk that reads nothing is red rather than trivially matching an empty
    distribution.

    (d) **Rewrite the doc as the narrowed claim.** The block must state, in this order and in
    words: (i) what the mechanism checks — every executable occurrence of the escape call under
    `src/ui/` is composed with the control-class call on the same logical unit, within
    `CALL_JOIN_LINES` physical lines; (ii) the MEASURED reach, file by file, with the total and
    the "N of 16 files" framing, and a sentence saying the numbers are pinned by (c) rather than
    asserted here; (iii) **the shrinking-coverage property** — a render site that applies
    NEITHER class carries no needle and is invisible, and every successful conversion REMOVES a
    needle, so the reach shrinks monotonically as the work this census certifies succeeds —
    with the direction **under-detection, silent, growing over time**; (iv) that the census
    therefore carries NO completeness claim, and the two mechanisms that do, named: the sealed
    `RenderAdjudicated` supertrait on `Screen`, which makes an unadjudicated screen a compile
    error, and the per-screen behavioural probes in `render_escape_guard.rs`; (v) pass 11's
    hand-trace of 63 `Span::raw`/`Span::styled` sites outside the needle, WITH attribution and
    with its conclusion in those words — a false completeness claim with an undisclosed
    residual, **not a live leak**. Keep the two existing residuals (alias/re-export invisibility;
    over-length composition read as un-composed) with their directions, and keep the three
    committed REDs — they are still true of the mechanism and deleting evidence is not
    narrowing.

    (e) **Change no other behaviour.** Do not touch `EXEMPTIONS`, `WAVE_PENDING`, `census`,
    `stale_exemptions` or the outstanding-sites equality. Prohibition 1 forbids a new walk, a
    new needle or a new file set; the pin's entire content is the measurement (d) states.
  </action>
  <acceptance_criteria>
    - `rtk proxy grep -c "every_render_site_under_ui_composes_both_classes" src/ui/mod.rs` returns 0, and `rtk proxy grep -c "no_display_identity_call_under_ui_stands_outside_a_composition" src/ui/mod.rs` returns at least 1.
    - `rtk proxy cargo test --lib -- ui::tests --nocapture` exits 0; the census's printed raw site list is quoted in the SUMMARY and `src/ui/mod.rs` does not appear in it.
    - The reach pin's committed distribution is quoted in the SUMMARY beside the executor's own re-measurement, with a statement that they agree, or with the difference reported and the constant set to the measured value.
    - The reach pin is observed RED by temporarily adding one executable occurrence of the needle in an unowned file under `src/ui/`; the panic is quoted verbatim, the plant reverted, and `rtk proxy git status --porcelain` shown clean.
    - The SUMMARY quotes the whole rewritten doc block and confirms it contains: the file-by-file reach, the phrase naming the shrinking property with an explicit direction, the two named mechanisms that carry completeness, and pass 11's hand-trace result with its "not a live leak" conclusion.
    - `stale_exemptions(&files, &EXEMPTIONS)` is still empty and `EXEMPTIONS` still has exactly 2 entries and `WAVE_PENDING` exactly 0; all three re-measured and quoted.
    - `rtk proxy cargo clippy -- -D warnings` exits 0 after this task.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::tests --nocapture</automated>
  </verify>
  <done>The census is named for the calls it checks rather than the sites it does not; its reach is measured, disclosed file by file and PINNED as a two-direction equality observed red by a reverted plant; the shrinking-coverage property is stated with its direction; and the completeness claim is handed by name to the sealed trait bound and the behavioural probes rather than implied.</done>
  <reversibility rating="reversible">A test rename, one added `#[cfg(test)]` function and constant, and a doc rewrite in one file. No production line changes; revertible from git.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: give EditBuffer the certificate its doc claims, correct the example that never compiled, and make the backlog order total over elements</name>
  <files>src/ui/screens/mod.rs, src/state_reader/backlog.rs</files>
  <read_first>
    - src/ui/screens/mod.rs lines 735-800 — `EditBuffer`'s doc in full, including the three claimed absences, the `buffer.clone()` sentence pass 11 measured as inaccurate, the one-raw-take argument and the character-operations argument; then the struct declaration and its hand-written `Default`. Note what the type derives — the `Clone` the example assumes is not among them.
    - src/ui/screens/mod.rs lines 800-870 — `seed_from_untrusted_source`, `push_char`, `pop_char`, `clear`, `shown` and `take_raw_for_persistence`, so the probe's presence arms and the corrected example name real methods.
    - src/text.rs lines 1980-2150 — `mod trait_probe` and the `implements_*` macros: the autoref-specialization pattern, how each macro's two impls resolve, and how the presence control arm over `String` is written. **READ ONLY — this file belongs to `21-32` in this wave and must not be edited.** The pattern is replicated locally, not imported.
    - src/state_reader/backlog.rs lines 105-160 — `backlog_sort_key` and `backlog_number_ordering` with its full doc, including the measured refutation of the review's panic claim and the sentence explaining why there is no fallback arm. The tiebreak is appended to the comparator; the doc's existing argument stays.
    - src/state_reader/backlog.rs lines 275-340 — the existing ordering controls, including `a_non_numeric_suffix_cannot_make_the_order_depend_on_the_input_permutation`, so the tiebreak's effect on them is expected rather than discovered.
  </read_first>
  <behavior>
    - A local probe answers `false` for `EditBuffer` on each of `Display`, `AsRef<str>` and `Into<Cow<'static, str>>`, and `true` for `String` on each of the same three, so a probe that answers `false` to everything cannot pass.
    - Adding any one of those impls for `EditBuffer` makes the corresponding absence assertion fail, one at a time.
    - Two backlog items whose numbers both map to the same sort key are ordered by their numbers, not by the order they were read, and that order is the same for every input permutation.
    - Items with distinct numeric keys keep the order they have today.
  </behavior>
  <action>
    (a) **Re-measure first**, under `rtk proxy`: the three gates; `rtk proxy grep -c "implements_" src/ui/screens/mod.rs` (expected 0, the measurement WR-04 rests on); what `EditBuffer` derives; the current ordering controls' results in `src/state_reader/backlog.rs`.

    (b) **Build the probe locally.** In `src/ui/screens/mod.rs`'s test module, add a private
    `mod` replicating the autoref-specialization pattern from `src/text.rs` — one wrapper type
    per trait with an inherent method returning `true` and a blanket-ish fallback returning
    `false`, plus one macro per trait — for exactly the three traits `EditBuffer`'s doc claims:
    `Display`, `AsRef<str>` and `Into<Cow<'static, str>>`. Do not import from `src/text.rs`
    (D-21-58).

    (c) **Assert both directions.** Add
    `an_edit_buffer_implements_none_of_the_string_conversions` asserting each absence for
    `EditBuffer` AND the matching presence for `String`, with a failure message per arm naming
    what the absence protects: adding `Display` restores the round trip CR-03 closed, and every
    existing test in this repository stays green while it does.

    (d) **Observe RED by planting, one trait at a time.** Add the impl for one trait, run, capture
    the panic VERBATIM, remove it, confirm `rtk proxy git status --porcelain` clean; repeat for
    the other two. Three reds, three clean trees. A control never observed red is not a
    certificate.

    (e) **Correct the doc's wrong example by measuring it.** Replace the `buffer.clone()`
    sentence with the construction that actually fails: write the failing render call into a
    scratch edit, capture the VERBATIM compiler error, revert, and quote that error in the doc
    beside the corrected sentence. Name `shown` as the one route to a cell. Add one line
    recording that the previous example named a method this type does not have, so the
    correction is dated and legible rather than silent. Then update the doc's absence claim to
    say the absences are now CERTIFIED by the control from (c), named.

    (f) **Close WR-05 at the comparator.** Append a tiebreak on the element to
    `backlog_number_ordering` so that when two keys compare equal the numbers themselves decide.
    Extend the function's doc with one short block: what was still wrong (total over KEYS, and
    every unusable suffix maps to the same key, so tied ELEMENTS inherited `read_dir`'s order —
    the very defect the failure message describes), and what the tiebreak fixes, with
    attribution to WR-05. Add a control asserting that two items with distinct unusable suffixes
    come out in the same order for both input permutations, observed RED against the pre-tiebreak
    comparator with the red captured verbatim.

    (g) **Report, do not absorb, any order change.** Re-run every existing ordering control in
    that file. If the tiebreak changes an asserted order, quote the old and new orders and the
    test name, state why the new one is correct, and update the assertion in the same commit —
    never silently.

    (h) **Re-measure the four pre-existing clippy lints after the edit**, since this task edits a
    `src/state_reader/` file: exactly four, same two kinds, same two files, and
    `rtk proxy git diff --stat` names neither `src/browser.rs` nor `src/project_creator.rs`.
  </action>
  <acceptance_criteria>
    - `rtk proxy grep -c "implements_" src/ui/screens/mod.rs` returns at least 3 after the change, and the SUMMARY quotes the pre-change value of 0 as the measurement WR-04 rests on.
    - Three verbatim REDs from step (d) are quoted in the SUMMARY, one per planted impl, each followed by a clean `rtk proxy git status --porcelain`.
    - The SUMMARY quotes the verbatim compiler error from step (e) and states in one sentence that the previous example named a method `EditBuffer` does not have.
    - `rtk proxy cargo test --lib -- ui::screens::tests --nocapture` exits 0 and its counts are quoted.
    - The verbatim RED from step (f) is quoted, captured against the pre-tiebreak comparator, and `rtk proxy cargo test --lib -- state_reader::backlog --nocapture` exits 0 afterwards with its counts quoted.
    - The SUMMARY lists every ordering control in `src/state_reader/backlog.rs` with its before and after result; any changed asserted order is quoted old-and-new with the reason.
    - `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings` reports exactly FOUR lints, same two kinds, same two files, before and after both quoted.
    - `rtk proxy git diff --stat` for this task names ONLY `src/ui/screens/mod.rs` and `src/state_reader/backlog.rs`.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::screens::tests --nocapture</automated>
  </verify>
  <done>`EditBuffer`'s three claimed trait absences are certified by a local probe with presence control arms, each observed red by planting its impl; the doc's example is corrected with a measured compiler error and the previous one's inaccuracy is dated on the record; and the backlog comparator is total over elements, with the tiebreak observed red first and every existing ordering control re-run and reported.</done>
  <reversibility rating="reversible">One appended comparator clause affecting display ordering only, plus test-module additions and doc corrections. No persisted artifact, no on-disk format; revertible from git.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: deliver 21-29's S1 in the probe module, and close F3 as unnecessary with the measurement</name>
  <files>src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - src/ui/screens/render_escape_guard.rs lines 360-380 — the screen disposition table, including the five input-echo screens (`AddProjectScreen`, `CreateProjectScreen`, `DriverInjectScreen`, `DriverStartScreen`, `EnqueueScreen`) and their files. Pick the spot-check's subject from this table.
    - src/ui/screens/render_escape_guard.rs lines 890-1000 — `probe_ctx`, its doc about arrival per tab, and specifically line 982's `super::tests::ctx_with_aliases(&[identity])` call. That single line is the measurement that closes F3.
    - src/ui/screens/render_escape_guard.rs lines 1180-1200 — `chrome_ctx`, the second `super::tests::ctx_with_aliases` call site, so the F3 measurement rests on two independent sites rather than one.
    - src/ui/screens/render_escape_guard.rs lines 1730-1900 — the per-screen constructors for `EnqueueScreen`, `DriverInjectScreen`, `DriverStartScreen`, `AddProjectScreen` and `CreateProjectScreen`, so the spot-check builds its subject the way this module already builds it rather than inventing a sixth construction.
    - src/ui/screens/render_escape_guard.rs — the module's existing expected-escaped-form helpers and its buffer-to-text extraction. The spot-check must REUSE them; a second oracle computed a second way is a second thing that can disagree.
    - src/test_support.rs lines 118-135 — `LOOK_ALIKE_PAIRS`, seven entries, each a (clean, hostile) pair. The spot-check drives ONE entry in both directions. This file is read-only for this plan.
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-29-SUMMARY.md around line 577 — S1 self-disclosed as NOT met, and why; and `21-30-SUMMARY.md`'s record that the one-line `pub(crate)` widening alone does not close it.
  </read_first>
  <behavior>
    - Rendering an input-echo screen through its real `Screen::render` with the CLEAN member of a `LOOK_ALIKE_PAIRS` entry produces that clean value byte-identical in the buffer — a clean value is not rewritten by the escape.
    - Rendering the same screen with the HOSTILE member of the same entry produces zero characters satisfying `crate::text::is_invisible_formatting_char` anywhere in the buffer.
    - The clean value's arrival is asserted BEFORE the hostile property, so a fixture that never reaches the buffer is reported as an arrival failure rather than counted as a pass.
    - Reverting one escape at a converted input-echo site makes the hostile direction fail and names the screen.
    - No visibility declaration in `src/ui/screens/mod.rs` changes.
  </behavior>
  <action>
    (a) **Record the F3 measurement first, and close it.** Quote
    `render_escape_guard.rs:982` and `:1191` — both calling `super::tests::ctx_with_aliases` —
    and state in the SUMMARY, in one sentence, that this module is a CHILD of `ui::screens` and
    can therefore already reach that module's private `tests` and its `pub(super)` fixture, so
    the widening `21-29` F3 asked for and `21-30` deferred is **unnecessary, not merely
    deferred**. Confirm it by measurement at the end of the task:
    `rtk proxy git diff --stat` must not name `src/ui/screens/mod.rs` for this task, and
    `rtk proxy grep -c "pub(super) fn ctx_with_aliases" src/ui/screens/mod.rs` must still return
    1. Prohibition 4 forbids widening anyway.

    (b) **Choose the subject and say why.** Pick ONE input-echo screen from the disposition
    table whose rendered value is what the OPERATOR TYPED rather than what a `.planning/` file
    said — that distinction is the whole of S1 — and name it, its file, and the widget family it
    renders through. If the widget family is one ratatui 0.30 drops zero-width graphemes in,
    say so and rely on the invisible-character-count assertion rather than on a raw-form absence
    assertion; if it is a preserving family, say that too. Prohibition 5 makes naming the family
    mandatory, because an absence assertion over a dropping family is vacuous forever.

    (c) **Write the spot-check, arrival first.** Add
    `an_input_echo_screen_escapes_a_hostile_value_and_leaves_a_clean_one_alone` as a `#[test]`
    that, for ONE `LOOK_ALIKE_PAIRS` entry imported by index (never respelled): builds the ctx
    with `super::tests::ctx_with_aliases`, constructs the chosen screen the way this module
    already constructs it, renders it through its real `Screen::render` into a ratatui `Buffer`
    for the CLEAN member and asserts the clean value is present byte-identical (arrival AND the
    no-over-escaping direction in one assertion, with a failure message distinguishing "the
    fixture never reached the buffer" from "the clean value was rewritten"); then renders the
    HOSTILE member and asserts zero characters in the buffer satisfy
    `crate::text::is_invisible_formatting_char`, reusing this module's existing expected-form
    helper for the escaped shape rather than computing a second one.

    (d) **Observe it RED by a temporary plant, and revert.** Temporarily revert one escape at a
    converted site in the chosen screen's own file — an unowned file this round — run the
    spot-check, capture the panic VERBATIM, restore the escape, and confirm
    `rtk proxy git status --porcelain` clean. The plant's file must be absent from the final
    `git diff --stat` (prohibition 6).

    (e) **State what the spot-check does NOT cover, with its direction.** It is ONE screen and
    ONE fixture pair, chosen because S1 named the input-echo class and not because one screen
    stands for five. Say that in the test's doc: the other four input-echo screens are covered
    by the per-screen behavioural probes this module already runs, which is a different and
    weaker check than this two-direction one; **under-detection, silent, bounded by those
    probes and not by this test.** Do not let a single spot-check acquire a class-wide claim —
    that is this round's own subject.
  </action>
  <acceptance_criteria>
    - `rtk proxy git diff --stat` for this task names ONLY `src/ui/screens/render_escape_guard.rs`, and `rtk proxy grep -c "pub(super) fn ctx_with_aliases" src/ui/screens/mod.rs` returns 1 — the F3 decline, measured.
    - The SUMMARY quotes `render_escape_guard.rs:982` and `:1191` as the two independent call sites that make F3 unnecessary, and states the parent/child module relationship in one sentence.
    - The SUMMARY names the chosen screen, its file, and the ratatui widget family it renders through, and states whether that family drops or preserves zero-width graphemes and which assertion the spot-check therefore relies on.
    - The verbatim RED from step (d) is quoted, the plant's file is named, and `rtk proxy git status --porcelain` is shown clean afterwards.
    - `rtk proxy cargo test --lib -- ui::screens::render_escape_guard --nocapture` exits 0 and its counts are quoted against the pre-task counts, with the delta attributed to this test.
    - The test's doc states the one-screen-one-pair limit with an explicit direction word, and the SUMMARY quotes that sentence.
    - `rtk proxy cargo test --workspace --no-fail-fast` → 0 failures, or `driver_reattach` alone named as the documented flake and shown green on an isolated re-run; totals quoted against the measured 1417/0/13 green baseline with every delta attributed to a test this plan added.
    - `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings` reports exactly FOUR lints, same two kinds, same two files.
  </acceptance_criteria>
  <verify>
    <automated>rtk proxy cargo test --lib -- ui::screens::render_escape_guard --nocapture</automated>
  </verify>
  <done>21-29's S1 is delivered whole: a two-direction spot-check on a named input-echo screen, rendered through its real `Screen::render`, asserting arrival and the no-over-escaping direction before the hostile property, with the widget family named and the check observed red by a reverted plant — and F3 is closed as UNNECESSARY by the measurement that the probe module already reaches the fixture, with no visibility anywhere widened.</done>
  <reversibility rating="reversible">One added `#[cfg(test)]` test in the probe module. No production line changes and no visibility changes; revertible from git.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
**Created by 21-33** (`21-31`'s, `21-32`'s and `21-34`'s symbols are listed in those plans):

| Kind | Symbol | File |
|---|---|---|
| test | `no_display_identity_call_under_ui_stands_outside_a_composition` — the renamed census | `src/ui/mod.rs` |
| fn (test) | `needle_distribution(files) -> Vec<(String, usize)>` — executable needle occurrences per file | `src/ui/mod.rs` |
| const | the committed per-file reach distribution, asserted by equality in both directions | `src/ui/mod.rs` |
| doc rule | the narrowed claim: measured reach, the shrinking-coverage property with its direction, the two named mechanisms that carry completeness, and pass 11's hand-trace with its "not a live leak" conclusion | `src/ui/mod.rs` |
| mod (test) | a local autoref-specialization trait probe for `Display`, `AsRef<str>` and `Into<Cow<'static, str>>`, with `String` presence arms | `src/ui/screens/mod.rs` |
| test | `an_edit_buffer_implements_none_of_the_string_conversions` | `src/ui/screens/mod.rs` |
| doc rule | `EditBuffer`'s corrected non-compiling example with its verbatim compiler error, and its absence claim re-stated as certified | `src/ui/screens/mod.rs` |
| test | the tied-element ordering control for the backlog comparator | `src/state_reader/backlog.rs` |
| test | `an_input_echo_screen_escapes_a_hostile_value_and_leaves_a_clean_one_alone` — 21-29's S1 | `src/ui/screens/render_escape_guard.rs` |

**Changed:** the census's test name and doc; `backlog_number_ordering` gains a string tiebreak
so the order is total over ELEMENTS; `EditBuffer`'s doc names its certificate and carries a
measured example.

**Removed:** the census's completeness claim, replaced by a narrower claim plus a disclosed and
pinned reach; `EditBuffer`'s `buffer.clone()` example, which named a method the type does not
have.

**DECLINED with a measurement, not deferred:** 21-29's F3 — widening `ctx_with_aliases` and its
enclosing module. `render_escape_guard.rs:982` and `:1191` already reach the fixture, so no
visibility changes.

**Unchanged and RUN, never edited:** `src/text.rs` (read for its probe pattern, owned by
`21-32`), `src/test_support.rs`, `tests/driver_injection_corpus.rs`, `src/browser.rs`,
`src/project_creator.rs`, `.planning/REQUIREMENTS.md`.
</artifacts_this_phase_produces>

<verification>
- `rtk proxy cargo build` exit 0; `rtk proxy cargo test --workspace --no-fail-fast` → 0
  failures, totals quoted against the measured 1417/0/13 green baseline with every delta
  attributed to a test this plan added, and any `driver_reattach` failure named as the
  documented flake and shown green on an isolated re-run.
- `rtk proxy cargo clippy -- -D warnings` exit 0; `rtk proxy cargo clippy --all-targets -- -D warnings`
  reports exactly four lints, same two kinds, same two files, before and after — both quoted.
- Six verbatim REDs, each with a clean `rtk proxy git status --porcelain` after the plant: the
  reach pin against a planted needle occurrence; three trait absences against three planted
  impls; the backlog tiebreak against the pre-tiebreak comparator; and the S1 spot-check against
  a reverted escape.
- One verbatim compiler error for `EditBuffer`'s corrected example.
- The SUMMARY states the pattern call for gaps[3] in the executor's own words with the reasoning
  intact, and states the F3 decline with its two-line-number measurement.
- `rtk proxy cargo test --test driver_injection_corpus` re-run at its HEAD counts (13/0/10) with
  `rtk proxy git diff --stat` showing the file unchanged; no work claimed against criterion 4.
- `rtk proxy git diff --stat` names only `src/ui/mod.rs`, `src/ui/screens/mod.rs`,
  `src/ui/screens/render_escape_guard.rs` and `src/state_reader/backlog.rs`.
- `.planning/REQUIREMENTS.md` untouched.
</verification>

<success_criteria>
- The census claims exactly what it checks, its reach is a pinned number rather than a sentence,
  and the shrinking-coverage property that made the old name false is stated with its direction.
- The completeness claim is not dropped, it is HANDED to the two mechanisms that carry it, by
  name, so a reader who loses the census's reach knows where the guarantee lives.
- `EditBuffer`'s three claimed absences are certified by a control observed red three times, and
  its doc no longer argues from a construction that does not exist.
- The backlog order is total over elements, and every existing ordering control's result is
  reported rather than assumed unchanged.
- 21-29's S1 is delivered whole in one plan, and F3 is closed by measurement rather than carried
  a third time or half-moved.
- Every residual this plan leaves is named WITH its failure direction, and no single spot-check
  is allowed to acquire a class-wide claim.
</success_criteria>

<output>
Create `.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-33-SUMMARY.md` when done.
</output>
