---
phase: 21-llm-goal-layer-prompt-injection-hardening
plan: 18
type: execute
wave: 2
gap_closure: true
depends_on: ["21-17"]
files_modified:
  - tests/spawn_seam_guard.rs
  - src/driver/mod.rs
  - src/driver/run.rs
  - tests/driver_dry_run.rs
  - tests/driver_goal_seam.rs
autonomous: true
requirements: [DRIVE-04, SAFE-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Guard nine reports every spelling pass 6 measured it silent on, and every claim is certified by a planted-defect control calling the SAME extracted fns the live assertion consumes: `pub(crate)`/`pub(super)` field openers (the exact spelling `ITEM_OPENERS` was widened for four commits later in the same file), `Box<str>`/`Cow<'_, str>`/`&'static str` payload spellings, and a trailing-`//`-comment declaration — last field AND mid-struct, with attribution correct (no swallowed next line). `OsString` is reported DENY-BY-DEFAULT — any field in the scanned body whose type names `OsString` is reported unless its field NAME is on the explicit two-entry suppress-allowlist (`claude_args`, which legitimately carries argv as `Vec<OsString>`; `claude_program`) — so a seventh field, which by definition carries a NEW name, is bound without anyone pre-listing it. The false-positive direction and the allowlist's own integrity each have a control: `Vec<OsString>`/`Option<PathBuf>` asserted NOT reported, and each allowlisted name pinned to its legitimate type as declared in the real body (`claude_args: Vec<OsString>`, `claude_program: Option<PathBuf>`) so an allowlist entry cannot be silently repurposed (pass-6 gap 2, WR-02, 21-16 truth 1 falsified)."
    - "Guard nine's limits block claims exactly what its committed controls measure: it names what remains silent (macro-expanded declarations, type aliases — each with direction and its own bounding assertion) and states what the ≥10-field/≥6-NonBlank floor ACTUALLY bounds — extraction breakage and wholesale style drift, NOT a single added field, which pass 6 measured passing the floor silently at field_lines=12/protected=6. No guard header in this plan's diff claims a bound no committed control goes red for."
    - "`one_of_each` claims exactly what it delivers: its doc and the pin's failure message state that the ONLY compile-time anchor on `CommandSource`'s arity is `variant_name`'s wildcard-free match (which forces classification, not sweep growth), that constructing values carries no exhaustiveness obligation, and that a fourth variant constructed nowhere passes the set-equality pin until a human grows `one_of_each` — the residual named in the artifact instead of papered over (pass-6 gap 2, WR-01, 21-16 truth 4 falsified; pass-5 warning 4's second recurrence ended by honesty, not by a fictitious mechanism)."
    - "The acceptance matrix sweeps ALL SEVEN argv positions with ALL FOUR padded payloads — the `--alias`/`--run-id` narrowing at src/driver/mod.rs:2563-2566 is deleted, because pass 6 measured its rationale false (`from_argv` never calls `is_plain_path_component` for either field). The comment that replaces it is TRUE after 21-17: `from_argv` judges visibility only; identity look-alikes are refused downstream at the seams, where `LOOK_ALIKE_PAIRS` pins them (pass-6 IN-01, the cycle-3 defect's recurrence in the acceptance matrix, removed)."
    - "`DEGENERATE` is spelled in exactly one place TREE-WIDE and the guard's message is finally true: the uniqueness scan covers tests/ as well as src/, and the three hand-picked subsets (driver_dry_run.rs ×2, driver_goal_seam.rs ×1) consume `test_support::DEGENERATE` — reachable since 21-17 dropped the cfg gate (pass-6 WR-04)."
    - "The record is corrected where round 6 inherited falsehoods: this plan's SUMMARY states plainly that 21-16 truth 1 (the floor bounds the declaration-style gap) and truth 4 (a fourth variant is a compile error in two places) were false as shipped, what was measured, and what is true now — so a seventh reader inherits measurements, not certifications (pass-6 gap 2's 'missing' item 4)."
    - "The CR-01 tracer's blind half is either seen or named: `a_run_with_no_command_source_writes_nothing_before_refusing` also asserts the envelope root received no write, via an injectable root; if env-var injection cannot be made race-free under parallel tests, the test's doc names the envelope half as unobserved WITH its failure direction (silent — a regression moving resolution below establish_envelope keeps this test green) instead of leaving the narrowness undisclosed (pass-6 artifacts table, run.rs:3788 warning)."
    - "A `--max-escalations` value at or above the run's RESOLVED step cap is refused at the seam and a value below it is accepted — both directions pinned and green (`driver_escalation_cap` 8/8), reconfirmed against this plan's final tree (probe: DRIVE-04 boundary)."
    - "The escalation counter's precision holds: the decomposition consultation is counted against the same cap as every other consultation — no consultation path is exempt — reconfirmed by the existing cap pins passing unmodified against this plan's final tree (probe: DRIVE-04 precision)."
    - "Third-party text from `.planning/` files reaches a prompt only inside the labelled untrusted boundary, and arrival is asserted before influence: `driver_injection_corpus`'s arrival-before-influence pins pass unmodified against this plan's final tree (probe: SAFE-07 boundary)."
    - "The untrusted boundary's precision holds: only the enumerated strings cross — never a whole file — reconfirmed by guard seven (`FIELD_OBSERVED_MARKERS` named only where the schema declares it) passing inside this plan's own spawn_seam_guard run (probe: SAFE-07 precision)."
  prohibitions:
    - statement: "MUST NOT certify any guard or mechanism claim with a grep for a sentence of prose. Every claim this plan makes about what a scan reports is certified by a planted-defect control committed red-first against the unfixed scanner — pass 6 proved every round-5 prose-grep criterion passed while both structural claims they certified were false, and this plan exists to end that."
      category: transparency
    - statement: "MUST NOT hand-copy any subset of the degenerate payload set anywhere in the tree — src/ OR tests/; every blank-shape pin consumes `test_support::DEGENERATE`, and the widened uniqueness guard is the standing check."
      category: transparency
    - statement: "MUST NOT leave a guard doc, limits block, or plan truth claiming a compile-time or floor-based bound that no committed control measures; where a mechanism cannot deliver a bound, the residual is named in the doc and in the SUMMARY, never certified."
      category: transparency
    - statement: "MUST NOT flip any `.planning/REQUIREMENTS.md` requirement to `[x]`/Complete in any commit of this plan; requirement status is decided exclusively by a `passed` verification (`828d7cc`, `0c4f712` — twice reverted, three rounds held)."
      category: process
  artifacts:
    - path: "tests/spawn_seam_guard.rs"
      provides: "guard nine seeing pub(-opened fields, non-String string payload types, and trailing-comment declarations, with control arms per claim; honest limits block; tree-wide DEGENERATE uniqueness scan"
      contains: "pub("
    - path: "src/driver/mod.rs"
      provides: "one_of_each with an honest doc and pin message; the acceptance matrix sweeping 7 positions x 4 padded payloads with no exemption"
    - path: "tests/driver_dry_run.rs"
      provides: "both blank-payload lists consuming test_support::DEGENERATE"
      contains: "DEGENERATE"
    - path: "tests/driver_goal_seam.rs"
      provides: "the hostile blank-token list consuming test_support::DEGENERATE"
      contains: "DEGENERATE"
  key_links:
    - from: "tests/spawn_seam_guard.rs (guard nine controls)"
      to: "tests/spawn_seam_guard.rs (raw_string_argv_fields / names_bare_string)"
      via: "every planted spelling exercises the SAME extracted fns the live assertion consumes — a control that re-implemented the scan would witness only its agreement with itself"
      pattern: "raw_string_argv_fields"
    - from: "tests/driver_dry_run.rs, tests/driver_goal_seam.rs"
      to: "src/test_support.rs (DEGENERATE)"
      via: "integration-side consumption, reachable since 21-17 removed the cfg gate; the widened uniqueness guard enforces it stays this way"
      pattern: "DEGENERATE"
    - from: "src/driver/mod.rs (padded acceptance matrix)"
      to: "src/journal/mod.rs (is_plain_path_component) via the seam pins"
      via: "the replacing comment's claim — identity look-alikes refused downstream — is true because 21-17's clause and LOOK_ALIKE_PAIRS pins landed first (the wave dependency)"
      pattern: "LOOK_ALIKE_PAIRS"
---

<objective>
Close pass-6's gap 2: both anti-recurrence mechanisms round 5 built constrain materially less
than their docs, their plan truths, and 21-16-SUMMARY's audit table certify. The verifier
graded this MORE important than the Critical, because these are the artifacts a sixth reader
trusts instead of re-deriving — and the previous five rounds each inherited exactly one wrong
assumption this way.

Measured at pass 6, reproduced independently of the review:

- **Guard nine is silent on spellings `DriveArgs` already uses**: `pub(crate)`, `pub(super)`
  (the exact spelling `ITEM_OPENERS` was widened for, four commits later, in the same file),
  `Box<str>`, `Cow<str>`, `&'static str`, `OsString`, and a trailing-`//` last field — with a
  mid-struct trailing comment additionally swallowing the next declaration line. Its stated
  ≥10/≥6 floor was measured NOT binding: `field_lines=12 protected=6 → PASSES (silent)` with a
  planted `pub(crate)` offender.
- **`one_of_each` constrains nothing**: zero compile errors for a fourth variant; the pin
  passes with a whole variant unswept. Its doc claims a compile error "in two ways"; 21-16
  truth 4 claims "TWO places". Both false.
- **The acceptance matrix hand-exempts `--alias`/`--run-id`** on a rationale about a function
  `from_argv` never calls — the cycle-3 defect recurring.
- **The uniqueness guard's message overclaims its scan** (src/ only; three subsets in tests/).

One qualification the verifier insists on, kept here so this plan does not over-correct:
`from_argv`'s no-`..` destructure IS a real compile-time bound — it forces classification. It
does not force a payload TYPE, and guard nine is the check that does; this plan makes guard
nine deliver that, and makes every other mechanism say exactly what it delivers.

The method is the one the verifier named as the model: the `ITEM_OPENERS` half of round 5
(plant the defect, feed it to the same extracted fn, watch it go red, fix, watch it go green).
Every acceptance criterion in this plan is a mechanism test or a control — none is a grep for
a sentence of prose.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-PREMISES.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-REVIEW.md
@.planning/phases/21-llm-goal-layer-prompt-injection-hardening/deferred-items.md
</context>

<tooling_note>
**`rtk` filters build/test/grep output.** Every count-bearing or presence-bearing check runs
as `rtk proxy <cmd>`. The clippy gate is `rtk proxy cargo clippy --lib -- -D warnings`
(`--all-targets` carries 4 pre-existing lints in untouched files). Known flakes, not
regressions: `driver_reattach`'s two tests and `envelope_tracer`'s relocated-stub test; the
honest whole-suite gate is
`rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2`, re-running a flaked
binary alone before reporting a failure.
</tooling_note>

<edge_probe_audit>
Spec-less probe fallback is ACTIVE. The 7-row disposition table lives in 21-17's
`<edge_probe_audit>`; this plan authors the four resolvable rows as explicit truths (DRIVE-04
boundary → truth 8, DRIVE-04 precision → truth 9, SAFE-07 boundary → truth 10, SAFE-07
precision → truth 11 — each verified by a named green suite against this plan's final tree,
not by presence). The three unclassified rows are flagged assumptions in 21-17. 7 surfaced =
4 authored here + 3 flagged there; no silent drops. Recalled prohibitions are authored
descriptor-less in `must_haves.prohibitions` above.
</edge_probe_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard suite → future maintainers | a guard whose header overclaims is inherited as fact; four consecutive rounds each inherited one wrong assumption this way |
| `DriveArgs` declaration → guard nine | the scan is the only mechanism forcing a payload TYPE on a new argv field; its blind spellings are the reopening path |
| plan/SUMMARY record → round 7 | 21-16 truths 1 and 4 are false in the record; an uncorrected record is the inheritance vector |

## STRIDE Threat Register (ASVS L1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-18-01 | Tampering | a seventh argv field in a spelling guard nine cannot see | high | mitigate | Reproduced (WR-02): 6 of 10 planted declarations silent, 1 misattributed, floor non-binding. Task 1 fixes openers, type spellings, trailing comments, pending flush — each red-armed with a control first. |
| T-21-18-02 | Repudiation | guard headers / plan truths certifying unmeasured bounds | high | mitigate | The inheritance vector. Task 1 rewrites guard nine's limits to match its controls; Task 3 corrects 21-16 t1/t4 in the record. |
| T-21-18-03 | Tampering | a fourth `CommandSource` variant swept nowhere | medium | mitigate | Reproduced (WR-01): pin passes with variant unswept. Task 2 makes the doc honest and names the residual; `variant_name`'s wildcard-free match remains the real (classification-only) anchor. |
| T-21-18-04 | Repudiation | the CR-01 ordering regression invisible to its own tracer | medium | mitigate | Task 3 extends the assertion to the envelope root or names the blind half with direction in the test doc. |
| T-21-18-05 | Tampering | hand-copied degenerate subsets drifting from the const | low | mitigate | Task 2 widens the uniqueness scan to tests/ and converts the three subsets. |

**Package legitimacy:** no `cargo add`, no `Cargo.toml` change, no dependency movement.
</threat_model>

<decisions>
## Decisions and reversibility (REVERSIBILITY_GATES=true)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| D-18-1 | `one_of_each`'s false compile-error claim is DELETED and the residual named, rather than simulating exhaustiveness | **reversible** | The verifier's own analysis: even a destructuring helper forces classification, not collection growth. The only honest anchors are `variant_name`'s wildcard-free match (kept) and the named residual. A fictitious second mechanism is the defect class this plan closes. |
| D-18-2 | Guard nine widens by fixing the extracted fns, with one control arm per claim, committed red-first; `OsString` reported deny-by-default with a two-entry suppress-allowlist (`claude_args`, `claude_program`) | **reversible** | The `ITEM_OPENERS` model, applied to the guard that skipped it. Deny-by-default chosen over a protected-name rule because a seventh argv field carries a new name by definition — a by-name rule covers no case the threat model names. The allowlist errs loud (a new legitimate OsString field goes red until a human lists it) and each allowlisted entry is pinned to its legitimate type in the live run, so it cannot be repurposed silently. |
| D-18-3 | The padded acceptance matrix sweeps all 7 columns × 4 payloads; the replacing comment defers identity to the seams | **reversible** | Honest only after 21-17 (the seams now refuse look-alikes and pin it) — hence the wave dependency. Deleting the exemption cannot hide a refusal failure; it removes two columns of missing acceptance coverage. |
| D-18-4 | The uniqueness guard scans tests/ as well as src/ | **reversible** | Enabled by 21-17's cfg drop. The alternative (narrowing the message to src/) leaves three drift-prone hand copies in place; converting them is the same size and stronger. |
| D-18-5 | 21-16's falsified truths are corrected in THIS plan's SUMMARY, not by editing 21-16's artifacts | **reversible** | Prior plans and summaries are immutable history; the correction is a new record that names them, which is how the phase has handled every falsified comment (the correction rides the commit that falsifies the old text). |

No decision is `one-way`; no `checkpoint:decision` gate is required.
</decisions>

<tasks>

<task type="tracer">
  <name>Task 1 (TRACER): guard nine's blind spellings planted and red, then the scanner fixed, then green — and a limits block that matches its controls</name>
  <read_first>
    - tests/spawn_seam_guard.rs (lines 2660-2965: guard nine end to end — the limits header, raw_string_argv_fields, drive_args_body, names_bare_string, the planted-field control, the live assertion with its two floors; lines 1986-2060: the ITEM_OPENERS control that is the model)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md (the WR-02 reproduction table — the exact ten planted declarations and which were silent/misattributed, and the floor measurement)
    - src/driver/mod.rs (the DriveArgs declaration — the real body the live assertion reads; confirm the twelve fields and the two cfg-gated OsString/PathBuf fields)
  </read_first>
  <action>
    RED FIRST, committed before the fix. Extend
    `the_raw_argv_field_scanner_reports_a_planted_string_field` (or add a sibling control test
    `the_raw_argv_field_scanner_sees_every_measured_silent_spelling` if the existing fn grows
    unwieldy) with synthetic bodies planting, one assertion each, the pass-6 table verbatim:
    `pub(crate) goal_file: Option<String>,` must be reported; `pub(super) goal_file:
    Option<String>,` must be reported; `pub goal_file: Option<Box<str>>,` must be reported;
    `pub goal_file: Option<Cow<'static, str>>,` must be reported; `pub goal_file:
    Option<&'static str>,` must be reported; a LAST field `pub goal_file: Option<String>, //
    seventh` must be reported; a MID-STRUCT `pub goal_file: Option<String>, // seventh`
    followed by `pub dry_run: bool,` must be reported WITH the following line NOT swallowed
    into its text and dry_run not reported; and `pub goal_file: Option<OsString>,` must be
    reported (the deny-by-default rule below — `goal_file` is on no allowlist, which is the
    point: it stands in for the seventh field's new name). All of these exercise the SAME
    `raw_string_argv_fields`/`names_bare_string` fns the live assertion consumes. Also plant
    the floor probe: a full twelve-field DriveArgs body PLUS one `pub(crate) goal_file:
    Option<String>` and assert the scan reports the offender (pass 6 measured
    offenders=[] on exactly this input). Run
    `rtk proxy cargo test --test spawn_seam_guard the_raw_argv_field_scanner` and RECORD the
    failures verbatim — at HEAD six plants are silent and one misattributes. COMMIT the red
    arm before the fix: gate the new control assertions behind `#[ignore = "red: pass-6 WR-02
    reproduction; un-ignored in the fix commit"]` on the new test fn, with the verbatim red
    output quoted in its doc comment and the commit message. The SUMMARY quotes the same.

    THEN FIX, in the next commit, removing the `#[ignore]`:

    (a) Openers: in `raw_string_argv_fields` the field-start test becomes
    `trimmed.starts_with("pub ") || trimmed.starts_with("pub(")`, and the SAME widening lands
    in the live assertion's own field-line filter (the floor's filter — the second place pass
    6 named). One helper `fn is_field_opener(trimmed: &str) -> bool` used by BOTH, so the scan
    and the floor cannot disagree again.

    (b) Type spellings: widen `names_bare_string` into `names_string_payload(text: &str)`
    matching, with the existing word-boundary semantics, any of the tokens `String`, `str`
    (which covers `Box<str>`, `Cow<'_, str>`, `&'static str`, `&str`) — keeping `OsString`
    and `PathBuf` unmatched BY TOKEN (word-boundary on `str` must not fire inside `OsStr`/
    `OsString`; note `str` appears as a whole word in `Box<str>` but not in `OsString`, and
    add an explicit control asserting `Vec<OsString>` on an allowlisted name is not reported).
    Handle `OsString` DENY-BY-DEFAULT: any field in the scanned body whose type text names
    `OsString` (word-boundary token) is reported UNLESS the field's name is on the explicit
    suppress-allowlist `OSSTRING_ALLOWED: ["claude_args", "claude_program"]`. Chosen over a
    protected-name rule because the threat is a SEVENTH argv field, which by definition
    carries a name no protected list contains — a by-name rule covers no case the threat
    model names. The allowlist can only SUPPRESS, never widen: a new legitimate
    OsString-carrying field goes RED until a human adds its name — over-reporting, loud, the
    safe direction. And the allowlist cannot be silently repurposed: the live assertion
    additionally pins each allowlisted name to its legitimate type in the real body
    (`claude_args: Vec<OsString>`, `claude_program: Option<PathBuf>`), so respelling either field's
    type breaks the pin loudly. Reason in the doc: `OsString` is the type argv actually
    arrives in, so a payload field respelled `OsString` is the same defect wearing the
    platform type.

    (c) Trailing comments and the pending buffer: before the `ends_with(',')` test, strip a
    trailing `// …` from the joined text (split on `//` outside any type context — a simple
    rsplit on `//` with the left half trimmed is sufficient for declaration lines and is
    asserted by the two comment controls); when the body ends with `pending` still `Some`,
    flush and judge it rather than dropping it (the last-field case).

    (d) Rewrite guard nine's limits header to match the controls: what is now SEEN
    (pub(-opened fields, str-family payload types, OsString deny-by-default over the whole
    body, trailing comments, both comment positions), what remains SILENT with direction
    (macro-expanded declarations; a local type alias hiding `String`/`OsString` under a new
    name — still bounded by its own zero-assertion), and
    what the floor ACTUALLY bounds: the ≥10/≥6 floors catch extraction breakage and wholesale
    declaration-style drift; they provably do NOT catch a single added field (pass 6:
    field_lines=12/protected=6 passes) — the OFFENDER SCAN is what catches that, which is why
    its blind spellings were the whole gap. Delete every sentence the controls do not measure.

    GREEN. `rtk proxy cargo test --test spawn_seam_guard` → 0 failures with all control
    assertions un-ignored; the live `drive_args_declares_no_raw_argv_string_field` still
    passes against the real (fully protected) DriveArgs.
  </action>
  <acceptance_criteria>
    - Red-arm evidence committed BEFORE the fix commit (the `#[ignore]`-marked control with
      verbatim red output in doc + commit message), and quoted in the SUMMARY.
    - All planted-spelling controls pass un-ignored: pub(crate), pub(super), Box<str>,
      Cow<'static, str>, &'static str, last-field trailing comment, mid-struct trailing
      comment with correct attribution, OsString deny-by-default (`pub goal_file:
      Option<OsString>,` reported), and the twelve-plus-one floor probe
      — `rtk proxy cargo test --test spawn_seam_guard` → 0 failures.
    - `Vec<OsString>` (`claude_args`) and `Option<PathBuf>` (`claude_program`) are asserted
      NOT reported (the false-positive direction has its own control).
    - The allowlist is suppress-only and pinned: the live assertion asserts `claude_args`'s
      type is `Vec<OsString>` and `claude_program`'s is `Option<PathBuf>` — the declarations
      as they exist in the real DriveArgs body,
      so an allowlisted name cannot be silently repurposed for a payload field.
    - One shared `is_field_opener` helper is consumed by both the scan and the floor filter
      (`rtk proxy grep -c "is_field_opener" tests/spawn_seam_guard.rs` ≥ 3).
    - Guard nine's limits block: every claim in it has a named committed control, the floor
      sentence states extraction/style bounding only, and the remaining silent shapes are
      named with directions. Certified by the controls existing and passing — no prose-grep
      criterion for the block's wording.
    - `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat); `rtk proxy cargo clippy --lib -- -D warnings` clean.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: one_of_each says what it does; the acceptance matrix loses its hand-exemption; DEGENERATE becomes unique tree-wide</name>
  <read_first>
    - src/driver/mod.rs (lines 2040-2130: variant_name, one_of_each and its doc, the both-directions pin; lines 2549-2580: one_visible_character_is_accepted_in_every_argv_position and the narrowing)
    - tests/spawn_seam_guard.rs (lines 3067-3097: the_degenerate_payload_set_is_spelled_in_exactly_one_place, and the source_files helper it scans with; lines 2380-2423: guard eight's limits block — the 1,6,2,3,4,5 numbering to fix in passing)
    - tests/driver_dry_run.rs (lines 595-715: the two hand-picked blank lists and the in-place disclosure at :602-610 this task retires)
    - tests/driver_goal_seam.rs (line 335: the third subset; lines 1500-1530: the assertion messages with lost line continuations, to repair in passing)
    - src/test_support.rs (DEGENERATE and LOOK_ALIKE_PAIRS — both now reachable from tests/)
  </read_first>
  <action>
    (a) `one_of_each`, honest: rewrite its doc and the pin's failure message. Delete "is a
    compile error HERE too, in two ways" and every equivalent. State what is measured: an
    array literal of constructed values carries NO exhaustiveness obligation — pass 6 built a
    fourth variant with `variant_name`'s single arm added and this pin PASSED with the variant
    unswept, zero compile errors. The only compile-time anchor is `variant_name`'s
    wildcard-free match, and it forces CLASSIFICATION (someone must open this module and name
    the variant), not sweep growth. The residual, named: a fourth variant classified in
    `variant_name` but not constructed in `one_of_each` passes this pin with the variant
    unswept by the matrix's coverage assertion; what then catches it is nothing mechanical —
    it is the reviewer reading this sentence, which is why the sentence is here. Update the
    pin message's "compile error in `variant_name` and in `one_of_each`" claim to name
    `variant_name` alone. (D-18-1: no simulated exhaustiveness — a `named(source)` helper
    would also force only classification, and a second mechanism that overpromises is the
    defect class this plan closes.)

    (b) The acceptance matrix: in `one_visible_character_is_accepted_in_every_argv_position`,
    delete the `--alias`/`--run-id` narrowing (the `if position.starts_with…` branch at
    :2563-2566) and sweep all seven positions with all four padded payloads
    (`"x"`, `" x "`, `"\u{200b}x"`, `"x\u{feff}"`). Replace the false rationale comment with
    the true one: `from_argv` judges VISIBILITY — one visible character is an instruction in
    every position, including alias and run id, and `from_argv` accepts a padded look-alike
    in those positions BY DESIGN; the identity question ("do these bytes name exactly one
    thing?") is answered downstream at the seams (`is_plain_path_component` at the
    run-id/target-phase seams since 21-17's clause, `Alias::new` at registration), each
    pinned with `LOOK_ALIKE_PAIRS` in its own suite. Cite the drive-level consequence: a
    look-alike `--run-id` passes this boundary and is refused by `drive` at the seam before
    anything is created.

    (c) DEGENERATE uniqueness, tree-wide: convert the three hand subsets to consume
    `gsd_meta_manager::test_support::DEGENERATE` — tests/driver_dry_run.rs :611 and :704
    (retiring the :602-610 in-place disclosure with a one-line correction: the const became
    reachable when the cfg gate dropped in 21-17), and tests/driver_goal_seam.rs :335 (its
    list serves a hostile-token loop; DEGENERATE's six shapes are a superset of the four it
    hand-picked — verify each of the six is a valid fixture for that loop's assertion, and if
    any shape is genuinely out of place there, keep the loop over DEGENERATE and handle the
    exception with a named, commented filter rather than a re-copied list). Then widen
    `the_degenerate_payload_set_is_spelled_in_exactly_one_place` to scan tests/ as well as
    src/ (extend or complement `source_files()` with a tests/ walk in the same shape), so its
    tree-wide message becomes true. Its doc drops the src/-only caveat; its non-vacuity
    (home-file hit == 1) is unchanged.

    (d) In passing, in the files already open: renumber guard eight's limits block 1-6
    (currently 1,6,2,3,4,5 — the Info finding; this block is what prohibition audits cite),
    and repair the assertion messages that lost their `\` line continuations and embed 8-10
    space runs (tests/driver_goal_seam.rs :1514+, tests/spawn_seam_guard.rs :2132/:2136) —
    these are the messages a future reviewer reads when a guard fires.
  </action>
  <acceptance_criteria>
    - `rtk proxy sh -c "sed -n '/fn one_of_each/,/^    }/p' src/driver/mod.rs"` output
      contains no claim of a compile error attributed to one_of_each or the array length
      (checked by reading the extraction in the SUMMARY, not by a prose grep — quote the
      rewritten doc there), and `all_variant_names_matches_the_variant_set_in_both_directions`
      still passes.
    - The padded sweep covers 7 × 4: `rtk proxy grep -c "position.starts_with" src/driver/mod.rs`
      → 0 within the acceptance test (the narrowing branch is gone), and
      `rtk proxy cargo test --lib one_visible_character` → 0 failures.
    - `rtk proxy grep -rn "for blank in \[" tests/` → 0 hits, with the positive control
      `rtk proxy grep -rln "DEGENERATE" tests/` ≥ 3 files — the three subsets consume the
      const and no new hand list appeared.
    - The uniqueness guard scans tests/: feeding it is certified by its own behavior — with
      the three subsets converted it passes; the SUMMARY additionally quotes a one-off local
      probe (a scratch re-add of one hand list under tests/, never committed) showing the
      widened scan reports it — the planted-defect standard applied to the widened region.
    - `rtk proxy cargo test --test spawn_seam_guard --test driver_dry_run --test driver_goal_seam`
      → 0 failures.
    - `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat); `rtk proxy cargo clippy --lib -- -D warnings` clean.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: the CR-01 tracer's envelope half, the probe-authored reconfirmations, the record corrections, and the audits</name>
  <read_first>
    - src/driver/run.rs (lines 2400-2500: execute_run's resolution-above-writes ordering and establish_envelope at :2493; lines 3770-3800: a_run_with_no_command_source_writes_nothing_before_refusing and its fixture)
    - src/envelope/mod.rs (the envelope root resolution — how GSD_MM_ENVELOPE_ROOT / dirs::data_local_dir is consulted, and whether a root can be injected without process-global env mutation)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-16-SUMMARY.md (lines 245-270: the prohibition-audit table whose certification pass 6 measured false — the register this plan's SUMMARY must correct)
    - .planning/phases/21-llm-goal-layer-prompt-injection-hardening/21-VERIFICATION.md (the run.rs:3788 warning and the behavioral spot-check table — the suites the probe truths cite)
  </read_first>
  <action>
    (a) The CR-01 tracer's blind half. Determine whether the envelope root the test's run
    would use can be observed without process-global env mutation: if `establish_envelope`
    resolves through `GSD_MM_ENVELOPE_ROOT`, set it via the test-fixture pattern the envelope
    suites already use IF one exists that is safe under `--test-threads=2` (read the envelope
    tests first; a `std::env::set_var` in a parallel test is NOT safe and must not be added).
    If safe injection exists: extend
    `a_run_with_no_command_source_writes_nothing_before_refusing` to assert the injected
    envelope root received no write (absent or empty) after the refusal. If it does not:
    extend the TEST'S DOC instead, naming the unobserved half with its failure direction in
    guard six's register — this assertion covers `.planning/meta-manager` only; the envelope
    root lives outside the project by construction, so a regression moving the source
    resolution below `establish_envelope` (:2493) keeps this test green while writing into
    the developer's real data directory — silent, bounded by the ordering being read directly
    at review time (cite pass 6 doing exactly that). Either way the narrowness stops being
    undisclosed; record which branch was taken in the SUMMARY as the disposition of pass-6's
    run.rs:3788 warning.

    (b) Probe-authored reconfirmations (truths 8-11): run
    `rtk proxy cargo test --test driver_escalation_cap --test driver_injection_corpus --test spawn_seam_guard -- --test-threads=2`
    against this plan's final tree and quote the per-binary pass counts in the SUMMARY beside
    each truth (8/8 cap both-directions; injection corpus arrival-before-influence; guard
    seven inside the 0-failure guard run). These truths assert reconfirmation against THIS
    tree, not historical greens.

    (c) Record corrections, in this plan's SUMMARY under a `## Record corrections` heading:
    (1) 21-16 truth 1 as shipped ("bounded by the ≥10/≥6 floor") was false — pass 6 measured
    `field_lines=12 protected=6 → PASSES (silent)` with a planted offender; what is true now
    is Task 1's control-certified scan and the rewritten floor sentence. (2) 21-16 truth 4 as
    shipped ("a compile error in TWO places") was false — pass 6 measured zero compile errors
    with the pin passing; what is true now is Task 2's honest doc: classification forced by
    `variant_name` alone, residual named. (3) 21-16-SUMMARY's prohibition-audit row
    certifying prohibition 1 via that floor was a certification of an unmeasured bound; this
    plan's own audit (below) certifies only control-measured claims. Prior artifacts are not
    edited (D-18-5); this correction is the new record round 7 inherits.

    (d) Prohibition audit, required for completion, every row a MECHANISM: (1) no-prose-grep
    — the audit itself demonstrates it: each structural claim row cites its control test name
    and result (Task 1's controls, Task 2's uniqueness probe), and no row's check is a grep
    for a sentence; (2) DEGENERATE uniqueness — the widened guard's pass plus Task 2's
    scratch-probe quote; (3) no-unmeasured-bound — the limits-block claims each mapped to a
    control in the Task 1 table; (4)
    `rtk proxy git log --format=%H -- .planning/REQUIREMENTS.md` → no commit of this plan.

    (e) Named-shape audit: reproduce 21-17's table rows 9-16 and 19 with final states
    (control test names + results, or the disclosure location for the one_of_each residual
    and the envelope-half disposition; row 16 resolves via this SUMMARY's Record corrections;
    row 19 via Task 2(d)). Rows 1-8 and the two disclosed deviations (rows 17-18) are cited
    as closed/disclosed by 21-17's SUMMARY; row 20 (IN-04) is cited as adjudicated
    pre-existing/out-of-scope per review G.4 — no covering task in either plan.
  </action>
  <acceptance_criteria>
    - The CR-01 tracer either asserts the envelope root untouched (with a race-safe injected
      root) or its doc names the unobserved half with direction; the SUMMARY records which,
      as the disposition of pass-6's run.rs:3788 warning. No `std::env::set_var` is added to
      any parallel-executed test.
    - `rtk proxy cargo test --test driver_escalation_cap` → 8 passed / 0 failed;
      `rtk proxy cargo test --test driver_injection_corpus` → 0 failed;
      `rtk proxy cargo test --test spawn_seam_guard` → 0 failed — quoted beside truths 8-11
      in the SUMMARY.
    - The SUMMARY carries: `## Record corrections` (21-16 t1, t4, and the audit-table row,
      each with the pass-6 measurement and the now-true statement), the prohibition audit
      (4 rows, mechanism checks with quoted output), and the named-shape audit rows 9-16
      and 19 resolved.
    - `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` → 0 failures
      (flake caveat per tooling_note); `rtk proxy cargo build --all-targets` and
      `rtk proxy cargo clippy --lib -- -D warnings` clean.
    - No commit of this plan touches `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- Guard nine, control-certified: `is_field_opener` (shared opener helper),
  `names_string_payload` (widened from `names_bare_string`; str-family tokens +
  OsString deny-by-default with the `OSSTRING_ALLOWED` two-entry suppress-allowlist and its
  type pins), trailing-comment stripping and pending-flush in
  `raw_string_argv_fields`; the rewritten limits block
- Control tests: `the_raw_argv_field_scanner_sees_every_measured_silent_spelling` (or the
  extended existing control) covering the nine pass-6 rows plus the floor probe and the
  false-positive direction (`Vec<OsString>`, `Option<PathBuf>`)
- `one_of_each` with an honest doc and pin message; the named residual
- `one_visible_character_is_accepted_in_every_argv_position` sweeping 7 × 4 with no
  exemption branch
- `the_degenerate_payload_set_is_spelled_in_exactly_one_place` scanning src/ AND tests/;
  the three tests/ subsets consuming `test_support::DEGENERATE`
- The CR-01 tracer's envelope-half assertion or its named-direction doc
- SUMMARY sections: Record corrections; prohibition audit (mechanism rows); named-shape
  audit rows 9-16 and 19
</artifacts_produced>

<verification>
1. `rtk proxy cargo build --all-targets` — clean; `rtk proxy cargo clippy --lib -- -D warnings` — clean.
2. `rtk proxy cargo test --workspace --no-fail-fast -- --test-threads=2` — 0 failures (the
   documented flakes excepted; re-run the binary alone).
3. Red-arm evidence for the tracer: the `#[ignore]`-marked control commit precedes the fix
   commit in `git log`; the SUMMARY quotes the verbatim red output (six silent, one
   misattributed — the pass-6 table reproduced in-tree).
4. Every structural claim in this plan maps to a committed control that goes red without the
   fix: the guard-nine spelling table, the floor probe, the uniqueness scratch-probe, guard
   ten's planted ninth (21-17). No acceptance criterion greps for prose.
5. The probe-authored truths 8-11 quoted with per-binary counts from runs against the final
   tree.
6. The SUMMARY's Record corrections section exists and names 21-16 t1/t4 with measurements.
</verification>
