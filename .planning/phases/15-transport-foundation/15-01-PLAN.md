---
phase: 15-transport-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-transport-foundation/15-SPIKE-OQ1.md
  - Cargo.toml
  - Cargo.lock
  - README.md
  - CLAUDE.md
  - CONTRIBUTING.md
  - docs/DEVELOPMENT.md
  - docs/GETTING-STARTED.md
  - docs/TESTING.md
  - tests/fixtures/transcripts/01-success-textonly.ndjson
  - tests/fixtures/transcripts/02-budget-exhausted.ndjson
  - tests/fixtures/transcripts/03-tooluse-success-settingsources.ndjson
  - tests/fixtures/transcripts/04-hookhang-aborted-tools.ndjson
  - tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson
  - tests/fixtures/transcripts/06-interrupt-aborted-streaming.ndjson
  - tests/fixtures/transcripts/07-interrupt-early.ndjson
  - tests/fixtures/transcripts/08-tooluse-queued-two-turns.ndjson
  - tests/fixtures/transcripts/README.md
autonomous: true
requirements: [TRANS-01, TRANS-02, TRANS-04]

must_haves:
  truths:
    - "A real multi-step GSD skill that spawns subagent waves runs headlessly against a disposable scratch GSD project to completion and the process exits on its own, not on an external timeout (D-27, D-28)"
    - "`15-SPIKE-OQ1.md` carries a machine-readable `**Verdict:** PASS` or `**Verdict:** FAIL` line, and a FAIL halts the phase instead of being worked around (D-27)"
    - "`cargo build` succeeds with `rust-version = \"1.87\"` declared in Cargo.toml and both process-wrap and uuid resolved (D-03, D-20)"
    - "Every project-MSRV statement in the docs states the new floor and attributes it to the process-wrap dependency, not to the crate edition or the file-watcher crate (D-20)"
    - "Eight golden NDJSON transcripts are committed under tests/fixtures/transcripts/, every line parses as JSON, and none carries a host-absolute path or a credential-shaped string (D-24, D-25)"
  artifacts:
    - path: ".planning/phases/15-transport-foundation/15-SPIKE-OQ1.md"
      provides: "OQ1 multi-step spike verdict and evidence"
      contains: "**Verdict:**"
    - path: "Cargo.toml"
      provides: "machine-checkable MSRV floor and the two transport dependencies"
      contains: "rust-version"
    - path: "tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson"
      provides: "the two-system/init, two-result multi-turn transcript that pins D-29 and D-30"
    - path: "tests/fixtures/transcripts/README.md"
      provides: "per-fixture provenance, exit code, and redaction record"
  key_links:
    - from: "Cargo.toml"
      to: "process-wrap 9.1.0 published manifest"
      via: "rust-version floor is inherited from the dependency's own rust-version"
      pattern: "rust-version"
    - from: "docs/GETTING-STARTED.md"
      to: "Cargo.toml"
      via: "documented toolchain requirement must equal the declared rust-version key"
      pattern: "1\\.87"
---

<objective>
Gate the phase on the OQ1 multi-step spike (D-27, D-28), then lay the two pieces of
foundation every later plan stands on: the toolchain and dependency floor (D-03, D-20) and
the golden NDJSON transcript fixtures (D-24, D-25).

Purpose: Research CONFIRMED that `--setting-sources project` suppresses the `PreToolUse`
hook hang at single-tool-call scale (exit 0 in 8s versus exit 124 at a 90s cap without it).
What is still unproven — and what the whole v2.0 milestone rests on — is the multi-step
generalisation: a real GSD skill that itself spawns subagent waves, run headlessly to
completion. If that fails, this phase must stop and say so, not build on sand (D-27).

Output: `15-SPIKE-OQ1.md` with a machine-readable verdict; a Cargo manifest carrying
`rust-version` and the two new transport dependencies; eight redacted golden transcripts
under `tests/fixtures/transcripts/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-transport-foundation/15-CONTEXT.md
@.planning/phases/15-transport-foundation/15-RESEARCH.md
@.planning/phases/15-transport-foundation/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OQ1 multi-step spike — the phase gate (D-27, D-28)</name>
  <reversibility rating="reversible">The spike writes no production code and touches no
  registered project; a wrong probe design is re-run, not migrated.</reversibility>
  <files>.planning/phases/15-transport-foundation/15-SPIKE-OQ1.md, .planning/phases/15-transport-foundation/transcripts-raw/08-oq1-multistep.ndjson, .planning/phases/15-transport-foundation/transcripts-raw/09-oq1-tooluse-two-turns.ndjson</files>
  <read_first>
    - `.planning/phases/15-transport-foundation/15-RESEARCH.md` — sections `## Spike Outcomes (OQ1 / OQ2 / OQ3)` (the exact argv both arms used, the exit-124 signature, the `duration_ms`-pinned-to-cap tell) and `## Assumptions Log` rows A1 and A3
    - `.planning/phases/15-transport-foundation/15-CONTEXT.md` — D-01 (argv baseline), D-15 (permission flags), D-27 (this task gates the phase), D-28 (safety fences)
    - `.planning/ROADMAP.md` — the Phase 15 success criteria and the MUST-SPIKE risk block
  </read_first>
  <action>
    Run the multi-step generalisation of the OQ1 probe and record a machine-readable verdict.

    SAFETY FENCES, non-negotiable (D-28): the spike targets a disposable scratch GSD project
    created for the purpose; it must NOT target this repository (an autonomous milestone run
    is in flight against it and a spike driving it would race the orchestrator); it must NOT
    target any project registered in the user's gsd-meta-manager config; it must NOT use
    `--dangerously-skip-permissions` or `--permission-mode bypassPermissions`; every
    invocation runs under an external wall-clock bound.

    Step 1 — build the scratch project. `mktemp -d` a directory under the system temp root
    (outside this repo), `git init` it, commit one trivial file. Write a minimal
    `.planning/` tree: `PROJECT.md`, `ROADMAP.md` declaring one phase, `STATE.md`, and
    `.planning/phases/01-hello/01-01-PLAN.md` containing two trivial `type="auto"` tasks
    (each creates one text file and verifies with `test -f`). This is the disposable target.

    Step 2 — pre-flight the skill surface. Run a 60-second text-only probe from inside the
    scratch directory using the D-01 argv baseline plus `--permission-mode dontAsk` and
    `--strict-mcp-config` (D-15), and inspect the first `system/init`. Its `skills[]` or
    `slash_commands[]` must contain at least one entry whose name begins with `gsd`. If none
    does, symlink the user-level GSD install (the `gsd-*` skill directories and `gsd-core`)
    into `<scratch>/.claude/` and re-probe. Record in the report whether this was necessary —
    a spike that silently ran against a Claude with no GSD skills proves nothing.

    Step 3 — the main run. From inside the scratch directory, with a fresh `uuidgen` session
    id, spawn the D-01 baseline argv (`claude -p --input-format stream-json --output-format
    stream-json --verbose --replay-user-messages --session-id <uuid> --setting-sources
    project`) plus `--permission-mode dontAsk --strict-mcp-config`, under `timeout -s TERM
    900`, feeding one NDJSON user message on stdin whose text is the GSD execute-phase slash
    command for phase 1. Set `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` explicitly in the child
    environment rather than inheriting the silent default (D-14), and scrub every other
    inherited `CLAUDE_*` variable from the child environment — this executor is itself
    running inside a Claude Code session, so unscrubbed inheritance would make the result
    unreproducible. Capture stdout to
    `.planning/phases/15-transport-foundation/transcripts-raw/08-oq1-multistep.ndjson` and
    stderr alongside it.

    Step 4 — the A3 sub-probe, near-zero marginal cost. A second short run in the same
    scratch directory holding stdin open: message 1 asks for a long text-only answer,
    message 2 (written mid-turn) instructs a tool use (read a file in the scratch dir and
    report one value), then close stdin. Capture stdout to `09-oq1-tooluse-two-turns.ndjson`.
    This settles RESEARCH Assumption A3 — whether the per-turn `system/init` + `result`
    behaviour holds when the queued turn uses tools, not just for text-only turns.

    Step 5 — write `15-SPIKE-OQ1.md`. It opens with a line of exactly the form
    `**Verdict:** PASS` or `**Verdict:** FAIL`, and then records: exit code and wall clock;
    `duration_ms` versus `duration_api_ms` from the last `result`; the count of each
    `type`/`subtype` pair observed; whether any `system/hook_started` event appeared (the
    causal tell for the hang); the last `result` envelope verbatim; whether the scratch
    project's two task files were actually created (proof the subagent wave did real work);
    whether the run crossed the background-subagent ceiling; and the A3 finding — how many
    `system/init` and how many `result` envelopes the tool-using two-message run emitted.

    PASS requires ALL of: the process exited on its own with a code other than 124; the last
    `result`'s `duration_ms` is NOT pinned to the wall-clock bound; no `terminal_reason` of
    `aborted_tools` caused by silence; and the scratch project's task files exist on disk.

    FAILURE BRANCH (D-27) — if any PASS condition is unmet: still write
    `15-SPIKE-OQ1.md` with `**Verdict:** FAIL` and the full evidence above, plus a
    `## What this means` section stating that the milestone's premise — that `claude -p`
    reliably runs a multi-step GSD skill headlessly — is not established. Then STOP. Do NOT
    start Task 2. Do NOT attempt a workaround, a longer timeout, a different flag set, or a
    narrower probe. Return to the orchestrator reporting OQ1 FAILED and that the milestone
    premise needs revisiting with the user. A failed OQ1 is a legitimate, reportable outcome.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-transport-foundation/15-SPIKE-OQ1.md &amp;&amp; grep -qE '^\*\*Verdict:\*\* PASS$' .planning/phases/15-transport-foundation/15-SPIKE-OQ1.md &amp;&amp; test -s .planning/phases/15-transport-foundation/transcripts-raw/08-oq1-multistep.ndjson &amp;&amp; test -s .planning/phases/15-transport-foundation/transcripts-raw/09-oq1-tooluse-two-turns.ndjson</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-transport-foundation/15-SPIKE-OQ1.md` exists and its first verdict line matches `^\*\*Verdict:\*\* (PASS|FAIL)$` exactly (D-27)
    - The report names the scratch directory path and states in one line that it is neither this repository nor any registered project (D-28)
    - The report records the observed exit code, the wall clock, and `duration_ms` vs `duration_api_ms` from the last `result`
    - The report records whether any `system/hook_started` event was observed before the first `system/init`
    - The report records the A3 finding as a count: how many `system/init` and how many `result` envelopes the tool-using two-message run emitted
    - `transcripts-raw/08-oq1-multistep.ndjson` and `transcripts-raw/09-oq1-tooluse-two-turns.ndjson` are both non-empty
    - On a FAIL verdict, Task 2 was not started and the plan returned a blocked result naming OQ1 (D-27)
  </acceptance_criteria>
  <done>`15-SPIKE-OQ1.md` carries a PASS verdict backed by the evidence listed above, or the plan has halted with a FAIL verdict recorded and reported.</done>
</task>

<task type="auto">
  <name>Task 2: Toolchain floor and transport dependencies (D-03, D-20)</name>
  <precondition>`.planning/phases/15-transport-foundation/15-SPIKE-OQ1.md` exists and its verdict line reads `**Verdict:** PASS` (D-27)</precondition>
  <reversibility rating="costly">Declaring an MSRV on a crate published to crates.io narrows
  the toolchain every downstream consumer must have; undoing it means dropping process-wrap
  and hand-rolling process-group signalling and reaping.</reversibility>
  <files>Cargo.toml, Cargo.lock, README.md, CLAUDE.md, CONTRIBUTING.md, docs/DEVELOPMENT.md, docs/GETTING-STARTED.md, docs/TESTING.md</files>
  <read_first>
    - `Cargo.toml` — the whole file; note there is no `rust-version` key today and the feature-flag style already in use
    - `.planning/phases/15-transport-foundation/15-RESEARCH.md` — `## Standard Stack` (exact version strings and feature lists), `### D-05 resolution` (why no line-framing crate is added), `## Package Legitimacy Audit`, and `## process-wrap 9.1.0 — Verified API Surface` `### Manifest facts`
    - `.planning/phases/15-transport-foundation/15-PATTERNS.md` — `### Cargo.toml` and `### MSRV documentation surface (D-20)` (the verified eight sites plus the one that must not change)
    - `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `docs/DEVELOPMENT.md`, `docs/GETTING-STARTED.md`, `docs/TESTING.md`
  </read_first>
  <action>
    Raise the toolchain floor and add the two transport dependencies.

    Cargo manifest: add `rust-version = "1.87"` to `[package]`, immediately after the
    `edition` key. There is no such key today, so this also makes the floor machine-checkable
    for the first time (D-20). Add to `[dependencies]`:
    `process-wrap = { version = "9.1.0", features = ["tokio1"] }` — the `tokio1` frontend is
    NOT a default feature and the crate is inert without it (D-03) — and
    `uuid = { version = "1.24", features = ["v4", "serde"] }`, used to generate the
    `--session-id` before spawn. Add nothing else: D-05 is resolved in favour of
    `tokio::io::BufReader::lines()` plus an explicit byte-length bound, so the tokio codec /
    cancellation helper crate is deliberately not added; record that one-line reason in a
    comment beside the process-wrap line. Do NOT touch the `[package] version` key — bumping
    it belongs to the release process described in CLAUDE.md, not to this phase.

    Documentation surface (D-20): update all eight project-MSRV statements to state 1.87 —
    `README.md` (two sites), `CLAUDE.md` stack-table row, `CONTRIBUTING.md` prerequisites,
    `docs/DEVELOPMENT.md`, `docs/GETTING-STARTED.md` (the toolchain table row and the
    troubleshooting heading), and `docs/TESTING.md`.

    Two of those sites also state a false reason and must have their attribution corrected,
    not just their number: `docs/DEVELOPMENT.md` attributes the floor to the crate edition,
    and `docs/GETTING-STARTED.md`'s table row attributes it to the file-watcher dependency.
    The real cause is process-wrap 9.1.0, whose own published manifest declares
    `rust-version = "1.87.0"`. State that cause in both places.

    Leave the citation line near the bottom of `CLAUDE.md` — the bulleted notify-rs source
    reference — completely untouched. It reports the *file-watcher crate's* own minimum
    toolchain, not this project's, and changing it would fabricate a fact about a third-party
    crate.

    Finally run `cargo build` so `Cargo.lock` picks up process-wrap, uuid and their
    transitive dependencies, and commit the lockfile.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; grep -q 'rust-version = "1.87"' Cargo.toml &amp;&amp; grep -q 'process-wrap' Cargo.toml &amp;&amp; grep -q 'features = \["tokio1"\]' Cargo.toml &amp;&amp; grep -q 'uuid' Cargo.toml &amp;&amp; ! grep -q 'tokio-util' Cargo.toml &amp;&amp; ! grep -rq '1\.85' README.md CONTRIBUTING.md docs/ &amp;&amp; [ "$(grep -c '1\.85' CLAUDE.md)" = "1" ] &amp;&amp; grep -q 'process-wrap' Cargo.lock</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `rust-version = "1.87"` inside `[package]` (D-20)
    - `Cargo.toml` contains a `process-wrap` entry with `version = "9.1.0"` and `features = ["tokio1"]` (D-03)
    - `Cargo.toml` contains a `uuid` entry with both `v4` and `serde` features
    - `grep -q 'tokio-util' Cargo.toml` finds nothing — D-05 resolved against adding it
    - `Cargo.toml`'s `[package] version` value is byte-identical to its pre-task value
    - `grep -rq '1\.85' README.md CONTRIBUTING.md docs/` finds nothing
    - `grep -c '1\.85' CLAUDE.md` equals exactly 1 — the third-party file-watcher citation survives (D-20)
    - `docs/DEVELOPMENT.md` and `docs/GETTING-STARTED.md` both name process-wrap as the cause of the floor
    - `cargo build` exits 0 and `Cargo.lock` contains a `process-wrap` package entry
  </acceptance_criteria>
  <done>The crate declares a 1.87 floor, resolves process-wrap and uuid, builds clean, and every project-MSRV doc site states the new floor with the correct cause.</done>
</task>

<task type="auto">
  <name>Task 3: Redact and promote the captured transcripts as golden fixtures (D-24, D-25)</name>
  <precondition>`.planning/phases/15-transport-foundation/transcripts-raw/` contains the seven staged research transcripts plus the two written by Task 1</precondition>
  <reversibility rating="costly">These fixtures become the assertion surface for the whole
  stream-json layer; changing their shape later means rewriting every parser and outcome test
  that reads them.</reversibility>
  <files>tests/fixtures/transcripts/01-success-textonly.ndjson, tests/fixtures/transcripts/02-budget-exhausted.ndjson, tests/fixtures/transcripts/03-tooluse-success-settingsources.ndjson, tests/fixtures/transcripts/04-hookhang-aborted-tools.ndjson, tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson, tests/fixtures/transcripts/06-interrupt-aborted-streaming.ndjson, tests/fixtures/transcripts/07-interrupt-early.ndjson, tests/fixtures/transcripts/08-tooluse-queued-two-turns.ndjson, tests/fixtures/transcripts/README.md</files>
  <read_first>
    - `.planning/phases/15-transport-foundation/15-RESEARCH.md` — `## Captured Transcripts (golden fixture candidates)` (the per-file table, the redaction list, and the credential-scan result) and `### Pitfall G`
    - `.planning/phases/15-transport-foundation/15-CONTEXT.md` — D-24 (unit-testable without spawning claude) and D-25 (manual redaction at capture time)
    - `.planning/phases/15-transport-foundation/transcripts-raw/01-success-textonly.ndjson` — read one file to see the field layout before writing the transform
  </read_first>
  <action>
    Promote the staged transcripts into committed golden fixtures, redacted (D-25) so they are
    both non-disclosing and host-independent.

    Create `tests/fixtures/transcripts/` — the repository has no `tests/fixtures/` today, so
    this is greenfield. Copy the seven research transcripts under their existing names, plus
    Task 1's tool-using two-message capture as `08-tooluse-queued-two-turns.ndjson`. Do NOT
    promote Task 1's large multi-step run; its value is the spike verdict, and
    `15-SPIKE-OQ1.md` already carries the distilled evidence.

    Apply four transforms to every promoted file, preserving the line count and every
    envelope and field exactly:

    1. Path normalisation. The `cwd` field and `memory_paths.auto` carry the operator's
       absolute home-directory prefix and the session scratchpad prefix. Replace the home
       prefix with `/home/testuser` and the scratchpad prefix with `/tmp/scratch`. Sweep the
       whole file, not just those two fields — the same prefixes appear inside tool-result
       payloads.
    2. UUID normalisation. Rewrite every `session_id` in file NN to
       `00000000-0000-4000-8000-0000000000NN`, and every per-message `uuid` to a
       zero-padded sequential value of the form `11111111-1111-4111-8111-%012d` counting
       from 1 within that file. Tests can then assert on identity across turns — fixture 05
       must show the same `session_id` on both `result` envelopes.
    3. Prose trimming. Any `assistant` message text body longer than 200 characters becomes
       the literal marker `[trimmed for fixture]`. The fixtures exist to exercise envelope
       handling, not model prose; this takes fixtures 05 and 06 from tens of kilobytes to
       something diff-reviewable. Never trim a `result` envelope's own `result` field —
       those values are short and are asserted on.
    4. Credential re-scan. Re-run the credential-shape scan RESEARCH.md documents across all
       eight files. The researcher found none; confirm that independently rather than
       trusting the note, because this is the last moment before the content is permanent in
       git history (Pitfall G).

    Write `tests/fixtures/transcripts/README.md` recording, per fixture: the source probe,
    the observed process exit code, the line count, the protocol facts it pins down, and a
    one-line statement that it was redacted per D-25 and what was changed. This file is how a
    future reader knows a fixture is a real capture and not a hand-written guess.

    Leave `.planning/phases/15-transport-foundation/transcripts-raw/` and its gitignore entry
    in place. Retiring the staging directory is deliberately deferred to plan 15-02, which
    deletes it only after the parser tests prove every promoted fixture is readable — making
    the deletion a verified step rather than a blind one.
  </action>
  <verify>
    <automated>[ "$(ls tests/fixtures/transcripts/*.ndjson | wc -l)" = "8" ] &amp;&amp; for f in tests/fixtures/transcripts/*.ndjson; do jq -e -s 'length > 0' "$f" > /dev/null || exit 1; done &amp;&amp; ! grep -rq '/home/blk' tests/fixtures/transcripts/ &amp;&amp; ! grep -rq '/tmp/claude-1000' tests/fixtures/transcripts/ &amp;&amp; ! grep -rEq 'sk-[A-Za-z0-9]{8}|ghp_|Bearer |ANTHROPIC_API_KEY|oauth_token' tests/fixtures/transcripts/ &amp;&amp; [ "$(grep -c '"type":"result"' tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson)" = "2" ] &amp;&amp; test -f tests/fixtures/transcripts/README.md &amp;&amp; test -d .planning/phases/15-transport-foundation/transcripts-raw</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/transcripts/*.ndjson | wc -l` equals 8 (D-24)
    - Every fixture file parses line-by-line as JSON under `jq -e -s`
    - Grepping the fixture directory for the operator home prefix or the session scratchpad prefix returns nothing (D-25)
    - Grepping the fixture directory for credential-shaped strings returns nothing (D-25)
    - `grep -c '"type":"result"' tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson` equals 2 — the multi-turn fact survived redaction (D-29)
    - `grep -c '"subtype":"init"' tests/fixtures/transcripts/05-queued-injection-two-turns.ndjson` equals 2 (D-30)
    - Fixture 02 still omits the `result` field entirely on its error envelope (D-32)
    - `tests/fixtures/transcripts/README.md` has one row per fixture naming source probe, exit code, and what it pins down
    - `.planning/phases/15-transport-foundation/transcripts-raw/` still exists — retirement is 15-02's verified step
  </acceptance_criteria>
  <done>Eight redacted, host-independent, JSON-parseable golden transcripts are committed with a provenance README, and the staging directory is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike → `claude` subprocess | This is the first time the tool executes an external agent; the child inherits environment and filesystem authority from this process |
| captured transcript → git history | Content crosses from an ephemeral session artifact into a permanent, publicly published repository |
| crates.io → build | Two new dependencies enter the build graph of a published crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information disclosure | `tests/fixtures/transcripts/*.ndjson` | high | mitigate | D-25 redaction of both absolute-path prefixes and normalisation of every UUID, plus an independently re-run credential-shape scan, all asserted in Task 3's `<verify>` before the files are committed. Pitfall G: a log written before a redaction retrofit stays unredacted forever, and git history is forever. |
| T-15-02 | Elevation of privilege | the OQ1 spike's `claude` invocation | high | mitigate | D-28 fences enforced in Task 1's action: disposable scratch project outside this repo and outside the registered set; `--permission-mode dontAsk` denies anything outside the read-only set; `--strict-mcp-config` prevents a project-local MCP config introducing tools; permission-bypass flags never used. |
| T-15-03 | Denial of service | the spike hanging unbounded | medium | mitigate | Every spike invocation runs under an external `timeout -s TERM` bound; exit 124 is itself one of the recorded FAIL signals rather than an accident. |
| T-15-04 | Information disclosure | inherited `CLAUDE_*` variables leaking into the spike child | medium | mitigate | Task 1 scrubs every inherited `CLAUDE_*` variable except the background-wait ceiling it sets deliberately (D-14), so the spike measures the real thing and carries nothing from the parent session. |
| T-15-05 | Repudiation | the spike verdict being restated loosely later | low | accept | The verdict is a planning artifact with a machine-readable line and a git commit; that is a sufficient trail for a spike report. |
| T-15-SC | Tampering | cargo dependency installs | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` verified `process-wrap` 9.1.0 and `uuid` 1.24.0 against the live crates.io API, with the process-wrap tarball downloaded and read; both are `OK`/Approved with no `[ASSUMED]`, `[SUS]` or `[SLOP]` rows, so no blocking legitimacy checkpoint fires. Exact versions are pinned in `Cargo.toml` and `Cargo.lock` is committed. Neither crate runs network access from a build script. |
</threat_model>

## Artifacts this phase produces

Produced by this plan:

- **New file paths:** `.planning/phases/15-transport-foundation/15-SPIKE-OQ1.md`,
  `tests/fixtures/transcripts/` (eight `*.ndjson` files plus `README.md`)
- **New Cargo keys:** `[package] rust-version = "1.87"`;
  `[dependencies] process-wrap = { version = "9.1.0", features = ["tokio1"] }`;
  `[dependencies] uuid = { version = "1.24", features = ["v4", "serde"] }`
- **No new Rust symbols** — this plan is the gate plus the toolchain and fixture foundation.

Produced by the phase as a whole (for orientation; each is owned by the plan named):

- `src/executor/mod.rs` (15-02): `Executor` trait, `ExecutionOptions`, `ExecutionHandle`,
  `ExecutionEvent`, `ExecutionId`, `ExecutionTarget::Host`, `DrivableProject`, `RunOutcome`,
  `TurnOutcome`, `RunState`
- `src/executor/stream_json.rs` (15-02): `StreamMessage`, `SystemMessage`, `InitMessage`,
  `TurnMessage`, `ResultMessage`, `ControlResponse`, `ControlResponseBody`, `UserMessage`,
  `Envelope`, `parse_line`
- `src/executor/claude.rs` (15-02, 15-03, 15-04): `ClaudeExecutor`, `MAX_LINE_BYTES`,
  `build_argv`, `send`, `interrupt`, the supervisor, the teardown
- `src/executor/gate.rs` (15-02, 15-03): the capability, version-floor and auth-source gate
- `src/executor/outcome.rs` (15-02, 15-05): `RunSnapshot`, `DiskDelta`, `derive_run_outcome`
- `src/error.rs` (15-02, 15-03): `SpawnError`, `SendError`, `CapabilityError`
- `src/state_reader/git_ops.rs` (15-05): `head_sha`, `is_dirty`
- `src/main_loop.rs` (15-06): `pump`, `PumpOutcome`, `EXEC_BATCH`
- `src/ui/screens/mod.rs` (15-06): `AppContext.exec_tx`, `AppContext.run_states`
- `src/app.rs` (15-06): `App::apply_exec_event`, `App::new_for_test`
- Test surfaces: `tests/executor_transport.rs`, `tests/executor_lifecycle.rs`,
  `tests/fixtures/fake-claude.sh` and its `slow` / `spawner` / `deaf` / `echo` variants
- **No new CLI flags.** `Commands::Drive` is Phase 17's.

<verification>
- `cargo build && cargo test && cargo clippy -- -D warnings` passes (the CLAUDE.md project gate)
- `cargo clippy --all-targets -- -D warnings` still reports exactly 5 lints — the frozen
  pre-existing count from `browser.rs` (×3), `project_creator.rs` (×1) and
  `state_reader/mod.rs` (×1). It must not grow.
- `15-SPIKE-OQ1.md` carries `**Verdict:** PASS`
</verification>

<success_criteria>
- The OQ1 multi-step spike has run against a disposable scratch GSD project and returned a
  recorded verdict; a FAIL has halted the phase rather than being worked around
- The crate declares `rust-version = "1.87"`, resolves process-wrap and uuid, and builds clean
- Eight redacted golden transcripts are committed and every line parses as JSON
- The pre-existing `--all-targets` clippy lint count is still exactly 5
</success_criteria>

<output>
Create `.planning/phases/15-transport-foundation/15-01-SUMMARY.md` when done. Record the OQ1
verdict verbatim in the summary — the orchestrator surfaces it upward, and plans 15-02
through 15-06 each assert it as a precondition.
</output>
