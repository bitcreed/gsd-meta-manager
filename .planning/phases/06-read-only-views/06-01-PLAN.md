---
phase: 06-read-only-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.rs
  - src/action.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/detail.rs
  - src/state_reader/mod.rs
  - src/state_reader/backlog.rs
  - src/state_reader/git_ops.rs
autonomous: true
requirements: [BLOG-01, GIT-01]

must_haves:
  truths:
    - "Detail view shows a tab bar with 4 tabs: Phases, Roadmap, Backlog, Git"
    - "User can switch tabs with number keys 1-4 and left/right arrows"
    - "Tab selection persists per project"
    - "BacklogItem and GitLogEntry data structs exist for downstream use"
    - "New Action variants exist for async data loading"
  artifacts:
    - path: "src/state_reader/backlog.rs"
      provides: "BacklogItem struct and parse_backlog_items function"
      exports: ["BacklogItem", "parse_backlog_items"]
    - path: "src/state_reader/git_ops.rs"
      provides: "GitLogEntry, GitDiffStat structs and async git functions"
      exports: ["GitLogEntry", "GitDiffStat", "load_git_log", "load_diff_stat"]
    - path: "src/app.rs"
      provides: "Extended DetailSubView enum with Backlog and GitHistory variants"
      contains: "DetailSubView"
    - path: "src/action.rs"
      provides: "Action variants for BacklogLoaded, GitLogLoaded, etc."
      contains: "BacklogLoaded"
  key_links:
    - from: "src/ui/screens/detail.rs"
      to: "src/app.rs"
      via: "DetailSubView enum matching"
      pattern: "DetailSubView::Backlog"
    - from: "src/ui/screens/detail.rs"
      to: "ratatui::widgets::Tabs"
      via: "Tab bar widget rendering"
      pattern: "Tabs::new"
---

<objective>
Create the tab system foundation and data models for the backlog browser and git history viewer.

Purpose: Both new views (backlog, git) need shared infrastructure: an extended DetailSubView enum, a tab bar header, tab navigation keybindings, new Action variants for async data, and data structs. Building this first prevents both Plan 02 and 03 from duplicating the same foundation work.

Output: Tab bar visible in detail view with 4 tabs, navigation working, data models and Action variants defined (but tab content for Backlog/Git is placeholder "Loading..." text until Plans 02/03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-read-only-views/06-CONTEXT.md
@.planning/phases/06-read-only-views/06-RESEARCH.md

@src/app.rs
@src/action.rs
@src/ui/screens/mod.rs
@src/ui/screens/detail.rs
@src/state_reader/mod.rs

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app.rs:
```rust
#[derive(Debug, Clone, PartialEq, Default)]
pub enum DetailSubView {
    #[default]
    PhaseList,
    RoadmapViz,
}
```

From src/action.rs:
```rust
#[derive(Debug, Clone)]
pub enum Action {
    Tick,
    RawKey(KeyEvent),
    Resize,
    FileChanged { project_path: std::path::PathBuf },
    CreateProjectResult { alias: String, path: std::path::PathBuf, success: bool, error: Option<String> },
    ProjectStateLoaded { alias: String, state: ProjectState },
    Noop,
}
```

From src/ui/screens/mod.rs:
```rust
pub trait Screen {
    fn handle_key(&mut self, code: KeyCode, modifiers: KeyModifiers, ctx: &mut AppContext) -> ScreenAction;
    fn render(&self, frame: &mut Frame, area: Rect, ctx: &AppContext);
    fn name(&self) -> &str;
}

pub struct AppContext {
    pub detail_sub_view_per_project: HashMap<String, DetailSubView>,
    // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data models, Action variants, and ProjectViewCache</name>
  <files>
    src/state_reader/backlog.rs,
    src/state_reader/git_ops.rs,
    src/state_reader/mod.rs,
    src/action.rs,
    src/app.rs,
    src/ui/screens/mod.rs
  </files>
  <read_first>
    src/state_reader/mod.rs,
    src/action.rs,
    src/app.rs,
    src/ui/screens/mod.rs
  </read_first>
  <action>
1. Create `src/state_reader/backlog.rs` with:
   - `pub struct BacklogItem { pub dir_name: String, pub number: String, pub slug: String, pub description: String, pub content: Option<String> }` -- derive Debug, Clone
   - `pub fn parse_backlog_dir_name(name: &str) -> Option<(String, String)>` -- skip entries containing `{` or `\n`; parse "999.N-slug" format returning (number, slug)
   - `pub fn parse_backlog_items(planning_dir: &Path) -> Vec<BacklogItem>` -- read `.planning/phases/` for 999* dirs, call parse_backlog_dir_name, for description read first `.md` file's first `# heading`, fallback to humanized slug (replace `-` with spaces). Sort by number ascending. Do NOT load content field here (None).
   - `pub fn load_backlog_content(planning_dir: &Path, dir_name: &str) -> Option<String>` -- read first `.md` file in the 999.N-* directory, return full content string

2. Create `src/state_reader/git_ops.rs` with:
   - `pub struct GitLogEntry { pub hash: String, pub date: String, pub author: String, pub message: String }` -- derive Debug, Clone
   - `pub struct GitDiffStat { pub files_changed: u32, pub insertions: u32, pub deletions: u32, pub file_stats: Vec<String> }` -- derive Debug, Clone, Default
   - `pub async fn load_git_log(project_path: &Path, planning_only: bool, limit: usize) -> anyhow::Result<Vec<GitLogEntry>>` -- use `tokio::process::Command` with `git log --max-count={limit} --format=%h\x1f%ad\x1f%an\x1f%s --date=short`, add `-- .planning/` if planning_only. Parse `\x1f`-delimited fields. Return empty vec on non-success exit code.
   - `pub async fn load_diff_stat(project_path: &Path, hash: &str) -> anyhow::Result<GitDiffStat>` -- use `tokio::process::Command` with `git diff-tree --stat --no-commit-id -r {hash}`. Parse last line for summary counts (regex or manual split on "files? changed", "insertions", "deletions"). All other lines go in file_stats. Return Default on error.

3. Update `src/state_reader/mod.rs`:
   - Add `pub mod backlog;` and `pub mod git_ops;` module declarations

4. Update `src/action.rs`:
   - Add import: `use crate::state_reader::backlog::BacklogItem;`
   - Add import: `use crate::state_reader::git_ops::{GitLogEntry, GitDiffStat};`
   - Add Action variants:
     ```
     BacklogLoaded { alias: String, items: Vec<BacklogItem> },
     BacklogContentLoaded { alias: String, item_number: String, content: String },
     GitLogLoaded { alias: String, entries: Vec<GitLogEntry>, planning_only: bool },
     GitDiffStatLoaded { alias: String, hash: String, stat: GitDiffStat },
     ```

5. Update `src/app.rs` -- extend `DetailSubView` enum:
   - Add `Backlog` and `GitHistory` variants after `RoadmapViz`
   - In the `handle_key` for `'r'` toggle: remove or keep for backward compat (will be replaced by tab nav in Task 2)

6. Update `src/ui/screens/mod.rs` -- add `ProjectViewCache` struct to `AppContext`:
   - Add `use crate::state_reader::backlog::BacklogItem;`
   - Add `use crate::state_reader::git_ops::{GitLogEntry, GitDiffStat};`
   - Define `pub struct ProjectViewCache`:
     ```rust
     pub struct ProjectViewCache {
         pub backlog_items: Vec<BacklogItem>,
         pub backlog_selected: usize,
         pub backlog_expanded: bool,
         pub git_entries: Vec<GitLogEntry>,
         pub git_selected: usize,
         pub git_planning_only: bool,
         pub git_diff_stat: Option<GitDiffStat>,
         pub loading_backlog: bool,
         pub loading_git: bool,
         pub loading_diff: bool,
     }
     ```
   - Add `impl Default for ProjectViewCache` -- all numeric fields 0, bools false, vecs empty, option None
   - Add field to AppContext: `pub view_cache: HashMap<String, ProjectViewCache>,`
   - Initialize `view_cache: HashMap::new()` in `App::new()` in app.rs

7. Update `src/app.rs` `App::update` to handle new Action variants:
   - `Action::BacklogLoaded { alias, items }` -- get or insert default ProjectViewCache for alias, set `backlog_items = items`, `loading_backlog = false`, set `needs_redraw = true`
   - `Action::BacklogContentLoaded { alias, item_number, content }` -- find the BacklogItem in cache with matching number, set its `content = Some(content)`, set `needs_redraw = true`
   - `Action::GitLogLoaded { alias, entries, planning_only }` -- set cache `git_entries = entries`, `git_planning_only = planning_only`, `loading_git = false`, `needs_redraw = true`
   - `Action::GitDiffStatLoaded { alias, hash, stat }` -- set cache `git_diff_stat = Some(stat)`, `loading_diff = false`, `needs_redraw = true`
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/state_reader/backlog.rs exists and contains `pub struct BacklogItem`
    - src/state_reader/backlog.rs contains `pub fn parse_backlog_items`
    - src/state_reader/backlog.rs contains `pub fn load_backlog_content`
    - src/state_reader/git_ops.rs exists and contains `pub struct GitLogEntry`
    - src/state_reader/git_ops.rs contains `pub async fn load_git_log`
    - src/state_reader/git_ops.rs contains `pub async fn load_diff_stat`
    - src/state_reader/mod.rs contains `pub mod backlog;` and `pub mod git_ops;`
    - src/action.rs contains `BacklogLoaded` and `GitLogLoaded` and `GitDiffStatLoaded` and `BacklogContentLoaded`
    - src/app.rs contains `DetailSubView::Backlog` and `DetailSubView::GitHistory`
    - src/ui/screens/mod.rs contains `pub struct ProjectViewCache`
    - src/ui/screens/mod.rs AppContext contains `pub view_cache`
    - `cargo check` exits with status 0
  </acceptance_criteria>
  <done>All data models defined, Action variants added, ProjectViewCache on AppContext, DetailSubView extended. cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tab bar rendering and tab navigation in DetailScreen</name>
  <files>
    src/ui/screens/detail.rs
  </files>
  <read_first>
    src/ui/screens/detail.rs,
    src/app.rs,
    src/ui/screens/mod.rs
  </read_first>
  <action>
Refactor `src/ui/screens/detail.rs` to use a tab bar system per D-09:

1. Add imports at top:
   - `use ratatui::widgets::Tabs;`
   - `use crate::state_reader::backlog::BacklogItem;`
   - `use crate::state_reader::git_ops::{GitLogEntry, GitDiffStat};`
   - `use crate::action::Action;`

2. Add helper function `fn tab_index(sub_view: &DetailSubView) -> usize`:
   - PhaseList => 0, RoadmapViz => 1, Backlog => 2, GitHistory => 3

3. Add helper function `fn sub_view_from_index(index: usize) -> DetailSubView`:
   - 0 => PhaseList, 1 => RoadmapViz, 2 => Backlog, 3 => GitHistory, _ => PhaseList

4. Replace the `handle_key` method in `impl Screen for DetailScreen`:
   - Remove the `KeyCode::Char('r')` toggle handler
   - Add `KeyCode::Char('1')` through `KeyCode::Char('4')`: set `ctx.detail_sub_view_per_project.insert(self.alias.clone(), sub_view_from_index(N-1))`, reset `self.scroll_offset = 0`
   - Add `KeyCode::Left`: get current tab index, if > 0 go to index-1
   - Add `KeyCode::Right`: get current tab index, if < 3 go to index+1
   - On tab switch to Backlog (index 2): check if `ctx.view_cache.get(&self.alias)` has empty `backlog_items` and `loading_backlog == false`, if so dispatch `Action` to trigger async load via `ScreenAction::DispatchAction`. Use `tokio::task::spawn_blocking` pattern -- but since we're in sync handle_key, set `loading_backlog = true` on the cache entry and return `ScreenAction::DispatchAction(Action::Noop)` as a placeholder. Actually, the proper pattern is: just set the loading flag and return ScreenAction::None. The actual async load dispatch should happen in the render path or in app.rs update. For simplicity: on switching to Backlog tab, if cache is empty, synchronously call `backlog::parse_backlog_items` (it's just filesystem reads, fast enough) and store directly in cache. This matches the existing synchronous state loading pattern.
   - On tab switch to GitHistory (index 3): if `ctx.view_cache.get(&self.alias)` has empty `git_entries` and `loading_git == false`, set `loading_git = true` on cache and return `ScreenAction::DispatchAction(Action::Noop)` -- then in app.rs, when we see Noop... Actually, better approach: dispatch a new custom action. But simplest: use `event_tx` to spawn async. In handle_key: get event_tx from ctx, clone it, get project path, spawn tokio task that calls `load_git_log` and sends `GitLogLoaded` back via tx. Set `loading_git = true` on cache.
   - Keep existing j/k/Up/Down scroll, Esc/q pop, e enqueue, ? help handlers
   - Add `KeyCode::Enter` handler: if on Backlog tab, toggle `backlog_expanded` on the cache; if on GitHistory tab, load diff stat for selected commit (spawn async via event_tx)
   - Add `KeyCode::Char('p')` handler: if on GitHistory tab, toggle `git_planning_only`, clear git_entries, set loading_git = true, spawn reload via event_tx per D-07

5. Modify the `render` method:
   - At top of render, split layout into: tab bar area (Length 3), content area (Min 0), footer (Length 1)
   - Render tab bar using ratatui `Tabs` widget:
     ```rust
     let titles = vec!["1:Phases", "2:Roadmap", "3:Backlog", "4:Git"];
     let tab_idx = tab_index(&sub_view);
     let tabs_widget = Tabs::new(titles)
         .select(tab_idx)
         .highlight_style(Style::default().add_modifier(Modifier::BOLD).fg(Color::Cyan))
         .divider("|");
     let tab_block = Block::default()
         .borders(Borders::BOTTOM)
         .title(format!(" Project: {} ", alias));
     frame.render_widget(tabs_widget.block(tab_block), tab_area);
     ```
   - For PhaseList and RoadmapViz tabs: keep existing rendering logic but render into `content_area` instead of `main_area`
   - For Backlog tab (content_area): render dim "Loading..." if `loading_backlog`, else render placeholder list "Backlog items: N" (full implementation in Plan 02)
   - For GitHistory tab (content_area): render dim "Loading..." if `loading_git`, else if git_planning_only show header "[.planning/ only]" in yellow, render placeholder "Git log: N entries" (full implementation in Plan 03)

6. Update `render_main_only` similarly -- add the tab bar rendering there too (this is used by EnqueueScreen overlay). Keep same tab dispatch logic.

7. Update footer to show tab-appropriate hints:
   - Common: `[Esc]back  [1-4]tabs  [j/k]scroll`
   - Backlog tab add: `[Enter]expand  [e]enqueue`
   - Git tab add: `[p]planning-only  [Enter]diff`
   - Remove `[r]roadmap` hint (replaced by tab system)
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/screens/detail.rs contains `Tabs::new`
    - src/ui/screens/detail.rs contains `"1:Phases"` and `"2:Roadmap"` and `"3:Backlog"` and `"4:Git"`
    - src/ui/screens/detail.rs contains `KeyCode::Char('1')` through `KeyCode::Char('4')` handlers
    - src/ui/screens/detail.rs contains `KeyCode::Left` and `KeyCode::Right` handlers
    - src/ui/screens/detail.rs contains `DetailSubView::Backlog` match arm in render
    - src/ui/screens/detail.rs contains `DetailSubView::GitHistory` match arm in render
    - src/ui/screens/detail.rs does NOT contain the old `KeyCode::Char('r')` toggle between PhaseList/RoadmapViz
    - `cargo check` exits with status 0
  </acceptance_criteria>
  <done>Tab bar renders in detail view with 4 tabs. Number keys 1-4 and left/right arrows switch tabs. Backlog and Git tabs show placeholder content. Old 'r' toggle removed. cargo check passes.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes with no errors
2. `cargo test` passes (existing tests still work)
3. Running the app and entering detail view shows a tab bar with "1:Phases | 2:Roadmap | 3:Backlog | 4:Git"
4. Pressing 1-4 switches between tabs
5. Left/Right arrows cycle tabs
6. Backlog tab shows placeholder or loading state
7. Git tab shows placeholder or loading state
</verification>

<success_criteria>
- Detail view has a visible 4-tab header bar
- Tab navigation works via number keys and arrow keys
- BacklogItem and GitLogEntry structs exist in state_reader
- Action enum has BacklogLoaded, BacklogContentLoaded, GitLogLoaded, GitDiffStatLoaded variants
- ProjectViewCache exists on AppContext
- All existing tests pass
- cargo check and cargo test both pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-read-only-views/06-01-SUMMARY.md`
</output>
