---
phase: 25-running-agents-live-wave-view
plan: 04
type: execute
wave: 3
depends_on: [25-01, 25-03]
files_modified:
  - src/action.rs
  - src/app.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/normal.rs
  - src/ui/screens/detail.rs
  - src/ui/screens/delete_confirm.rs
  - src/ui/screens/driver_confirm.rs
autonomous: true
requirements: [AGENT-05]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The agents scan rides the existing ~5 s session-poll counter in `Action::Tick` — it gets no timer of its own — runs in `tokio::task::spawn_blocking`, and returns its whole result as `Action::AgentsScanned`; no `notify` watcher is added on `~/.claude` or on worktree directories (D-C01, D-B04)."
    - "`AppContext.agents_scan_in_flight` is set before the spawn and cleared by the `AgentsScanned` handler; while it is set no second scan is spawned, and the blocking closure always sends, even an empty result after a panic, so the flag can never stick (D-C01, concurrency edge, RESEARCH Pitfall 6)."
    - "The handler derives each project's `AgentView` with `waves::derive` against the already-parsed `ProjectState` (never at render time), keeps only registered aliases with at least one agent row or live worktree-less agent, and replaces `AppContext.agent_views` wholesale, requesting a redraw only when the map changed; agent state never lands on `ProjectState` (D-C01, D-B04)."
    - "While a project has an active agent, its dashboard Status cell shows the widest `summary_forms()` entry whose `Line::width()` fits `status_column_cells(terminal_width)` — `w2/11 13run` at 80 and 120 columns for the 13-executor wave — without a new column, without a badge change, and never clipped mid-segment (D-C14)."
    - "`status_column_cells(w)` re-runs the dashboard `Table`'s own layout over `dashboard_columns(w)` and equals the rendered Status column width at 50, 60, 80, 100, 120, 160 and 200 columns; nothing hard-codes 13 (D-C14)."
    - "A form exactly as wide as the Status column is chosen; one cell narrower and the next narrower form is chosen (boundary edge); widths are display cells from `Line::width`, never `str::len` (precision edge)."
    - "A project with no active agent renders a dashboard row byte-identical to today's; an all-stalled project shows the `N stalled` form (D-C14)."
    - "Log lines about the scan carry counts only, never an agent description or type (D-C13)."
  artifacts:
    - path: "src/action.rs"
      provides: "Action::AgentsScanned"
      contains: "AgentsScanned"
    - path: "src/app.rs"
      provides: "tick wiring with the in-flight gate, the AgentsScanned handler, AppContext init"
      contains: "Action::AgentsScanned"
    - path: "src/ui/screens/mod.rs"
      provides: "AppContext.agent_views and agents_scan_in_flight with sibling-map docs"
      contains: "pub agent_views: HashMap<String, crate::agents::waves::AgentView>"
    - path: "src/ui/screens/normal.rs"
      provides: "status_column_cells, agent_summary_line, the Status-cell wrap"
      contains: "fn status_column_cells"
  key_links:
    - from: "src/app.rs Action::Tick (session_poll_counter >= 20)"
      to: "crate::agents::scan_projects_guarded"
      via: "spawn_blocking gated by agents_scan_in_flight"
      pattern: "scan_projects_guarded"
    - from: "src/app.rs Action::AgentsScanned handler"
      to: "crate::agents::waves::derive"
      via: "per registered alias, against ctx.project_states"
      pattern: "waves::derive"
    - from: "src/ui/screens/normal.rs status cell"
      to: "AgentView::summary_forms"
      via: "agent_summary_line(view, status_column_cells(terminal_width))"
      pattern: "agent_summary_line"
  prohibitions:
    - "MUST NOT change a dashboard row when its project has no active agent — the summary appears only while agents run (D-C14)."
    - "MUST NOT write agent descriptions, agent types or branch names into log output — they are agent-authored text; logs carry counts only (D-C13)."
---

<objective>
Wire the scan into the app and put its summary on the dashboard (AGENT-05): the scan rides the
existing 5 s poll counter (D-C01), results land in a wholesale-replaced sibling map on `AppContext`,
the wave model is derived in the handler, and the Status cell shows the widest summary form that fits
its measured width (D-C14).

Purpose: this is the first user-visible slice of the phase. After this plan, a user with a live GSD
wave sees `w2/11 13run` on that project's dashboard row without opening anything.

Output: `Action::AgentsScanned`, the tick wiring and handler in `src/app.rs`, two `AppContext` fields
(added at all six `AppContext { .. }` literals), and the Status-cell ladder in `src/ui/screens/normal.rs`.

Sequencing: wave 3 because the handler needs 25-03's `AgentView`/`derive` (semantic dependency) and
25-01's scan entry point; 25-05 and 25-06 follow because they share `app.rs`, `ui/screens/mod.rs`,
`detail.rs` and `normal.rs` with this plan and need `agent_views`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-running-agents-live-wave-view/25-CONTEXT.md
@.planning/phases/25-running-agents-live-wave-view/25-RESEARCH.md
@.planning/phases/25-running-agents-live-wave-view/25-PATTERNS.md
@.planning/phases/25-running-agents-live-wave-view/25-01-SUMMARY.md
@.planning/phases/25-running-agents-live-wave-view/25-03-SUMMARY.md

<interfaces>
From 25-01: `crate::agents::{scan_projects_guarded, ProjectAgents, AgentRow, AgentLiveness}` — `scan_projects_guarded(&[(String, PathBuf)], SystemTime) -> Vec<(String, ProjectAgents)>`, sorted by alias.
From 25-03: `crate::agents::waves::{derive, AgentView, PlanRef}` — `derive(&ProjectAgents, &ProjectState) -> AgentView`; `AgentView::summary_forms() -> Vec<String>` (widest first; empty = leave the cell alone); every field `pub`, `Default` derived.

Created here:
- `Action::AgentsScanned { per_project: Vec<(String, crate::agents::ProjectAgents)> }`
- `AppContext.agent_views: HashMap<String, crate::agents::waves::AgentView>`, `AppContext.agents_scan_in_flight: bool`
- `fn status_column_cells(terminal_width: u16) -> u16` and `fn agent_summary_line(view: &AgentView, cells: u16) -> Option<Line<'static>>` in `normal.rs` (module-private, `pub(crate)` if 25-05 needs it).
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 25 plan; newly created names are not drift).

**New files:** `src/agents/mod.rs`, `src/agents/worktrees.rs`, `src/agents/adapters/mod.rs`, `tests/agents_scan.rs` (25-01); `src/agents/adapters/claude.rs`, `tests/agents_claude.rs` (25-02); `src/agents/waves.rs`, `tests/agents_waves.rs` (25-03); `src/agents/fixers.rs`, `tests/agents_fixers.rs` (25-06).
**Core (25-01):** `AgentLiveness`, `LIVE_SECS`, `IDLE_SECS`, `MAX_AGENT_AGE_SECS`, `classify_liveness`, `AgentRow`, `ProjectAgents`, `scan_project_with`, `scan_projects_guarded`; `RawWorktree`, `CoreWorktree`, `CoreScan`, `BranchPlan`, `parse_porcelain_z`, `parse_porcelain_lines`, `is_agent_worktree`, `parse_agent_branch`, `valid_agent_id`, `scan_worktrees`; `AgentAdapter`, `CoreSnapshot`, `Enrichment`, `ChildAgent`, `AdapterReport`, `registered_adapters`; git helpers `worktree_list_porcelain`, `PorcelainListing`, `commits_ahead`, `dirty_count`, and `GIT_OPTIONAL_LOCKS=0` inside `git_read_raw`.
**Claude adapter (25-02):** `ClaudeCodeAdapter::{new, from_env}`, `resolve_config_root`, `encode_project_dir`, tolerant `Meta`, `read_meta_capped`, `META_READ_CAP`.
**Wave model (25-03):** `PlanRef`, `PlanState`, `WaveRow`, `AgentView`, `attribute`, `commit_scope_plan`, `derive`, `AgentView::{summary_forms, is_active}`; `AgentRow.{plan, summary_in_worktree}`; `DiskInference.summarized_plans`; `git_ops::log_subjects`; `disk_status::{plan_index, leading_frontmatter_nested_value}` made `pub(crate)`.
**App and dashboard (25-04):** `Action::AgentsScanned`, `AppContext.{agent_views, agents_scan_in_flight}`, `status_column_cells`, `agent_summary_line`.
**Detail view (25-05):** `DetailSubView::Agents`, `ProjectViewCache.agents_selected`, `sessions_sub_tab_strip`, `sessions_sub_tab_row`, `render_agents_tab`; key `m` on Sessions/Agents.
**Fixer estimate (25-06):** `FixerEstimate`, `fixers::estimate`, `fixers::finding_ids`, `ProjectAgents.fixer_estimate`, `AgentView.fixers`.
**Requirements (25-01):** AGENT-01..AGENT-07 in REQUIREMENTS.md and the ROADMAP Phase 25 entry.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Tick → scan → AgentsScanned → derive → dashboard Status cell, end to end</name>
  <files>src/action.rs, src/app.rs, src/ui/screens/mod.rs, src/ui/screens/normal.rs, src/ui/screens/detail.rs, src/ui/screens/delete_confirm.rs, src/ui/screens/driver_confirm.rs</files>
  <read_first>
    - `src/action.rs` — `Action::RunsReconciled` (~138-170) and its sizing note (~110-126): the doc structure to copy.
    - `src/app.rs` — the `Action::Tick` 20-tick block (~1129-1171) and its "rides THIS counter" comments; the `RunsReconciled` handler (~1615-1647); the `AppContext` literal in `App::from_config` (~569-615); test helpers `obs_app` (~2635), `reconciled` (~2658), `render_top_screen` (~4890), and `OBS_ALIAS`.
    - `src/ui/screens/mod.rs` — `AppContext` (~1263) and the `observed_runs` sibling-map doc (~1333-1355), plus the "not pruned reintroduces the Phase 16 leak" warning (~1406-1410); `ctx_with_aliases` (~2347-2390).
    - `src/ui/screens/normal.rs` — `STATUS_COLUMN_MIN_CELLS` and `dashboard_columns` (~310-370), `dashboard_table` (~377-388), the row build including `terminal_width` (~743) and the status cell (~823-853), `ctx_with_aliases` (~1196-1240).
    - The four other `AppContext { .. }` literals: `src/ui/screens/detail.rs` `test_ctx` (~11122), `src/ui/screens/delete_confirm.rs` (~249), `src/ui/screens/driver_confirm.rs` (~696), `src/ui/screens/mod.rs` (~2364).
    - `src/agents/waves.rs` (25-03) — `AgentView`, `summary_forms`.
    - `.planning/phases/25-running-agents-live-wave-view/25-RESEARCH.md` § System Architecture Diagram, § Pattern 6, § Code Examples "Tick wiring".
  </read_first>
  <action>
**action.rs.** Add `AgentsScanned { per_project: Vec<(String, crate::agents::ProjectAgents)> }`, with a doc copying the `RunsReconciled` structure:
- the payload is the WHOLE scan, not a delta, so the handler replaces rather than merges;
- every field is plain data (paths, counts, strings, times), never a handle, so `Action` stays `Clone`;
- a `Vec` is 24 bytes, so no `Box` is needed.

**ui/screens/mod.rs.** Add `pub agent_views: HashMap<String, crate::agents::waves::AgentView>` and `pub agents_scan_in_flight: bool` to `AppContext`. The first gets a doc in the `observed_runs` bullet style:
- a sibling map, not on `ProjectState`, because that type's `PartialEq` suppresses "Updated: {alias}" and agent state moves every scan;
- ids, counts, strings and times only;
- replaced wholesale by every scan over the registered projects, which is why it needs no prune and cannot reintroduce the Phase 16 leak;
- a project with no agent rows has no entry.

The flag's doc says it is set before the spawn and cleared by the handler (RESEARCH Pitfall 6). Add both fields, `HashMap::new()` and `false`, to all six `AppContext { .. }` literals: `app.rs`, `ui/screens/mod.rs`, `normal.rs`, `detail.rs`, `delete_confirm.rs`, `driver_confirm.rs`.

**app.rs tick (D-C01, D-B04).** Inside `if self.session_poll_counter >= 20`, after the reconcile task, add a block with a comment in the existing voice: this scan rides THIS counter and must never get a timer of its own, and it adds no `notify` watcher because polling already bounds staleness at about 5 s (D-C01). When `!self.ctx.agents_scan_in_flight` and `event_tx` is `Some`:
1. Set the flag.
2. Collect `(alias, project.path)` pairs from `self.ctx.config.projects`.
3. `spawn_blocking` a closure that takes `SystemTime::now()`, runs `crate::agents::scan_projects_guarded` inside `std::panic::catch_unwind(AssertUnwindSafe(..))`, maps a panic to an empty Vec, and ALWAYS sends `Action::AgentsScanned`.

**app.rs handler.** An `Action::AgentsScanned { per_project }` arm:
1. Clear `agents_scan_in_flight` unconditionally, first.
2. Build a new map: skip aliases not in `self.ctx.config.projects`, and skip entries with no rows and no worktree-less agents. For the rest, call `crate::agents::waves::derive(&agents, state)`, where `state` is the alias's `ProjectState` or `ProjectState::default()`.
3. If the new map differs from `self.ctx.agent_views`, replace it, set `needs_redraw` and `tracing::debug!` the project count and total row count only.

Document that `AgentView` carries the scan instant, so a project with agents redraws once per scan: that is what advances the age column. A fleet with no agents never redraws, because the map stays empty.

**normal.rs (D-C14).** Add `fn status_column_cells(terminal_width: u16) -> u16`. It re-runs the layout `Table` uses — `Layout::horizontal(dashboard_columns(terminal_width).1).flex(Flex::Start).spacing(1)` — over the block's inner width (`terminal_width - 2`) minus the 2-cell `"> "` highlight symbol, and returns the Status column's width, saturating at 0. Doc: single source of truth with `dashboard_columns`, never a literal 13. When no row is selected, ratatui reserves no highlight column, so this under-estimates by 2 and stays safe. Add `fn agent_summary_line(view: &AgentView, cells: u16) -> Option<Line<'static>>`: the first `summary_forms()` entry with `Line::width() <= cells`. Style it cyan; a form ending in `stalled` is yellow. Meaning is carried by the text, never by the colour. Return `None` when there are no forms or none fits. Compute `status_column_cells(terminal_width)` once before building rows. Then wrap the existing status-cell expression unchanged: `ctx.agent_views.get(alias).and_then(|v| agent_summary_line(v, cells))` wins, and otherwise the milestone/pipeline/status expression runs exactly as today. Do not touch `alias_badge` or `row_badge`.

**Tracer test (app.rs, `#[tokio::test]`), `an_agents_scan_reaches_the_dashboard_status_cell`.**
1. Take `obs_app` over a tempdir and insert a `ProjectState::default()` for `OBS_ALIAS` whose `phase_disk_statuses["13"]` is a `DiskInference { plan_waves: [w1: 13-01, w2: 13-02 + 13-03], summarized_plans: ["13-01"], plan_count: 3, summary_count: 1, ..Default::default() }`.
2. Send `Action::AgentsScanned` with one `ProjectAgents` holding one `Live` row whose `plan` is 13-02, then recompute filtered aliases.
3. Assert `render_top_screen(&app)` (120 columns, where the Status column is 13 cells) contains `w2/2 · 1 run`, and that `agent_views` holds the alias.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib app::tests::an_agents_scan_reaches_the_dashboard_status_cell</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed" (a renamed or filtered-away test)</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - `an_agents_scan_reaches_the_dashboard_status_cell` appears as `ok` (1 passed).
    - `rtk proxy git grep -c 'agents_scan_in_flight: false' -- src` reports six occurrences across the six literal sites (the `App::from_config` one plus five test literals).
    - `rtk proxy cargo test --test async_blocking_guard --no-fail-fast` passes.
  </acceptance_criteria>
  <done>A scan result for a live executor turns into a derived wave view and a fitted summary in that project's dashboard Status cell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Status-cell ladder at every width — column measurement, boundary, unchanged rows</name>
  <files>src/ui/screens/normal.rs</files>
  <read_first>
    - `src/ui/screens/normal.rs` as Task 1 left it; its test harness `render_dashboard_interior` / `render_dashboard_rows` / `pipeline_row` (~1911-1990) and `test_dashboard_columns_status_floor_at_upper_tiers` (~1988).
    - `src/agents/waves.rs` — `AgentView` fields and `summary_forms` executor/stalled/generic modes.
    - `.planning/phases/25-running-agents-live-wave-view/25-RESEARCH.md` § Pattern 6 (measured widths 13/13/13/13/13/19/25 at 60/70/80/100/120/160/200) and Pitfall 5.
  </read_first>
  <behavior>
    - A dashboard row whose Status cell holds `"X".repeat(200)` renders exactly `status_column_cells(w)` contiguous `X` cells at widths 50, 60, 80, 100, 120, 160, 200 (with a selected row).
    - With the 13-executor view (`AgentView { active_phase: 13, current_wave: 2, max_wave: 11, plan_total: 35, done: 8, running: 13, agents: 13 Live rows attributed to phase 13, ..Default::default() }`), the rendered Status cell, trimmed, equals the first form whose width fits `status_column_cells(w)` at 60, 80, 120, 160 and 200; at 80 and 120 that is `w2/11 13run`.
    - `agent_summary_line(view, cells)` with `cells == Line::width(form_k)` returns form k; with one cell fewer it returns form k+1.
    - A row for a project whose view has only `Unknown` rows (no forms) renders byte-identically to the same row with no `agent_views` entry; the same holds for a project with no entry at all compared with today's render.
    - An all-`Stalled` view shows `2 stalled`; a milestone-complete project with an active view shows the summary instead of `Complete`.
    - `Line::from("·").width() == 1`.
  </behavior>
  <action>
Write the behaviours as `normal.rs` render tests reusing the existing harness (extend `render_dashboard_interior` with an `AppContext`-driven variant only if the row-list helper cannot carry an `agent_views` entry):
- `status_column_cells_matches_the_rendered_status_column`
- `the_agent_summary_is_a_whole_ladder_form_at_every_width`
- `a_form_exactly_as_wide_as_the_column_is_chosen`
- `a_row_without_active_agents_is_byte_identical`
- `the_stalled_form_replaces_the_status_cell`
- `the_agent_summary_replaces_a_milestone_complete_cell`
- `the_middle_dot_is_one_cell`

Build views as `AgentView { .., ..Default::default() }` literals so fields later plans add do not break these tests. `the_middle_dot_is_one_cell` carries a doc noting that U+00B7 is East-Asian-Ambiguous (measured). That is the same class as the `│` borders ratatui already draws everywhere, and the 13-cell common-case forms contain no `·` [inferred — audit]. Fix whatever the tests expose in `status_column_cells` or `agent_summary_line`; never special-case a width.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::normal</automated>
    <fails_when>non-zero exit, or "0 passed", or any of the seven named tests absent from the `ok` lines</fails_when>
  </verify>
  <acceptance_criteria>
    - The seven named tests appear as `ok`, alongside every pre-existing `ui::screens::normal` test.
    - `rtk proxy git grep -n 'fn status_column_cells' -- src/ui/screens/normal.rs` prints one line, and the function body contains no integer literal 13.
  </acceptance_criteria>
  <done>The summary always renders as a whole ladder form sized to the measured Status column, and rows without agents are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scan cadence and cache robustness — the in-flight gate, redraw guard, alias filter</name>
  <files>src/app.rs</files>
  <read_first>
    - `src/app.rs` as Task 1 left it; the `obs_app` tests of the reconciliation scan (~2635-2700) as the shape to copy, and why `#[tokio::test]` is required for anything reaching `spawn_blocking` (~2613).
  </read_first>
  <behavior>
    - With `obs_app` over a non-git tempdir, 20 `Action::Tick`s set `agents_scan_in_flight` and an `Action::AgentsScanned` naming the alias arrives on the channel within 5 s; feeding it back clears the flag and leaves `agent_views` empty (no rows).
    - With the flag already set, 20 more ticks spawn no scan: draining the channel for 500 ms yields no `AgentsScanned`.
    - An `AgentsScanned` with an empty `per_project` clears the flag.
    - Applying the same scan twice: the second application leaves `needs_redraw` false.
    - A scan entry for an alias that is not registered is dropped; an entry with no rows and no worktree-less agents creates no view.
  </behavior>
  <action>
Write the behaviours as `app.rs` tests: `the_agents_scan_rides_the_session_poll_counter` (`#[tokio::test]`; drain other actions while waiting, bounded by `tokio::time::timeout`), `no_second_agents_scan_spawns_while_one_is_in_flight`, `the_agents_scan_handler_clears_the_in_flight_flag_for_an_empty_result`, `an_unchanged_agents_scan_does_not_request_a_redraw`, `an_agents_scan_for_an_unregistered_alias_is_dropped`, `a_project_with_no_agent_rows_gets_no_view`. Fix whatever they expose. Then run the plan gates, including `async_blocking_guard` (the scan must stay behind `spawn_blocking`).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib app::tests</automated>
    <fails_when>non-zero exit, or "0 passed", or any of the six named tests absent from the `ok` lines</fails_when>
    <automated>rtk proxy cargo test --test async_blocking_guard --no-fail-fast</automated>
    <fails_when>non-zero exit, or any FAILED line</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer than 52 `test result:` lines (49 + `agents_scan` + `agents_claude` + `agents_waves`)</fails_when>
  </verify>
  <acceptance_criteria>
    - The six named app tests appear as `ok`.
    - `rtk proxy git grep -n 'scan_projects_guarded' -- src/app.rs` prints exactly one non-test call, inside the `session_poll_counter >= 20` block.
    - Clippy, the async guard and the full suite meet their stated pass conditions.
  </acceptance_criteria>
  <done>Scans never overlap or stick, unchanged scans never redraw, and only registered projects with agents hold a view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blocking scan task → event loop | results cross a channel into state the render reads |
| agent-authored text → logs | descriptions and types are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-18 | Denial of Service | overlapping or stuck scans | medium | mitigate | `agents_scan_in_flight` gate; closure wrapped in `catch_unwind` and always sends; `no_second_agents_scan_spawns_while_one_is_in_flight`, `the_agents_scan_handler_clears_the_in_flight_flag_for_an_empty_result` |
| T-25-19 | Denial of Service (frozen UI) | blocking I/O on the async loop | medium | mitigate | scan only inside `spawn_blocking`; handler and render pure; `tests/async_blocking_guard.rs` |
| T-25-20 | Information Disclosure | tracing output | low | mitigate | log counts only; descriptions never logged |
| T-25-21 | Integrity (stale entries) | `agent_views` for removed projects | low | mitigate | wholesale replacement per scan plus the registered-alias filter (`an_agents_scan_for_an_unregistered_alias_is_dropped`) |
| T-25-SC | Tampering | package installs | low | accept | no new crates |
</threat_model>

<verification>
- `rtk proxy cargo test --lib app::tests` and `rtk proxy cargo test --lib ui::screens` green.
- `rtk proxy cargo test --test async_blocking_guard --no-fail-fast` green.
- `rtk proxy cargo clippy -- -D warnings` clean; full `--no-fail-fast`: only the known witness fails, at least 52 suites.
</verification>

<success_criteria>
- AGENT-05: a live wave's summary appears in its dashboard Status cell at every width, and rows without agents are unchanged.
- The scan runs on the existing cadence with no overlap, no stuck flag and no render-time derivation.
</success_criteria>

<output>
Create `.planning/phases/25-running-agents-live-wave-view/25-04-SUMMARY.md` when done
</output>
