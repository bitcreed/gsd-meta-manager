---
phase: 25-running-agents-live-wave-view
plan: 05
type: execute
wave: 4
depends_on: [25-04]
files_modified:
  - src/app.rs
  - src/ui/screens/detail.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/render_escape_guard.rs
  - src/ui/screens/help.rs
  - README.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [AGENT-06]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The detail view keeps its eight tabs plus Driver: the Sessions tab (`6:Ss`) gains sub-views `Sessions | Agents`, `DetailSubView::Agents` shares Sessions' tab index 5, `sub_view_from_index(5)` stays `Sessions`, and a `[Sessions] │ Agents   m switch` strip heads both sub-views, bracketed and reversed so it reads in monochrome (D-C15)."
    - "`m` on Sessions switches to Agents and `m` on Agents switches back — pressing it twice returns to the start — through `switch_to_sub_view`; `m` stays inert on every tab except Docs and Sessions; `n` still launches a session only from the Sessions sub-view [inferred — RESEARCH A4, audit] (D-C15)."
    - "The Agents sub-view shows, top to bottom: the widest summary form that fits (the dashboard's ladder); one row per wave such as `w2  running 13 · done 8 (+2 unmerged) · queued 14`, the current wave marked `▸` and bold so the highlight is not colour-only; a scrollable list of agent rows — state word, plan or description, agentType, `+commits`, `~dirty`, age of last activity; and a `Worktree-less (live)` group (D-C15, D-C07)."
    - "State words are ASCII (`live`, `idle`, `done`, `stall`, `?`, `ended`), and every width is measured with `Span::width` / `Line::width`, never `str::len` (D-C15)."
    - "Agent rows order by liveness (live, idle, done, stall, ?, ended), then plan, then path — the same order on every render of the same scan (ordering edge)."
    - "A nested agent renders on the line directly below its worktree row, indented (adjacency edge, D-C06)."
    - "No agent rows and no live worktree-less agents render exactly `No running agents`; a row without adapter metadata renders its branch and path with `(no agent metadata)`; a count git could not produce renders `?` and never hides the row (D-C16, empty edge)."
    - "`j`/`Down` stops at the last list line and `k`/`Up` at the first (PageDown/PageUp likewise); a one-line list and an inner area under 3 rows render without panicking (boundary edge)."
    - "Ages render floor-rounded from the scan instant — `45s`, `3m`, `2h`, `1d` — a missing mtime renders `-`, and a future mtime renders `0s` (precision edge)."
    - "Every agent-authored string in the sub-view — description, agentType, branch, worktree path, child and worktree-less descriptions — renders through `Untrusted::shown()`, and `render_escape_guard` probes the Agents sub-view with a hostile identity in each; the dashboard summary is recorded as drawing no agent-authored text (D-C13)."
    - "The three observed shapes render legibly at 80×24: one executor in a single-plan wave, three code-fixers, and thirteen executors in wave 2 of 11 (D-C15)."
  artifacts:
    - path: "src/app.rs"
      provides: "DetailSubView::Agents with its doc"
      contains: "Agents,"
    - path: "src/ui/screens/detail.rs"
      provides: "tab_index arm, `m` toggle, sessions_sub_tab_strip / sessions_sub_tab_row, render_agents_tab, agent_list_len, scroll arms, footer arms"
      contains: "fn render_agents_tab"
    - path: "src/ui/screens/render_escape_guard.rs"
      provides: "11-entry ALL_SUB_VIEWS, Agents label and arrival row, hostile agent_views in probe_ctx"
      contains: "Agents sub-view"
    - path: "src/ui/screens/help.rs"
      provides: "the shared `m` row"
      contains: "Docs / Sessions tab: switch sub-view (detail view)"
  key_links:
    - from: "detail.rs `m` arm on Sessions | Agents"
      to: "switch_to_sub_view"
      via: "the one arrival rule"
      pattern: "DetailSubView::Sessions \\| DetailSubView::Agents"
    - from: "render_agents_tab"
      to: "ctx.agent_views[alias]"
      via: "AgentView from 25-04's handler; summary via summary_forms()"
      pattern: "agent_views"
    - from: "key clamp and render"
      to: "agent_list_len"
      via: "one helper counts list lines for both, so they cannot drift"
      pattern: "agent_list_len"
  prohibitions:
    - "MUST NOT offer any action on an agent — kill, remove or prune a worktree, merge, open a terminal in it: this phase observes only, and acting on agents is a separate phase (phase boundary)."
    - "MUST NOT carry an agent's state or the current wave by colour alone — every state has a word and the current wave a `▸` marker (accessibility)."
coupling_justified: ["25-06: both read AgentView from 25-03; 25-06 adds `AgentView.fixers` and fixer summary forms with Default values, and this plan builds views only through `..Default::default()` and renders summary_forms() generically — either merge order compiles and renders correctly (the README line describing the fixer estimate refers to D-C12, which 25-06 implements)"]
---

<objective>
Add the Agents sub-view to the detail view's Sessions tab (AGENT-06, D-C15): a scrollable per-agent,
per-wave picture of what is running for the selected project, with explicit empty and degraded states
(D-C16) and escape-safe rendering of every agent-authored string (D-C13).

Purpose: the dashboard says `w2/11 13run`; this view says which thirteen, on which plans, how far along,
and which wave is next — without leaving the TUI.

Output: `DetailSubView::Agents`, `render_agents_tab` and its key handling in `detail.rs`, the shared
`m` help row, escape-guard coverage, and README / architecture notes.

Sequencing: wave 4 after 25-04 because both edit `app.rs`, `ui/screens/mod.rs` and `detail.rs`, and this
view reads 25-04's `agent_views`. It runs in parallel with 25-06: no shared file.

**Executor instruction:** `detail.rs` is ~12k lines and line numbers drift — grep each named symbol
before editing. Let the compiler name every exhaustive `DetailSubView` match; each gets an `Agents` arm.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-running-agents-live-wave-view/25-CONTEXT.md
@.planning/phases/25-running-agents-live-wave-view/25-RESEARCH.md
@.planning/phases/25-running-agents-live-wave-view/25-PATTERNS.md
@.planning/phases/25-running-agents-live-wave-view/25-04-SUMMARY.md

<interfaces>
From 25-01: `crate::agents::{AgentRow, AgentLiveness, ProjectAgents}`, `crate::agents::adapters::ChildAgent` — `AgentRow.{path, branch, adapter, agent_type, description, commits_ahead, dirty, last_activity, liveness, children, plan}`.
From 25-03: `crate::agents::waves::{AgentView, WaveRow, PlanRef}` — `AgentView.{waves, agents (display-ordered), worktreeless, scanned_at}`, `WaveRow::label()`, `PlanRef::label()`, `AgentView::summary_forms()`.
From 25-04: `AppContext.agent_views: HashMap<String, AgentView>`.

Created here: `DetailSubView::Agents`; `ProjectViewCache.agents_selected: usize`; in `detail.rs`: `pub(crate) fn sessions_sub_tab_strip(active: &DetailSubView) -> Line<'static>`, `fn sessions_sub_tab_row(frame: &mut Frame, area: Rect, active: &DetailSubView) -> Rect`, `fn render_agents_tab(&self, frame: &mut Frame, area: Rect, ctx: &AppContext)`, `fn agent_list_len(view: &AgentView) -> usize`.
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 25 plan; newly created names are not drift).

**New files:** `src/agents/mod.rs`, `src/agents/worktrees.rs`, `src/agents/adapters/mod.rs`, `tests/agents_scan.rs` (25-01); `src/agents/adapters/claude.rs`, `tests/agents_claude.rs` (25-02); `src/agents/waves.rs`, `tests/agents_waves.rs` (25-03); `src/agents/fixers.rs`, `tests/agents_fixers.rs` (25-06).
**Core (25-01):** `AgentLiveness`, `LIVE_SECS`, `IDLE_SECS`, `MAX_AGENT_AGE_SECS`, `classify_liveness`, `AgentRow`, `ProjectAgents`, `scan_project_with`, `scan_projects_guarded`; `RawWorktree`, `CoreWorktree`, `CoreScan`, `BranchPlan`, `parse_porcelain_z`, `parse_porcelain_lines`, `is_agent_worktree`, `parse_agent_branch`, `valid_agent_id`, `scan_worktrees`; `AgentAdapter`, `CoreSnapshot`, `Enrichment`, `ChildAgent`, `AdapterReport`, `registered_adapters`; git helpers `worktree_list_porcelain`, `PorcelainListing`, `commits_ahead`, `dirty_count`, and `GIT_OPTIONAL_LOCKS=0` inside `git_read_raw`.
**Claude adapter (25-02):** `ClaudeCodeAdapter::{new, from_env}`, `resolve_config_root`, `encode_project_dir`, tolerant `Meta`, `read_meta_capped`, `META_READ_CAP`.
**Wave model (25-03):** `PlanRef`, `PlanState`, `WaveRow`, `AgentView`, `attribute`, `commit_scope_plan`, `derive`, `AgentView::{summary_forms, is_active}`; `AgentRow.{plan, summary_in_worktree}`; `DiskInference.summarized_plans`; `git_ops::log_subjects`; `disk_status::{plan_index, leading_frontmatter_nested_value}` made `pub(crate)`.
**App and dashboard (25-04):** `Action::AgentsScanned`, `AppContext.{agent_views, agents_scan_in_flight}`, `status_column_cells`, `agent_summary_line`.
**Detail view (25-05):** `DetailSubView::Agents`, `ProjectViewCache.agents_selected`, `sessions_sub_tab_strip`, `sessions_sub_tab_row`, `render_agents_tab`; key `m` on Sessions/Agents.
**Fixer estimate (25-06):** `FixerEstimate`, `fixers::estimate`, `fixers::finding_ids`, `ProjectAgents.fixer_estimate`, `AgentView.fixers`.
**Requirements (25-01):** AGENT-01..AGENT-07 in REQUIREMENTS.md and the ROADMAP Phase 25 entry.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Sessions › Agents end to end — the variant, `m`, the strip, render_agents_tab, and escape-guard census</name>
  <files>src/app.rs, src/ui/screens/detail.rs, src/ui/screens/mod.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - `src/app.rs` — the `DetailSubView` enum (~15-51), especially the `Archive` doc as the sub-view doc to copy.
    - `src/ui/screens/detail.rs` — `tab_index` (~994-1013, exhaustive and wildcard-free on purpose), `sub_view_from_index` (~1027-1045), `switch_to_sub_view` (~1697), the Docs `m` arm (~3242-3257), both render dispatches (grep `DetailSubView::Sessions => self.render_sessions_tab`), `render_sessions_tab` (~4911-4999), `render_archive_tab`'s strip call and vertical split (~5006-5020), `docs_sub_tab_strip` / `docs_sub_tab_row` (~6464-6491), `m_switches_docs_between_files_and_milestones` and `m_is_inert_outside_docs` (grep), `test_ctx`, `press`, `render_detail_to_text`, `active_tab_text`.
    - `src/ui/screens/mod.rs` — `ProjectViewCache` (~909-970, `sessions_selected`).
    - `src/ui/screens/render_escape_guard.rs` — `ALL_SUB_VIEWS` and its doc (~1392-1400), `DETAIL_TAB_ARRIVAL` (~1423-1490, the Sessions row as the voice), `sub_view_label` (~1618), `probe_ctx` (~1068-1120).
    - `src/ui/roadmap_view.rs` — its existing use of `▸`.
    - `src/agents/waves.rs` — `AgentView`, `WaveRow`, `summary_forms`.
    - `.planning/phases/25-running-agents-live-wave-view/25-RESEARCH.md` § Pattern 7.
  </read_first>
  <action>
**Variant (D-C15).** Add `Agents` after `Sessions` in `DetailSubView` with a doc modelled on `Archive`'s: the Sessions tab's Agents sub-view, not a tab of its own; it shares Sessions' index so a digit or arrow never lands on it; it is reached with `m` on the Sessions tab; per-project view state is in memory only, so a stored `Agents` needs no migration. Add `pub agents_selected: usize` beside `sessions_selected` in `ProjectViewCache`.

**Index and toggle.** `tab_index`: `DetailSubView::Sessions | DetailSubView::Agents => 5`, with a comment matching the Docs arm's. `sub_view_from_index(5)` is unchanged. Add a second guarded `m` arm: `if matches!(current_view, DetailSubView::Sessions | DetailSubView::Agents)`, calling `switch_to_sub_view` with the other one. First confirm that neither Sessions nor Agents has a text-input mode that could want the letter, and cite that in the arm's comment as the Docs arm does. Agents arrival does no discovery work; if `switch_to_sub_view` resets `sessions_selected` on Sessions arrival, reset `agents_selected` the same way on Agents arrival. Add an `Agents` arm to every exhaustive match the compiler names. The `n` arm stays guarded on `== DetailSubView::Sessions`.

**Strip.** `sessions_sub_tab_strip(active)` mirrors `docs_sub_tab_strip`: `[Sessions] │ Agents   m switch` on Sessions and `Sessions │ [Agents]   m switch` on Agents, the active label bracketed plus cyan, bold and reversed, the rest dim. `sessions_sub_tab_row(frame, area, active) -> Rect` draws it in the first row and returns the remainder, like `docs_sub_tab_row`. Call it first in `render_sessions_tab`, so that body shrinks by one row, and in `render_agents_tab`.

**render_agents_tab (happy path).** Follow `render_sessions_tab`:
- Draw the strip row, then a bordered `Block` titled ` Agents `, returning early when `inner.height < 3 || inner.width < 10`.
- Take `view = ctx.agent_views.get(&self.alias)`. With no view, or a view with no agents and no worktree-less agents, draw a `Paragraph` reading `No running agents`.
- Otherwise split the inner area vertically: `Length(1)` for the summary, `Length(min(waves, inner.height / 3))` for the wave rows, `Min(0)` for the list.
- Summary: the first `summary_forms()` entry whose `Line::width()` fits `inner.width`; when there are no forms, `no active agents` in dark gray.
- Wave rows: `▸ ` plus bold for the current wave, two spaces otherwise, then `{label}  running {r} · done {d}` with ` (+{f} unmerged)` when `finished > 0`, `· queued {q}`, and ` · stalled {s}` when `stalled > 0`.
- List: one line per agent row, `{state:<5}  {plan label, else description.shown(), else branch}  {agent_type.shown()}  +{commits|?}  ~{dirty|?}  {age}`. State words: `Live`→`live`, `Idle`→`idle`, `Finished`→`done`, `Stalled`→`stall`, `Unknown`→`?`, `Ended`→`ended`. Each child goes on an indented line directly below its row. Then a `Worktree-less (live)` header line and one line per worktree-less agent.
- `agent_list_len(view)` counts exactly those list lines. Draw them with `List::new(items).highlight_style(cyan + bold).highlight_symbol("> ")` and a `ListState` selecting `agents_selected` clamped to `agent_list_len - 1`.
- Every agent-authored string goes through `.shown()` (D-C13). Add the render at BOTH dispatch sites.

**Escape-guard census (so it stays green in this task).** `ALL_SUB_VIEWS` becomes `[..; 11]` with `Agents` after `Sessions`, and its doc says why, the same way it explains `Archive`. Add `Agents => "Agents sub-view"` to `sub_view_label`, and add a `DETAIL_TAB_ARRIVAL` row `("Agents sub-view", true, …)` in the Sessions row's voice: it draws description, agentType, branch, path and child text from `ctx.agent_views`, populated by 25-05, while the strip above it is authored text. In `probe_ctx`, insert `ctx.agent_views[identity]` as an `AgentView { .. , ..Default::default() }` holding:
- one `Live` row with no `plan`, whose `description`, `agent_type` and `branch` carry `identity`, plus a child whose description carries it;
- one row with `adapter: None` whose path's final component and branch carry `identity`;
- one worktree-less agent whose description carries it.

**Tracer tests (detail.rs):** `m_switches_sessions_between_sessions_and_agents` (on both sub-views the tab bar shows tab 6 active, and `m` twice returns to Sessions); `the_agents_sub_view_draws_the_summary_waves_and_agent_rows` (a two-wave view with one `Live` row on 13-02 renders its summary, `▸ w2`, `live` and `13-02`). Rename `m_is_inert_outside_docs` to `m_is_inert_outside_docs_and_sessions`, exclude Sessions' index as well, and update its doc.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail</automated>
    <fails_when>non-zero exit, or "0 passed", or `m_switches_sessions_between_sessions_and_agents` / `the_agents_sub_view_draws_the_summary_waves_and_agent_rows` / `m_is_inert_outside_docs_and_sessions` absent from the `ok` lines</fails_when>
    <automated>rtk proxy cargo test --lib render_escape_guard</automated>
    <fails_when>non-zero exit, or "0 passed", or any FAILED line (the Agents probe did not see the identity, or saw it unescaped)</fails_when>
  </verify>
  <acceptance_criteria>
    - The three named detail tests appear as `ok`; every `render_escape_guard` test passes with 11 sub-views.
    - `every_tab_index_round_trips_through_its_sub_view` (grep its current name in detail.rs tests) gains an assertion that `tab_index(&DetailSubView::Agents)` equals `tab_index(&DetailSubView::Sessions)` and that `sub_view_from_index` of that index is `Sessions`, and passes.
    - `rtk proxy git grep -n "KeyCode::Char('n') if current_view == DetailSubView::Sessions" -- src/ui/screens/detail.rs` prints one line.
  </acceptance_criteria>
  <done>A user on the Sessions tab presses `m` and sees the project's running agents, waves and summary, with every agent-authored string escaped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scrolling, footer and help for the Agents sub-view</name>
  <files>src/ui/screens/detail.rs, src/ui/screens/help.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — the `j`/`Down`, `k`/`Up`, `PageDown`, `PageUp` and `Enter` arms, each with its `DetailSubView::Sessions` branch (grep `DetailSubView::Sessions => {`), `PAGE_SCROLL_LINES`, `footer_spans` (the Sessions, Archive and Browse arms) and any measured footer-width tests (grep `footer` in the tests module).
    - `src/ui/screens/help.rs` — `row("m", …)` (~244) and `the_docs_milestones_switch_is_documented` (~936-953).
  </read_first>
  <behavior>
    - On Agents with 5 list lines: 10 × `j` leaves `agents_selected == 4`; 10 × `k` leaves 0; `PageDown` from 0 stops at 4; `PageUp` from 4 stops at 0; a one-line list stays at 0 for every key.
    - `Enter` on Agents returns `ScreenAction::None` and changes nothing; `n` on Agents returns `ScreenAction::None` and sets no status message.
    - The Sessions footer contains `[m]` and ` agents`; the Agents footer contains `[j/k]` and `[m]` with ` sessions`.
    - The help popup contains the row `m  Docs / Sessions tab: switch sub-view (detail view)` exactly once, with the experimental flag on and off.
  </behavior>
  <action>
Add an `Agents` branch next to each `Sessions` branch in the `j`/`Down`, `k`/`Up`, `PageDown` and `PageUp` arms. Each clamps `agents_selected` against `agent_list_len(view)` looked up through `ctx.agent_views.get(&self.alias)`, with a missing view counting as zero lines. The `Enter` arm's `Agents` branch returns `ScreenAction::None`, so this phase offers no action on an agent. Footer: append `[m]` + ` agents  ` to the Sessions arm, and add an `Agents` arm with `[j/k]` + `scroll  ` and `[m]` + ` sessions  `, following the Archive/Browse convention. If a measured footer-width test pins the Sessions footer, update its expected width to the new measured value and keep the test. Help: change the `m` row to `row("m", "Docs / Sessions tab: switch sub-view (detail view)")`, and in the same commit rename `the_docs_milestones_switch_is_documented` to `the_m_sub_view_switch_is_documented`, asserting the new row exactly once with the flag on and off.

Tests (detail.rs): `agents_selection_clamps_at_both_ends`, `agents_paging_clamps_at_both_ends`, `enter_and_n_do_nothing_on_the_agents_sub_view`, `the_sessions_and_agents_footers_advertise_m`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or "0 passed", or any FAILED line, or any of the five named tests absent from the `ok` lines</fails_when>
  </verify>
  <acceptance_criteria>
    - `agents_selection_clamps_at_both_ends`, `agents_paging_clamps_at_both_ends`, `enter_and_n_do_nothing_on_the_agents_sub_view`, `the_sessions_and_agents_footers_advertise_m` and `the_m_sub_view_switch_is_documented` appear as `ok`.
    - `rtk proxy git grep -n 'Docs / Sessions tab: switch sub-view (detail view)' -- src/ui/screens/help.rs` prints the row and its test.
  </acceptance_criteria>
  <done>The Agents list scrolls within bounds, offers no action, and is discoverable from the footer and help.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Degraded and empty states, ages, the three observed shapes, the dashboard escape record, docs</name>
  <files>src/ui/screens/detail.rs, src/ui/screens/render_escape_guard.rs, README.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - `src/ui/screens/detail.rs` — `render_agents_tab` as Tasks 1-2 left it; `render_detail_to_text` / `render_detail_to_text_at` test helpers.
    - `src/ui/screens/render_escape_guard.rs` — the NormalScreen states (~1891-1932) and how a state records that a surface draws no identity.
    - `README.md` — the Features list (~26-66) and the 8-tab bullet; `docs/ARCHITECTURE.md` — the `src/` tree (~132-160) and the data-flow section (~60-130).
    - `.planning/phases/25-running-agents-live-wave-view/25-CONTEXT.md` § Specific Ideas (three shapes), D-C16.
  </read_first>
  <behavior>
    - A row with `adapter: None`, branch `worktree-agent-p13-02-1790386422` and no counts renders `?`, the branch, the path and `(no agent metadata)`; it is not hidden.
    - A row with `commits_ahead: Some(0)`, `dirty: Some(0)` renders `+0  ~0`.
    - Ages from `scanned_at`: 45 s → `45s`, 199 s → `3m`, 7300 s → `2h`, 90000 s → `1d`; `last_activity` after `scanned_at` → `0s`; no `last_activity` → `-`.
    - A row with one child renders the child on the next line, indented.
    - Given two rows equal in liveness and plan, the one with the lexically smaller path renders first; two renders of the same view are identical.
    - A view with only an `Ended` row shows that row and a `no active agents` summary; a context with no view shows exactly `No running agents`.
    - At 80×24: shape 1 (one executor, single-plan wave) shows its wave rows and one `live` line; shape 2 (three `gsd-code-fixer` rows with descriptions) shows `3 agents` and three descriptions; shape 3 (13 executors, 11 waves) shows the summary, the current wave marked `▸ w2`, and a scrollable list whose first lines are `live` rows.
    - A 12×4 area renders without panicking.
    - For a view whose every agent-authored field carries a hostile identity, no `summary_forms()` entry contains that identity.
  </behavior>
  <action>
Complete `render_agents_tab`'s degraded paths per D-C16 and the behaviours: `(no agent metadata)` rows, `?` counts, ended rows, children, the age formatter (a private `fn agent_age(scanned_at, last_activity) -> String`, floor-rounded, future clamped to 0), and the tiny-area guard. Tests (detail.rs): `a_row_without_metadata_shows_branch_path_and_marker`, `zero_counts_render_as_zero_not_question_marks`, `ages_floor_round_and_clamp_future_to_zero`, `a_nested_agent_renders_indented_under_its_worktree`, `equal_rows_order_by_path_and_render_identically`, `no_running_agents_is_the_empty_state`, `the_three_observed_shapes_render_at_80_by_24`, `a_tiny_area_renders_without_panicking`, `the_current_wave_is_marked_in_text_not_only_colour`. Build views with `..Default::default()`.

**Dashboard escape record (D-C13).** In `render_escape_guard.rs` add `the_dashboard_agent_summary_draws_no_identity`: build the hostile view that `probe_ctx` uses and assert that no `summary_forms()` entry contains the identity. The Status-cell summary is numbers and authored words only. Add a prose line beside the NormalScreen states recording that fact and naming this test, so a later edit that puts a description into the summary turns the test red.

**Docs.** README: add a Features bullet in the list's style: "Running agents — for each project, which GSD agents are running right now, read from git worktrees and Claude Code's subagent metadata without invoking Claude: the dashboard Status cell summarises the live wave (e.g. `w2/11 13run`, or an estimated `~5/12 fixed` for code-review fix runs), and the Sessions tab's Agents sub-view (`m`) lists every agent with its plan, commits, dirty files and last activity, wave by wave." Extend the 8-tab bullet's `Sessions` entry to `Sessions (with an Agents sub-view)`. `docs/ARCHITECTURE.md`: add `agents/` to the `src/` tree ("read-only agent-worktree scan, runtime adapters, wave model") and one data-flow sentence: `Action::Tick` every 20 ticks → `spawn_blocking(agents::scan_projects_guarded)` → `Action::AgentsScanned` → `waves::derive` → `AppContext.agent_views`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or "0 passed", or any FAILED line, or any of the ten named tests absent from the `ok` lines</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer than 52 `test result:` lines</fails_when>
  </verify>
  <acceptance_criteria>
    - The nine detail tests and `the_dashboard_agent_summary_draws_no_identity` appear as `ok`.
    - `README.md` contains `Agents sub-view` and `w2/11 13run`; `docs/ARCHITECTURE.md` contains `agents/` and `AgentsScanned`.
    - Clippy and the full suite meet their stated pass conditions.
  </acceptance_criteria>
  <done>Every degraded and empty state is explicit, the three observed runs read clearly at 80 columns, the escape guard covers the new surfaces, and the docs describe the feature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ctx.agent_views` → terminal | descriptions, agent types, branches and paths are agent- or clone-authored text |
| keypress → sub-view | a mis-routed key could launch a session or swallow typed text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-22 | Tampering / Spoofing (terminal escape, bidi) | Agents sub-view text | high | mitigate | every agent-authored string via `Untrusted::shown()`; `render_escape_guard` Agents row `arrives: true` with a hostile identity in description, agentType, branch, path, child and worktree-less text; `the_dashboard_agent_summary_draws_no_identity` |
| T-25-23 | Denial of Service | `m` swallowing typed text | low | mitigate | guarded arm on Sessions/Agents only; executor confirms neither has a text-input mode (cited in the arm comment) |
| T-25-24 | Elevation (unintended action) | `n` / `Enter` on Agents | low | mitigate | `n` stays guarded on Sessions; `Enter` on Agents is a no-op (`enter_and_n_do_nothing_on_the_agents_sub_view`) |
| T-25-SC | Tampering | package installs | low | accept | no new crates |
</threat_model>

<verification>
- `rtk proxy cargo test --lib ui::screens` green (detail, help, escape guard).
- `rtk proxy cargo clippy -- -D warnings` clean; full `--no-fail-fast`: only the known witness fails, at least 52 suites.
</verification>

<success_criteria>
- AGENT-06: the Sessions tab's Agents sub-view shows the summary, per-wave rows, a scrollable agent list and the worktree-less group, with explicit empty and degraded states.
- Eight tabs plus Driver are unchanged; `m` and `n` behave as documented.
</success_criteria>

<output>
Create `.planning/phases/25-running-agents-live-wave-view/25-05-SUMMARY.md` when done
</output>
