---
phase: 25-running-agents-live-wave-view
plan: 07
type: execute
wave: 5
depends_on: [25-05, 25-06]
gap_closure: true
files_modified:
  - src/agents/mod.rs
  - src/agents/waves.rs
  - src/agents/fixers.rs
  - src/ui/screens/normal.rs
  - tests/agents_claude.rs
  - tests/agents_waves.rs
  - tests/agents_fixers.rs
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-04, AGENT-05, AGENT-06, AGENT-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An agent whose last activity is older than `MAX_AGENT_AGE_SECS` (86 400 s) reads `Ended`, whatever its lock says: `classify_facts` consumes the bound, which it never did before. At exactly the bound the agent still reads `Finished` or `Stalled` (CR-01, D-C08)."
    - "A registered project whose only agent worktrees are aged-out orphans (lock released or still held, transcript days old) renders a dashboard frame byte-identical to the frame with no agent view at all. A milestone-complete project's Status cell reads `v1.0 Complete` again (CR-01, AGENT-05, D-C14)."
    - "Only a `Live` or `Idle` row, or a live worktree-less agent, switches the Status-cell summary on. A view whose rows are all `Finished` has `is_active() == false` and no summary forms, so a released-lock orphan stops overriding the cell once its transcript is older than `LIVE_SECS`. Inside a ladder that something running switched on, a `Finished` plan still counts toward `done` (CR-01, D-C14)."
    - "A `Stalled` orphan shows the `N stalled` alert only until its last activity passes `MAX_AGENT_AGE_SECS`; after that it reads `Ended` and the cell is untouched (D-C08, D-C14)."
    - "Running agents choose what the summary describes. `Finished` and `Stalled` rows vote for the active phase only when no attributed row is `Live` or `Idle`. The executor ladder is chosen only by a `Live` or `Idle` row attributed to the active phase. The `N agents` form counts running agents only. So an orphan from another phase never replaces a live wave's ladder (WR-01 folded in [inferred], D-C10, D-C14)."
    - "The fixer estimate exists only while at least one unattributed `gsd-code-fixer` row is `Live` or `Idle`. Inside such a run, a `Finished` fixer still counts toward `fixers`, and its commits still count toward `fixed` (CR-01, D-C12)."
    - "The scan never hides or prunes an orphan. Its row stays in `ProjectAgents.rows` with its git counts and adapter data, and renders `ended` in the Agents sub-view. No worktree is pruned, unlocked, removed or written (D-A03, D-C16, D-B01; prune is a deferred action)."
  artifacts:
    - path: "src/agents/mod.rs"
      provides: "the age bound in classify_facts, AgentLiveness::is_running, amended Ended / MAX_AGENT_AGE_SECS docs, the boundary unit test"
      contains: "age > MAX_AGENT_AGE_SECS"
    - path: "src/agents/waves.rs"
      provides: "running-only activation, the tiered active-phase vote, the running-row executor-mode rule"
      contains: "is_running()"
    - path: "src/agents/fixers.rs"
      provides: "the running-fixer gate at the top of estimate"
      contains: "is_running()"
    - path: "src/ui/screens/normal.rs"
      provides: "byte-identical dashboard regression tests for aged-out and within-bound orphans"
      contains: "fn an_aged_out_orphan_leaves_the_status_cell_byte_identical"
    - path: "tests/agents_claude.rs"
      provides: "real-worktree orphan tests through the Claude adapter"
      contains: "fn a_released_lock_orphan_days_stale_reads_ended_and_is_not_active"
    - path: "tests/agents_fixers.rs"
      provides: "the finished-fixer gate tests"
      contains: "fn a_finished_fixer_run_yields_no_estimate"
  key_links:
    - from: "src/agents/mod.rs classify_facts"
      to: "MAX_AGENT_AGE_SECS"
      via: "an agent inactive for more than the bound classifies Ended before the Finished/Stalled rules run"
      pattern: "age > MAX_AGENT_AGE_SECS"
    - from: "src/agents/waves.rs AgentView::is_active / derive / summary_forms"
      to: "src/agents/mod.rs AgentLiveness::is_running"
      via: "running-only activation, first-tier phase vote and executor-mode selection"
      pattern: "is_running()"
    - from: "src/ui/screens/normal.rs Status cell"
      to: "AgentView::summary_forms"
      via: "agent_summary_line returns None for an orphan-only view, so the existing milestone/pipeline expression renders unchanged"
      pattern: "agent_summary_line"
    - from: "src/agents/fixers.rs estimate"
      to: "src/agents/mod.rs AgentLiveness::is_running"
      via: "no estimate, and no git or file read, unless a fixer is running"
      pattern: "is_running()"
  prohibitions:
    - "MUST NOT prune, unlock, remove or write any worktree or file. Classifying an orphan is read-only (D-B01); pruning an orphaned worktree is a deferred action."
    - "MUST NOT drop an orphan's row from `ProjectAgents.rows` or the Agents sub-view (D-A03, D-C16)."
---

<objective>
Close the verification gap CR-01 (`25-VERIFICATION.md` gaps[0], `25-REVIEW.md` CR-01). An aborted or crashed GSD run leaves its agent worktrees on disk. Today those worktrees read `Finished` or `Stalled` for as long as they exist: `MAX_AGENT_AGE_SECS` is declared but never read by `classify_facts`, and `AgentView::is_active()` and `is_active_fixer` count `Finished` as active. The dashboard Status cell (`normal.rs` ~896-901) therefore shows an agent summary instead of the project's real status, including milestone-complete, with nothing running.

After this plan:
- an agent inactive past the bound reads `Ended`;
- `Finished` alone never switches the summary on;
- running agents, not leftovers, choose the phase and the ladder;
- the fixer estimate needs a running fixer;
- regression tests prove that an orphan-only project's dashboard frame is byte-identical to the frame with no agent view.

Purpose: the phase goal is to show which agents are **currently** running (AGENT-05: "with no active agents the row is unchanged"). Orphans are expected, not rare. D-A08 and CONTEXT's deferred list name "prune an orphaned worktree" as future work, and this project's own history includes runs halted mid-wave.

Output: edits to `src/agents/{mod,waves,fixers}.rs`, new regression tests in `src/agents/mod.rs`, `src/agents/waves.rs`, `src/ui/screens/normal.rs`, `tests/agents_claude.rs` and `tests/agents_fixers.rs`, and one updated assertion in `tests/agents_waves.rs`.

Scope decision [inferred]: WR-01 (the active-phase vote gave orphans equal weight) is folded in. Once the age bound and the `Finished` rule land, a within-bound orphan from another phase can still out-vote a live wave and replace its ladder. The CR-01 truth "an orphan stops overriding the Status cell" is incomplete without the fix, and the fix sits in the same function. WR-02..WR-05 and IN-01..IN-04 are out of scope; see "Deferred review findings".
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-running-agents-live-wave-view/25-CONTEXT.md
@.planning/phases/25-running-agents-live-wave-view/25-VERIFICATION.md
@.planning/phases/25-running-agents-live-wave-view/25-REVIEW.md
@.planning/phases/25-running-agents-live-wave-view/25-04-SUMMARY.md
@.planning/phases/25-running-agents-live-wave-view/25-06-SUMMARY.md

<interfaces>
Existing (read, do not re-derive):
- `crate::agents::{AgentLiveness, AgentRow, ProjectAgents, classify_liveness, scan_project_with, LIVE_SECS, IDLE_SECS, MAX_AGENT_AGE_SECS}`. `classify_liveness(facts: Option<&Enrichment>, summary_in_worktree: bool, now: SystemTime) -> AgentLiveness` is public. The private `classify_facts(ended, last_activity, lock_released, summary_in_worktree, now)` is the one classifier every row and child goes through. `classify_child` routes children through it.
- `crate::agents::adapters::{Enrichment, ChildAgent}`: both are `Default`, with public fields.
- `crate::agents::waves::{derive, AgentView}`: `derive(&ProjectAgents, &ProjectState) -> AgentView`, plus `AgentView::{is_active, summary_forms}`. `waves.rs` has the private helper `is_active_liveness` (Live|Idle|Finished), the vote at the top of `derive`, and `executor_mode` inside `summary_forms`.
- `crate::agents::fixers::{estimate, FixerEstimate}` and the private `is_active_fixer` (Live|Idle|Finished).
- `normal.rs` tests: `ctx_with_aliases`, `render_selected(ctx, width)`, `status_cell_text(rows, row)`, `row_with(rows, text)`, `text_is_colored`, `agent_row(n, liveness, id)`, `thirteen_executor_view()`.
- `tests/agents_claude.rs`: `claude_fixture()`, `ClaudeFixture::{add_agent_worktree(id, lock_pid), write_meta, write_transcript(id, now, age), scan(now)}`, `executor_meta(wt)`, `only_row`, `shown`, `AGENT_ID`.
- `tests/agents_fixers.rs`: `review_repo(Some(REVIEW_48))`, `add_agent_worktree`, `commit`, `Scripted { agent_type, description, age_secs, ended }` whose `enrich` hard-codes `lock_released: Some(false)`, `scan`, `FIXER_A`, `FIXER_B`.

Created here:
- `impl AgentLiveness { pub fn is_running(self) -> bool }`: true only for `Live` and `Idle`.
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This is the phase-wide inventory, identical in every Phase 25 plan. Newly created names are not drift.

**New files:**
- 25-01: `src/agents/mod.rs`, `src/agents/worktrees.rs`, `src/agents/adapters/mod.rs`, `tests/agents_scan.rs`
- 25-02: `src/agents/adapters/claude.rs`, `tests/agents_claude.rs`
- 25-03: `src/agents/waves.rs`, `tests/agents_waves.rs`
- 25-06: `src/agents/fixers.rs`, `tests/agents_fixers.rs`
- 25-07 creates no new file.

**Core (25-01):**
- `AgentLiveness`, `LIVE_SECS`, `IDLE_SECS`, `MAX_AGENT_AGE_SECS`, `classify_liveness`, `AgentRow`, `ProjectAgents`, `scan_project_with`, `scan_projects_guarded`
- `RawWorktree`, `CoreWorktree`, `CoreScan`, `BranchPlan`, `parse_porcelain_z`, `parse_porcelain_lines`, `is_agent_worktree`, `parse_agent_branch`, `valid_agent_id`, `scan_worktrees`
- `AgentAdapter`, `CoreSnapshot`, `Enrichment`, `ChildAgent`, `AdapterReport`, `registered_adapters`
- git helpers `worktree_list_porcelain`, `PorcelainListing`, `commits_ahead`, `dirty_count`, and `GIT_OPTIONAL_LOCKS=0` inside `git_read_raw`

**Claude adapter (25-02):** `ClaudeCodeAdapter::{new, from_env}`, `resolve_config_root`, `encode_project_dir`, tolerant `Meta`, `read_meta_capped`, `META_READ_CAP`.

**Wave model (25-03):**
- `PlanRef`, `PlanState`, `WaveRow`, `AgentView`, `attribute`, `commit_scope_plan`, `derive`, `AgentView::{summary_forms, is_active}`
- `AgentRow.{plan, summary_in_worktree}`; `DiskInference.summarized_plans`; `git_ops::log_subjects`
- `disk_status::{plan_index, leading_frontmatter_nested_value}` made `pub(crate)`

**App and dashboard (25-04):** `Action::AgentsScanned`, `AppContext.{agent_views, agents_scan_in_flight}`, `status_column_cells`, `agent_summary_line`.

**Detail view (25-05):** `DetailSubView::Agents`, `ProjectViewCache.agents_selected`, `sessions_sub_tab_strip`, `sessions_sub_tab_row`, `render_agents_tab`; key `m` on Sessions/Agents.

**Fixer estimate (25-06):** `FixerEstimate`, `fixers::estimate`, `fixers::finding_ids`, `ProjectAgents.fixer_estimate`, `AgentView.fixers`.

**Orphan aging (25-07, gap closure CR-01 + WR-01):**
- New method: `AgentLiveness::is_running`.
- `MAX_AGENT_AGE_SECS` is now consumed by `classify_facts`.
- In `waves.rs`, the private `is_active_liveness` is replaced by `AgentLiveness::is_running` wherever it switches the summary on, votes in the first tier, selects executor mode, or feeds the `N agents` count.
- A running-fixer gate in `fixers::estimate`.
- New tests:
  - `agents::tests::an_agent_silent_past_the_age_bound_reads_ended`
  - `agents::tests::only_live_and_idle_are_running`
  - `agents::waves::tests::a_finished_row_alone_never_switches_the_summary_on`
  - `agents::waves::tests::running_agents_outvote_orphans_for_the_active_phase`
  - `ui::screens::normal::tests::an_aged_out_orphan_leaves_the_status_cell_byte_identical`
  - `ui::screens::normal::tests::a_finished_orphan_within_the_age_bound_leaves_the_status_cell_byte_identical`
  - `tests/agents_claude.rs::{a_released_lock_orphan_days_stale_reads_ended_and_is_not_active, a_locked_orphan_days_stale_reads_ended_not_stalled}`
  - `tests/agents_fixers.rs::a_finished_fixer_run_yields_no_estimate`
- Modified test: `tests/agents_waves.rs::a_summary_committed_in_the_worktree_reads_finished_before_the_merge`.
- Test helper field: `Scripted.lock_released` in `tests/agents_fixers.rs`.

**Requirements (25-01):** AGENT-01..AGENT-07 in REQUIREMENTS.md and the ROADMAP Phase 25 entry.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Aged-out orphan end to end: real worktree → Claude adapter → classify_facts age bound → derive → byte-identical dashboard row</name>
  <files>src/agents/mod.rs, src/ui/screens/normal.rs, tests/agents_claude.rs</files>
  <read_first>
    - `src/agents/mod.rs`: the whole file, especially the threshold block (`LIVE_SECS`, `IDLE_SECS`, `MAX_AGENT_AGE_SECS`), `classify_facts`, `classify_liveness` and its "In order" doc, `classify_child`, the `AgentLiveness::Ended` doc, and the `#[cfg(test)]` helpers `at` and `facts`.
    - `src/ui/screens/normal.rs`: `agent_summary_line` (~417-431); the Status-cell build (~886-930); the 25-04 test block from `// --- 25-04` (~2733) through `the_agent_summary_replaces_a_milestone_complete_cell`, including `agent_row`, `render_selected`, `status_cell_text`, `a_row_without_active_agents_is_byte_identical`.
    - `tests/agents_claude.rs`: the header, `ClaudeFixture`, `claude_fixture`, `executor_meta`, `a_released_lock_with_a_stale_transcript_reads_finished`, `a_locked_worktree_with_a_stale_transcript_reads_stalled_even_with_a_live_pid`, `only_row`.
    - `.planning/phases/25-running-agents-live-wave-view/25-REVIEW.md` § CR-01 (the fix it proposes) and `25-VERIFICATION.md` `gaps[0].missing`.
  </read_first>
  <action>
Tracer, RED first. Write the three tests below, run them, and confirm they fail against today's tree. The released-lock orphan reads `Finished` and the view shows `1 agent` or `2 agents`; the held-lock orphan reads `Stalled` and shows `1 stalled`. Then implement the age bound, and only that. Task 2 owns the `Finished`-activation rule.

**Implementation, `src/agents/mod.rs` (D-C08, per 25-REVIEW CR-01's fix):**
- In `classify_facts`, right after `let age = age_secs(last, now);` and before the released-lock `Finished` test, return `AgentLiveness::Ended` when `age > MAX_AGENT_AGE_SECS`. Use strictly greater-than, matching the "at most this many seconds" boundary of `LIVE_SECS` and `IDLE_SECS`.
- The resulting order is: `ended` flag → no `last_activity` (`Unknown`) → past the age bound (`Ended`) → released lock plus SUMMARY-or-stale (`Finished`) → `Live` / `Idle` / `Stalled`.
- Children go through the same function via `classify_child`, so an aged child also reads `Ended`.
- A future mtime stays age 0, per the existing clamp.
- **[inferred]** An aged-out agent maps to the existing `Ended` variant, not a new one. Every consumer already treats `Ended` as over: `liveness_rank` sorts it last; it is excluded from the vote, `is_active` and the fixer gate; and the Agents sub-view shows it as the word `ended`. So no renderer changes, and D-C16 still holds because the row is shown. A distinct variant is a reversible follow-up if the deferred prune action ever needs to tell the two apart.
- Rewrite three docs:
  - `AgentLiveness::Ended`: the runtime recorded the agent as ended, **or** its last activity is older than `MAX_AGENT_AGE_SECS`. That is an aborted or crashed run's leftover worktree, which is over whatever its lock says (CR-01).
  - `MAX_AGENT_AGE_SECS`: activity older than this (a day) is over. The core classifies such an agent `Ended`, and the Claude adapter's worktree-less pass skips sessions with no spawn this recent. The bound is on **inactivity**, so an agent that keeps writing its transcript is never aged out, however long it runs.
  - The "In order" list in `classify_liveness`'s doc gains the new step.
- Reuse the one constant. Do not add a second threshold (D-C08: thresholds in one place) **[inferred: a day, per 25-VERIFICATION's missing item and 25-REVIEW's patch]**.
- No new I/O, no write, no prune, no unlock (D-B01).

**Test 1, `src/agents/mod.rs` `#[cfg(test)]`, `an_agent_silent_past_the_age_bound_reads_ended`.** Reuse the `facts` and `at` helpers, and convert `MAX_AGENT_AGE_SECS` to the helper's `i64` age with `i64::try_from(..).expect(..)`.
- At exactly `MAX_AGENT_AGE_SECS`:
  - lock `Some(true)` → `Finished`;
  - `Some(true)` with `summary_in_worktree` → `Finished`;
  - `Some(false)` → `Stalled`;
  - `None` → `Stalled`.
- One second past it, all four cases → `Ended`.
- A `ChildAgent` whose `last_activity` is one second past the bound, passed through `classify_child`, → `Ended`.
- Pin `MAX_AGENT_AGE_SECS == 86_400`.

**Test 2, `src/ui/screens/normal.rs` 25-04 test block, `an_aged_out_orphan_leaves_the_status_cell_byte_identical`.** This is the dashboard end of the tracer, and it needs no git.
- Build `ctx_with_aliases(&["orbit", "beta"])`. Make orbit milestone-complete: `completed_phases = 3`, `total_phases = 3`, `milestone = "v1.0"`. Render baselines with `render_selected` at 80 and 120 columns.
- Take `now = SystemTime::now()`. Classify two orphans three days stale through the real public classifier, `crate::agents::classify_liveness(Some(&crate::agents::adapters::Enrichment { last_activity: Some(now - 3 days), lock_released: .., ..Default::default() }), false, now)`: one with a released lock (`Some(true)`) and one with a held lock (`Some(false)`). Assert both are `AgentLiveness::Ended`.
- Build rows with `agent_row(0, released, Some("13-02"))` and `agent_row(1, held, None)`. Derive a view with `crate::agents::waves::derive(&ProjectAgents { rows, scanned_at: Some(now), ..Default::default() }, <orbit's ProjectState from ctx.project_states>)`. Assert `!view.is_active()` and that `summary_forms()` is empty. Insert the view as `ctx.agent_views["orbit"]`.
- Assert that the full rendered frame at 80 and at 120 equals its baseline, and that `status_cell_text` of the orbit row is `v1.0 Complete`.
- Control arm, which proves the view reaches the cell: the same two facts classified at 7 200 s (inside the bound). The held-lock orphan reads `Stalled`, the released-lock one `Finished`. Use a view holding only the held-lock row. Its orbit Status cell reads `1 stalled`, because a crashed run's alert stays until the bound.

**Test 3, `tests/agents_claude.rs`, real worktrees (D-C17: the tempdir root only, clocks injected, mtimes set with `set_modified`).** Add imports `gsd_meta_manager::agents::waves::derive` and `gsd_meta_manager::state_reader::ProjectState`.
- `a_released_lock_orphan_days_stale_reads_ended_and_is_not_active`:
  - Set up with `add_agent_worktree(AGENT_ID, None)`, `write_meta(AGENT_ID, &executor_meta(&wt))` and `write_transcript(AGENT_ID, now, 3 days)`.
  - `only_row` must be `!locked`, with `adapter == Some("claude-code")`, `agent_type` shown as `gsd-executor`, `commits_ahead == Some(0)` and `dirty == Some(0)`. The row is still enumerated, with adapter and git facts (AGENT-01, D-A03, D-C16). Its liveness must be `Ended`.
  - `derive(&scan, &ProjectState::default())` must have `!is_active()` and empty `summary_forms()`.
- `a_locked_orphan_days_stale_reads_ended_not_stalled`:
  - With `add_agent_worktree(AGENT_ID, Some(std::process::id()))` and a transcript 3 days old, the row is `locked` and `Ended`, and the derived view has no summary forms: no `1 stalled`.
  - In a second fixture, a transcript at `MAX_AGENT_AGE_SECS - 60` seconds reads `Stalled`. The exact second is pinned by Test 1; filesystem mtime granularity makes an exact-boundary file test flaky.
  </action>
  <reversibility rating="reversible">Mapping an aged-out agent to `Ended` and bounding it with the existing one-day constant is a single comparison in one function plus docs. Either choice changes by editing that line or the constant.</reversibility>
  <verify>
    <automated>rtk proxy cargo test --lib agents::</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `an_agent_silent_past_the_age_bound_reads_ended` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens::normal</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `an_aged_out_orphan_leaves_the_status_cell_byte_identical` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --test agents_claude --no-fail-fast</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or either `a_released_lock_orphan_days_stale_reads_ended_and_is_not_active` or `a_locked_orphan_days_stale_reads_ended_not_stalled` not listed as `ok`</fails_when>
  </verify>
  <acceptance_criteria>
    - The three new test groups fail before the `classify_facts` edit (the RED run is recorded in the SUMMARY with the observed `Finished` / `1 stalled` / `N agents` output) and pass after it.
    - `classify_facts` in `src/agents/mod.rs` contains the comparison `age > MAX_AGENT_AGE_SECS` on a non-comment line, placed before the `lock_released == Some(true)` test.
    - `AgentLiveness::Ended`'s doc names `MAX_AGENT_AGE_SECS`. `classify_liveness`'s "In order" doc lists the age-bound step.
    - Every pre-existing test in `src/agents/mod.rs`, `tests/agents_claude.rs` and `ui::screens::normal` still passes unchanged. `a_released_lock_with_a_stale_transcript_reads_finished` (121 s) and `a_day_old_session_is_skipped_for_worktree_less_agents_only` in particular are still `ok`.
    - The orphan row in both `tests/agents_claude.rs` tests is present in `scan.rows` with `adapter == Some("claude-code")` and git counts `Some(0)`. Nothing is hidden.
  </acceptance_criteria>
  <done>An orphan whose last activity is older than a day reads `Ended` through the real Claude adapter. Its project's dashboard frame is byte-identical to the no-view frame and reads `v1.0 Complete`. A within-bound stalled orphan still alerts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finished alone never switches the summary on; running agents choose the phase and the ladder (CR-01 rule + WR-01)</name>
  <files>src/agents/mod.rs, src/agents/waves.rs, tests/agents_waves.rs, src/ui/screens/normal.rs</files>
  <read_first>
    - `src/agents/mod.rs` as Task 1 left it (`AgentLiveness`).
    - `src/agents/waves.rs`: the whole file, especially `is_active_liveness`, the vote at the top of `derive` and its doc, `plan_state`, `AgentView::is_active`, `summary_forms` (the `executor_mode` computation and the `N agents` count) and its doc, and the tests `finished_agents_count_as_done_plus_unmerged`, `stalled_and_unknown_only_states` and `three_code_fixers_without_plans_use_the_generic_form`, plus the helpers `row`, `agents`, `state`, `phase_13_state`, `thirteen_live_executors`.
    - `tests/agents_waves.rs`: `phase_repo`, `add_agent_worktree`, `commit_in`, `Scripted`, `released_30s`, `a_summary_committed_in_the_worktree_reads_finished_before_the_merge`.
    - `src/ui/screens/normal.rs`: the Task 1 test `an_aged_out_orphan_leaves_the_status_cell_byte_identical`.
    - `.planning/phases/25-running-agents-live-wave-view/25-REVIEW.md` § CR-01 (the `is_active` rule: "`Finished` still counts inside the wave ladder while a wave is in flight. It just must not switch the ladder on by itself") and § WR-01 (the tiered vote).
  </read_first>
  <behavior>
    - `AgentLiveness::is_running()` is true for `Live` and `Idle`, and false for `Finished`, `Stalled`, `Ended` and `Unknown`.
    - A view derived from only `Finished` rows attributed to phase 13 (`phase_13_state`) has `is_active() == false` and `summary_forms()` empty. Its counts are still derived: `finished` equals the number of those plans.
    - `finished_agents_count_as_done_plus_unmerged` (11 Live + 2 Finished) is unchanged: `P13 · w2/11 · 11 run · 10/35 done`. A `Finished` plan still counts inside a running ladder.
    - `Finished` rows attributed to phase 13 plus one live worktree-less `ChildAgent` give an active view whose `summary_forms()` is exactly `["1 agent"]`, not the ladder. The same holds for one `Stalled` attributed row plus one live worktree-less agent.
    - Two `Stalled` and one `Finished` rows on 12-01..12-03, plus two `Live` rows on 13-01/13-02, with a `ProjectState` holding disk statuses for both phases: `active_phase` is 13, `running == 2`, and the first summary form starts with `P13`.
    - Only the three phase-12 orphans: `active_phase` is 12 (the fallback tier) and `summary_forms() == ["2 stalled"]`.
    - A tie inside the first tier still goes to the higher phase.
    - `tests/agents_waves.rs`: a lone released-lock worktree holding its SUMMARY reads `Finished`, its view is inactive with no forms, and its counts stay `(done, finished, running) == (1, 1, 0)`. After a second worktree on `agent-p13-03-1790386423` (no SUMMARY, so `Live` under `released_30s`), the counts are `(1, 1, 1)` and the first form is `P13 · w2/2 · 1 run · 2/3 done`. Done shown to the user is still main-done plus finished-unmerged.
    - Dashboard: an orphan whose lock is released and whose transcript is 7 200 s old reads `Finished`, and on a milestone-complete project it leaves the frame byte-identical to the no-view baseline, with a Status cell of `v1.0 Complete`.
  </behavior>
  <action>
RED first: add the behaviour tests above, run them, confirm the new ones fail, then implement.

**`src/agents/mod.rs`.** Add `impl AgentLiveness { pub fn is_running(self) -> bool }`, true only for `Live | Idle`. Doc it as the CR-01 rule, in one place beside the thresholds. A running agent is the only thing that:
- switches the dashboard summary on;
- wins the first tier of the active-phase vote;
- selects the executor ladder;
- is counted by the `N agents` form.

`Finished` is done work. It counts as a finished plan inside a ladder that something running switched on, and in a running fixer run, but never switches either on by itself. Unit test `only_live_and_idle_are_running` covers all six variants.

**`src/agents/waves.rs`.** Remove the private `is_active_liveness` helper; its two callers (items 1 and 2 below) switch to `AgentLiveness::is_running`, and items 3 and 4 adopt it too:
1. **`AgentView::is_active`:** any row `is_running()`, or any worktree-less agent (the scan keeps only live ones).
2. **The `N agents` count in `summary_forms`:** rows that are `is_running()` plus worktree-less agents **[inferred: the count answers "how many are running"; otherwise 13 finished executors plus one live researcher would read `14 agents`]**.
3. **`executor_mode`:** `plan_total > 0` and some row that `is_running()` is attributed to the active phase **[inferred: without this, a within-bound orphan plus a live worktree-less agent would put the orphan's phase ladder in the cell]**.
4. **The vote in `derive` (WR-01, [inferred] folded in):**
   - Tier 1 counts attributed rows that `is_running()`.
   - Only when tier 1 is empty does tier 2 count attributed `Finished | Stalled` rows.
   - Otherwise fall back to `state.active_phase_number()`.
   - Keep the existing tie rule: BTreeMap ascending plus `max_by_key`, so a tie goes to the higher phase.
   - `Ended` and `Unknown` never vote. `plan_state` is unchanged: `Finished` plans still count, and a SUMMARY in the worktree still finishes a plan whatever its agent's liveness.

Rewrite the docs on `derive` (the Active-phase bullet), `AgentView::is_active` and `summary_forms` (the mode paragraph: "Executor mode (the active phase has plans, and some Live or Idle row is attributed to one of them) …") to state the rule and cite CR-01 / WR-01. This amends RESEARCH Pattern 6's "show the summary when ≥1 agent is live, idle or finished" **[inferred, per 25-REVIEW CR-01]**. It follows D-C14's own wording, "while agents are running". A consequence to state in the doc: between a wave's last finisher and the orchestrator's merge, nothing is running, so the Status cell shows the project's normal status.

New pure tests in `waves.rs`, with inputs built by hand and no git (the file's spawn rule):
- `a_finished_row_alone_never_switches_the_summary_on`, covering the Finished-only, Finished-plus-worktree-less and Stalled-plus-worktree-less cases;
- `running_agents_outvote_orphans_for_the_active_phase`, covering the 12/13 mix, the fallback tier and the tie.

To build a two-phase `ProjectState`, insert two `DiskInference` entries into `phase_disk_statuses`.

**`tests/agents_waves.rs`.** Rewrite `a_summary_committed_in_the_worktree_reads_finished_before_the_merge` per the behaviour list. Keep the `row.summary_in_worktree` and `Finished` assertions. Replace the single-row summary assertion with the inactive / no-forms assertion. Then add the second worktree, `agent-p13-03-1790386423`, through `add_agent_worktree`, rescan with `released_30s()`, and assert the `(1, 1, 1)` counts and the `P13 · w2/2 · 1 run · 2/3 done` first form. Update the test's doc comment to say why: CR-01, `Finished` alone does not switch the ladder on.

**`src/ui/screens/normal.rs`.** Add `a_finished_orphan_within_the_age_bound_leaves_the_status_cell_byte_identical`, in the same shape as Task 1's test. Classify the released-lock orphan at 7 200 s through `classify_liveness`, assert it is `Finished`, then assert the byte-identical frame at 80 and 120 and `v1.0 Complete`.

After implementing, the existing suites for `ui::screens::detail` and `app::tests` must pass unchanged. No detail test depends on a `Finished`-only view being active. Their fixtures use `Live`, `Idle` and `Ended` rows.
  </action>
  <reversibility rating="reversible">These are local predicate changes inside `waves.rs` plus one small method. Restoring `Finished` to the activation set, or collapsing the vote back to one tier, is a one-line edit each.</reversibility>
  <verify>
    <automated>rtk proxy cargo test --lib agents::</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or any of `only_live_and_idle_are_running`, `a_finished_row_alone_never_switches_the_summary_on`, `running_agents_outvote_orphans_for_the_active_phase`, `finished_agents_count_as_done_plus_unmerged` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --test agents_waves --no-fail-fast</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `a_summary_committed_in_the_worktree_reads_finished_before_the_merge` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `a_finished_orphan_within_the_age_bound_leaves_the_status_cell_byte_identical` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --lib app::tests</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `an_agents_scan_reaches_the_dashboard_status_cell` not listed as `ok`</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/agents/mod.rs` defines `pub fn is_running(self) -> bool` on `AgentLiveness`, matching exactly `Live` and `Idle`.
    - `src/agents/waves.rs` calls `is_running()` in `AgentView::is_active`, in the first vote tier of `derive`, in `executor_mode`, and in the `N agents` count. The crate builds without an unused-function warning; Task 3's clippy gate confirms it.
    - The new tests fail before the implementation and pass after it; the RED run is recorded in the SUMMARY.
    - The pre-existing tests in `agents::waves::tests` pass unchanged; only `tests/agents_waves.rs::a_summary_committed_in_the_worktree_reads_finished_before_the_merge` is rewritten, as described. That includes `finished_agents_count_as_done_plus_unmerged`, `stalled_and_unknown_only_states`, `three_code_fixers_without_plans_use_the_generic_form` and `display_order_is_total_and_stable`.
    - The `derive`, `is_active` and `summary_forms` doc comments cite CR-01, and `derive`'s cites WR-01.
    - The `ui::screens` suite passes, and `app::tests` passes with `an_agents_scan_reaches_the_dashboard_status_cell` unchanged. This includes `the_agent_summary_is_a_whole_ladder_form_at_every_width`, `the_stalled_form_replaces_the_status_cell`, the fixer ladder test and every `detail` Agents sub-view test.
  </acceptance_criteria>
  <done>A view whose only rows are finished or orphaned never switches the Status cell on or picks its ladder. Live agents win the phase over leftovers, and a finished plan still counts inside a live wave.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The fixer estimate needs a running fixer, plus the phase gate</name>
  <files>src/agents/fixers.rs, tests/agents_fixers.rs</files>
  <read_first>
    - `src/agents/fixers.rs`: the module doc, `FixerEstimate.fixers` doc, `is_active_fixer`, and `estimate` with its numbered doc.
    - `src/agents/mod.rs` `AgentLiveness::is_running` (Task 2).
    - `tests/agents_fixers.rs`: `review_repo`, `REVIEW_48`, `add_agent_worktree`, `commit`, `Scripted` and its `enrich` (which hard-codes `lock_released: Some(false)`), `scan`, `counts`, `attributed_or_inactive_fixers_are_not_counted`.
    - `.planning/phases/25-running-agents-live-wave-view/25-REVIEW.md` § CR-01 ("Apply the same rule to `is_active_fixer`").
  </read_first>
  <behavior>
    - A scan whose fixer rows are all `Finished` has `fixer_estimate == None`, and `derive(..).summary_forms()` is empty. This is the `Scripted` adapter with age 200 s and `lock_released: Some(true)`, so every row reads `Finished`. This is the case of an orphaned fix run whose fixers have finished.
    - A scan whose fixer rows are three days stale reads `Ended` and has `fixer_estimate == None`.
    - Two fixer worktrees on the real review repo:
      - FIXER_A carries `fix(12): WR-08 x` and FIXER_B carries `fix(12): CR-01 y`. Take their scanned rows. With both set to `Finished`, `fixers::estimate(&root, main_worktree, base_sha, &rows)` returns `None`.
      - With FIXER_A `Finished` and FIXER_B `Live`, it returns `Some(FixerEstimate { fixers: 2, phase: 12, fixed: Some(2), total: Some(48) })`. A finished fixer's size and unmerged commits still count inside a running fix run.
    - A single hand-built `Finished` fixer row under a nonexistent root returns `None`, so the gate runs before any git call or file read.
    - Every existing test in `tests/agents_fixers.rs` and `agents::fixers::tests` passes unchanged.
  </behavior>
  <action>
RED first: add the tests, confirm they fail, then implement.

**`src/agents/fixers.rs` (D-C12, CR-01).** Keep `is_active_fixer`'s set (unattributed `gsd-code-fixer` rows that are `Live`, `Idle` or `Finished`), because it defines the run's size and whose worktrees' subjects are unioned. In `estimate`, immediately after collecting `fixers` and before `own_subjects` (the first git call), return `None` unless at least one collected row's `liveness.is_running()` is true. That is the same rule as `AgentView::is_active`: `Finished` counts inside a running run and never switches it on.

Rewrite these docs to state the gate and cite CR-01:
- the module doc's "It runs only while an active fixer exists" becomes "while a running (Live or Idle) fixer exists";
- the `FixerEstimate.fixers` field doc;
- `estimate`'s step 1.

**`tests/agents_fixers.rs`.** Give `Scripted` a `lock_released: Option<bool>` field. `Scripted::fixer()` sets `Some(false)`, which preserves every existing test, and `enrich` passes the field through instead of the hard-coded `Some(false)`.

Add `a_finished_fixer_run_yields_no_estimate`, covering the scan cases, the two-worktree hand-edited-liveness cases and the nonexistent-root case from the behaviour list. Take the rows, `main_worktree` and `base_sha` from a real `scan(&root, Scripted::fixer())` result, then change the rows' `liveness` before calling `fixers::estimate`, so the base sha is git's own.

Then run the phase-wide gates.
  </action>
  <reversibility rating="reversible">The gate is one early return in `estimate`. Removing it restores the 25-06 behaviour.</reversibility>
  <verify>
    <automated>rtk proxy cargo test --test agents_fixers --no-fail-fast</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `a_finished_fixer_run_yields_no_estimate` not listed as `ok`, or fewer than 7 tests listed as `ok`</fails_when>
    <automated>rtk proxy cargo test --test agents_scan --no-fail-fast</automated>
    <fails_when>non-zero exit, or "test result: FAILED", or `no_file_under_src_agents_writes_reads_proc_or_spawns` not listed as `ok`</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against` (the known local git-version witness), or fewer than 53 `test result:` lines, or fewer than 2479 passed in total (the 25-06 floor)</fails_when>
  </verify>
  <acceptance_criteria>
    - `estimate` in `src/agents/fixers.rs` calls `is_running()` on a non-comment line, before the first `git_ops::log_subjects` call in the function body.
    - `a_finished_fixer_run_yields_no_estimate` fails before the gate and passes after it; the RED run is recorded in the SUMMARY.
    - The existing tests in `tests/agents_fixers.rs` pass unchanged apart from the added `lock_released: Some(false)` default. These include `a_code_fixer_run_shows_an_estimated_fixed_over_total`, `attributed_or_inactive_fixers_are_not_counted` and `the_phase_comes_from_the_fixers_own_commits_when_the_description_has_none`.
    - `tests/agents_scan.rs::no_file_under_src_agents_writes_reads_proc_or_spawns` passes, so the plan added no write, `/proc` read or spawn under `src/agents/` (D-B01).
    - Clippy is clean. The full suite fails only on the known git-version witness, with at least 53 `test result:` lines and at least 2479 passed.
  </acceptance_criteria>
  <done>An orphaned or finished fix run no longer shows `N fixers`. A running fix run still counts its finished fixers and their commits. The full gate is green, apart from the known local witness.</done>
</task>

</tasks>

<flagged_assumptions>
## Inferred decisions (unattended run; audit these)

- **[inferred] An aged-out agent reads `Ended`, not a new `Orphaned` variant.** This is 25-REVIEW's primary suggestion. Every consumer already treats `Ended` as over, so no renderer, glyph or width changes are needed, and D-C16 still holds. The cost is that "runtime said ended" and "inactive past a day" share one state. Reversible.
- **[inferred] The bound is the existing `MAX_AGENT_AGE_SECS` (a day of inactivity).** One constant, as D-C08 requires. Because the bound measures inactivity, a long-running agent that keeps writing is never aged out. A crashed run's `N stalled` alert therefore persists up to a day, which is the intended signal.
- **[inferred] `Finished` never switches the summary on.** This amends RESEARCH Pattern 6 ("show when ≥1 live, idle or finished") per 25-REVIEW CR-01's fix, and matches D-C14's "while agents are running". The visible consequence: between a wave's last finisher and the orchestrator's merge, the Status cell shows the project's normal status.
- **[inferred] WR-01 is folded in.** Without the tiered vote, a within-bound orphan from another phase still replaces a live wave's ladder, which is the CR-01 defect in another form.
- **[inferred] The executor ladder and the `N agents` count follow running rows.** This closes the last two ways a within-bound orphan shapes the cell (orphan plus a live worktree-less agent). The fixer forms keep counting finished fixers inside a running run, because `fixers` is the run's size and the numerator needs their unmerged commits.
- **Interaction risk with IN-04 (deferred, unconfirmed).** `lock_released: Some(worktree.locked.is_none())` reports "released" for a worktree the runtime never locked. With `Finished` no longer activating, such an agent silent for more than 120 s no longer switches the cell on until it writes again. RESEARCH A1 observed Claude Code locking 13/13 worktrees, so this needs a runtime that does not lock. It is recorded here and not changed.
</flagged_assumptions>

<deferred_review_findings>
## Deferred review findings (not in this plan)

| Finding | Why it is out of scope |
|---|---|
| WR-02: an unbounded git call can freeze the scan | Availability. No must-have promises a scan timeout, and it is independent of liveness classification. |
| WR-03: the fixer estimate reads `-EVAL-REVIEW.md` | Estimate quality (denominator file selection); AGENT-07 requires only a labelled estimate. |
| WR-04: earlier review runs inflate `fixed` | Estimate quality (numerator scope and clamp); independent of orphan aging. |
| WR-05: the long-path prefix fallback adopts another project's directory | Adapter directory resolution; an edge case with no must-have wording. |
| IN-01: leftover-only views redraw every scan | Cosmetic redraw churn. The rendered frame is byte-identical, as this plan proves. |
| IN-02: tests via `registered_adapters()` stat the real `~/.claude` | Test hygiene (D-C17); no behaviour change. |
| IN-03: relative `gitdir:` pointers skip the ledger tier | Attribution tier 5 only. |
| IN-04: "released" for a never-locked worktree | Unconfirmed premise; see the interaction note above. |
</deferred_review_findings>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime-written files → core classifier | transcript mtimes, meta flags and worktree lock state, written by another process, decide what the dashboard claims is running |
| classifier verdict → dashboard Status cell | the user's at-a-glance account of every project; a wrong verdict hides real status, including milestone-complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-28 | Spoofing (status misrepresentation) | `classify_facts`, `AgentView::is_active`, `derive` vote, Status cell | medium | mitigate | Age bound → `Ended`; `Finished` never activates; running rows choose phase and ladder. Pinned by `an_aged_out_orphan_leaves_the_status_cell_byte_identical`, `a_finished_orphan_within_the_age_bound_leaves_the_status_cell_byte_identical`, `a_released_lock_orphan_days_stale_reads_ended_and_is_not_active` and `running_agents_outvote_orphans_for_the_active_phase`. |
| T-25-29 | Tampering (future-dated mtime) | `age_secs` | low | accept | A future mtime is age 0 by D-C08's clock-skew clamp, so a transcript dated ahead reads `Live`. Only a same-user process can set it, the effect is display-only, and no action follows from liveness. |
| T-25-30 | Tampering (destructive repair) | orphaned worktrees | high | mitigate | The fix classifies only. It never prunes, unlocks, removes or writes (D-B01; pruning is a deferred action). Enforced by `tests/agents_scan.rs::no_file_under_src_agents_writes_reads_proc_or_spawns` and the plan prohibitions. |
| T-25-31 | Denial of Service | `fixers::estimate` | low | mitigate | The running-fixer gate returns before any git call or file read. Pinned by the nonexistent-root case in `a_finished_fixer_run_yields_no_estimate`. |
| T-25-32 | Information Disclosure | logs | low | mitigate | No new `tracing` call; the handler still logs counts only (D-C13). |
| T-25-SC | Tampering | package installs | low | accept | No new crates; nothing is installed. |
</threat_model>

<verification>
- `rtk proxy cargo test --lib agents::`, `rtk proxy cargo test --lib ui::screens`, `rtk proxy cargo test --lib app::tests` are green, with every named new test `ok`.
- `rtk proxy cargo test --test agents_claude --no-fail-fast`, `--test agents_waves`, `--test agents_fixers` and `--test agents_scan` are green.
- `rtk proxy cargo clippy -- -D warnings` is clean.
- `rtk proxy cargo test --no-fail-fast`: the only failure is the known local git-version witness, with at least 53 `test result:` lines and at least 2479 passed.
- Gap truth check (25-VERIFICATION gaps[0].missing):
  - item 1, the age bound: `age > MAX_AGENT_AGE_SECS` in `classify_facts`;
  - item 2, the regression: `a_released_lock_orphan_days_stale_reads_ended_and_is_not_active` plus the two byte-identical dashboard tests.
</verification>

<success_criteria>
- CR-01 closed. An orphaned agent worktree stops counting as active: for a released lock, as soon as its transcript is past `LIVE_SECS`; for a held lock, once it is past `MAX_AGENT_AGE_SECS`. The dashboard row returns to byte-identical output.
- AGENT-05 "with no active agents the row is unchanged" holds for aborted and crashed runs.
- AGENT-02's active phase follows the running agents (WR-01). AGENT-07's estimate needs a running fixer. AGENT-01's enumeration still lists every orphan row, and AGENT-06's Agents sub-view shows it as `ended`.
</success_criteria>

<output>
Create `.planning/phases/25-running-agents-live-wave-view/25-07-SUMMARY.md` when done
</output>
