---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 33
type: execute
wave: 23
gap_closure: true
gap_ids: [T-19-122, T-19-123, T-19-124, T-19-125, T-19-126]
depends_on: [19-32]
files_modified:
  - src/envelope/policy.rs
  - src/envelope/ledger.rs
  - src/envelope/cred.rs
  - tests/envelope_word_set.rs
  - tests/envelope_interior_path.rs
  - tests/envelope_carrier_reach.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-04, SAFE-05, SAFE-06]

estimate:
  tokens: 158000
  raw_tokens: 158000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "**THIS ROUND CLOSES THE OTHER TWO WAYS A BOUNDARY CAN BE SILENT, AND AUDIT 12 NAMED BOTH.** *\"the condition enumerates the silences of a READING; a boundary has three ways to be silent, and the code's other two are unnamed.\"* Round 12 got the READING right and inherited two holes it never named: **the WORD SET** — `consume_redirection` (`policy.rs:3532-3609`) deletes a redirection's target word and records it on the `Segment` only under `if pathname_target && target.literal` (`:3581`), so a here-string word is in NEITHER of the two classes `protected_carrier_named` iterates (`:6118-6128`) — and **the PATH SET** — `word_is_within` (`:5685-5711`) is a component-wise PREFIX over `<root>/<alias>` whose `word.len() < dir.len()` early return (`:5706`) answers `false` for every ANCESTOR. **Audit 12 drove both to harm twice each, with a control beside every leg**: a bare remote's `main` moved `c13f9ea` → `371a2f5` and `0658c51` → `140421f`, and a fired PR cap reset to PERMITTED twice. **Both are PRE-EXISTING — the `dd17bfb` control binary permits both — so they are gaps twelve audits did not find rather than regressions.**"
    - "**EVERY ROW `19-32` LEFT RED WAS OBSERVED RED IN COMMITS WITH ZERO `src/` HUNKS, AND THIS PLAN'S FIRST ACTION IS CONFIRMING THEY ARE STILL RED — AND ITS SECOND IS CONFIRMING THE PERMITTED SET IS STILL PERMITTED.** If any row is already green against `19-32`'s post-state, the rule is a rule nobody has shown is needed and that is a finding to report with the measured verdict, not a task to complete. This is the mechanism `19-15`, `19-17`, `19-19`, `19-21`, `19-23`, `19-25`, `19-27`, `19-29` and `19-31` used and it is the one number a run that did nothing cannot forge. **And the converse check is load-bearing four ways this round**: `19-32` pinned `cat <<<x` and `cat <<< /tmp/x` PERMITTED, so a here-string BAN turns them red; it pinned `ls <ENV>/unrelated-sibling`, `<ENV>/beta` and the user-set-root case PERMITTED, so a PREFIX widened to the root turns them red; it pinned `ls <ENV-PARENT>` and `/tmp` PERMITTED, so an ancestor clause that walked to `/` turns them red; and it pinned a 100 000-character no-slash word fast and permitted, so a work bound that simply refused long words turns it red."
    - "**`T-19-122`'s RULE IS A SECOND `Segment` FIELD AND A THIRD WORD CLASS, AND THE FENCED-FILE ENUMERATION DECIDED IT BEFORE THIS PLAN WAS WRITTEN.** Widening `RedirectionOperator::pathname_target` for the five non-pathname operators turns **`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693-10727`)** red — it asserts `targets_of(\"ls <<<here\")`, `(\"ls <<EOF\")`, `(\"ls <<-EOF\")`, `(\"ls >&2\")` and `(\"ls <&0\")` are ALL EMPTY — **and the twelve-operator grammar pin (`:10586`, `:10603`)** red, which asserts `pathname_target` TRUE for the seven and FALSE for the five. **A SECOND field turns NEITHER red**, because `redirection_targets`, `pathname_target`, `redirection_operator`, `skip_redirection_target`, `segment.tokens`, `SEPARATORS` and `is_separator` are all left byte-identical. The word's `text` and `literal` are ALREADY by-products of the walk `skip_redirection_target` performs to find the target's extent, so the second field costs one `String` on a path already walking those characters — **the same argument round 11 made for the first field, applied to the other half of bash's own split.** The rule is chained at the SAME reading site as a THIRD `.chain()`."
    - "**THE PRINCIPLED RESTATEMENT IS THAT THE PREDICATE'S SUBJECT IS WHAT A LINE *NAMES*, NOT WHAT THE SHELL DOES WITH A WORD.** Round 11 justified reading pathname targets because *\"a redirection target does name one\"*; the pathname/non-pathname distinction is a fact about the SHELL's use of the word, and this predicate decides on the word's TEXT. **So the honest word set is every LITERAL word the line contains, whether it survives into argv or is consumed by a redirection, pathname or not** — and `redirection_operator`'s split stays exactly where bash's grammar puts it, read from BOTH sides for different reasons and said so in place. **The third design — treating every deleted word as UNRESOLVABLE — is rejected in writing**: it would deny `cat <<< hello`, `sort <<< \"$x\"` and every fd duplication on the line, which is an outage rather than a boundary (AR-19-11)."
    - "**THE `T-19-122` COST IS DISCLOSED IN ADVANCE AND IS THE SAME FAMILY AS ONE ALREADY DISCLOSED.** `cat <<< <ENV>/alpha/pr-ledger.ndjson` becomes refused — a here-string naming a protected path is refused even where the program would only ever have read it as data — which is exactly the family of `cat <ledger>` being refused, already in the predicate's own cost section. It fails CLOSED and is bounded to words naming this run's own two paths. **`cat <<<x` and `cat <<< /tmp/x` stay permitted and `19-32` pinned both**, which is what keeps the new class a PATH class rather than a here-string ban. **`git <<<x push --force origin main` stays exit 2 `force_push_blocked`** — round 6's and `T-19-97`'s own row — and a change of identifier there is a finding about the FIX."
    - "**`T-19-123`'s RULE IS AN ANCESTOR CLAUSE BESIDE THE PREFIX ONE, BOUNDED AT THE ENVELOPE ROOT, AND WHAT THE PROTECTED PATH SET BECOMES IS STATED EXACTLY.** A candidate is refused when it is **(1) UNDER `<root>/<alias>` component-wise** (the existing PREFIX, unchanged), **(2) a proper ANCESTOR of `<root>/<alias>` that is itself AT OR UNDER `<root>`** (the new clause), or **(3) EQUAL to `current_exe()`** (the existing EQUALITY, unchanged). **It grows the protected set by EXACTLY ONE path**, because `envelope_dir_in`'s alias is a plain single path component — `ledger_path_in` refuses anything else outright — but it is WRITTEN in its general form so it stays correct if the directory ever becomes deeper. **It is a component-vector comparison the other way round and costs one more `if`**, exactly as audit 12 described it, applied per candidate inside the same walk."
    - "**WHERE THE ANCESTOR CLAUSE STOPS IS `<root>`, AND THE REASON IS OWNERSHIP RATHER THAN CONVENIENCE.** `<root>` is `GSD_MM_ENVELOPE_ROOT`, or the default `~/.local/share/gsd-meta-manager/envelope`; this tool creates it and it contains only the alias directories this tool created. **Above it the chain is `~/.local/share`, `$HOME`, `/tmp`, `/`** — directories shared with everything the user has. A clause reaching them would refuse `ls /`, `df /`, `du -sh $HOME` and `ls /tmp`, which is **not a boundary but an outage** (AR-19-11: a refusal a user cannot act on is a control that gets switched off), and it is the SAME argument `word_is_exactly` already gives one path over for keeping the binary half an EQUALITY rather than a prefix over a shared `bin`. **The residue above the root is registered, disclosed, unaccepted and named on the PATH-SET axis of the restated condition** — `rm -rf /tmp` when the root is `/tmp/xyz` reaches the same nine carriers and no rule refuses it."
    - "**IT IS AN ANCESTOR CLAUSE AND NOT THE EXISTING PREFIX WIDENED TO `<root>`, AND `19-32` PINNED THE ROWS THAT TELL THEM APART.** A prefix over `<root>` refuses every sibling alias's directory and every file directly under the root; and because `GSD_MM_ENVELOPE_ROOT` is USER-SETTABLE, a user who sets it to a directory holding unrelated files gets that whole subtree refused, while the ancestor clause refuses only the single word naming the root. **`19-32` pinned `ls <ENV>/unrelated-sibling`, `<ENV>/beta` and the user-set-root case PERMITTED before and after**, so a prefix implementation turns that file red rather than turning a driven run unusable. **A red one there is a finding about the RULE, reported with both verdicts, never an assertion to edit.**"
    - "**THE TWO FENCED PINS MOVE, AND MOVING THEM IS A NAMED, BOUNDED, CROSS-FENCE EXCEPTION WITH EACH REASONING REWRITTEN RATHER THAN DELETED.** `policy.rs:10022-10026` pins `word_is_within(\"/tmp/envroot\", &dir) == false` with the stated reason *\"the PARENT of the envelope directory is not under it. The boundary is the directory this run owns, not everything beside it\"*, and `tests/envelope_interior_path.rs:2341` pins the same row inside `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule`. **Audit 12 verified both GREEN and NON-VACUOUS and said plainly that the code does exactly what the pin says — what is missing is the threat row and the residue clause, not the implementation — and that moving the pin is a design change and not a test edit.** So each row MOVES from the negative list to the positive list **with its reasoning REWRITTEN in place under the WR-02 discipline**: why it was right when written (the boundary was the directory this run owns), what changed (the harm the permit admits was measured — `rm -rf <root>` took the same nine carriers), and what is true now (the boundary is the directory this run owns PLUS its ancestors up to the root this run was given, and no further). **This is the shape round 11 used for `direction_i_…_stays_permitted`, which needed a human authorisation; this plan carries that authorisation explicitly, names both rows, and moves NOTHING ELSE under `tests/` that is not a comment.**"
    - "**NO ANCESTOR CLAUSE IS WRITTEN FOR THE BINARY, AND THE HONESTY GAP THAT LEAVES IS REPAIRED IN `word_is_exactly`'s OWN DOC.** `rm -rf <binary-parent>` is exit 0 and removes the binary — audit 12 measured it and added it to `T-19-116`'s route list. `word_is_exactly`'s permit is explicitly reasoned (`ls <parent>` and every `cargo install` must stay permitted, and a prefix over a shared `bin` would refuse both) **but the reasoning argues only that a PREFIX would be wrong; it never states that the parent's own DELETION removes the binary.** That is an HONESTY gap rather than a rule gap: the residue is real, the reason for leaving it is sound, and the doc must say BOTH. The repair states the residue in place, at the same weight as the reasoning, and `19-32`'s corpus RECORDS the row in neither direction because no rule is written for it."
    - "**`T-19-124`'s BOUND IS ON WORK RATHER THAN ON INPUT, AND AUDIT 12'S OWN PARENTHETICAL REMEDY IS INSUFFICIENT — WHICH `19-32` DERIVED IN WRITING.** The suggestion was *\"a cap on the number of `/`-anchored candidates per word (or an early return once a candidate is shorter than the shortest protected path, which is the same `word.len() < dir.len()` test hoisted to the byte level)\"*. **A byte floor prunes only the cheap end of a quadratic**: for the measured 16 KB word with 8 000 slashes and an envelope directory of about 18 bytes it skips NINE candidates of EIGHT THOUSAND, and the measured 5 873 ms does not move. **And a candidate CAP is a bound on work only if it is over a quantity the request cannot multiply** — a per-WORD cap is amplified by the word count and a per-SEGMENT cap by the segment count, so a 1 MiB request of many medium words each spending exactly the cap defeats either. **So the property is LINEARITY — the scan's work must be a linear function of the word's length, which is what makes `MAX_GUARD_REQUEST_BYTES` a genuine bound on work rather than only on input — with a FAIL-CLOSED work ceiling beside it as the backstop**, refusing at `ParkReason::EnvelopeAssertionFailed` rather than overrunning the deadline if a later edit reinstates a super-linear scan. **The ceiling must be DRIVABLE and driven, because a ceiling that cannot fire is a control that cannot fail.** `MAX_LEDGER_BYTES`' value, `GUARD_TIMEOUT_SECS` and `MAX_GUARD_REQUEST_BYTES` are all untouched — **a bound over the wrong property is not corrected by moving a number.**"
    - "**`T-19-124`'s FIX MUST MOVE NO VERDICT, AND THE PROOF IS MECHANICAL RATHER THAN ARGUED.** Whatever shape the linear reformulation takes, it must compute, for every `/`-anchored candidate of a word, exactly the component vector `lexical_absolute_components` computes for it today — **including the leading-`./` strip, the `.` drop, the repeated-separator collapse and the TEXTUAL `..` collapse, and including `..` at the root collapsing to the root.** `containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` are the two standing mechanical proofs and both must stay GREEN; **`19-32`'s cost half and rounds 4 through 12's whole pin family are the behavioural proof.** A verdict that moves is a finding about the FIX, reported with both answers, never an assertion to edit. **If the reformulation cannot be written without moving a verdict, SEVER Task 3 and report it** — `T-19-124` is `medium` and non-blocking, and the honesty repairs land either way."
    - "**ROUND 3'S PRINCIPLE IS DISCHARGED RATHER THAN WEAKENED, AND THE DESIGN IS CONSTRAINED SO IT CANNOT BE OTHERWISE.** The third word class is a third `.chain()` on the iterator `protected_carrier_named` already builds from the ONE segment its caller holds; the ancestor clause is a second comparison inside the same per-candidate walk; the linear reformulation is a different way of computing the SAME candidate set inside the SAME predicate. **There is no second pass over the segments, no second scan of the argv, no new `ParkReason`, no change to `Token`, `Segment::tokens`, `SEPARATORS` or `is_separator`, and no third reading site.** `hooks.rs` is NOT OPENED AT ALL — `guard`, `guard_in` and `classify_segments` already thread the binary, the root and the segment. **If the design needs a second reading site, a `hooks.rs` change, or a work budget threaded across the guard call, STOP and report it as a finding**; a budget that must be threaded is evidence the bound is over the wrong quantity, which is `T-19-124`'s own shape arriving in the round that fixes `T-19-124`."
    - "**EVERY NEW HELPER GOES INSIDE THE NO-FILESYSTEM SLICE, AND A POSITIVE CONTROL NAMES EACH.** `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so` (`policy.rs`) slices from the literal `fn lexical_absolute_components(word: &str)` to the `#[cfg(test)]` sentinel and asserts `canonicalize`, `read_link`, `symlink_metadata`, `metadata`, `current_dir`, `current_exe`, `exists` and `Command::new` appear NOWHERE in the sliced CODE, with three positive controls plus the fourth `19-31` added for `slash_anchored_candidates`. **Every helper this plan adds — the ancestor comparison and the linear candidate normaliser — MUST be placed AT OR AFTER that anchor so it falls INSIDE the slice**, and a POSITIVE CONTROL NAMING EACH is added, so a future slice that missed one fails loudly rather than certifying the head of the region. **`lexical_absolute_components`' SIGNATURE TEXT is the anchor and is load-bearing beyond its behaviour**; it does not change."
    - "**THE RESIDUE IS RESTATED OVER ALL THREE AXES, WITH NO COUNT ON ANY AXIS, AND IT IS WRITTEN LAST SO IT RECORDS AN OBSERVED OUTCOME RATHER THAN PREDICTING ONE.** **AXIS 1, WHICH WORDS REACH THE READER**: every LITERAL non-operator token, every LITERAL PATHNAME redirection target, and every LITERAL NON-PATHNAME redirection target — silent about a word a production that failed to complete produced, which fails closed at `Token::redirection_unresolvable` rather than being read. **AXIS 2, WHAT THE READER SEES IN A WORD IT RECEIVED**: every `/`-anchored substring of the word's text, normalised — silent about a word the SHELL MAY REWRITE, a word whose TEXT CARRIES NO ABSOLUTE PATH ANYWHERE, and a word that reaches a protected path ONLY THROUGH A LINK. **AXIS 3, WHICH PATHS THE COMPARISON PROTECTS**: under the envelope directory, an ancestor of it at or under the envelope root, and equal to this run's own binary — silent about an ancestor ABOVE the root and about the binary's PARENT and every ancestor of it, because refusing a word naming one of those is an outage rather than a boundary. **NO COUNT is written on any axis** (audit 11's own recommendation, and `T-19-107`'s registered shape in a shorter sentence), **the spellings are INSTANCES**, and **none of it is handed to a pin, a schedule or a version witness** — `19-25`'s witness schedules a re-audit against a FUTURE GIT and every clause here is reachable by the driven agent TODAY."
    - "**THE FOUR HONESTY REPAIRS EACH LAND WHETHER OR NOT ANY RULE DOES, AND EACH PREFERS A CLAIM THAT STAYS TRUE TO ONE THAT MUST BE MAINTAINED.** *(a)* **`T-19-125`** — the disclosed cost (`policy.rs:6044-6048`) says *\"contains … as a `/`-anchored substring\"* and the comparison NORMALISES: `dd of=<ENV>/./alpha/x` and `dd of=<ENV>/zzz/../alpha/x` are both exit 2 and neither contains `<ENV>/alpha` as a substring at all. The corrected surface is *any word one of whose `/`-anchored substrings NORMALISES to a path under this run's directory, to an ancestor of it at or under this run's root, or equal to its binary*. *(b)* `envelope_carrier_refusal`'s *\"To proceed: name a path outside that directory\"* is STALE for that case, where the word merely CONTAINS the directory and the program would never have opened it — and it must ALSO become true of an ancestor word, whose recovery is different again. *(c)* **`T-19-126`(i)** — `ledger.rs`'s comment says the API name is left unspelled so a grep gate does not pass vacuously, and that reason is **INVERTED**: writing the literal to forbid it turns a zero-occurrence gate RED. `19-32` replaced the one-shot with a PRODUCTION-SLICE pin that CAN name the API, because the name sits below the `#[cfg(test)]` sentinel and outside the slice, and with a BEHAVIOURAL pin for the property the gate stood in for. The comment is corrected to name the standing pins. *(d)* **`cred.rs`'s WHAT IT DOES NOT COVER list has TWO items and omits the alias-body route** — `git -c alias.q='!git -c credential.helper=store credential fill' q` is exit 0 and returned the ambient secret under full envelope posture. **That is `T-19-86`'s route; audit 12 did not fold it in and neither does this plan** — the item is ADDED to the list, naming the route and the rule that does not reach it, with the attribution left exactly where audit 12 put it."
    - "**THE `SEPARATORS` CITATION IS STALE IN THREE FENCED FILES AND THE SWEEP IS A COMMENT-ONLY, MECHANICALLY-GATED CROSS-FENCE EXCEPTION.** The constant is BYTE-IDENTICAL and has moved to **`policy.rs:2422`** because round 12 added prose above it; `git log -L 2422,2422:src/envelope/policy.rs` returns exactly ONE commit for the whole phase, **`84a9b05`** (plan 19-05), and the old line now returns `af72137` and says nothing about `SEPARATORS`. **The claim is TRUE and the citation is stale** — a citation to re-derive rather than a mechanism that moved. Three sites under `tests/` carry it and all three are in files this phase fences. **The sweep is authorised here, is COMMENT-ONLY, changes not one assertion, and is gated mechanically**: every deleted line under `tests/` must be a comment line or one of the two authorised ancestor-pin rows, and the total is bounded."
    - "**ROUNDS 4 THROUGH 12'S MECHANISMS MUST NOT BECOME DEAD CODE, AND THIS ROUND PUTS TWO OF THEM UNDER DIRECT PRESSURE — WHICH IS ITSELF A CLAIM TO VERIFY RATHER THAN ASSERT.** `SEPARATORS` stays byte-identical at `policy.rs:2422` and `is_separator(\">\")` stays `false`; **`segment.tokens` stays byte-identical and the SEGMENT-COUNT pins stay green and falsifiable in both the SPLIT and the DISPLACED variant — round 6's deletion model is the mechanism the third word class sits closest to, and its over-deletion control `git x2>/tmp/o push --force origin main` must stay exit 0 AS EXACTLY ONE SEGMENT**; `Token::literal`, `Token::expansion`, `Token::word_splitting_flush`, `Token::brace_splice`, `Token::redirection_unresolvable`, `Segment::head_is_command_position`, `Segment::brace_spliced` and `Segment::redirection_targets` all keep their present meanings exactly; **round 11's redirection channel is the mechanism the ancestor clause sits closest to and its `: > <ENV>/alpha/pr-ledger.ndjson` row and both near-miss controls must not move**; rounds 5, 7, 8, 9 and 10's clauses stay non-dead with their controls at exit 0 and both `T-19-86` `!`-bodied rows at exit 0; and round 12's own — the interior scan, `containment_holds_…`, the `credential.helper` clause with its five near-miss controls PERMITTED at the same identifier, and the ledger KIND check at all four kinds. **Not one of `envelope_config_resolution`'s THIRTY verdicts may move.**"
    - "**THIS PLAN OPENS THREE `src/` FILES AND FOUR `tests/` FILES, AND EVERY ONE IS A NAMED, NARROW, BOUNDED EXCEPTION STATED AS AN EXCEPTION.** **`policy.rs`** — the second `Segment` field and its recording arm inside the existing `consume_redirection`; the third word class chained at the existing site; the ancestor comparison placed inside the no-filesystem slice; the linear candidate normaliser, same placement; the predicate's doc (the three-axis condition, the widened word set, the third path-set member, the corrected disclosed cost); `word_is_exactly`'s parent-deletion residue; `envelope_carrier_refusal`'s two recovery lines; the two authorised unit-pin moves; and additions inside the existing `mod tests`. **`ledger.rs`** — ONLY the inverted sentence in `record_and_check_in`'s KIND-check comment. **`cred.rs`** — ONLY the WHAT IT DOES NOT COVER list, which gains one item. **`tests/envelope_word_set.rs`** and **`tests/envelope_interior_path.rs`** — additions, plus the ONE authorised ancestor-pin move in the latter. **`tests/envelope_carrier_reach.rs`** and **`tests/envelope_wrapper_class.rs`** — COMMENT-ONLY citation corrections and nothing else. **`hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` are NOT OPENED AT ALL.**"
    - "**`SECTION_ENVELOPE` IS NOT OPENED AND ITS HEADROOM IS NOT SPENT.** Audit 11 verified the first `Guaranteed` clause TRUE at **211 whitespace tokens of an UNRAISED 215 cap**, widest line **74** of 80, and the `Not guaranteed` half already generalises to *\"the files and the binary this envelope runs on\"*, which covers both of this round's routes. **\"As started\" states what the envelope ESTABLISHES and stops**, so neither new class falsifies it. `19-32` re-measured all three numbers. **This plan changes nothing in `advisory.rs` and the diff must show zero lines there.**"
    - "**`T-19-17r` STAYS OUTSTANDING AND THIS PLAN DOES NOT RESOLVE IT EITHER.** `grep -cE '^\\| AR-19-13 \\|'` over `19-SECURITY.md` is **0**. Audits 5 through 12 and plans 19-24 … 19-32 all confirmed the measurement and both pins and all seventeen explicitly declined. **This plan is the eighteenth to decline, because accepting a risk is a human decision.** `AR-19-04` and `AR-19-05` are NOT un-accepted, re-rated or renumbered, and **`T-19-23` is NOT marked closed or re-closed.**"
    - "**`/gsd-secure-phase 19` IS NOT CLEARED BY THIS PLAN.** `T-19-86` remains OPEN at `high` by explicit user scoping decision, with `T-19-111` kept OUT of it as `19-27` moved it. `T-19-91` remains OPEN at `high`. `T-19-111` remains OPEN at `high` with NO rule. `T-19-112` is narrowed further and still not closed. `T-19-121` remains a bounded residue of a control that landed. **`T-19-116` is NARROWED and NOT closed, and that is stated at the same weight as the narrowing**: the third word class closes `xargs cp /bin/true <<< <BINARY>`, and `rm -rf <binary-parent>` stays exit 0 because no ancestor clause is written for the binary, beside the four original residues. `T-19-115` still gets no rule; it appears on axis 2 of the restated condition. **Whether `T-19-122` … `T-19-126` close is audit 13's judgement rather than this plan's claim: this plan says NARROWED-or-CORRECTED and states what remains open.** **Audit 12's finish-line sentence is NOT written by this plan** — whether the open set has become the human-decision items plus the deliberately-unruled residues is audit 13's to say. The SUMMARY must say all of that FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT begin any fix before confirming, against `19-32`'s recorded post-state, that every RED row `19-32-SUMMARY.md` names is STILL RED and that every PERMITTED row is STILL PERMITTED, and recording both observations verbatim. If any RED row is already green, report it as a finding with the measured verdict and the reason identifier rather than proceeding — a rule certified by a corpus that could not fail on its class is the exact defect thirteen rounds of this phase have been about."
      category: honesty
    - statement: "MUST NOT implement `T-19-122` by widening `RedirectionOperator::pathname_target`, by changing `redirection_operator`'s twelve-operator match, by recording a non-pathname target onto `Segment::redirection_targets`, or by adding an operator to `SEPARATORS`. **Design (A) turns `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693`) and the twelve-operator grammar pin (`:10586`, `:10603`) RED**, both of which are fenced pins this plan may only ADD beside. The mandated design is a SECOND `Segment` field carrying LITERAL NON-PATHNAME redirection target words, recorded in the SAME `consume_redirection` arm from the SAME `skip_redirection_target` by-product, and chained as a THIRD word class at the SAME reading site."
      category: safety
    - statement: "MUST NOT refuse a command merely because it carries a non-pathname redirection. The third word class decides on the WORD'S PATH exactly as the other two do, and `19-32` pinned `cat <<<x` and `cat <<< /tmp/x` PERMITTED and `git <<<x push --force origin main` at exit 2 `force_push_blocked`. **A red one there is a rule that banned a channel instead of reading a path**, and it is a finding about the FIX reported with both verdicts, never an assertion to edit."
      category: safety
    - statement: "MUST NOT implement `T-19-123` by widening the existing PREFIX from `<root>/<alias>` to `<root>`, by comparing against a shorter directory, or by any clause that walks the ancestor chain above `<root>`. **`19-32` pinned `ls <ENV>/unrelated-sibling`, `<ENV>/beta` and the user-set-`GSD_MM_ENVELOPE_ROOT` case PERMITTED, and `ls <ENV-PARENT>` and `/tmp` PERMITTED**, so both mistakes turn that file red. The mandated design is an ANCESTOR clause BESIDE the prefix one, over the same component vectors the other way round, bounded at `<root>`."
      category: safety
    - statement: "MUST NOT write an ancestor clause, a prefix, or any widening for the BINARY or for any ancestor of it. `word_is_exactly` stays an EQUALITY; `ls <BINPAR>`, `cp /bin/true <BINPAR>/some-other-file` and every `cargo install` stay permitted. **`rm -rf <binary-parent>` stays exit 0, is a `T-19-116` route audit 12 registered, and this plan repairs only the DOC that never said the parent's own deletion removes the binary.** MUST NOT describe that repair as a mitigation."
      category: safety
    - statement: "MUST NOT bound `T-19-124` by lowering `MAX_GUARD_REQUEST_BYTES`, by refusing long words, by capping the word count, by capping the segment count, or by any cap whose quantity the request can multiply. **A bound over the wrong property is not corrected by moving a number** — that is `T-19-120`'s own shape, which this round exists partly to avoid repeating. The property is LINEARITY of the scan's work in the word's length, with a FAIL-CLOSED work ceiling as the backstop. `MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and `MAX_LEDGER_BYTES` keep their values."
      category: safety
    - statement: "MUST NOT add a work budget threaded through `guard`, `guard_in` or `classify_segments`, MUST NOT open `src/envelope/hooks.rs` at all, and MUST NOT introduce a second reading site, a second pass over the segments, a second scan of the argv, a filter after `resolve_program_with_head` returned, a new arm in `resolve_program`, a change to `ProgramResolution`, or a new `ParkReason`. **If the bound appears to need threading, that is a finding to report — it means the bound is over the wrong quantity.** `resolve_program`, `resolve_program_with_head`, `first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api`, `forbidden_repo_path` and `forbidden_repo_prefixes` are NOT touched."
      category: safety
    - statement: "MUST NOT let the linear reformulation change one component vector. For every `/`-anchored candidate of every word it must compute exactly what `lexical_absolute_components` computes today — the leading-`./` strip, the `.` drop, the repeated-separator collapse, the TEXTUAL `..` collapse and `..` at the root collapsing to the root. `containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` must stay GREEN, and `19-32`'s cost half must stay exit 0. **A moved verdict is a finding about the FIX. If it cannot be written without moving one, SEVER Task 3 and report it** — `T-19-124` is `medium` and non-blocking and the honesty repairs land either way."
      category: safety
    - statement: "MUST NOT ship a fail-closed work ceiling that cannot fire. **A ceiling no test drives is a control that cannot fail** — the exact defect `T-19-126`(i) records one file over. Its value must be reachable under `MAX_GUARD_REQUEST_BYTES`, a test must DRIVE it and observe the refusal at `ParkReason::EnvelopeAssertionFailed`, and the over-refusal it creates must be disclosed in the predicate's own cost section."
      category: honesty
    - statement: "MUST NOT apply any candidate scan, ancestor comparison or new word class before the `Token.literal` filter, and MUST NOT weaken, move or bypass that filter. `protected_carrier_named` filters `!token.operator && token.literal` before any word reaches the path halves, and the second `Segment` field must be populated only for a target the walk already found `literal`. **A candidate produced from a non-literal word would turn the tilde, glob, brace and expansion-borne rows RED in every word position** — rows this phase has pinned PERMITTED for three rounds and for which no rule can be written."
      category: safety
    - statement: "MUST NOT change `lexical_absolute_components`' SIGNATURE text. `fn lexical_absolute_components(word: &str)` is the literal slice anchor of `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so`; a changed signature makes the slice unfindable and every absence assertion below it certify nothing. **Every helper this plan adds MUST be placed AT OR AFTER that anchor so it falls INSIDE the slice, and a POSITIVE CONTROL NAMING EACH is added.**"
      category: safety
    - statement: "MUST NOT call `canonicalize`, `read_link`, `metadata`, `symlink_metadata`, `exists`, `current_dir`, `current_exe`, `Command::new` or any other filesystem or process API from any new helper, from any path half, from `protected_carrier_named` or from anything in the sliced region; MUST NOT shell out from the guard's own path; and MUST NOT read a config file or consult the environment at guard time. The predicate stays PURE and normalises LEXICALLY, following no link. Every probe lives in a TEST."
      category: safety
    - statement: "MUST NOT clear, repurpose or widen `Token::literal`, `Token::expansion`, `Token::word_splitting_flush`, `Token::brace_splice`, `Token::redirection_unresolvable`, `Segment::head_is_command_position`, `Segment::brace_spliced`, `Segment::splice_can_produce_governed` or `Segment::redirection_targets`, and MUST NOT alter `segment.tokens`, `Token`, `SEPARATORS` or `is_separator`. `policy.rs:2422` has ONE commit in the whole phase (`84a9b05`), `is_separator(\">\")` stays FALSE by construction, and the SEGMENT-COUNT pins assert exactly ONE `Segment` with exactly the surviving argv for both the `>/dev/null` and the `x2>` rows. **A red one is a finding about the FIX, reported with the observed segment list, never an assertion to edit.**"
      category: safety
    - statement: "MUST NOT add a new `ParkReason` variant, and MUST NOT use `HookBypassBlocked` or `PrCapExceeded` for any new refusal. A command naming a path in this run's protected set, an ancestor of that path at or under this run's root, and a word whose candidate scan cannot be completed inside the deadline are all `ParkReason::EnvelopeAssertionFailed` — the general unresolvable identifier the sibling refusals already carry and the one `19-32`'s rows are asserted at. `HookBypassBlocked` names a hook-path mechanism and `PrCapExceeded` names a cap that fired; attributing a refusal to a mechanism that did not produce it is D-24, the defect `19-21`'s executor reported as a blocker."
      category: safety
    - statement: "MUST NOT quote the command back in any refusal message (SAFE-04). Naming the envelope DIRECTORY, the envelope ROOT, the BINARY's own path and the LEDGER's path is permitted and required. **Each recovery step must be one the agent or a human can act on** (AR-19-11) — and the recovery line must now be true for THREE cases: a word naming a path under the directory, a word naming an ancestor at or under the root, and a word that merely CONTAINS one of them where the program would never have opened it. `git config --get core.hooksPath` and `command -v` still report the paths."
      category: safety
    - statement: "MUST NOT claim in any doc, message or record that the widened rule is fail-closed, that it CLOSES `T-19-112`, `T-19-113`, `T-19-115`, `T-19-116`, `T-19-121`, `T-19-122`, `T-19-123`, `T-19-124`, `T-19-125` or `T-19-126`, or that its silence is a refusal. **It fails OPEN under a stated CONDITION over three axes, and not one instance of that condition has an automated control.** **MUST NOT hand any residue to a pin, a schedule, a version witness or any other control**, and MUST NOT create a revisit condition for them. **MUST NOT write a COUNT of the silences on any axis.** **MUST NOT write audit 12's finish-line sentence** — whether the open set has become the human-decision items plus the deliberately-unruled residues is audit 13's judgement, and writing it here would be this phase's own certification defect in its purest form."
      category: honesty
    - statement: "MUST NOT edit or delete any row of `tests/envelope_control_carrier.rs`, `tests/envelope_reparsed_value.rs`, `tests/envelope_config_resolution.rs`, `tests/envelope_callee_grammar.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs`, `tests/envelope_pr_cap.rs`, `tests/envelope_credential.rs`, `tests/envelope_advisory.rs`, `tests/envelope_wiring.rs`, `tests/envelope_hook_refusals.rs`, `tests/envelope_tracer.rs` or `tests/envelope_wrapper_bypass.rs` to make a fix pass. **The permitted test writes are ADDITIONS ONLY, plus exactly TWO authorised exceptions**: the ancestor-pin MOVE in `tests/envelope_interior_path.rs` and the COMMENT-ONLY `SEPARATORS` citation corrections in `tests/envelope_carrier_reach.rs` and `tests/envelope_wrapper_class.rs`. **Every other deleted line under `tests/` is a finding about the RULE**: report it with the row, the expected verdict and the measured one, and change the rule or report the plan as inconclusive — never the assertion."
      category: honesty
    - statement: "MUST NOT move the two ancestor pins silently, as a test edit, or with their reasoning DELETED. `policy.rs:10022-10026` and `tests/envelope_interior_path.rs:2341` each move from the negative list to the positive list **with the reasoning REWRITTEN in place under the WR-02 discipline** — why it was right when written, what changed, and what is true now — the shape round 11 used for `direction_i_…_stays_permitted`. **Both moves are named in the record as a cross-fence exception, and no third row moves.**"
      category: honesty
    - statement: "MUST NOT fold the new pins into an existing `#[test]` fn. This is load-bearing arithmetic rather than style: turning a failing test green leaves `passed + failed` unchanged, so NEW `#[test]` fns are the ONLY thing that makes this plan's gate arithmetically satisfiable. Folding them in makes the gate unsatisfiable — round 3's failure mode, which only execution caught."
      category: honesty
    - statement: "MUST NOT place a `#[cfg(test)]` line anywhere in `src/envelope/policy.rs` above the existing sentinel, or a second one anywhere in `src/envelope/ledger.rs` or `src/envelope/cred.rs` above theirs. The anti-vacuity stripper truncates its view at the FIRST one and each file's sentinel-count assertion forbids a second. **`19-32`'s `ledger.rs` production-slice pin depends on that sentinel being where it is.** MUST NOT lower `POLICY_MIN_PRODUCTION_BYTES` or `HOOKS_MIN_PRODUCTION_BYTES`, weaken the proportional floor's VALUE, the deep anchor or the sentinel-count assertions, or economise on `policy.rs`'s prose to protect a floor."
      category: safety
    - statement: "MUST NOT open any part of `src/envelope/ledger.rs` other than the inverted sentence in `record_and_check_in`'s KIND-check comment. The KIND check itself, the size bound, the `Err` arm, `tally`, `append_entry`, `ends_mid_line`, `parse_stamp`, `saturating_bump`, `ledger_path_in`, `describe_file_kind`, `MAX_LEDGER_BYTES` and `WINDOW_SECS` are untouched, and the link-following stat is NOT replaced by the non-following variant. MUST NOT open any part of `src/envelope/cred.rs` other than the WHAT IT DOES NOT COVER list; `hooks_path_env`'s injected pairs, the `credential fill` criterion table, `write_gitconfig`, `write_gitconfig_in`, `gitconfig_body`, `config_env` and `build_env_in` are untouched — audit 11 verified that control CLOSED `T-19-118` as scoped and it must not be disturbed."
      category: scope
    - statement: "MUST NOT open `src/envelope/hooks.rs`, `src/envelope/mod.rs`, `src/envelope/advisory.rs`, `src/scan.rs` or `src/config.rs`. `SECTION_ENVELOPE`'s first `Guaranteed` clause is TRUE at 211 tokens of an unraised 215 cap and *\"As started\"* means neither new class falsifies it. **Headroom that is spent cannot be got back.**"
      category: scope
    - statement: "MUST NOT fix, narrow, re-scope or re-classify `T-19-86`, and MUST NOT fold `T-19-111` into it. **The `!`-bodied alias credential route is `T-19-86`'s and is ADDED TO `cred.rs`'s NOT-COVERED LIST as a route the named rule does not reach — it is NOT folded into `T-19-86`'s declared harm and NOT used to re-rate it**; audit 12 declined and so does this plan. MUST NOT close, re-scope or add a remedy for `T-19-91`. MUST NOT write a rule for `T-19-111`, `T-19-115`, `T-19-96` or `T-19-110`. MUST NOT re-open `T-19-74` (core rows frozen). MUST NOT write a rule, pin or doc claim for `.git/config`, `.claude/settings.json`, `.git/info/exclude`, the run journal or `~/.config/gsd-meta-manager/config.json`, and MUST NOT clamp `pr_cap_per_24h` or `pr_cap_per_run` — `C-11` … `C-15` keep control (e), and `19-32` RECORDED every one of those rows precisely because no rule is planned."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85`. Do not move `park_refusal` above the `?` writes in `deny`, do not add park-coverage controls, and do not touch `scan.rs` or `config.rs`. MUST NOT repair `C-08`'s claimed-but-unwired second carrier. **`C-08`'s, `T-19-120`'s and `T-19-124`'s behavioural halves stay UNMEASURED and are claimed in NEITHER direction.** MUST NOT remove `--hostname` from `FORGE_VALUE_OPTS` or expand into the `glab --host` cell; **`glab` is confirmed NOT INSTALLED** and a pin that would skip is a fail-open pin."
      category: scope
    - statement: "MUST NOT un-accept, re-open or re-rate `AR-19-04` or `AR-19-05`, MUST NOT add, edit or renumber any Accepted-Risks-Log row, and **MUST NOT mark `T-19-23` closed or re-closed.** MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, MUST NOT create `AR-19-13`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere. Seventeen agents have deliberately left that acceptance unmade; this plan is the eighteenth to decline."
      category: honesty
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — `Cargo.toml` and `Cargo.lock` appear in no phase-19 commit. Every probe uses `std::process::Command` inside `#[cfg(test)]`."
      category: honesty
    - statement: "MUST NOT report a green run of any control without `19-32`'s recorded RED output beside it in the SUMMARY, and MUST NOT create a control and its fix in the same commit. The commits here are all fixes; the RED is `19-32`'s and is confirmed still red before the first of them."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Gate on `passed + failed` against the exact total `19-32-SUMMARY.md` records, and state the arithmetic: a red test RAN, so red→green leaves the total unchanged and any increase comes ONLY from new `#[test]` fns. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy` (D-34). **All NINETEEN `envelope_*` binaries must have RUN.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the four documented flakes — two `driver_reattach`, the `envelope_tracer` `ExecutableFileBusy` race and the ETXTBSY race over the binary — and MUST NOT treat their absence from a run as a regression. **The ETXTBSY race is `C-10`'s own seam and this round changes what the guard does about that seam**, so a firing here is RECORDED with its verbatim output and its relation to this round claimed in NEITHER direction. MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lints; the gate is `cargo clippy -- -D warnings`."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, the plan-19-21 blocker-row resolution record, or any subsection plans 19-13 … 19-32 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-33 execution record."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere, and MUST NOT write that `T-19-116`, `T-19-121`, `T-19-122`, `T-19-123`, `T-19-124`, `T-19-125` or `T-19-126` is CLOSED. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86`, `T-19-91`, `T-19-111`, `T-19-112`, `T-19-116` and `T-19-121` all remain OPEN at `high`. **State plainly that this plan does NOT clear `/gsd-secure-phase 19`.**"
      category: honesty
  artifacts:
    - path: "src/envelope/policy.rs"
      provides: "a SECOND `Segment` field carrying LITERAL NON-PATHNAME redirection target words, recorded in the SAME `consume_redirection` arm from the SAME `skip_redirection_target` by-product, with `redirection_targets`, `pathname_target`, `redirection_operator`, `segment.tokens`, `SEPARATORS` and `is_separator` byte-identical; a THIRD word class chained at the ONE existing reading site; an ANCESTOR comparison beside the PREFIX one, bounded at the envelope root, placed INSIDE the no-filesystem slice with a positive control naming it; a LINEAR candidate normalisation with a FAIL-CLOSED work ceiling beside it, same placement, same control; the predicate's doc rewritten in place — the residue restated as a CONDITION over ALL THREE AXES with NO count on any axis and handed to no control, the word set widened, the path set gaining its third member with the stop and the residue above it stated, and the disclosed cost corrected to say NORMALISES and gaining the two new members; `word_is_exactly`'s doc gaining the parent-deletion residue at the same weight as its prefix reasoning; `envelope_carrier_refusal`'s two recovery lines made true for all three cases; the TWO authorised unit-pin moves with their reasoning REWRITTEN under WR-02; and new `#[test]` fns inside the existing `mod tests`"
      contains: "protected_carrier_named"
    - path: "src/envelope/ledger.rs"
      provides: "ONLY the corrected sentence in `record_and_check_in`'s KIND-check comment — the stated reason was INVERTED, writing the forbidden API's literal name to forbid it turns a zero-occurrence gate RED rather than making it pass vacuously, and the honest form names the two STANDING pins `19-32` wrote (a behavioural pin driving a symlinked-to-regular ledger permitted and counted, and a production-slice pin that CAN name the API because the name sits below the `#[cfg(test)]` sentinel) — and NOTHING ELSE in the file"
      contains: "is_file"
    - path: "src/envelope/cred.rs"
      provides: "ONLY the WHAT IT DOES NOT COVER list, which gains a THIRD item naming the `!`-bodied alias-body route — measured exit 0 and returning the ambient secret under full envelope posture — as a route the named rule does not reach, with the attribution left exactly where audit 12 put it and `T-19-86` neither widened nor re-rated; every other item byte-identical"
      contains: "T-19-86"
    - path: "tests/envelope_word_set.rs"
      provides: "ADDITIONS ONLY — new `#[test]` fns pinning the ORDERING of the widened word set and widened path set against the existing refusals at deliberately different identifiers; the fail-closed work ceiling DRIVEN so it is a control that can fire; the 50 000-slash and 1 MiB rows promoted from RECORDED to asserted now that they are fast; and the ancestor STOP re-asserted after the rule"
      contains: "ordering"
    - path: "tests/envelope_interior_path.rs"
      provides: "ADDITIONS, plus the ONE authorised ancestor-pin MOVE inside `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` — the row moves from the negative list to the positive list with its reasoning REWRITTEN under the WR-02 discipline and NOT deleted — and the COMMENT-ONLY `SEPARATORS` citation correction"
      contains: "PARENT of the envelope directory"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-33 execution record, carrying the statement FIRST that the gate is not cleared and that nothing here is described as a closure and that audit 12's finish-line sentence is NOT written here; `19-32`'s RED rows with their before and after verdicts and its PERMITTED rows re-checked both sides; both design answers as implemented with the fenced-pin derivation that decided the first and the stop that bounds the second; the RESTATED THREE-AXIS CONDITION quoted as it now stands in the predicate's doc; the two authorised pin moves with both reasonings quoted before and after; `T-19-124`'s post-fix curve against the pre-round-12 control with the ceiling driven; all four honesty repairs quoted before and after; the `SEPARATORS` citation sweep; the file exceptions stated AS exceptions; and the gate arithmetic"
      contains: "19-33"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-122` … `T-19-126` recorded NARROWED-or-CORRECTED and NOT CLOSED, each with what remains; `T-19-116` recorded NARROWED with the here-string route closed and `rm -rf <binary-parent>` plus the four original residues still open; the ancestor residue ABOVE the root registered as new, disclosed and unaccepted; the residue recorded as a THREE-AXIS CONDITION with no count wherever this plan states it; and `T-19-86`, `T-19-91`, `T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-115`, `T-19-121`, `T-19-74`, `C-08`, `C-15`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open"
      contains: "T-19-122"
  key_links:
    - from: "a literal absolute path arriving in a word the tokenizer DELETES"
      to: "`ParkReason::EnvelopeAssertionFailed` before any classifier and before the ledger write"
      via: "a second `Segment` field populated in the same `consume_redirection` arm from the same walk's by-product, chained as a third word class at the one existing reading site, with both fenced redirection pins left green"
      pattern: "word_set"
    - from: "a fix that widened `pathname_target` instead"
      to: "a design decided by the fenced-file enumeration before the rule was written"
      via: "`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator grammar pin, named in advance as the two rows design (A) turns red"
      pattern: "derivation"
    - from: "a directory destroyed by naming its parent"
      to: "an ANCESTOR clause beside the PREFIX one, bounded at the envelope root"
      via: "the same component vectors compared the other way round, per candidate, inside the same walk — with `19-32`'s sibling, second-alias, user-set-root, root's-parent and `/tmp` rows pinned PERMITTED so a prefix widened to the root and an unbounded chain both turn red"
      pattern: "ancestor"
    - from: "a scan whose work the input bound does not bound"
      to: "a work bound that is linear in the word's length, with a fail-closed ceiling that a test DRIVES"
      via: "the linearity ratio and the no-slash control `19-32` pinned, re-measured against the rebuilt pre-round-12 control binary, with every component vector proved unchanged by two standing mechanical pins"
      pattern: "work"
    - from: "a residue stated over one axis of three"
      to: "a condition stated over the word set, the reading and the path set, written LAST so it records an observed outcome"
      via: "the doc task ordered after all three rule tasks, on the ordering discipline `19-31`'s `cred.rs` repair established, with no count on any axis and nothing handed to a pin, schedule or witness"
      pattern: "condition"
---

<objective>
**The word the guard deletes, the directory destroyed by naming its parent, and the scan that bounded the wrong quantity.**

`19-32` widened the corpus onto the two axes audit 12 found unnamed, observed it RED and committed it with zero `src/` hunks and zero `tests/` deletions. This plan writes the rules and the honesty repairs and is gated on that recorded RED state. **Its first action is confirming those rows are still red, and its second is confirming `19-32`'s PERMITTED rows are still permitted** — because a rule that turns any of them red is a rule that quietly widened past the boundary it declares.

Audit 12 named the defect, the order and the honest limit:

> **(a) — `T-19-122` FIRST among the fixes, and it is cheap.** The whole gap is `consume_redirection`
> recording a target only when `pathname_target && target.literal`. A here-string word is `literal` and is
> already parsed by `skip_redirection_target`; carrying it on a SECOND `Segment` field — a data-word class,
> distinct from `redirection_targets` so the pathname/non-pathname split stays intact — reaches it without
> touching `SEPARATORS`, `is_separator` or `segment.tokens`. **The over-refusal must be measured from both
> sides before it lands.**

> **(b) — `T-19-123`.** Either an ANCESTOR clause beside the PREFIX one — a candidate that is a proper
> prefix OF the envelope directory, which is a component-vector comparison the other way round and costs one
> more `if` — or an explicit accepted risk saying the envelope root's own parent is out of scope. **The unit
> pin at `policy.rs:10022` and `tests/envelope_interior_path.rs:2341` must MOVE either way, and moving it is
> a design change, not a test edit.**

> **(c) — `T-19-124`.** Bound the candidate scan, not the input.

**The decision (b) leaves open is made here and it is the clause, not the acceptance** — because the harm was driven twice, because the clause costs one comparison, and because an acceptance is a human decision this phase has correctly refused to make seventeen times for a smaller item.

## The boundary, stated exactly — axis 1, the WORD SET

For every word of a segment, the predicate reads: every LITERAL non-operator `Token`; every LITERAL PATHNAME redirection target (`Segment::redirection_targets`, round 11's channel, unchanged); **and every LITERAL NON-PATHNAME redirection target, on a SECOND `Segment` field new in this round.**

The word's `text` and its `literal` bit are already by-products of the walk `skip_redirection_target` performs to find the target's extent, and the recording arm already exists:

```
policy.rs:3581      if pathname_target && target.literal { redirections.push(...) }
                    ^^^^^^^^^^^^^^^^^^ the whole gap; the else of this is the whole fix
```

**Why a second field rather than widening the flag — decided by the fenced-file enumeration, not by taste:**

| Design | What it turns RED |
|---|---|
| widen `RedirectionOperator::pathname_target` for the five | **`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693-10727`)**, which asserts `targets_of("ls <<<here")`, `("ls <<EOF")`, `("ls <<-EOF")`, `("ls >&2")` and `("ls <&0")` are ALL EMPTY; **and the twelve-operator grammar pin (`:10586`, `:10603`)**, which asserts the flag TRUE for the seven and FALSE for the five |
| **a SECOND `Segment` field and a THIRD word class** | **nothing** |

`redirection_operator`'s split is bash's grammar and it stays exactly where its doc says it is derived. **What changes is that the guard reads BOTH sides of it, for different reasons, and says so in place** — the pathname side because the shell will `open(2)` the word, the non-pathname side because the predicate's subject is what a line NAMES and `xargs` is a program that turns data into argv.

**The rejected third design, costed rather than dismissed.** *Treat every deleted-and-unrecorded word as UNRESOLVABLE* would deny `cat <<< hello`, `sort <<< "$x"` and every fd duplication on the line. That is an outage rather than a boundary (AR-19-11), and `19-32` pinned `cat <<<x` and `cat <<< /tmp/x` PERMITTED so the mistake turns a file red rather than turning a driven run unusable.

**The cost, disclosed before it lands.** `cat <<< <ENV>/alpha/pr-ledger.ndjson` becomes refused — a here-string naming a protected path is refused even where the program would only ever have read it as data. **It fails CLOSED, it is bounded to words naming this run's own two paths, and it is the same family as `cat <ledger>` being refused, already in the predicate's own cost section.**

## The boundary, stated exactly — axis 3, the PATH SET

```
a candidate is refused when it is
  (1) UNDER `<root>/<alias>`, component-wise                   ← the existing PREFIX, unchanged
  (2) a proper ANCESTOR of `<root>/<alias>` that is itself
      AT OR UNDER `<root>`                                      ← the NEW clause, one more `if`
  (3) EQUAL to `current_exe()`                                  ← the existing EQUALITY, unchanged
```

**It grows the protected set by EXACTLY ONE path**, because `envelope_dir_in`'s alias is a plain single path component and `ledger_path_in` refuses anything else — but it is WRITTEN in its general form so it stays correct if the directory ever becomes deeper, and so a reader can check it against the code rather than against that arithmetic.

**Where it stops: `<root>`. Why: ownership.** `<root>` is `GSD_MM_ENVELOPE_ROOT`, or the default `~/.local/share/gsd-meta-manager/envelope`; this tool creates it and it holds only alias directories this tool created. **Above it the chain is `~/.local/share`, `$HOME`, `/tmp`, `/`** — shared with everything the user has. A clause reaching them would refuse `ls /`, `df /`, `du -sh $HOME` and `ls /tmp`: **not a boundary but an outage**, and AR-19-11 is this phase's own name for why that matters. It is the same argument `word_is_exactly` gives one path over for keeping the binary half an EQUALITY.

**And it is an ANCESTOR clause, not the existing PREFIX widened to `<root>`:**

```
                                    ancestor clause (mandated)   prefix widened to <root> (forbidden)
rm -rf <root>                       REFUSED                      REFUSED
rm -rf <root>/alpha                 REFUSED                      REFUSED
ls <root>/unrelated-sibling         PERMITTED                    REFUSED   ← 19-32 pinned this
ls <root>/beta  (a second alias)    PERMITTED                    REFUSED   ← and this
user sets GSD_MM_ENVELOPE_ROOT to a
directory holding unrelated files   PERMITTED                    REFUSED   ← and this: a whole subtree
ls <root-parent>  /  ls /tmp        PERMITTED                    PERMITTED ← 19-32 pinned the STOP too
```

**The residue this leaves is stated at the same weight as what closes.** An ancestor ABOVE `<root>` reaches the same nine carriers and **no rule is written for it**; it is registered, disclosed, unaccepted, and named on axis 3 of the restated condition.

**No ancestor clause is written for the BINARY.** `rm -rf <binary-parent>` stays exit 0. `word_is_exactly`'s permit is explicitly reasoned — `ls <parent>` and every `cargo install` must stay permitted — **but the reasoning argues only that a PREFIX would be wrong; it never states that the parent's own DELETION removes the binary.** That is an HONESTY gap, and Task 4 repairs it in that doc, at the same weight as the reasoning. **It is not described as a mitigation.**

## The two fenced pins, and the authorisation to move them

```
policy.rs:10022-10026        ("/tmp/envroot", "the PARENT of the envelope directory is not under it. …")
tests/envelope_interior_path.rs:2341   the same row, inside `every_fenced_unit_pin_…`
```

Audit 12 verified both **GREEN and NON-VACUOUS** and wrote: *"the code does exactly what the pin says. What is missing is the threat row and the residue clause, not the implementation"* — and, in its closure order, *"the unit pin … must MOVE either way, and moving it is a design change, not a test edit."*

**This plan carries that authorisation explicitly.** Each row moves from the negative list to the positive list **with its reasoning REWRITTEN in place under the WR-02 discipline** — why it was right when written, what changed, what is true now — **never deleted**. That is the shape round 11 used for `direction_i_…_stays_permitted`, which needed a human authorisation. **No third row under `tests/` moves that is not a comment**, and the gate checks that mechanically.

## `T-19-124` — bound the work, and the audit's own parenthetical is not a bound

```
slashes      HEAD          dd17bfb
  8 000     5 873 ms       39 ms      ← GUARD_TIMEOUT_SECS = 5 crossed; a 16 KB command line
 20 000    24 591 ms       47 ms
100 000 chars, NO slashes      66 ms  ← the cost is the SCAN, not the LENGTH
```

`MAX_GUARD_REQUEST_BYTES = 1 MiB` bounds the INPUT and bounds no work. **`19-32` derived in writing that a byte floor at the shortest protected path skips NINE candidates of EIGHT THOUSAND and leaves the quadratic untouched**, and that a per-word or per-segment candidate cap is amplified by the word or segment count.

**So the property is LINEARITY**: the scan's work must be a linear function of the word's length, which is what makes `MAX_GUARD_REQUEST_BYTES` a genuine bound on work rather than only on input — **with a FAIL-CLOSED work ceiling beside it as the backstop**, refusing at `ParkReason::EnvelopeAssertionFailed` if a later edit reinstates a super-linear scan. **The ceiling must be DRIVABLE and DRIVEN**, because a ceiling that cannot fire is a control that cannot fail — the exact defect `T-19-126`(i) records one file over.

**And it must move no verdict.** Whatever shape the reformulation takes, it computes for every candidate exactly the component vector `lexical_absolute_components` computes today, including the leading-`./` strip, the `.` drop, the separator collapse and the TEXTUAL `..` collapse with `..` at the root collapsing to the root. `containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` are the standing mechanical proofs; `19-32`'s cost half and rounds 4–12's pin family are the behavioural one. **If it cannot be written without moving a verdict, SEVER Task 3 and report it** — `T-19-124` is `medium`, non-blocking, and the honesty repairs land either way.

## Round 3's principle is discharged, not weakened

The third word class is a third `.chain()` on the iterator built from the ONE segment the caller already holds; the ancestor clause is a second comparison inside the same per-candidate walk; the linear reformulation computes the SAME candidate set inside the SAME predicate. **No second pass, no second scan, no new `ParkReason`, no change to `Token`, `segment.tokens`, `SEPARATORS` or `is_separator` — and `hooks.rs` is not opened at all.** **If the design needs a second reading site or a budget threaded across the guard call, that is a finding**: a budget that must be threaded is evidence the bound is over the wrong quantity, which would be `T-19-124`'s own shape arriving in the round that fixes `T-19-124`.

**One mechanical placement constraint carries the whole no-filesystem guarantee.** `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so` slices from the literal `fn lexical_absolute_components(word: &str)` to the `#[cfg(test)]` sentinel and asserts eight filesystem and process APIs appear nowhere in that code, with three positive controls plus `19-31`'s fourth. **Every helper this plan adds goes AT OR AFTER that anchor, and a positive control names each.**

## The residue, restated over all three axes — and written LAST

> **AXIS 1 — WHICH WORDS REACH THE READER.** This predicate reads every LITERAL word of the segment that is
> not an operator, every LITERAL PATHNAME redirection target, and every LITERAL NON-PATHNAME redirection
> target. **It is silent about a word produced by a redirection production that did not COMPLETE**, which
> fails closed at `Token::redirection_unresolvable` rather than being read.
>
> **AXIS 2 — WHAT THE READER SEES IN A WORD IT RECEIVED.** It reads every `/`-ANCHORED SUBSTRING of that
> word's text, normalised. **It is silent about a word the SHELL MAY REWRITE**, because the guard cannot
> know its final text; **about a word whose TEXT CARRIES NO ABSOLUTE PATH ANYWHERE**, because there is
> nothing in it to normalise; **and about a word that reaches a protected path ONLY THROUGH A LINK**,
> because no lexical reading of the text names it.
>
> **AXIS 3 — WHICH PATHS THE COMPARISON PROTECTS.** A candidate under this run's envelope directory, a
> candidate that is a proper ANCESTOR of it at or under this run's envelope ROOT, and a candidate EQUAL to
> this run's own binary. **It is silent about an ancestor ABOVE the root — `$HOME`, `/tmp`, `/` — and about
> the binary's PARENT and every ancestor of it**, because refusing a word that names one of those is an
> outage rather than a boundary, which is the same reason the binary half is an EQUALITY.

**No count is written on any axis**, the spellings are INSTANCES, and **none of it is handed to a pin, a schedule or a version witness.** **It is written LAST, in a task ordered after all three rule tasks**, so it records an observed outcome rather than predicting one — the ordering discipline the `19-30`/`19-31` plan-check established for `cred.rs` and which `19-31` executed.

## What this plan is NOT

**`/gsd-secure-phase 19` is NOT cleared by this plan.** `T-19-86` remains open at `high` by explicit user scoping decision — with `T-19-111` kept OUT of it — `T-19-91` and `T-19-111` remain open at `high`, `T-19-112` is narrowed and not closed, `T-19-121` remains a bounded residue of a control that landed, and **`T-19-116` is NARROWED and not closed**: the third word class closes `xargs cp /bin/true <<< <BINARY>`, and `rm -rf <binary-parent>` stays exit 0 beside the four original residues. `T-19-115` gets no rule at all.

**And this plan does NOT write audit 12's finish-line sentence.** Whether the open set has become the human-decision items plus the deliberately-unruled residues is **audit 13's judgement**; a plan that certified its own round on that question would be this phase's certification defect in its purest form. The SUMMARY must say all of that first, and must never write an unqualified "T-19-60 is closed".

Purpose: make a word the guard deletes and a path one component up into facts the guard establishes, make the scan's cost a linear function of its input, and make four claims the code states about this exact cell true.
Output: a third word class and an ancestor clause at one existing decision point, a work bound that is on work, four corrected claims, two authorised pin moves, a three-axis condition written last, the appended execution record, and a green `--no-fail-fast` gate whose arithmetic is stated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-32-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@src/envelope/ledger.rs
@src/envelope/cred.rs
@tests/envelope_word_set.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm still RED and still PERMITTED, then the THIRD WORD CLASS in `policy.rs`</name>
  <files>src/envelope/policy.rs</files>
  <precondition>
    `19-32` is committed and its SUMMARY records the complete RED name list, the complete PERMITTED list,
    the `passed + failed` total, the per-binary counts for all NINETEEN `envelope_*` binaries, the WORD-SET
    SWEEP with its `bash` reach column and the heredoc verdict, the ANCESTOR-STOP table, `T-19-124`'s
    three-tree curve, the fenced-file enumeration and the `SECTION_ENVELOPE` re-measurement. Work in the
    main checkout with a clean tree — **ISOLATION=none is forced for this phase, so do NOT create a
    worktree and do NOT check out, reset or stash the working tree.** `cargo build` produces
    `./target/debug/gsd-meta-manager`. A real `git` binary is on `PATH` and `gh` is installed; no pin may
    be written to skip when either is absent, because a skipped pin is a fail-open pin. `glab` is NOT
    installed and no pin may depend on it. The `dd17bfb` control binary `19-32` built is still available,
    or is rebuilt the same way — by `git archive` extraction outside the repository, never by mutating the
    working tree.
  </precondition>
  <read_first>
    `19-32-SUMMARY.md` in full — **the RED list, the PERMITTED list, the WORD-SET SWEEP, the ANCESTOR-STOP
    table, the fenced-file enumeration and both design answers are this plan's handoff contract**, and the
    rules are built from those measurements rather than from this plan's text.
    `src/envelope/policy.rs:3162-3229` (**`RedirectionOperator` and `redirection_operator` — read the
    twelve-operator match and the doc's statement that the split is a property of the OPERATOR. It does
    NOT change**), `:3231-3300` (**`RedirectionTarget` and `skip_redirection_target` — the target's `text`
    and `literal` are already by-products, and the WR-02 note already there records that a SECOND question
    is now asked of the same walk; this round asks a THIRD and the note is extended, not replaced**),
    `:3532-3610` (**`consume_redirection` — `:3581`'s `if pathname_target && target.literal` is the arm
    this task extends, and the comment above it claiming the by-product is kept "only when the operator
    takes a FILENAME" is the sentence that must become true again**), `:2486-2560` (**`Segment`'s fields
    and `redirection_targets`' own doc, read so the new field sits beside it with the same shape of
    reason**), `:5779-5810` (**`protected_carrier_named`'s TWO WORD CLASSES paragraph and the
    one-reading-site argument, which the third class must keep true**), `:6109-6137` (**the predicate's
    body — the `!token.operator && token.literal` filter and the `operands.chain(targets)` this task adds a
    third `.chain()` to**), `:6003-6070` (the disclosed cost, read so the new member is added in Task 4
    rather than here), `:2422` (`SEPARATORS`, read to leave byte-identical),
    **`:10560-10610` (the twelve-operator grammar pin) and `:10692-10727`
    (`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` — read the five
    `is_empty()` rows and the SEGMENT-COUNT rows below them; BOTH tests must stay green and NEITHER may be
    edited)**.
    `src/envelope/hooks.rs:954-1030` (`classify_segments` and the ONE call site — **read only to confirm
    the signature does NOT change and that this file is not opened**).
  </read_first>
  <action>
**FIRST, and before any production line moves: confirm `19-32`'s RED set is still RED and its PERMITTED set
is still PERMITTED.** Re-run the two evidence suites and the axis suite against the unmodified tree with
`--no-fail-fast` and record the failing names verbatim. Diff that list against `19-32-SUMMARY.md`'s recorded
RED set. **If any row is already green, STOP and report it as a finding** with the measured verdict and the
reason identifier. Then drive `19-32`'s PERMITTED rows — `cat <<<x`, `cat <<< /tmp/x`,
`git <<<x push --force origin main`, `ls <ENV>/unrelated-sibling`, `<ENV>/beta`, the user-set-root case,
`ls <ENV-PARENT>`, `/tmp`, both binary EXACT-PATH controls, **every COST row**, the no-slash word, the
FRESH-ROOT ledger row and the symlinked-to-regular ledger row — against the unmodified tree and record them
PERMITTED. **That is the baseline the widening check compares against at the end of every task.** Put the
verbatim pre-fix output in the SUMMARY.

**Then the production work, in `policy.rs` only, and only the word set.**

**1. A SECOND `Segment` field.** Add one field beside `Segment::redirection_targets` carrying **every
LITERAL word this simple command names after a NON-PATHNAME redirection operator, as written.** Its doc
states, in the shape `redirection_targets`' own doc already uses:

  - **what it is and why it is a SECOND field rather than a widened flag** — `redirection_targets` is the
    set of words the shell will hand to `open(2)`, and `RedirectionOperator::pathname_target` answers that
    question from bash's own grammar. Widening it would make that flag say something bash does not say, and
    would turn `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the
    twelve-operator grammar pin red. **The split stays intact and both sides are read, for different
    reasons;**
  - **why the guard reads it at all** — a here-string is a DATA channel, and `xargs` is a program that turns
    data into argv. **The predicate's subject is what a line NAMES, not what the shell does with a word;**
  - **that `Segment::tokens` is byte-for-byte unchanged** and the target still produces no `Token`, so
    round 6's deletion model, the SEGMENT-COUNT pins and the over-deletion control are untouched.

**2. Populate it in the SAME arm, from the SAME by-product.** `consume_redirection`'s existing
`if pathname_target && target.literal` gains its `else if !pathname_target && target.literal` — one branch,
in the walk that already produced `target.text` and `target.literal`. **Nothing else in `consume_redirection`
moves**: the fd-prefix handling, the operator length, the `skip_redirection_target` call, the unresolvable
arm and the `splice_word = false` are byte-identical. **Correct the comment above it in place** so the
sentence *"records it on the `Segment` only when the operator takes a FILENAME"* becomes true again, under
the WR-02 discipline: why it was right when written, what changed, what is true now.

**3. A THIRD word class at the ONE existing reading site.** `protected_carrier_named` chains the new field
after `operands` and `targets`. **The `!token.operator && token.literal` filter still runs FIRST for the
token class**, and the new class carries only literal words by construction, exactly as
`redirection_targets` does — say so where the existing doc says it. Extend the TWO-WORD-CLASSES paragraph to
THREE, keeping its one-reading-site argument intact and stating it in the same terms: **reading three fields
of the ONE segment the caller already holds is one reading site, not three.**

**4. Run the two fenced redirection pins IMMEDIATELY, before going further**, and record both: the
twelve-operator grammar pin, and `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move`
including its SEGMENT-COUNT rows. **A red one is a finding about the FIX — it means the flag was widened
rather than a field added — reported with the observed answer and never an assertion to edit.**

**5. New `#[test]` fns inside the existing `mod tests`** — not folded into existing ones, because new fns
are the only thing that moves the gate total. At minimum: a here-string target naming the envelope
directory; one naming the binary; one under an operator other than `<<<`; one whose path sits at a NON-ZERO
index inside the here-string word, so the third class and round 12's interior scan compose; and the two
negative controls — a here-string carrying an unprotected path, and one carrying no path at all — asserted
`false` so the class is a PATH class rather than a channel ban.

Run the evidence and axis suites. **Then re-drive `19-32`'s whole PERMITTED set and diff against the
baseline. A row that moved from permitted to refused is a finding about the RULE, reported with both
verdicts, never an assertion to edit.** Commit as its own commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --lib envelope::policy --no-fail-fast &gt;/tmp/19-33-t1-unit.log 2&gt;&amp;1 || { echo "FAIL: policy.rs unit pins are RED — the flag was widened rather than a field added, or a fenced redirection pin moved"; rtk proxy grep -A2 'FAILED\|panicked' /tmp/19-33-t1-unit.log | head -40; exit 1; }; for t in envelope_argv_deletion envelope_carrier_reach envelope_interior_path envelope_control_carrier envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_credential envelope_advisory envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: UNEXPECTED RED in $t — a fenced file moved"; exit 1; }; done; rtk proxy cargo test --test envelope_word_set --no-fail-fast &gt;/tmp/19-33-t1-ev.log 2&gt;&amp;1; rtk proxy grep 'test result:' /tmp/19-33-t1-ev.log; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; test "$(git diff --name-only HEAD~1..HEAD -- src/ | rtk proxy grep -vc 'src/envelope/policy.rs' || true)" -eq 0 || { echo "FAIL: this commit touches a src/ file other than policy.rs"; exit 1; }; test -z "$(git diff --numstat HEAD~1..HEAD -- src/envelope/hooks.rs src/envelope/mod.rs src/envelope/advisory.rs src/scan.rs src/config.rs)" || { echo "FAIL: a forbidden src/ file moved"; exit 1; }; test -z "$(git diff --numstat HEAD~1..HEAD -- tests/)" || { echo "FAIL: this commit touches tests/ — the word-class task is production-only"; exit 1; }</automated>
  </verify>
  <done>`19-32`'s RED set was confirmed STILL RED and its PERMITTED set STILL PERMITTED against the unmodified tree, both recorded verbatim, before any production line moved; a SECOND `Segment` field exists carrying every LITERAL word the simple command names after a NON-PATHNAME redirection operator, with a doc stating why it is a second field rather than a widened flag, why the guard reads it at all, and that `Segment::tokens` is byte-for-byte unchanged; **it is populated in the SAME `consume_redirection` arm from the SAME `skip_redirection_target` by-product, with the rest of that macro byte-identical and its stale comment corrected in place under the WR-02 discipline**; a THIRD word class is chained at the ONE existing reading site with the TWO-WORD-CLASSES paragraph extended to THREE and its one-reading-site argument intact; **the twelve-operator grammar pin and `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` were run IMMEDIATELY after the change and both are GREEN, including the SEGMENT-COUNT rows — neither was edited**; new `#[test]` fns were ADDED (never folded) covering a here-string over both protected paths, an operator other than `<<<`, a non-zero-index path inside the here-string word, and two negative controls asserting the class is a PATH class rather than a channel ban; **`19-32`'s whole PERMITTED set was re-driven after the change and diffed against the baseline with zero moves**, including `cat <<<x`, `cat <<< /tmp/x` and `git <<<x push --force origin main` at their unchanged verdicts; `RedirectionOperator`, `redirection_operator`, `skip_redirection_target`'s parsing, `Segment::redirection_targets`, `segment.tokens`, `Token`, `SEPARATORS`, `is_separator`, `tokenize`'s stream, `split_segments_with_heads`, `resolve_program`, `resolve_program_with_head`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api`, `forbidden_repo_path` and the floor comment show ZERO behavioural change; `envelope_config_resolution` is 30/0; **`hooks.rs`, `mod.rs` and `advisory.rs` show ZERO diff lines**; and this commit touches `policy.rs` alone and nothing under `tests/`.</done>
</task>

<task type="auto">
  <name>Task 2: the ANCESTOR clause, its stop, and the two authorised fenced-pin moves</name>
  <files>src/envelope/policy.rs, tests/envelope_interior_path.rs</files>
  <read_first>
    `19-32-SUMMARY.md`'s ANCESTOR-STOP table in full — **the sibling under the root, the second alias, the
    user-set-root case, the root's own parent, `/tmp` and both binary EXACT-PATH controls are the rows this
    task may not move**, and the `T-19-123` design answer with its four-row discriminator.
    `src/envelope/policy.rs:5572-5606` (`lexical_absolute_components` — **read its signature text, which is
    the SLICE ANCHOR, and note it does not change**), `:5608-5662` (`slash_anchored_candidates` and its
    cost paragraph), `:5664-5711` (**`word_is_within` in full — the `dir.is_empty()` guard, the
    `word.len() < dir.len()` early return applied PER CANDIDATE, the component-wise compare, and the doc's
    *"A PREFIX is right HERE and wrong one path over"* paragraph, which this task extends rather than
    replaces**), `:5713-5762` (**`word_is_exactly` — read the whole reasoning for the EQUALITY, because
    Task 4 repairs its gap and this task must not weaken it**), `:5779-5830`
    (`protected_carrier_named`'s TWO PATHS paragraph, which gains the third member),
    `:10001-10047` (**the `word_is_within` unit pins — the ancestor row at `:10022-10026` is the FIRST
    authorised move; read its stated reason verbatim, because it is REWRITTEN and not deleted; and read the
    `alphax`, `alpha2`, basename, `..`-walk and relative rows, none of which moves**), `:10287-10330`
    (`each_half_of_the_path_set_is_silent_when_it_was_not_given_a_path`), and the no-filesystem slice
    assertion with its positive controls.
    `src/envelope/mod.rs`'s `envelope_dir_in` and `src/envelope/ledger.rs`'s `ledger_path_in` — **read ONLY
    to confirm the alias is a plain single path component and that anything else is refused outright, which
    is why the ancestor set is exactly `{<root>}`; change neither file.**
    `tests/envelope_interior_path.rs:2318-2400` (**`every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule`
    — the ancestor row at `:2341` is the SECOND authorised move, the binary's-parent row at `:2348` does
    NOT move, and the positive-control list below is where the moved row lands**).
  </read_first>
  <action>
**Only the path set moves in this task, and only for the envelope directory.**

**1. An ANCESTOR comparison beside the PREFIX one, placed INSIDE the no-filesystem slice.** Add one private
helper **at or after the `fn lexical_absolute_components(word: &str)` anchor, never above it**, answering
whether one component vector is a PROPER ANCESTOR of another. It touches nothing but two slices. Its doc
states:

  - **what it is for** — deleting an ancestor of the envelope directory destroys the envelope directory, and
    `rm -rf <root>` takes the same nine carriers `word_is_within`'s prefix exists to protect, one component
    up;
  - **that it is the same component-vector comparison the other way round**, which is why it costs one more
    `if` and cannot introduce a new normalisation or a new reading;
  - **why it is BOUNDED at the envelope ROOT** — the root is the directory this tool creates and owns;
    above it are `~/.local/share`, `$HOME`, `/tmp` and `/`, shared with everything the user has, and
    refusing a word naming one of those is an outage rather than a boundary (AR-19-11). **The same argument
    `word_is_exactly` gives one path over.**

**2. Apply it in `word_is_within`, per candidate, beside the prefix test — and take the ROOT as an explicit
input rather than deriving it.** `protected_carrier_named` already receives `envelope_dir`; the root it must
compare against is the directory the guard was given, and the clause must be written so it cannot silently
become a prefix over the root. **State in the doc that the set grows by exactly one path because the alias
is a plain single component (`ledger_path_in` refuses anything else), and that the clause is written in its
general form so it stays correct if the directory becomes deeper.**

**Do NOT widen the existing prefix, do NOT compare against a shorter directory, and do NOT walk the chain
above the root.** `19-32` pinned `ls <ENV>/unrelated-sibling`, `<ENV>/beta`, the user-set-root case,
`ls <ENV-PARENT>` and `/tmp` PERMITTED; **a red one there is a finding about the RULE, reported with both
verdicts.**

**3. Extend `protected_carrier_named`'s TWO PATHS paragraph to name the third member**, with the stop and
the residue above it stated at the same weight as the clause. **`word_is_exactly` is NOT changed in this
task** — its doc repair is Task 4's and it is an honesty repair, not a mitigation.

**4. Re-run the `policy.rs` unit pins IMMEDIATELY and record each**: the `..`-walk rows, the `alphax` and
`alpha2` sibling rows, the basename near miss, the relative rows, the binary's-parent row and both
`each_half_…` rows. **Exactly ONE of them moves — the ancestor row — and every other must be green.** A
second mover is a finding about the RULE.

**5. THE FIRST AUTHORISED PIN MOVE — `policy.rs:10022-10026`.** Move the row from the negative list to the
positive list. **REWRITE its reasoning in place under the WR-02 discipline and do not delete it**: state
that it was right when written (the boundary was the directory this run owns, and a prefix over the parent
would have been wrong); what changed (the harm the permit admitted was measured — `rm -rf <root>` moved a
bare remote's `main` and reset a fired PR cap, driven twice with a control beside every leg); and what is
true now (the boundary is the directory this run owns **plus its ancestors up to the root this run was
given, and no further**, with the reason the chain stops there). **Add the row that pins the STOP beside
it** — the root's own PARENT still answering `false` — so the move is bounded by an assertion rather than by
prose.

**6. THE SECOND AUTHORISED PIN MOVE — `tests/envelope_interior_path.rs:2341`.** The identical move inside
`every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule`: the row leaves the
negative list, joins the positive-control list, and its reason is REWRITTEN in the same terms. **The
binary's-parent row at `:2348` does NOT move**, and its comment gains one sentence saying why: no ancestor
clause is written for the binary, because its directory is shared. **No third row in that file moves**, and
the test's failure message keeps its statement that a mover is a finding about the DESIGN.

**7. New `#[test]` fns inside `policy.rs`'s existing `mod tests`** — the ancestor at the root; the root with
a trailing slash; the root reached through a `..` walk; the root's PARENT answering `false`; a sibling under
the root answering `false`; a second alias directory answering `false`; and the binary's parent answering
`false`. **Each names its control and what makes the pair discriminating.**

Run the evidence and axis suites, then re-drive `19-32`'s whole PERMITTED set and diff against Task 1's
baseline. Commit as its own commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --lib envelope::policy --no-fail-fast &gt;/tmp/19-33-t2-unit.log 2&gt;&amp;1 || { echo "FAIL: policy.rs unit pins are RED — more than the one authorised ancestor row moved"; rtk proxy grep -A2 'FAILED\|panicked' /tmp/19-33-t2-unit.log | head -40; exit 1; }; rtk proxy cargo test --test envelope_interior_path --no-fail-fast &gt;/tmp/19-33-t2-fence.log 2&gt;&amp;1 || { echo "FAIL: the fenced-pin file is RED after the authorised move"; rtk proxy grep -A4 'FAILED\|panicked' /tmp/19-33-t2-fence.log | head -40; exit 1; }; for t in envelope_carrier_reach envelope_control_carrier envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_credential envelope_advisory envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: UNEXPECTED RED in $t — a fenced file moved"; exit 1; }; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; test "$(git diff --name-only HEAD~1..HEAD -- src/ | rtk proxy grep -vc 'src/envelope/policy.rs' || true)" -eq 0 || { echo "FAIL: this commit touches a src/ file other than policy.rs"; exit 1; }; test "$(git diff --name-only HEAD~1..HEAD -- tests/ | rtk proxy grep -vc 'tests/envelope_interior_path.rs' || true)" -eq 0 || { echo "FAIL: this commit touches a tests/ file other than the one authorised for the pin move"; exit 1; }; DEL=$(git diff -U0 HEAD~1..HEAD -- tests/ | rtk proxy grep -c '^-[^-]' || true); test "$DEL" -le 6 || { echo "FAIL: $DEL deleted lines under tests/ — the authorised move is ONE row"; exit 1; }; git diff -U0 HEAD~1..HEAD -- tests/ | rtk proxy grep '^-[^-]' | rtk proxy grep -vE '^-[[:space:]]*(//|///)|PARENT of the envelope directory' | rtk proxy grep -q . &amp;&amp; { echo "FAIL: a deleted line under tests/ is neither a comment nor the authorised ancestor row"; exit 1; }; test -z "$(git diff --numstat HEAD~1..HEAD -- src/envelope/hooks.rs src/envelope/mod.rs src/envelope/advisory.rs src/scan.rs src/config.rs)" || { echo "FAIL: a forbidden src/ file moved"; exit 1; }; echo OK</automated>
  </verify>
  <done>An ANCESTOR comparison exists in `policy.rs`, **placed AT OR AFTER the `fn lexical_absolute_components(word: &str)` slice anchor** so it falls inside the no-filesystem assertion, calling no filesystem or process API and touching nothing but two component slices, with a doc stating what it is for, that it is the same comparison the other way round, and **why it is BOUNDED at the envelope ROOT with the AR-19-11 argument and the `word_is_exactly` parallel**; it is applied in `word_is_within` PER CANDIDATE beside the prefix test, taking the root as an explicit input so it cannot silently become a prefix over the root; `protected_carrier_named`'s TWO PATHS paragraph names the THIRD member with the stop and the residue above it stated at the same weight; **`word_is_exactly` was NOT changed**; the `policy.rs` unit pins were re-run immediately and **EXACTLY ONE moved — the ancestor row — with the `..`-walk, `alphax`, `alpha2`, basename, relative, binary's-parent and both `each_half_…` rows all green**; **BOTH authorised pin moves were performed — `policy.rs:10022-10026` and `tests/envelope_interior_path.rs:2341` — each moving from the negative list to the positive list with its reasoning REWRITTEN in place under the WR-02 discipline (right when written / what changed / what is true now, with the stop and its reason) and NOT deleted, each with a new row pinning the STOP beside it, and the binary's-parent row at `:2348` left in place with one sentence added saying why no ancestor clause is written for the binary**; new `#[test]` fns were ADDED covering the root, the trailing slash, the `..` walk to the root, the root's parent, a sibling under the root, a second alias and the binary's parent, each naming its control; `19-32`'s whole PERMITTED set was re-driven and diffed against Task 1's baseline with zero moves, including `ls <ENV>/unrelated-sibling`, `<ENV>/beta`, the user-set-root case, `ls <ENV-PARENT>` and `/tmp`; this commit touches `policy.rs` and `tests/envelope_interior_path.rs` alone; **no deleted line under `tests/` is anything but a comment or the one authorised ancestor row**, and the deleted count is within the stated bound; and `hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` show zero diff lines.</done>
</task>

<task type="auto">
  <name>Task 3 (SEVERABLE): bound the candidate scan's WORK, and drive the ceiling</name>
  <files>src/envelope/policy.rs, tests/envelope_word_set.rs</files>
  <read_first>
    `19-32-SUMMARY.md`'s `T-19-124` section in full — **the three-tree curve, the linearity ratio with its
    derived threshold, the no-slash control, the arithmetic showing a byte floor skips nine candidates of
    eight thousand, and the amplification argument against per-word and per-segment caps.** The rule is
    built from those measurements rather than from this plan's text.
    `src/envelope/policy.rs:5572-5606` (**`lexical_absolute_components` — the exact normalisation any
    reformulation must reproduce: the leading-`./` strip BEFORE the `starts_with('/')` test, the `""` and
    `"."` drops, and the `..` pop which is a no-op on an empty vector, so `..` at the root collapses to the
    root**), `:5608-5662` (`slash_anchored_candidates` — the iterator whose per-candidate application is
    the quadratic), `:5664-5711` and `:5713-5762` (both halves, now carrying three comparisons between
    them), and the no-filesystem slice assertion with its positive controls.
    `src/envelope/hooks.rs:730-740` and `:1480-1490` (`MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` —
    **read to record the two constants the bound is derived against; this file is NOT opened and neither
    constant moves**).
    `tests/envelope_interior_path.rs`'s `containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte`
    and `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` — **the two
    standing mechanical proofs that no component vector moved; read them and change NEITHER.**
  </read_first>
  <action>
**The bound is on WORK. If it cannot be written without moving a verdict or without threading a budget
through the guard call, SEVER this task, report the finding with its measurement, and let Tasks 4 and 5
proceed — `T-19-124` is `medium` and non-blocking and the honesty repairs land either way.**

**1. Make the scan's work LINEAR in the word's length.** Today `lexical_absolute_components` runs once per
`/`; each run is O(word length); the product is the quadratic audit 12 measured. **The reformulation must
compute, for every `/`-anchored candidate, exactly the component vector today's code computes for it** —
including the leading-`./` strip, the `""`/`"."` drops, the repeated-separator collapse and the TEXTUAL `..`
collapse with `..` at the root collapsing to the root. **The candidate set does not change, the
normalisation does not change, and the comparisons do not change; only the number of times the same
characters are walked does.**

**Place the helper AT OR AFTER the `fn lexical_absolute_components(word: &str)` anchor** so it falls inside
the no-filesystem slice, and **ADD A POSITIVE CONTROL NAMING IT** to that assertion, the same reason the
existing four controls give. `lexical_absolute_components`' signature text does not change and neither does
its behaviour for a caller that hands it one string.

**A shape that satisfies this is a single RIGHT-TO-LEFT pass over the word's `/`-separated parts with
structure sharing** — for each starting part, whether that part survives and which surviving part follows
it, so every suffix's component list is a shared chain built in one walk — after which comparing a
candidate against the envelope directory, its ancestors or the binary walks at most as many links as those
paths have components. **The executor is not required to use that shape**: any reformulation that meets the
measured property and moves no verdict is acceptable, and a simpler one is preferred. **A deviation is
RECORDED with its own measurement rather than assumed equivalent.**

**2. A FAIL-CLOSED WORK CEILING beside it, and it must be able to fire.** Carry a bound on the scan's work
for one predicate invocation. When it is exhausted the predicate **cannot establish that no protected path
was named**, so the segment is REFUSED at `ParkReason::EnvelopeAssertionFailed` — never permitted, never a
new park reason, never `HookBypassBlocked` or `PrCapExceeded`. Its doc states:

  - **that it is a backstop and not the bound** — the bound is the linearity above, which is what makes
    `MAX_GUARD_REQUEST_BYTES` a genuine bound on work; the ceiling exists so that a later edit reinstating
    a super-linear scan REFUSES rather than overruns `GUARD_TIMEOUT_SECS`;
  - **why it is not a cap on the word count or the segment count** — those are quantities the request
    multiplies, so a bound over them is a bound over the wrong property, which is `T-19-120`'s shape and
    the defect this row exists to correct;
  - **the over-refusal it creates**, disclosed in the predicate's cost section by Task 4: a word whose scan
    exceeds the ceiling is refused whatever it names.

**Its value must be REACHABLE under `MAX_GUARD_REQUEST_BYTES`, and a test must DRIVE it** and observe the
refusal at its identifier. **A ceiling no test drives is a control that cannot fail** — the exact defect
`T-19-126`(i) records one file over, and this round is not going to ship a second one.
**`MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and `MAX_LEDGER_BYTES` keep their values**: a bound over
the wrong property is not corrected by moving a number.

**3. Prove no verdict moved, mechanically and behaviourally.** Run
`containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and
`every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` and record both GREEN;
re-drive `19-32`'s whole cost half and rounds 4 through 12's pin family and record every one unchanged.
**A moved verdict is a finding about the FIX, reported with both answers.**

**4. Re-measure the curve on THREE trees and record the table.** The built binary at this commit, the
rebuilt `dd17bfb` control binary, and `19-32`'s recorded HEAD-before numbers, at 1 000 / 2 000 / 5 000 /
8 000 / 20 000 / 50 000 slashes plus the 100 000-character no-slash control. **State the post-fix ratio
against the pin's threshold of 8 and against `dd17bfb`'s own curve.**

**5. ADD to `tests/envelope_word_set.rs`, never edit it.** New `#[test]` fns: the fail-closed ceiling DRIVEN
and observed at `envelope_assertion_failed`; the 50 000-slash row and **the 1 MiB slash-dense word**, both
promoted from `19-32`'s RECORDED status to asserted now that they answer inside `GUARD_TIMEOUT_SECS`; and
the no-slash control re-asserted. **Every one names its control.**

Commit as its own commit. **If this task is severed, say so in the SUMMARY with the measurement that forced
it, record `T-19-124` as OPEN at `medium` with the property unmet, and proceed to Task 4.**
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --lib envelope::policy --no-fail-fast &gt;/tmp/19-33-t3-unit.log 2&gt;&amp;1 || { echo "FAIL: policy.rs unit pins are RED — the reformulation moved a component vector"; rtk proxy grep -A2 'FAILED\|panicked' /tmp/19-33-t3-unit.log | head -40; exit 1; }; rtk proxy cargo test --test envelope_interior_path --no-fail-fast &gt;/tmp/19-33-t3-fence.log 2&gt;&amp;1 || { echo "FAIL: containment or the fenced-corpus proof went RED — a candidate normalisation moved"; rtk proxy grep -A4 'FAILED\|panicked' /tmp/19-33-t3-fence.log | head -40; exit 1; }; rtk proxy cargo test --test envelope_word_set --no-fail-fast &gt;/tmp/19-33-t3-ev.log 2&gt;&amp;1 || { echo "FAIL: the evidence file is RED after the work bound"; rtk proxy grep -A4 'FAILED\|panicked' /tmp/19-33-t3-ev.log | head -40; exit 1; }; rtk proxy grep 'test result:' /tmp/19-33-t3-ev.log; for t in envelope_carrier_reach envelope_control_carrier envelope_wrapper_class envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_credential envelope_advisory envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: UNEXPECTED RED in $t"; exit 1; }; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; test "$(git diff --name-only HEAD~1..HEAD -- src/ | rtk proxy grep -vc 'src/envelope/policy.rs' || true)" -eq 0 || { echo "FAIL: this commit touches a src/ file other than policy.rs"; exit 1; }; test -z "$(git diff --numstat HEAD~1..HEAD -- src/envelope/hooks.rs)" || { echo "FAIL: hooks.rs was opened — a bound that must be threaded is a bound over the wrong quantity"; exit 1; }; test "$(git diff --numstat HEAD~1..HEAD -- tests/ | awk '{s+=$2} END{print s+0}')" -eq 0 || { echo "FAIL: this commit deletes lines under tests/ — this task ADDS only"; exit 1; }; echo OK</automated>
  </verify>
  <done>The candidate scan's work is a LINEAR function of the word's length, or this task was SEVERED with the measurement that forced it recorded and `T-19-124` left OPEN at `medium` with the property unmet; the reformulation computes for every `/`-anchored candidate exactly the component vector today's code computes — the leading-`./` strip, the `""`/`"."` drops, the separator collapse and the TEXTUAL `..` collapse with `..` at the root collapsing to the root — and **`containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` are both GREEN**, with `19-32`'s cost half and rounds 4 through 12's pin family all re-driven unchanged; **the helper is placed AT OR AFTER the slice anchor with a POSITIVE CONTROL NAMING IT added to the no-filesystem assertion**, and `lexical_absolute_components`' signature text and behaviour are unchanged; **a FAIL-CLOSED work ceiling exists, refuses at `ParkReason::EnvelopeAssertionFailed`, is REACHABLE under `MAX_GUARD_REQUEST_BYTES`, and is DRIVEN by a test that observes the refusal at its identifier**, with its doc stating that it is a backstop rather than the bound, why a per-word or per-segment cap is a bound over the wrong property, and the over-refusal it creates; **`MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and `MAX_LEDGER_BYTES` keep their values and `hooks.rs` was not opened**; the curve was re-measured on three trees at seven sizes with the post-fix ratio stated against the threshold of 8 and against `dd17bfb`'s own curve; the 50 000-slash and 1 MiB rows were promoted from RECORDED to asserted and answer inside `GUARD_TIMEOUT_SECS`, with the no-slash control re-asserted beside them; every new test is an ADDITION naming its control; and this commit touches `policy.rs` and `tests/envelope_word_set.rs` alone with ZERO deletions under `tests/`.</done>
</task>

<task type="auto">
  <name>Task 4: the four honesty repairs, the citation sweep, and the THREE-AXIS condition written LAST</name>
  <files>src/envelope/policy.rs, src/envelope/ledger.rs, src/envelope/cred.rs, tests/envelope_carrier_reach.rs, tests/envelope_wrapper_class.rs, tests/envelope_interior_path.rs</files>
  <read_first>
    **The tree as Tasks 1, 2 and 3 left it** — this task RECORDS observed outcomes and predicts none. Run
    `git log --oneline` and read which of the three landed and which, if any, was severed, and grep for the
    identifiers each added. **The `19-30`/`19-31` plan-check established this ordering discipline for
    `19-31`'s `cred.rs` repair, and `19-31` executed it: a repair written before the rule it describes must
    either guess or be falsified.**
    `src/envelope/policy.rs:5891-5921` (**the residue condition as it stands — one axis of three**),
    `:5923-6001` (the INSTANCES and the no-revisit-condition paragraph, both of which stay with their
    reasons), `:6003-6070` (**the disclosed cost — the `/`-anchored-substring wording at `:6044-6048` is
    `T-19-125`, and the existing `dd`/`tee` WR-02 note is the shape this task reuses**), `:5713-5762`
    (**`word_is_exactly`'s reasoning for the EQUALITY, which argues only that a PREFIX would be wrong**),
    `:6139-6187` (**`envelope_carrier_refusal`'s two messages and their two `To proceed:` lines**).
    `src/envelope/ledger.rs:340-372` (**the KIND-check comment — the sentence claiming the API name is left
    unspelled so a grep gate does not pass vacuously is INVERTED, and `19-32` replaced the one-shot with two
    standing pins whose names this comment must now carry**).
    `src/envelope/cred.rs:416-457` (**the WHAT IT DOES NOT COVER list — TWO items — and the WHY THIS BULLET
    IS WORDED THIS WAY paragraph, which is the discipline this repair extends rather than restates**).
    `19-32-SUMMARY.md`'s alias-body credential drive, its `T-19-125` measurement with the substring absence
    verified mechanically, its `T-19-126` standing pins, and its `SEPARATORS` re-derivation with the three
    stale citation sites named.
    `tests/envelope_carrier_reach.rs` and `tests/envelope_wrapper_class.rs` at the two cited sites, read to
    confirm each is a COMMENT line and that no assertion sits on it.
  </read_first>
  <action>
**This task RECORDS what Tasks 1-3 did. Read the tree first and write to that observation, never to a
prediction.**

**1. THE THREE-AXIS CONDITION, replacing the one-axis one in `protected_carrier_named`'s doc.** Write it in
the shape the objective states — AXIS 1 the WORD SET, AXIS 2 the READING, AXIS 3 the PATH SET — with each
axis naming what the predicate reads or protects and then its own silences. **State it from the tree**: if
Task 3 was severed, axis 2 is unchanged and the ceiling is absent; if Task 1 or 2 had been severed the
corresponding axis would say so plainly. **NO COUNT on any axis**, the spellings kept as INSTANCES, the
NARROWED-not-closed language kept for every partially-mitigated instance, and the no-revisit-condition
paragraph kept with its reason and applied to all three axes. **Nothing is handed to a pin, a schedule or a
version witness.** Add the WR-02 note recording what the previous condition said, why it was right when
written (round 12 corrected the READING and the reading was the axis it was about), and what it omitted.

**2. `T-19-125` — the disclosed cost corrected, and the new members added.** Replace *"contains … as a
`/`-anchored substring"* with the true surface: **any word one of whose `/`-anchored substrings NORMALISES
to a path under this run's directory, to an ancestor of it at or under this run's root, or equal to its
binary.** Add the WR-02 note: it was right-sounding, it was FALSE about this code because the comparison
normalises and `dd of=<ENV>/./alpha/x` and `dd of=<ENV>/zzz/../alpha/x` are refused while containing no such
substring, and it is true now. **Add the round's own new cost members beneath the surface**: a here-string
naming a protected path is refused although the program would only have read it as data (same family as
`cat <ledger>`); a word naming the envelope ROOT is refused although the run's own directory is one
component below it; and, if Task 3 landed, a word whose candidate scan exceeds the work ceiling is refused
whatever it names.

**3. `envelope_carrier_refusal`'s recovery lines made true for all three cases.** *"To proceed: name a path
outside that directory"* is not actionable for a word that merely CONTAINS the directory, and is wrong for a
word naming an ancestor. Extend both messages minimally so the recovery is actionable for a word under the
protected directory, a word naming an ancestor at or under the root, and a word that contains one of them
without meaning to. **No command is quoted back (SAFE-04)**, the protected path is still named, and
`git config --get core.hooksPath` and `command -v` still report the paths. **Verify by grep BEFORE editing
that no test asserts the wording**, and record the check.

**4. `word_is_exactly`'s parent-deletion residue, stated at the same weight as its reasoning.** The doc
argues correctly that a PREFIX over the binary's parent would refuse `ls <parent>` and every `cargo
install`. **Add that the parent's own DELETION removes the binary, that `rm -rf <binary-parent>` is exit 0
and is registered as a `T-19-116` route, and that no ancestor clause is written here for the same reason the
prefix is not** — the directory is shared, and refusing a word that names it is an outage. **Do not describe
this as a mitigation.**

**5. `T-19-126`(i) — the inverted sentence in `ledger.rs` corrected, and the standing pins named.** The
comment says the API's literal name is left unspelled so a verify grep does not pass vacuously. **That is
backwards: writing the literal to forbid it turns a zero-occurrence gate RED.** Correct it under WR-02, and
**name the two STANDING pins `19-32` wrote** — the behavioural pin driving a symlinked-to-regular ledger
permitted and counted, and the production-slice pin that CAN name the API because the name sits below the
`#[cfg(test)]` sentinel and outside the slice. **Spell the API name if and only if `19-32`'s pin is a
production-SLICE pin rather than a whole-file grep; verify that from the tree before writing it.** Nothing
else in `ledger.rs` moves.

**6. `cred.rs` — the third item on the WHAT IT DOES NOT COVER list.** Add the `!`-bodied alias-body route
measured by `19-32`: `git -c alias.q='!git -c credential.helper=store credential fill' q` is exit 0 and
returned the ambient `~/.git-credentials` secret under full envelope posture, and the by-name clause does
not reach it because the key is inside an alias BODY rather than on a leading option. **State that this is
`T-19-86`'s route and that it is recorded here rather than folded into `T-19-86`** — widening that row to
carry a credential-reach harm is the attribution move `19-27` and audit 11 established must not be made, and
audit 12 declined it explicitly. **Name the rule that does not reach it rather than asserting that a layer
governs it**, which is the discipline the bullet's own WHY THIS BULLET IS WORDED THIS WAY paragraph states.
Nothing else in `cred.rs` moves.

**7. The `SEPARATORS` citation sweep — COMMENT-ONLY, three sites.** The constant is byte-identical and has
moved to `policy.rs:2422`; `git log -L 2422,2422:src/envelope/policy.rs` returns exactly one commit for the
phase, `84a9b05`. Correct the three citations `19-32` named under `tests/` — in
`tests/envelope_wrapper_class.rs`, `tests/envelope_carrier_reach.rs` and `tests/envelope_interior_path.rs` —
**so each cites the current line.** **Every changed line is a comment; not one assertion moves; re-derive
both `git log -L` results yourself rather than inheriting them.** Record the sweep as a named, bounded
cross-fence exception.

**8. New `#[test]` fns** for anything this task made assertable, ADDED and never folded. Then re-drive
`19-32`'s whole PERMITTED set one final time and diff against Task 1's baseline.

Commit as its own commit, the last code commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-33-t4.log 2&gt;&amp;1 || { echo "FAIL: the suite is not GREEN after the honesty repairs"; rtk proxy grep 'test result:' /tmp/19-33-t4.log | rtk proxy grep -v ' 0 failed'; exit 1; }; rtk proxy grep 'test result:' /tmp/19-33-t4.log | tail -55; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; for f in tests/envelope_wrapper_class.rs tests/envelope_carrier_reach.rs tests/envelope_interior_path.rs; do test "$(rtk proxy grep -c 'policy.rs:2422' $f || true)" -ge 1 || { echo "FAIL: $f does not cite SEPARATORS at its current line"; exit 1; }; done; test "$(git diff --name-only HEAD~1..HEAD -- src/ | rtk proxy grep -vcE 'src/envelope/(policy|ledger|cred)\.rs' || true)" -eq 0 || { echo "FAIL: a src/ file outside the three named exceptions was opened"; exit 1; }; test -z "$(git diff --numstat HEAD~1..HEAD -- src/envelope/hooks.rs src/envelope/mod.rs src/envelope/advisory.rs src/scan.rs src/config.rs)" || { echo "FAIL: a forbidden src/ file moved"; exit 1; }; DEL=$(git diff -U0 HEAD~1..HEAD -- tests/ | rtk proxy grep -c '^-[^-]' || true); test "$DEL" -le 12 || { echo "FAIL: $DEL deleted lines under tests/ — the citation sweep is three comment sites"; exit 1; }; git diff -U0 HEAD~1..HEAD -- tests/ | rtk proxy grep '^-[^-]' | rtk proxy grep -vE '^-[[:space:]]*(//|///)' | rtk proxy grep -q . &amp;&amp; { echo "FAIL: a deleted line under tests/ is not a comment — the sweep is COMMENT-ONLY"; exit 1; }; echo OK</automated>
  </verify>
  <done>The tree was READ before this task wrote anything, and every claim records an observed outcome rather than a prediction — including which of Tasks 1, 2 and 3 landed and whether Task 3 was severed; **`protected_carrier_named`'s residue is stated as a CONDITION over ALL THREE AXES — the WORD SET, the READING and the PATH SET — each naming what the predicate reads or protects and then its own silences, with NO COUNT on any axis, the spellings kept as INSTANCES, the NARROWED-not-closed language kept, the no-revisit-condition paragraph kept and applied to all three, and a WR-02 note recording what the one-axis condition said and what it omitted**; **nothing is handed to a pin, a schedule or a version witness**; the disclosed cost says NORMALISES rather than *contains as a substring*, carries its WR-02 note with both measured instances, and gains the round's new members — the here-string, the envelope root, and the work ceiling if Task 3 landed; `envelope_carrier_refusal`'s two recovery lines are actionable for all three cases with no command quoted back and the grep proving no test asserts the wording recorded BEFORE the edit; **`word_is_exactly`'s doc states the parent-deletion residue at the same weight as its prefix reasoning, names `rm -rf <binary-parent>` as a `T-19-116` route, and does NOT describe the statement as a mitigation**; **`ledger.rs`'s inverted sentence is corrected under WR-02 and names `19-32`'s two STANDING pins, with the API spelled only after verifying from the tree that the pin is a production-SLICE pin**, and nothing else in the file moves; **`cred.rs`'s WHAT IT DOES NOT COVER list gains a THIRD item naming the `!`-bodied alias-body route with its measured verdict and the secret's presence, stating it is `T-19-86`'s route recorded here rather than folded, and naming the rule that does not reach it rather than asserting a layer governs it**, and nothing else in the file moves; **the `SEPARATORS` citation was re-derived independently at `policy.rs:2422` with `git log -L` returning exactly `84a9b05`, and all three sites under `tests/` cite the current line — every changed line a comment, not one assertion moved, and the deleted count within the stated bound**; new `#[test]` fns were ADDED and never folded; `19-32`'s whole PERMITTED set was re-driven a final time with zero moves; the full suite is GREEN with zero failures; and this commit touches `policy.rs`, `ledger.rs`, `cred.rs` and the three comment sites alone, with `hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` at zero diff lines.</done>
</task>

<task type="auto">
  <name>Task 5: the execution record, the registrations, and the gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s "Threats found by audit 12", "`T-19-121` — NARROWED HARD, NOT CLOSED", "The known-open
    set, as audit 12 verified it", "The mechanism pins, verified non-dead", "Gates observed", "Audit 12's
    bookkeeping", "Audit 12 — what the round-12 control can and cannot fail on" and "Audit 12 Sign-Off";
    plus the appended records of plans 19-30, 19-31 and **19-32 — which this plan's subsection FOLLOWS and
    does not edit.** The Accepted Risks Log.
    `deferred-items.md`'s plan-19-32 section and its "Still OPEN and unchanged" rows.
    `19-32-SUMMARY.md`'s per-binary counts for all NINETEEN binaries, its recorded `passed + failed` total,
    its complete RED and PERMITTED lists, and its `SECTION_ENVELOPE` re-measurement.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-32 record and touching nothing that
precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan 19-33 —
carrying:

  - **FIRST, and in these words: `/gsd-secure-phase 19` is NOT cleared, nothing here is described as a
    closure, and this plan does NOT write audit 12's finish-line sentence.** `T-19-86`, `T-19-91`,
    `T-19-111`, `T-19-112`, `T-19-116` and `T-19-121` remain open at `high`; whether `T-19-122` …
    `T-19-126` close is audit 13's judgement; **and whether the open set has become the human-decision
    items plus the deliberately-unruled residues is audit 13's to say, not this plan's** — a plan that
    certified its own round on that question would be this phase's certification defect in its purest form.
  - **`19-32`'s RED rows confirmed still red before any production line moved**, with the verbatim pre-fix
    output and each row's after-verdict; **and `19-32`'s PERMITTED rows re-checked before AND after every
    task**, with any that moved reported as a finding about the RULE.
  - **THE `T-19-122` DESIGN AS IMPLEMENTED, WITH THE DERIVATION THAT DECIDED IT.** The second `Segment`
    field, the recording arm, the third `.chain()` at the one existing reading site; **the table of what
    widening `pathname_target` would have turned red and what the second field turned red (nothing)**,
    naming `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the
    twelve-operator grammar pin and confirming BOTH green after; the rejected third design with its cost;
    the principled restatement that the predicate's subject is what a line NAMES; and the disclosed
    over-refusal with its two permitted controls. **State that `redirection_operator`, `pathname_target`,
    `Segment::redirection_targets`, `segment.tokens`, `Token`, `SEPARATORS` and `is_separator` show ZERO
    behavioural change, verified by reading the diff.**
  - **THE `T-19-123` DESIGN AS IMPLEMENTED, WITH THE PATH SET, THE STOP AND THE COST.** The three-member
    set in its general form; that it grew by EXACTLY ONE path and why; **exactly where it stops and why, in
    ownership and AR-19-11 terms, with the chain above it named**; the four-row ANCESTOR-vs-PREFIX
    discriminator with `19-32`'s pinned rows still permitted, including the user-set-root case; and **the
    residue ABOVE the root registered, disclosed and unaccepted.**
  - **BOTH AUTHORISED PIN MOVES, WITH EACH REASONING QUOTED BEFORE AND AFTER.** `policy.rs:10022-10026` and
    `tests/envelope_interior_path.rs:2341`. State audit 12's own words — *"the code does exactly what the
    pin says … moving it is a design change, not a test edit"* — that each reasoning was REWRITTEN and not
    deleted, that a STOP row was added beside each, that the binary's-parent row did NOT move and why, and
    that **no third row under `tests/` moved that is not a comment**, checked mechanically.
  - **`T-19-124` — WHETHER THE BOUND LANDED AND WHAT IT IS.** If it landed: the reformulation's shape, the
    proof that no component vector moved (both standing mechanical pins green, `19-32`'s cost half and
    rounds 4–12's pin family unchanged), the post-fix curve on three trees against the threshold of 8 and
    against `dd17bfb`'s own numbers, **the fail-closed ceiling with the test that DRIVES it**, and the
    statement that `MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and `MAX_LEDGER_BYTES` kept their values
    because a bound over the wrong property is not corrected by moving a number. **If it was severed: the
    finding with its measurement, `T-19-124` recorded OPEN at `medium` with the property unmet, and the
    statement that the honesty repairs landed anyway.** **Either way the behavioural half stays UNMEASURED
    and is claimed in NEITHER direction.**
  - **THE RESTATED THREE-AXIS CONDITION, QUOTED AS IT NOW STANDS IN THE PREDICATE'S DOC**, with the note
    that it is written over what the predicate READS, what REACHES it and what it PROTECTS; that no count
    is written on any axis; that it was written LAST so it records an observed outcome; and that it is
    handed to no pin, schedule or witness.
  - **ALL FOUR HONESTY REPAIRS, QUOTED BEFORE AND AFTER, each with its WR-02 reasoning and the note that
    each was required regardless of whether any rule landed** — the disclosed cost's substring wording, the
    refusal's recovery lines, `word_is_exactly`'s parent-deletion residue, `ledger.rs`'s inverted gate
    reason with `19-32`'s two standing pins named, and `cred.rs`'s third list item. **Record that the
    alias-body route is `T-19-86`'s and was NOT folded**, and that the repair names the rule that does not
    reach it rather than asserting a layer governs it.
  - **THE `SEPARATORS` CITATION SWEEP**, with both `git log -L` results re-derived independently, the three
    corrected sites named, and the statement that it was COMMENT-ONLY and mechanically gated.
  - **THE FILE EXCEPTIONS STATED AS EXCEPTIONS**, each with its bound and the verification it held, and the
    explicit statement that **`hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` were NOT
    OPENED** and that **`SECTION_ENVELOPE` shows zero diff lines with its headroom unspent**, citing
    `19-32`'s re-measurement.
  - **THE SLICE PLACEMENT**, stated as a mechanical fact: every helper this round added sits inside
    `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so`'s region and a positive
    control names each.
  - the mechanism pins and byte floors re-measured across rounds 4 through 12 with `envelope_config_resolution`
    at 30/0; **round 6's deletion model and round 11's redirection channel called out by name as the two
    mechanisms this round sat closest to, with their controls at exit 0**; `C-11` … `C-15` at control (e)
    with no `pr_cap_*` clamp; `C-08`'s, `T-19-120`'s and `T-19-124`'s behavioural halves UNMEASURED;
    `T-19-115` with NO rule and its instances on axis 2; **`T-19-116` NARROWED with the here-string route
    closed and `rm -rf <binary-parent>` plus the four original residues still open, stated at the same
    weight as the narrowing**; the `glab --host` cell unchanged with `--hostname` KEPT; **`T-19-17r`
    OUTSTANDING for the eighteenth time with no `AR-19-13` and no use of the word "accepted"**; `AR-19-04`,
    `AR-19-05` and `T-19-23` untouched; and the gate arithmetic.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any earlier
appended subsection.

**2. `deferred-items.md`.** Append a plan-19-33 section recording `T-19-122` … `T-19-126` as
NARROWED-or-CORRECTED and **NOT CLOSED**, each with what remains; **`T-19-116` NARROWED with the here-string
route closed and `rm -rf <binary-parent>` plus its four original residues named**; **the ancestor residue
ABOVE the root registered as new, disclosed and unaccepted**; `T-19-115` OPEN with no rule; the residue
recorded as a THREE-AXIS CONDITION with no count wherever this plan states it; and `T-19-86`, `T-19-91`,
`T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-121`, `T-19-74`, `C-08`, `C-15`, the
`glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open.

**3. The gate, with its arithmetic STATED and CHECKED.**

  - `rtk proxy cargo test --no-fail-fast`, counted with `rtk proxy grep` over a redirected log, never a
    plain `grep` (D-34).
  - Gate on `passed + failed` against the exact total `19-32-SUMMARY.md` records, **and state the
    arithmetic: a red test RAN, so red→green leaves the total unchanged and any increase comes ONLY from
    the new `#[test]` fns this plan adds**, counted from `git show`. A disagreement is a finding.
  - **ZERO failures** — this plan's job is to turn `19-32`'s RED green without turning anything else red.
  - **All NINETEEN `envelope_*` binaries must have RUN**, with per-binary counts recorded.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.
  - The four documented flakes are out of scope; record any firing with its verbatim output, state that
    absence is not evidence they are fixed, and claim the ETXTBSY race's relation to this round in NEITHER
    direction.
  - **`git diff --name-only` over `src/` across this plan's commits lists exactly `policy.rs`, `ledger.rs`
    and `cred.rs` and nothing else**, and **every deleted line under `tests/` is either a comment or the
    ONE authorised ancestor row**, checked mechanically.

Commit the record as its own commit, the last.
  </action>
  <verify>
    <automated>BASE=$(git log --format=%H -n 1 --grep='^docs(19-32)'); test -n "$BASE" || { echo "FAIL: cannot locate 19-32's record commit — the range this gate compares over is unknown"; exit 1; }; rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-33-gate.log 2&gt;&amp;1 || { echo "FAIL: the gate is not GREEN"; rtk proxy grep 'test result:' /tmp/19-33-gate.log | rtk proxy grep -v ' 0 failed'; exit 1; }; rtk proxy grep 'test result:' /tmp/19-33-gate.log | tail -55; git log --oneline -6; git diff --name-only "$BASE"..HEAD -- src/; test "$(rtk proxy grep -cE '^\| AR-19-13 \|' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md || true)" -eq 0 || { echo "FAIL: an AR-19-13 row exists"; exit 1; }; test "$(rtk proxy grep -c 'Running tests/envelope_' /tmp/19-33-gate.log || true)" -eq 19 || { echo "FAIL: not all NINETEEN envelope_* binaries ran"; exit 1; }; test "$(git diff --name-only "$BASE"..HEAD -- src/ | rtk proxy grep -vcE 'src/envelope/(policy|ledger|cred)\.rs' || true)" -eq 0 || { echo "FAIL: a src/ file outside the three named exceptions was opened"; exit 1; }; test -z "$(git diff --numstat "$BASE"..HEAD -- src/envelope/hooks.rs src/envelope/mod.rs src/envelope/advisory.rs src/scan.rs src/config.rs)" || { echo "FAIL: a forbidden src/ file moved"; exit 1; }; test -z "$(git diff --name-only "$BASE"..HEAD -- Cargo.toml Cargo.lock)" || { echo "FAIL: a manifest moved (T-19-SC)"; exit 1; }; DEL=$(git diff -U0 "$BASE"..HEAD -- tests/ | rtk proxy grep -c '^-[^-]' || true); test "$DEL" -le 18 || { echo "FAIL: $DEL deleted lines under tests/ across this plan"; exit 1; }; git diff -U0 "$BASE"..HEAD -- tests/ | rtk proxy grep '^-[^-]' | rtk proxy grep -vE '^-[[:space:]]*(//|///)|PARENT of the envelope directory' | rtk proxy grep -q . &amp;&amp; { echo "FAIL: a deleted line under tests/ is neither a comment nor the one authorised ancestor row"; exit 1; }; echo OK</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-33 record stating FIRST that `/gsd-secure-phase 19` is NOT cleared, that nothing here is a closure, and **that this plan does NOT write audit 12's finish-line sentence because whether the open set has become the human-decision items plus the deliberately-unruled residues is audit 13's judgement**; `19-32`'s RED rows confirmed still red before any production line moved with the verbatim pre-fix output and each after-verdict, and its PERMITTED rows re-checked before AND after every task; **the `T-19-122` design as implemented with the design-(A)-versus-(B) table naming both fenced pins and confirming both green after, the rejected third design with its cost, the principled restatement, the disclosed over-refusal with its two controls, and the zero-behavioural-change list verified by reading the diff**; **the `T-19-123` design as implemented with the three-member path set in its general form, that it grew by exactly one path and why, exactly where it stops and why in ownership and AR-19-11 terms with the chain above it named, the four-row discriminator including the user-set-root case, and the residue above the root registered**; **BOTH authorised pin moves with each reasoning quoted BEFORE and AFTER, audit 12's own words on why moving them is a design change, the STOP rows added beside each, the binary's-parent row left unmoved with its reason, and the mechanical check that no third non-comment row under `tests/` moved**; **`T-19-124` recorded as landed — with its shape, both standing mechanical pins green, the post-fix three-tree curve against the threshold and against `dd17bfb`, the fail-closed ceiling and the test that DRIVES it, and the three constants unmoved — or as SEVERED with its measurement and the property recorded unmet, with the behavioural half claimed in neither direction either way**; **the RESTATED THREE-AXIS CONDITION quoted as it now stands, with no count on any axis, written LAST so it records an observed outcome, and handed to no pin, schedule or witness**; **all four honesty repairs quoted before and after with their WR-02 reasoning and the note that each was required regardless of any rule, including `ledger.rs`'s inverted gate reason with `19-32`'s two standing pins named and `cred.rs`'s third item recorded as `T-19-86`'s route and explicitly NOT folded**; the `SEPARATORS` citation sweep with both `git log -L` results re-derived and the three corrected sites named as COMMENT-ONLY and mechanically gated; the file exceptions stated AS exceptions with the explicit statement that `hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` were NOT opened and that `SECTION_ENVELOPE` shows zero diff lines; the slice placement with a positive control naming every helper added; the mechanism pins and byte floors re-measured across rounds 4 through 12 with `envelope_config_resolution` at 30/0 and round 6's deletion model and round 11's redirection channel called out by name; `C-11` … `C-15` at control (e) with no clamp; `C-08`'s, `T-19-120`'s and `T-19-124`'s behavioural halves claimed in neither direction; `T-19-115` with no rule; **`T-19-116` NARROWED with the here-string route closed and `rm -rf <binary-parent>` plus its four original residues still open, stated at the same weight as the narrowing**; the `glab --host` cell unchanged; and `T-19-17r` OUTSTANDING for the eighteenth time with no `AR-19-13` and no use of "accepted" — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection; `deferred-items.md` carries a plan-19-33 section recording all five rows NARROWED-or-CORRECTED and NOT CLOSED, `T-19-116` narrowed with its remaining routes, **the ancestor residue ABOVE the root registered as new, disclosed and unaccepted**, `T-19-115` restated, and every other open item unchanged; the `--no-fail-fast` run reports **ZERO failures**, `passed + failed` exceeds `19-32`'s total by exactly the number of new `#[test]` fns counted from `git show` with the arithmetic stated and checked, and ALL NINETEEN `envelope_*` binaries RAN with per-binary counts recorded; `git diff --name-only` over `src/` across this plan's commits lists exactly `policy.rs`, `ledger.rs` and `cred.rs`; **every deleted line under `tests/` is a comment or the one authorised ancestor row**, checked mechanically and within the stated bound; neither manifest moved; and any documented flake that fired is recorded with its verbatim output, unfixed, with its relation to this round claimed in neither direction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, and to the files and binary the guard's own controls live in |
| **a word the LINE contains → a word the PREDICATE receives** | **Closed for LITERAL words by carrying non-pathname redirection targets on a SECOND `Segment` field and chaining them as a THIRD word class at the one existing reading site, with bash's pathname/non-pathname split left byte-identical and read from both sides** |
| **a path the COMPARISON protects → a path whose destruction destroys it** | **Closed for the envelope directory's ancestors up to the envelope ROOT by an ANCESTOR clause beside the PREFIX one. NOT closed above the root, and NOT closed for the binary's parent — both stated as residues at the same weight** |
| a rule's stated residue → the spellings its silence permits | Restated as a CONDITION over THREE axes — the word set, the reading and the path set — with no count on any axis, handed to no control, and written LAST so it records an observed outcome |
| a control's stated CAPABILITY → the quantity it bounds | The candidate scan's work becomes linear in the word's length, which is what makes `MAX_GUARD_REQUEST_BYTES` a genuine bound on work; a fail-closed ceiling is the backstop and a test DRIVES it |
| an evidence artefact's claimed WEIGHT → what it can actually fail on | The `ledger.rs` one-shot grep is replaced in the record by `19-32`'s two standing pins, and the comment's inverted reason is corrected |
| a doc's disclosed COST → the surface the code actually refuses | The disclosed cost says NORMALISES rather than *contains as a substring*, and gains the here-string, the envelope root and the work ceiling as members |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-122 | Elevation of Privilege / Tampering | `consume_redirection` (`policy.rs:3532-3609`) and the two word classes (`:6118-6128`) | high | **mitigate — NARROWED, and this plan does not call it closed** | A SECOND `Segment` field carrying LITERAL NON-PATHNAME redirection target words, populated in the SAME arm from the SAME `skip_redirection_target` by-product, chained as a THIRD word class at the ONE existing reading site; **the design decided by the fenced-file enumeration rather than by taste**, with `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator grammar pin green after; the cost disclosed from both sides against `19-32`'s `cat <<<x`, `cat <<< /tmp/x` and `git <<<x push --force` controls, with a red one there treated as a rule that banned a channel instead of reading a path |
| T-19-123 | Elevation of Privilege / Tampering | `word_is_within` (`policy.rs:5685-5711`) and the two pins at `policy.rs:10022` and `tests/envelope_interior_path.rs:2341` | high | **mitigate — NARROWED, and the residue above the root is registered rather than accepted** | An ANCESTOR clause beside the PREFIX one, over the same component vectors the other way round, applied per candidate inside the same walk, **bounded at the envelope ROOT on ownership and AR-19-11 grounds**; the set grows by exactly one path and the clause is written in its general form; both fenced pins MOVED as a named cross-fence exception with each reasoning REWRITTEN rather than deleted and a STOP row added beside each; **`19-32`'s sibling, second-alias, user-set-root, root's-parent and `/tmp` rows pinned PERMITTED so a prefix widened to the root and an unbounded chain both turn a file red.** No ancestor clause for the binary; its parent-deletion residue stated in `word_is_exactly`'s doc as an honesty repair and never as a mitigation |
| T-19-124 | Denial of Service | `slash_anchored_candidates` applied per candidate at `policy.rs:5697` and `:5760` | medium | **mitigate (SEVERABLE) — NARROWED, not called closed** | The scan's work made LINEAR in the word's length so `MAX_GUARD_REQUEST_BYTES` becomes a genuine bound on work, with a FAIL-CLOSED work ceiling as the backstop refusing at `EnvelopeAssertionFailed` and **a test that DRIVES it**; every component vector proved unchanged by two standing mechanical pins plus `19-32`'s cost half and rounds 4–12's pin family; the curve re-measured on three trees against the pinned ratio; `MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and `MAX_LEDGER_BYTES` untouched. **Severed rather than allowed to move a verdict or to need a budget threaded through `hooks.rs`. The behavioural half stays UNMEASURED** |
| T-19-125 | Information Disclosure (documentation accuracy) | the disclosed cost (`policy.rs:6044-6048`); `envelope_carrier_refusal`'s recovery lines (`:6168`, `:6180`) | low | **mitigate — the correction lands whether or not any rule does** | The surface restated over NORMALISATION rather than substring containment, with a WR-02 note and `19-32`'s two measured instances; the round's three new cost members added beneath it; both recovery lines made actionable for a word under the directory, a word naming an ancestor, and a word that merely contains one — with no command quoted back and the no-test-asserts-the-wording grep recorded before the edit |
| T-19-126 | Tampering (evidence quality) | `ledger.rs`'s KIND-check comment | low | **mitigate — the two STANDING pins are `19-32`'s; the inverted reason is corrected here** | The sentence corrected under WR-02 — writing the forbidden API's literal name turns a zero-occurrence gate RED rather than making it pass vacuously — and the two standing pins named in place, with the API spelled only after verifying from the tree that the replacement is a production-SLICE pin whose name sits below the `#[cfg(test)]` sentinel |
| T-19-116 | Elevation of Privilege | `C-10`, the binary every stub and `guard_command` exec | high | **carried forward OPEN — NARROWED, NOT CLOSED** | The third word class closes `xargs cp /bin/true <<< <BINARY>` and its `-I{}` spelling; **`rm -rf <binary-parent>` stays exit 0 because no ancestor clause is written for the binary**, beside the expansion-borne, tilde, relative and `PATH`-symlink residues. All five named at the same weight as the narrowing |
| T-19-121 | Information Disclosure / Elevation of Privilege | the `credential.helper` clause's residue; `cred.rs`'s NOT-COVERED list | high | **carried forward OPEN — a bounded residue of a control that landed** | The `!`-bodied alias-body route ADDED to the list as a route the named rule does not reach, with its measured verdict and the secret's presence. **Recorded as `T-19-86`'s route and NOT folded into `T-19-86`, not used to re-rate anything** — audit 12 declined and so does this plan |
| T-19-107-shape | Repudiation | a control's own text claiming a reach, a bound or an evidentiary weight the measurement denies | — | mitigate | Four claims corrected under the WR-02 discipline, each required regardless of any rule; each names the rule that acts or plainly names its absence rather than asserting that a layer governs something; `SECTION_ENVELOPE` left byte-identical with its headroom unspent |
| T-19-115, T-19-111, T-19-112, T-19-113 | Spoofing / Elevation of Privilege / Tampering | the tilde, glob and brace spellings; `C-11`'s `.git/config`; the SAFE-06 ledger and the hook stubs as FILES | medium / high / high / medium | **carried forward OPEN or NARROWED-NOT-CLOSED** | No rule is written for `T-19-115` and none can be — a tilde needs the environment and a glob the filesystem; its instances sit on axis 2 of the restated condition. `19-27`'s five-site attribution correction stays as performed. Both new classes narrow `T-19-112`'s cap route further without closing it; nothing here is described as closure |
| T-19-86, T-19-91 | Elevation of Privilege | `classify_git`'s denylist default arm through a governed program's own operand; a decision operand assembled by expansion | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; rows at exit 0, pins green and UNMODIFIED. Because both remain open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-96, T-19-110, T-19-74 | Elevation of Privilege | a glob in a push flag; region 2's operand grammar; the frozen wrapper rows | medium | **accept for this plan (registered, not fixed)** | Left exactly as pinned |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — gap RECORDED, acceptance deliberately unmade for the eighteenth time** | No `AR-19-13`, no use of the word "accepted" |
| — | Tampering | `C-08`'s claimed-but-unwired second carrier; `C-15`'s unclamped `pr_cap_*`; `C-12`; `C-13`; `C-14` | planner-derived / control (e) | **carried forward UNFIXED** | No rule, no clamp, no acceptance. `C-08`'s, `T-19-120`'s and `T-19-124`'s behavioural halves stay UNMEASURED |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added. `Cargo.toml` and `Cargo.lock` are not in `files_modified` and the gate asserts neither moved. Every probe uses `std::process::Command` inside `#[cfg(test)]` |
</threat_model>

<verification>
- **`19-32`'s complete RED list was confirmed STILL RED and its complete PERMITTED list STILL PERMITTED
  against the unmodified tree before any production line moved**, both recorded verbatim. A row already
  green is a finding, reported rather than proceeded past.
- **`19-32`'s PERMITTED set was re-driven after EVERY task and diffed against the Task-1 baseline with zero
  moves.** A row that moved from permitted to refused is a finding about the RULE, never an assertion to
  edit — and `cat <<<x`, `cat <<< /tmp/x`, `git <<<x push --force origin main`,
  `ls <ENV>/unrelated-sibling`, `<ENV>/beta`, the user-set-root case, `ls <ENV-PARENT>`, `/tmp`, both
  binary EXACT-PATH controls and the 100 000-character no-slash word are the ten that catch each of this
  round's four possible over-widenings.
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34), with **ZERO failures**, the number read as `passed + failed` with `rtk proxy grep`, and the
  arithmetic stated and checked against the new `#[test]` fns counted from `git show`.
- **ALL NINETEEN `envelope_*` binaries RAN** with per-binary counts recorded.
- **`git diff --name-only` over `src/` lists exactly `policy.rs`, `ledger.rs` and `cred.rs`.**
  `src/envelope/hooks.rs`, `src/envelope/mod.rs`, `src/envelope/advisory.rs`, `src/scan.rs` and
  `src/config.rs` show ZERO diff lines.
- **`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator
  grammar pin are GREEN and were run IMMEDIATELY after the word-class change** — the mechanical proof the
  flag was not widened. Neither was edited.
- `Segment::redirection_targets`, `RedirectionOperator::pathname_target`, `redirection_operator`,
  `segment.tokens`, `Token`, `SEPARATORS` (byte-identical at `policy.rs:2422`, ONE commit in the phase,
  `84a9b05`), `is_separator` (`is_separator(">")` still `false`), `tokenize`'s stream,
  `split_segments_with_heads`, `resolve_program`, `resolve_program_with_head`,
  `first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api`,
  `forbidden_repo_path` and `forbidden_repo_prefixes` show ZERO behavioural change, verified by reading the
  diff. The SEGMENT-COUNT pins are green in both the SPLIT and the DISPLACED variant.
- **EXACTLY ONE `policy.rs` unit pin moved — the ancestor row — and both authorised pin moves carry their
  reasoning REWRITTEN rather than deleted, each with a STOP row added beside it.** The binary's-parent row
  did not move. **Every deleted line under `tests/` across this plan is either a comment or that one
  authorised row**, checked mechanically, and the deleted count is within the stated bound.
- Every helper added sits **AT OR AFTER** `fn lexical_absolute_components(word: &str)`, so it falls inside
  `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so`'s region, and a POSITIVE
  CONTROL NAMES EACH. `lexical_absolute_components`' signature text and behaviour are unchanged.
- The new word class runs AFTER the `!token.operator && token.literal` filter and the new field carries only
  literal words by construction; the tilde, glob, brace and expansion-borne rows are still PERMITTED in
  every word position.
- **`containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte` and
  `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` are GREEN**, which
  is the mechanical proof `T-19-124`'s reformulation moved no component vector.
- **The fail-closed work ceiling is REACHABLE under `MAX_GUARD_REQUEST_BYTES` and a test DRIVES it**,
  observing the refusal at `envelope_assertion_failed`; `MAX_GUARD_REQUEST_BYTES`, `GUARD_TIMEOUT_SECS` and
  `MAX_LEDGER_BYTES` keep their values — or Task 3 was SEVERED with its measurement recorded.
- **`envelope_config_resolution` is 30/0 and not one verdict moved**, checked after each code commit and
  again at the gate. Every mechanism pin from rounds 4 through 12 is green, with round 6's deletion model
  and round 11's redirection channel called out by name as the two this round sat closest to.
- **All four honesty repairs landed**, each quoted before and after, each in a task that records an observed
  outcome rather than a prediction because the doc task is ordered LAST.
- **The residue is stated as a CONDITION over THREE axes with no count on any of them**, and nothing is
  handed to a pin, a schedule or a version witness.
- **`T-19-116` is recorded NARROWED and OPEN at `high`**, with the here-string route closed and
  `rm -rf <binary-parent>` plus the four original residues named at the same weight.
- The `SEPARATORS` citation was re-derived independently at `policy.rs:2422` and all three sites under
  `tests/` cite the current line; **the sweep is COMMENT-ONLY and mechanically gated.**
- `POLICY_MIN_PRODUCTION_BYTES`, `HOOKS_MIN_PRODUCTION_BYTES`, the proportional floor's value, the deep
  anchor and the sentinel counts are unchanged; no second `#[cfg(test)]` sentinel was introduced in any of
  the three files.
- The `AR-19-13` row count is zero, the word "accepted" is not applied to `T-19-17r`, no `AR-` row is added,
  edited, renumbered or un-accepted, and `T-19-23` is not marked closed.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml` nor
  `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-32 record.
- Five commits exist in order — the third word class; the ancestor clause and both pin moves; the work bound
  (or a recorded severance with no commit); the honesty repairs and the citation sweep; the record — **with
  the doc task ORDERED AFTER all three rule tasks, so the three-axis condition and every repaired claim
  record an observed outcome; under severance this plan has FOUR commits and every repair still lands.**
</verification>

<success_criteria>
1. **`19-32`'s RED rows were confirmed still red before the first production line moved, and its PERMITTED
   rows were confirmed still permitted after every task** — with any move reported as a finding about the
   RULE rather than edited.
2. **A literal absolute path arriving in a word the tokenizer DELETES is refused, at the ONE existing
   reading site**, by a SECOND `Segment` field and a THIRD word class — **and the design was decided by the
   fenced-file enumeration, with both redirection pins green after and neither edited.**
3. **A directory destroyed by naming its parent is refused**, by an ANCESTOR clause beside the PREFIX one,
   **bounded at the envelope ROOT**, growing the protected set by exactly one path — and it is an ancestor
   clause rather than a prefix widened to the root, proved by `19-32`'s sibling, second-alias and
   user-set-root rows staying permitted, and bounded above by the root's-parent and `/tmp` rows staying
   permitted.
4. **Both authorised pin moves were performed with each reasoning REWRITTEN rather than deleted**, a STOP
   row added beside each, the binary's-parent row left in place with its reason, and **no third non-comment
   row under `tests/` moved**, checked mechanically.
5. **`T-19-124`'s bound is on WORK — linearity in the word's length, with a fail-closed ceiling a test
   DRIVES — or the task was SEVERED with its measurement recorded**, and either way no component vector
   moved, the three constants kept their values, `hooks.rs` was not opened, and the behavioural half is
   claimed in neither direction.
6. **The residue is a CONDITION over THREE axes — the word set, the reading and the path set — with no
   count on any axis, written LAST so it records an observed outcome, and handed to no pin, schedule or
   witness.**
7. **All four honesty repairs landed** — the disclosed cost's normalisation, the refusal's recovery lines,
   `word_is_exactly`'s parent-deletion residue, `ledger.rs`'s inverted gate reason and `cred.rs`'s third
   list item — each under the WR-02 discipline, each naming the rule that acts or plainly naming its
   absence, and **the alias-body route recorded as `T-19-86`'s and NOT folded.**
8. **`T-19-116` is NARROWED and stated OPEN at `high`**, with the here-string route closed and
   `rm -rf <binary-parent>` plus the four original residues named at the same weight as what closed.
9. **The `SEPARATORS` citation is corrected at all three sites under `tests/`, COMMENT-ONLY and
   mechanically gated**, with both `git log -L` results re-derived independently.
10. **Three `src/` files were opened and no more**; `hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and
    `config.rs` show zero diff lines, and `SECTION_ENVELOPE`'s headroom is unspent.
11. Every helper added sits INSIDE the no-filesystem slice with a positive control naming it, and
    `lexical_absolute_components`' signature text is unchanged.
12. The gate is green with ZERO failures, all nineteen `envelope_*` binaries ran, the arithmetic is stated
    and checked, `T-19-17r` is OUTSTANDING for the eighteenth time, and the SUMMARY states FIRST that
    `/gsd-secure-phase 19` is NOT cleared and **that this plan does not write audit 12's finish-line
    sentence** — with no unqualified "T-19-60 is closed" anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-33-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared, that `T-19-86`, `T-19-91`,
`T-19-111`, `T-19-112`, `T-19-116` and `T-19-121` remain open at `high`, that nothing here is described as a
closure, and **that this plan does NOT write audit 12's finish-line sentence because that is audit 13's
judgement**; **`19-32`'s complete RED list with its verbatim pre-fix output and each row's after-verdict,
and its complete PERMITTED list re-checked after every task with the zero-move diff**; the commit SHAs in
order with `git show --stat`, and the note that the doc task is ORDERED LAST so every repaired claim records
an observed outcome; **the `T-19-122` design as implemented with the design-(A)-versus-(B) table naming both
fenced pins and confirming both green, the rejected third design, the principled restatement, the disclosed
over-refusal with its controls, and the zero-behavioural-change list read out of the diff**; **the
`T-19-123` design as implemented with the three-member path set, that it grew by exactly one path and why,
exactly where it stops and why, the four-row ANCESTOR-vs-PREFIX discriminator including the user-set-root
case, and the residue above the root**; **both authorised pin moves with each reasoning quoted BEFORE and
AFTER, the STOP rows added beside each, and the binary's-parent row left unmoved with its reason**;
**whether `T-19-124`'s bound landed, its shape, the post-fix three-tree curve against the pinned ratio and
against `dd17bfb`, the fail-closed ceiling and the test that drives it — or the severance with its
measurement — and the behavioural half claimed in neither direction**; **the RESTATED THREE-AXIS CONDITION
quoted verbatim from the predicate's doc, with no count on any axis, handed to nothing**; **all four
honesty repairs quoted before and after with their WR-02 reasoning, the alias-body route recorded as
`T-19-86`'s and explicitly NOT folded, and `ledger.rs`'s inverted reason corrected with `19-32`'s two
standing pins named**; the `SEPARATORS` citation sweep with both `git log -L` results and the three
corrected sites; the slice placement with a positive control naming every helper; **what closing
`T-19-122` does and does NOT do for `T-19-116`, with `rm -rf <binary-parent>` and the four original residues
at exit 0**; the `envelope_config_resolution` 30/0 confirmation; the mechanism pins and byte floors
re-measured across rounds 4 through 12 with round 6's deletion model and round 11's redirection channel
called out by name; the statement that `hooks.rs`, `mod.rs`, `advisory.rs`, `scan.rs` and `config.rs` were
NOT opened and that `SECTION_ENVELOPE` shows zero diff lines; the gate with `passed + failed`, the
arithmetic checked against `git show`, zero failures and all nineteen binaries; `AR-19-04`, `AR-19-05` and
`T-19-23` carried forward untouched; `C-08`, `C-15`, `T-19-110`, `T-19-115` and the `glab --host` cell
carried forward unchanged; the `T-19-17r` bookkeeping gap OUTSTANDING with the explicit statement that this
plan did NOT accept it and added no Accepted-Risks-Log row; any documented flake that fired recorded with
its output and not fixed; and a "what remains uncovered" section naming `T-19-86` FIRST — open at `high` by
user scoping decision — followed by `T-19-91`, `T-19-111`, `T-19-112`, `T-19-116` and `T-19-121` (open at
`high`), then `T-19-113`, `T-19-115`, `T-19-122`, `T-19-123`, `T-19-124`, `T-19-125`, `T-19-126` (narrowed
or corrected — none closed by this plan's claim), **the ancestor residue ABOVE the root as a NEW registered
residue**, `T-19-96`, `T-19-110`, `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit
statement that only the WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>

