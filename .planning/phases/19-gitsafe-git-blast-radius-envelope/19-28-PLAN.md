---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 28
type: execute
wave: 18
gap_closure: true
gap_ids: [T-19-116, T-19-118, T-19-115, T-19-117]
depends_on: [19-27]
files_modified:
  - tests/envelope_carrier_reach.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-04, SAFE-05, SAFE-06]

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, and this is the EIGHTH time that ordering is the reason a plan exists separately.** It worked seven times — 19-14/19-15, 19-16/19-17, 19-18/19-19, 19-20/19-21, 19-22/19-23, 19-24/19-25, 19-26/19-27 — and the cross-plan carry-forward is what proves a rule load-bearing. This plan writes the corpus and the reproducers and STOPS. `19-29` writes the rules and the honesty repairs. **Every commit here shows ZERO `src/` hunks under `git show --stat`.**"
    - "**AUDIT 10'S FINDING IS ADOPTED WHOLE AND IT SETS THIS ROUND'S SHAPE: THE MODELLED SURFACE IS COMPLETE, SO THIS ROUND FINISHES THE TENTH PLANE RATHER THAN LOOKING FOR AN ELEVENTH.** Audit 10 swept every cell its mandate named — carriers outside the root (`C-10` … `C-15`, all enumerated), the guard's own binary and the path that execs it (`C-10`), what changes WHEN the guard runs (`C-08`, `C-10` stop it; `C-01`'s SIZE can make it miss its deadline), the state the guard carries between calls (a fresh process reading exactly three things that outlive it — `C-01`, `C-14`, `C-15`), and whether a permitted read can locate a carrier for an ungoverned write (direction (ii), reproduced). It checked the off-plane candidates rather than assuming them: the ENVIRONMENT is `E-01`, modelled by `ENVELOPE_ENV_KEYS` and drift-pinned, with `GSD_MM_ENVELOPE_ROOT` measured INERT again; TIME's only lever on the 24 h window is the ledger and APPENDING to it counts toward the cap (`T-19-36`), so that direction fails CLOSED; the network probe runs once at run start; and `gsd-meta-manager envelope --help` exposes `pre-push`, `pre-commit`, `askpass`, `guard` and `scan` and **no `install`**. **Round 10 enumerated fifteen carriers, gave NINE a rule, FIVE control (e), and ONE nothing at all. This round's product is that every one of the fifteen ends it with a letter.**"
    - "**`C-10` IS THE WIDEST CARRIER OF THE FIFTEEN AND IT HAS NO THREAT ID, NO SEVERITY, NO DEFERRED-ITEMS ROW AND NO CONTROL LETTER — AND THAT IS HOW IT SURVIVED A ROUND THAT ENUMERATED IT.** `19-26` wrote it down as `C-10` and marked it UNOBSERVED; `19-27`'s disposition table has no row for it. Audit 10 drove it end to end: `cp /bin/true <binary>` is **exit 0** through the guard, names no envelope path and never touches `GIT_CONFIG_COUNT`; against a rebuilt bare-remote fixture with `pre-push`/`pre-commit` delivered exactly as the envelope delivers them, a force push the hook had REFUSED then **COMPLETED and moved `main` `01f9664` → `2537e97`**, and restoring the binary restored the refusal. **The same replacement makes the GUARD ITSELF answer exit 0** on `git push --force origin main`, where the real binary answers exit 2. **That is layer 2 AND layer 3 in one permitted call — strictly wider than `C-02`** (`T-19-113`, `medium`), which removes layer 3 only, and wider than `env -u GIT_CONFIG_COUNT`, which removes layer 3 only and is REFUSED. **`T-19-01`'s declared mitigation is correct for what it covers and covers nothing here**: `assert_provenance_in` certifies a RELOCATED COPY, and a replaced exec TARGET never re-enters the binary that would assert provenance. The corpus's own `record_only(\"C-10 the exec target\")` comment says exactly this and was never driven. **This plan drives it, and every leg carries a control.**"
    - "**`T-19-118` IS `T-19-23` REOPENED, IT FALSIFIES `SECTION_ENVELOPE`'S *FIRST* `Guaranteed` CLAUSE, AND IT ARRIVES THROUGH FAIL-OPEN DIRECTION (i).** `printf '[credential]\\n\\thelper = store\\n' >> <env>/<alias>/gitconfig` is **exit 0** through the guard — a REDIRECTION, which rule (a) cannot see — and `git credential fill` for `https://github.com` then returns the ambient username and password from `~/.git-credentials`, against a CONTROL in which the same file, exactly as `write_gitconfig_in` leaves it (`cred.rs:393-415`), answers `git config --get credential.helper` at exit 1 with no helper. Both halves of the clause are then false: the generated file DOES name a credential helper, and the run DID reach the ambient credentials. `T-19-23` is `high` and `closed`, and its declared mitigation is precisely the control that consists of the generated file naming no helper. **`19-27` repaired the `Guaranteed` CAP clause and left this `Guaranteed` CREDENTIAL clause unqualified, one clause over from the repair, although `19-26` had already recorded the falsification.** `AR-19-04` accepts `T-19-29` (*\"agent rewrites the generated git config\"*, `medium`) on the rationale *\"the envelope regenerates it at each run start\"*, which three records now note does not cover a write DURING the run — **that acceptance is about TAMPERING at medium and says nothing about credential reachability. It is RECORDED here and NOT un-accepted; un-accepting a risk is a human decision.**"
    - "**THE TENSION ROUND 10 LEFT MUST BE ADDRESSED HEAD-ON RATHER THAN RE-ASSERTED, AND THIS PLAN'S JOB IS TO MEASURE THE THING THAT DECIDES IT.** `19-26`'s plan-check and audit 10 both called direction (i) *\"real by MECHANISM, not by choice\"*: `tokenize` (`policy.rs:3144-3220`) consumes bash's redirection production and emits no token for the operator **or its target**, because bash deletes both before `execve`, so the pinned-PERMITTED `printf … >` row cannot flip under a rule reading operands. **That reasoning is sound about the TOKENIZER and it is now demonstrated HARMFUL: a fail-open direction carrying a live `high`-severity credential exploit is no longer an acceptable residue.** The mandate's question — does closing it mean the DELETION MODEL must read redirection TARGETS — is answered in this plan's objective and MEASURED here: **it does not.** Round 6's model answers *which words ARRIVE at the program*, and a redirection target does not arrive; rule (a) asks a different question about the same walk — *does this line NAME a path this run's controls live in*. `skip_redirection_target` (`policy.rs:3007-3057`) already walks the target's extent, with the target's own quoting rules, in order to skip it; its doc's sentence *\"Its text is never needed, only its extent\"* is the sentence that becomes false. **The measurement that decides whether that is achievable is the SEGMENT-COUNT pin below, and it is this plan's sharpest new control.**"
    - "**THE SEGMENT-COUNT PIN IS THE ONE MECHANICAL CONTROL WITHOUT WHICH `19-29` CANNOT BE CERTIFIED, AND IT IS DERIVED FROM READING `split_segments_with_heads` RATHER THAN FROM CAUTION.** That function (`policy.rs:2444-2526`) **excludes every operator token from `segment.tokens`** (`if token.operator { … continue; }`) and flushes `current` into a `Segment` the instant one arrives. So a redirection-target token emitted INTO the token stream would **split `git >/dev/null push --force origin main` into two segments** — `[git]` and `[push, --force, origin, main]` — and the first resolves `Governed` with an EMPTY argv, which `classify_git` answers `Allow` for. **A rule written the obvious way would silently convert this phase's own round-6 headline row from a refusal into a permit.** This plan therefore asserts, over `policy::split_segments_with_heads` directly and in both directions, that `git >/dev/null push --force origin main` produces exactly **ONE** `Segment` whose tokens are exactly `[git, push, --force, origin, main]`, and that `git x2>/tmp/o push --force origin main` produces exactly ONE whose tokens are exactly `[git, x2, push, --force, origin, main]`. **Both are GREEN today and must stay green through `19-29`.** The existing `Segment::redirection_unresolvable` machinery (`:2402-2418`, `:2461-2505`) is the precedent the fix must follow: a fact carried on the SEGMENT, accumulated in the operator arm of the ONE walk, applied retroactively over `segments[command_start..]` and reset at each REAL command operator — with `segment.tokens` byte-for-byte unchanged."
    - "**`T-19-115` IS `T-19-76`'S FAILURE MODE FOR THE TENTH CONSECUTIVE ROUND, AND FOR THE FIRST TIME IT IS IN THE CELL THE PREVIOUS ROUND JUST FILLED.** Rule (a) decides on `Token.literal` — round 5's own bit — and inherits round 5's whole class list by construction; round 5's literalness table names EXPANSION, PATHNAME, TILDE and BRACE as the four classes that clear it. **The round-10 corpus draws only the one class the rule catches.** Verified by reading: every one of `ENVELOPE_ROOT_OPERAND_CARRIERS`' eleven entries (`tests/envelope_wrapper_class.rs:8057`) is an ABSOLUTE LITERAL path, `CONTROL_CARRIER_EXPANSION_BORNE` (`:8094`) is exactly two `$(…)` entries, and **not one entry of any of the seven classes carries a `~`, a `*`, a `?` or a `{a,b}` in a carrier path.** So the axis is structurally incapable of failing on the class. Audit 10 measured every spelling live: `rm -rf ~/.local/share/gsd-meta-manager/envelope/alpha` is **exit 0** while its absolute-literal twin is **exit 2**; `rm -rf <env>/alph?` takes nine carriers in one call at exit 0; `rm -f <env>/alpha/*`, `rm -f <env>/alpha/pr-ledger.ndjso?` and `rm -f <env>/alpha/{pr-ledger.ndjson,x}` are all exit 0; and `bash` confirmed each really reaches the file. **The residue's own arithmetic is wrong in the docs**: they say rule (a) *\"fails OPEN in FOUR named directions\"* and every cited spelling is `$`-shaped. **There are SEVEN**, and the three unnamed ones need no prior read, no symlink and no cwd."
    - "**`T-19-117` IS THE ONE ANSWER TO *WHAT CHANGES WHEN THE GUARD RUNS*, AND ITS BEHAVIOURAL HALF IS UNMEASURED AND MUST BE CLAIMED IN NEITHER DIRECTION.** `ledger::record_and_check_in` reads the ledger WHOLE (`ledger.rs:225-227`) and `tally` (`:338-377`) walks every line, with no size bound anywhere, on the guard's registered critical path against `GUARD_TIMEOUT_SECS = 5` (`hooks.rs:1408`, delivered at `:1504`). Audit 10 measured, entries dated outside the 24 h window so the cap itself is unaffected: empty **5 ms**, 10,000 lines (0.9 MB) **49 ms**, 200,000 lines (19 MB) **0.95 s**, **2,000,000 lines (190 MB) 7.69 s — past the registered 5 s timeout** — while against the same root `ls` is 6 ms, `git status` 5 ms and `git push --force origin main` 4 ms, because the cost falls **only on the forge commands the cap governs**. The inflation is delivered by direction (i). **This is the same file whose `ends_mid_line` (`:288-303`) already refuses a whole-file read for a one-byte question, in its own words *\"a whole-file read to answer a one-byte question is the sort of thing that turns a guard into a hang\"* — and the read three functions down is unbounded.** **What the agent CLI does with a `PreToolUse` hook past its registered timeout is a property of a closed-source binary, is UNMEASURED, and is claimed in NEITHER direction here — the same discipline `C-08`'s behavioural half is held to.**"
    - "**EVERY POST-FIX EXPECTATION CARRIES A WRITTEN DERIVATION, AND EVERY ROW IS CHECKED FOR A DISCRIMINATING CONTROL BEFORE IT IS WRITTEN.** Two rounds running an executor caught a plan mandating a test that certifies nothing: `19-26` refused a false converse degenerate-proof its own plan mandated and wrote the true one instead, and `19-27` measured that its plan's own certifying row (`git config --file <ENV>/alpha/gitconfig alias.x '…'`) was refused at the SAME identifier as its outside control, so the row could not tell the two mechanisms apart and would have stayed green under the mis-placed rule it was written to catch. **So: for every row asserted at a post-fix verdict, name the CONTROL and state what makes the pair discriminating.** A row whose control fails the same way at the same identifier is RECORDED with its overlap stated, never asserted. A verdict that cannot be derived from `19-29`'s mandated design is measured pre-fix and RECORDED."
    - "**THE AXIS GROWS FROM SEVEN CLASSES TO ELEVEN, AND FOUR OF THE CHANGES ARE FORCED BY MEASUREMENT RATHER THAN CHOSEN FOR SYMMETRY.** (1) **`CONTROL_CARRIER_REDIRECTION_TARGETS` MOVES ARM.** All four of its entries carry ABSOLUTE LITERAL targets and are REFUSED after `19-29`, so leaving them in `CONTROL_CARRIER_INVARIANCE_ALPHABETS` would put entries whose verdict the fix CHANGES into the invariance-preserving arm — `19-18`'s `{v}>` blocker, `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split, `19-22`'s indirection/confined split and `19-24`'s re-parsed/confined split, a fifth time. They get their own fail-closed property, and a NEW verdict-preserving redirection alphabet is created for the targets that stay permitted. (2) **THREE NEW SPELLING CLASSES** — tilde-borne, glob-borne and brace-list-borne — each verdict-PRESERVING, each drawing in BOTH word positions (operand and redirection target), which is the cell nobody has drawn. (3) **A NEW FAIL-CLOSED CLASS FOR THE BINARY**, because `C-10` is outside the envelope root, so class 1 does not draw it and class 11 (the complement) WOULD — which would assert a row PERMITTED that `19-29` refuses, landing permanently red in a file `19-29` may not edit. (4) **CLASS 1 AND CLASS 5 ARE REFINED**, because a glob or brace spelling under the root satisfies class 1 today and a tilde spelling satisfies class 5 today, and either collision fires the disjointness fence. **Every `MIN_*`, `*_CASES`, `*_SLOTS` and per-class count is RE-DERIVED from the new generation arithmetic and STATED as an exact equality** — audit 5 found `19-16` set a floor of 50 against a maximum of 40 by construction, so state the arithmetic and check it."
    - "**THE CORPUS MUST BE ABLE TO FAIL IN BOTH DIRECTIONS, AND THIS ROUND'S PERMITTED HALF IS WHAT STOPS THE BINARY CLAUSE BECOMING A DIRECTORY PREFIX.** The envelope directory is a PREFIX boundary because this envelope owns it entirely; **the binary is an EXACT-PATH boundary because its directory is shared with everything else the user installed**, and a prefix over `~/.cargo/bin` would refuse `ls ~/.cargo/bin` and every `cargo install`. So the near-miss controls for `C-10` are `cp /bin/true <binary-parent>/some-other-file` and `ls <binary-parent>`, both measured exit 0 and both asserted **exit 0 AFTER** — this round's `--signed no`, from the other side. Beside them the whole of `19-26`'s permitted half is carried forward unchanged: `rm -f /tmp/x`, `rm -rf /tmp/scratch`, `cp /bin/true /tmp/t`, `truncate -s 0 /tmp/f`, `cat /tmp/x`, `ls`, `cargo test`, `rg 'pr-ledger.ndjson' src/`, `rm -f /tmp/pr-ledger.ndjson`, the one-character-changed root, `git config --get core.hooksPath` pinned UNCHANGED, and rounds 6, 8 and 9's whole permitted halves."
    - "**NOTHING ROUNDS 4 THROUGH 10 SETTLED IS RE-OPENED, AND THE MECHANISM PINS SAY SO MECHANICALLY RATHER THAN IN PROSE.** All are re-measured at this plan's base and carried into the new file: **round 5's literalness bit NON-VACUOUS** (`git pus? --force origin main` → 2 `envelope_assertion_failed`); **round 6's deletion model NON-DEAD** (`git >/dev/null push --force origin main` → 2 `force_push_blocked`, `git x2>/tmp/o push --force origin main` → **0**, `git 2>/dev/null push --force origin main` → 2, `git >` failing closed); **`SEPARATORS` byte-identical at `policy.rs:2297` with ONE commit in the whole phase (`84a9b05`) and `policy::is_separator(\">\") == false` by construction**; **round 7's fail-closed callee grammar** (`git --attr-source HEAD push …` → 2, `git --bogus-opt status` → 2, `git - push --force origin main` → **0**, `git -- push --force origin main` → 2); **round 8's confinement clause** (`git -c include.path=/tmp/evil.cfg status` → 2 beside `includepath`/`notinclude.path` at 0); **round 9's re-parse clause** (`git -c alias.q=\"-c include.path=… status\" q` → 2 beside `aliasx.`/`notalias.` at 0, and both `T-19-86` `!`-bodied rows at **0**); and **round 10's own clause** over all nineteen envelope-root operand rows. **`Token.literal` is READ in a second place by round 10 and is READ in a third by round 11, so a change that cleared or repurposed it would silently widen both rules as well as turning rounds 5 and 6's pins vacuous.**"
    - "**THE BYTE FLOORS AND THE ANTI-VACUITY STRIPPER'S THREE PROTECTIONS ARE LOAD-BEARING, THE FLOOR COMMENT IS NOW TRUE, AND ONE EDITORIAL NUMBER IN IT IS WRONG.** `POLICY_MIN_PRODUCTION_BYTES = 40_000` and `HOOKS_MIN_PRODUCTION_BYTES = 20_000` stay at their values or rise. Audit 10 re-derived every number in the corrected comment independently and found each exactly right: `policy.rs` production half **277,570 bytes / 276,660 characters / 5,414 lines**, `hooks.rs` **78,465 bytes / 1,636 lines**, exactly ONE `#[cfg(test)]` sentinel per file by trimmed equality (`policy.rs` line 5,415; `hooks.rs` line 1,637), deep anchor `fn forbidden_repo_path` at line **5,157**, `fn scan_leading` at **416**, 40,000 bytes at **752**, 180,000 at **3,613**, and the proportional floor passing at **64.85%**. **Do not re-break it.** The ONE defect is editorial and audit 10 named it: the comment says the file carries *\"842 multi-byte characters\"*. **842 is the byte-minus-character DIFFERENCE; the count is 421**, each three-byte character contributing two extra bytes. It appears at `src/envelope/policy.rs:7450` and at `deferred-items.md:2172`. **`policy.rs`'s is `19-29`'s to edit; `deferred-items.md`'s sits inside the plan-19-27 section and is corrected BESIDE, in this plan's own section, never by editing it.**"
    - "**`T-19-17r` STAYS OUTSTANDING AND THIS PLAN DOES NOT RESOLVE IT EITHER.** The Accepted Risks Log runs `AR-19-01` … `AR-19-12`; `grep -cE '^\\| AR-19-13 \\|'` over `19-SECURITY.md` is **0**. Audits 5 through 10 and plans 19-24, 19-25, 19-26 and 19-27 all confirmed the measurement and both pins and all ten explicitly declined. **This plan is the eleventh to decline, because accepting a risk is a human decision.** And **`AR-19-04` and `AR-19-05` are NOT un-accepted, re-rated or renumbered** — `AR-19-04`'s reasoning gap is RECORDED beside it, now with `T-19-118`'s measured consequence beside that, and nothing more."
    - "**`/gsd-secure-phase 19` IS NOT CLEARED BY THIS PLAN, BY `19-29`, OR BY THE TWO TOGETHER.** `T-19-86` remains OPEN at `high` by explicit user scoping decision, with `T-19-111` moved OUT of it and not into it. `T-19-91` remains OPEN at `high` with its arms unweakened. `T-19-111` remains OPEN at `high` with NO rule and its corpus rows RECORDED, not asserted. `T-19-112` is NARROWED further and not closed. `T-19-96` and `T-19-110` are registered open at `medium`; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. **Whether `T-19-116` and `T-19-118` close is audit 11's judgement and not this plan's claim.** The SUMMARY must say all of that FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-29` is the rules and the honesty repairs. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`, `19-16`, `19-18`, `19-20`, `19-22`, `19-24` and `19-26` met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured or the one DERIVED IN WRITING from `19-29`'s mandated design. If any audit-10 row does not reproduce, report it as a finding with the measured verdict and STOP — do not assert the audit's number over your own measurement and do not quietly drop the row. In particular MUST NOT inherit audit 10's END-TO-END claims by citation: the bare-remote fixture is REBUILT and every `C-10` leg re-measured independently with the remote's SHA recorded BEFORE and AFTER and a CONTROL beside each."
      category: honesty
    - statement: "MUST NOT assert a row whose CONTROL is refused at the SAME identifier for the SAME reason. `19-27` measured that its plan's own certifying row `git config --file <ENV>/alpha/gitconfig alias.x '<body>'` and its outside-the-envelope control are BOTH exit 2 `envelope_assertion_failed` — by round 9's re-parse clause over `alias.x` — so the row could not tell the two mechanisms apart and certified nothing. **Name the control for every asserted row and state what makes the pair discriminating.** A row whose control fails the same way is `record_only`'d with its overlap stated."
      category: honesty
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised command wrote nothing must use a FRESH `GSD_MM_ENVELOPE_ROOT` of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line — `gh pr create --title x` is the ready-made one. **The ledger-SIZE rows are the place a fresh root per row would destroy the measurement**: each inflation level is built once in ONE persistent root and every latency row for that level is driven against it, with the line count and byte size recorded before and after."
      category: honesty
    - statement: "MUST NOT write a row whose expected mechanism is that `>` or `<` was added to `SEPARATORS`, and MUST NOT propose it. `policy.rs:2297` has ONE commit in the whole phase (`84a9b05`, plan 19-05), `is_separator` is a bare `SEPARATORS.contains` so `is_separator(\">\") == false` by construction, and round 6's over-deletion control pins `git x2>/tmp/o push --force origin main` PERMITTED. **`19-29`'s redirection-target channel is a SECOND RETURN VALUE of the same walk, never a token in the stream and never a separator**, and the corpus states that mechanically through the segment-count pins rather than in prose."
      category: safety
    - statement: "MUST NOT write a row whose expected mechanism is that a redirection-target token was pushed INTO `tokenize`'s token stream. `split_segments_with_heads` (`policy.rs:2466-2514`) flushes `current` into a `Segment` the instant an operator token arrives, so such a token would SPLIT `git >/dev/null push --force origin main` into `[git]` and `[push, --force, origin, main]` — and the first resolves `Governed` with an empty argv, which `classify_git` answers `Allow` for. **The segment-count pins assert exactly this in both directions and they are GREEN today**; a red one after `19-29` is a finding about the FIX, reported with the observed segment list, never an assertion to edit."
      category: safety
    - statement: "MUST NOT put an entry whose verdict the fix CHANGES into the invariance-preserving arm of any generative property. This is `19-18`'s `{v}>` blocker, `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split, `19-22`'s indirection/confined split, `19-24`'s re-parsed/confined split and `19-26`'s own envelope-root split, a sixth time. **The four ABSOLUTE LITERAL redirection targets and every BINARY-operand entry are REFUSED after `19-29` and belong in a fail-closed arm; the tilde, glob, brace, expansion-borne, symlinked, relative and ordinary alphabets are verdict-PRESERVING and belong in the invariance arm.** The doc says why rather than leaving it to be inferred."
      category: safety
    - statement: "MUST NOT write a row whose expected mechanism is a list of program names — `rm`, `cp`, `truncate`, `printf`, `sed`, `tee`, `yes`, `head`, `shred`, `python3` — or a 'dangerous command' denylist of any kind. `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` (`tests/envelope_wrapper_class.rs`) is the mechanical control and it must stay green, and D-08's whole argument is that `resolve_program` never asks what the wrapper is CALLED. **The NO-PROGRAM-NAMES fence is extended rather than re-spelled**: at least one BINARY-carrier row and at least one row from each of the three new spelling classes is drawn under a program name absent from both production halves."
      category: safety
    - statement: "MUST NOT stand the new spelling classes up as a SIXTH axis or fold them onto `CONFIG_RESOLUTION_CLASSES`, `UNREADABLE_CLASSES`, `DELETION_CLASSES` or `CALLEE_GRAMMAR_CLASSES`. They are the CONTROL-CARRIER axis's own classes and belong on it: rule (a) decides on `Token.literal`, so the spellings that clear that bit are the same axis's own alphabet. All four earlier axes, all of their predicates, all of their degenerate-proofing and all of their floors stay BYTE-IDENTICAL, and `MIN_CONFIG_RESOLUTION_CLASSES` stays at **6**."
      category: safety
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet ENTRY, property or assertion anywhere under `tests/`. Entries may be ADDED and an alphabet may MOVE ARM with its reason stated; nothing may be removed, reworded away or narrowed. `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` keep their values or rise, and the anti-vacuity stripper's three protections — the proportional floor `>= 180_000`, the deep anchor `fn forbidden_repo_path` and the exactly-ONE `#[cfg(test)]` sentinel count per file — keep their present strictness. A case that stops being drawn is a case that stops being able to fail."
      category: scope
    - statement: "MUST NOT modify `tests/envelope_reparsed_value.rs`, `tests/envelope_config_resolution.rs`, `tests/envelope_command_position.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_callee_grammar.rs`, `tests/envelope_expansion_slots.rs`, `tests/envelope_pr_cap.rs`, `tests/envelope_credential.rs`, `tests/envelope_wiring.rs`, `tests/envelope_tracer.rs`, `tests/envelope_advisory.rs`, `tests/envelope_wrapper_bypass.rs`, `tests/envelope_hook_refusals.rs` or `tests/envelope_control_carrier.rs`. Rounds 3 through 10's evidence stays attributable to the round that produced it, which is why `19-14` created a third file, `19-16` a fourth, `19-18` a fifth, `19-20` a sixth, `19-22` a seventh, `19-24` an eighth and `19-26` a ninth; **this plan creates a TENTH for the same reason.** The carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place."
      category: scope
    - statement: "MUST NOT write the rule, sketch it in `src/`, or add any production line. In particular MUST NOT touch `policy.rs`'s `tokenize`, `skip_redirection_target`, `redirection_operator_len`, `split_segments_with_heads`, `Token`, `Segment`, `envelope_carrier_operand`, `word_is_within`, `lexical_absolute_components` or `envelope_carrier_refusal`; MUST NOT touch `hooks.rs`'s `guard`, `guard_in`, `classify_segments` or the call site at `:1018-1029`; MUST NOT touch `ledger.rs`'s `record_and_check_in` or `tally`; MUST NOT edit `SECTION_ENVELOPE`, `cred.rs`, `mod.rs:165-168` or `policy.rs:7450`'s floor comment. **All of those are `19-29`'s. This plan RECORDS every one of them as a finding and performs none.**"
      category: scope
    - statement: "MUST NOT write a rule, a pin, or a doc claim for `.git/config`, `.claude/settings.json`, `.git/info/exclude`, the run journal or `~/.config/gsd-meta-manager/config.json`. `C-11` … `C-15` keep control **(e)** and `19-29` writes no rule for any of them, so a row asserted REFUSED lands permanently red in a file `19-29` may not edit — **and asserting only that they are PERMITTED is equally forbidden, because that would pin a live bypass as correct.** Every repo-side row is MEASURED and RECORDED in a comment and a print, never written as an assertion. `19-22` asserted such a row against its own comment, its SUMMARY and its plan-check, and it halted `19-23` mid-plan."
      category: honesty
    - statement: "MUST NOT clamp `pr_cap_per_24h` or `pr_cap_per_run`, propose clamping them, or write a row whose mechanism is a clamp. Clamping a configured cap is a PRODUCT decision about what a user may configure, not a guard rule, and making it inside a security round would decide it silently. `C-15` is carried forward at its measured severity with control (e) and NO rule, re-recorded as a DISTINCT ROUTE to SAFE-06 rather than a spelling of `T-19-112`, with audit 10's mechanical detail beside it: `envelope guard` ignores the global `--config` flag and always reads `Config::default_path()` (`hooks.rs:837-840`), so `C-15` is steered by `HOME`/`XDG_CONFIG_HOME` and not by argv."
      category: scope
    - statement: "MUST NOT fix, narrow, re-scope or re-classify `T-19-86`, and MUST NOT fold `T-19-111` into it. Its four registered rows and its persisted-alias arm stay at exit 0 and its pins stay green UNMODIFIED. MUST NOT close, re-scope or add a remedy for `T-19-91`, whose three arms stay as audits 5 through 10 verified them. MUST NOT write a rule for `T-19-111`, whose corpus rows stay RECORDED and asserted in neither direction, and whose five-site attribution correction `19-27` performed stays as performed. MUST NOT fix `T-19-96` or `T-19-110`, and MUST NOT re-open `T-19-74`, whose core rows are frozen."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not add park-coverage controls and do not open `scan.rs` or `config.rs`. MUST NOT repair `C-08`'s claimed-but-unwired second carrier: the mechanical half is settled (`grep -rn 'settings_json' src/` is exactly two hits while production pushes `--settings <path>`) and repairing a delivery is a change to the SPAWN SEAM. **`C-08`'s behavioural half stays UNMEASURED and is claimed in NEITHER direction.**"
      category: scope
    - statement: "MUST NOT remove `--hostname` from `FORGE_VALUE_OPTS`, propose removing it, or write a row whose mechanism is its removal — audit 9 re-measured and overturned audit 8's own suggestion, because removal moves a counted creation form to UNCOUNTED (`T-19-35`). **`glab` is confirmed NOT INSTALLED**, so any `glab` latency row is RECORDED as unmeasurable-against-its-callee rather than driven or inferred, and a pin that would skip is a fail-open pin and is not written."
      category: scope
    - statement: "MUST NOT un-accept, re-open or re-rate `AR-19-04` or `AR-19-05`, and MUST NOT add, edit or renumber any Accepted-Risks-Log row. **RECORD** that `AR-19-04`'s stated mitigation does not cover a write DURING the run and that `T-19-118` is its measured consequence at a higher severity than the acceptance covers — and record that `T-19-118` is registered as its OWN row rather than moved into `T-19-23` or into `AR-19-04`. Un-accepting a risk is a human decision and no plan makes it."
      category: honesty
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, MUST NOT create `AR-19-13`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Ten agents have deliberately left that acceptance unmade because it is a human decision; this plan is the eleventh to decline."
      category: honesty
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` between `cc65220` and this plan's base — and every parser in this module is hand-rolled against `std` for exactly that reason. The real-git probes, the bare-remote fixture, the credential-fill drive, the ledger inflation and every latency measurement shell out through `std::process::Command` inside `#[cfg(test)]`."
      category: scope
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against **1771**, the number `19-27-SUMMARY.md` records and audit 10 independently re-measured over 45 result lines. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing — `19-19` first read 1364 instead of 1605 that way (D-34). **A run reporting fewer than SEVENTEEN `envelope_*` binaries after this plan is a run in which this plan's own evidence file did not execute.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes — the two `tests/driver_reattach.rs` failures and the `tests/envelope_tracer.rs` ETXTBSY stub-write race — and MUST NOT treat their absence from a run as a regression. One fired during round 10's execution and was recorded rather than smoothed; none fired in audit 10's run. Absence is not evidence they are fixed and one firing is not evidence of a new defect. **The ETXTBSY flake is `C-10`'s own seam and this plan will be exercising it — a firing is RECORDED with its output and is not this plan's to fix.**"
      category: scope
    - statement: "MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lint errors in `src/browser.rs` and `src/project_creator.rs`. The gate is `cargo clippy -- -D warnings`. Do not fix those lints."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, the plan-19-21 blocker-row resolution record, or any subsection plans 19-13 … 19-27 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-28 planning and execution record. Audit 10 checksummed the body below the frontmatter before writing; that provenance is the file's value and a plan writing into an audit's own tables forges it. **The `deferred-items.md:2172` multi-byte-count error is corrected BESIDE, in this plan's own section, and never by editing it.**"
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere, and MUST NOT claim that `T-19-116`, `T-19-118`, `T-19-115` or `T-19-117` is CLOSED. This plan closes nothing at all — it measures, widens the corpus and goes RED. Whether `19-29`'s rules close anything is audit 11's judgement. `T-19-86`, `T-19-91`, `T-19-111` and `T-19-112` all remain OPEN at `high`, so this plan does not clear `/gsd-secure-phase 19` and neither does `19-29`."
      category: honesty
  artifacts:
    - path: "tests/envelope_carrier_reach.rs"
      provides: "the TENTH evidence file — `T-19-116`'s `C-10` rows driven END TO END against real git in a REBUILT bare-remote fixture with a control beside every leg and the guard's own exit-0 answer measured separately; `T-19-118`'s credential-fill drive with the generated-file control and the six write spellings; the planner-derived EMPTY-`credential.helper` candidate control measured and RECORDED in both its effect and its cost; `T-19-115`'s seven spelling rows in BOTH operand and redirection-target position, each with its absolute-literal twin and with `bash` confirming the shell reaches the file; `T-19-117`'s latency curve against `GUARD_TIMEOUT_SECS` with the forge/non-forge split and the derived post-fix bound row beside its just-under-the-bound discriminating control; the two ORDERING pins at deliberately different identifiers; the EXACT-PATH-not-PREFIX near-miss controls for the binary; the SEGMENT-COUNT pins over `split_segments_with_heads`; the repo-side and `C-08` rows RECORDED and never asserted; and every carried-forward mechanism pin from rounds 4 through 10"
      contains: "T-19-116"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "the CONTROL-CARRIER axis widened from seven classes to ELEVEN with `MIN_CONTROL_CARRIER_CLASSES = 11` — class 1 and class 5 refined so a glob, brace or tilde spelling no longer collides with them; `CONTROL_CARRIER_REDIRECTION_TARGETS` MOVED into the fail-closed arm with its reason stated; a new verdict-preserving redirection alphabet for the targets that stay permitted; three new verdict-preserving spelling alphabets (tilde, glob, brace) drawing in BOTH word positions; a new fail-closed `CONTROL_CARRIER_BINARY` alphabet with its EXACT-PATH-not-PREFIX near-miss controls; the NO-PROGRAM-NAMES fence extended to the new classes; a NO-EXPANSION-SPELLING fence asserting the axis can now DRAW a `~`, a `*`, a `?` and a `{a,b}`; disjointness re-proved over all eleven; and every floor, case count, slot count and per-class count RE-DERIVED and stated as an exact equality"
      contains: "MIN_CONTROL_CARRIER_CLASSES"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-28 record rather than an audit finding, carrying the FIFTEEN-carrier disposition table with a control letter for every one including `C-10`'s; `T-19-116`'s end-to-end drive with both remote SHAs and every control; `T-19-118`'s credential-fill drive with its control and the `AR-19-04` reasoning gap recorded and NOT un-accepted; the answer to the direction-(i) question stated in full, with the segment-count measurement that decides it; `T-19-115`'s seven spellings with the residue's corrected arithmetic; `T-19-117`'s latency curve with the behavioural half marked UNMEASURED; the axis arithmetic re-derived; the corrected `421` multi-byte count with `deferred-items.md:2172` corrected BESIDE; the mechanism pins and byte floors re-measured; `T-19-17r` OUTSTANDING for the eleventh time; and the statement that this plan closes nothing and clears no gate"
      contains: "19-28"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-116` registered at `high` with `C-10`'s deferred-items row and control letter — the entry whose absence is how it survived a round that enumerated it; `T-19-118` registered at `high` with `T-19-23`'s reopening stated and no acceptance made; `T-19-115` and `T-19-117` registered at `medium` with their corpus recorded RED or RECORDED; the seven fail-open directions replacing the four; the `AR-19-04` reasoning gap recorded; the `deferred-items.md:2172` multi-byte-count correction stated BESIDE; and `T-19-86`, `T-19-91`, `T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-74`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open"
      contains: "T-19-116"
  key_links:
    - from: "audit 10's measured `C-10` and `C-05` bypasses and the seven spelling classes"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary and against real git with a fresh root per row — and ONE persistent root per ledger inflation level — before any assertion is written and before any production line moves"
      pattern: "RED"
    - from: "a redirection-target token pushed into `tokenize`'s stream"
      to: "a red segment-count pin rather than a silently permitted `git >/dev/null push --force origin main`"
      via: "assertions over `policy::split_segments_with_heads` that the redirected simple command produces exactly ONE `Segment` with exactly the surviving argv, green today and green after"
      pattern: "segment_count"
    - from: "a binary clause written as a DIRECTORY prefix"
      to: "a red near-miss control rather than a guard that refuses `ls ~/.cargo/bin`"
      via: "`cp /bin/true <binary-parent>/other` and `ls <binary-parent>` measured exit 0 and asserted exit 0 AFTER — this round's `--signed no`, from the other side"
      pattern: "exact_path"
    - from: "a rule whose residue is disclosed as FOUR directions when there are SEVEN"
      to: "an axis that can DRAW a tilde, a glob and a brace list in both word positions"
      via: "three new verdict-preserving spelling classes with their own alphabets and floors, and a fence asserting each metacharacter really appears in a carrier path"
      pattern: "T-19-115"
    - from: "an enumerated carrier marked UNOBSERVED with no threat id, severity, deferred-items row or control letter"
      to: "fifteen carriers and fifteen letters, with the widest of them registered and driven"
      via: "the disposition table in the appended record and the `T-19-116` entry in `deferred-items.md`, both written whether or not any rule lands"
      pattern: "C-10"
---

<objective>
**The carriers rule (a) cannot see: the binary outside the root, the target after a `>`, and the three spellings that clear `Token.literal` without a `$`.**

Audit 10 (`19-SECURITY.md`, the authority) answered the question it was set, and answered it plainly:

> **There is NO further layer. The modelled surface is complete** … Round 10 found the right plane and it
> is the last one: every control this envelope installs is reached either by an argv (nine rounds, all
> closed for their measured rows) or by a FILE (round 10).

> **So the answer to the tenth question is: stop looking for an eleventh plane and finish dispositioning
> the tenth.** `19-26` enumerated fifteen carriers. Nine got rule (a). Five got control (e). **One got
> nothing, and it is the one that takes two layers in a single call.**

> **The one-sentence version for the next round.** Ten rounds have modelled how a command line becomes
> behaviour and how a file becomes a control, and there is no eleventh plane to find — **what is left is
> that the tenth plane has fifteen carriers and only nine of them have a control.**

This plan adopts that finding whole. It looks for no eleventh plane. It measures the four cells audit 10
left on the tenth, widens the corpus so the axis can fail on each, observes it RED, and stops. `19-29`
writes the rules and the honesty repairs and is gated on this plan's recorded RED state.

**If the widened corpus does not go red against the pre-fix tree, that is a finding — stop and report it.**

## The base, stated so the handoff number is checkable

This plan's base is `ca30c90`. `git diff --numstat 0092009..ca30c90 -- src/ tests/` returns **nothing**,
so the built binary here is the binary audit 10 measured against, and the only change between them is
audit 10's own append to `19-SECURITY.md`. Re-verify that before the first measurement rather than
trusting this paragraph.

## The four cells, and what each is

| Row | Severity | Carrier | Why rule (a) does not reach it |
|---|---|---|---|
| **`T-19-116`** | **high, BLOCKING** | `C-10` — the binary every stub and the guard itself exec (`hooks.rs:196-204`, `:1518-1524`) | **it is not under the envelope root**, and `--disallowedTools` carries git-verb prefixes that `cp /bin/true …` matches none of |
| **`T-19-118`** | **high, BLOCKING** | `C-05` — the generated `gitconfig` on BOTH `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` (`cred.rs:519-526`) | it is inside the root, but the write arrives as a **REDIRECTION TARGET** — fail-open direction (i), which rule (a) cannot see |
| `T-19-115` | medium | `C-01` … `C-09`, all nine | the word is ABSOLUTE and under the root but is **not `Token.literal`** — a tilde, a glob or a brace list, three spellings the residue does not name |
| `T-19-117` | medium | `C-01`, as **work** rather than as a record | it is not a path question at all: the ledger's SIZE is unbounded on the guard's critical path against `GUARD_TIMEOUT_SECS = 5` |

## `C-10` — an enumerated carrier with no disposition, which is how it survived

`19-26` wrote `C-10` down and marked it UNOBSERVED. `19-27`'s disposition table gives `C-01` … `C-09`
control (a) and `C-11` … `C-15` control (e); **there is no row for `C-10` at all — no threat id, no
severity, no deferred-items entry and no letter.** Audit 10 registered it as `T-19-116` at `high` and
drove it:

```
CONTROL   git push --force origin HEAD:refs/heads/main   -> REFUSED by the hook, remote main 01f9664 UNMOVED
LEG B     cp /bin/true <binary>                          -> exit 0 through the guard, no envelope path named
          git push --force origin HEAD:refs/heads/main   -> COMPLETED, remote main 01f9664 -> 2537e97 MOVED
CONTROL   <binary> restored, same push                   -> REFUSED, remote UNMOVED
LAYER 2   <replaced binary> envelope guard alpha  on `git push --force origin main`  -> exit 0 (PERMIT)
          <real binary>     envelope guard alpha  on the same                        -> exit 2
```

**Layer 2 and layer 3 in one permitted call.** Strictly wider than `C-02` (`T-19-113`, `medium`), which
removes layer 3 only, and wider than `env -u GIT_CONFIG_COUNT`, which removes layer 3 only and is
REFUSED. `T-19-01`'s mitigation is correct and irrelevant here: `assert_provenance_in` (`hooks.rs:242`)
certifies a RELOCATED COPY, and **a replaced exec target never re-enters the binary that would assert
provenance.** The registration is the honest first step and it costs nothing.

## The design question, answered — and the answer is BOTH, plus a third thing that is neither

Rule (a) reads a path and refuses if it resolves under the envelope root. `C-10` is outside the root;
`C-05` is inside but reached through a direction the rule cannot see. **Widen the path SET, widen what
the rule can SEE, and fix the ledger's size where neither applies.**

| Cell | Widen the PATH SET | Widen what the rule SEES | Neither |
|---|---|---|---|
| `T-19-116` (`C-10`) | **YES** — `{envelope_dir_in(root, alias)} ∪ {the binary this guard is running as}`, the second as an **EXACT PATH** and not a prefix | — | — |
| `T-19-118` (`C-05` via (i)) | — | **YES** — pathname REDIRECTION TARGETS, on a channel `Segment::tokens` never carries | — |
| `T-19-115` (tilde/glob/brace) | — | **NO** — a rule would have to perform expansions the guard is forbidden to perform | the residue's **arithmetic** is corrected: state the CONDITION, do not enumerate spellings |
| `T-19-117` (ledger size) | — | — | a size bound in `ledger.rs` that fails **CLOSED** |

**The two boundary KINDS differ and the reason is measured, not aesthetic.** The envelope directory is a
PREFIX boundary because this envelope owns every byte under it. The binary is an EXACT-PATH boundary
because its directory is shared with everything else the user installed: a prefix over `~/.cargo/bin`
would refuse `ls ~/.cargo/bin` and every `cargo install`. So the corpus carries `cp /bin/true
<binary-parent>/some-other-file` and `ls <binary-parent>` as PERMITTED near-miss controls, and a rule
written as a directory prefix turns both red.

## Does closing direction (i) mean the DELETION MODEL must read redirection TARGETS? No — and this is the round's central mechanical claim

`19-26`'s plan-check and audit 10 both called direction (i) *"real by MECHANISM, not by choice"*, and
about the tokenizer that is exactly right. It is also, now, demonstrably harmful: a fail-open direction
carrying a live `high`-severity credential exploit is not an acceptable residue.

**Round 6's deletion model answers *which words ARRIVE at the program*. A redirection target does not
arrive, and that answer does not change.** Rule (a) asks a different question about the same walk: *does
this line NAME a path this run's controls live in?* Those are two questions about one token stream.

Three mechanical facts, read out of the source rather than argued:

1. **`skip_redirection_target` (`policy.rs:3007-3057`) already walks the target's full extent, applying
   the target's own quoting rules, in order to skip it.** Its doc says *"Its text is never needed, only
   its extent"* — **that is the sentence that becomes false**, and it becomes false in the WR-02 shape
   this codebase has a name for: a stated reason that went stale and must be corrected rather than
   quietly outgrown.
2. **`redirection_operator_len` (`policy.rs:2968-2992`) is a FINITE, CLOSED grammar of twelve
   operators**, and it already distinguishes them by name. Only some have a PATHNAME target: `>`, `>>`,
   `>|`, `<`, `<>`, `&>` and `&>>`. **`<<` and `<<-` take a heredoc DELIMITER, `<<<` a here-STRING, and
   `>&`/`<&` an fd number** — none of them a path, and recording any of them as a carrier candidate
   would be wrong. The split is derived at the operator, in the same match that already computes the
   operator's length.
3. **`split_segments_with_heads` (`policy.rs:2466-2514`) EXCLUDES every operator token from
   `segment.tokens` and flushes `current` into a `Segment` the instant one arrives.** So a
   redirection-target token pushed into the stream would split
   `git >/dev/null push --force origin main` into `[git]` and `[push, --force, origin, main]`, and the
   first resolves `Governed` with an empty argv, which `classify_git` answers `Allow` for. **A rule
   written the obvious way silently converts round 6's headline refusal into a permit.** The precedent
   the fix must follow instead is already in the file: `Segment::redirection_unresolvable`
   (`:2402-2418`, accumulated at `:2461-2505`) is a fact carried on the SEGMENT, gathered in the
   operator arm of the ONE walk, applied retroactively over `segments[command_start..]` and reset at
   each real command operator — **with `segment.tokens` untouched.**

**So the cost, stated plainly:** one new field on `Token` and one on `Segment`, both additive and both
compiler-enforced across the six `Token { … }` construction sites and two `Segment { … }` sites in
`policy.rs`; a return-type change on `tokenize`, private with four call sites; a corrected doc sentence
on `skip_redirection_target`; and a pathname/non-pathname split of a grammar the file already
enumerates. **`SEPARATORS` does not move, `is_separator(">")` stays `false`, no token enters the stream,
and round 6's over-deletion control stays PERMITTED.** Round 3's principle is discharged rather than
weakened: the targets are found BY the walk that already finds them, and rule (a) is still raised at ONE
site reading ONE segment. **If the fix needs a second scan or a second reading site, that is a finding
to report, not a place to add one.**

**This plan's job is to make that claim FALSIFIABLE before `19-29` relies on it**, which is what the
segment-count pins are.

## What must be RED at this plan's end

Measured at `ca30c90` — fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards:

```
exit=0  cp /bin/true <BINARY>                                          <- T-19-116, layers 2 AND 3
exit=0  printf '[credential]\n\thelper = store\n' >> <ENV>/alpha/gitconfig   <- T-19-118, via direction (i)
exit=0  : > <ENV>/alpha/pr-ledger.ndjson                               <- direction (i), T-19-112's residue
exit=0  printf 'exit 0' > <ENV>/alpha/hooks/pre-push                   <- direction (i), T-19-113's residue
exit=0  echo evil > <ENV>/alpha/askpass                                <- direction (i)
exit=0  cat /tmp/evil >> <ENV>/alpha/gitconfig                         <- direction (i)
exit=0  gh pr create --title x   (against a 190 MB ledger)             <- T-19-117, 7.69 s past a 5 s deadline
```

All are asserted at their DERIVED post-fix verdict — exit 2 `envelope_assertion_failed` — so they are red
now and green after. **Each carries its control and the statement of what makes the pair discriminating.**

## The rows that must NOT move

```
exit=0  cp /bin/true <BINARY-PARENT>/some-other-file    <- EXACT-PATH-not-PREFIX control
exit=0  ls <BINARY-PARENT>                              <- the same, from the other side
exit=0  cp /bin/true $(command -v gsd-meta-manager)     <- direction (ii) over the BINARY
exit=0  rm -rf ~/<ENV-REL>/alpha                        <- direction (v), TILDE      (T-19-115)
exit=0  rm -rf <ENV>/alph?                              <- direction (vi), GLOB      (T-19-115)
exit=0  rm -f  <ENV>/alpha/{pr-ledger.ndjson,x}         <- direction (vii), BRACE    (T-19-115)
exit=0  : > ~/<ENV-REL>/alpha/pr-ledger.ndjson          <- (v) in REDIRECTION-TARGET position
exit=0  : > <ENV>/alpha/pr-ledger.ndjso?                <- (vi) in redirection-target position
exit=0  : > <ENV>/alpha/{pr-ledger.ndjson,x}            <- (vii) in redirection-target position
exit=0  D=$(git config --get core.hooksPath); rm -f $D/../pr-ledger.ndjson   <- (ii)
exit=0  rm -f /tmp/l   /  : > /tmp/l                    <- (iii) symlink
exit=0  rm -f pr-ledger.ndjson  /  cp /bin/true pre-push <- (iv) relative
exit=0  rm -f /tmp/pr-ledger.ndjson  /  cat <ENVX>/alpha/pr-ledger.ndjson    <- round 10's near misses
exit=0  git config --get core.hooksPath                 <- the permitted read, UNCHANGED
exit=2  git >/dev/null push --force origin main   AS EXACTLY ONE SEGMENT    <- round 6, and this round's pin
exit=0  git x2>/tmp/o push --force origin main    AS EXACTLY ONE SEGMENT    <- round 6's over-deletion control
exit=2  git 2>/dev/null push --force origin main                            <- the IO_NUMBER row
exit=2  git -c alias.q="-c include.path=/tmp/evil.cfg status" q             <- round 9, NON-DEAD
exit=0  git -c aliasx.q="…" q  /  git -c notalias.q="…" q  /  git -c a=b status
exit=0  git -c alias.p='!git push --force origin main' p                    <- T-19-86, may not move
```

## The rows that are RECORDED and never asserted

Because `19-29` writes **no rule** for them, a row asserted REFUSED lands permanently red in a file
`19-29` may not edit, and a row asserted PERMITTED pins a live bypass as correct.

```
printf … >> .git/config ; git fp                        <- C-11, T-19-111 (no rule, five-site attribution done)
printf … >> .claude/settings.json                       <- C-12
rm -f <project>/.git/info/exclude                       <- C-13
rm -rf <project>/.planning/meta-manager/runs/<run_id>   <- C-14
raise pr_cap_* in ~/.config/gsd-meta-manager/config.json <- C-15, no ledger write, no envelope path
printf … > <ENV>/alpha/settings.json  (does the CLI re-read it mid-run?)  <- C-08, behavioural half
[alias] non-`!` body written into <ENV>/alpha/gitconfig  <- C-05's ALIAS half, T-19-111 at the GLOBAL level
GIT_CONFIG_KEY_n=credential.helper GIT_CONFIG_VALUE_n=   <- the CANDIDATE control for T-19-118: does an
                                                            EMPTY helper value reset the list, and at
                                                            what cost to askpass, gh and a later `-c`?
what the agent CLI does past GUARD_TIMEOUT_SECS          <- T-19-117's behavioural half, UNMEASURED
```

## What this plan is NOT

It closes nothing. `T-19-115`, `T-19-116`, `T-19-117` and `T-19-118` all stay open at this plan's end,
and **whether `19-29` closes any of them is audit 11's judgement rather than either plan's claim.**

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-29`, or by the two together.** `T-19-86`,
`T-19-91`, `T-19-111` and `T-19-112` all remain open at `high`. The SUMMARY must say so first, and must
never write an unqualified "T-19-60 is closed".

Purpose: make the corpus capable of failing on a carrier outside the envelope root, on a path after a
redirection operator, and on the three spellings that clear `Token.literal` without a `$` — and prove
it does.
Output: a tenth evidence file, an eleven-class axis with re-derived floors, two committed RED states, one
appended record carrying the fifteen-carrier disposition table with a letter for every one, and an exact
handoff number for `19-29` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-27-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/hooks.rs
@src/envelope/ledger.rs
@src/envelope/cred.rs
@tests/envelope_wrapper_class.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE all four cells, then RED — the tenth evidence file</name>
  <files>tests/envelope_carrier_reach.rs</files>
  <precondition>
    Work in the main checkout at `ca30c90` or later with a clean tree — **ISOLATION=none is forced for
    this phase, so do NOT create a worktree.** `git diff --numstat 0092009..ca30c90 -- src/ tests/`
    returns nothing, so this base is the tree audit 10 measured; verify that rather than assuming it.
    `cargo build` produces `./target/debug/gsd-meta-manager`, which is what every guard measurement is
    driven against. A real `git` binary is on `PATH` (`git version 2.43.0` on the machine audits 9 and 10
    measured on), `gh` 2.45.0 is installed, and a writable temp directory with at least 400 MB free is
    available — the ledger inflation rows need it. **`glab` is NOT installed** and no row may depend on
    it or skip because of it. The three documented flakes are pre-existing, and the `envelope_tracer`
    ETXTBSY race is `C-10`'s own seam, so a firing here is recorded rather than fixed.
  </precondition>
  <read_first>
    `tests/envelope_control_carrier.rs:1-160` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, `permits`, the
    one-`TempDir`-per-row discipline and the walk — and its bare-remote fixture and four-call cap-reset
    drive, which this file rebuilds rather than cites.
    **`src/envelope/policy.rs:2158-2355`** (`Token` — **read `literal`'s own table naming EXPANSION,
    PATHNAME, TILDE and BRACE as the four classes that clear the bit, because `T-19-115` is round 5's
    own class list arriving in round 10's corpus** — and `redirection_unresolvable` at `:2259-2278`),
    **`:2357-2418`** (`Segment`, and in particular `redirection_unresolvable`'s doc: *"A property of the
    COMMAND and not of a word … Computed in `split_segments_with_heads` — the ONE walk"* — **this is the
    shape `19-29`'s redirection-target channel must copy**), **`:2420-2526`**
    (`split_segments_with_heads` — **read the `if token.operator { … continue; }` arm and the flush
    above it, because that is why a redirection-target token in the stream would SPLIT a redirected
    simple command, which is the defect the segment-count pins exist to catch**), `:2297`
    (`SEPARATORS`, read to record byte-identical), `:2954-2992` (`redirection_operator_len` — **the
    twelve-operator closed grammar, and which of them take a PATHNAME rather than a heredoc delimiter,
    a here-string or an fd number**), `:2994-3057` (`skip_redirection_target` — **its doc's *"Its text
    is never needed, only its extent"* is the sentence `19-29` must correct, and the quote handling it
    already performs is what would produce both the text and the literalness**), `:3144-3220`
    (`consume_redirection!` — the IO_NUMBER arm, the over-deletion control it names, and the
    unresolvable arm that DOES push a token), `:5179-5234` (`lexical_absolute_components`,
    `word_is_within` — the component-wise comparison rule (a) already makes), `:5236-5413`
    (`envelope_carrier_operand` and `envelope_carrier_refusal` — **the four conditions, the four stated
    fail-open directions whose count this round corrects to seven, and the rejected-options block**),
    `:7415-7485` (the anti-vacuity stripper's three protections and **the floor comment's `842`, which
    is the byte-minus-character DIFFERENCE and not the count**).
    **`src/envelope/hooks.rs:87-204`** (`install`, `install_in` — **read `:103-108` on why `install_in`
    takes `binary` EXPLICITLY, since under `cargo test` `current_exe()` is the test binary; that is the
    precedent for how `19-29` threads the guard's own binary** — and `write_stub`, `stub_body`),
    `:216-260` (`assert_provenance` and `assert_provenance_in` — **read what it certifies and what it
    does not: a RELOCATED COPY, never the exec target's bytes**), `:771-846` (the guard's three latency
    rules and `guard`, which resolves the root and hands `Config::default_path()` to `guard_in`),
    `:848-900` (`guard_in`'s signature), `:954-1030` (`classify_segments` and **round 10's clause at
    `:1018-1029`, the one site `19-29` widens**), `:1390-1534` (`GUARD_TIMEOUT_SECS`, the D-07
    second-carrier table, `settings_value`, `guard_command` — **the binary is baked into BOTH the stub
    and the guard registration, which is why `C-10` is layer 2 and layer 3 at once**).
    **`src/envelope/ledger.rs:1-100`** (the module doc's *"a cap the driven agent can reset is not a
    cap"*, `LEDGER_FILE`, `ledger_path_in`), **`:180-250`** (`record_and_check`, `record_and_check_in`
    and the **unbounded `std::fs::read` at `:225-227`**), **`:280-303`** (`ends_mid_line` — **read its
    doc: *"a whole-file read to answer a one-byte question is the sort of thing that turns a guard into
    a hang"*. That reasoning is three functions above the unbounded read, and stating the contrast is
    `T-19-117`'s whole finding**), `:305-377` (`Tally`, `saturating_bump`, `tally` — **and its
    *"a line this function cannot parse is COUNTED, not skipped … under-counting is the one failure this
    file exists to prevent"* paragraph, which is why a TAIL read is the wrong bound and a fail-CLOSED
    size bound is the right one**).
    **`src/envelope/cred.rs:355-434`** (`hooks_path_env`, `write_gitconfig`'s doc naming the three
    helper routes it puts out of reach, `write_gitconfig_in`, `gitconfig_body` — **the file `T-19-118`
    appends to, and the claim it falsifies**), `:200-215` (`config_env` — **the `GIT_CONFIG_COUNT`
    triplet builder the empty-helper candidate control would extend**), `:480-560` (`build_env_in` —
    `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` both pointed at the generated file, `GIT_ASKPASS`,
    `SSH_AUTH_SOCK` removed rather than emptied).
    **`src/envelope/advisory.rs:195-264`** (**the three rules for the next editor, the pinned-phrase
    ordering with `cannot reach your ambient git credentials` FIRST — which sits INSIDE the clause
    `T-19-118` falsifies — the G-19-1 compression record, and `SECTION_ENVELOPE` itself. Read to RECORD
    and to count its whitespace tokens; do NOT edit it here**).
    `src/envelope/mod.rs:146-226` (`ENVELOPE_ROOT_ENV` **with its residual-limit paragraph at
    `:165-168`, which `T-19-116` contradicts and which `19-29` corrects**, `envelope_root`,
    `envelope_dir_in`).
    `19-SECURITY.md`'s sections "Threats found by audit 10", "`T-19-114` — CLOSED AS SCOPED", "The four
    fail-open directions — re-measured", "`SECTION_ENVELOPE`'s repaired cap clause", "The known-open
    set, as audit 10 verified it", "The mechanism pins, verified non-dead", "The byte floors and the
    stripper's three protections, RE-DERIVED", "Gates observed", "Audit 10 — what the round-10 control
    can and cannot fail on" and "Audit 10 Sign-Off".
  </read_first>
  <action>
**Measure before you assert. Nothing in this file is written until it has been driven, and every asserted
row names its CONTROL and states what makes the pair discriminating.**

Build, then drive every guard row the way audits 9 and 10 did: one JSON `PreToolUse` request on stdin
carrying `tool_name` `Bash` and a `tool_input.command`, piped to `./target/debug/gsd-meta-manager
envelope guard alpha` with a fresh `GSD_MM_ENVELOPE_ROOT` from `mktemp -d`, and the whole root walked
afterwards. Build the JSON with `python3 -c` using `json.dumps` so nested quoting survives as the bytes
it is. **Verify by eye that each driven row's argv is the one intended** — a mis-quoted `>` silently
turns an operand row into a redirection row, which on this axis changes the CLASS. Prove the walk
non-blind on every pass with a positive control that leaves exactly one ledger line.

**1. `T-19-116` — `C-10`, driven independently and end to end, with a control beside every leg.**

Rebuild the bare-remote fixture rather than citing audit 10's: a local bare upstream, a working clone on
`refs/heads/gsd-auto/alpha/w`, and `pre-push`/`pre-commit` written in `stub_body`'s exact three-line
shape and delivered exactly as the envelope delivers them, through the `GIT_CONFIG_COUNT` /
`GIT_CONFIG_KEY_0` / `GIT_CONFIG_VALUE_0` triplet naming `core.hooksPath`. Record the remote's SHA
before and after every leg. Drive, in this order: the plain force push with the real binary in place
(expect REFUSED, remote unmoved); the binary replaced with `cp /bin/true`, then the same push (expect
COMPLETED, remote moved, both SHAs recorded); the binary restored, same push (expect REFUSED, remote
unmoved again). **Measure LAYER 2 separately** by running the replaced binary as `envelope guard alpha`
against `git push --force origin main` and recording its exit code, then the real binary against the
same. **If any leg does not reproduce, that is a finding — record the measured behaviour and report it.**

Then the guard-level rows, with their controls:

  - `cp /bin/true <BINARY>` and at least two further operand spellings naming the same absolute literal
    path under programs whose names appear in neither production half — the NO-PROGRAM-NAMES fence one
    level over. Each measured exit 0 today and asserted **exit 2 `envelope_assertion_failed`** after,
    with the derivation written beside it: `19-29`'s clause widens the PATH SET to the binary this guard
    is running as, compared as an EXACT PATH, raised at the same one site before the resolution match,
    so the identifier is the general unresolvable one.
  - **the EXACT-PATH-not-PREFIX controls, which are this round's `--signed no` from the other side**:
    `cp /bin/true <BINARY-PARENT>/some-other-file` and `ls <BINARY-PARENT>`, both measured exit 0 and
    both asserted **exit 0 AFTER**, with the comment stating that a clause written as a DIRECTORY prefix
    over the binary's parent turns both red and would refuse `ls ~/.cargo/bin` and every `cargo install`.
  - the discovery rows `command -v gsd-meta-manager` and `cat /proc/self/cmdline`, measured and pinned
    PERMITTED, because a carrier whose location is trivially discoverable is part of the finding.
  - the spellings that will REMAIN open, each measured exit 0 and asserted **exit 0 AFTER** as
    verdict-preserving: `cp /bin/true $(command -v gsd-meta-manager)` (direction (ii) over the binary), a
    tilde spelling of the binary's path, and a relative spelling reached after a `cd`.
  - **and the one measurement that decides whether a SEVENTH silence exists over the new path**: record
    whether `std::env::current_exe()` reports the same path the stub and `guard_command` baked in. If
    the installation is reached through a symlink they differ, and the difference is direction (iii)
    arriving over a second path. **Measure it; do not assume either answer.**

**2. `T-19-118` — `C-05`'s credential half, with the generated-file control beside it.**

Reproduce the envelope's own posture: `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` both pointed at a
`gitconfig` written by `write_gitconfig_in`, a fake `HOME` holding a `.git-credentials` with a known
username and password, and `GIT_TERMINAL_PROMPT` as the envelope sets it. Drive:

  - **the CONTROL first**: the file exactly as `write_gitconfig_in` leaves it, `git config --get
    credential.helper` — record the exit code and that no helper is named;
  - the write through the guard: appending a `[credential]` section naming `helper = store` by
    redirection — record exit 0 and the empty walk, and state in the comment that this is fail-open
    direction (i) and that rule (a) cannot see a redirection target;
  - **the harm**: `git credential fill` for `https://github.com` under that posture — record whether the
    ambient username and password come back, and record them as PRESENT/ABSENT rather than transcribing
    the secret into the file (SAFE-04's own reasoning applies to this corpus too);
  - **the six write spellings**, so the record states exactly which four rule (a) already reaches: `cp`,
    `sed -i`, `tee -a` and a `python3 -c` payload naming the absolute path (expect exit 2), against
    `printf … >>` and `cat … >>` (expect exit 0).

Assert the redirection spelling at **exit 2 `envelope_assertion_failed`** after, with its discriminating
control being the same append to a path outside the envelope, which stays exit 0. **Record `AR-19-04`
verbatim beside it** — its mitigation *"the envelope regenerates it at each run start"* — with the
observation that it does not cover a write during the run and that this consequence is `T-19-23`'s
subject at `high` while the acceptance is about tampering at `medium`. **Do NOT un-accept it.**

**Then the CANDIDATE CONTROL, planner-derived, MEASURED and RECORDED — never asserted.** The envelope
already injects `core.hooksPath` through `GIT_CONFIG_COUNT`, and env-injected pairs are applied after
every file. Add a second pair naming `credential.helper` with an EMPTY value and measure, with the
no-injected-pair control beside each: whether `git config --get credential.helper` then reports nothing
even with `helper = store` appended to the generated file; whether `git credential fill` then returns
nothing; **and the COST** — whether `GIT_ASKPASS` still answers, whether `gh` still operates, and
whether a later `-c credential.helper=store` on the same command line overrides the reset. **This is the
one control that would defend `C-05`'s credential half against EVERY spelling rather than one, because
it does not read the command line at all. Record all of it; assert none of it.** `19-29` takes it only if
it reproduced.

**3. `T-19-115` — the seven spellings, each with its absolute-literal twin.**

For each of TILDE, GLOB and BRACE, drive at least the rows audit 10 measured and one more of your own,
in **both** word positions:

  - as an OPERAND: a tilde spelling of the alias directory and of a hook stub; `<ENV>/alph?`;
    `<ENV>/alpha/*`; `<ENV>/alpha/pr-ledger.ndjso?`; `<ENV>/alpha/{pr-ledger.ndjson,x}`;
  - as a REDIRECTION TARGET — **the cell nobody has drawn**: the same three spellings after a `>`;
  - **against the REAL envelope root as well as a temporary one** for the tilde row, because the tilde
    spelling of the real root is the natural one and audit 10 measured it exit 0 while its absolute twin
    is exit 2;
  - **with `bash` confirming the shell really reaches the file** in each case — a spelling the guard
    permits but the shell does not resolve is not a bypass, and this is the leg that tells them apart;
  - **and the ABSOLUTE LITERAL TWIN beside every one**, measured exit 2, which is what makes each row
    discriminating rather than a bare permit.

Every one of these is asserted **exit 0 BEFORE and AFTER**, verdict-preserving, with the comment stating
that `19-29` writes NO rule for them because a rule would have to perform expansions the guard is
forbidden to perform — a tilde needs the environment, a glob needs the filesystem — and that the fix is
the residue's ARITHMETIC: the docs say FOUR directions and there are SEVEN.

**4. `T-19-117` — the ledger as work, and the derived bound.**

In ONE persistent root per inflation level, with entries dated OUTSIDE the 24 h window so the cap itself
is unaffected, build ledgers at empty, ten thousand lines, two hundred thousand lines and two million
lines, recording the byte size and line count of each. For each level time, against that same root:
`ls`, `git status`, `git push --force origin main`, and the forge commands the cap governs — `gh pr
create --title x` and a `gh api` pull-request creation. **Record the split**: the cost falls only on the
commands that reach `record_and_check_in`. Record the largest level's forge timing against
`GUARD_TIMEOUT_SECS = 5` explicitly. Record the three inflation spellings that deliver it through
direction (i). **`glab` is NOT installed; record its cell as unmeasurable against its callee rather than
driving or inferring it.**

**The BEHAVIOURAL half — what the agent CLI does with a `PreToolUse` hook past its registered timeout —
is UNMEASURED, is a property of a closed-source binary, and is claimed in NEITHER direction.** Say so in
the file, in those terms, beside the mechanical rows.

Then the DERIVED post-fix row and its discriminating control: with a fail-CLOSED size bound in
`record_and_check_in`, a ledger past the bound makes `gh pr create --title x` answer **exit 2
`envelope_assertion_failed`** — the general unresolvable identifier, not `PrCapExceeded`, which names a
mechanism this refusal does not use (D-24) — **while a ledger just UNDER the bound still permits and
still counts correctly.** Both halves are asserted; without the second the row proves only that a large
number refuses. Write the bound's derivation: the default caps and the 24 h window put an upper bound on
how many entries legitimate use can produce, so a ledger larger than that could not have arisen from
permitted forge calls, and refusing is the direction that preserves `tally`'s own no-under-count
invariant where a TAIL read would violate it.

**5. The two ORDERING pins, at deliberately DIFFERENT identifiers**, which are the mechanical proof
`19-29`'s widened clause is still raised in the one per-segment walk rather than in a second pass.
Compose a segment carrying BOTH a new carrier and an existing refusal, and pin the identifier each
ordering produces — a redirection target under the envelope root beside a `--force` push, and a binary
operand beside a `--force` push, each in both orders, each with an outside-the-envelope control.
**Measure both orderings before writing either**, and RECORD rather than assert any whose post-fix
identifier cannot be derived from the placement.

**6. The SEGMENT-COUNT PINS — this plan's sharpest new control, and they are GREEN today.** Assert over
`policy::split_segments_with_heads` directly, in both directions, that
`git >/dev/null push --force origin main` produces exactly ONE `Segment` whose tokens are exactly the
five surviving argv words, and that `git x2>/tmp/o push --force origin main` produces exactly ONE whose
tokens are exactly the six surviving words. The failure message says what a red means: a
redirection-target token entered `tokenize`'s stream, `split_segments_with_heads` flushed the segment at
it, and the leading `git` now resolves `Governed` with an empty argv, which `classify_git` answers
`Allow` for — **so the correct response is to move the target onto the SEGMENT the way
`Segment::redirection_unresolvable` already travels, never to relax this assertion.** Add the guard-level
twins beside them: the `>/dev/null` row still exit 2 `force_push_blocked`, the `x2>` row still exit 0,
the `2>/dev/null` row still exit 2, and `git >` still failing closed. **And a heredoc row**: measure
what `git <<EOF push --force origin main` answers today and pin it, with the comment recording that
`<<`'s target is a DELIMITER rather than a path and must never be recorded as a carrier candidate.

**7. The rows that are RECORDED and never asserted**, each with its measured verdict, its walk and the
reason no assertion is possible: every `C-11` … `C-15` row, `C-08`'s behavioural half, `C-05`'s alias
half, and the empty-helper candidate control. Print them under a heading that says RECORDED, not
asserted, and add the mechanical self-assertion this phase uses — a test that reads this file's own text
and proves no repo-side row is written as an assertion.

**8. Carry forward every mechanism pin** from rounds 4 through 10, re-asserted in this file over the
same public functions rather than moved: round 5's literalness bit non-vacuous, round 6's deletion model
and its over-deletion control, `SEPARATORS` byte-identical with `is_separator(">")` false, round 7's four
callee-grammar directions, round 8's confinement clause, round 9's re-parse clause with the `aliasx.` /
`notalias.` controls and both `T-19-86` `!`-bodied rows, and round 10's clause over the envelope-root
operand rows. **All green today and all green after `19-29`.**

Print the walk for at least one refused row and one counted row. Commit as its own commit, RED, with
**zero `src/` hunks**.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_carrier_reach --no-fail-fast 2&gt;&amp;1 | tail -40; for t in envelope_control_carrier envelope_wrapper_class envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_advisory envelope_credential envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || echo "UNEXPECTED RED: $t"; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git show --stat HEAD | tail -8; git show --numstat HEAD -- src/ | wc -l</automated>
  </verify>
  <done>`git diff --numstat 0092009..ca30c90 -- src/ tests/` was confirmed empty before the first measurement; `tests/envelope_carrier_reach.rs` exists as the TENTH evidence file with its header stating what it is and which round produced it; `T-19-116`'s bare-remote fixture was REBUILT and every leg re-measured with both remote SHAs recorded and a control on both sides, with layer 2 measured separately and the `current_exe()`-versus-baked-path question answered by measurement; `T-19-118`'s credential-fill drive ran with the generated-file control first, the six write spellings recorded, the secret recorded as present/absent rather than transcribed, and `AR-19-04` quoted verbatim beside it and NOT un-accepted; the EMPTY-`credential.helper` candidate control was measured in both its effect and its cost and is RECORDED, never asserted; `T-19-115`'s tilde, glob and brace rows were driven in BOTH operand and redirection-target position, against the real envelope root as well as a temporary one, with `bash` confirming the shell reaches the file and the absolute-literal twin measured exit 2 beside each; `T-19-117`'s latency curve was driven in one persistent root per level with the forge/non-forge split recorded, the largest level stated against `GUARD_TIMEOUT_SECS = 5`, `glab` recorded as unmeasurable against its callee, and **the behavioural half stated UNMEASURED and claimed in neither direction**; the derived bound row carries its just-under-the-bound discriminating control and its written derivation; the two ORDERING pins were measured in both orders before either was written and land at deliberately different identifiers; **the SEGMENT-COUNT pins assert exactly ONE `Segment` with exactly the surviving argv for both the `>/dev/null` and the `x2>` rows and are GREEN**, with a failure message naming the defect and the correct response; every asserted row names its control and states what makes the pair discriminating, and any row whose control fails at the same identifier is `record_only`'d with its overlap stated; every `C-11` … `C-15` row, `C-08`'s behavioural half, `C-05`'s alias half and the candidate control are RECORDED with a mechanical self-assertion proving none is written as an assertion; every mechanism pin from rounds 4 through 10 is re-asserted and green; the file is RED on exactly the derived-post-fix rows and the failing names are recorded verbatim; and this commit shows ZERO `src/` hunks.</done>
</task>

<task type="auto">
  <name>Task 2: the AXIS — from seven classes to eleven, with the redirection alphabet moved arm</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:7769-7819` (**section 17's header — the argument for why round 10 is
    a fifth axis, the mechanical reachability proof, and the paragraph on what is NOT claimed about
    text-level disjointness. This task EXTENDS that section and must keep its argument true**),
    `:7821-7908` (`REPRESENTATIVE_ENVELOPE_ROOT`, `REPRESENTATIVE_NEAR_MISS_ROOT`, the two placeholders,
    `SYMLINK_MARKER` **with its statement that a symlink is not textually distinguishable — which is
    exactly why direction (iii) fails open**, `CARRIER_BASENAMES`, `REPO_SIDE_CARRIER_PATHS`,
    `REDIRECTION_OPERATORS`' reuse, `CarrierWord`, `carrier_words`, `carrier_basename`),
    `:7910-8022` (**the seven class predicates — read `draws_an_envelope_root_operand`'s bare
    `starts_with(REPRESENTATIVE_ENVELOPE_ROOT)`, which a GLOB or BRACE spelling under the root also
    satisfies, and `draws_a_relative_carrier`'s `!starts_with('/')` guard, which a TILDE spelling also
    satisfies. Both collisions fire the disjointness fence and both are why this task REFINES two
    existing predicates rather than only adding three**), `:8024-8033`
    (`CONTROL_CARRIER_REPRESENTATIVES`), `:8035-8069` (`ENVELOPE_ROOT_OPERAND_CARRIERS` and its
    **KEPT OUT OF THE INVARIANCE ARM** doc, which is the exact argument this task reuses for the
    redirection and binary alphabets), `:8071-8176` (the six remaining alphabets, the near-misses and
    `CONTROL_CARRIER_GOVERNED_TWIN`), `:8178-8198` (`CONTROL_CARRIER_ALPHABETS` and
    `CONTROL_CARRIER_INVARIANCE_ALPHABETS` — **the arm assignment this task changes**), `:8200-8296`
    (the case, slot, per-alphabet and per-class arithmetic, **all EXACT equalities that must be
    re-derived**), and every `#[test]` from `:8298` to the end of section 17 — the degenerate-proofing,
    the two fences, the disjointness proof and the invariance property.
    The four earlier axes' class lists and floors — `UNREADABLE_CLASSES`, `DELETION_CLASSES`,
    `CALLEE_GRAMMAR_CLASSES`, `CONFIG_RESOLUTION_CLASSES` — **read only to confirm they stay
    byte-identical and that `MIN_CONFIG_RESOLUTION_CLASSES` stays at 6.**
    `tests/envelope_wrapper_class.rs`'s anti-vacuity stripper, `POLICY_MIN_PRODUCTION_BYTES` and
    `HOOKS_MIN_PRODUCTION_BYTES`, and `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`.
    Task 1's measured verdicts, which are what every new entry's arm assignment is derived from.
  </read_first>
  <action>
**Extend section 17 in place. Nothing outside it moves, and no existing alphabet ENTRY is deleted,
reworded or narrowed.**

**1. REFINE two existing class predicates, because two collisions are structural rather than incidental.**
`draws_an_envelope_root_operand` must exclude a word carrying a pathname-expansion or brace-list
metacharacter, and `draws_a_relative_carrier` must exclude a word beginning with a tilde. Without both
refinements a glob or brace spelling under the root satisfies class 1 and a tilde spelling satisfies
class 5, and the disjointness fence fires on a collision that is a corpus artefact rather than a fact
about the guard. **The refinement is stated in each predicate's doc as a refinement, with the spelling it
hands to the new class named.**

**2. MOVE `CONTROL_CARRIER_REDIRECTION_TARGETS` out of the invariance arm and split it.** All four of its
current entries carry ABSOLUTE LITERAL targets and Task 1 measured them refused after `19-29`. Leaving
them in `CONTROL_CARRIER_INVARIANCE_ALPHABETS` would put entries whose verdict the fix CHANGES into the
invariance-preserving arm — `19-18`'s `{v}>` blocker, `19-20`'s split, `19-22`'s, `19-24`'s and
`19-26`'s own, a sixth time. Give the absolute-literal targets their own **fail-closed** alphabet with
its own property, and create a NEW verdict-preserving redirection alphabet for the targets that stay
permitted: an expansion-borne target, a tilde target, a glob target, a brace target and a relative
target. **Both docs state the arm they are in and why**, in the shape `ENVELOPE_ROOT_OPERAND_CARRIERS`'
own doc already uses.

**3. ADD three verdict-preserving SPELLING classes** — tilde-borne, glob-borne and brace-list-borne
carrier words — each with its own alphabet drawing in **both** word positions, operand and redirection
target. Their predicates decide on the metacharacter and the carrier path, never on a program name.
Their docs state that `19-29` writes NO rule for them and why: a tilde needs the environment and a glob
needs the filesystem, and the guard is forbidden both, so **these are directions (v), (vi) and (vii) of a
residue the docs currently give as four.**

**4. ADD one fail-closed BINARY class.** `C-10` is outside the envelope root, so class 1 does not draw it
and the ordinary-operand complement WOULD — which would assert PERMITTED a row `19-29` refuses, landing
permanently red in a file `19-29` may not edit. Its alphabet carries the operand spellings Task 1
measured, and **the EXACT-PATH-not-PREFIX near-miss controls go in the ORDINARY alphabet and are asserted
BY NAME by a fence**, the way `CONTROL_CARRIER_NEAR_MISSES` already is: a clause written as a directory
prefix over the binary's parent turns them red, and only an exact-path comparison keeps them green. Use a
representative binary path constant beside `REPRESENTATIVE_ENVELOPE_ROOT`, with a placeholder the
guard-driven properties substitute, exactly as the envelope root is handled.

**5. RE-DERIVE the arithmetic and state it as exact equalities.** `MIN_CONTROL_CARRIER_CLASSES` becomes
eleven, `CONTROL_CARRIER_CASES` and `CONTROL_CARRIER_SLOTS` are recomputed from the alphabets, every
`MIN_*` per-alphabet floor is set to its alphabet's exact length, and `CONTROL_CARRIER_CLASS_COUNTS` is
recomputed per class over the whole generated set with each count's derivation written beside it —
including any deliberate overlap, which is RECORDED as an overlap rather than smoothed away, the way
class 4's `ln -s` mitigation already is. **Audit 5 found `19-16` set a floor of 50 against a maximum of
40 by construction; state the arithmetic and check it against the alphabets rather than against the
prose.**

**6. EXTEND the two mechanical fences and add a third.** The NO-PROGRAM-NAMES fence must now also draw at
least one BINARY-carrier row and one row from each of the three spelling classes under a program name
absent from both production halves. The PATH-PREFIX-NOT-BASENAME fence gains the binary's near-miss
controls by name. **And add a NO-EXPANSION-SPELLING fence: assert mechanically that the axis can now DRAW
a `~`, a `*` or `?`, and a `{a,b}` inside a CARRIER PATH** — counting entries, not prose — because the
absence of exactly that is `T-19-115`, and a floor that cannot fail on a class is how this failure mode
reached its tenth consecutive round. Its failure message says the correct response is to add the
spelling, never to relax the fence.

**7. RE-PROVE disjointness and the class structure over all eleven**, extending the existing degenerate
-proofing rather than replacing it: one representative per class, each satisfying its own predicate and
no other except where an overlap is RECORDED with its reason. **Keep section 17's honest limitation
intact** — the disjointness that is asserted is about REACHABILITY, not about text, because a text-level
claim against the four existing axes would be false and `19-26` already refused to assert it.

**8. Leave everything else byte-identical.** The four earlier axes, their predicates, their
degenerate-proofing and their floors do not move; `MIN_CONFIG_RESOLUTION_CLASSES` stays at 6; the
anti-vacuity stripper's three protections keep their present strictness; and
`wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` stays green.

Commit as its own commit, with **zero `src/` hunks**. Record which of this file's tests are RED and why
each is red — a floor that cannot be met until `19-29` lands is a different kind of red from a verdict
assertion, and the SUMMARY distinguishes them.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo test --test envelope_carrier_reach --no-fail-fast 2&gt;&amp;1 | tail -25; for t in envelope_control_carrier envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || echo "UNEXPECTED RED: $t"; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -n 'MIN_CONTROL_CARRIER_CLASSES\|MIN_CONFIG_RESOLUTION_CLASSES\|CONTROL_CARRIER_CASES\|CONTROL_CARRIER_SLOTS' tests/envelope_wrapper_class.rs; git show --numstat HEAD -- src/ | wc -l; git diff --numstat HEAD~1..HEAD -- tests/</automated>
  </verify>
  <done>Section 17 names ELEVEN classes with `MIN_CONTROL_CARRIER_CLASSES = 11`; `draws_an_envelope_root_operand` and `draws_a_relative_carrier` are REFINED so a glob, brace or tilde spelling no longer collides with them, each refinement stated in its own doc with the class it hands the spelling to; `CONTROL_CARRIER_REDIRECTION_TARGETS`' absolute-literal entries are MOVED into a fail-closed arm with their own property and the `19-18`/`19-20`/`19-22`/`19-24`/`19-26` precedent named, and a NEW verdict-preserving redirection alphabet carries the targets that stay permitted; three verdict-preserving spelling alphabets (tilde, glob, brace) draw in BOTH operand and redirection-target position with their docs stating that no rule is written and why; one fail-closed BINARY alphabet exists with a representative-path constant and placeholder beside the envelope root's, and its EXACT-PATH-not-PREFIX near-miss controls sit in the ORDINARY alphabet and are asserted BY NAME; every `MIN_*`, `CONTROL_CARRIER_CASES`, `CONTROL_CARRIER_SLOTS` and per-class count is RE-DERIVED from the alphabets and stated as an exact equality with its arithmetic written beside it, and every deliberate overlap is RECORDED rather than smoothed; the NO-PROGRAM-NAMES fence draws a binary row and one row per new spelling class under program names absent from both production halves; the PATH-PREFIX-NOT-BASENAME fence names the binary's controls; **a NO-EXPANSION-SPELLING fence asserts mechanically that a `~`, a `*` or `?`, and a `{a,b}` each really appear inside a carrier path**, with a message saying to add the spelling rather than relax the fence; disjointness is re-proved over all eleven with section 17's reachability-not-text limitation intact; the four earlier axes, their predicates, their degenerate-proofing and their floors are byte-identical, `MIN_CONFIG_RESOLUTION_CLASSES` is still 6, and no existing alphabet entry was deleted, reworded or narrowed; `git diff --numstat` over `tests/` shows ZERO deletions; and this commit shows ZERO `src/` hunks.</done>
</task>

<task type="auto">
  <name>Task 3: the disposition table, the registrations, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s "Threats found by audit 10" table, "The four fail-open directions — re-measured,
    and handed to NO control", "`SECTION_ENVELOPE`'s repaired cap clause", "The known-open set, as audit
    10 verified it", "Audit 10's bookkeeping", "The four execution-time judgements" (**item 3 carries
    the `842`/`421` correction and item 4 carries `mod.rs:165-168`'s handoff**), "Audit 10 — what the
    round-10 control can and cannot fail on" and "Audit 10 Sign-Off"; plus the appended execution
    records of plans 19-26 (`:8435`) and 19-27 (`:9014`) — **which this plan's subsection FOLLOWS and
    does not edit.** The Accepted Risks Log, and `AR-19-03`, `AR-19-04` and `AR-19-05` in particular.
    `deferred-items.md`'s plan-19-26 section at `:1755` and plan-19-27 section at `:2040`, **its
    `842` at `:2172` — the site corrected BESIDE here** — and its "Still OPEN and unchanged" rows.
    `19-27-SUMMARY.md`'s per-binary counts and its recorded `passed + failed` total.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-27 record and touching nothing that
precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan 19-28 —
carrying:

  - **FIRST, and in these words: this plan closes nothing and `/gsd-secure-phase 19` is NOT cleared.**
    `T-19-86`, `T-19-91`, `T-19-111` and `T-19-112` remain open at `high`; `T-19-115` … `T-19-118` are
    all open at this plan's end; and whether `19-29` closes any of them is audit 11's judgement rather
    than either plan's claim.
  - **THE FIFTEEN-CARRIER DISPOSITION TABLE, WITH A CONTROL LETTER FOR EVERY ONE** — the round's central
    product. `C-01` … `C-09` control (a) as `19-27` wrote it; **`C-10` control (a), EXACT PATH,
    PARTIAL**, with `T-19-116` as its threat id, `high` as its severity, and the note stated plainly
    that its absence from `19-27`'s table is how a carrier that takes two layers in one call survived a
    round that enumerated it; `C-11` … `C-15` control (e), no rule, unaccepted. **Fifteen carriers,
    fifteen letters, nothing left undispositioned.**
  - **`T-19-116` driven end to end**, with both remote SHAs, every control, the separately measured
    layer-2 answer, the discovery rows, and `T-19-01`'s mitigation recorded as correct-for-what-it-covers
    and unweakened.
  - **`T-19-118` driven with its control**, the six write spellings, the harm recorded as present/absent
    rather than transcribed, and **`AR-19-04`'s reasoning gap recorded and explicitly NOT un-accepted**,
    with the statement that `T-19-118` is registered as its own row rather than moved into `T-19-23` or
    into the acceptance.
  - **THE ANSWER TO THE DIRECTION-(i) QUESTION, STATED IN FULL** — that closing it does NOT require the
    deletion model to read redirection targets as argv, because round 6's model answers which words
    ARRIVE and rule (a) asks whether the line NAMES a control's path; that `skip_redirection_target`
    already walks the target's extent with its own quoting rules; that `redirection_operator_len` is a
    closed twelve-operator grammar of which only some take a PATHNAME; that
    `split_segments_with_heads` excludes operator tokens from `segment.tokens` and flushes at them, so
    a token in the stream would split a redirected simple command and turn round 6's headline refusal
    into a permit; **and what it costs** — two additive fields, a return-type change on a private
    function with four call sites, a corrected doc sentence, and a pathname/non-pathname split of a
    grammar already enumerated. Record the SEGMENT-COUNT pin measurements that make the claim
    falsifiable.
  - **`T-19-115`'s seven spellings with the residue's CORRECTED ARITHMETIC** — the docs say FOUR
    directions and there are SEVEN; the three unnamed ones need no prior read, no symlink and no cwd;
    and `19-29` writes no rule for them because a rule would have to perform expansions the guard is
    forbidden to perform. Record that this is `T-19-76`'s failure mode for the tenth consecutive round
    and that for the first time it is in the cell the previous round just filled.
  - **`T-19-117`'s latency curve** with the forge/non-forge split, the largest level against
    `GUARD_TIMEOUT_SECS = 5`, the derived fail-closed bound with its just-under-the-bound control and
    its derivation, `ends_mid_line`'s own contrasting reasoning quoted from three functions above the
    unbounded read, and **the behavioural half stated UNMEASURED and claimed in neither direction**.
  - **the axis arithmetic**, eleven classes with every floor re-derived, the redirection alphabet's arm
    change with its reason, and the NO-EXPANSION-SPELLING fence.
  - **the corrected multi-byte count**: `421`, not `842`, which is the byte-minus-character difference —
    with `policy.rs:7450` named as `19-29`'s to edit and `deferred-items.md:2172` **corrected BESIDE
    here, quoted and never edited**. State that every other number in that comment was re-derived by
    audit 10 and is right, and that the floor's value and strictness do not move.
  - **`mod.rs:165-168` put explicitly IN SCOPE for `19-29`**, with the reason: `T-19-116` is a route
    that lowers a standing boundary without controlling the environment the TUI starts in, which
    contradicts that paragraph's *"does not lower a boundary that was standing"* argument. `19-27` read
    it and correctly declined because it was outside its `files_modified`; this round names it.
  - the mechanism pins and byte floors re-measured; the `glab --host` cell unchanged with `--hostname`
    KEPT and `glab` confirmed not installed; `C-08` carried forward with its behavioural half
    UNMEASURED; `T-19-110` at `medium`; **`T-19-17r` OUTSTANDING for the eleventh time with no
    `AR-19-13` and no use of the word "accepted"**; and the gate.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Append a plan-19-28 section registering **`T-19-116` at `high` with `C-10`'s
own deferred-items row and control letter** — the entry whose absence is the finding — **`T-19-118` at
`high`** with `T-19-23`'s reopening stated and no acceptance made, and **`T-19-115` and `T-19-117` at
`medium`** with their corpus recorded RED or RECORDED. Replace the four-direction residue with the seven
directions wherever this plan's own section states it, and record that the docs still say four and that
`19-29` corrects them. Record the `AR-19-04` reasoning gap; record the `421` correction with
`:2172` quoted BESIDE; carry `C-08`, `C-15`, `T-19-110` and the `glab --host` cell forward unchanged; and
leave `T-19-86`, `T-19-91`, `T-19-96`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-74` and the `T-19-17r`
bookkeeping gap unchanged and open.

**3. The gate, with its arithmetic STATED and CHECKED.**

  - The command is `rtk proxy cargo test --no-fail-fast`, counted with `rtk proxy grep` over a redirected
    log, never a plain `grep` (D-34).
  - Baseline is **1771** on `passed + failed`, recorded by `19-27-SUMMARY.md` over 45 result lines and
    independently re-measured by audit 10 with 13 ignored. After this plan `passed + failed` is strictly
    greater than 1771; **a red test RAN, so red→green leaves the total unchanged and every increase
    comes ONLY from new `#[test]` fns** — count them from `git show` and check the identity. A
    disagreement is a finding.
  - **This plan's gate NAMES its expected failures rather than requiring zero.** Record the complete RED
    name list verbatim and the complete PERMITTED list — both are `19-29`'s handoff contract, and
    `19-29`'s first action is confirming both.
  - **All SEVENTEEN `envelope_*` binaries must RUN**, with their per-binary counts recorded. **A run
    reporting sixteen is a run in which this plan's own evidence file did not execute.**
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.
  - The three documented flakes are out of scope; record any firing with its output and state that
    absence is not evidence they are fixed. **The `envelope_tracer` ETXTBSY race is `C-10`'s own seam and
    this plan exercises it.**

Commit the record as its own commit, the third, with **zero `src/` hunks**.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-28-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -cE '^\| AR-19-13 \|' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-28-gate.log | tail -55; rtk proxy grep -c 'Running tests/envelope_' /tmp/19-28-gate.log; git show --numstat HEAD -- src/ | wc -l; git diff --numstat HEAD~3..HEAD -- src/ | wc -l; git diff --numstat HEAD~3..HEAD -- tests/ | tail -6; git log --oneline -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-28 record stating FIRST that this plan closes nothing and clears no gate; carrying the FIFTEEN-CARRIER DISPOSITION TABLE with a control letter for every one including `C-10`'s new (a)-exact-path-partial and its `T-19-116`/`high` registration, with the note that its absence from `19-27`'s table is how it survived; `T-19-116`'s end-to-end drive with both remote SHAs, every control, the separately measured layer-2 answer and `T-19-01` recorded unweakened; `T-19-118`'s drive with its control, the six spellings, the harm recorded rather than transcribed, and `AR-19-04` recorded and NOT un-accepted; **the direction-(i) question answered in full with its three source-read mechanical facts, its stated cost, and the segment-count measurements that make the claim falsifiable**; `T-19-115`'s seven spellings with the residue's corrected arithmetic; `T-19-117`'s curve with the derived fail-closed bound, its discriminating control and the behavioural half stated UNMEASURED; the axis arithmetic; the corrected `421` with `deferred-items.md:2172` quoted BESIDE and `policy.rs:7450` named as `19-29`'s; **`mod.rs:165-168` put explicitly in scope for `19-29` with its reason**; the mechanism pins, byte floors, `glab --host`, `C-08`, `C-15` and `T-19-110` carried forward; and `T-19-17r` OUTSTANDING for the eleventh time with no `AR-19-13` and no use of "accepted" — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection; `deferred-items.md` carries a plan-19-28 section registering `T-19-116` and `T-19-118` at `high` and `T-19-115` and `T-19-117` at `medium`, with `C-10`'s own row and letter, the seven-direction residue, the `AR-19-04` gap, the `421` correction stated BESIDE, and every other open item unchanged; the `--no-fail-fast` run's `passed + failed` exceeds 1771 by exactly the number of new `#[test]` fns counted from `git show`, with the arithmetic stated and checked and the complete RED and PERMITTED name lists recorded verbatim as `19-29`'s handoff contract; ALL SEVENTEEN `envelope_*` binaries RAN with their per-binary counts recorded; `git diff --numstat HEAD~3..HEAD -- src/` is EMPTY across all three commits and `tests/` shows ZERO deletions; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, to what git does with a value the guard approved, and to the files the guard's own controls live in |
| **a control → the FILE it lives in, OUTSIDE the envelope root** | **This round's first boundary. `C-10` is the binary every hook stub and the guard registration exec, and rule (a)'s path set does not contain it** |
| **a path NAMED on the line → a path the rule can SEE** | **This round's second boundary. A redirection target is named and written but produces no token, so rule (a) is silent on it — fail-open direction (i), which carries a live `high` credential exploit** |
| **`Token.literal`'s class list → the corpus's alphabet** | **Round 5's own table names expansion, pathname, tilde and brace. The corpus draws one of the four, so the axis cannot fail on three of the rule's own silences** |
| the guard's DEADLINE → the work it must finish | `GUARD_TIMEOUT_SECS = 5` is registered in a file the guard writes; the ledger it must read whole is a file a permitted command can inflate |
| a rule's stated residue → the spellings its silence permits | Stated as FOUR. Measured as SEVEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-116 | Elevation of Privilege | `C-10` — the binary the hook stubs and `guard_command` exec (`hooks.rs:196-204`, `:1518-1524`); enumerated by `19-26`, marked UNOBSERVED, and given no threat id, severity, deferred-items row or control letter by `19-27` | high | **mitigate — CORPUS ONLY here; the rule is `19-29`'s** | Driven end to end against real git with a control on both sides and layer 2 measured separately; registered with a threat id, a severity, a deferred-items row and control **(a), EXACT PATH, PARTIAL**; the fail-closed corpus rows asserted at their derived post-fix verdict beside EXACT-PATH-not-PREFIX near-miss controls pinned PERMITTED, so a clause written as a directory prefix turns red |
| T-19-118 | Information Disclosure / Elevation of Privilege | `C-05` — the generated `gitconfig` on BOTH `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` (`cred.rs:519-526`), reached by fail-open direction (i) | high | **mitigate — CORPUS ONLY here** | The credential-fill drive re-run with the generated-file control first and the six write spellings recorded; the redirection spelling asserted at its derived post-fix verdict against a discriminating outside-the-envelope control; the EMPTY-`credential.helper` candidate control measured in effect AND cost and RECORDED; `AR-19-04` recorded and **NOT un-accepted** |
| T-19-115 | Spoofing / Elevation of Privilege | rule (a)'s `LITERAL` condition versus its own FOUR-direction disclosure (`policy.rs:5293-5325`), and the axis's carrier alphabets (`tests/envelope_wrapper_class.rs:8057`, `:8094`) | medium | **mitigate — the corpus gains the class; NO RULE is written** | Three verdict-preserving spelling classes drawing in both word positions, each row measured with its absolute-literal twin and with `bash` confirming the shell reaches the file; a NO-EXPANSION-SPELLING fence asserting the axis can draw each metacharacter; the residue's arithmetic corrected from four to seven, which is `19-29`'s doc change |
| T-19-117 | Denial of Service / Elevation of Privilege | `C-01` as unbounded work — `ledger::tally` over `std::fs::read` (`ledger.rs:225-227`) against `GUARD_TIMEOUT_SECS = 5` (`hooks.rs:1408`, `:1504`) | medium | **mitigate — the curve and the derived bound row here; the bound is `19-29`'s** | Latency curve driven per inflation level in one persistent root with the forge/non-forge split recorded; the derived fail-CLOSED bound row asserted beside a just-under-the-bound control that still permits and still counts, so the row proves discrimination rather than that a large number refuses. **The behavioural half is UNMEASURED and claimed in neither direction** |
| T-19-107-shape | Repudiation | a control's own text claiming a reach the measurement denies | — | mitigate | The corpus pins `SECTION_ENVELOPE`'s FIRST `Guaranteed` clause as `19-29`'s one pre-authorised update, named in advance by test function name, and records its whitespace-token headroom so `19-29` cannot raise the cap to make room |
| T-19-111 | Elevation of Privilege | the re-parse clause's CARRIER SET — `C-11`, a `.git/config` written by any command | high | **carried forward OPEN, NO rule, corpus rows RECORDED not asserted** | `19-27`'s five-site attribution correction stays as performed; no rule, no acceptance, and the rows are asserted in neither direction |
| T-19-112, T-19-113 | Tampering | the SAFE-06 ledger and the hook stubs as FILES | high / medium | **carried forward NARROWED-NOT-CLOSED** | Direction (i) is the residue they share with `T-19-118`, so this round's corpus rows for it are theirs too. `C-15` remains a distinct route and the deferred option (b) a further one; **nothing here is described as closure** |
| T-19-86, T-19-91 | Elevation of Privilege | `classify_git`'s denylist default arm through a governed program's own operand; a git classifier's decision operand assembled by expansion | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; rows at exit 0, pins green and UNMODIFIED. Because both remain open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-96, T-19-110, T-19-74 | Elevation of Privilege | a glob in a push flag; region 2's operand grammar; the frozen wrapper rows | medium | **accept for this plan (registered, not fixed)** | Left exactly as pinned |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — gap RECORDED, acceptance deliberately unmade for the eleventh time** | No `AR-19-13`, no use of the word "accepted" |
| — | Tampering | `C-08`'s claimed-but-unwired second carrier; `C-15`'s unclamped `pr_cap_*`; `C-12`; `C-13`; `C-14` | planner-derived / control (e) | **carried forward UNFIXED, measured and RECORDED** | No rule, no clamp, no acceptance. `C-08`'s behavioural half stays UNMEASURED; `C-15` stays a DISTINCT route to SAFE-06 steered by `HOME`/`XDG_CONFIG_HOME` rather than by argv |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. Every probe uses `std::process::Command` inside `#[cfg(test)]` |
</threat_model>

<verification>
- `git diff --numstat 0092009..ca30c90 -- src/ tests/` is EMPTY, verified before the first measurement,
  so this plan's base is the tree audit 10 measured.
- **Every commit shows ZERO `src/` hunks**, verified by `git diff --numstat HEAD~3..HEAD -- src/`
  returning nothing.
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`, and
  MUST be read as `passed + failed` against the exact total `19-27-SUMMARY.md` records — **1771**.
- The gate arithmetic is STATED and CHECKED: 1771 plus the NEW `#[test]` fns this plan adds, counted from
  `git show`. A disagreement is a finding.
- **ALL SEVENTEEN `envelope_*` binaries RAN** and their per-binary counts are recorded. Sixteen means this
  plan's own evidence file did not execute.
- Every asserted row was MEASURED against the built binary before it was written, with a fresh
  `GSD_MM_ENVELOPE_ROOT` per row, the whole root walked afterwards and the walk proved non-blind by a
  positive control leaving exactly one ledger line — **except the ledger-size rows, which use ONE
  persistent root per inflation level, and the cap-reset drive, which uses one persistent root**.
- **Every asserted row NAMES its control and states what makes the pair discriminating.** A row whose
  control fails the same way at the same identifier is `record_only`'d with its overlap stated.
- `T-19-116`'s bare-remote fixture was REBUILT and every leg re-measured independently, with the remote's
  SHA recorded BEFORE and AFTER each and a control beside every one, and layer 2 measured separately.
- `T-19-118`'s harm was driven with the generated-file CONTROL first and the secret recorded as
  present/absent rather than transcribed.
- **The SEGMENT-COUNT pins assert exactly ONE `Segment` with exactly the surviving argv for both
  `git >/dev/null push --force origin main` and `git x2>/tmp/o push --force origin main`, and are GREEN
  at this plan's end.**
- The two ORDERING pins were measured in both orders before either was written and land at deliberately
  different identifiers; any underivable one is RECORDED.
- The axis names ELEVEN classes; the absolute-literal redirection targets and the binary alphabet are in
  a FAIL-CLOSED arm; the tilde, glob, brace, expansion-borne, symlinked, relative and ordinary alphabets
  are verdict-PRESERVING; every floor and count is an exact equality re-derived from the alphabets.
- The NO-PROGRAM-NAMES, PATH-PREFIX-NOT-BASENAME and **NO-EXPANSION-SPELLING** fences all pass, and
  `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` stays green.
- The four earlier axes, their predicates, their degenerate-proofing and their floors are BYTE-IDENTICAL;
  `MIN_CONFIG_RESOLUTION_CLASSES` is 6; `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES`
  are unchanged; the stripper's three protections keep their present strictness.
- `SEPARATORS` is byte-identical at `policy.rs:2297`, `policy::is_separator(">")` is `false`, and every
  mechanism pin from rounds 4 through 10 is re-asserted and green.
- Every `C-11` … `C-15` row, `C-08`'s behavioural half, `C-05`'s alias half and the empty-helper candidate
  control are RECORDED and never asserted, with a mechanical self-assertion proving it.
- No row anywhere under `tests/` was edited or deleted; `git diff --numstat` over `tests/` shows ZERO
  deletions across all three commits.
- The `AR-19-13` row count is zero, the word "accepted" is not applied to `T-19-17r`, and no `AR-` row is
  added, edited, renumbered or un-accepted.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml` nor
  `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-27 record.
- Three commits exist in order: the evidence file, the axis, the record.
</verification>

<success_criteria>
1. The corpus is RED against the pre-fix tree on exactly the derived-post-fix rows, the failing names are
   recorded verbatim, and **every commit shows zero `src/` hunks** — the evidence a corpus can fail
   before a control exists.
2. **`C-10` has a threat id, a severity, a deferred-items row and a control letter**, and the record
   states plainly that its absence from `19-27`'s disposition table is how a carrier that takes layer 2
   and layer 3 in one permitted call survived a round that enumerated it. Fifteen carriers, fifteen
   letters.
3. `T-19-116` and `T-19-118` were driven END TO END against real git with a CONTROL beside every leg,
   independently rather than by citation, and the harm of each is recorded as measured rather than
   inferred.
4. **The direction-(i) question is answered explicitly and the answer is falsifiable**: closing it does
   not require the deletion model to read redirection targets as argv, the three source-read mechanical
   facts are on the record, the cost is stated, and the SEGMENT-COUNT pins that would catch getting it
   wrong are written and GREEN.
5. **The axis can now DRAW a `~`, a `*` or `?`, and a `{a,b}` inside a carrier path**, asserted
   mechanically by a fence rather than claimed in prose, and every row carries its absolute-literal twin
   with `bash` confirming the shell reaches the file.
6. The absolute-literal redirection targets and the binary alphabet are in a FAIL-CLOSED arm rather than
   the invariance arm, with the `19-18`/`19-20`/`19-22`/`19-24`/`19-26` precedent named — the sixth time
   that split has been forced by measurement.
7. `T-19-117`'s curve is measured with the forge/non-forge split, the derived bound row carries a
   discriminating just-under-the-bound control, and **the behavioural half is claimed in neither
   direction**.
8. Every asserted row names its control and states what makes the pair discriminating; every row whose
   verdict cannot be derived is RECORDED with its reason, and a mechanical self-assertion proves no
   repo-side row is asserted.
9. Every axis floor and count is an exact equality re-derived from the alphabets, and the four earlier
   axes, the byte floors and the stripper's three protections are untouched.
10. The `421` correction is recorded with `deferred-items.md:2172` quoted BESIDE and `policy.rs:7450`
    named as `19-29`'s, and `mod.rs:165-168` is put explicitly in scope for `19-29` with its reason.
11. `T-19-17r` is OUTSTANDING for the eleventh time, no `AR-` row is added or un-accepted, and
    `AR-19-04`'s reasoning gap is recorded rather than acted on.
12. The SUMMARY states FIRST that this plan closes nothing, that `T-19-86`, `T-19-91`, `T-19-111` and
    `T-19-112` remain open at `high`, and that `/gsd-secure-phase 19` is NOT cleared — and no unqualified
    "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-28-SUMMARY.md` when done.
It must contain: the statement FIRST that this plan closes nothing, that `T-19-86`, `T-19-91`,
`T-19-111` and `T-19-112` remain open at `high`, and that `/gsd-secure-phase 19` is NOT cleared; the
confirmation that `git diff --numstat 0092009..ca30c90 -- src/ tests/` was empty; the three commit SHAs
in order with `git show --stat` and the proof that each carries zero `src/` hunks; **the COMPLETE RED
name list verbatim and the COMPLETE PERMITTED list, which together are `19-29`'s handoff contract**; the
`--no-fail-fast` `passed + failed` totals before and after with the arithmetic stated and checked against
`git show`; the per-binary counts for all SEVENTEEN `envelope_*` binaries with every one confirmed to
have RUN; **the fifteen-carrier disposition table with a control letter for every one, including
`C-10`'s**; `T-19-116`'s end-to-end drive with both remote SHAs, every control and the separately
measured layer-2 answer; `T-19-118`'s drive with its control, the six write spellings and the harm
recorded as present/absent; **the EMPTY-`credential.helper` candidate control's measurement in both
effect and cost, RECORDED and asserted nowhere, with the explicit note that `19-29` takes it only if it
reproduced**; `T-19-115`'s seven spellings in both word positions with their absolute-literal twins and
the `bash` confirmations; `T-19-117`'s latency curve with the forge/non-forge split, the largest level
against `GUARD_TIMEOUT_SECS = 5`, the derived bound with its discriminating control, and **the
behavioural half stated UNMEASURED**; **the direction-(i) answer with its three source-read mechanical
facts, its stated cost, and the SEGMENT-COUNT pin measurements**; the two ordering pins at their
identifiers; the axis arithmetic with all eleven classes, every re-derived floor and the redirection
alphabet's arm change with its reason; the three fences with the new one named; the rows RECORDED and
never asserted, with the mechanical self-assertion that proves it; the mechanism pins and byte floors
re-measured; the `421` correction with `:2172` quoted beside and `policy.rs:7450` named as `19-29`'s;
`mod.rs:165-168` named in scope for `19-29`; `AR-19-04` and `AR-19-05` carried forward NOT un-accepted;
`C-08`, `C-15`, `T-19-110` and the `glab --host` cell carried forward unchanged; the `T-19-17r`
bookkeeping gap OUTSTANDING with the explicit statement that this plan did NOT accept it and added no
Accepted-Risks-Log row; any documented flake that fired recorded with its output and not fixed; and a
"what remains uncovered" section naming `T-19-86` FIRST — open at `high` by user scoping decision —
followed by `T-19-91`, `T-19-111` and `T-19-112` (open at `high`), `T-19-113`, `T-19-115` … `T-19-118`
(all open at this plan's end), `T-19-96`, `T-19-110`, `T-19-74`, `T-19-84`, `T-19-85` and
`T-19-61`…`T-19-73`, with the explicit statement that only the WRAPPER-OPERAND sub-class of `T-19-60` is
closed.
</output>
