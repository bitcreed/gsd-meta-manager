---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 16
type: execute
wave: 6
gap_closure: true
gap_ids: [T-19-95, T-19-92, T-19-93, T-19-94]
depends_on: [19-15]
files_modified:
  - tests/envelope_literal_decision.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-05, SAFE-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE. That ordering is the whole point of this plan existing separately from `19-17`.** Four consecutive rounds have certified a control with a corpus structurally incapable of failing on the class the round certified — `T-19-76`, then `T-19-83`, then `T-19-89`, now `T-19-95` — and every one of the first three was found by the NEXT audit rather than by the round's own evidence. Audit 4 verified by reading every entry of `ASSIGNMENT_PREFIXES`, `WRAPPERS`, `REFUSED_BASES`, `DECOY_OPERANDS`, `EXPANSION_WRAPPERS`, `SHELL_LAYERS` and `SEVERED_PREFIXES` that **not one entry anywhere contains a comma inside braces, and not one contains a `*`, a `?` or a `[`**. This plan writes the corpus and the reproducers and STOPS. `19-17` writes the rule."
    - "**If the widened corpus does not go RED against the pre-fix tree, that is a FINDING — stop and report it, do not proceed.** A row that is already refused is a row that certifies nothing about the class this round is about, and a property that is green before the fix is a property that could not have failed on it. `19-15` hit exactly this and handled it correctly: the plan's spelling of its bare-brace row (`C=GIT_CONFIG_COUNT; env -u ${C} git fetch origin`) turned out to be already refused for another reason, and the executor substituted the binding-free spelling and disclosed it rather than shipping a control that could not fail. Every row in this plan is measured against the built binary BEFORE it is written as an assertion, and any row that does not reproduce is reported with its measured verdict instead of being asserted anyway."
    - "**Every claimed bypass is confirmed under `bash` against argv-printing shims, and a row the shell does not actually run is DISCARDED rather than pinned.** This is audit 4's own method and it is not optional: audit 4 discarded three candidate rows on this step — `{ git,push } --force origin main` is a bash syntax error, `sh -c 'gh {pr,create} --title x'` does not brace-expand under `dash`, and `gh -R o/r api repos/o/r/pulls -f title=x` is rejected by the real `gh` with `unknown shorthand flag: 'R'`. A guard permit is a finding only when the shell really runs the dangerous command. Rows that are class coverage rather than measured bypasses — the tilde spellings, and the `IFS` spellings that Rule A already refuses — are LABELLED as such in the test file and in the SUMMARY, so a later reader cannot mistake a coverage row for a reproducer."
    - "**The measurement harness is audit 4's, unchanged: the built binary, a FRESH envelope root per row, and the envelope directory WALKED afterwards.** `tests/envelope_expansion_slots.rs` already carries that harness — `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, one `TempDir` per row — and this plan's new file carries it forward rather than inventing a second one. The walk is proved non-blind on every pass by a positive control: a permitted `gh pr create --title x` leaves a ledger line in the walk, so an EMPTY walk beside a refusal means something."
    - "**`T-19-93`'s bar is COUNT, not refuse, and the assertion that proves it is a LEDGER LINE — twice.** `gh api repos/{owner}/{repo}/pulls -f title=x` is `gh`'s own documented placeholder syntax and bash passes those braces through unchanged (`printf \"[%s]\" repos/{owner}/{repo}/pulls` prints them verbatim; there is no comma, so there is no brace expansion). The row is a FALSE NEGATIVE on legitimate input: today it exits 0 with an EMPTY walk while the quoted spelling exits 0 WITH a ledger line. The rows written here therefore assert that after `19-17` the unquoted placeholder form is PERMITTED **and** leaves a ledger line, and that a SECOND placeholder-form creation in the same envelope root is refused under `pr_cap_exceeded` — which is the one assertion an implementation cannot satisfy by refusing."
    - "**A non-vacuity floor per axis, and the axes are SEVEN classes rather than one — because 'a brace expansion' is the granularity that let two live cells through in two consecutive plan-check rounds.** `EXPANSION_METACHARACTERS` is exactly `['$', '`', '{', '(']` and every entry satisfying today's floor satisfies it through an expansion MARKER. Its successor covers the constructs that make a word UNREADABLE, and the floor is asserted per alphabet AND per class: (i) a WHOLE-WORD splice — a brace expansion occupying its own word, (ii) a **CONCATENATED splice** — a comma-bearing brace expansion with a non-empty literal prefix or suffix in the SAME word, (iii) a **RANGE** — `..` inside braces, in both its whole-word and concatenated spellings, and including the increment form `{a..b..n}`, (iv) a **MULTI-EXPANSION word** — TWO OR MORE brace expansions inside one whitespace-delimited word, which is the class a per-`{` product computation walks around, (v) a LITERAL brace pair — braces with no comma and no range, which bash passes through unchanged and which `T-19-93` requires to keep working, (vi) a pathname-expansion character — `*`, `?` or `[`, and (vii) a tilde. **The predicates must be written so that `{a,b}` cannot satisfy (ii), (iii) or (iv)**: (ii) requires a non-empty literal run adjacent to the braces inside the same word, (iii) requires a `..` between the braces, and (iv) requires the count of top-level `{`…`}` expansion pairs within a single whitespace-delimited word to be at least two. A single 'brace expansion' class is what let `{g..g}it` through, and a class set without (iv) is what let `{g..g}{i..i}t` through one round later — a floor a single-brace entry satisfies is a floor the live cell walks around. Counted floors over the generated cases stand beside the per-alphabet floors, counted while generating rather than inferred from alphabet sizes."
    - "**A metacharacter entry goes ONLY into an alphabet whose property asserts REFUSAL, unless its verdict is genuinely invariant — and getting this wrong turns the permitted corpus red for the wrong reason.** `WRAPPERS`, `ASSIGNMENT_PREFIXES` and `SHELL_LAYERS` feed properties that assert the wrapped verdict EQUALS the unwrapped one, and they are shared with `PERMITTED_BASES`. A brace expansion spliced into a permitted governed command is refused after `19-17` — `FOO={a,b} git status` is a pinned COST row — so putting one in `ASSIGNMENT_PREFIXES` would make the permitted corpus stricter than its base and break invariance in the direction the file already records for the `GIT_CONFIG_COUNT=0` prefix. The refusal-asserting alphabets are `REFUSED_BASES` (invariant because the base is refused both wrapped and unwrapped), `DECOY_OPERANDS`, `EXPANSION_WRAPPERS`, `SEVERED_PREFIXES` and the forge-slot spellings. The invariance-preserving alphabets get the LITERAL-brace and operand-glob entries instead, and that split is what makes the corpus able to tell a brace expansion from a brace pair rather than merely able to draw a `{`."
    - "**The rule this corpus is written against asks what a splice can PRODUCE, never what its alternatives are CALLED, and every row here is written to that principle.** A brace expansion CONCATENATED with literal characters produces a governed program that no alternative spells: `{g..g}it push --force origin main` and `g{i,i}t push --force origin main` both run `git push --force origin main` under bash and are measured at exit 0 today, while no segment resolves `Governed` — the head word is `it` — and no comma-separated alternative names anything. A corpus of WHOLE-WORD splices cannot fail on either, and a rule keyed on alternative NAMES cannot close either. **This is the fifth occurrence of the defect the round exists to end, caught in plan-check rather than by the next audit**, and both the alphabets and the enumerated rows are widened for it: a concatenated splice and a `..` range are their own classes, with their own floors, not instances of 'a brace expansion'."
    - "**The cells found while PLANNING this round are measured and pinned at whatever verdict they actually have, and the ones measured permitted are REGISTERED rather than closed.** Eleven were probed: `{git,push,--force,origin,main}` — the splice at position 0, where nothing resolves `Governed` at all; **`{g..g}it push --force origin main` — a RANGE concatenated with a literal suffix**; **`g{i,i}t push --force origin main` — alternatives concatenated on BOTH sides**, neither of which any alternative-name test reaches; **`{g..g}{i..i}t push --force origin main` and `{g,g}{i,i}{t,t} push --force origin main` — TWO and THREE expansions in one word**, which no PER-`{` product computation reaches because bash composes across `{`s; **`{g..g..1}it push --force origin main` — an INCREMENT range**, which is neither a comma list nor a two-endpoint range and which no enumerability trigger written for the other spellings catches; `git push {--force,origin} main` — the splice into a governed command's FLAG slot, where the verb is readable and the flags are not; `git commit -m {a,b}` — the splice into an OPERAND, which is the cost side of the same rule; `git config core.hooksPat? /tmp/x` and `gh api repos/o/r/pul?s -f title=x` — the glob in the two decision operands `19-14` closed for `$`; and `git push --forc? origin refs/heads/gsd-auto/alpha/w` — a glob in a PUSH FLAG, which is one slot outside the decision region `19-14` defined and which no rule in this round reaches. The first ten are inside this round's declared scope. **The eleventh is not, and it is MEASURED AND REGISTERED, never fixed** — a plan cannot both discover a threat and be the plan that measured it fail first, which is the discipline `19-14` established for `T-19-91`."
    - "**Nothing `19-14` or `19-15` settled is re-opened, and the mechanism pins say so mechanically rather than in prose.** The six severed-prefix rows and the two carry-forward rows stay refused under `envelope_assertion_failed`, and — because `19-17` changes the tokenizer — this plan additionally pins the MECHANISM: `policy::split_segments_with_heads` must still report `head_is_command_position == false` for the last segment of `C=GIT_CONFIG; env -u ${C}_COUNT git fetch origin`. A verdict pin alone would stay green if `19-17` silently made Rule B dead code, and a control nobody can tell has stopped being load-bearing is the failure mode this whole phase is about."
    - "**This plan's gate names its expected failures rather than requiring zero, and it is the handoff contract `19-17` gates against.** `19-15-SUMMARY.md` records `1533 passed / 0 failed / 13 ignored`, `passed + failed = 1533` in both flake modes, and audit 4 independently re-measured that number. After this plan `passed + failed` is strictly greater than 1533; every failure name is either one of the two flaky `driver_reattach` names or one of the RED test names this plan creates; the RED set is non-empty and every one of its names is listed in the SUMMARY; and `--test envelope_expansion_slots` and `--test envelope_command_position` are FULLY GREEN, because the widening must not disturb round-4's evidence."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-17`, or by the two together.** `T-19-86` remains OPEN at `high` by explicit user scoping decision and its four rows must keep exiting 0; `T-19-91` remains OPEN at `high` and this plan widens its RECORD rather than its remedy; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. The SUMMARY must say so first rather than let a red-by-design suite or a later green one imply otherwise."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-17` is the rule. Every commit here must show ZERO `src/` hunks under `git show --stat`, which is the same discipline `19-15`'s RED commit met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured. If a row audit 4 recorded at exit 0 does not reproduce, report it as a finding with the measured verdict and stop — do not assert the audit's number over your own measurement, and do not quietly drop the row. If a row is refused for a DIFFERENT reason identifier than the class expects, pin it at its true identifier in a test of its own and disclose the interaction, exactly as `19-15` did for the bare-brace spelling."
      category: honesty
    - statement: "MUST NOT pin a bypass row that `bash` does not actually run. Confirm every claimed permit under `bash` with shims named `git`, `gh` and `glab` first on `PATH` printing their own argv, and DISCARD any row the shell does not assemble into the dangerous command — audit 4 discarded three that way and recorded them so audit 5 would not spend the measurement again. Rows that are class coverage rather than measured bypasses (the tilde spellings; the `IFS` spellings Rule A already refuses) MUST be labelled as coverage in the test file and in the SUMMARY."
      category: honesty
    - statement: "MUST NOT add a brace-expansion, glob or tilde entry to `WRAPPERS`, `ASSIGNMENT_PREFIXES`, `SHELL_LAYERS` or `PERMITTED_BASES` unless its verdict is genuinely INVARIANT under wrapping. Those alphabets feed properties that compare the wrapped verdict to the unwrapped one and are shared with the permitted corpus; an entry that is refused on its own account is STRICTER than its base and breaks invariance in the direction this file already records for the `GIT_CONFIG_COUNT=0` prefix. Entries whose verdict is a refusal belong in `REFUSED_BASES`, `DECOY_OPERANDS`, `EXPANSION_WRAPPERS`, `SEVERED_PREFIXES` or the forge-slot spellings, each of which asserts refusal already."
      category: safety
    - statement: "MUST NOT widen an alphabet with brace-EXPANSION entries alone. Each widened alphabet must also be able to draw a LITERAL brace pair — braces containing no comma and no range, which bash passes through unchanged — because a corpus that can only draw `{a,b}` cannot fail on the difference between a brace expansion and a brace pair, and that difference IS `T-19-93`. The same applies to the glob axis: a decision-slot glob (refused) and an operand glob (permitted) must both be drawable."
      category: safety
    - statement: "MUST NOT satisfy the splice floors with WHOLE-WORD or SINGLE-BRACE entries. `{a,b}` standing as its own word must not satisfy the concatenated-splice class, the range class or the multi-expansion class: the concatenated predicate requires a non-empty literal run adjacent to the braces inside the same word, the range predicate requires a `..` between them, and the multi-expansion predicate requires at least TWO top-level `{`…`}` expansion pairs inside one whitespace-delimited word. A corpus of whole-word splices cannot fail on `{g..g}it push --force origin main`; a corpus of single-brace entries cannot fail on `{g..g}{i..i}t push --force origin main`; both are measured at exit 0 today, and shipping either would be another round of a corpus incapable of failing on the class that got through it."
      category: safety
    - statement: "MUST NOT pin a POST-fix expectation that cannot be DERIVED from the rule's clauses, and MUST write the derivation beside every row that carries one. The derivation names which clause produces the verdict: clause 1 (a named decision word is not literal), clause 2a (a segment of a brace-spliced simple command resolves `Governed` or `NestedPayload`), or clause 2b (a word the splice can PRODUCE has a governed basename). This plan measures PRE-fix, so its measure-first discipline cannot catch a wrong post-fix expectation — a row pinned at a refusal the rule cannot produce lands RED and `19-17` is forbidden to edit it, which stalls the round. **A row whose post-fix verdict cannot be derived is measured pre-fix, recorded, and left for `19-17` to measure and pin.**"
      category: honesty
    - statement: "MUST NOT pin a row as a NEW cost without first measuring that it is permitted TODAY. A shape already refused for another reason — Rule B reaches several brace spellings through `}`'s word-splitting flush — is green before and after, which is a control that cannot fail on its class. Any such row is pinned at its true identifier in a test named for the interaction, exactly as `19-15` did with `the_severed_spelling_that_binds_a_whole_key_is_already_refused_for_another_reason`, and is reported as an interaction rather than a cost."
      category: honesty
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet entry or property in `tests/envelope_wrapper_class.rs`. `MIN_REFUSED_CASES`, `MIN_DISTINCT_CHAINS`, `MIN_DISTINCT_REASONS`, `MIN_DECOY_OPERANDS`, `MIN_DECOY_CASES`, `MIN_DISTINCT_DECOY_CHAINS`, `MIN_EXPANSION_WRAPPERS`, `MIN_EXPANSION_WRAPPER_CASES`, `MIN_DISTINCT_EXPANSION_CHAINS`, `MIN_LIVE_EXPANSION_CASES`, `MIN_FORGE_SLOT_CASES`, `MIN_SEVERED_PREFIXES`, `MIN_SEVERED_CASES` and `MIN_DISTINCT_SEVERED_CHAINS` keep their current values or rise. A case that stops being drawn is a case that stops being able to fail, and this file's own section header says so."
      category: scope
    - statement: "MUST NOT modify `tests/envelope_expansion_slots.rs` or `tests/envelope_command_position.rs`. Round-4's evidence stays attributable to the round that produced it, which is the reason `19-14` created a third test file rather than appending to the second; this plan creates a FOURTH for the same reason. The mechanism pin for Rule B is written in the new file, not added to `19-15`'s."
      category: scope
    - statement: "MUST NOT fix, close, narrow or re-scope `T-19-86`. `the_t_19_86_residual_is_permitted_today_and_this_plan_leaves_it_permitted` stays green UNMODIFIED with all four rows exiting 0, and `T-19-86` is named FIRST in the SUMMARY's 'what remains uncovered' section."
      category: scope
    - statement: "MUST NOT close or re-scope `T-19-91`. This plan MEASURES `git push $REF` in a repository whose current branch is inside the envelope's namespace and records the verdict; the code-side record correction is `19-17`'s. Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push`, do not extend the denylist, and do not delete or weaken `the_t_19_91_residual_is_measured_and_pinned_rather_than_closed`."
      category: scope
    - statement: "MUST NOT fix the glob-in-a-push-flag cell (`git push --forc? origin refs/heads/gsd-auto/alpha/w`) or any other cell found while planning that is outside the declared scope. Measure it, pin it at its measured verdict, register it, and stop. A plan cannot both discover a threat and be the plan that measured it fail first — the discipline `19-14` established for `T-19-91` and `19-13` for `T-19-86`."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85`. Do not touch `cred.rs`, `advisory.rs`, `scan.rs`, `config.rs`, the honesty statement, the second-carrier table or the park-coverage surface."
      category: scope
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` — and every parser in this module is hand-rolled against `std` for exactly that reason."
      category: scope
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised forge command wrote nothing must use a FRESH envelope root of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line. A row that read one expected path would pass identically if the line were written somewhere else."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against 1533, the number `19-15-SUMMARY.md` records and audit 4 re-measured. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing (D-34)."
      category: honesty
    - statement: "MUST NOT fix, edit or work around the two `tests/driver_reattach.rs` failures, and MUST NOT treat their absence from a run as a regression. They are pre-existing, flaky, documented in `deferred-items.md`, and out of scope."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or the subsections plans 19-13, 19-14 and 19-15 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-16 execution record. Re-measuring and re-classifying these rows is `/gsd-secure-phase 19`'s job; an audit's own tables are its provenance and a plan writing into them forges it."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`."
      category: honesty
  artifacts:
    - path: "tests/envelope_literal_decision.rs"
      provides: "the fourth evidence file — audit 4's `T-19-92`, `T-19-93` and `T-19-94` reproducers measured against the built binary and confirmed under bash shims, the five in-scope cells found while planning, the `T-19-93` counted rows asserting a LEDGER LINE and a second-creation `pr_cap_exceeded`, the newly-refused cost rows each beside its permitted twin, the must-not-move controls including the grouping rows and `git reflog delete HEAD@{0}`, the Rule B MECHANISM pin over `split_segments_with_heads`, the `T-19-91` in-namespace `git push $REF` measurement, and the registered-only push-flag glob row"
      contains: "T-19-92"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "`EXPANSION_METACHARACTERS`' successor covering brace expansions, literal brace pairs, globs and tildes; brace-expansion and glob entries in `REFUSED_BASES`, `DECOY_OPERANDS`, `EXPANSION_WRAPPERS` and `SEVERED_PREFIXES`; literal-brace and operand-glob entries in the invariance-preserving alphabets; new forge-slot spellings for the splice and the glob; and a per-alphabet AND per-class non-vacuity floor with counted live-case floors beside it"
      contains: "T-19-95"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-16 execution record rather than an audit finding, carrying every measured verdict, the bash-shim confirmations, any audit row that failed to reproduce, the cells found while planning with the one registered open, the `T-19-91` in-namespace measurement, and the statement that this plan closes nothing and clears no gate"
      contains: "19-16"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "the registered-only push-flag glob cell in the shape the `T-19-86` and `T-19-91` entries already use, with the `T-19-92` … `T-19-95` entries updated only to record that their corpus exists and is RED"
      contains: "registered"
  key_links:
    - from: "audit 4's measured bypass rows"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary with a fresh root per row and a bash-shim confirmation, BEFORE any assertion is written and BEFORE any production line moves"
      pattern: "RED"
    - from: "the four classes that make a word unreadable"
      to: "seven alphabets that can each draw one"
      via: "a per-alphabet AND per-class floor, with the refusal-asserting and invariance-preserving alphabets widened differently so the corpus can tell a brace EXPANSION from a brace PAIR"
      pattern: "floor"
    - from: "`gh api repos/{owner}/{repo}/pulls -f title=x`"
      to: "a LEDGER LINE, and a second creation refused under `pr_cap_exceeded`"
      via: "an assertion no implementation can satisfy by refusing, because the bar for `T-19-93` is COUNT rather than refuse"
      pattern: "pr_cap_exceeded"
    - from: "Rule B's severed-prefix mechanism"
      to: "a pin `19-17` cannot make dead code silently"
      via: "`split_segments_with_heads` asserted to still report `head_is_command_position == false`, not merely the exit code the verdict pins"
      pattern: "head_is_command_position"
---

<objective>
**The corpus, first — and it must go RED before a single line of the rule is written.**

Audit 4 (`19-SECURITY.md`, the authority) verified that plans 19-14 and 19-15 genuinely closed
`T-19-87`, `T-19-88`, `T-19-89` and `T-19-90`, each re-measured against the built binary. It then
found four more, and one of them is the reason this plan exists separately from `19-17`:

> `T-19-95` — `EXPANSION_METACHARACTERS` is exactly `['$', '`', '{', '(']`, and **not one entry of
> any alphabet in `tests/envelope_wrapper_class.rs` contains a comma inside braces, a `*`, a `?` or
> a `[`.** The corpus is structurally incapable of generating, and so of failing on, `T-19-92` and
> `T-19-94`. This is `T-19-76`'s failure mode for the **fourth consecutive round**, after `T-19-83`
> and `T-19-89`, and the first three were each found by the NEXT audit rather than by the round's
> own evidence.

So: the corpus is widened here, observed failing here, and committed here. `19-17` writes the rule
and is gated on this plan's recorded RED state. **If the widened corpus does not go red against the
pre-fix tree, that is a finding — stop and report it.**

## What must be RED at this plan's end

Measured by audit 4 at `b72237e`, fresh root per row, walk after, every one confirmed under `bash`
against argv-printing `git`/`gh`/`glab` shims:

```
exit=0  git {-c,core.hooksPath=/dev/null,push,--force,origin,main}   <- T-19-92, ALL THREE LAYERS
exit=0  git {push,--force} origin main                               <- T-19-92
exit=0  git {update-ref,-d,refs/heads/main}                          <- T-19-92, no second carrier
exit=0  git {config,core.hooksPath,/tmp/x}                           <- T-19-92
exit=0  gh {pr,create} --title x                    (empty walk)     <- T-19-92, SAFE-06 uncounted
exit=0  gh {api,repos/o/r/pulls} -f title=x         (empty walk)     <- T-19-92, SAFE-06 uncounted
exit=0  glab {mr,create} --title x                                   <- T-19-92
exit=0  bash -lc "git {push,--force} origin main"                    <- T-19-92, nested payload
exit=0  gh api repos/{owner}/{repo}/pulls -f title=x (empty walk)    <- T-19-93, MUST BECOME COUNTED
exit=0  git pus? --force origin main                                 <- T-19-94
exit=0  git ?ush --force origin main                                 <- T-19-94
exit=0  git stas?                                                    <- T-19-94, no second carrier
exit=0  gh p? create --title x                      (empty walk)     <- T-19-94
exit=0  touch push && git pus? --force origin main                   <- T-19-94, precondition in one call
```

`T-19-93` is the odd one and its row is written differently on purpose. It is a **false negative on
legitimate input**: `{owner}`/`{repo}` is `gh`'s own documented placeholder syntax, bash passes those
braces through unchanged because there is no comma in them, and the QUOTED spelling
`gh api "repos/{owner}/{repo}/pulls" -f title=x` already exits 0 **with** a ledger line. The bar is
therefore **count, not refuse**, and the assertion is a ledger line plus a second creation refused
under `pr_cap_exceeded` — the one thing an implementation cannot satisfy by refusing.

## The cells found while PLANNING this round

Same provenance caveat `19-14` established: these were found while planning, not by an audit. Ten
are inside the declared scope and are measured and written here; the eleventh is measured, pinned at
its verdict, **registered and not fixed**.

**The first six are one class and they set the rule's shape.** A splice CONCATENATED with literal
characters, or COMPOSED with another splice in the same word, produces a governed program that no
alternative spells. So the operative test is what a word can **produce** — the cartesian product of
its literal runs with every expansion in it, composed **over the whole word and never per-`{`**,
because bash composes across `{`s — and never what its alternatives are *called*.

| Cell | Line | Why the declared class does not reach it as written |
|---|---|---|
| the splice at position 0 | `{git,push,--force,origin,main}` | nothing resolves `Governed` at all — the one segment is the single word `git,push,--force,origin,main`, whose basename is not a governed program, so a rule keyed on the resolution alone permits it |
| a RANGE with a literal suffix | `{g..g}it push --force origin main` | measured **exit 0**; bash runs `git push --force origin main`. The head word is `it`, so nothing resolves `Governed`, and there is no comma-separated alternative at all. Only the PRODUCT `g` + `it` = `git` reaches it |
| alternatives concatenated on both sides | `g{i,i}t push --force origin main` | measured **exit 0**; the alternatives are `i`, which names nothing. The composed word never exists as a token, so the word-level literalness bit cannot see it either |
| TWO expansions in one word | `{g..g}{i..i}t push --force origin main` | measured **exit 0**. Bash composes ACROSS `{`s. A product computed per-`{` answers `g` for the first (its suffix run ends at the following `{`) and `it` for the second (its prefix is empty, the intervening `}` having flushed the word) — neither governed, both sets enumerating cleanly. Only a product composed over the WHOLE WORD reaches it |
| three expansions, comma spelling | `{g,g}{i,i}{t,t} push --force origin main` | measured **exit 0**; the same mechanism with commas rather than ranges, so a corpus that widened only the range class would still miss it |
| an INCREMENT range | `{g..g..1}it push --force origin main` | measured **exit 0**; `{a..b..n}` is neither a comma list nor a two-endpoint range, so it falls outside "each member of a `..` range" and outside every enumerability trigger stated for the other spellings. It is named UNENUMERABLE by `19-17`'s rule |
| the splice into a FLAG slot | `git push {--force,origin} main` | the verb `push` is readable and literal, so a rule about decision WORDS is satisfied; what the splice hides is `--force`, which `classify_push` never sees |
| the splice into an OPERAND | `git commit -m {a,b}` | the cost side of the same rule — the argv the classifier reads is not the argv that runs, so the whole simple command is unresolvable and this is refused. Pinned as a COST with its permitted twin |
| the glob in `config`'s key operand | `git config core.hooksPat? /tmp/x` | `19-14` closed this operand for `$`; a glob sets no expansion bit, so the same `Allow` arm is reached on an operand `classify_config` cannot read |
| the glob in the `api` endpoint | `gh api repos/o/r/pul?s -f title=x` | `endpoint_is_pulls` compares the last path segment; a glob makes it unreadable and the pull request opens uncounted |
| **a glob in a PUSH FLAG** | `git push --forc? origin refs/heads/gsd-auto/alpha/w` | **OUTSIDE this round's scope (the eleventh).** `19-14`'s git decision region is the VERB plus `config`'s key operand; `classify_push`'s flags are a further arm, exactly as `classify_reflog`'s and `classify_symbolic_ref`'s operands are (`T-19-91`). MEASURED and REGISTERED here, never fixed |

## What this plan is NOT

It closes nothing. `T-19-92`, `T-19-93`, `T-19-94` and `T-19-95` stay open at this plan's end;
`T-19-95` is closed only when `19-17`'s rule is certified by the corpus written here, because a
corpus is evidence about a control and there is no control yet.

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-17`, or by the two together.**
`T-19-86` remains open at `high` by explicit user scoping decision and `T-19-91` remains open at
`high`. The SUMMARY must say so first.

Purpose: make the corpus capable of failing on the class round 5 is about, and prove it does.
Output: a fourth evidence file, seven widened alphabets, two committed RED states, one appended
execution record, and an exact handoff number for `19-17` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-15-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@tests/envelope_expansion_slots.rs
@tests/envelope_wrapper_class.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE, then RED — the audit-4 reproducers, the planning cells, and the counted-not-refused rows</name>
  <files>tests/envelope_literal_decision.rs</files>
  <precondition>
    Work in the main checkout at `d4befaa` or later with a clean tree — ISOLATION=none is forced for
    this phase, so do NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`,
    which is what every measurement is driven against. A `bash` with a writable directory first on
    `PATH` is available for the shim confirmations. The two `tests/driver_reattach.rs` failures are
    pre-existing and FLAKY.
  </precondition>
  <read_first>
    `tests/envelope_expansion_slots.rs:93-260` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, and the
    one-`TempDir`-per-row discipline. `19-SECURITY.md`'s sections "Threats found by audit 4" and
    "Audit 4 method", for the measured rows and the harness that produced them.
    `src/envelope/policy.rs:1244-1372` (`tokenize` and its separator arm), `:1206-1241`
    (`split_segments_with_heads`), `:2461-2532` (`scan_gh_api`, `gh_api_posts_a_pull_request`,
    `endpoint_is_pulls`) and `:2168-2261` (`expansion_in_decision_region`) — so every row is written
    against what the splitter and the scans actually do rather than against a description of them.
  </read_first>
  <action>
**Measure before you assert. Nothing in this file is written until it has been driven.**

Build, then drive every row below exactly as audit 4 did:

```
printf '{"tool_name":"Bash","tool_input":{"command":"<CMD>"}}' \
  | GSD_MM_ENVELOPE_ROOT=$(mktemp -d) ./target/debug/gsd-meta-manager envelope guard alpha
```

one fresh root per row, `find` over the root afterwards. Then confirm every claimed permit under
`bash` with shims named `git`, `gh` and `glab` first on `PATH` printing their own argv, and record
the printed argv. **A row the shell does not assemble into the dangerous command is DISCARDED and
recorded as discarded** — audit 4 discarded three that way, and one of them (`sh -c 'gh {pr,create}'`
not brace-expanding under `dash`) is directly adjacent to rows written here.

Any audit-4 row that does not reproduce at the recorded verdict: STOP, record the measured verdict,
and report it as a finding rather than asserting the audit's number over your own measurement.

Then create `tests/envelope_literal_decision.rs`. Its header states what the file is: audit 4's
`T-19-92`/`T-19-93`/`T-19-94` reproducers plus the cells found while planning round 5, measured
against the built binary BEFORE the rule was written so the corpus cannot have been shaped to agree
with the code, and written as a FOURTH file rather than appended to `tests/envelope_expansion_slots.rs`
so round-4 evidence and round-5 evidence stay attributable to the round that produced them. It also
states that the whole file except its controls is RED at this plan's end, by design, and that `19-17`
closes it.

Carry the harness forward from `tests/envelope_expansion_slots.rs` unchanged in shape, including the
walk. Print the walk for at least one refused forge row and one COUNTED row so the SUMMARY records
listings that were measured.

**Rows that must be REFUSED after `19-17`, each asserting exit code 2 AND
`policy::REASON_ENVELOPE_ASSERTION_FAILED`:**

  - **the brace-expansion class**, in a test whose doc records the mechanism in bash's own terms —
    `{` is the reserved word opening a group ONLY at a command position; inside a simple command it
    opens a brace expansion whose contents bash splices back into that same command, so the guard
    judges fragments the shell never runs: all eight `T-19-92` rows above, including the nested
    `bash -lc "…"` spelling whose braces are literal until the payload is re-split;
  - **the splice at position 0** — `{git,push,--force,origin,main}` — in a test of its own, whose
    comment records why it is a separate row: nothing in it resolves `Governed`, so a rule keyed on
    the resolution alone permits it and only a rule that also asks what the splice can PRODUCE
    reaches it;
  - **the CONCATENATED splice, the MULTI-EXPANSION word and the RANGE**, in a test of their own and
    named for the class. All five measured at exit 0 today and all five assembled by bash into a real
    force push: `{g..g}it push --force origin main`, `g{i,i}t push --force origin main`,
    **`{g..g}{i..i}t push --force origin main`**, **`{g,g}{i,i}{t,t} push --force origin main`** and
    **`{g..g..1}it push --force origin main`**. Their comment states the mechanism that makes them
    one class rather than five rows: the composed word never exists as a token, so no word-level bit
    can see it; the head word is `it`, so nothing resolves `Governed`; the alternatives are `g`, `i`
    and `t`, which name nothing — **only the PRODUCT of the word's literal runs with its expansions
    reaches them.** It states separately why rows 3–5 are not spelling variants of rows 1–2:
    **bash composes across `{`s**, so a product computed per-`{` answers `g` and `it` for
    `{g..g}{i..i}t` and permits it, and `{g..g..1}` is an INCREMENT range that is neither a comma
    list nor a two-endpoint range. Add the two spellings that vary where the literal characters sit —
    a prefix-only (`g{it,x} push --force origin main`) and a suffix-only
    (`{g,x}it push --force origin main`) — so the class is drawn rather than typed once each, and one
    forge twin (`{g,x}{h,h} pr create --title x`) with an empty-walk assertion;
  - **the splice into a FLAG slot** — `git push {--force,origin} main` — whose comment records that
    the verb is readable and literal here, so this row is not closed by any rule about decision
    words; what the splice hides is `--force`;
  - **the glob class** — the four `T-19-94` rows, plus the two-segment
    `touch push && git pus? --force origin main` whose comment records that `touch push` is
    `Ungoverned` and the segments are judged independently, so the precondition is satisfiable inside
    one tool call;
  - **the two glob decision-operand cells found while planning** — `git config core.hooksPat? /tmp/x`
    and `gh api repos/o/r/pul?s -f title=x` with an empty-walk assertion — whose comments record that
    these are the operands `19-14` closed for `$` reached by a character class that sets no expansion
    bit;
  - **the tilde and `IFS` coverage rows, LABELLED as coverage rather than as reproducers.**
    `git ~push --force origin main` is not a measured bypass — bash leaves `~push` alone when no such
    user exists — and `X=push,--force; IFS=,; git $X origin main` is ALREADY refused by Rule A
    because `$X` carries an expansion. Measure both, pin them at their measured verdicts, and state
    in the comment which is which. A coverage row asserted as a reproducer would be this phase's own
    failure mode inverted.

**The `T-19-93` rows, which assert COUNTED and not refused.** In their own test, whose doc states
that this row is a false negative on `gh`'s own documented syntax and that the correctness bar is a
ledger line rather than an exit code:

  - `gh api repos/{owner}/{repo}/pulls -f title=x` in a fresh root → PERMITTED **and the walk holds
    exactly one ledger line**. RED today: measured exit 0 with an EMPTY walk;
  - a SECOND `gh api repos/{owner}/{repo}/pulls -f title=x` in the SAME root → REFUSED under
    `policy::REASON_PR_CAP_EXCEEDED`. This is the assertion an implementation cannot satisfy by
    refusing the first one, and it is why the row is written as a pair;
  - the quoted control `gh api "repos/{owner}/{repo}/pulls" -f title=x` → PERMITTED with a ledger
    line, which passes TODAY and must keep passing — it is the positive control proving the walk and
    the cap are both live;
  - `gh api repos/{owner}/{repo}/pulls/7 -f body=x` → PERMITTED with NO ledger line, because a
    single pull request is not the collection (`endpoint_is_pulls`'s own documented boundary), so
    the placeholder tolerance cannot be implemented as "any endpoint containing braces counts".

**The newly-refused COST rows, each pinned beside its permitted twin AND beside the clause that
produces it**, in a test whose doc states that these assert a COST rather than a desirable behaviour
and that a later change reducing the cost must delete the rows deliberately. **Every post-fix
expectation here is DERIVED, and the derivation is written in the comment** — a plan that measures
pre-fix cannot catch a wrong post-fix expectation, and a row pinned at a refusal the rule cannot
produce lands RED with `19-17` forbidden to edit it:

```
refused after 19-17                    by clause   permitted twin, before and after
git commit -m {a,b}                    2a          git commit -m ab
git add {src,tests}/x.rs               2a          git add src/x.rs tests/x.rs
echo {git,x}                           2b          echo git
git config --get-regexp branch.*       1           git config --get-regexp 'branch.*'
git -c 'user.na*e=x' commit -m y       1 (textual) git -c user.name="$NAME" commit -m x
```

`echo {git,x}` is clause 2(b)'s OWN over-refusal and it belongs in the cost list rather than in the
threat list: the products are `git` and `x`, and `git`'s basename is governed, so an ungoverned
`echo` is refused. Its sibling **`ls {git,svn}-repo` is a PERMITTED control, not a cost** — the
products are `git-repo` and `svn-repo`, neither of whose basename is governed — and its comment
records that this pair is what distinguishes a rule reading PRODUCTS from one reading NAMES. A rule
that refused `ls {git,svn}-repo` would be testing for a mention, and the whole finding that reshaped
this round is that a mention test is one slot away from the class.

The `-c` pair's comment records why it is the odd one: the `-c` key half is guarded by a TEXTUAL
check inside `scan_leading`, which is a pure argv function and cannot see that the quoting made the
`*` literal, so a quoted key half carrying one of these characters is refused too. Git config keys
are `section.key` over alphanumerics, `.`, `-` and `_`, so no legal key can carry one — the
false-positive cost is disclosed and essentially zero, and it is pinned rather than argued.

**Two shapes are MEASURED AND CLASSIFIED rather than pinned as costs**, because their post-fix
verdict cannot be derived from the clauses with certainty and this plan measures pre-fix only:

  - `FOO={a,b} git status` — `}` severs the word `b`, so the segment `git status` may already be
    refused by **Rule B** (`head_is_command_position == false`) rather than by anything this round
    adds. Measure it. If it is already refused today, it is NOT a new cost: pin it at its true
    identifier in a test named for the interaction — the shape `19-15` established with
    `the_severed_spelling_that_binds_a_whole_key_is_already_refused_for_another_reason` — and report
    it as an interaction. If it is permitted today, derive its post-fix verdict and pin it as a cost
    with the twin `FOO=ab git status`;
  - `rg "git status" {src,tests}` — whether clause 2(a) reaches it depends on whether
    `resolve_program` answers `NestedPayload` for the quoted payload inside a marked command, which
    is a resolution this plan cannot state with certainty. **Measure it pre-fix, record the verdict,
    and pin NOTHING post-fix.** `19-17` measures it after the rule exists and pins it there with the
    clause that produced it. Its permitted sibling `rg "git status" src/` is pinned here as a
    must-not-move control, because that one is unambiguous.

**The must-not-move controls, green BEFORE this plan and green after `19-17`** — this is the half
that proves no character changed meaning, and the half a reader will check first:

```
permitted: { git status; }            permitted: echo {a,b}          permitted: ls *.rs
permitted: ( git status )             permitted: mkdir -p {src,tests} permitted: rg "x" src/*
permitted: (git status)&&git fetch origin  permitted: cp x{,.bak}     permitted: cd ~/projects
permitted: ls {git,svn}-repo          permitted: rg "git status" src/
permitted: git add src/*.rs           permitted: git log -1 HEAD@{0}  permitted: git reflog show HEAD@{0}
permitted: git commit -m "use ${HOME} here"        permitted: gh pr create --title 'fix $PATH handling'
permitted: git -c user.name="$NAME" commit -m x    permitted: git config user.email "$EMAIL"
permitted: echo $(git rev-parse HEAD)              permitted: ROOT=$(git rev-parse --show-toplevel)
permitted: gh api repos/o/r/pulls -f title="$T"    (with a ledger line)
permitted: env $X push --force origin main         permitted: X=git; env $X push --force origin main

refused  [force_push_blocked]        git push --force origin main
refused  [force_push_blocked]        { git push --force origin main; }
refused  [force_push_blocked]        ( git push --force origin main )
refused  [force_push_blocked]        git reflog delete HEAD@{0}
```

`git reflog delete HEAD@{0}` is the sharpest of these and its comment says why: `@{0}` is a
BRACE PAIR with no comma, which bash passes through unchanged. Today the tokenizer fragments it and
the guard refuses the fragment `git reflog delete HEAD@`; after `19-17` the word survives whole and
the guard refuses `reflog delete HEAD@{0}`. **The verdict must be identical either way**, and a
change that absorbed literal braces while LOSING a refusal would show up here and nowhere else.

**The Rule B mechanism pin, which a verdict pin cannot replace.** In a test of its own, assert over
`policy::split_segments_with_heads` — not over an exit code — that for
`C=GIT_CONFIG; env -u ${C}_COUNT git fetch origin` the LAST segment still reports
`head_is_command_position == false`, and the same for `K=GIT_SSH; env -u ${K}_COMMAND git fetch
origin` and `env -u ${C} git fetch origin`. Its comment records the reason: `19-17` changes the
tokenizer's treatment of `{`, and if the parameter-expansion case were folded into the literal-brace
case those lines would still be refused — by `resolve_program` step 5's prefix rule instead of by
Rule B — and every verdict pin in the suite would stay green while Rule B quietly became dead code.
This passes TODAY and must pass after.

**`T-19-91` — measure the in-namespace `git push $REF`, do not close it.** Audit 4 measured
`git push $REF` at **exit 0** in a repository whose current branch is under
`refs/heads/gsd-auto/alpha/`, contradicting the "already fails closed" recorded in `19-SECURITY.md`,
`19-14-SUMMARY.md` and `19-15-SUMMARY.md`. Reproduce it: build a fixture repo on
`gsd-auto/alpha/work` with an upstream configured, drive the guard with that repository as its
project root, and record the verdict. **Pin it only if the pin does not make the verdict depend on
the test process's own working directory** (`T-19-80`, and the reason `19-14` declined the pin);
if it does, record the measurement in the SUMMARY without a pin and state that reason. Either way
`git push origin $REF` stays pinned refused as `19-14` wrote it, and `T-19-91` is NOT closed.

**The registered-only cell.** Measure `git push --forc? origin refs/heads/gsd-auto/alpha/w` — a glob
in a PUSH FLAG, one slot outside `19-14`'s decision region — together with its literal control
`git push --force origin refs/heads/gsd-auto/alpha/w`. Pin whatever verdict it actually has in a
test named for the residual, with a comment recording that it is REGISTERED and not closed, that the
reason is round discipline rather than blast radius, and that `git push` does have `pre-push` behind
it while `git stash` and `git update-ref` do not. **Do not add a rule for it.**

**Anti-vacuity controls that pass TODAY**, so a red file cannot be red because the harness cannot
observe anything: `git push --force origin main` refuses; `git stash` refuses;
`env -u git git push --force origin main` refuses under `envelope_assertion_failed`;
`gh pr create --title x` PERMITS and leaves exactly one ledger line in the walk.

Run the file, capture the verbatim RED output with `rtk proxy` (the RTK hook strips `test result:`
lines, so an unproxied run reports nothing usable), record it and every measurement in the SUMMARY,
and commit this RED state on its own. **`git show --stat` for this commit must show zero `src/`
hunks.**
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_literal_decision 2>&amp;1 | tail -60; rtk proxy cargo test --test envelope_literal_decision &gt;/dev/null 2&gt;&amp;1; test $? -ne 0 &amp;&amp; echo "RED-CONFIRMED-LITERAL-DECISION"; rtk proxy cargo test --test envelope_expansion_slots &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_command_position &gt;/dev/null 2&gt;&amp;1 || exit 1; echo "ROUND-4-EVIDENCE-STILL-GREEN"</automated>
  </verify>
  <done>Every audit-4 row was driven against the built binary with a fresh root and a walk, and confirmed under bash shims, before any assertion was written; any row that failed to reproduce is reported as a finding with its measured verdict. `tests/envelope_literal_decision.rs` compiles against the unfixed tree; every brace-expansion row, the position-0 splice, **the concatenated-splice, multi-expansion and range rows — all five measured cells plus the prefix-only, suffix-only and forge spellings**, the flag-slot splice, every glob row, the two glob decision-operand cells, the `T-19-93` counted pair and the cost rows FAIL; **every cost row carries a written derivation naming the clause that produces its verdict (1, 2a or 2b), and no post-fix expectation is pinned without one**; `FOO={a,b} git status` and `rg "git status" {src,tests}` are measured and classified as the action specifies rather than pinned as costs; every must-not-move control including `ls {git,svn}-repo` and `rg "git status" src/`, the Rule B mechanism pin, the anti-vacuity controls, the `T-19-91` pins and the registered-only push-flag row PASS; `tests/envelope_expansion_slots.rs` and `tests/envelope_command_position.rs` are untouched and fully green; the walked listings for one refused forge row and one counted row are recorded; and the RED state is committed as its own commit with zero `src/` hunks.</done>
</task>

<task type="auto">
  <name>Task 2: RED — the alphabets, widened to the four classes that make a word unreadable</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs` in full, and in particular `ASSIGNMENT_PREFIXES:225`,
    `WRAPPERS:242`, `ShellLayer:281` with `inner_quotes` and `apply`, `SHELL_LAYERS:344`,
    `REFUSED_BASES:413` with its floors at `:451-457`, `PERMITTED_BASES:807`, `DECOY_OPERANDS:1215`,
    `EXPANSION_WRAPPERS:1275`, `EXPANSION_METACHARACTERS:1717` with
    `carries_a_metacharacter` / `carries_a_live_expansion` and the floor test at `:1741`,
    `ForgeSlot:2004` with `FORGE_SLOTS`, `DISPLACING_OPTS`, `expansion_spelling:2054` and
    `forge_slot_case:2063`, and `SEVERED_PREFIXES:2342` with `SEVERED_BASES` and its floors.
    `19-SECURITY.md` row `T-19-95` and the section "Audit 4 — what the round-3 controls can and
    cannot fail on", whose one-sentence version is the claim this task refutes.
  </read_first>
  <action>
Add to the same file, as its own clearly separated section, deleting and reordering nothing. Its doc
comment states audit 4's finding in the auditor's own terms and does not soften it: the alphabets
model `$`-shaped assembly and nothing else, so any control certified by them is certified against
`$`-shaped assembly and nothing else — the fourth consecutive round in which the corpus could not
draw the cell the next audit walked through. That sentence belongs in the file because it is the
reason the section must never be narrowed.

**1. The metacharacter constant's successor.** `EXPANSION_METACHARACTERS` names the characters that
MARK an expansion. Replace it with a successor naming the four classes that make a word
**unreadable**, with the discrimination the class needs rather than a flat character list:

  - a **WHOLE-WORD splice** — a `{`…`}` pair carrying a top-level comma and occupying its own word;
  - a **CONCATENATED splice** — the same, with a non-empty literal run before or after the braces in
    the SAME word. Bash joins that run to every alternative, which is how `g{i,i}t` produces `git`;
  - a **RANGE** — `..` between the braces, in both its whole-word and concatenated spellings and
    including the increment form `{a..b..n}`, which is how `{g..g}it` produces `git` with no
    alternative spelling anything;
  - a **MULTI-EXPANSION word** — two or more brace expansions inside ONE whitespace-delimited word,
    which is how `{g..g}{i..i}t` produces `git` while every per-`{` product is `g` or `it`;
  - a **LITERAL brace pair** — a `{`…`}` pair with neither. Bash passes it through byte-identically,
    which audit 4 measured (`printf "[%s]" repos/{owner}/{repo}/pulls` prints it unchanged) and
    which is exactly what `T-19-93` requires to keep working AND be counted;
  - a **pathname-expansion character** — `*`, `?` or `[`, whose result depends on the working
    directory and is therefore unknowable at guard time whether or not a file matches today;
  - a **tilde**, whose expansion depends on the passwd database.

Keep `carries_a_live_expansion` exactly as it is — it backs an existing counted floor — and add
sibling predicates for the new classes with the same single-quote discipline, so a class inside
single quotes is not counted as live.

**2. The widening, and WHICH alphabet gets WHICH class is the substance of this task.**

Alphabets whose property asserts **REFUSAL** get the unreadable entries:

  - `REFUSED_BASES` gains brace-expansion and glob bases: the splice in the verb slot
    (`git {push,--force} origin main`), the splice at position 0 (`{git,push,--force,origin,main}`),
    **the RANGE `{g..g}it push --force origin main`, the CONCATENATED splice
    `g{i,i}t push --force origin main`, the MULTI-EXPANSION words
    `{g..g}{i..i}t push --force origin main` and `{g,g}{i,i}{t,t} push --force origin main`, and the
    INCREMENT range `{g..g..1}it push --force origin main`** — the five cells plan-check measured at
    exit 0, which no whole-word splice, no alternative-name test and no PER-`{` product computation
    reaches — the splice in a flag slot
    (`git push {--force,origin} main`), the glob verb (`git pus? …`,
    `git stas?`) and the glob config key (`git config core.hooksPat? /tmp/x`). These are RED against
    the pre-fix tree at the property's own floor 2, which measures every base refused UNWRAPPED
    before the wrapping loop runs — the exact non-vacuity this widening is about. Keep FORGE bases
    out for the reason the file already records: the invariance property shares one envelope root
    and a permitted forge base exhausts the cap;
  - `REFUSED_BASES` also gains **`git reflog delete HEAD@{0}`**, which is refused BEFORE and AFTER.
    Its comment records that it is a control rather than a reproducer: it is the literal-brace case
    inside a refused command, and a change that absorbed literal braces while losing a refusal turns
    it red;
  - `DECOY_OPERANDS` gains a splice and a glob carrying a governed name — e.g. an operand whose
    braces expand to a governed program, and `env -u gi?` — each of which is refused on its own
    account;
  - `EXPANSION_WRAPPERS` gains brace-expansion and glob wrapper prefixes, at least one of them a
    CONCATENATED spelling (`env -u GIT_CONFIG{_COUNT,_COUNT}`) so the wrapper axis can draw the class
    too;
  - `SEVERED_PREFIXES` gains a splice spelling **and one concatenated-or-range spelling**, so Rule
    B's alphabet can draw the class that reaches the same seam through a different character.

Alphabets whose property asserts **INVARIANCE**, and which are shared with the permitted corpus,
get only entries whose verdict is genuinely invariant:

  - `ASSIGNMENT_PREFIXES` gains a LITERAL-brace value and a value whose glob is in a position that
    is nobody's decision word — never a brace expansion, because
    `FOO={a,b} git status` is a pinned COST row after `19-17` and would make the permitted corpus
    stricter than its base;
  - `WRAPPERS` gains a wrapper carrying a literal brace pair;
  - `PERMITTED_BASES` gains an operand-glob base (`git add src/*.rs`) and a literal-brace base
    (`git log -1 HEAD@{0}`), which are the permitted twins of the refused entries above and the only
    way the corpus can tell the two apart;
  - `SHELL_LAYERS` is NOT widened. It already carries `BraceGroup` and `Subshell`, and a
    brace-expansion layer would be a layer whose verdict is a refusal.

**3. The forge slots.** Extend `expansion_spelling` with a splice spelling and a glob spelling, and
extend `forge_slot_case` so every existing slot can carry them — first subcommand word, second
subcommand word, `api` endpoint, DISPLACED `api` endpoint, `api` method, and both `api` flag
positions. Add a slot for the **literal-brace endpoint** whose expected verdict is PERMITTED WITH A
LEDGER LINE rather than refused, so the forge property carries `T-19-93`'s counted case generatively
rather than only as an enumerated row. Its doc records that a property which could only draw the
refused half would certify the fix for `T-19-92` while remaining silent on `T-19-93`, which is the
same defect one class over.

**4. The floors, asserted BEFORE every loop, and SPLIT BY CLASS because one 'brace expansion' class
is what let the live cell through.** Extend the per-alphabet floor test to assert, for each of the
seven alphabets, that it can draw an entry of the class that alphabet is supposed to carry — and
separately assert **per class** that the corpus as a whole can draw a WHOLE-WORD splice, a
CONCATENATED splice, a RANGE, a **MULTI-EXPANSION word**, a literal brace pair, a glob character and
a tilde.

**The predicates must be written so `{a,b}` cannot satisfy the concatenated, range or
multi-expansion class**: the concatenated predicate requires a non-empty literal run adjacent to the
braces WITHIN the same whitespace-delimited word; the range predicate requires a `..` between the
braces; and the multi-expansion predicate requires the count of top-level `{`…`}` expansion pairs
**within a single whitespace-delimited word** to be at least two, which no single-brace entry can
satisfy however it is spelled. A floor a whole-word splice satisfies is a floor
`{g..g}it push --force origin main` walks around; a floor a single-brace entry satisfies is a floor
`{g..g}{i..i}t push --force origin main` walks around. Both were found in plan-check, one round
apart, and shipping either would be another round of a corpus incapable of failing on the class that
got through it.

Add counted floors over the generated cases for each new class, counted while generating rather than
inferred from alphabet sizes, in the shape `MIN_LIVE_EXPANSION_CASES` already establishes. The
floor's failure message records that the correct response is to RESTORE the entries and never to
delete the floor, names this as `T-19-95` — the fourth occurrence of `T-19-76`'s failure mode — and
records that the class split itself is the fifth occurrence, caught in plan-check.

Renaming the existing floor test to name the wider class it now covers is permitted and expected;
record the rename in the SUMMARY with the old name, the new name and the reason. Widening a FLOOR is
strengthening it, which is the one edit to a pre-existing test this plan allows, and it is not the
same act as re-wording a BOUND that was measured false.

On failure every new and every widened property prints the generated command verbatim, the recipe
that produced it, the observed verdict and the seed, plus the sentence naming the correct repair: a
change to the tokenizer's brace classification or to the decision-word rule in `policy.rs`, never a
name or a character added to a list in `src/`, and never a narrowing of an alphabet.

Leave every other existing test UNMODIFIED — the invariance property, the permitted corpus, the
alphabet-absence control, the decoy property, the expansion-wrapper property, the forge-slot
property's existing slots, the severed-prefix property, the two `T-19-74` bound tests,
`the_residual_begins_exactly_at_the_command_line_boundary` and
`resolve_programs_own_doc_still_discloses_the_residual_it_does_not_cover`.

Run the file, capture the verbatim RED output with `rtk proxy`, record both the failure list and the
measured per-class generated-case counts in the SUMMARY, and commit this RED state as its own
commit, again with zero `src/` hunks. **A corpus that was never observed failing is a corpus nobody
has proved can fail, and that — for the fourth round running — is the finding this task exists to
close.**
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_wrapper_class 2>&amp;1 | tail -55; rtk proxy cargo test --test envelope_wrapper_class &gt;/dev/null 2&gt;&amp;1; test $? -ne 0 &amp;&amp; echo "RED-CONFIRMED-WIDENED-ALPHABETS"; rtk proxy cargo clippy --all-targets 2>&amp;1 | tail -5</automated>
  </verify>
  <done>The widened `REFUSED_BASES` entries — including the RANGE, CONCATENATED-splice, MULTI-EXPANSION and INCREMENT-range bases — fail at the invariance property's own unwrapped floor, and the widened decoy, expansion-wrapper, severed-prefix and forge-slot properties FAIL against the pre-fix tree with the generated commands printed; the per-alphabet floor, the new per-CLASS floors — whole-word splice, concatenated splice, range, multi-expansion, literal brace pair, glob, tilde, with predicates that a whole-word `{a,b}` cannot satisfy for the concatenated or range classes and that no SINGLE-BRACE entry can satisfy for the multi-expansion class — and the counted live-case floors are asserted before their loops and all PASS; `git reflog delete HEAD@{0}` passes as a control both before and after; no existing floor was lowered and no existing alphabet entry, property or test other than the renamed floor was modified; the verbatim RED output and the per-class generated-case counts are recorded; and the red state is committed as its own commit with zero `src/` hunks.</done>
</task>

<task type="auto">
  <name>Task 3: the execution record, the registration, and the handoff gate `19-17` is measured against</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s appended subsections for plans 19-13, 19-14 and 19-15 — this plan's subsection
    FOLLOWS them and edits none of them. `deferred-items.md`'s `T-19-86` and `T-19-91` entries, for
    the registration shape to match, and its "Audit 4" section, whose `T-19-92` … `T-19-95` entries
    this plan updates only to record that a corpus now exists and is red.
  </read_first>
  <action>
Append ONE subsection to `19-SECURITY.md`, after the one `19-15` appended and touching nothing that
precedes it, titled so a reader cannot mistake it for an audit finding — an execution record made by
plan 19-16. It carries:

  - **what was measured, and how**: the built binary, one fresh envelope root per row, the envelope
    directory walked afterwards, and the `bash` shim confirmation with the printed argv for each
    claimed bypass. Any audit-4 row that did not reproduce, with its measured verdict. Any row
    DISCARDED because the shell does not run what the guard permitted, recorded the way audit 4
    recorded its three, so audit 5 does not spend the measurement again;
  - **the five in-scope cells found while planning**, with the same provenance caveat `19-14`
    established — found while planning, not by an audit — and their measured verdicts;
  - **the `T-19-93` counted rows**, stating explicitly that the bar is a LEDGER LINE and a second
    creation refused under `pr_cap_exceeded`, not an exit code, and that the quoted spelling already
    counts today and is the positive control;
  - **the `T-19-91` in-namespace measurement** of `git push $REF`, recorded as confirming audit 4's
    correction. State plainly that `T-19-91` stays OPEN at `high`, that the correction to the
    code-side record is `19-17`'s work, and that `reflog` and `symbolic-ref` have no `pre-push` or
    `pre-commit` second carrier while `push` does;
  - **the registered-only cell** — the glob in a push flag — with its measured verdict, the
    statement that it is registered and NOT fixed, and the reason: it is one slot outside `19-14`'s
    decision region, it was found while planning, and a plan cannot both discover a threat and be the
    plan that measured it fail first;
  - **the statement that this plan CLOSES NOTHING.** `T-19-92`, `T-19-93`, `T-19-94` and `T-19-95`
    are all open at its end; `T-19-95` is closed only when `19-17`'s rule is certified by this
    corpus, because a corpus is evidence about a control and there is no control yet. And the
    statement that `/gsd-secure-phase 19` is not cleared, `T-19-86` and `T-19-91` being open at
    `high`, and that re-measuring and re-classifying is that command's job rather than a plan's.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

Then `deferred-items.md`: add the registered-only push-flag glob cell in the shape the `T-19-86` and
`T-19-91` entries already use — symptom, mechanism, why it is registered rather than fixed, the
absent-or-present second carrier, and a suggested owner. Update the `T-19-92` … `T-19-95` entries
only to record that the corpus exists, is committed RED, and names which test file carries which
class. Leave the `T-19-86`, `T-19-87` and `T-19-91` entries otherwise untouched.

Then run the gate. **This plan's gate names its expected failures rather than requiring zero.**

  - The command is `rtk proxy cargo test --no-fail-fast`. A plain `cargo test` fail-fasts at
    `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes
    a single envelope suite; `rtk proxy` is required because the RTK hook strips the `warning:` and
    `test result:` lines any count is read from (D-34).
  - `passed + failed` is strictly greater than **1533**, the number `19-15-SUMMARY.md` records and
    audit 4 independently re-measured, and which is mode-independent because the `driver_reattach`
    pair always RUNS.
  - Every failure name is either `a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`,
    `a_run_killed_without_an_ending_is_reported_crashed_and_nothing_on_disk_is_repaired`, or one of
    the RED test names this plan creates. **A third kind of name is a regression to report rather
    than absorb.**
  - The RED set is non-empty, and every one of its names is listed in the SUMMARY. A run that added
    nothing reports exactly 1533 and no RED names at all, so this gate cannot pass vacuously.
  - `--test envelope_expansion_slots` and `--test envelope_command_position` are **fully green** —
    the widening must not disturb round-4's evidence — and `--lib envelope` is fully green.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0, and `cargo clippy --all-targets` reports
    no more than the four pre-existing warnings (three in `src/browser.rs`, one in
    `src/project_creator.rs`).

Record in the SUMMARY the per-binary counts for `--test envelope_literal_decision`,
`--test envelope_wrapper_class`, `--test envelope_expansion_slots`,
`--test envelope_command_position` and `--lib envelope::policy`, the full-suite line, and — the
handoff number — **the exact post-state `passed + failed` total and the complete list of failing test
names**, which is what `19-17` confirms still red before it writes a line.

Commit the record as its own commit, the third.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --lib envelope &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_expansion_slots &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_command_position &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-16-gate.log 2>&amp;1; echo "full_suite_exit=$?"; tail -60 /tmp/19-16-gate.log; git diff --stat HEAD~2..HEAD -- src/ | tail -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries one further appended, clearly-attributed execution-record subsection with no edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection; `deferred-items.md` carries the registered-only push-flag cell and the corpus notes with `T-19-86`, `T-19-87` and `T-19-91` unchanged; `cargo build` and `cargo clippy -- -D warnings` exit 0 with the `--all-targets` warning count still four; `--lib envelope`, `--test envelope_expansion_slots` and `--test envelope_command_position` are fully green; the `--no-fail-fast` run reports `passed + failed` strictly greater than 1533 with every failure name either a `driver_reattach` name or one of this plan's RED names; the exact post-state total and the complete failing-name list are recorded for `19-17`; `git diff --stat` across all three commits touches no file outside `files_modified` and shows **zero** `src/` hunks; and the record is a third commit distinct from the two RED states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must split into words before it can attribute any of them |
| written word → word the shell runs | Which bytes reach `execve` is decided by expansions the guard does not perform, of which parameter expansion is only one |
| the fix → the corpus that certifies it | What a later reader believes is closed depends on whether the corpus could have failed on the class |
| a measured bypass → a pinned bypass | A guard permit is a finding only if the shell really runs the dangerous command |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-95 | Spoofing | the 19-14 / 19-15 generative alphabets and `EXPANSION_METACHARACTERS` | medium | mitigate | The constant's successor names the SEVEN classes that make a word UNREADABLE rather than the ones that mark an expansion — whole-word splice, CONCATENATED splice, RANGE (including the increment form), MULTI-EXPANSION word, literal brace pair, pathname expansion, tilde. The class split is itself a finding, twice over: a single "brace expansion" class is satisfied by `{a,b}` and would never generate `{g..g}it`, and a class set without MULTI-EXPANSION is satisfied by any single-brace entry and would never generate `{g..g}{i..i}t` — the fifth and sixth occurrences of this failure mode, one plan-check round apart. Hence the predicates: a literal run adjacent to the braces, a `..` between them, and at least two top-level expansion pairs inside one word. Seven alphabets are widened, split by whether their property asserts refusal or invariance so the corpus can tell a brace EXPANSION from a brace PAIR; the forge property gains a splice slot, a glob slot and a literal-brace slot whose expected verdict is COUNTED. Per-alphabet floors, per-CLASS floors and counted live-case floors are asserted before every loop. **Observed RED against the pre-fix tree, which is the whole of this plan's contribution** |
| T-19-92 | Elevation of Privilege | `policy::tokenize` — a `{` that opens a brace EXPANSION is read as a brace GROUP | high | **mitigate (reproduced and pinned here; the rule is `19-17`)** | All eight audit-4 rows plus the position-0 splice, **the RANGE (`{g..g}it …`) and CONCATENATED (`g{i,i}t …`) splices in four spellings with a forge twin**, and the flag-slot splice are measured against the built binary, confirmed under bash shims, and written as REFUSAL assertions that are RED at this plan's end. Those three cells are the ones a rule reading alternative NAMES cannot reach and a corpus of whole-word splices cannot draw, so they set the shape of the rule `19-17` writes: what a splice can PRODUCE. The must-not-move controls (`{ git status; }`, `( git status )`, `(git status)&&git fetch origin`, the force-push grouping spellings, `ls {git,svn}-repo`) and the newly-refused COST rows with their permitted twins and their written clause derivations are beside them, so the cost is pinned from both sides before it is incurred |
| T-19-93 | Repudiation / Elevation of Privilege | the SAFE-06 PR cap vs. `gh`'s own `{owner}`/`{repo}` placeholders | high | **mitigate (reproduced and pinned here; the rule is `19-17`)** | Written as COUNTED rather than refused, because the row is a false negative on `gh`'s documented syntax: the assertion is a ledger line in a walked fresh root plus a SECOND creation refused under `pr_cap_exceeded`, with the already-counted quoted spelling as the positive control and `…/pulls/7` as the boundary control. No implementation can satisfy these by refusing |
| T-19-94 | Elevation of Privilege | pathname expansion in a decision slot | medium | **mitigate (reproduced and pinned here; the rule is `19-17`)** | The four measured rows, the `touch push && …` single-tool-call precondition row, and the two glob decision-operand cells found while planning, each pinned beside an operand-glob permit (`git add src/*.rs`, `rg "x" src/*`) so the decision-region boundary is a behaviour rather than an intention |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (measured, not closed)** | The in-namespace `git push $REF` is reproduced in a purpose-built fixture and recorded, confirming audit 4's correction that the row is cwd-dependent and permitted in the configuration a driven run is designed to be in. The code-side record correction is `19-17`'s. No rule is added for `reflog`, `symbolic-ref` or `push`, and the absent second carrier for the first two is restated |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG — one slot outside `19-14`'s decision region | medium | **register only (measured, pinned, NOT fixed)** | `git push --forc? origin refs/heads/gsd-auto/alpha/w` is measured with its literal control and pinned at its measured verdict, registered in `19-SECURITY.md`'s appended subsection and in `deferred-items.md`. Registered rather than fixed on round discipline: it was found while planning, and a plan cannot both discover a threat and be the plan that measured it fail first. `git push` has `pre-push` behind it, which `stash` and `update-ref` do not |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (registered, pinned, out of scope by user decision)** | Unchanged and unremediated. Its four rows stay pinned at exit 0. Because it remains open at `high`, neither this plan nor `19-17` clears `/gsd-secure-phase 19`, and the SUMMARY says so first |
| T-19-16r | Denial of Service | a corpus that is RED at a plan's end | low | **accept** | This plan deliberately leaves the suite failing, exactly as `19-14` left two rows red for `19-15`. The failures are enumerated by name in the SUMMARY and in the gate, `--test envelope_expansion_slots` and `--test envelope_command_position` stay fully green, and `19-17` follows immediately and begins by confirming the same names still red. A red state nobody named would be a regression; a red state named, committed and handed forward is the evidence that the rule was needed |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added; every parser here is hand-rolled against `std`. `Cargo.toml` and `Cargo.lock` are not in `files_modified`, and this plan touches no `src/` file at all |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo clippy -- -D warnings` exit 0; `cargo clippy
  --all-targets` reports no more than the four pre-existing warnings.
- The suite number MUST come from a `--no-fail-fast` run and MUST be read as `passed + failed`
  against **1533**. After this plan that total is strictly greater; every failure name is a
  `driver_reattach` name or one of this plan's RED names; the RED set is non-empty and fully listed
  in the SUMMARY.
- `--test envelope_expansion_slots`, `--test envelope_command_position` and `--lib envelope` are
  fully green. Per-binary counts for all five envelope suites are recorded.
- Every measured row was driven against the BUILT BINARY with a fresh envelope root and a walk, and
  every claimed bypass was confirmed under `bash` against argv-printing shims; discarded rows are
  recorded as discarded.
- Three commits exist in order: the reproducer RED, the alphabet RED, the record. **All three show
  zero `src/` hunks** under `git show --stat`.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`, nor `tests/envelope_expansion_slots.rs`, nor `tests/envelope_command_position.rs`.
  The `19-SECURITY.md` diff is one appended subsection and nothing else.
- No existing floor was lowered and no existing alphabet entry was deleted or reordered; the one
  permitted edit to a pre-existing test is the rename and widening of the per-alphabet floor, named
  in the SUMMARY with its old name, new name and reason.
- The Rule B mechanism pin asserts over `policy::split_segments_with_heads`, not over an exit code,
  and passes today.
- `the_t_19_86_residual_is_permitted_today_and_this_plan_leaves_it_permitted` is green and
  unmodified, all four rows at exit 0.
- `git reflog delete HEAD@{0}` is refused, and `git log -1 HEAD@{0}` and `git reflog show HEAD@{0}`
  are permitted, before this plan and after it.
</verification>

<success_criteria>
1. Every audit-4 `T-19-92`/`T-19-93`/`T-19-94` row was measured against the built binary and
   confirmed under bash shims BEFORE being written, and any row that did not reproduce is reported
   as a finding rather than asserted.
2. The widened corpus is observed RED against the pre-fix tree along every axis it widens — including
   the CONCATENATED-splice and RANGE classes, whose floors a whole-word `{a,b}` cannot satisfy; if
   any axis is green, that is reported as a finding rather than shipped.
3. `T-19-93`'s rows assert COUNTED — a ledger line in a walked fresh root plus a second creation
   refused under `pr_cap_exceeded` — and the quoted spelling and the `…/pulls/7` boundary control
   pass today.
4. The ten in-scope cells found while planning are measured and written — including the RANGE and
   CONCATENATED splices that no alternative-name test reaches, and the two MULTI-EXPANSION words and
   the INCREMENT range that no per-`{` product computation reaches; the eleventh is measured, pinned,
   REGISTERED and not fixed.
5. Non-vacuity floors exist per alphabet AND per class — whole-word splice, concatenated splice,
   range, multi-expansion, literal brace pair, glob, tilde — asserted before every loop, with counted
   live-case floors beside them, with predicates a whole-word `{a,b}` cannot satisfy for the
   concatenated and range classes and that no single-brace entry can satisfy for the multi-expansion
   class.
6. The newly-refused cost rows are each pinned beside a permitted twin AND a written derivation
   naming the clause that produces the verdict, before the cost is incurred; the two rows whose
   post-fix verdict cannot be derived are measured and classified rather than pinned, and
   `ls {git,svn}-repo` is pinned PERMITTED as the control distinguishing a product test from a name
   test.
7. The must-not-move controls, including the grouping rows and `git reflog delete HEAD@{0}`, and the
   Rule B MECHANISM pin over `split_segments_with_heads`, all pass today.
8. Nothing in `src/` changed, `T-19-86` and `T-19-91` are untouched and open, no unqualified
   "T-19-60 is closed" appears anywhere, and the SUMMARY states that `/gsd-secure-phase 19` is NOT
   cleared.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-16-SUMMARY.md` when done.
It must contain: the verbatim measured verdicts for every audit-4 row and every planning cell, with
the bash-shim printed argv beside each claimed bypass and any discarded row recorded as discarded;
the verbatim RED output from Tasks 1 and 2; the three commit SHAs in order with the confirmation
that all three show zero `src/` hunks; the `--no-fail-fast` `passed + failed` totals before and
after, **the exact post-state total and the COMPLETE list of failing test names for `19-17` to
confirm still red**; the per-binary counts for all five envelope suites; the walked envelope listings
for one refused forge row and one counted row; the measured per-class generated-case counts for all
seven classes; the written clause derivation for every pinned cost row, and the measured verdicts and
classifications for `FOO={a,b} git status` and `rg "git status" {src,tests}`, which are NOT pinned
post-fix here; the
renamed floor with its old name, new name and reason; the `T-19-91` in-namespace `git push $REF`
measurement; the registered-only push-flag glob cell with its verdict and the reason it is registered
rather than fixed; and a "what remains uncovered" section naming `T-19-86` FIRST — open at `high` by
user scoping decision — followed by `T-19-91` (open at `high`, record correction handed to `19-17`),
`T-19-92`/`T-19-93`/`T-19-94`/`T-19-95` (all still OPEN — this plan closes nothing), `T-19-96`
(registered), `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit statement
that `/gsd-secure-phase 19` is NOT cleared by this plan or by `19-17`.
</output>
