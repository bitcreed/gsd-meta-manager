---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 25
type: execute
wave: 15
gap_closure: true
gap_ids: [T-19-108, T-19-109, T-19-106, T-19-105]
depends_on: [19-24]
files_modified:
  - src/envelope/policy.rs
  - src/envelope/cred.rs
  - tests/envelope_reparsed_value.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-04, SAFE-05, SAFE-06]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "**THE DESIGN QUESTION, ANSWERED: OPTION (b), AND THE REASON IS MEASURED RATHER THAN PREFERRED.** The guard reads config assignments in two regions and git resolves configuration from a third — a value the guard itself CONFINED. The fix is **not** to scan that value; it is to treat an assignment whose key names a value git RE-PARSES AS A COMMAND LINE as one the guard cannot bound, on exactly the footing round 8's confinement clause already means. **Option (a) — apply the same clause recursively to the confined value with a stated depth bound — is REJECTED on three measured grounds.** (1) It needs a SECOND TOKENIZER: git splits an alias body with its OWN rules, not the shell's — measured, `git -c alias.m='config --get \"core.hooksPath\"' m` resolves — so the guard would have to implement a foreign grammar it cannot observe, which is the exact class of unmeasured claim `T-19-106` and `T-19-107` were both registered for. (2) It needs UNBOUNDED RECURSION: measured at depth 2, `git -c alias.a='-c alias.b=\"-c include.path=<f> config --get core.hooksPath\" b' a` resolves `/INCLUDE_WINS`, so any stated depth bound leaves a fail-open residue at depth N+1 that an ATTACKER reaches by adding one nesting level today — strictly worse than round 8's residue, which needs a future git release. (3) It cannot read a body it does not have: `--config-env=alias.q=<VAR>` delivers the body through an environment variable NAME, and `git -c alias.q status` has no value half at all, so option (a) must fall back to option (b) at exactly the carriers that matter. **Option (b) buys a closed grammatical fact for one legible over-refusal family, and that is the same trade round 8 made and audit 8 endorsed.**"
    - "**WHAT GIT RE-PARSES FROM A CONFIG VALUE IS A THREE-KIND ENUMERATION AND ONLY ONE KIND REACHES LAYER 3, MEASURED IN BOTH DIRECTIONS.** (K1) **re-parsed as a GIT command line, IN-PROCESS, including its own leading options** — `alias.<name>` with a non-`!` body, and it is the only member; (K2) **re-parsed as a SHELL command line, executed as a CHILD that INHERITS the envelope's injection** — `alias.<name>` with a `!` body, `core.pager`, `pager.<cmd>`, `core.editor`, `sequence.editor`, `core.askPass`, `credential.helper`, `core.sshCommand`, `core.gitProxy`, `core.fsmonitor`, `core.alternateRefsCommand`, `diff.external`, `diff.<d>.command`, `diff.<d>.textconv`, `merge.<d>.driver`, `mergetool.<t>.cmd`, `difftool.<t>.cmd`, `filter.<n>.clean`/`.smudge`/`.process`, `gpg.program`, `uploadpack.packObjectsHook`, `protocol.<n>.command`, `remote.<n>.uploadpack`/`.receivepack`, `trailer.<t>.command`, `submodule.<n>.update`, `web.browser`/`browser.<t>.cmd`; (K3) **re-parsed as a FILE PATH spliced at the directive's precedence** — `include.path`, `includeIf.<cond>.path`, closed by `19-23`. **K2's inertness is a MEASUREMENT, not a category argument**: `git -c alias.b='!git config --get core.hooksPath' b` prints **`/ENV_WINS`** while the non-`!` body prints **`/INCLUDE_WINS`**, which is audit 8's `GIT_PAGER`/`GIT_EDITOR`/`GIT_SSH` finding restated on the config side. So the new constant names **one section**, and it names it because that section is where K1 lives."
    - "**THE RULE, EXACTLY: THE SECTION PLUS ONE BYTE OF THE VALUE, AND THAT ONE BYTE IS GIT'S OWN RULE RATHER THAN A CONCESSION.** A new constant `REPARSED_COMMAND_SECTIONS: &[&str] = &[\"alias\"]` beside `INDIRECTION_SECTIONS`, a new predicate `config_key_names_a_reparsed_command_section` mirroring `config_key_names_an_indirection_section` exactly — the text before the FIRST `.`, compared with `eq_ignore_ascii_case`, `None` answering `false` so a dotless key stays CONFINED — and a value test that asks only whether the FIRST BYTE is `!`. Git's documented rule is precisely that, and `19-24` measured it in four spellings: a non-`!` body is re-parsed in-process at command-line precedence (`/INCLUDE_WINS`), a `!` body runs in a child that inherits the injection (`/ENV_WINS`), a `!` that is not first is not a shell body (`/ENV_WINS`), and a leading space makes git refuse to expand at all. **A value the guard cannot read the first byte of — no `=` at all, or a `--config-env` carrier whose value is an environment variable NAME — is UNBOUNDED and refuses**, which is the fail-closed direction and is what makes the clause complete over both carriers `leading_git_option` returns."
    - "**THE `!` CARVE-OUT IS THE SEAM THIS ROUND WOULD OTHERWISE HAVE BLOCKED ON, AND `19-24` FENCED IT MECHANICALLY IN BOTH FILES.** `tests/envelope_command_position.rs:550` asserts `permits(\"git -c alias.p='!git push --force origin main' p\")` — round 3's registered `T-19-86` row — and `tests/envelope_config_resolution.rs:1539-1543` asserts `permits(\"git config alias.p \\\"!git push --force origin HEAD:refs/heads/main\\\"\")` — `19-22`'s persisted `T-19-86` arm. **This plan may edit neither file.** A rule that refused every `alias.*` assignment turns both permanently red — `19-18`'s `{v}>` blocker and `19-22`'s `-c a=b` seam, one region over. The fence is asserted in `19-24`'s corpus and in `tests/envelope_wrapper_class.rs`; this plan must not defeat it."
    - "**THE RULE IS ASKED AT TWO DECISION REGIONS THAT ALREADY EXIST AND ALREADY READ THE SAME WORD, AND THAT IS ROUND 3'S PRINCIPLE DISCHARGED RATHER THAN WEAKENED.** Region 1 is `scan_leading`'s `if let Some(assignment)` block, where the new clause sits beside the confinement clause and BEFORE `is_hooks_path_key`, returned through the `(usize, Option<GitVerdict>)` channel that already carries three refusals. Region 2 is `classify_config`'s `key_operand()`, which **already reads that exact operand with `is_hooks_path_key`** and already refuses `git config core.hooksPath /dev/null` at `hook_bypass_blocked` — measured. Neither is a new reading site: each decision is derived from the scan the classifier already runs for that decision, `config_key_operand_index` is the index primitive both share, and no second walk is introduced. **If the fix needs a THIRD site, that is a finding to report, not a place to add one.**"
    - "**REGION 2 IS REQUIRED, AND THE ASYMMETRY WITH `include.path` IS WHY — GET IT BACKWARDS AND THE ROUND SHIPS AN INERT CLAUSE.** Audit 8 measured persisted `git config include.path <evil>` INERT at repo-local, `--worktree` and GLOBAL, because the envelope's injection outranks it, so the include family correctly needs no clause at the write site. **An alias does not have to WIN a precedence contest; it only has to EXIST.** `19-24` measured, under the envelope's own `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` redirect at a generated helper-free file, that a `--global` alias and a repo-local alias are BOTH live and both resolve the included value. `T-19-108`'s second leg is two separately-permitted tool calls the stateless guard cannot correlate — `git config alias.p '<body>'` then `git p` — and the only one of them the guard can decide on is the WRITE. **MUST NOT add an `include.path` clause at region 2: it would be inert over-refusal with no measured harm behind it, and audit 8 said so explicitly.**"
    - "**AND STATE HONESTLY WHICH DIRECTION THIS RULE FAILS — IT FAILS OPEN, IN THREE NAMED DIRECTIONS, AND NOT ONE OF THEM HAS AN AUTOMATED CONTROL.** Round 7's clause failed open on a future third indirection section with no automated control, the plan that hid that behind a pin was caught in review, and this round does not repeat it. **(i) An `alias.*` already present in a config file the guard never saw a write to.** The guard is stateless and argv-only; `git co` is an unknown verb at exit 0 and stays there. Under the envelope a user's own global aliases are out of reach because `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` both point at the generated helper-free file, but a repo-local `.git/config` alias predating the run, or one written by a means that is not `git config`, is live. That is `T-19-86`'s shape and it stays open. **(ii) A `!`-bodied alias body carrying its own carrier** — audit 7's destructive pair, which rewrote a bare remote's `main` at `fb43577` and **still works after this round**, because closing it means taking on `T-19-86`. **(iii) A future git that re-parses a SECOND config value as a git command line with its own leading options** — the same shape as `INDIRECTION_SECTIONS`'s residue, the same absence of control. **All three go in the constant's own doc, in the predicate's doc, in the refusal's doc and in the record, in those words. MUST NOT hand any of them to a pin.**"
    - "**THE COST IS PINNED FROM BOTH SIDES AND THE HONEST ANSWER IS THAT ORDINARY ALIASES DO NOT ALL KEEP WORKING.** The question was asked directly and the answer is stated plainly rather than softened: **defining a non-shell alias is refused; using one is not.** `git -c alias.st=status st`, `git -c alias.lg=\"log --oneline\" lg`, `git -c alias.co=checkout co` and `git config alias.co checkout` are all measured exit 0 today and refused after — `19-24` pinned every one of them beside the permitted twin that does the same work (`git status`, `git log --oneline`, `git checkout`), and pinned the INVOCATION rows `git p`, `git co` and `git q` UNCHANGED at exit 0. **The over-refusal is one legible family** — an agent defining a git alias mid-run rather than running the command — and it is the same trade AR-19-11 governs, made in the direction that keeps ordinary work working: `git -c user.name=\"$NAME\" commit -m x` and every ordinary `-c` in the suite keep their present verdicts, and round 7's generative property stays green behind `CALLEE_KNOWN_LEADING_PREFIX = \"-c a=b\"` because a dotless key names no section and is CONFINED."
    - "**`T-19-108` CLOSES ONLY AS SCOPED, AND THE SCOPE IS STATED RATHER THAN IMPLIED — THE SHAPE AUDIT 8 USED FOR `T-19-103`.** Closed: the **non-shell** alias body, in the `-c` carrier (audit 8's leg 1), in the `--config-env` carrier, in the persisted write (audit 8's leg 2, the one that moved `refs/heads/gsd-auto/alpha/w` from `3b84f30` to `1ab87f6`), and at both keys the body can carry — `include.path` and `core.hooksPath` by name (the `alias.z` variant). **NOT closed: audit 8's leg 3** — `git config alias.q '!git -c include.path=<evil> push --force origin HEAD:refs/heads/main'` then `git q`, the pair that rewrote the bare remote's `main` from `ac303dc` to `9f62444`. **That pair still works after this plan and the SUMMARY must say so FIRST, in those words.** It is `T-19-86` — a whole command line handed to a governed program as data — which audit 8's own boundary paragraph lists as a separate bullet from the re-parsed config value, and which is out of scope by explicit user decision with both its rows pinned PERMITTED in files this plan may not edit."
    - "**`T-19-109` IS FIXED BY REPLACING A COUNT WITH A REGION, WHICH IS THE ONLY FIX THAT STAYS TRUE.** `cred.rs:246-260` claims *\"FIVE forms outrank this injection\"* with a five-row table. Measured, there is a sixth — a non-`!` `alias.<name>` body beginning `-c include.path=<file>` — and the claim was wrong the day it was written. **A counted completeness claim is strictly harder to keep true than the sentence it replaced**, and this is the second time that paragraph has been corrected, which is `T-19-84`'s, `T-19-107`'s and this row's shared shape. The replacement states the REGIONS each closure covers — the argv leading-option region, the environment, and a config value the guard confines — and states what is NOT covered, without counting anything. **Same for \"closing `T-19-103` is a RESTORATION of layer 3's catch\"**, which is true only for an alias body carrying no carrier of its own: it appears in the 19-22 record, the 19-23 record and `19-23-SUMMARY.md`, and **the correction is recorded BESIDE those subsections rather than as an edit to them** — the provenance discipline audits 7 and 8 both used for exactly this shape. This plan opens `cred.rs` for that paragraph and NOTHING else; `T-19-61` … `T-19-73` are not taken on and `git diff --unified=0` over the file, filtered for lines that are not `///`, must return NOTHING."
    - "**`T-19-106`'S REMAINDER IS ONE PIN THAT CAN RUN TODAY AND ONE ENTRY THAT MUST BE KEPT WITH ITS REASON — AND AUDIT 8'S OWN SUGGESTION IS CORRECTED BY MEASUREMENT ON THE SECOND HALF.** `GH_API_VALUE_OPTS` is a pure `gh api` constant, `gh` 2.45.0 IS installed, and all seventeen entries answer `flag needs an argument` — re-measured while planning — so the two-sided pin gets written, with **two negative controls that pin the classifier itself**: `--bogus-opt` must come back unknown, and `--paginate` must come back as an existing flag that needs NO value, which is the control without which the pin would pass on a probe reporting *requires a value* for everything. **`FORGE_VALUE_OPTS`'s `--hostname` is KEPT and its reason RECORDED, and removing it would be a REGRESSION.** Measured at `21655c5`: `glab --hostname gitlab.com mr create --title x` leaves exactly ONE ledger line while an option NOT in that constant leaves ZERO, so removal moves a counted creation form to UNCOUNTED — the under-counting direction the cap exists to prevent (`T-19-35`). `gh pr create` answers `unknown flag: --hostname`, so the entry is INERT for `gh` and load-bearing for `glab`, whose callee remains unconfirmed. **The pin is asymmetric by construction and says so.** `subcommand_word_indices` is not otherwise touched and the `glab --host` cell is carried forward unfixed."
    - "**THE FAIL-OPEN RESIDUE GETS A REVISIT CONDITION, NOT AN ACCEPTANCE.** Audit 8 judged the residue itself acceptable and properly bounded — the admission is complete in five places and nothing claims the pin covers it — with one gap: its only control is a human reading a future git's release notes, and **nothing schedules that.** There is no `AR-` row, no revisit condition of the kind `AR-19-03` carries at `policy.rs:238-241`, and no assertion that goes red when the installed git changes. The fix is a revisit condition in the shape `classify_git`'s doc already uses — a named, concrete trigger and what to do when it fires — carried by BOTH `INDIRECTION_SECTIONS` and the new `REPARSED_COMMAND_SECTIONS`, **plus a VERSION-WITNESS assertion that records the `git --version` both constants were derived against and goes RED when the installed version differs**, with a failure message saying the correct response is to RE-DERIVE both constants against the new git and update the recorded version, never to delete the assertion. **Its cost is stated rather than hidden: it fires on every git upgrade, including harmless ones — that IS the schedule**, and it is the only shape that turns *a human remembers* into *a test fires*. It MUST NOT skip when git is absent. **This is NOT an acceptance: no `AR-` row is added, no risk is accepted, and `T-19-17r` stays OUTSTANDING.**"
    - "**Every row `19-24` left RED was observed RED first, in commits with zero `src/` hunks — and this plan's FIRST action is confirming they are still red.** If any row is already green against `19-24`'s post-state, the rules are rules nobody has shown are needed and that is a finding to report with the measured verdict, not a task to complete. This is the mechanism `19-15`, `19-17`, `19-19`, `19-21` and `19-23` used and it is the one number a run that did nothing cannot forge."
    - "**Rounds 5, 6, 7 and 8's mechanisms must not become dead code, and `19-24` pinned all of them.** `Token.literal` stays `false` for `pus?` and `true` for every word of the bypass line — **the bit is RIGHT about all of them**, and a fix that made it wrong to produce a refusal would turn round 5's verdict pins green while the new rule stopped being what produced them. `git >/dev/null push --force origin main` stays refused and `git x2>/tmp/o push --force origin main` stays PERMITTED. `SEPARATORS` stays **byte-identical** at `policy.rs:1786` and `policy::is_separator(\">\")` stays `false`. `git --attr-source HEAD push --force origin main` stays refused, `git --bogus-opt status` stays refused at `envelope_assertion_failed`, `git --no-pager status` and `git - push --force origin main` stay at exit 0, and `git -- push --force origin main` stays refused. **Round 8's confinement clause stays non-dead**: `git -c include.path=/tmp/evil.cfg status` refused beside `git -c includepath=…` and `git -c notinclude.path=…` at exit 0. `POLICY_MIN_PRODUCTION_BYTES = 40_000` and `HOOKS_MIN_PRODUCTION_BYTES = 20_000` stay at their values, and **the anti-vacuity stripper's three protections all keep their present strictness** — the proportional floor `>= 180_000` (78.9% of a 228,101-byte production half), the deep anchor `fn forbidden_repo_path` at production line 4,592 of 4,613, and the exactly-ONE `#[cfg(test)]` sentinel count at line 4,614. This plan adds prose to `policy.rs`, which only grows the production half."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan.** `T-19-86` remains OPEN at `high` by explicit user scoping decision — its four registered rows and its persisted-alias arm keep exiting 0, audit 8 found it measurably WIDER than the register credits, and audit 7's `!`-bodied destructive pair still works after this plan. `T-19-91` remains OPEN at `high` with its arms unweakened. `T-19-96` is registered open; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched apart from the ONE named `cred.rs` doc exception. The SUMMARY must say so FIRST, and must never write an unqualified \"T-19-60 is closed\". **The `T-19-17r` bookkeeping gap stays OUTSTANDING**: this plan adds no Accepted-Risks-Log row, creates no `AR-19-13`, and does not call it accepted."
  prohibitions:
    - statement: "MUST NOT begin any fix before confirming, against `19-24`'s recorded post-state, that every RED row `19-24-SUMMARY.md` names is STILL RED, and recording that observation verbatim. If any is already green, report it as a finding with the measured verdict and the reason identifier rather than proceeding — a rule certified by a corpus that could not fail on its class is the exact defect nine rounds of this phase have been about."
      category: honesty
    - statement: "MUST NOT refuse an `alias.*` assignment whose VALUE begins with `!`. `tests/envelope_command_position.rs:550` and `tests/envelope_config_resolution.rs:1539-1543` both pin a `!`-bodied alias PERMITTED as a registered `T-19-86` row and this plan may edit NEITHER file. Git's own rule is the first byte and `19-24` measured it in four spellings; a `!` body runs in a CHILD that INHERITS the envelope's injection, measured `/ENV_WINS`, so layer 3 is intact for it. A blanket refusal is permanently red in files this plan may not edit — `19-18`'s `{v}>` blocker one region over."
      category: safety
    - statement: "MUST NOT close `T-19-108` by scanning the confined value, by tokenizing an alias body, by recursing to any depth, or by any construction that needs git's `split_cmdline` grammar. Measured: git splits an alias body with its OWN quoting rules, an alias body can carry `-c alias.<n>=…` and re-parse at depth 2, and `--config-env=alias.q=<VAR>` and `git -c alias.q` deliver no readable body at all — so option (a) needs a foreign parser, an attacker-controllable depth residue and a fallback to option (b) at exactly the carriers that matter. Cost it in the record and REJECT it there; do not implement it."
      category: safety
    - statement: "MUST NOT add `alias` to `INDIRECTION_SECTIONS`. That constant's doc is a closed grammatical fact about FILE SPLICING — a value that names a file read at the directive's precedence — and an alias value is a command line, not a path. Conflating them makes the constant's own doc false, makes the refusal message name a mechanism that did not produce it (D-24), and merges two residues that need two different revisit conditions. The new family is a SECOND named constant with its OWN doc, its OWN predicate and its OWN refusal helper, consulted from the SAME clause position."
      category: safety
    - statement: "MUST NOT refuse an assignment whose key the guard cannot decompose into a section. A key with no `.` names no section and is provably not an alias — it is CONFINED and reaches the existing clauses exactly as today. `tests/envelope_wrapper_class.rs:5197` splices round 7's ENTIRE callee-grammar generative property behind `CALLEE_KNOWN_LEADING_PREFIX = \"-c a=b\"`, real git RUNS `git -c a=b version` at rc 0, and both `19-22` and `19-24` fenced this mechanically. A refusal there is permanently red in files this plan may not edit."
      category: safety
    - statement: "MUST NOT write the rule as a prefix or substring test on `alias`. `19-24` pinned `git -c aliasx.q=\"…\" q` and `git -c notalias.q=\"…\" q` PERMITTED before and after — this round's `--signed no` — and only a SECTION comparison against the text before the FIRST `.`, folded with `eq_ignore_ascii_case`, keeps them green. And MUST NOT refuse an assignment the guard DOES bound: every row `19-24` pinned at exit 0 stays at exit 0, including the whole of round 8's permitted half."
      category: safety
    - statement: "MUST NOT shell out to `git`, or to anything else, from the guard's own path — not from `scan_leading`, not from `leading_git_option`, not from `classify_git`, not from `classify_config`, not from `resolve_program`, and not from any function they call. **In particular MUST NOT read a config file, resolve an alias, or consult the environment at guard time.** The guard runs synchronously on the agent's `PreToolUse` critical path where a reproduced 180-240 second hang is why `push_needs_resolved_dests` exists; the config may change between guard and exec (TOCTOU); and a guard that asks the program it is guarding to describe its own state can be lied to by a `git` earlier on `PATH`. **`scan_leading` stays a pure argv function** and so does every function this plan touches. Every probe lives in a TEST."
      category: safety
    - statement: "MUST NOT implement the refusal as a second pass over the leading tokens, a second scan of the argv, a filter in `classify_git` after `scan_leading` returned, a new arm in `resolve_program_with_head`, or a THIRD reading site of any kind. Region 1 is `scan_leading`'s existing `if let Some(assignment)` block, returned through the channel that already carries three refusals; region 2 is `classify_config`'s `key_operand()`, which already reads that operand with `is_hooks_path_key` and shares `config_key_operand_index` with `first_unreadable_decision_word`. `first_unreadable_decision_word`'s `at(index, role)` closure is NOT touched, no index primitive changes, `subcommand_word_indices` and `scan_gh_api` are not touched, and `hooks.rs` is NOT opened. **A second pass also turns `19-24`'s ordering pins red. If the fix needs a third site, stop and report it as a finding.**"
      category: safety
    - statement: "MUST NOT add an `include.path` or `includeIf` clause at `classify_config`'s key operand. Audit 8 measured persisted `git config include.path <evil>` INERT at repo-local, `--worktree` and GLOBAL, and stated explicitly that it correctly needs no clause. Adding one would be over-refusal with no measured harm behind it, in a round whose whole thesis is that a rule's reach must be derived from a measurement."
      category: scope
    - statement: "MUST NOT add a new `ParkReason` variant. An assignment whose value git re-parses as a command line is `ParkReason::EnvelopeAssertionFailed`, the identifier the sibling confinement and unreadable-key refusals already carry and the one `19-24`'s rows are asserted at. The MESSAGE must name THIS mechanism rather than the include one — a refusal that named file splicing on a line that splices no file would attribute it to a mechanism that did not produce it (D-24), the defect `19-21`'s executor reported as a blocker."
      category: safety
    - statement: "MUST NOT quote the command back in the refusal message (SAFE-04). Naming the KEY and the SECTION is permitted and required, on the same footing as `scan_leading`'s three existing refusals, because a refusal a user cannot act on is a control that gets switched off (AR-19-11). **The recovery step must be one git accepts** — run the command the alias would have run — which is the correction `19-21`'s attached-spelling wording needed."
      category: safety
    - statement: "MUST NOT claim in any doc, message or record that this rule is fail-closed, that it is a fifth or sixth inversion, or that its silence is a refusal. **It fails OPEN in THREE named directions and NONE of them has an automated control**: an alias already persisted in a config file the guard never saw a write to; a `!`-bodied body carrying its own carrier, which is `T-19-86` and which leaves audit 7's destructive pair working; and a future git that re-parses a second config value as a git command line. All three go in the constant's doc, the predicate's doc, the refusal's doc and the record, in those terms. **MUST NOT hand any of them to the drift pin or to any other control.** Round 7's clause hid exactly this behind a pin and a plan-check caught it; `T-19-107` is this phase's registered instance of false reassurance in a control's own doc, and reproducing it here would be that finding committed by the round that inherits it."
      category: honesty
    - statement: "MUST NOT write the version-witness assertion so that it skips, warns without failing, or passes when `git` is absent. A skipped pin is a fail-open pin and this phase has been punished six times for that shape. And MUST NOT present the revisit condition as an ACCEPTANCE: no `AR-` row is added, no risk is accepted, and the word \"accepted\" is not applied to the residue or to `T-19-17r`."
      category: honesty
    - statement: "MUST NOT open any part of `src/envelope/cred.rs` other than the `hooks_path_env` limit paragraph at `:241-272`. This is a NAMED, NARROW, DELIBERATE exception to this phase's `cred.rs` fence, granted on the same standard as `19-23`'s: it changes ZERO non-doc lines, verified by `git diff --unified=0` filtered for lines that are not `///` returning NOTHING. **`T-19-61` … `T-19-73` are NOT taken on**, `config_env`, `hooks_path_env`'s body, `write_gitconfig`, `build_env_in`, `EnvelopeEnv` and every other item are untouched, and the exception is stated in the SUMMARY and the record as an exception rather than presented as ordinary scope."
      category: scope
    - statement: "MUST NOT replace `cred.rs`'s counted claim with another counted claim. A table of five was wrong the day it was written and a table of six would be wrong the moment a seventh is found — that is `T-19-84`'s, `T-19-107`'s and `T-19-109`'s shared shape. State the REGIONS each closure covers and what is not covered; count nothing."
      category: honesty
    - statement: "MUST NOT edit the 19-22 record, the 19-23 record or `19-23-SUMMARY.md` to fix the \"RESTORATION of layer 3's catch\" claim. The correction is recorded BESIDE them in this plan's own appended subsection, which is the provenance discipline audits 7 and 8 both used for this exact shape. `19-24-SUMMARY.md` is likewise not edited."
      category: honesty
    - statement: "MUST NOT remove `--hostname` from `FORGE_VALUE_OPTS`. Measured at `21655c5`: `glab --hostname gitlab.com mr create --title x` leaves exactly ONE ledger line and an option NOT in that constant leaves ZERO, so removal moves a counted creation form to UNCOUNTED — the under-counting direction the cap exists to prevent (`T-19-35`). The entry is KEPT with its reason recorded: rejected by `gh pr create` (`unknown flag: --hostname`), therefore inert for `gh`, and load-bearing for `glab`, whose callee is unconfirmed because `glab` is NOT installed. Re-measure before writing; if the measurement disagrees, report it as a finding."
      category: safety
    - statement: "MUST NOT write the `GH_API_VALUE_OPTS` pin without BOTH negative controls. `--bogus-opt` must come back unknown, and an existing flag that needs no value (`--paginate`) must come back existing-and-not-requires-value — without the second, the pin passes on a probe that reports *requires a value* for everything, which is the vacuity `19-23`'s `CONFIG_VALUE_OPTS` redesign exists to exclude. MUST NOT complete either constant from help text; every membership decision is a two-sided real-`gh` measurement recorded beside it."
      category: safety
    - statement: "MUST NOT fix, pin or expand into the `glab --host` cell or `subcommand_word_indices`'s other behaviour. **`glab` is confirmed NOT INSTALLED**, so a two-sided pin over `FORGE_VALUE_OPTS` cannot run against its real callee and a pin that skips is fail-open. Carry the cell forward unfixed with its unconfirmed-callee caveat; audits 7 and 8 both explicitly declined to upgrade it."
      category: scope
    - statement: "MUST NOT edit or delete a row of `tests/envelope_reparsed_value.rs`, `tests/envelope_config_resolution.rs`, `tests/envelope_wrapper_class.rs`, `tests/envelope_callee_grammar.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_expansion_slots.rs` or `tests/envelope_command_position.rs` to make a fix pass. Those are `19-24`'s and earlier rounds' evidence. If a row cannot be satisfied, that is a finding about the RULE; report it with the row, the expected verdict and the measured one, and change the rule or report the plan as inconclusive — never the assertion. **The permitted test writes are ADDITIONS ONLY**: new `#[test]` fns in `tests/envelope_reparsed_value.rs` pinning the two verdicts `19-24` measured but could not derive, and new `#[test]` fns in `src/envelope/policy.rs`'s own `#[cfg(test)] mod tests`. **`19-24` granted no replacement exception and none is needed.** A deletion anywhere under `tests/` is a finding about the rule."
      category: honesty
    - statement: "MUST NOT fold the new pins into an existing `#[test]` fn. This is load-bearing arithmetic rather than style: turning a failing test green leaves `passed + failed` unchanged, so NEW `#[test]` fns are the ONLY thing that makes this plan's gate arithmetically satisfiable. Folding them in makes the gate unsatisfiable — round 3's failure mode, which only execution caught."
      category: honesty
    - statement: "MUST NOT place a `#[cfg(test)]` line anywhere in `src/envelope/policy.rs` above the existing sentinel at line 4,614. The anti-vacuity stripper truncates its view at the FIRST one, and the file's own sentinel-count assertion forbids a second. New test-only constants belong inside the existing `#[cfg(test)] mod tests`, which is where `GIT_GLOBAL_UNPROBED_OPTS` and `ENVELOPE_ENV_DEFEATING_KEYS` already live."
      category: safety
    - statement: "MUST NOT clear or repurpose `Token.literal`, `Token.expansion`, `Token.word_splitting_flush`, `Token.brace_splice`, `Segment.head_is_command_position` or `Segment.redirection_unresolvable`. Every word of the bypass line is literal and the bit is RIGHT about all of them. A fix that made the bit wrong in order to produce a refusal would turn rounds 5 and 6's verdict pins green while their mechanisms stopped being what produced them."
      category: safety
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS` or remove any character from it, and MUST NOT weaken round 6's deletion model, round 7's fail-closed callee grammar or round 8's confinement clause. `policy::is_separator(\">\")` stays FALSE, `separators_are_named_in_one_list_that_the_predicate_reads` stays GREEN and UNMODIFIED, and every one of `19-24`'s six mechanism pins stays green."
      category: safety
    - statement: "MUST NOT lower `POLICY_MIN_PRODUCTION_BYTES` or `HOOKS_MIN_PRODUCTION_BYTES`, MUST NOT weaken the proportional floor, the deep anchor or the sentinel-count assertion, and MUST NOT economise on `policy.rs`'s prose to protect a floor. The three protections are what the anti-vacuity control rests on and all three must keep their present strictness."
      category: safety
    - statement: "MUST NOT add a list of git verbs, wrapper program names, config VALUES, file names or 'dangerous' characters keyed to a program. `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` is the mechanical control and it must stay green. The new constant names ONE SECTION of git's own config grammar, which is the re-parse question, and the pin holds it to the installed git's answer."
      category: scope
    - statement: "MUST NOT fix, narrow, re-scope or re-classify `T-19-86`, and MUST NOT fix `T-19-96` or re-open `T-19-74`, whose core rows are frozen. `T-19-86`'s four registered rows and its persisted-alias arm stay at exit 0, its pins stay green UNMODIFIED, and audit 8's finding that it is measurably WIDER than the register credits is RECORDED rather than acted on. **Audit 7's `!`-bodied destructive pair still works after this plan and the SUMMARY says so first.** `env $X push --force origin main` and `X=git; env $X push --force origin main` stay at exit 0."
      category: scope
    - statement: "MUST NOT close, narrow, renumber or re-scope `T-19-91`, and MUST NOT weaken its record. Its arms stay as audits 5 through 8 verified them. Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push` and do not extend the denylist."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85`. Do not move `park_refusal` above the `?` writes in `deny`, do not add park-coverage controls, do not touch `advisory.rs`, `scan.rs`, `config.rs`, `mod.rs`, `hooks.rs`, the honesty statement or the second-carrier table. The `cred.rs` doc correction is the ONE named exception."
      category: scope
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, MUST NOT create `AR-19-13`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere. Record the gap as still OUTSTANDING. Accepting a risk is a human decision and four audits running have deliberately left it unmade."
      category: honesty
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock`. Every probe uses `std::process::Command` inside `#[cfg(test)]`."
      category: scope
    - statement: "MUST NOT report a green run of any control without `19-24`'s recorded RED output beside it in the SUMMARY, and MUST NOT create a control and its fix in the same commit. The four commits here are all fixes; the RED is `19-24`'s and is confirmed still red before the first of them."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Gate on `passed + failed` against the exact total `19-24-SUMMARY.md` records, and state the arithmetic: a red test RAN, so red→green leaves the total unchanged and any increase comes ONLY from new `#[test]` fns. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy` (D-34). **All FIFTEEN `envelope_*` binaries must have RUN.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes, and MUST NOT treat their absence from a run as a regression — one FIRED in audit 8's run and passed on re-run. MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lints in `src/browser.rs` and `src/project_creator.rs`; the gate is `cargo clippy -- -D warnings`."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or any earlier appended subsection including the plan-19-21 blocker-row resolution record and the 19-22, 19-23 and 19-24 records. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-25 execution record."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere — in code, a test name, a comment, the SUMMARY or the appended subsection. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`. **State plainly that this plan does NOT clear `/gsd-secure-phase 19`.**"
      category: honesty
  artifacts:
    - path: "src/envelope/policy.rs"
      provides: "`REPARSED_COMMAND_SECTIONS` and `config_key_names_a_reparsed_command_section` beside `INDIRECTION_SECTIONS` and its predicate, with the SECTION read exactly as that predicate reads it and a dotless key CONFINED; a value test on the FIRST BYTE implementing git's own shell-alias rule, with an unreadable value UNBOUNDED; the clause asked at BOTH existing decision regions — `scan_leading`'s assignment block and `classify_config`'s key operand — with no third reading site and no new `ParkReason`; a refusal helper naming THIS mechanism rather than the include one; the three fail-open directions stated in the constant's, the predicate's and the refusal's docs and handed to no control; a REVISIT CONDITION on both indirection and re-parse constants plus a version-witness assertion that goes red on a git upgrade and cannot skip; the `GH_API_VALUE_OPTS` two-sided pin with both negative controls; `FORGE_VALUE_OPTS`'s `--hostname` KEPT with its measured asymmetric reason recorded; and the disclosed over-refusal family documented beside `T-19-74`, `T-19-75`, `T-19-86`, `T-19-91` and `T-19-96`"
      contains: "REPARSED_COMMAND_SECTIONS"
    - path: "src/envelope/cred.rs"
      provides: "the corrected `hooks_path_env` limit paragraph — a statement of the REGIONS each closure covers and what is NOT covered, replacing the counted five-form table — and NOTHING ELSE in the file, with zero non-doc lines changed"
      contains: "region"
    - path: "tests/envelope_reparsed_value.rs"
      provides: "ADDITIONS ONLY — new `#[test]` fns pinning the two verdicts `19-24` measured but could not derive, each with the clause that produced it: `git --config-env alias.q=BODYVAR status` in its separate-word spelling, and `git -c alias.q status` with no `=`"
      contains: "derived"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-25 execution record, carrying the statement FIRST that the gate is not cleared and that audit 7's `!`-bodied pair still works, `19-24`'s RED rows with their before and after verdicts and identifiers, the design question answered with option (a) costed by measurement and rejected, the three fail-open directions stated and handed to no control, the two-region rule with the `include`/`alias` persistence asymmetry that makes region 2 required and an include clause there inert, the `T-19-108` scoped closure with its boundary stated, the `cred.rs` region correction recorded as a named exception, the `RESTORATION` correction recorded BESIDE the 19-22 and 19-23 subsections, the `T-19-106` remainder with both pins and the measured `FORGE_VALUE_OPTS` regression, the revisit condition and its stated cost with no acceptance made, the over-refusal cost measured from both sides, the `T-19-17r` gap still OUTSTANDING, and the statement that `T-19-86` and `T-19-91` remain open at `high`"
      contains: "19-25"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-105`, `T-19-106` and `T-19-109` updated to record their closure and which clause closed each; `T-19-108` recorded CLOSED AS SCOPED with the `!`-bodied arm explicitly still open under `T-19-86`; `T-19-86` (recorded WIDER, including its persisted-alias arm), `T-19-91`, `T-19-96`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open"
      contains: "T-19-108"
  key_links:
    - from: "an assignment whose key names a value git RE-PARSES as a command line"
      to: "`ParkReason::EnvelopeAssertionFailed` before any classifier and before the ledger write"
      via: "a SECTION comparison plus git's own one-byte shell rule, asked at the two decision regions that already read that word"
      pattern: "REPARSED_COMMAND_SECTIONS"
    - from: "an alias that only has to EXIST rather than win a precedence contest"
      to: "a clause at the `git config` key operand, where an include clause would be inert"
      via: "`classify_config`'s existing `key_operand()` region, the one that already refuses `git config core.hooksPath /dev/null`"
      pattern: "key_operand"
    - from: "a rule that refused every `alias.*` assignment"
      to: "a red test rather than two permanently red evidence files"
      via: "git's measured one-byte rule, `19-24`'s mechanical SHELL-ALIAS fence, and the `!`-bodied entries in `CONFIG_CONFINED_CARRIERS`"
      pattern: "shell_alias"
    - from: "a residue whose only control is a human remembering to read release notes"
      to: "a test that fires on a git upgrade and names what to re-derive"
      via: "a revisit condition in `AR-19-03`'s shape on both constants plus a non-skipping version-witness assertion, with its cost stated and no acceptance made"
      pattern: "revisit"
    - from: "a counted completeness claim that was wrong the day it was written"
      to: "a statement of REGIONS that stays true when a seventh form is found"
      via: "one narrow, named exception to the `cred.rs` fence, confined to the `hooks_path_env` limit paragraph and changing zero non-doc lines"
      pattern: "region"
---

<objective>
**The rules: a value the guard confined is still an input, and git re-parses exactly one kind of it.**

`19-24` widened the corpus, observed it RED and committed it in commits with zero `src/` hunks. This
plan writes the rules and is gated on that recorded RED state. **Its first action is confirming those
rows are still red** — if any is already green, the rules are rules nobody has shown are needed, and
that is a finding to report rather than a task to complete.

Audit 8 named the defect, the shape of its durable fix, and the order to take it in:

> **But it reads config assignments in ONE region — the leading-option region `scan_leading` walks —
> and git resolves configuration from a second place the guard hands it: a config VALUE the clause
> itself CONFINES.**

> **(a) — `T-19-108`, and the durable shape is a REGION rule, not a longer list of sections.** The
> clause already asks the right question; it asks it of the wrong extent.

## The design question, answered — option (b), with option (a) costed by measurement

**Option (a) — scan the confined value, applying the same clause recursively with a stated depth
bound — is REJECTED on three measured grounds, not on preference.**

1. **It needs a SECOND TOKENIZER.** Git splits an alias body with its own rules, not the shell's —
   measured, `git -c alias.m='config --get "core.hooksPath"' m` resolves. Implementing a foreign
   grammar the guard cannot observe is the exact class of unmeasured claim `T-19-106` and `T-19-107`
   were both registered for.
2. **It needs UNBOUNDED RECURSION.** Measured at depth 2:
   `git -c alias.a='-c alias.b="-c include.path=<f> config --get core.hooksPath" b' a` resolves
   `/INCLUDE_WINS`. Any stated depth bound leaves a fail-open residue at depth N+1 that an **attacker
   reaches today by adding one nesting level** — strictly worse than round 8's residue, which needs a
   future git release.
3. **It cannot read a body it does not have.** `--config-env=alias.q=<VAR>` delivers the body through
   an environment variable NAME, and `git -c alias.q status` has no value half at all. Option (a) must
   fall back to option (b) at exactly the carriers that matter, so it buys precision nowhere and costs
   a parser and a residue everywhere.

**Option (b) — ADOPTED.** An assignment whose key names a value git re-parses as a command line is one
the guard cannot bound, on exactly the footing the confinement clause already means.

## What else git re-parses from a config value — three kinds, one reaches layer 3

* **K1 — re-parsed as a GIT command line, IN-PROCESS, including its leading options.** `alias.<name>`
  with a non-`!` body. **The only member.**
* **K2 — re-parsed as a SHELL command line, executed as a CHILD that INHERITS the injection.**
  `alias.<name>` with a `!` body, `core.pager`, `pager.<cmd>`, `core.editor`, `sequence.editor`,
  `core.askPass`, `credential.helper`, `core.sshCommand`, `core.gitProxy`, `core.fsmonitor`,
  `core.alternateRefsCommand`, `diff.external`, `diff.<d>.command`, `diff.<d>.textconv`,
  `merge.<d>.driver`, `mergetool.<t>.cmd`, `difftool.<t>.cmd`, `filter.<n>.*`, `gpg.program`,
  `uploadpack.packObjectsHook`, `protocol.<n>.command`, `remote.<n>.uploadpack`/`.receivepack`,
  `trailer.<t>.command`, `submodule.<n>.update`, `web.browser`/`browser.<t>.cmd`. **Measured INERT**:
  `git -c alias.b='!git config --get core.hooksPath' b` prints `/ENV_WINS`.
* **K3 — re-parsed as a FILE PATH spliced at the directive's precedence.** Closed by `19-23`.

So the new constant names **one section**, and it names it because that section is where K1 lives.

## The rule, exactly

A second constant beside `INDIRECTION_SECTIONS`, a second predicate mirroring
`config_key_names_an_indirection_section` exactly — the text before the FIRST `.`, `eq_ignore_ascii_case`,
`None` answering `false` — and **one byte of the value**: git treats a body whose first byte is `!` as a
shell command run in a CHILD that inherits the injection, and anything else as a command line it
re-parses in-process. `19-24` measured that in four spellings. **A value the guard cannot read the first
byte of is UNBOUNDED and refuses.**

Asked at **two decision regions that already exist and already read the same word**: `scan_leading`'s
assignment block, and `classify_config`'s `key_operand()` — which already refuses
`git config core.hooksPath /dev/null` at `hook_bypass_blocked`. **Neither is a new reading site**, and
if the fix needs a third, that is a finding.

**Region 2 is required, and the asymmetry with `include.path` is why.** A persisted include must WIN a
precedence contest and loses to the injection — audit 8 measured it INERT at repo-local, `--worktree`
and GLOBAL. **An alias only has to EXIST**, and `19-24` measured both a `--global` and a repo-local
alias live under the envelope's own config posture. An `include.path` clause at region 2 would be inert
over-refusal and must not be added.

## And the honest part — which direction this rule fails

**It fails OPEN, in three named directions, and not one of them has an automated control.**

1. **An `alias.*` already present in a config file the guard never saw a write to.** The guard is
   stateless and argv-only; `git co` is an unknown verb at exit 0 and stays there. A repo-local
   `.git/config` alias predating the run is live. That is `T-19-86`'s shape and it stays open.
2. **A `!`-bodied body carrying its own carrier.** Audit 7's destructive pair rewrote a bare remote's
   `main` at `fb43577` and **still works after this plan**, because closing it means taking on
   `T-19-86`, which is out of scope by explicit user decision and whose rows are pinned PERMITTED in
   files this plan may not edit.
3. **A future git that re-parses a SECOND config value as a git command line with its own leading
   options.** Same shape as `INDIRECTION_SECTIONS`'s residue, same absence of control.

**All three go in the docs and the record, in those words, and none is handed to a pin.** Round 7's
clause hid exactly this behind a pin and a plan-check caught it; that is not repeated here.

## The cost, measured from both sides — and ordinary aliases do NOT all keep working

**Defining a non-shell alias is refused; using one is not.** `git -c alias.st=status st`,
`git -c alias.lg="log --oneline" lg`, `git -c alias.co=checkout co` and `git config alias.co checkout`
are refused after, and `19-24` pinned each beside the permitted twin that does the same work.
`git p`, `git co` and `git q` — INVOCATION — stay at exit 0. Round 8's whole permitted half stays
green, and round 7's generative property stays green behind `CALLEE_KNOWN_LEADING_PREFIX = "-c a=b"`,
because a dotless key names no section and is CONFINED.

## What this plan is NOT

**`/gsd-secure-phase 19` is NOT cleared by this plan.** `T-19-86` remains open at `high` — recorded
WIDER by audit 8 — and `T-19-91` remains open at `high`. **`T-19-108` closes only AS SCOPED**, and
audit 7's `!`-bodied destructive pair still works. The SUMMARY must say so first, and must never write
an unqualified "T-19-60 is closed".

Purpose: make what git does with a value the guard approved a fact the guard establishes rather than
one it inherits.
Output: the re-parse clause at two existing regions, a revisit condition and a version witness for both
residues, a corrected claim in `cred.rs`, the `GH_API_VALUE_OPTS` pin, the `FORGE_VALUE_OPTS` reason,
the appended execution record, and a green `--no-fail-fast` gate whose arithmetic is stated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-24-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@src/envelope/cred.rs
@tests/envelope_reparsed_value.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm still RED, then the RE-PARSE clause at region 1 — `scan_leading`</name>
  <files>src/envelope/policy.rs</files>
  <precondition>
    `19-24` is committed and its SUMMARY records the complete RED name list and the `passed + failed`
    total. Work in the main checkout with a clean tree — ISOLATION=none is forced for this phase, so do
    NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`. A real `git`
    binary is on `PATH` and `gh` 2.45.0 is installed; the pins' probes require both and MUST NOT be
    written to skip when either is absent, because a skipped pin is a fail-open pin. `glab` is NOT
    installed and no pin may depend on it.
  </precondition>
  <read_first>
    `19-24-SUMMARY.md` in full — the RED name list is this plan's handoff contract, and the recorded
    K1/K2/K3 enumeration, the one-byte boundary measurements, the persistence asymmetry and the depth-2
    and alias-body-quoting facts are what the clause is built from rather than from this plan's text.
    `src/envelope/policy.rs:402-533` (`scan_leading` — its one-scan-two-callers doc, the `-` and `--`
    breaks, and **the `if let Some(assignment)` block at `:425-528`: the rewriting-key refusal at
    `:466-480`, the confinement clause at `:481-514` whose channel, identifier and comment structure the
    new clause mirrors, and `is_hooks_path_key` at `:515-527` which the new clause is ordered BEFORE**),
    `:548-565` (`push_needs_resolved_dests`, the second caller, which must inherit the refusal exactly
    as it inherits the existing three), `:732-840` (`leading_git_option` — **arm (a) returns the NEXT
    token as the assignment for a bare `-c`/`--config-env` and arm (b) the attached one, so BOTH
    carriers reach the same key check and the clause needs no carrier-specific arm**), `:842-857`
    (`config_key_of` — **it returns the WHOLE token when there is no `=`, which is the no-value case
    that must fail CLOSED**), `:855-1008` (`INDIRECTION_SECTIONS` with its residue paragraph and the
    absence-of-control sentence, `config_key_section`, `config_key_names_an_indirection_section` and
    `unbounded_config_assignment_refusal` — **the exact shape to mirror, including the rejected-options
    block and the disclosed-cost paragraph**), `:1010-1030` (`is_hooks_path_key` and its
    more-permissive-than-git note), `:226-241` (`classify_git`'s doc — **the REVISIT CONDITION shape:
    a named concrete trigger and what to do when it fires**).
  </read_first>
  <action>
**FIRST, and before any production line moves: confirm `19-24`'s RED set is still RED.** Re-run
`rtk proxy cargo test --test envelope_reparsed_value --no-fail-fast` and
`rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast` against the unmodified tree and
record the failing names verbatim. Diff that list against `19-24-SUMMARY.md`'s recorded RED set. **If
any row is already green, STOP and report it as a finding** with the measured verdict and reason
identifier — a rule certified by a corpus that could not fail on its class is the defect nine rounds of
this phase have been about. Put the verbatim pre-fix output in the SUMMARY.

**Then the clause, at region 1.**

**1. `REPARSED_COMMAND_SECTIONS`, a SECOND constant beside `INDIRECTION_SECTIONS`, never an entry in
it.** It names the config sections whose VALUE git re-parses as a **git command line including its own
leading options**. One entry: `alias`. Its doc carries, in the shape `INDIRECTION_SECTIONS`'s already
uses:

  - **the measured K1/K2 split, in both directions**, with the envelope's own triplet as the control:
    a non-`!` `alias.<name>` body carrying `-c include.path=<f>` resolves `/INCLUDE_WINS` in-process,
    while a `!` body runs in a CHILD that INHERITS the injection and resolves `/ENV_WINS`. **That is
    why the constant names one section and the clause reads one byte**, and it is why the K2 family —
    `core.pager`, `core.editor`, `credential.helper`, `diff.external` and the rest, enumerated in
    `19-24`'s record — needs no entry: each executes a child that inherits;
  - **the RESIDUE, stated plainly and handed to no control**, in the same voice
    `INDIRECTION_SECTIONS`'s residue paragraph uses: **this constant is a recognition of a closed
    grammatical fact, not a fail-closed default; its SILENCE IS A PERMIT**; a future git that re-parses
    a SECOND config value as a git command line is **not covered, this rule fails OPEN on it, and there
    is NO automated control over that direction**;
  - **the two OTHER fail-open directions**, which are this constant's own and must not be omitted: an
    `alias.*` already present in a config file the guard never saw a write to (the guard is stateless
    and argv-only, `git co` stays at exit 0, and a repo-local `.git/config` alias predating the run is
    live — `T-19-86`'s shape), and a `!`-bodied body carrying its own carrier, **which leaves audit 7's
    destructive pair working after this round** and is `T-19-86`, out of scope;
  - **the REVISIT CONDITION, in `AR-19-03`'s shape** (`policy.rs:238-241`): a named, concrete trigger
    and what to do when it fires — this constant and `INDIRECTION_SECTIONS` are both claims about a
    specific git version; when the installed `git --version` differs from the one recorded here, both
    are re-derived against the new git and the recorded version is updated. **Add the same revisit
    condition to `INDIRECTION_SECTIONS`'s doc**, which is the gap audit 8 named: its residue is
    correctly disclosed and correctly unclaimed, and its only control was a human remembering.

**2. `config_key_names_a_reparsed_command_section`, mirroring
`config_key_names_an_indirection_section` exactly.** `config_key_section`'s answer only, compared with
`eq_ignore_ascii_case`; `None` answers `false`, so a dotless key is CONFINED and reaches the existing
clauses as today. Its doc states why the SUBSECTION and the VARIABLE are never read — `alias` has no
subsection and every variable in it is an alias name, so reading either would import an open family for
nothing — and it repeats the dotless fence with `CALLEE_KNOWN_LEADING_PREFIX` named.

**3. The VALUE test, and it reads exactly one byte.** A helper whose doc records git's own rule and
`19-24`'s four measured spellings: first byte `!` → shell command in a child that inherits the
injection → **CONFINED**; anything else → re-parsed in-process at command-line precedence →
**UNBOUNDED**; a `!` that is not first is not a shell body; a leading space makes git refuse to expand
at all. **A value the guard cannot read — no `=` in the assignment, or a `--config-env` carrier whose
value is an environment variable NAME — is UNBOUNDED and refuses**, which is the fail-closed direction
and completes the clause over both carriers `leading_git_option` returns. The doc says plainly that the
`!` arm is a carve-out for `T-19-86`'s registered rows, names
`tests/envelope_command_position.rs:550` and `tests/envelope_config_resolution.rs:1539-1543`, and
states that it leaves audit 7's destructive pair working.

**4. The clause, placed beside the confinement clause and BEFORE `is_hooks_path_key`.** Same channel,
same `ParkReason::EnvelopeAssertionFailed`, **no new variant**, no second pass, no new reading site.
Its comment records the ordering as load-bearing exactly as the confinement clause's does: the refusal
is raised at the FIRST assignment the loop cannot bound and no assignment after it is read, which is
why `19-24`'s two ordering rows are pinned at deliberately different identifiers and why a
second-pass implementation turns one of them red.

**5. A refusal helper of its own, naming THIS mechanism.** Never quote the command back (SAFE-04);
name the KEY and the SECTION. The message says what the guard could not establish — this key's section
names a value git re-parses as a command line including its own leading options, so what
`core.hooksPath` will be while that command runs cannot be established from this line — and gives a
recovery step **git accepts**: run the command the alias would have run. **It must not reuse
`unbounded_config_assignment_refusal`'s wording**: a message about splicing a file on a line that
splices no file attributes the refusal to a mechanism that did not produce it (D-24), the defect
`19-21`'s executor reported as a blocker.

**6. The pins, in `policy.rs`'s own `#[cfg(test)] mod tests`** — never above the sentinel at line
4,614, and never introducing a second `#[cfg(test)]` line:

  - a **two-sided real-git pin** over `REPARSED_COMMAND_SECTIONS` in the shape
    `INDIRECTION_SECTIONS`'s pin uses: for each entry, an alias in that section carrying
    `-c include.path=<f>` makes the control print the INCLUDED value, and a `!`-bodied one leaves it at
    the control's value. **Its own doc must open by stating the direction it holds** and must say in
    terms that it is **NOT** a control over the fail-open residue, because it cannot observe a section
    it does not name — the sentence `19-23`'s pin doc already carries, and the one a plan-check caught
    an earlier draft omitting;
  - the **VERSION-WITNESS assertion**: the `git --version` both constants were derived against is
    recorded as a constant, the installed version is read, and a mismatch is a FAILURE whose message
    says to RE-DERIVE both constants against the new git and update the recorded version — never to
    delete the assertion. **It must not skip, warn-without-failing, or pass when `git` is absent.** Its
    doc states the cost plainly: it fires on every git upgrade, including harmless ones, **and that is
    the schedule** — the thing that turns "a human reads the release notes" into "a test fires";
  - unit pins on the predicate: `alias.q`, `ALIAS.q` and `alias.st` answer `true`; `aliasx.q`,
    `notalias.q`, `include.path`, `core.hooksPath`, `user.name` and the dotless `a` answer `false`.

Commit as its own commit, the first fix commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_reparsed_value --no-fail-fast 2&gt;&amp;1 | tail -30; rtk proxy cargo test --lib --no-fail-fast 2&gt;&amp;1 | tail -20; for t in envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || exit 1; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c '^#\[cfg(test)\]' src/envelope/policy.rs; rtk proxy grep -n 'const SEPARATORS' src/envelope/policy.rs; git diff --stat HEAD~1..HEAD</automated>
  </verify>
  <done>`19-24`'s complete RED set was re-run against the unmodified tree BEFORE any production line moved, the failing names recorded verbatim and diffed against `19-24-SUMMARY.md`, with any already-green row reported as a finding rather than proceeded past; `REPARSED_COMMAND_SECTIONS` exists as a SECOND constant beside `INDIRECTION_SECTIONS` naming one section, with the measured K1/K2 split in both directions, the reason the K2 family needs no entry, the residue stated as failing OPEN with NO automated control, the two other fail-open directions stated including that audit 7's `!`-bodied pair still works, and a REVISIT CONDITION in `AR-19-03`'s shape — **added to `INDIRECTION_SECTIONS`'s doc as well**; `config_key_names_a_reparsed_command_section` mirrors its sibling exactly, reads the SECTION only with `eq_ignore_ascii_case`, and answers `false` for a dotless key with `CALLEE_KNOWN_LEADING_PREFIX` named; the VALUE test reads exactly the FIRST BYTE, implements git's own rule with `19-24`'s four measured spellings in its doc, treats an unreadable value as UNBOUNDED, and names both files that pin a `!` body permitted; the clause sits beside the confinement clause and BEFORE `is_hooks_path_key`, in the same block, on the same channel, at `ParkReason::EnvelopeAssertionFailed` with NO new variant, no second pass and no new reading site; its refusal helper names THIS mechanism and a recovery step git accepts, and does not reuse the include wording; the two-sided real-git pin exists with its doc opening on the direction it holds and stating it is NOT a control over the residue; the VERSION-WITNESS assertion fails on a version mismatch, cannot skip when git is absent, and states its cost; the predicate's unit pins cover both directions; `policy.rs` still has exactly ONE `#[cfg(test)]` line and `SEPARATORS` is unchanged at `:1786`; the six prior evidence files are GREEN and unmodified; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: region 2 — the persisted write at `classify_config`, and the two rows `19-24` could not derive</name>
  <files>src/envelope/policy.rs, tests/envelope_reparsed_value.rs</files>
  <read_first>
    `src/envelope/policy.rs:1340-1430` (`scan_config`, `ConfigScan`, `CONFIG_READ_OPTS`,
    `CONFIG_WRITE_OPTS`, `CONFIG_VALUE_OPTS`, `CONFIG_WRITE_SUBCOMMANDS`, `CONFIG_READ_SUBCOMMANDS`,
    `key_operand()`, `key_operand_count()` and `config_key_operand_index` — **the index primitive
    `first_unreadable_decision_word` shares, which is why region 2 is not a new reading site**),
    `:1429-1468` (`classify_config` — the `whole_file_write` arm, the `is_write && !is_read` gate and
    **the `is_hooks_path_key(key)` refusal at `:1453-1465`, beside which the new question goes**),
    `:4111-4194` (`first_unreadable_decision_word`'s `config` arm at `:4185-4193`, which computes its
    decision region from `config_key_operand_index` — **read to confirm no change is needed there and
    to report it as a finding if one is**). `19-24-SUMMARY.md`'s persistence-asymmetry table and its
    two recorded-not-asserted rows. `tests/envelope_reparsed_value.rs`'s section 9 — the two rows
    RECORDED rather than asserted, whose pins this task ADDS as new `#[test]` fns.
  </read_first>
  <action>
**1. The same question at region 2, and only the re-parse half of it.** In `classify_config`, beside
the existing `is_hooks_path_key(key)` refusal and under the same `is_write && !is_read` gate, ask
`config_key_names_a_reparsed_command_section(key)` on the key operand and apply the same FIRST-BYTE
test to the VALUE operand. A write persisting a non-`!` alias body is refused at
`ParkReason::EnvelopeAssertionFailed` with the same refusal helper Task 1 added; a `!`-bodied one is
PERMITTED, because `tests/envelope_config_resolution.rs:1539-1543` pins it as a `T-19-86` row and this
plan may not edit that file. A **read** — `git config --get alias.p` — stays permitted, because reading
an alias establishes nothing about what a later command runs under.

**MUST NOT add an `include.path` or `includeIf` clause here.** Audit 8 measured persisted
`git config include.path <evil>` INERT at repo-local, `--worktree` and GLOBAL — the envelope's
injection outranks it — and stated explicitly that it correctly needs no clause. **The asymmetry is the
point and the doc must carry it**: an include has to WIN a precedence contest and loses; **an alias only
has to EXIST**, and `19-24` measured a `--global` and a repo-local alias both live under the envelope's
own `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` posture. Adding an inert clause in a round whose thesis is
that a rule's reach must be derived from a measurement would be that thesis broken by its own round.

**2. Confirm region 2 is not a new reading site, and report it as a finding if it is.** `classify_config`
already reads this operand with `is_hooks_path_key` and `git config core.hooksPath /dev/null` is
measured refused at `hook_bypass_blocked`; `config_key_operand_index` is the index primitive
`first_unreadable_decision_word` computes its `config` decision region from. Verify mechanically that
`first_unreadable_decision_word` needs no change, that no index primitive moves, that
`subcommand_word_indices` and `scan_gh_api` are untouched, and that `hooks.rs` is not opened. **If the
fix needs a third site, stop and report it as a finding.**

**3. Then MEASURE and PIN the two rows `19-24` recorded but could not derive**, as new `#[test]` fns in
`tests/envelope_reparsed_value.rs` — **ADDITIONS ONLY, and never folded into an existing fn**, because
new fns are the only thing that makes this plan's gate arithmetically satisfiable:

  - `git --config-env alias.q=BODYVAR status` in its SEPARATE-WORD spelling — measure it against the
    built binary after the clause exists and pin what was measured, with the clause that produced it
    named beside it;
  - `git -c alias.q status` with no `=` at all — `config_key_of` returns the whole token, so there is
    no value half; measure and pin, naming which clause raised the fail-closed answer.

Each pin's comment states that `19-24` measured these pre-fix and RECORDED them rather than asserting
them, and that this task pins what was measured post-fix rather than what was hoped — the discipline
`19-22` broke and `19-23` halted on.

**4. Add the region-2 verdict pins** as new `#[test]` fns in the same file: the persisted non-`!` write
refused in its bare, `--global`, `--worktree`, `--file`, `--add` and `--replace-all` spellings; the
persisted `!` write PERMITTED with the `T-19-86` pin it mirrors named; `git config --get alias.p`
permitted as a read; and `git config include.path /tmp/evil.cfg` **PERMITTED**, with the comment
recording audit 8's INERT measurement as the reason a clause there would be inert over-refusal.

Commit as its own commit, the second fix commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_reparsed_value --no-fail-fast 2&gt;&amp;1 | tail -30; rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -30; rtk proxy cargo test --test envelope_pr_cap --test envelope_wiring &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git diff --numstat HEAD~2..HEAD -- tests/ | tail -8; git diff --stat HEAD~1..HEAD -- src/envelope/hooks.rs | wc -l; git log --oneline -2</automated>
  </verify>
  <done>`classify_config` asks `config_key_names_a_reparsed_command_section` on the key operand under the existing `is_write && !is_read` gate with the same FIRST-BYTE test on the value operand, refusing a persisted non-`!` alias body at `EnvelopeAssertionFailed` with Task 1's helper, PERMITTING a persisted `!` body with the `T-19-86` pin it mirrors named in the comment, and PERMITTING a read; **no `include.path` or `includeIf` clause was added at region 2**, with the persistence asymmetry recorded in the doc and audit 8's INERT measurement cited as the reason; region 2 was confirmed mechanically not to be a new reading site — `first_unreadable_decision_word` unchanged, no index primitive moved, `subcommand_word_indices` and `scan_gh_api` untouched, `hooks.rs` not opened — and a third site would have been reported as a finding; the two rows `19-24` recorded but could not derive are MEASURED against the built binary after the clause exists and pinned at what was measured, each as a NEW `#[test]` fn with the clause that produced it named; the region-2 verdict pins exist as new `#[test]` fns covering the bare, `--global`, `--worktree`, `--file`, `--add` and `--replace-all` write spellings, the `!` write, the read and the permitted `git config include.path`; every test write is an ADDITION and `git diff --numstat` over `tests/` shows ZERO deletions; and this is the second fix commit.</done>
</task>

<task type="auto">
  <name>Task 3: `T-19-109`'s region correction, and `T-19-106`'s remaining pin and kept entry</name>
  <files>src/envelope/cred.rs, src/envelope/policy.rs</files>
  <read_first>
    `src/envelope/cred.rs:224-275` — `hooks_path_env`'s four-things block, **the limit paragraph at
    `:241-272` carrying "FIVE forms outrank this injection" and its five-row table, which is the ONLY
    block this plan may open** — and `:277-300` (`write_gitconfig`'s doc, read to understand the
    generated helper-free file both `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` point at; **do not
    edit**). `src/envelope/policy.rs:4322-4400` (`FORGE_VALUE_OPTS` at `:4328` with its
    under-counting doc, `subcommand_word_indices` at `:4346`, `GH_API_VALUE_OPTS` at `:4374` and
    `GH_API_IMPLIES_POST` at `:4399`), and the `CONFIG_VALUE_OPTS` two-sided probe `19-23` wrote —
    **the shape this task's `gh` probe mirrors, including its two negative controls**.
  </read_first>
  <action>
**1. `T-19-109` — replace the COUNT with the REGIONS, in `cred.rs` and nowhere else.**

The paragraph currently claims **"FIVE forms outrank this injection"** with a five-row table. Measured,
there is a sixth — a non-`!` `alias.<name>` body beginning `-c include.path=<file>` — and the claim was
wrong the day it was written. **A counted completeness claim is strictly harder to keep true than the
sentence it replaced**, and this is the second correction of that paragraph, which is `T-19-84`'s,
`T-19-107`'s and `T-19-109`'s shared shape.

Rewrite it as a statement of the **REGIONS** each closure covers, counting nothing:

  - the **argv leading-option region** `scan_leading` walks — the by-name `core.hooksPath` deny (plan
    19-02), the confinement clause over indirection sections (`19-23`), and the re-parse clause over
    sections whose value git re-parses as a command line (this round);
  - the **environment** — `ENVELOPE_ENV_KEYS` and its second, defeating-keys pin source (`19-23`);
  - the **`git config` write operand** — the by-name deny and this round's re-parse clause;
  - and, stated in the same paragraph, **what is NOT covered**: a `!`-bodied alias body carrying its
    own carrier, an alias already persisted in a config file the guard never saw a write to, a future
    git that adds a third indirection section, and a future git that re-parses a second config value as
    a command line. **None of these has an automated control**, and the paragraph says so.

Keep the closing sentence that the closures are client-side and are not guarantees, and that
server-side branch protection is the only boundary that does not depend on the agent's cooperation.
**Change ZERO non-doc lines**, verified by `git diff --unified=0` over the file filtered for lines that
are not `///` returning NOTHING. **`T-19-61` … `T-19-73` are NOT taken on.**

**The "RESTORATION of layer 3's catch" claim is NOT edited where it appears.** It is in the 19-22
record, the 19-23 record and `19-23-SUMMARY.md`, it is true only for an alias body carrying no carrier
of its own, and the correction belongs BESIDE those subsections in Task 4's appended record — the
provenance discipline audits 7 and 8 both used for exactly this shape.

**2. `T-19-106` — the pin that can run, and the entry that must be kept.**

**Write the `GH_API_VALUE_OPTS` two-sided pin.** `gh` 2.45.0 is installed and all seventeen entries
were re-measured while planning answering ``flag needs an argument``. The pin drives real `gh` for each
entry and requires that answer, and it carries **BOTH negative controls that pin the classifier
itself**: `--bogus-opt` must come back **unknown**, and `--paginate` — an existing flag that needs no
value — must come back **existing-and-not-requires-value**. Without the second, the pin passes on a
probe reporting *requires a value* for everything, which is the vacuity `19-23`'s `CONFIG_VALUE_OPTS`
redesign exists to exclude. **It must not skip when `gh` is absent.** Its doc records that the reason
this pin was absent — that `glab` is not installed — was a fact about a different callee, which is
audit 8's finding.

**KEEP `--hostname` in `FORGE_VALUE_OPTS` and RECORD WHY.** This is a correction to audit 8's own
suggestion and it is measured: at `21655c5`, `glab --hostname gitlab.com mr create --title x` leaves
exactly ONE ledger line, and an option NOT in that constant leaves ZERO — so removing it moves a
counted creation form to **UNCOUNTED**, the under-counting direction the cap exists to prevent
(`T-19-35`). **Re-measure both before writing; if the measurement disagrees, report it as a finding
rather than writing this paragraph.** The doc records the asymmetry in terms: `gh pr create` answers
``unknown flag: --hostname``, so the entry is **inert for `gh`** and cannot mis-index a `gh` command
that runs; it is **load-bearing for `glab`**, whose callee is unconfirmed because `glab` is not
installed; and the correct disposition is therefore to keep it with the reason recorded rather than to
remove it as a stale entry of the `--comment` / `--super-prefix` shape. Pin the `gh` half two-sided —
`-R` and `--repo` require a value, `--hostname` is unknown — and state plainly that the `glab` half
**cannot be pinned on this machine and is not**, because a pin that skips is fail-open.

`subcommand_word_indices` is not otherwise touched, the `glab --host` cell is carried forward unfixed,
and `T-19-106` is closed only when both halves above are done.

Commit as its own commit, the third fix commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --lib --no-fail-fast 2&gt;&amp;1 | tail -20; rtk proxy cargo test --test envelope_pr_cap --test envelope_credential --no-fail-fast 2&gt;&amp;1 | tail -20; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git diff --stat HEAD~1..HEAD -- src/envelope/cred.rs; echo "NON-DOC CRED LINES (must be 0):"; git diff --unified=0 HEAD~1..HEAD -- src/envelope/cred.rs | rtk proxy grep -E '^[+-]' | rtk proxy grep -Ev '^(\+\+\+|---)' | rtk proxy grep -Evc '^[+-][[:space:]]*///'; rtk proxy grep -c '"--hostname"' src/envelope/policy.rs</automated>
  </verify>
  <done>`cred.rs`'s limit paragraph states the REGIONS each closure covers — the argv leading-option region, the environment and the `git config` write operand — and what is NOT covered, with all four uncovered directions named and stated to have no automated control, **counting nothing**; the closing client-side/branch-protection sentence is kept; ZERO non-doc lines changed, verified by `git diff --unified=0` filtered for lines that are not `///` returning NOTHING, and no other item in the file was opened; the "RESTORATION of layer 3's catch" claim was NOT edited where it appears and its correction is left for Task 4's record BESIDE those subsections; the `GH_API_VALUE_OPTS` two-sided pin drives real `gh` over all seventeen entries requiring `flag needs an argument`, carries BOTH negative controls (`--bogus-opt` unknown, `--paginate` existing-and-not-requires-value), does not skip when `gh` is absent, and records that the reason for its earlier absence was a fact about a different callee; **`--hostname` is KEPT in `FORGE_VALUE_OPTS`** with the measured regression recorded — one ledger line with it, zero without — the `gh`/`glab` asymmetry stated, the `gh` half pinned two-sided, and the `glab` half stated plainly as unpinnable on this machine; `subcommand_word_indices` is otherwise untouched and the `glab --host` cell is carried forward unfixed; and this is the third fix commit.</done>
</task>

<task type="auto">
  <name>Task 4: the record, the registrations, and the gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s "Threats found by audit 8", "What audit 8 measured and found INERT", "The
    fail-open residue", "The four execution-time judgements", "Audit 8 — what the round-8 control can
    and cannot fail on" and "Audit 8 Sign-Off", plus the appended records of plans 19-22, 19-23 and
    19-24, **which this plan's subsection FOLLOWS and does not edit**. `19-24-SUMMARY.md` in full.
    `deferred-items.md`'s round-9 registration section written by `19-24`.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-24 record and touching nothing
that precedes it, clearly attributed as a plan-19-25 execution record, carrying:

  - **FIRST: `/gsd-secure-phase 19` is NOT cleared**, `T-19-86` and `T-19-91` remain OPEN at `high`,
    **`T-19-108` closes only AS SCOPED**, and **audit 7's `!`-bodied destructive pair — the one that
    rewrote a bare remote's `main` from `ac303dc` to `9f62444` at `fb43577` — still works after this
    plan**, because closing it means taking on `T-19-86`;
  - **`19-24`'s RED rows, before and after**, each with its verdict, its reason identifier and the
    clause that produced it, and the verbatim pre-fix failing output recorded beside the post-fix
    green;
  - **the design question answered, with option (a) COSTED BY MEASUREMENT and rejected** — the second
    tokenizer, the depth-2 measurement that makes any stated depth bound an attacker-reachable residue,
    and the carriers that deliver no readable body — and option (b) adopted with its one-byte value
    test derived from git's own rule;
  - **the K1/K2/K3 enumeration**, with the statement that `alias.<name>` with a non-`!` body is K1's
    only member and that K2 is inert because the child inherits the injection;
  - **the two-region rule**, with the `include`/`alias` persistence asymmetry stated as the reason
    region 2 is required and the reason an `include.path` clause there would be inert;
  - **the three fail-open directions, stated in those words and handed to NO control**, with the note
    that this is the correction round 7's clause needed and that a plan-check caught an earlier draft
    hiding a residue behind a pin;
  - **the revisit condition and the version witness**, with the cost stated plainly — it fires on every
    git upgrade, including harmless ones, and that is the schedule — and the explicit statement that
    **no acceptance was made and no `AR-` row was added**;
  - **the over-refusal cost measured from BOTH sides**, with every refused alias-definition row beside
    its permitted twin and every invocation row recorded unchanged, and the honest sentence: ordinary
    aliases do not all keep working — defining a non-shell one is refused, using one is not;
  - **the `cred.rs` correction recorded as a NAMED, NARROW exception** with the zero-non-doc-lines
    measurement, and **the "RESTORATION of layer 3's catch" correction recorded BESIDE the 19-22 and
    19-23 subsections and `19-23-SUMMARY.md` rather than as an edit to any of them**;
  - **`T-19-106`'s two halves**, the seventeen-entry `gh` pin with both negative controls, and the
    measured `FORGE_VALUE_OPTS` regression that makes "keep and record why" the correct disposition —
    recorded as a correction to audit 8's own suggestion, with the measurement beside it;
  - **`T-19-86` recorded WIDER and NOT closed**, `T-19-91`, `T-19-96` and `T-19-74` untouched, the
    `glab --host` cell carried forward unfixed, and **the `T-19-17r` gap still OUTSTANDING with the
    acceptance deliberately unmade for the fifth round running**;
  - **the byte floors and all three stripper protections, re-measured**, and the confirmation that
    `policy.rs` still carries exactly ONE `#[cfg(test)]` line;
  - **what this record does NOT do**: it re-measures and re-classifies nothing in an audit's tables,
    which is `/gsd-secure-phase 19`'s job.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Record `T-19-105`, `T-19-106` and `T-19-109` CLOSED with the clause that
closed each; record **`T-19-108` CLOSED AS SCOPED** with the `!`-bodied arm explicitly still open under
`T-19-86` and audit 7's pair recorded as still working; leave `T-19-86` (recorded WIDER, including its
persisted-alias arm), `T-19-91`, `T-19-96`, `T-19-74`, the `glab --host` forge cell and the `T-19-17r`
bookkeeping gap unchanged and open.

**3. The gate.**

  - `rtk proxy cargo test --no-fail-fast`, counts read with **`rtk proxy grep`** over a redirected log
    (D-34).
  - **State the arithmetic explicitly.** `passed + failed` equals the exact total `19-24-SUMMARY.md`
    records plus the number of NEW `#[test]` fns this plan adds, counted from `git show`. **A red test
    RAN, so red→green leaves the total unchanged** and every increase comes only from new fns. A
    disagreement is a finding.
  - Every failure name is one of the three documented flakes; **`19-24`'s entire RED set is now GREEN**
    and every name is listed.
  - All FIFTEEN `envelope_*` binaries RAN, with per-binary counts recorded.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.

Commit the record as its own commit, the fourth.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-25-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -c '^| AR-19-13 |' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-25-gate.log | tail -50; rtk proxy grep -c 'Running tests/envelope_' /tmp/19-25-gate.log; git diff --numstat HEAD~4..HEAD -- tests/ | tail -8; git diff --stat HEAD~4..HEAD | tail -12; git log --oneline -4</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-25 record stating FIRST that the gate is not cleared, that `T-19-108` closes only AS SCOPED and that audit 7's `!`-bodied destructive pair still works, and carrying `19-24`'s RED rows before and after with the verbatim pre-fix output, the design question answered with option (a) costed by measurement and rejected, the K1/K2/K3 enumeration, the two-region rule with the persistence asymmetry and the reason an include clause at region 2 would be inert, the three fail-open directions stated in those words and handed to no control, the revisit condition and version witness with their cost stated and no acceptance made, the over-refusal cost from both sides with the honest sentence about ordinary aliases, the `cred.rs` correction as a named narrow exception with its zero-non-doc-lines measurement, the "RESTORATION" correction recorded BESIDE the 19-22 and 19-23 subsections rather than as an edit, `T-19-106`'s two halves with the seventeen-entry probe and the measured `FORGE_VALUE_OPTS` regression as a correction to audit 8's suggestion, `T-19-86` recorded WIDER and not closed, the `glab --host` cell unfixed, the `T-19-17r` gap OUTSTANDING with the acceptance deliberately unmade, and the re-measured floors and three stripper protections — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection, and the Accepted-Risks-Log row count for that risk ID still zero; `deferred-items.md` records `T-19-105`, `T-19-106` and `T-19-109` CLOSED and `T-19-108` CLOSED AS SCOPED with its `!`-bodied arm still open under `T-19-86`, leaving `T-19-86`, `T-19-91`, `T-19-96`, `T-19-74`, the forge cell and the `T-19-17r` gap unchanged; the `--no-fail-fast` run reports `passed + failed` equal to `19-24`'s recorded total plus the new `#[test]` fns with the arithmetic stated and checked against `git show`, counted with `rtk proxy grep`, `19-24`'s entire RED set is GREEN with every name listed, and every remaining failure is one of the three documented flakes; all FIFTEEN `envelope_*` binaries RAN with per-binary counts recorded; `git diff --numstat` over `tests/` shows ZERO deletions across all four commits; and this is a fourth commit distinct from the three before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, and to what git does with a value the guard approved |
| the verb → the CONFIGURATION it runs under, in the ARGV and ENVIRONMENT regions | Rounds 8's two closed regions; audit 8 verified every row |
| **a value the guard CONFINED → what git does with it** | **This round's boundary. Git re-parses a non-`!` `alias.<name>` body as a command line INCLUDING its leading options, in-process, so a `-c include.path=` or `-c core.hooksPath=` inside the value is applied by git itself at command-line precedence** |
| **the guard's statelessness → a persisted alias** | **Two separately-permitted tool calls the guard cannot correlate. The only one it can decide on is the WRITE, which is why the rule is asked at `classify_config`'s key operand as well** |
| **a rule's REACH → a pin in a file it may not edit** | **A blanket `alias.*` refusal turns two registered `T-19-86` rows permanently red; git's one-byte shell rule is the fence and it is measured** |
| **a residue → the control that is supposed to cover it** | **Three fail-open directions, none with an automated control, all stated and none handed to a pin. Round 7's clause hid exactly this and a plan-check caught it** |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-108 | Elevation of Privilege | the CONFINEMENT clause's REACH — `config_key_names_an_indirection_section` (`policy.rs:976`) and `scan_leading` (`policy.rs:402`) read config assignments in the leading-option region only; a value the clause CONFINES carries the same indirection into a region the scan never reads | high | **mitigate — CLOSED AS SCOPED** | `REPARSED_COMMAND_SECTIONS` and `config_key_names_a_reparsed_command_section` beside `INDIRECTION_SECTIONS`, reading the SECTION plus git's own one-byte shell rule on the value, asked at BOTH existing decision regions — `scan_leading`'s assignment block and `classify_config`'s key operand — at `EnvelopeAssertionFailed` with no new `ParkReason`, no second pass and no third reading site. **Closed for the non-shell alias body in the `-c`, `--config-env` and persisted deliveries. NOT closed for a `!`-bodied body carrying its own carrier — that is `T-19-86`, and audit 7's destructive pair still works.** Three fail-open directions stated in the docs and the record and handed to no control |
| T-19-109 | Repudiation | `cred::hooks_path_env`'s counted "FIVE forms" table (`cred.rs:246-260`) and the "RESTORATION of layer 3's catch" claim in three documents | low | **mitigate — CLOSED** | The count is replaced by a statement of the REGIONS each closure covers and what is not covered, counting nothing, as a named narrow `cred.rs` exception changing ZERO non-doc lines. The "RESTORATION" correction is recorded BESIDE the 19-22 and 19-23 subsections rather than as an edit to them |
| T-19-106 | Spoofing | the drift pins' REACH — `GH_API_VALUE_OPTS` (`policy.rs:4374`) and `FORGE_VALUE_OPTS`'s `--hostname` (`policy.rs:4328`) | medium | **mitigate — CLOSED** | The seventeen-entry two-sided `gh` pin is written with BOTH negative controls and cannot skip. `--hostname` is KEPT with its measured reason: removing it moves a counted `glab` creation form to UNCOUNTED, the under-counting direction the cap exists to prevent — a correction to audit 8's own suggestion, measured rather than argued |
| T-19-105 | Spoofing | every generative alphabet, and `CONFIG_RESOLUTION_CLASSES`'s classes | medium | **mitigate — CLOSED** | Closed by `19-24`'s sixth class certifying these rules, which is what a corpus is for: it failed on this class before any production line moved |
| — | Denial of Service | the fail-open residues of `INDIRECTION_SECTIONS` and `REPARSED_COMMAND_SECTIONS` | — | **mitigate — revisit condition added, NO acceptance made** | Both constants gain a revisit condition in `AR-19-03`'s shape plus a non-skipping version-witness assertion that fails when the installed git differs from the recorded one, with the cost stated: it fires on every git upgrade, and that is the schedule. **No `AR-` row is added and no risk is accepted** |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — bookkeeping gap RECORDED, acceptance deliberately unmade** | Five audits and plans running have declined to make it because it is a human decision. No `AR-19-13`, no log row, and the word is not applied |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; four registered rows plus the persisted-alias arm still at exit 0 and their pins green UNMODIFIED. Recorded as measurably WIDER than the register credits, and **audit 7's `!`-bodied destructive pair still works after this plan**, stated first in the SUMMARY. Because it remains open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (registered, arms unweakened, not remedied)** | Record unweakened, no remedy added, counts toward `threats_open` |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG, one slot outside the decision region | medium | **accept for this plan (registered by `19-16`, not fixed)** | Left exactly as pinned |
| — | Elevation of Privilege | the `glab --host` forge cell | — | **out of scope, carried forward unfixed** | `glab` is confirmed NOT installed, so a two-sided pin cannot run against its real callee and a pin that skips is fail-open. Audits 7 and 8 both declined to upgrade it |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. Every probe uses `std::process::Command` inside `#[cfg(test)]` |
</threat_model>

<verification>
- `19-24`'s complete RED set is re-run against the unmodified tree BEFORE any production line moves,
  the failing names recorded verbatim and diffed against `19-24-SUMMARY.md`. An already-green row is a
  finding, not a step to skip.
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`,
  and MUST be read as `passed + failed`.
- The gate arithmetic is STATED and CHECKED: `passed + failed` = `19-24`'s recorded total plus the
  number of NEW `#[test]` fns, counted from `git show`. A red test RAN, so red→green contributes
  nothing; a disagreement is a finding.
- All FIFTEEN `envelope_*` binaries RAN and their per-binary counts are recorded.
- `19-24`'s entire RED set is GREEN and every name is listed; every remaining failure is one of the
  three documented flakes.
- **No test file row was edited or deleted.** `git diff --numstat` over `tests/` shows ZERO deletions
  across all four commits, and every test write is a NEW `#[test]` fn — none folded into an existing
  one, because that is what makes the gate arithmetically satisfiable.
- `tests/envelope_command_position.rs` and `tests/envelope_config_resolution.rs` are UNMODIFIED, and
  both `T-19-86` `!`-bodied rows are GREEN.
- The clause is raised inside `scan_leading`'s existing block and at `classify_config`'s existing key
  operand, with **no third reading site**: `first_unreadable_decision_word`, `config_key_operand_index`,
  `subcommand_word_indices`, `scan_gh_api` and `resolve_program_with_head` have no behavioural hunk,
  no `ParkReason` variant was added, and `hooks.rs` was not opened.
- `19-24`'s two ordering rows land at their deliberately different identifiers, which is the mechanical
  proof the clause is raised inside the ONE left-to-right walk.
- **No `include.path` or `includeIf` clause was added at `classify_config`**, and
  `git config include.path /tmp/evil.cfg` is pinned PERMITTED with audit 8's INERT measurement cited.
- The three fail-open directions are stated in the constant's doc, the predicate's doc, the refusal's
  doc and the record, and **none is handed to the pin or to any other control**; the pin's own doc opens
  by stating the direction it holds and says it is NOT a control over the residue.
- The version-witness assertion fails on a mismatch, cannot skip when `git` is absent, and states its
  cost. **No `AR-` row was added and the word "accepted" is not applied to the residue or to
  `T-19-17r`**; the Accepted-Risks-Log row count for that risk ID is zero.
- `cred.rs` changed ZERO non-doc lines, verified by `git diff --unified=0` filtered for lines that are
  not `///`, and no item other than the `hooks_path_env` limit paragraph was opened.
- The `GH_API_VALUE_OPTS` pin drives real `gh` over all seventeen entries and carries BOTH negative
  controls; `--hostname` is still present in `FORGE_VALUE_OPTS` with its measured reason recorded.
- Rounds 5, 6, 7 and 8's mechanism pins are all green and non-dead; `SEPARATORS` is byte-identical and
  `is_separator(">")` is false; `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` are
  unchanged; the proportional floor, the deep anchor and the sentinel count keep their strictness; and
  `policy.rs` still carries exactly ONE `#[cfg(test)]` line.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-24 record.
- Four commits exist in order: region 1, region 2 plus the derived pins, the two corrections, the
  record.
</verification>

<success_criteria>
1. `19-24`'s RED set was confirmed STILL RED before any production line moved, and the verbatim pre-fix
   output is in the SUMMARY beside every post-fix green.
2. The design question is answered explicitly: **option (b)**, with option (a) costed by measurement —
   the second tokenizer, the depth-2 recursion that makes any depth bound attacker-reachable, and the
   carriers that deliver no readable body — and rejected on those grounds.
3. What else git re-parses from a config value is enumerated in three kinds, K2's inertness is a
   measurement rather than a category argument, and the constant names one section because that is
   where K1 lives.
4. The rule reads the SECTION plus git's own one-byte shell rule, is asked at the two decision regions
   that already read that word, adds no `ParkReason`, needs no second pass and no third reading site.
5. **Both `T-19-86` `!`-bodied rows stay GREEN in files this plan may not edit**, and the carve-out is
   documented as git's measured rule rather than a concession.
6. The cost is pinned from both sides and stated honestly: **defining a non-shell alias is refused,
   using one is not**, every refused row has a permitted twin, and round 8's permitted half and round
   7's generative property behind `CALLEE_KNOWN_LEADING_PREFIX` stay green.
7. **`T-19-108` is closed AS SCOPED and the boundary is stated**: audit 7's `!`-bodied destructive pair
   still works, that is `T-19-86`, and the SUMMARY says so first.
8. **The three fail-open directions are stated in the docs and the record and handed to no control**,
   and the residues gain a revisit condition plus a non-skipping version witness whose cost is stated
   — with **no acceptance made and no `AR-` row added**.
9. `T-19-109` is closed by replacing a count with a statement of regions, in a named narrow `cred.rs`
   exception changing zero non-doc lines, with the "RESTORATION" correction recorded BESIDE the
   subsections that carry it.
10. `T-19-106` is closed by writing the `gh` pin that can run today with both negative controls, and by
    KEEPING `--hostname` with its measured reason — a correction to audit 8's own suggestion.
11. The `T-19-17r` bookkeeping gap is still OUTSTANDING, no Accepted-Risks-Log row was added, and the
    word "accepted" is not used for it.
12. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared — `T-19-86` (recorded WIDER)
    and `T-19-91` remain OPEN at `high` — and no unqualified "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-25-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared, that `T-19-108` closes
only AS SCOPED, and that **audit 7's `!`-bodied destructive pair still works after this plan**; the
verbatim pre-fix RED output for `19-24`'s complete set with the post-fix green beside it; the four
commit SHAs in order with `git show --stat`; the `--no-fail-fast` `passed + failed` totals before and
after with the gate arithmetic stated and checked against `git show`; the per-binary counts for all
FIFTEEN `envelope_*` binaries with every one confirmed to have RUN; the design question answered with
option (a) costed by measurement and rejected on three grounds; the K1/K2/K3 enumeration; the two-region
rule with the `include`/`alias` persistence asymmetry and the reason an include clause at region 2 would
be inert; the mechanical confirmation that no third reading site was created and that the two ordering
rows landed at their different identifiers; the three fail-open directions stated in those words with
the explicit statement that none is handed to a control; the revisit condition and version witness with
their cost stated and the explicit statement that no acceptance was made and no `AR-` row added; the
over-refusal cost measured from both sides with every refused row beside its permitted twin and the
honest sentence that defining a non-shell alias is refused while using one is not; the `cred.rs`
correction recorded as a named narrow exception with its zero-non-doc-lines measurement; the
"RESTORATION of layer 3's catch" correction recorded BESIDE the 19-22 record, the 19-23 record and
`19-23-SUMMARY.md` rather than as an edit to any of them; `T-19-106`'s two halves with the
seventeen-entry probe, both negative controls, and the measured `FORGE_VALUE_OPTS` regression that makes
keeping `--hostname` correct; the re-measured byte floors and all three stripper protections with the
confirmation of exactly ONE `#[cfg(test)]` line; the `glab --host` forge cell carried forward unfixed;
the `T-19-17r` bookkeeping gap recorded as OUTSTANDING with the explicit statement that this plan did
NOT accept it and added no Accepted-Risks-Log row; and a "what remains uncovered" section naming
`T-19-86` FIRST — open at `high` by user scoping decision, recorded WIDER, carrying `T-19-108`'s
unclosed `!`-bodied arm, and NOT closed — followed by `T-19-91` (open at `high`, arms unweakened),
`T-19-96`, `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit statement that
only the WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>
