---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 32
type: execute
wave: 22
gap_closure: true
gap_ids: [T-19-122, T-19-123, T-19-124, T-19-125, T-19-126]
depends_on: [19-31]
files_modified:
  - tests/envelope_word_set.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-04, SAFE-05, SAFE-06]

estimate:
  tokens: 132000
  raw_tokens: 132000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THIS ROUND EXISTS TO MAKE ONE SENTENCE WRITABLE, AND AUDIT 12 WROTE THE SENTENCE ITSELF.** Asked whether the open set is now the human-decision items plus the deliberately-unruled residues, audit 12 answered: *\"Not yet. Five of the eight are (`T-19-86`, `T-19-91`, `T-19-111`, `T-19-112`, `T-19-116`), and `T-19-121` is a bounded residue of a control that genuinely landed. But `T-19-122` and `T-19-123` are neither — unmodelled classes with driven harm, absent from every residue statement, corpus alphabet and threat row until this audit. When those two are ruled or accepted, that sentence becomes true and will be worth writing.\"* **`T-19-119` and `T-19-120` both closed this audit; the plane is finished; this is the last narrowing.** This plan writes the corpus and the reproducers and STOPS; `19-33` writes the rules and the honesty repairs. **Whether the sentence becomes writable is audit 13's judgement, not this plan's claim.**"
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, AND THIS IS THE THIRTEENTH CONSECUTIVE ROUND THAT ORDERING IS THE REASON A PLAN EXISTS SEPARATELY.** It worked twelve times — 19-14/19-15, 19-16/19-17, 19-18/19-19, 19-20/19-21, 19-22/19-23, 19-24/19-25, 19-26/19-27, 19-28/19-29, 19-30/19-31 among them — and the cross-plan carry-forward is what proves a rule load-bearing. **Every commit here shows ZERO `src/` hunks under `git show --stat`.** `T-19-122` and `T-19-123` are absent from every corpus alphabet — audit 12 measured that directly: *\"the corpus that certifies the round draws eighty-four rows across twelve classes and not one of them is a here-string or an ancestor\"* — **so if the widened corpus does not go RED against the pre-fix tree, that is a FINDING: stop and report it.**"
    - "**THE DIAGNOSIS IS ADOPTED WHOLE: THE RESIDUE CONDITION ENUMERATES THE SILENCES OF A *READING*, AND A BOUNDARY HAS THREE WAYS TO BE SILENT.** Audit 12: *\"it enumerates the silences of a READING and the code's silences are also in its WORD SET and in its COMPARISON.\"* The condition in `policy.rs:5900-5905` says the predicate *\"reads every `/`-ANCHORED SUBSTRING of every LITERAL word, in either word class, against both paths\"* and then names three silences — all three of them properties of what the reader can SEE IN A WORD. **It says nothing about WHICH WORDS REACH THE READER AT ALL, and nothing about WHICH PATHS THE COMPARISON PROTECTS.** This round restates the condition over all THREE axes and measures the residue on each: (1) the WORD SET — which words reach the predicate; (2) the READING — what the predicate can see in a word it received; (3) the PATH SET — which paths the comparison protects. **Round 12 got axis 2 right and inherited two holes it never named.**"
    - "**`T-19-122` IS A WORD IN NEITHER WORD CLASS, AND AUDIT 12 DELETED THE WHOLE ENVELOPE WITH IT.** `xargs rm -rf <<< <ENV>/alpha` is **exit 0** against its twin `rm -rf <ENV>/alpha` at **exit 2** — the same program-free pair differing only in whether the path stands as an operand or after a `<<<`. The word is fully `Token.literal` (no `$`, no `~`, no glob, no brace), ABSOLUTE at index zero, needs no cwd and no link, and names the directory EXACTLY, **so it is none of the three declared silences.** The mechanism is exact: `consume_redirection` (`policy.rs:3532-3609`) consumes the target word and emits **no `Token`**, and pushes it onto `Segment::redirection_targets` only under `if pathname_target && target.literal` (`:3581`); `redirection_operator` (`:3205`) answers `pathname_target = false` for `<<<`. **So the word is in neither of the two classes `protected_carrier_named` iterates at `policy.rs:6118-6128`.** Audit 12 drove it END TO END TWICE with a control beside every leg — a bare remote's `main` moved `c13f9ea` → `371a2f5` and a fired PR cap reset to PERMITTED — and it reaches the binary half too (`xargs cp /bin/true <<< <BINARY>` and `xargs -I{} cp /bin/true {} <<< <BINARY>` both exit 0). **It is PRE-EXISTING**: the rebuilt `dd17bfb` control binary permits it, so it is a gap twelve audits did not find rather than a regression."
    - "**AUDIT 11 LOOKED AT THIS EXACT CELL AND CLASSIFIED IT AS A COST AVOIDED, AND THAT IS THE PROCESS FINDING, WRITTEN BESIDE RATHER THAN AS AN EDIT.** Audit 11 recorded the five non-pathname operators as *\"correctly NOT recorded as carriers, so the round bought its rule without an over-refusal\"*, with `cat <<<X` as the witness. **That is right about `cat` and wrong as a general statement: a here-string is a DATA channel, and `xargs` is a program that turns data into argv.** The observation was correct and the generalisation was not — the same shape as `19-28` seeing `T-19-119`'s spelling and calling it *\"Not a defect\"*, which is how that one survived a round that had already found it. **A recorded-and-misgeneralised observation is worth more to the next reader than the finding itself**, and it is recorded in this plan's own section, never as an edit to audit 11's."
    - "**THE `T-19-122` DESIGN IS A THIRD WORD CLASS ON A SECOND `Segment` FIELD, AND THE FENCED-FILE ENUMERATION DECIDES IT RATHER THAN TASTE.** Two designs were available. **(A) Widen `pathname_target`** so `<<<`, `<<`, `<<-`, `>&` and `<&` record onto `Segment::redirection_targets`. **(B) Carry a SECOND field** — literal NON-pathname redirection target words — and chain a THIRD class at the same one reading site. **The enumeration answers it mechanically: `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693-10727`) asserts `targets_of(\"ls <<<here\")`, `targets_of(\"ls <<EOF\")`, `targets_of(\"ls <<-EOF\")`, `targets_of(\"ls >&2\")` and `targets_of(\"ls <&0\")` are ALL EMPTY, and the twelve-operator grammar pin at `:10586` and `:10603` asserts `pathname_target` TRUE for the seven and FALSE for the five.** Design (A) turns BOTH red; design (B) turns NEITHER red, because `redirection_targets`, `pathname_target`, `redirection_operator` and the pathname/non-pathname split are all left byte-identical. **Design (B) is mandated and design (A) is forbidden, and the reason is a measured fence rather than a preference.** Audit 12's own suggested closure says the same thing — *\"a data-word class, distinct from `redirection_targets` so the pathname/non-pathname split stays intact\"*."
    - "**`T-19-123` IS A WORD NAMING AN ANCESTOR, AND THE ROOT IS ONE `dirname` FROM THE REFUSAL MESSAGE ITSELF.** `rm -rf <root>` is **exit 0** while `rm -rf <root>/alpha` is **exit 2**, because `word_is_within` (`policy.rs:5685-5711`) is a component-wise PREFIX over `<root>/<alias>` and its `word.len() < dir.len()` early return at `:5706` answers `false` for every ANCESTOR. `rm -rf <root>/`, `mv <root> /tmp/gone`, `tar -C<root> …`, `find <root> -delete` and `chmod 000 <root>` are all exit 0, and **the composite `protected_carrier_named`'s own doc claims as a narrowing is defeated by it**: the doc says `cd <env>/<alias> && rm -f pr-ledger.ndjson` *\"is refused BY SEGMENT ONE\"* — true — but `cd <root> && rm -rf alpha` is exit 0 because segment one names the ancestor. Audit 12 drove it END TO END TWICE with a control beside every leg: a bare remote's `main` moved `0658c51` → `140421f`, and a fired PR cap reset. **The root is trivially derivable** — `git config --get core.hooksPath` is permitted and reports `<root>/<alias>/hooks`, and the refusal message itself names `<root>/<alias>`. **PRE-EXISTING since round 10; the `dd17bfb` control binary permits it too.**"
    - "**`T-19-123` IS PINNED PERMITTED BY NAME IN TWO PLACES AND THE CODE DOES EXACTLY WHAT THE PIN SAYS — SO WHAT IS ABSENT IS THE THREAT ROW AND THE RESIDUE CLAUSE, NOT THE IMPLEMENTATION.** `policy.rs:10022-10026` pins `word_is_within(\"/tmp/envroot\", &dir) == false` with the stated reason *\"the PARENT of the envelope directory is not under it. The boundary is the directory this run owns, not everything beside it\"*, and `tests/envelope_interior_path.rs:2341` pins the same row inside `every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule`. **Audit 12 verified both green and non-vacuous.** This plan therefore does NOT treat the permit as a bug found: it treats it as a DECISION whose harm was never modelled, measures the harm, and hands `19-33` the decision with both pins named in advance. **If the verdict moves, BOTH pins move, and audit 12 says plainly that moving them is a design change and not a test edit** — so `19-33` moves them as a NAMED, BOUNDED, CROSS-FENCE exception with each pin's reasoning REWRITTEN rather than deleted, the shape round 11 used for `direction_i_…_stays_permitted`. **This plan names the exception in advance and performs none of it.**"
    - "**THE `T-19-123` DESIGN IS AN ANCESTOR CLAUSE THAT STOPS AT THE ENVELOPE ROOT, AND WHERE IT STOPS IS DERIVED RATHER THAN CHOSEN.** The protected path set becomes: **a candidate UNDER `<root>/<alias>` (the existing PREFIX), OR a candidate that is a proper ANCESTOR of `<root>/<alias>` AND is itself AT OR UNDER `<root>` (the new clause), OR a candidate EQUAL to `current_exe()` (the existing EQUALITY, unchanged).** Because `envelope_dir_in`'s alias is a PLAIN SINGLE PATH COMPONENT — `ledger_path_in` refuses anything else — **the ancestor set is exactly ONE path, `<root>`, and the protected set grows by exactly one member.** The clause is written in its GENERAL form so it stays correct if the directory ever becomes deeper. **It STOPS at `<root>` and the reason is ownership**: `<root>` is `GSD_MM_ENVELOPE_ROOT`, or the default `~/.local/share/gsd-meta-manager/envelope`, created by this tool and containing only alias directories it created. Above it are `~/.local/share`, `$HOME`, `/tmp` and `/` — directories shared with everything the user has, exactly as the binary's parent is. **A boundary reaching them would refuse `ls /`, `df /`, `du -sh $HOME` and `ls /tmp`, which is not a boundary but an outage** (AR-19-11: a refusal a user cannot act on is a control that gets switched off), and it is the same argument `word_is_exactly` already gives for keeping the binary half an EQUALITY."
    - "**THE ANCESTOR CLAUSE IS NOT A PREFIX WIDENED TO THE ROOT, AND THE CORPUS DRAWS THE ROW THAT TELLS THEM APART.** The obvious cheaper implementation is to move the existing PREFIX up one component — compare against `<root>` instead of `<root>/<alias>`. **It is wrong and the failure is measurable.** A prefix over `<root>` refuses every sibling alias's directory and every file directly under the root; and because `GSD_MM_ENVELOPE_ROOT` is USER-SETTABLE, a user who sets it to `/tmp` gets the entire `/tmp` subtree refused, while the ancestor clause under the same configuration refuses only the single word `/tmp`. **So the corpus pins `ls <ENV>/unrelated-sibling` PERMITTED before and after, and a prefix-to-the-root implementation turns THIS file red rather than turning a driven run unusable.** That row is the discriminator and it is drawn deliberately."
    - "**`T-19-124` IS ROUND 12'S OWN MECHANISM BOUNDING THE WRONG QUANTITY IN THE ROUND THAT FIXED `T-19-120`.** `slash_anchored_candidates` (`policy.rs:5658-5662`) is applied PER CANDIDATE inside `word_is_within` (`:5697`) and `word_is_exactly` (`:5760`), so `lexical_absolute_components` used to run ONCE PER WORD and now runs ONCE PER `/`. Audit 12 measured HEAD against a **REBUILT PRE-ROUND-12 CONTROL BINARY at `dd17bfb`**, same input, same machine, one word of the form `/a/a/a…`: 1 000 slashes 97 ms; 5 000 → 1 763 ms; 8 000 → **5 873 ms** against `dd17bfb`'s 39 ms; 20 000 → 24 591 ms against 47 ms; 50 000 → >116 000 ms against 72 ms. **A 16 KB command line crosses `GUARD_TIMEOUT_SECS = 5` (`hooks.rs:1484`).** The decisive control that attributes it to the SCAN rather than to LENGTH: **a 100 000-character word with NO slashes answers in 66 ms.** `MAX_GUARD_REQUEST_BYTES = 1 MiB` (`hooks.rs:733`) bounds the INPUT and bounds no work at all — **which is `T-19-120`'s own shape, a bound over the wrong property.** The round's cost claim (*\"the cost is bounded by the PATH SET, not by the split\"*) is TRUE and is about which candidates can be REFUSED; it says nothing about how many are COMPUTED. **The BEHAVIOURAL half — what the agent CLI does with a `PreToolUse` hook that overruns its timeout — is UNMEASURED and claimed in NEITHER direction**, on the discipline `C-08`'s, `T-19-117`'s and `T-19-120`'s behavioural halves are held to."
    - "**AUDIT 12'S OWN PARENTHETICAL REMEDY FOR `T-19-124` IS INSUFFICIENT, AND THE ARITHMETIC THAT SHOWS IT IS THIS PLAN'S TO DERIVE AND RECORD.** The suggestion is *\"a cap on the number of `/`-anchored candidates per word (or an early return once a candidate is shorter than the shortest protected path, which is the same `word.len() < dir.len()` test hoisted to the byte level)\"*. **The parenthetical half does not restore linearity.** A byte floor at the shortest protected path removes only the candidates SHORTER than that floor — for a 16 KB word of the form `/a/a/a…` with 8 000 slashes and a floor of about 18 bytes, that is **nine candidates out of eight thousand**, and the remaining 7 991 each still cost O(word length). The quadratic is untouched and the measured 5 873 ms would not move. **A cap on the candidate COUNT is a bound on work and does fail closed, but a per-word cap is amplified by the word count and a per-segment cap by the segment count** — a request of many medium words each spending the whole cap defeats either. **So the bound this round mandates is on the WORK, is derived from the deadline, and is checked by MEASUREMENT against the pre-round-12 curve rather than by argument.** `19-33` implements it; this plan measures the curve on three trees and writes the property the fix must satisfy."
    - "**THE `T-19-124` PROPERTY THE CORPUS PINS IS LINEARITY, NOT SPEED, BECAUSE SPEED IS A FACT ABOUT A MACHINE AND LINEARITY IS A FACT ABOUT THE CODE.** The mandated row is a RATIO: the guard's answer time for a word with 8 000 slashes divided by its answer time for a word with 2 000 slashes. Four times the input predicts about **4×** under a linear scan and about **16×** under a quadratic one; the pin's threshold is **8**, which leaves a factor of two of margin in both directions and is measured RED today at about 14. Beside it, and asserted exit 0 BEFORE AND AFTER, is **audit 12's decisive control — a 100 000-character word with NO slashes** — which is what attributes the cost to the scan rather than to the length. **The 1 MiB slash-dense row is RECORDED and NOT asserted as a `#[test]`**, because a test that is RED for hours is not a test; it is measured once under a hard external timeout, recorded with its wall time, and handed to `19-33` to assert once it is fast."
    - "**`T-19-125` IS A DISCLOSED-COST WORDING THAT IS ONE OPERATION SHORT, AND THE BEHAVIOUR IT DESCRIBES IS CORRECT.** `protected_carrier_named`'s disclosed cost (`policy.rs:6044-6048`) says *\"any word whose text contains THIS RUN'S OWN envelope directory or THIS RUN'S OWN binary path **as a `/`-anchored substring**\"*. **The comparison NORMALISES, so the real surface is wider than the sentence**: `dd of=<ENV>/./alpha/x` and `dd of=<ENV>/zzz/../alpha/x` are both **exit 2** and **neither word contains `<ENV>/alpha` as a substring at all**. The true surface is *any word one of whose `/`-anchored substrings NORMALISES to a path under this run's directory or equal to its binary*. **It fails CLOSED and the gap is small — and this phase has now shipped a \"FIVE forms\" claim, a \"four directions\" claim and a \"SEVEN spellings\" claim that were each wrong when written, and the round that removed the count replaced it with a wording that is one operation short.** The corpus asserts the BEHAVIOUR (both normalising instances exit 2 before and after, with their non-normalising twins beside them) and RECORDS the wording as `19-33`'s repair. **Recorded beside it: `envelope_carrier_refusal`'s line *\"To proceed: name a path outside that directory\"* (`policy.rs:6168`, `:6180`) is STALE for this case**, where the word merely CONTAINS the directory and the program would never have opened it."
    - "**`T-19-126` IS TWO EVIDENCE ARTEFACTS THAT DO NOT CARRY THE WEIGHT PUT ON THEM, AND THE FIRST OF THEM IS A GATE THAT CANNOT BE A STANDING CONTROL AT ALL.** *(i)* The `ledger.rs:354-361` forbidden-API gate is a **plan-time verify grep, not a standing test** — nothing under `tests/` or `src/` re-runs it — and audit 12 found **no standing pin** that a symlinked-to-REGULAR ledger is permitted and counted, the property the gate stands in for and the control that keeps the KIND check a kind bound rather than a link ban; audit 12 had to measure it by hand. **The comment's stated reason is also INVERTED**: writing the forbidden API's literal name in `ledger.rs` to forbid it would turn a zero-occurrence gate **RED**, not make it *\"pass vacuously\"*. **A grep gate whose forbidden literal cannot be written is a one-shot, not a standing control.** This plan gives the property a REAL standing pin two ways: a BEHAVIOURAL pin driving a symlinked-to-regular ledger through the built binary (exit 0, counted, walked), and a SOURCE-SLICE pin over `ledger.rs`'s PRODUCTION half — the pattern `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so` already uses, where the forbidden name lives BELOW the `#[cfg(test)]` sentinel and therefore outside the slice, **which is exactly what dissolves the inversion**. *(ii)* `CONTROL_CARRIER_INTERIOR_PATH` entry 7 (`tests/envelope_wrapper_class.rs:8450`), re-spelled to `tar --create --file /tmp/t -C<ENV>/alpha`, **does not reach the file under real GNU tar** — it answers *\"Cowardly refusing to create an empty archive\"* because the re-spelling moved `-C` last and dropped the `.` member — while the alphabet's own doc says every entry was drawn from `19-30`'s measured sweep. **The class is NOT vacuous** (entries 8 and 9 reach, verified), so this is one row's evidentiary value. **The repair is an ADDED reaching entry beside it and an APPENDED WR-02 note, never a deletion or a rewording** — and audit 12 measured that the re-spelling's original motive is moot, because with a `c`-containing envelope root BOTH forms answer exit 2, the carrier clause being raised above program resolution."
    - "**THREE THINGS THAT HAVE BEEN CONVENTION FOR TWELVE ROUNDS BECOME CONTRACTS HERE, BECAUSE THE SEAM HAS BROKEN EIGHT TIMES AND TWICE NEEDED A HUMAN AUTHORISATION.** *(i)* **A NAMING CONTRACT.** Every `#[test]` asserted at a DERIVED POST-FIX verdict is named `after_19_33_*` in the evidence file and suffixed `_after_19_33` on the axis; nothing else is. **So the RED gate asserts ROWS rather than the mere presence of failure** — a broken fixture, a mis-quoted argv, a mis-derived floor or a class collision fails under a name WITHOUT the marker and the gate catches it, where a bare *\"something failed\"* check passes on all four. `19-31`'s handoff already used `after_19_31_*` for eight names; this plan makes it checkable. *(ii)* **A SEVERANCE-RETIRABLE ROW.** `19-33`'s work-bound task is SEVERABLE, and **a severance that cannot be taken is not a severance** — so the linearity ratio is committed in a `#[test]` fn OF ITS OWN carrying that assertion and nothing else, named `after_19_33_the_candidate_scan_is_linear_in_the_words_length`, **so that under a severance the failure set is exactly that one name** and `19-33`'s two gates can each branch on it. *(iii)* **A HANDOFF ANCHOR.** `19-33` must resolve this plan's last commit; every earlier round resolved it from a commit-message SHAPE no plan ever mandated. **Two anchors are established and `19-33` requires them to AGREE** — the message begins exactly `docs(19-32)`, and the record commit is the one that adds `19-32-SUMMARY.md`, which resolves it from FILE CONTENT. **A gate that depends on a message shape is a gate one reworded commit disarms.**"
    - "**EVERY POST-FIX EXPECTATION CARRIES A WRITTEN DERIVATION, AND A VERDICT THAT CANNOT BE DERIVED IS MEASURED PRE-FIX AND RECORDED RATHER THAN ASSERTED.** `19-26` refused a false converse degenerate-proof its own plan mandated; `19-27` measured that its plan's own certifying row was refused at the SAME identifier as its outside control and `record_only`'d it; `19-28` found `19-29`'s blocking scope conflict by reading; `19-29` reported two unauthorised row corrections as findings. **And `19-28` saw `T-19-119`'s spelling, called it \"Not a defect\", and that is how it survived a round that had already found it — so an OBSERVATION that cannot be derived is recorded as UNRESOLVED, never classified.** For every row asserted at a post-fix verdict, NAME the control and state what makes the pair discriminating. On this round's axes that is: for `T-19-122`, whether the carrier word stands as an operand or after a NON-PATHNAME redirection operator; for `T-19-123`, whether the word names the envelope DIRECTORY or its PARENT; for the ancestor STOP, whether the word names the root's own parent or a sibling under the root."
    - "**THE AXIS GROWS FROM TWELVE CLASSES TO FOURTEEN, AND THE STRUCTURAL COLLISION IS FORCED FOR THE SEVENTH TIME — THIS TIME THROUGH CLASS 2 RATHER THAN THROUGH THE COMPLEMENT.** A here-string carrier entry such as `xargs rm -rf <<< <ENV>/alpha` has `is_redirection_target = true` in `carrier_words` (`tests/envelope_wrapper_class.rs:7949-7961`, because `REDIRECTION_OPERATORS` contains `<<<`), so it satisfies **class 2, `draws_a_redirection_target_carrier` (`:8003-8010`), whose predicate asks only `is_redirection_target && starts_with(ROOT)` and does NOT distinguish a PATHNAME operator from a non-pathname one** — and class 2's alphabet is `CONTROL_CARRIER_REDIRECTION_TARGETS`, the fail-closed class **round 11 closed**. Leaving it there would file a live, unruled bypass under a class the record says is closed, which is a misattribution rather than a wrong arm. **Class 2 is EXTENDED to require a PATHNAME operator, with its reason stated, and class 13 is a NEW fail-closed class for the five non-pathname operators.** This is `19-18`'s `{v}>` blocker, `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split, `19-22`'s indirection/confined split, `19-24`'s re-parsed/confined split, `19-28`'s class-2 collision and `19-30`'s complement extension, **a SEVENTH time.** Class 14, the ANCESTOR class, collides with **class 1** instead — `draws_an_envelope_root_operand` is `starts_with(REPRESENTATIVE_ENVELOPE_ROOT)` and a word that IS the root starts with itself — so class 1 is EXTENDED to exclude a word naming the root exactly, the same refinement round 11 made for globs and braces, **and the executor RE-DERIVES mechanically that no existing entry names the root exactly before extending it.**"
    - "**THE CORPUS MUST BE ABLE TO FAIL IN BOTH DIRECTIONS, AND THIS ROUND'S PERMITTED HALF IS WHAT PINS BOTH NEW BOUNDARIES FROM THE OTHER SIDE.** Asserted exit 0 **BEFORE AND AFTER**: `cat <<<x` and `cat <<< /tmp/x` (a here-string carrying no protected path — the control that keeps class 13 a PATH class rather than a here-string ban); `ls <ENV>/unrelated-sibling` (the row a prefix-widened-to-the-root turns red); `ls /tmp` and `ls <ENV-PARENT>` (the ancestor STOP, above which no rule is written); `ls <BINPAR>` and `cp /bin/true <BINPAR>/some-other-file` (the binary half's EXACT-PATH controls, unchanged because no ancestor clause is written for the binary); `git --git-dir=/tmp/g status` and the whole of `19-30`'s cost half; a 100 000-character word with no slashes; and the fresh-root and symlinked-to-regular ledger rows. **A rule that turned any of them red would be a rule that quietly widened, and it turns THIS file red rather than turning a driven run unusable.**"
    - "**NOTHING ROUNDS 4 THROUGH 12 SETTLED IS RE-OPENED, AND THE MECHANISM PINS SAY SO MECHANICALLY RATHER THAN IN PROSE.** All are re-measured at this plan's base and carried into the new file: **`SEPARATORS` byte-identical and re-derived at its CURRENT line `policy.rs:2422` — `git log -L 2422,2422:src/envelope/policy.rs` returns exactly ONE commit for the whole phase, `84a9b05`** — with `is_separator(\">\") == false` by construction; **`segment.tokens` byte-identical** with the SEGMENT-COUNT pins green and falsifiable in BOTH the SPLIT and the DISPLACED variant; round 5's literalness bit non-vacuous; round 6's deletion model with its over-deletion control at exit 0; round 7's four callee-grammar directions; round 8's confinement clause; round 9's re-parse clause with `aliasx.`/`notalias.` at 0 and both `T-19-86` `!`-bodied rows at 0; round 10's clause; round 11's redirection and exact-path clauses with both near-miss controls permitted; **and round 12's own — the interior scan's whole class at fourteen attachment characters, `containment_holds_the_candidate_set_carries_todays_answer_byte_for_byte`, the `credential.helper` clause with its five near-miss controls PERMITTED at the same identifier, the ledger KIND check at FIFO / directory / character-device / symlink-to-regular, and `envelope_config_resolution` at 30/0.** **`every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule` stays GREEN and NON-VACUOUS at this plan's end** — it is `19-31`'s file and this plan does not open it."
    - "**THE BYTE FLOORS AND THE ANTI-VACUITY STRIPPER'S THREE PROTECTIONS ARE LOAD-BEARING AND STAY AT THEIR PRESENT STRICTNESS.** `POLICY_MIN_PRODUCTION_BYTES = 40_000` and `HOOKS_MIN_PRODUCTION_BYTES = 20_000` stay at their values or rise; the proportional floor, the deep anchor `fn forbidden_repo_path` and the exactly-ONE `#[cfg(test)]` sentinel count per file keep their present strictness. **And a SECOND source-slicing control is named here in advance because `19-33` writes into its region**: `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so` slices from the literal `fn lexical_absolute_components(word: &str)` to the `#[cfg(test)]` sentinel and asserts eight filesystem and process APIs appear nowhere in the sliced CODE, with a FOURTH positive control naming `slash_anchored_candidates` added by `19-31`. **Every helper `19-33` adds — the ancestor comparison, the non-pathname word class's reader and the linear candidate normaliser — MUST fall AT OR AFTER that anchor**, and a FIFTH positive control naming each is `19-33`'s to add."
    - "**`SECTION_ENVELOPE` IS NOT OPENED THIS ROUND AND ITS HEADROOM IS NOT SPENT.** Audit 11 verified the first `Guaranteed` clause TRUE at **211 whitespace tokens of an UNRAISED 215 cap**, widest line **74** of 80, and its `Not guaranteed` half already generalises to *\"the files and the binary this envelope runs on\"*, which covers both of this round's routes. **\"As started\" states what the envelope ESTABLISHES and stops**, so neither `T-19-122` nor `T-19-123` falsifies it. This plan RE-MEASURES the token count, the widest line and the clause's truth and RECORDS all three; it edits nothing, and `19-33` is prohibited from opening the file."
    - "**`T-19-17r` STAYS OUTSTANDING AND THIS PLAN DOES NOT RESOLVE IT EITHER.** `grep -cE '^\\| AR-19-13 \\|'` over `19-SECURITY.md` is **0**. Audits 5 through 12 and plans 19-24 … 19-31 all confirmed the measurement and both pins and all sixteen explicitly declined; audit 12 called itself *\"the sixteenth agent to leave the acceptance unmade\"* and said plainly that it is a human decision. **This plan is the seventeenth to decline.** `AR-19-04` and `AR-19-05` are NOT un-accepted, re-rated or renumbered, and **`T-19-23` is NOT marked closed.**"
    - "**`/gsd-secure-phase 19` IS NOT CLEARED BY THIS PLAN, BY `19-33`, OR BY THE TWO TOGETHER.** Eight threats are open at `high`: `T-19-86` (by explicit user scoping decision), `T-19-91`, `T-19-111` (no rule; attribution stays OUT of `T-19-86`), `T-19-112`, `T-19-116`, `T-19-121`, `T-19-122` and `T-19-123`. **`T-19-116` gains TWO MORE ROUTES this audit** — `xargs cp /bin/true <<< <BINARY>` and `rm -rf <binary-parent>`, both exit 0 — **and closing `T-19-122` closes only the first of them**, because no ancestor clause is written for the binary. `T-19-121`'s residue is unchanged, and **the alias-body credential route belongs to `T-19-86` and is RECORDED, never folded.** The SUMMARY must say all of that FIRST and **must never write an unqualified \"T-19-60 is closed\"**."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-33` is the rules and the honesty repairs. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`, `19-16`, `19-18`, `19-20`, `19-22`, `19-24`, `19-26`, `19-28` and `19-30` met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured or the one DERIVED IN WRITING from `19-33`'s mandated design. If any audit-12 row does not reproduce, report it as a finding with the measured verdict and STOP — do not assert the audit's number over your own measurement and do not quietly drop the row. In particular MUST NOT inherit audit 12's END-TO-END claims by citation: the bare-remote fixture is REBUILT and every leg re-measured independently with the remote's SHA recorded BEFORE and AFTER and a CONTROL beside each, and each PR-cap reset is re-driven in one persistent root from a fired cap."
      category: honesty
    - statement: "MUST NOT assert a row whose CONTROL is refused at the SAME identifier for the SAME reason. `19-27` measured that its plan's own certifying row and its outside control were BOTH exit 2 `envelope_assertion_failed`, so the row certified nothing. **Name the control for every asserted row and state what makes the pair discriminating.** A row whose control fails the same way is `record_only`'d with its overlap stated."
      category: honesty
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised command wrote nothing must use a FRESH `GSD_MM_ENVELOPE_ROOT` of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line — `gh pr create --title x` is the ready-made one. **The two cap-reset drives, the symlinked-ledger row and the FIFO row are the places a fresh root per row would destroy the measurement**: each uses ONE persistent root."
      category: honesty
    - statement: "MUST NOT write a row whose expected mechanism is widening `RedirectionOperator::pathname_target`, changing `redirection_operator`'s twelve-operator match, or recording a non-pathname target onto `Segment::redirection_targets`. **That design turns `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693`) and the twelve-operator grammar pin (`:10586`, `:10603`) RED**, both of which are fenced pins in a file `19-33` may only ADD to. The mandated design is a SECOND `Segment` field carrying LITERAL NON-PATHNAME redirection target words and a THIRD word class chained at the SAME reading site."
      category: safety
    - statement: "MUST NOT write a row whose expected mechanism is a list of REDIRECTION OPERATOR SPELLINGS inside the PRODUCTION predicate, a list of attachment characters, or a list of program names. The production split between pathname and non-pathname operators is already DERIVED in `redirection_operator`'s own match from bash's closed twelve-operator grammar and is read rather than re-spelled; `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` is the mechanical control and must stay green. **A corpus-side class predicate MAY name operators**, because it decides which class an entry draws rather than deciding a guard verdict — the way `DISPLACING_REDIRECTIONS` already names all twelve."
      category: safety
    - statement: "MUST NOT write a row whose expected mechanism is widening the existing PREFIX from `<root>/<alias>` to `<root>`. That refuses every sibling alias and every file directly under the root, and with a user-set `GSD_MM_ENVELOPE_ROOT=/tmp` it refuses the whole `/tmp` subtree. **The mandated design is an ANCESTOR clause BESIDE the prefix one, bounded at `<root>`**, and the corpus states that mechanically by pinning `ls <ENV>/unrelated-sibling` PERMITTED before and after."
      category: safety
    - statement: "MUST NOT write a rule, a pin or a doc claim that extends the ANCESTOR clause above `<root>`, and MUST NOT write one for the BINARY's parent or any ancestor of it. `word_is_exactly` is an EQUALITY precisely because the binary's directory is shared with everything the user installed; `ls <BINPAR>`, `cp /bin/true <BINPAR>/some-other-file` and `cargo install` are pinned PERMITTED and stay so. **`rm -rf <binary-parent>` is a `T-19-116` residue and is RECORDED, never asserted in either direction** — asserting it REFUSED lands permanently red in a file `19-33` may not satisfy, and asserting it PERMITTED pins a live bypass as correct."
      category: safety
    - statement: "MUST NOT write a `T-19-124` row whose property is a WALL-CLOCK SPEED on this machine, and MUST NOT assert the 1 MiB slash-dense row as a `#[test]`. Speed is a fact about a machine; the property is LINEARITY, pinned as a RATIO between two input sizes with a stated threshold and a stated margin. The 1 MiB row is measured ONCE under a hard external timeout, RECORDED with its wall time, and handed to `19-33` — **a test that is RED for hours is not a test.**"
      category: safety
    - statement: "MUST NOT put an entry whose verdict the fix CHANGES into the invariance-preserving arm of any generative property, MUST NOT leave `draws_a_redirection_target_carrier` (class 2) unextended, and MUST NOT leave `draws_an_envelope_root_operand` (class 1) unextended. A here-string carrier satisfies class 2 today and would be filed under the class round 11 CLOSED; an ancestor word satisfies class 1 today and would be filed under the class round 10 closed. **Both are misattributions of a live, unruled bypass to a closed control**, which is worse than a wrong arm. Extend both, state the reason AS a reason, and RE-DERIVE mechanically that no existing entry falls out of a class as a result."
      category: safety
    - statement: "MUST NOT stand either new class up as a SIXTH axis or fold it onto `CONFIG_RESOLUTION_CLASSES`, `UNREADABLE_CLASSES`, `DELETION_CLASSES` or `CALLEE_GRAMMAR_CLASSES`. Both belong on the CONTROL-CARRIER axis: rule (a) decides on a word's PATH, so which words reach the rule and which paths it protects are the same axis's own alphabet. All four earlier axes, their predicates, their degenerate-proofing and their floors stay BYTE-IDENTICAL, and `MIN_CONFIG_RESOLUTION_CLASSES` stays at **6**."
      category: safety
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet ENTRY, property or assertion anywhere under `tests/`, and **MUST NOT DELETE OR REWORD A SINGLE LINE under `tests/`** — `git diff --numstat` over `tests/` must show ZERO deletions across all of this plan's commits. Entries may be ADDED, a predicate may be EXTENDED with its reason stated, and a doc may gain an APPENDED WR-02 note; nothing may be removed, reworded away or narrowed. `MIN_CONTROL_CARRIER_CLASSES` rises from 12 to 14 and no other floor falls. **The `T-19-126`(ii) repair is an ADDED reaching entry and an APPENDED note, never a re-spelling of entry 7.** A case that stops being drawn is a case that stops being able to fail."
      category: scope
    - statement: "MUST NOT modify `tests/envelope_interior_path.rs`, `tests/envelope_carrier_reach.rs`, `tests/envelope_control_carrier.rs`, `tests/envelope_reparsed_value.rs`, `tests/envelope_config_resolution.rs`, `tests/envelope_command_position.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_callee_grammar.rs`, `tests/envelope_expansion_slots.rs`, `tests/envelope_pr_cap.rs`, `tests/envelope_credential.rs`, `tests/envelope_wiring.rs`, `tests/envelope_tracer.rs`, `tests/envelope_advisory.rs`, `tests/envelope_wrapper_bypass.rs` or `tests/envelope_hook_refusals.rs`. Rounds 3 through 12's evidence stays attributable to the round that produced it, which is why `19-14` created a third file, `19-16` a fourth, `19-18` a fifth, `19-20` a sixth, `19-22` a seventh, `19-24` an eighth, `19-26` a ninth, `19-28` a tenth and `19-30` an eleventh; **this plan creates a TWELFTH for the same reason.** The carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place. **In particular `tests/envelope_interior_path.rs:2341`'s ancestor pin and `policy.rs:10022-10026`'s are NAMED here and MOVED BY NEITHER PLAN'S CORPUS TASK** — moving them is `19-33`'s named cross-fence exception."
      category: scope
    - statement: "MUST NOT write the rules, sketch them in `src/`, or add any production line. In particular MUST NOT touch `policy.rs`'s `consume_redirection`, `redirection_operator`, `skip_redirection_target`, `RedirectionOperator`, `Segment`, `Token`, `segment.tokens`, `SEPARATORS`, `is_separator`, `lexical_absolute_components`, `slash_anchored_candidates`, `word_is_within`, `word_is_exactly`, `protected_carrier_named`, `envelope_carrier_refusal` or the floor comment; MUST NOT touch `hooks.rs`'s `guard`, `guard_in`, `classify_segments`, `GUARD_TIMEOUT_SECS` or `MAX_GUARD_REQUEST_BYTES`; MUST NOT touch `ledger.rs`'s `record_and_check_in`, its KIND check or its comment; MUST NOT edit `cred.rs:416-434`, `policy.rs:6044-6048`, `policy.rs:6159-6187` or `SECTION_ENVELOPE`. **All of those are `19-33`'s. This plan RECORDS every one of them as a finding and performs none.**"
      category: scope
    - statement: "MUST NOT open `src/envelope/advisory.rs` or write any row, pin or claim that requires `SECTION_ENVELOPE` to change. **RE-MEASURE its whitespace-token count against the unraised 215 cap, its widest line against 80, and the first `Guaranteed` clause's truth, and RECORD all three; change nothing.** Headroom that is spent cannot be got back, and `19-33` is prohibited from opening the file too."
      category: scope
    - statement: "MUST NOT write a rule, a pin, or a doc claim for `.git/config`, `.claude/settings.json`, `.git/info/exclude`, the run journal or `~/.config/gsd-meta-manager/config.json`. `C-11` … `C-15` keep control **(e)** and `19-33` writes no rule for any of them, so a row asserted REFUSED lands permanently red in a file `19-33` may not edit — **and asserting only that they are PERMITTED is equally forbidden, because that would pin a live bypass as correct.** Every repo-side row is MEASURED and RECORDED in a comment and a print, never written as an assertion. `19-22` asserted such a row against its own comment, its SUMMARY and its plan-check, and it halted `19-23` mid-plan."
      category: honesty
    - statement: "MUST NOT clamp `pr_cap_per_24h` or `pr_cap_per_run`, propose clamping them, or write a row whose mechanism is a clamp. Clamping a configured cap is a PRODUCT decision about what a user may configure, not a guard rule. `C-15` is carried forward at its measured severity with control (e) and NO rule; audit 12 endorsed that reasoning for the third audit running."
      category: scope
    - statement: "MUST NOT fix, narrow, re-scope or re-classify `T-19-86`, and MUST NOT fold `T-19-111` into it — `19-27`'s five-site attribution correction stays as performed and audit 12 confirmed neither record folds it back. **The `!`-bodied alias credential route (`git -c alias.q='!git -c credential.helper=store credential fill' q`, exit 0, secret PRESENT under full envelope posture) is `T-19-86`'s route and is RECORDED, never folded into `T-19-86`'s declared harm and never used to re-rate it** — audit 12 declined to fold it and so does this plan. MUST NOT close, re-scope or add a remedy for `T-19-91`. MUST NOT write a rule for `T-19-111`. MUST NOT fix `T-19-96` or `T-19-110`, MUST NOT re-open `T-19-74` (core rows frozen), and MUST NOT reclassify `T-19-112`, `T-19-115`, `T-19-116` or `T-19-121` — `T-19-121` is narrowed HARD and NOT closed, and it is a bounded residue of a control that landed."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not add park-coverage controls and do not open `scan.rs` or `config.rs`. MUST NOT repair `C-08`'s claimed-but-unwired second carrier: **`C-08`'s behavioural half stays UNMEASURED and is claimed in NEITHER direction**, and so do `T-19-120`'s and `T-19-124`'s — what the agent CLI does with a `PreToolUse` hook that never returns or that overruns its timeout is a property of a closed-source binary."
      category: scope
    - statement: "MUST NOT remove `--hostname` from `FORGE_VALUE_OPTS`, propose removing it, or write a row whose mechanism is its removal — audit 9 re-measured and overturned audit 8's own suggestion, because removal moves a counted creation form to UNCOUNTED (`T-19-35`). **`glab` is confirmed NOT INSTALLED**, so any `glab` row is RECORDED as unmeasurable-against-its-callee rather than driven or inferred, and a pin that would skip is a fail-open pin and is not written."
      category: scope
    - statement: "MUST NOT un-accept, re-open or re-rate `AR-19-04` or `AR-19-05`, MUST NOT add, edit or renumber any Accepted-Risks-Log row, and **MUST NOT mark `T-19-23` closed or re-closed.** MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, MUST NOT create `AR-19-13`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Sixteen agents have deliberately left that acceptance unmade because it is a human decision; this plan is the seventeenth to decline."
      category: honesty
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — `Cargo.toml` and `Cargo.lock` appear in no phase-19 commit — and every parser in this module is hand-rolled against `std` for exactly that reason. The real-git probes, the bare-remote fixture, the here-string and heredoc drives under real `bash`, the `mkfifo` and symlink rows, the rebuilt `dd17bfb` control binary and every latency measurement shell out through `std::process::Command` inside `#[cfg(test)]` or from the task's own shell."
      category: honesty
    - statement: "MUST NOT rebuild the `dd17bfb` control binary by checking out, resetting or stashing the working tree. **ISOLATION=none is forced for this phase and there is no worktree**; extract the tree with `git archive dd17bfb | tar -x -C <tmpdir>` outside the repository and `cargo build` there, recording the binary's path and its `--version`. A control binary produced by mutating the working tree is a control that can lose the plan's own evidence."
      category: safety
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. **Gate on `passed + failed` against 1871**, the number `19-31-SUMMARY.md` records over 47 result lines with 13 ignored and audit 12 independently re-measured. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing — `19-19` first read 1364 instead of 1605 that way (D-34). **A run reporting fewer than NINETEEN `envelope_*` binaries after this plan is a run in which this plan's own evidence file did not execute.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the four documented flakes — the two `tests/driver_reattach.rs` failures, the `tests/envelope_tracer.rs` `ExecutableFileBusy` stub-write race, and the ETXTBSY race over the binary — and MUST NOT treat their absence from a run as a regression. Audit 12 recorded none firing. **Absence is not evidence any of them is fixed**, and the ETXTBSY race is `C-10`'s own seam, which this plan exercises directly through the here-string binary rows. A firing is RECORDED with its verbatim output and its relation to this round claimed in NEITHER direction."
      category: scope
    - statement: "MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lints in `src/browser.rs` and `src/project_creator.rs`. The gate is `cargo clippy -- -D warnings`. Do not fix those lints."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, the plan-19-21 blocker-row resolution record, or any subsection plans 19-13 … 19-31 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-32 planning and execution record. **In particular the correction to audit 11's generalisation about the five non-pathname operators, and any correction to audit 12's heredoc sentence, are written BESIDE, in this plan's own section, and never as edits to theirs** — that is the discipline audit 12 used for its own three corrections."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere, and MUST NOT claim that `T-19-122`, `T-19-123`, `T-19-124`, `T-19-125`, `T-19-126`, `T-19-116` or `T-19-121` is CLOSED. This plan closes nothing at all — it measures, widens the corpus and goes RED. Whether `19-33`'s rules close anything is audit 13's judgement. **State plainly that this plan does NOT clear `/gsd-secure-phase 19`**, with all eight `high` rows named."
      category: honesty
  artifacts:
    - path: "tests/envelope_word_set.rs"
      provides: "the TWELFTH evidence file — `T-19-122` driven END TO END against real git in a REBUILT bare-remote fixture with a control beside every leg and re-driven on the ledger from a FIRED PR cap in one persistent root, with the here-string, the heredoc and the fd-duplication forms each MEASURED separately and the heredoc's guard verdict recorded rather than inherited; `T-19-123` driven END TO END twice with the ANCESTOR-STOP cost sweep beside it — the sibling under the root, the root's own parent, `/tmp` and the binary's parent — so a prefix-widened-to-the-root implementation turns this file red; `T-19-124`'s curve measured on THREE trees (HEAD, a rebuilt `dd17bfb` control binary, and the derivation of the post-fix expectation) with the LINEARITY RATIO pinned rather than a wall-clock speed and audit 12's no-slash control beside it; `T-19-125`'s two normalising instances asserted exit 2 with their non-normalising twins; `T-19-126`'s two REAL standing pins — a symlinked-to-regular ledger permitted and counted through the built binary, and a source-slice pin over `ledger.rs`'s production half; the `SEPARATORS` citation re-derived at `policy.rs:2422`; the `!`-bodied alias credential route measured and RECORDED as `T-19-86`'s; the fenced-file enumeration re-derived mechanically; the two ORDERING pins at deliberately different identifiers; and every carried-forward mechanism pin from rounds 4 through 12"
      contains: "T-19-122"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "the CONTROL-CARRIER axis widened from twelve classes to FOURTEEN with `MIN_CONTROL_CARRIER_CLASSES = 14` — a new fail-closed `CONTROL_CARRIER_NON_PATHNAME_TARGET` alphabet whose entries carry the carrier path after one of the five NON-PATHNAME redirection operators, and a new fail-closed `CONTROL_CARRIER_ANCESTOR` alphabet whose entries name an ANCESTOR of the envelope directory at or under the root; `draws_a_redirection_target_carrier` (class 2) EXTENDED to require a PATHNAME operator and `draws_an_envelope_root_operand` (class 1) EXTENDED to exclude a word naming the root exactly, each with its reason stated and each re-derived mechanically to move no existing entry; `draws_an_ordinary_operand` (class 7, the COMPLEMENT) EXTENDED to exclude both new classes; the ordinary alphabet grown with the measured ANCESTOR-STOP controls; a new WORD-SET fence and a new ANCESTOR-STOP fence, both COUNTING rather than describing; the `T-19-126`(ii) reaching `tar -C` entry ADDED beside entry 7 with an APPENDED WR-02 note; disjointness re-proved over all fourteen; and every floor, case count, slot count and per-class count RE-DERIVED and stated as an exact equality"
      contains: "MIN_CONTROL_CARRIER_CLASSES"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-32 record rather than an audit finding, carrying the statement FIRST that this plan closes nothing and clears no gate; audit 12's finish-line sentence quoted verbatim as the reason this round exists; **the RESTATED THREE-AXIS RESIDUE CONDITION with the residue named on each axis and handed to no pin, schedule or witness**; `T-19-122`'s and `T-19-123`'s end-to-end drives with both remote SHAs and both fired-then-reset caps; **the DESIGN ANSWERS — the second-field third word class with the fenced-pin derivation that decides it, and the ancestor clause with what the protected path set becomes, exactly where it stops and why, and why it is not a prefix widened to the root**; `T-19-124`'s three-tree curve with the derivation that a byte floor is insufficient; the PROCESS finding that audit 11 generalised `cat <<<X` into a statement about all five non-pathname operators, written BESIDE; `T-19-125`'s and `T-19-126`'s registrations with their repairs named as `19-33`'s; the `SEPARATORS` citation re-derived at `:2422`; the `cred.rs` alias-body route recorded as `T-19-86`'s; the axis arithmetic; the mechanism pins and byte floors re-measured; `SECTION_ENVELOPE` re-measured and NOT edited; and `T-19-17r` OUTSTANDING for the seventeenth time"
      contains: "19-32"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-122` and `T-19-123` registered at `high` with their corpus recorded RED; `T-19-124` at `medium` with the work-bound property stated and the behavioural half UNMEASURED; `T-19-125` and `T-19-126` at `low` with their repairs registered as `19-33`'s whether or not any rule lands; the residue restated as a THREE-AXIS CONDITION wherever this plan's own section states it; the statement that `T-19-116` gains two routes this audit and that closing `T-19-122` closes only one of them; and `T-19-86`, `T-19-91`, `T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-115`, `T-19-121`, `T-19-74`, `C-08`, `C-15`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open"
      contains: "T-19-122"
  key_links:
    - from: "audit 12's measured here-string and ancestor bypasses"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary and against real git with a fresh root per row — and ONE persistent root for each cap-reset and each ledger row — before any assertion is written and before any production line moves"
      pattern: "RED"
    - from: "a fix that widened `pathname_target` instead of adding a second field"
      to: "a derivation made BEFORE the rule is written rather than a fenced pin discovered red mid-plan"
      via: "the fenced-file enumeration naming `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator grammar pin as the two rows design (A) turns red and design (B) does not"
      pattern: "word_set"
    - from: "an ancestor clause implemented as a prefix widened to the root"
      to: "a red cost control rather than a driven run that cannot name a sibling under its own root"
      via: "`ls <ENV>/unrelated-sibling` and the user-set-root case pinned PERMITTED before and after, beside `ls /tmp` and `ls <ENV-PARENT>` pinning the STOP"
      pattern: "ancestor"
    - from: "a here-string carrier filed under class 2, the class round 11 CLOSED"
      to: "a fail-closed class of its own and an extended class 2, so a live unruled bypass is never attributed to a closed control"
      via: "`draws_a_redirection_target_carrier` extended to require a PATHNAME operator, with the `19-18`/`19-20`/`19-22`/`19-24`/`19-28`/`19-30` precedent named"
      pattern: "collision"
    - from: "a `T-19-124` bound over the input, the word count or the segment count"
      to: "a bound over the WORK, measured against the pre-round-12 curve rather than argued"
      via: "the LINEARITY RATIO pinned at two input sizes with a stated threshold and margin, audit 12's no-slash control beside it, and the arithmetic showing a byte floor removes nine candidates of eight thousand"
      pattern: "work"
    - from: "a residue condition stated over what the predicate READS alone"
      to: "a condition stated over all THREE axes — which words reach the reader, what the reader sees, and which paths the comparison protects"
      via: "the restated condition with the residue named on each axis, recorded with no count and handed to no pin, schedule or witness"
      pattern: "condition"
---

<objective>
**The words the reader never receives, and the paths the comparison never protects — the other two ways a boundary can be silent.**

Audit 12 (`19-SECURITY.md`, the authority) was asked whether the residue condition was finally complete, and answered in three parts. **The narrowing is REAL and two threats genuinely close** — `T-19-119` on a class sweep at fourteen attachment characters, `T-19-120` on its mechanical half — and *"the boundary the round chose — `/`-anchored substrings rather than an `=`-split or a character list — is the right one and audit 12 endorses it."* **But the condition is still not the condition the code implements**, and this time there are two cases rather than one. **And one of the round's three new mechanisms does create new reachable state**: the candidate scan is quadratic on the guard's own critical path.

Then it wrote the sentence this round exists to make writable:

> **And the last thing this audit can usefully say: the open set is NOT yet the human-decision items plus the deliberately-unruled residues.** Five of the eight blocking rows are that — `T-19-86` (scoping decision), `T-19-91`, `T-19-111`, `T-19-112` and `T-19-116` are all registered, disclosed, and open by decision or by round discipline, and `T-19-121` is a bounded residue of a control that genuinely landed. **But `T-19-122` and `T-19-123` are neither.** They are unmodelled classes with reachable, driven harm, absent from every residue statement, every corpus alphabet and every threat row until this audit. **When those two are ruled or accepted, the sentence becomes true — and it will be worth writing then.**

**This plan adopts that framing whole.** It looks for no thirteenth plane — audit 12 confirmed the plane is finished for the third audit running. It measures the five cells audit 12 left, decides and MEASURES where each new boundary is, widens the corpus so the axis can fail on both new classes, observes RED, and stops. `19-33` writes the rules and the honesty repairs and is gated on this plan's recorded RED state.

**If the widened corpus does not go red against the pre-fix tree, that is a finding — stop and report it.**

## The base, stated so the handoff number is checkable

This plan's base is `8659a18`. `git diff --numstat f06d153..8659a18 -- src/ tests/` returns **nothing** — audit 12's only change was its own append to `19-SECURITY.md`, 748 insertions against 8 deletions in the frontmatter alone — so the built binary here is the binary audit 12 measured against. **Re-verify that before the first measurement rather than trusting this paragraph.**

## The diagnosis, adopted whole

> **it enumerates the silences of a READING and the code's silences are also in its WORD SET and in its COMPARISON.**

`protected_carrier_named`'s condition (`policy.rs:5900-5905`) reads:

> *This predicate reads every `/`-ANCHORED SUBSTRING of every LITERAL word, in either word class, against both paths. It is silent about a word the SHELL MAY REWRITE … about a word whose TEXT CARRIES NO ABSOLUTE PATH ANYWHERE … and about a word that reaches a protected path ONLY THROUGH A LINK.*

All three named silences are properties of **what the reader can see in a word it received**. The sentence names the word set (*"in either word class"*) and the path set (*"against both paths"*) as premises and then says nothing about either. **A boundary has three ways to be silent and round 12's condition names only the first.**

## The five cells, and what each is

| Row | Severity | Component | Which axis is silent |
|---|---|---|---|
| **`T-19-122`** | **high, BLOCKING** | `consume_redirection` (`policy.rs:3532-3609`) and the two word classes (`:6118-6128`) | **THE WORD SET.** A non-pathname redirection operator's target is DELETED from the token stream and recorded on no `Segment` field, so it is in NEITHER class |
| **`T-19-123`** | **high, BLOCKING** | `word_is_within` (`policy.rs:5685-5711`), pinned PERMITTED by name at `policy.rs:10022` and `tests/envelope_interior_path.rs:2341` | **THE PATH SET.** The comparison is a PREFIX over `<root>/<alias>`, so every ANCESTOR is shorter and `word.len() < dir.len()` returns early |
| `T-19-124` | medium | `slash_anchored_candidates` (`policy.rs:5658-5662`) applied per candidate at `:5697` and `:5760` | not a silence — **round 12's own mechanism turned a linear scan quadratic on the guard's critical path** |
| `T-19-125` | low | the disclosed cost (`policy.rs:6044-6048`) | the wording says *substring* where the comparison **normalises** |
| `T-19-126` | low | `ledger.rs:354-361`'s grep gate; `CONTROL_CARRIER_INTERIOR_PATH` entry 7 (`tests/envelope_wrapper_class.rs:8450`) | two evidence artefacts that do not carry the weight put on them |

## `T-19-122` — a word in NEITHER word class

`xargs rm -rf <<< <ENV>/alpha` is **exit 0**; `rm -rf <ENV>/alpha` is **exit 2**. The pair is program-free and differs only in whether the path stands as an operand or after a `<<<`. The word is fully `Token.literal`, ABSOLUTE at index zero, needs no cwd and no link, and names the directory EXACTLY — **so it is none of the three declared silences.**

The mechanism, read out of the source rather than inferred:

```
policy.rs:3564   let pathname_target = operator.is_some_and(|op| op.pathname_target);
policy.rs:3578   match skip_redirection_target(&chars, op_start + op_len) {
policy.rs:3581       if pathname_target && target.literal { redirections.push(...) }
policy.rs:3205       (Some('<'), Some('<')) => (3, false),   // <<<   here-string
```

The target word emits **no `Token`** (that is round 6's deletion model, unchanged and correct), and it is pushed onto `Segment::redirection_targets` only when the operator takes a pathname. `<<<`, `<<`, `<<-`, `>&` and `<&` all answer `false`. **The word is right there in the text the guard read, and it is in neither of the two classes `protected_carrier_named` iterates.**

Audit 12 drove it to harm twice:

```
(i) LAYER 3   CONTROL  hooks present, git push --force → REFUSED, remote main c13f9ea UNMOVED
              GUARD    xargs rm -rf <<< <ENV>/alpha    → exit 0   (twin at exit 2)
              LEG B    performed under real bash, envelope dir GONE, same push → COMPLETED,
                       remote main c13f9ea → 371a2f5 MOVED
              CONTROL  hooks restored, remote rewound, same push → REFUSED, UNMOVED
(ii) PR CAP   4× gh pr create → 1 permit + 3 pr_cap_exceeded, 4 ledger lines
              rm -f <ledger>              → exit 2
              xargs rm -f <<< <ledger>    → exit 0, performed
              call 5                      → PERMITTED again, 1 line
    BINARY    cp /bin/true <BINARY>                   → exit 2
              xargs cp /bin/true <<< <BINARY>         → exit 0
              xargs -I{} cp /bin/true {} <<< <BINARY> → exit 0
```

**It is PRE-EXISTING** — the rebuilt `dd17bfb` control binary permits it — so it is a gap twelve audits did not find rather than a regression.

### The process finding, written BESIDE

Audit 11 looked at this exact cell and recorded the five non-pathname operators as *"correctly NOT recorded as carriers, so the round bought its rule without an over-refusal"*, with `cat <<<X` as the witness. **That is right about `cat` and wrong as a general statement: a here-string is a DATA channel, and `xargs` is a program that turns data into argv.** This is the same shape as `19-28` observing `T-19-119`'s spelling and classifying it *"Not a defect"* — a recorded-and-misgeneralised observation is how a finding survives a round that already saw it. It is recorded in this plan's own section, **never as an edit to audit 11's**.

## The `T-19-122` design, DECIDED — and the fenced-file enumeration decides it

Two designs were available.

| Design | What it does | What it turns RED |
|---|---|---|
| **(A)** widen `RedirectionOperator::pathname_target` so the five non-pathname operators record onto `Segment::redirection_targets` | one flag flip | **`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` (`policy.rs:10693-10727`)**, which asserts `targets_of("ls <<<here")`, `("ls <<EOF")`, `("ls <<-EOF")`, `("ls >&2")` and `("ls <&0")` are ALL EMPTY; **and the twelve-operator grammar pin (`:10586`, `:10603`)**, which asserts `pathname_target` TRUE for the seven and FALSE for the five |
| **(B)** a SECOND `Segment` field carrying LITERAL NON-PATHNAME redirection target words, chained as a THIRD word class at the SAME reading site | one field, one `else if`, one `.chain()` | **nothing** — `redirection_targets`, `pathname_target`, `redirection_operator`, `skip_redirection_target`, `segment.tokens`, `SEPARATORS` and `is_separator` are all byte-identical |

**Design (B) is mandated and design (A) is forbidden, and the reason is a measured fence rather than a preference.** Audit 12's own suggested closure says the same thing — *"carrying it on a SECOND `Segment` field — a data-word class, distinct from `redirection_targets` so the pathname/non-pathname split stays intact — reaches it without touching `SEPARATORS`, `is_separator` or `segment.tokens`."* The pathname/non-pathname split is bash's grammar and it stays exactly where `redirection_operator`'s doc says it is derived; what changes is that the guard reads BOTH sides of it, for different reasons, and says so.

**The third design was considered and is rejected in writing.** *Treat a deleted-and-unrecorded word as UNRESOLVABLE* — refuse any command carrying a non-pathname redirection — would deny `cat <<< hello`, `sort <<< "$x"` and every fd duplication on the line, which is an outage rather than a boundary (AR-19-11). **The predicate's subject is what a line NAMES, not what the shell does with a word**, and the honest statement is that the word set is *every LITERAL word the line contains, whether it survives into argv or is consumed by a redirection, pathname or not.*

**The disclosed cost of the third class, stated in advance:** `cat <<< <ENV>/alpha/pr-ledger.ndjson` becomes refused. It is the same family as the already-disclosed refusal of `cat <ledger>`, it fails CLOSED, and it is bounded to words naming this run's own two paths. `cat <<<x` and `cat <<< /tmp/x` stay permitted, and both are pinned.

**And the heredoc form is MEASURED, not assumed.** Audit 12 records that `xargs rm -rf <<EOF\n<ENV>/alpha\nEOF` deleted the directory under real `bash` — a claim about the HARM, with no guard verdict beside it. `\n` is in `SEPARATORS` (`policy.rs:2422`), so the body's words are ordinary tokens of a segment of their own and may already be refused today. **Drive it, record the guard's exit code and its reason identifier, and state which it is.** If it is already exit 2, say so plainly and record the correction to audit 12's sentence BESIDE audit 12's row; if it is exit 0, it is a second instance of the same class. **Do not assert either before measuring.**

## `T-19-123` — a word naming an ANCESTOR

`rm -rf <root>` is **exit 0**; `rm -rf <root>/alpha` is **exit 2**. `word_is_within` compares COMPONENT VECTORS and returns early on `word.len() < dir.len()` (`policy.rs:5706`), so every ancestor of `<root>/<alias>` — being shorter — answers `false`. `rm -rf <root>/`, `mv <root> /tmp/gone`, `tar -C<root> …`, `find <root> -delete` and `chmod 000 <root>` are all exit 0. **`rm -rf <root>` takes the same nine carriers in one call that the prefix boundary exists to protect**, one component up.

**And the composite the predicate's own doc claims as a narrowing is defeated by it.** `protected_carrier_named` says `cd <env>/<alias> && rm -f pr-ledger.ndjson` *"is refused BY SEGMENT ONE"* — measured true — but `cd <root> && rm -rf alpha` is exit 0, because segment one names the ancestor.

**The root is one `dirname` from the refusal message itself:** `git config --get core.hooksPath` is permitted and reports `<root>/<alias>/hooks`, and `envelope_carrier_refusal` names `<root>/<alias>` in the refusal text. Audit 12 drove it end to end twice, control beside every leg — remote `main` `0658c51` → `140421f`, and a fired PR cap reset.

**It is PINNED PERMITTED BY NAME, and the code does exactly what the pin says.**

```
policy.rs:10022-10026        ("/tmp/envroot", "the PARENT of the envelope directory is not under it.
                              The boundary is the directory this run owns, not everything beside it")
tests/envelope_interior_path.rs:2341
                             ("/tmp/envroot", "the PARENT of the envelope directory is not under it")
```

So this is **not a bug found. It is a DECISION whose harm was never modelled** — absent from the threat register, from every residue clause and from every corpus alphabet until audit 12. This plan measures the harm and hands `19-33` the decision with both pins named in advance. **Audit 12 says plainly that moving them is a design change and not a test edit**, so `19-33` moves them as a NAMED, BOUNDED, CROSS-FENCE exception with each pin's reasoning **REWRITTEN rather than deleted** — the shape round 11 used for `direction_i_…_stays_permitted`, which needed a human authorisation. **This plan names the exception in advance and performs none of it.**

## The `T-19-123` design, DECIDED — what the protected path set becomes, where it stops, and why

**The protected path set becomes:**

```
a candidate is refused when it is
  (1) UNDER `<root>/<alias>`, component-wise                       ← the existing PREFIX, unchanged
  (2) a proper ANCESTOR of `<root>/<alias>` that is itself
      AT OR UNDER `<root>`                                          ← the NEW clause
  (3) EQUAL to `current_exe()`                                      ← the existing EQUALITY, unchanged
```

**How much it grows by, exactly.** `envelope_dir_in(root, alias)` is `<root>/<alias>`, and the alias is a **plain single path component** — `ledger_path_in` refuses anything else outright (*"alias … is not a plain path component, so it has no ledger"*). So the ancestor set of the envelope directory that lies at or under the root is **exactly `{<root>}`: the protected set grows by ONE path.** The clause is nevertheless written in its GENERAL form — *a proper ancestor at or under the root* — so it stays correct if the directory ever becomes deeper, and so that a reader can check it against the code rather than against this arithmetic.

**Where it stops: at `<root>`. Why, derived rather than chosen.** `<root>` is `GSD_MM_ENVELOPE_ROOT`, or the default `~/.local/share/gsd-meta-manager/envelope`; it is created by this tool and contains only the alias directories this tool created. **Above it the chain is `~/.local/share`, `$HOME`, `/tmp`, `/`** — directories shared with everything the user has. A boundary that reached them would refuse `ls /`, `df /`, `du -sh $HOME` and `ls /tmp`, which is **not a boundary but an outage**, and AR-19-11 is this phase's own name for why that matters: *a refusal a user cannot act on is a control that gets switched off*. It is the same argument `word_is_exactly` already gives, one path over, for keeping the binary half an EQUALITY rather than a prefix over a shared `bin` directory.

**The residue this leaves is stated at the same weight as what closes.** An ancestor ABOVE `<root>` — `rm -rf /tmp` when the root is `/tmp/xyz` — reaches the same nine carriers and **no rule is written for it.** It is registered, disclosed, unaccepted, and named on the PATH-SET axis of the restated condition.

**And it is an ANCESTOR clause, NOT the existing PREFIX widened to `<root>`.** The cheaper implementation — compare against `<root>` instead of `<root>/<alias>` — is wrong and the failure is measurable:

```
                                    ancestor clause (mandated)   prefix widened to <root> (forbidden)
rm -rf <root>                       REFUSED                      REFUSED
rm -rf <root>/alpha                 REFUSED                      REFUSED
ls <root>/unrelated-sibling         PERMITTED                    REFUSED   ← the discriminator
GSD_MM_ENVELOPE_ROOT=/tmp set by
the user; ls /tmp/anything-at-all   PERMITTED                    REFUSED   ← the whole /tmp subtree
```

`GSD_MM_ENVELOPE_ROOT` is user-settable, so the second row is reachable configuration rather than a thought experiment. **The corpus pins `ls <ENV>/unrelated-sibling` PERMITTED before and after, so a prefix-widened implementation turns THIS file red rather than turning a driven run unusable.**

**No ancestor clause is written for the BINARY, and that is stated rather than left to be noticed.** `rm -rf <binary-parent>` is exit 0 and removes the binary — audit 12 measured it and added it to `T-19-116`'s route list. `word_is_exactly`'s permit is explicitly reasoned (`ls <parent>` and every `cargo install` must stay permitted) **but the reasoning argues only that a PREFIX would be wrong; it never states that the parent's own DELETION removes the binary.** That is an HONESTY gap, not a rule gap: the residue is real, the reason for leaving it is sound, and the doc must say both. `19-33` states it in `word_is_exactly`'s own doc. **This plan RECORDS `rm -rf <binary-parent>` and asserts it in neither direction.**

## `T-19-124` — the bound must be on WORK, and the audit's own parenthetical is not

Audit 12's measurement, HEAD against a **rebuilt pre-round-12 control binary at `dd17bfb`**, same input, same machine, one word `/a/a/a…`:

```
slashes      HEAD          dd17bfb
  1 000        97 ms         —
  5 000     1 763 ms         —
  8 000     5 873 ms       39 ms      ← GUARD_TIMEOUT_SECS = 5 crossed; a 16 KB command line
 20 000    24 591 ms       47 ms
 50 000  >116 000 ms       72 ms
100 000 chars, NO slashes      66 ms  ← the decisive control: the cost is the SCAN, not the LENGTH
```

`MAX_GUARD_REQUEST_BYTES = 1 MiB` (`hooks.rs:733`) bounds the INPUT and bounds no work at all — **`T-19-120`'s own shape, a bound over the wrong property, in the round that fixed `T-19-120`.**

**Audit 12's suggested remedy has two halves and only one of them is a bound on work.** The suggestion is *"a cap on the number of `/`-anchored candidates per word (or an early return once a candidate is shorter than the shortest protected path, which is the same `word.len() < dir.len()` test hoisted to the byte level)."*

* **The parenthetical half does not restore linearity, and the arithmetic is short enough to write out.** A byte floor at the shortest protected path only skips candidates SHORTER than that floor. For the measured 16 KB word with 8 000 slashes and an envelope directory of about 18 bytes, that is the last **nine** candidates of eight thousand; the other 7 991 each still cost O(word length), and the measured 5 873 ms does not move. **A floor prunes the cheap end of a quadratic.**
* **A candidate CAP is a bound on work and does fail closed — but it must be over a quantity the request cannot multiply.** A per-WORD cap is amplified by the word count and a per-SEGMENT cap by the segment count: a 1 MiB request of many medium words, each spending exactly the cap, defeats either.

**So the property `19-33` must satisfy, and this plan pins, is LINEARITY — that the scan's work is a linear function of the word's length, so that `MAX_GUARD_REQUEST_BYTES` becomes a genuine bound on work rather than only on input — with a FAIL-CLOSED work ceiling beside it as the backstop**, refusing at `ParkReason::EnvelopeAssertionFailed` when a future edit reinstates a super-linear scan, rather than overrunning the deadline. `19-33` chooses the implementation and measures it against this curve on all three trees; **this plan writes the property and the measurement, and asserts no implementation.**

**The corpus pins linearity rather than speed, because speed is a fact about a machine and linearity is a fact about the code.** The mandated row is a RATIO — the guard's answer time at 8 000 slashes divided by its answer time at 2 000 slashes. Four times the input predicts about **4×** linear and about **16×** quadratic; the threshold is **8**, a factor of two of margin either way, and it measures RED today at about 14. Beside it, asserted exit 0 and fast BEFORE AND AFTER, is audit 12's no-slash control. **The 1 MiB slash-dense row is RECORDED and never asserted as a `#[test]`** — measured once under a hard external timeout, with its wall time written down, and handed to `19-33` to assert once it is fast. A test that is RED for hours is not a test.

**The BEHAVIOURAL half — what the agent CLI does with a `PreToolUse` hook that overruns its timeout — is UNMEASURED and is claimed in NEITHER direction**, on the same discipline `C-08`'s, `T-19-117`'s and `T-19-120`'s behavioural halves are held to.

## `T-19-125` and `T-19-126` — two claims and two artefacts

**`T-19-125`.** The disclosed cost says *"contains … as a `/`-anchored substring"*; the comparison NORMALISES. `dd of=<ENV>/./alpha/x` and `dd of=<ENV>/zzz/../alpha/x` are both exit 2 and **neither contains `<ENV>/alpha` as a substring at all**. The true surface is *any word one of whose `/`-anchored substrings NORMALISES to a path under this run's directory or equal to its binary*. It fails CLOSED and the gap is small — **and this phase has already shipped a "FIVE forms" claim, a "four directions" claim and a "SEVEN spellings" claim that were each wrong when written, and the round that removed the count replaced it with a wording that is one operation short.** Recorded beside it: `envelope_carrier_refusal`'s *"To proceed: name a path outside that directory"* (`policy.rs:6168`, `:6180`) is **stale for exactly this case**, where the word merely CONTAINS the directory and the program would never have opened it. This plan asserts the BEHAVIOUR and records both repairs as `19-33`'s.

**`T-19-126`(i) — a gate that cannot be a standing control, and the honest repair is two real pins.** `ledger.rs:354-361` says the link-NON-following stat variant must not be substituted, and adds that **the API name is deliberately not spelled out** because a verify step greps the file for it. Audit 12 found the gate is a plan-time grep that nothing re-runs, and that **no standing pin exists** for the property it stands in for — a symlinked-to-REGULAR ledger being permitted and counted, the control that keeps the KIND check a kind bound rather than a link ban. It also found the stated reason **inverted**: writing the literal to forbid it would turn a zero-occurrence gate **RED**, not make it pass vacuously.

**The repair is structural and it dissolves the inversion.** The pattern already exists one file over: `the_carrier_predicate_asks_the_filesystem_nothing_and_its_own_source_says_so` asserts eight forbidden APIs are absent from a **slice of `policy.rs`'s PRODUCTION half**, and it can name all eight because the names live in the TEST module, BELOW the `#[cfg(test)]` sentinel and therefore outside the slice. So:

* a **BEHAVIOURAL** standing pin — drive a symlinked-to-regular ledger through the built binary, assert exit 0, assert the line was COUNTED, walk the root;
* a **SOURCE-SLICE** standing pin over `ledger.rs`'s production half, asserting the forbidden API appears zero times in the sliced CODE, with the name written in the assertion below the sentinel.

Both are GREEN today and green after — **they are standing controls, not this round's RED**, and that is stated so the SUMMARY does not count them as evidence of a fix. `19-33` corrects the inverted comment.

**`T-19-126`(ii).** `CONTROL_CARRIER_INTERIOR_PATH` entry 7 was re-spelled to `tar --create --file /tmp/t -C<ENV>/alpha` and **does not reach the file under real GNU tar** — *"Cowardly refusing to create an empty archive"*, because the re-spelling moved `-C` last and dropped the `.` member — while the alphabet's doc says every entry was drawn from `19-30`'s measured sweep. The class is **NOT vacuous**: entries 8 and 9 reach. Audit 12 also measured the re-spelling's original motive moot — with a `c`-containing envelope root, both the re-spelled and the original forms answer exit 2, because the carrier clause is raised at the top of `classify_segments`' loop, above program resolution. **The repair is an ADDED reaching entry beside entry 7 and an APPENDED WR-02 note, never a deletion and never a re-spelling.**

## Also required

* **The `SEPARATORS` citation is stale and is re-derived here.** The constant is byte-identical but has moved from `policy.rs:2297` to **`policy.rs:2422`**, because round 12 added prose above it. `git log -L 2422,2422:src/envelope/policy.rs` returns **exactly one** commit for the whole phase, **`84a9b05`** (plan 19-05). This plan's own file cites `:2422`; the three stale citations under `tests/` are NAMED and are `19-33`'s to correct.
* **`cred.rs`'s WHAT IT DOES NOT COVER list has two items and omits the alias-body route.** `git -c alias.q='!git -c credential.helper=store credential fill' q` is **exit 0** and returned the ambient `~/.git-credentials` secret under full envelope posture. **That is `T-19-86`'s route.** Audit 12 measured it, recorded it under `T-19-121`, and deliberately did not fold it into `T-19-86` — *"widening it to carry a credential-reach harm would be the attribution move both `19-27` and audit 11 established must not be made."* **Neither does this plan.** It is MEASURED here and the list item is `19-33`'s to add; the attribution stays where audit 12 put it.

## Round 3's principle is discharged, not weakened

Both new classes read **fields of the ONE segment the existing loop already holds**, at the ONE site `protected_carrier_named` is already raised. The third word class is a third `.chain()` on the same iterator; the ancestor clause is a second comparison inside the same per-candidate walk. **There is no second pass over the segments, no second scan of the argv, no new `ParkReason` and no third reading site. If `19-33`'s design needs a second reading site, that is a finding to report, not a place to add one** — and this plan's job is to make that falsifiable before `19-33` relies on it. Ordering pin B is the mechanical proof and it must stay green.

## What must be RED at this plan's end

Measured at `8659a18` — fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards:

```
exit=0  xargs rm -rf <<< <ENV>/alpha                    <- T-19-122, layer 3, deleted the envelope
exit=0  xargs rm -f  <<< <ENV>/alpha/pr-ledger.ndjson   <- T-19-122, resets a FIRED cap
exit=0  xargs cp /bin/true <<< <BINARY>                 <- T-19-122, the BINARY half
exit=0  xargs -I{} cp /bin/true {} <<< <BINARY>         <- T-19-122, a second spelling
exit=0  xargs rm -rf <<< "x <ENV>/alpha"                <- T-19-122 × the interior scan, composed
exit=0  cat <&0 … / >&2 … naming a carrier path         <- T-19-122, the fd-duplication operators
exit=0  rm -rf <ENV>                                    <- T-19-123, the ANCESTOR, deleted the envelope
exit=0  mv <ENV> /tmp/gone                              <- T-19-123
exit=0  find <ENV> -delete  /  chmod 000 <ENV>          <- T-19-123
exit=0  tar -C<ENV> --create --file /tmp/t .            <- T-19-123 × the interior scan, composed
exit=0  cd <ENV> && rm -rf alpha                        <- T-19-123, defeats the doc's own narrowing
ratio≈14  t(8000 slashes) / t(2000 slashes)             <- T-19-124, the LINEARITY pin, threshold 8
```

All are asserted at their DERIVED post-fix verdict — exit 2 `envelope_assertion_failed`, and ratio < 8 — so they are red now and green after. **Each carries its control and the statement of what makes the pair discriminating.**

## The rows that must NOT move

```
exit=0  cat <<<x  /  cat <<< /tmp/x            <- class 13 is a PATH class, not a here-string ban
exit=2  git <<<x push --force origin main      <- T-19-97 / round 6, force_push_blocked, unchanged
exit=0  ls <ENV>/unrelated-sibling             <- the ANCESTOR-vs-PREFIX discriminator
exit=0  ls /tmp  /  ls <ENV-PARENT>            <- the ancestor STOP; no rule is written above the root
exit=0  ls <BINPAR>  /  cp /bin/true <BINPAR>/some-other-file   <- the EXACT-PATH controls
exit=0  git --git-dir=/tmp/g status            <- pinned PERMITTED in FOUR other files
exit=0  --format=%H / --author= / sed s/x/y/ / a URL / HEAD:refs/…   <- 19-30's whole cost half
exit=0  a 100 000-character word with NO slashes, answered fast     <- audit 12's decisive control
exit=0  GSD_MM_ENVELOPE_ROOT=/tmp/fresh gh pr create --title x      <- unprotected assignment prefix
exit=0  a FRESH root with no ledger; a symlinked-to-REGULAR ledger, PERMITTED and COUNTED
exit=2  <ENV>/alpha/… in every round 4-12 spelling; the fourteen attachment characters
exit=0  rm -rf ~/<ENV-REL>/alpha  /  <ENV>/alph?  /  {…,x}  /  $D/../pr-ledger.ndjson
exit=0  git -c alias.p='!git push --force origin main' p            <- T-19-86, may not move
```

## The rows that are RECORDED and never asserted

Because `19-33` writes **no rule** for them, a row asserted REFUSED lands permanently red in a file `19-33` cannot satisfy, and a row asserted PERMITTED pins a live bypass as correct.

```
rm -rf <binary-parent>                                  <- T-19-116, the binary's ancestor: no rule
rm -rf <ENV-PARENT> when it is /tmp or $HOME            <- T-19-123's residue ABOVE the root
cp /bin/true $(command -v …) / the tilde / cd-relative  <- T-19-116's four original residues
git -c alias.q='!git -c credential.helper=store credential fill' q  <- T-19-86's route; do NOT fold
printf … >> .git/config ; git fp                        <- C-11, T-19-111 (no rule)
printf … >> .claude/settings.json                       <- C-12
rm -f <project>/.git/info/exclude                       <- C-13
rm -rf <project>/.planning/meta-manager/runs/<run_id>   <- C-14
raise pr_cap_* in ~/.config/gsd-meta-manager/config.json <- C-15
printf … > <ENV>/alpha/settings.json                    <- C-08, behavioural half, UNMEASURED
what the agent CLI does with a hook that overruns its timeout  <- T-19-124's behavioural half
GIT_CONFIG_PARAMETERS="'credential.helper=store'" …     <- T-19-104, a different mechanism
a 1 MiB slash-dense word                                <- measured once, wall time recorded
the heredoc form's guard verdict                        <- MEASURED, then recorded as what it is
```

## What this plan is NOT

It closes nothing. `T-19-122` … `T-19-126` all stay open at this plan's end, and **whether `19-33` closes any of them is audit 13's judgement rather than either plan's claim.**

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-33`, or by the two together.** Eight threats remain open at `high` — `T-19-86`, `T-19-91`, `T-19-111`, `T-19-112`, `T-19-116`, `T-19-121`, `T-19-122` and `T-19-123`. The SUMMARY must say so first, and must never write an unqualified "T-19-60 is closed".

Purpose: make the corpus capable of failing on a word the guard deletes and on a path one component up — and prove it does, and prove that bounding either costs nothing where it matters.
Output: a twelfth evidence file, a fourteen-class axis with re-derived floors, two committed RED states, one appended record carrying the three-axis condition and both design answers, and an exact handoff number for `19-33` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-31-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@src/envelope/ledger.rs
@src/envelope/cred.rs
@tests/envelope_wrapper_class.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE both silent axes and the scan's cost, then RED — the twelfth evidence file</name>
  <files>tests/envelope_word_set.rs</files>
  <precondition>
    Work in the main checkout at `8659a18` or later with a clean tree — **ISOLATION=none is forced for
    this phase, so do NOT create a worktree and do NOT check out, reset or stash the working tree at any
    point.** `git diff --numstat f06d153..8659a18 -- src/ tests/` returns nothing, so this base is the
    tree audit 12 measured; verify that rather than assuming it. `cargo build` produces
    `./target/debug/gsd-meta-manager`, which is what every guard measurement is driven against. A real
    `git` binary is on `PATH`, `gh` is installed, and `bash`, `xargs`, `dd`, `tar`, `rsync`, `chmod`,
    `mkfifo`, `find`, `mv` and `python3` are available — **verify each with `command -v` and record the
    result; a row whose program is absent is RECORDED as undrivable, never skipped and never inferred.**
    **`glab` is NOT installed** and no row may depend on it. A writable temp directory outside the
    repository is available for the `dd17bfb` control binary. The four documented flakes are pre-existing,
    and the ETXTBSY race is `C-10`'s own seam, which the here-string binary rows exercise directly, so a
    firing here is recorded rather than fixed.
  </precondition>
  <read_first>
    `tests/envelope_interior_path.rs:1-200` for the harness this file carries forward — `Answer`, `ask`,
    `refuses_carrier`, `permits`, `envelope_files`, `ledger_lines_anywhere`, `simulated_protected`, the
    one-`TempDir`-per-row discipline and the walk — plus its bare-remote fixture, its PR-cap drive, its
    two ORDERING pins and **`every_fenced_unit_pin_and_fenced_corpus_row_keeps_its_answer_under_the_widened_rule`
    at `:2318-2400`, whose row at `:2341` is `T-19-123` pinned PERMITTED by name and whose row at `:2348`
    is the binary's parent — read both and change NEITHER; this file REBUILDS the harness rather than
    citing it, and `19-33` moves those two pins under a named exception.**
    **`src/envelope/policy.rs:3162-3229`** (`RedirectionOperator` and `redirection_operator` — **read the
    twelve-operator match and the `(3, false)` arm for `<<<` at `:3205`, which IS half the `T-19-122`
    finding, and read the doc's statement that the split is a property of the OPERATOR**), `:3231-3300`
    (`RedirectionTarget` and `skip_redirection_target` — **the target's `text` and `literal` are already
    by-products of the walk, which is why the second field costs nothing**), `:3532-3610`
    (**`consume_redirection` — read `:3581`'s `if pathname_target && target.literal`, which is the other
    half of the finding, and read the comment above it claiming the by-product is kept "only when the
    operator takes a FILENAME"**), `:2422` (`SEPARATORS`, read to record byte-identical **at its CURRENT
    line**), `:2486-2560` (`Segment`'s fields, read to record what a second field must sit beside),
    `:5587-5606` (`lexical_absolute_components`), `:5608-5662` (`slash_anchored_candidates` — **read the
    cost paragraph claiming the scan is bounded by the path set, which is TRUE and is about which
    candidates are REFUSED rather than how many are COMPUTED**), `:5664-5711` (**`word_is_within` — the
    `word.len() < dir.len()` early return at `:5706` IS the `T-19-123` finding, and the doc's *"A PREFIX is
    right HERE and wrong one path over"* paragraph is what this round tests**), `:5713-5762`
    (**`word_is_exactly` — read the paragraph reasoning the binary half must not be a prefix, and note that
    it argues only that a PREFIX would be wrong and never that the parent's own DELETION removes the
    binary**), `:5779-6137` (`protected_carrier_named` in full — **the residue condition at `:5891-5921`,
    which this round restates over three axes; the two word classes at `:6118-6128`; and the disclosed cost
    at `:6040-6070`, whose `/`-anchored-substring wording is `T-19-125`**), `:6139-6187`
    (`envelope_carrier_refusal` — **read the two `To proceed:` lines at `:6168` and `:6180`, which are
    stale for `T-19-125`'s case**), `:10001-10047` (**the `word_is_within` unit pins — the ancestor row at
    `:10022-10026` and the `./alpha/…` row's WR-02 comment, read to record and to name for `19-33`**),
    `:10560-10610` (the twelve-operator grammar pin) and **`:10692-10727`
    (`only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` — read the five
    `targets_of(…).is_empty()` rows for `<<EOF`, `<<-EOF`, `<<<here`, `>&2` and `<&0`, because THEY are
    what decide the `T-19-122` design)**.
    **`src/envelope/ledger.rs:307-400`** (**`record_and_check_in`'s `Ok` arm — read the KIND check, the
    paragraph on the link-following stat and the sentence claiming the API name is left unspelled so a
    grep gate does not pass vacuously, which is `T-19-126`(i) and is INVERTED**), and the file's
    `#[cfg(test)]` sentinel position, which is what makes a production-slice pin possible.
    **`src/envelope/cred.rs:416-457`** (**the WHAT IT DOES NOT COVER list — TWO items, and the alias-body
    route is absent**).
    `src/envelope/hooks.rs:730-740` (`MAX_GUARD_REQUEST_BYTES`) and `:1480-1490` (`GUARD_TIMEOUT_SECS`),
    read to record the two constants this round's cost claim is measured against; **do not open this file
    for any other purpose and change nothing in it.**
    `src/envelope/advisory.rs:195-264` (**`SECTION_ENVELOPE`, read to RE-MEASURE its whitespace-token count
    and its widest line and to record the first `Guaranteed` clause still TRUE; do NOT edit**).
    `19-SECURITY.md`'s sections "Threats found by audit 12", "`T-19-119` and `T-19-120` — CLOSED",
    "`T-19-121` — NARROWED HARD, NOT CLOSED", "The three mechanisms as attack surface in their own right",
    "The four execution-time judgements, assessed independently", "The known-open set, as audit 12 verified
    it", "The mechanism pins, verified non-dead", "Gates observed", "Audit 12 — what the round-12 control
    can and cannot fail on" and "Audit 12 Sign-Off".
    `19-31-SUMMARY.md`'s per-binary counts, its `passed + failed` total and its four deviations.
  </read_first>
  <action>
**Measure before you assert. Nothing in this file is written until it has been driven, and every asserted
row names its CONTROL and states what makes the pair discriminating.**

**THE NAMING CONTRACT, WHICH IS WHAT MAKES THIS FILE'S RED GATE ASSERT ROWS RATHER THAN THE MERE PRESENCE
OF FAILURE.** Every `#[test]` fn in this file that is asserted at a DERIVED POST-FIX verdict — and only
those — **is named with the prefix `after_19_33_`.** Every carried-forward pin, every control, every
verdict-preserving row and every `record_only` row is named WITHOUT that prefix. **The consequence is
mechanical: at this plan's end the failing set is exactly the `after_19_33_*` fns, so a broken fixture, a
mis-quoted argv or a harness bug shows up as a failure WITHOUT the prefix and the gate catches it** — where
a bare *"something failed"* check would have passed on any of them. `19-31`'s own RED handoff used this
convention (`after_19_31_*`, eight names); this plan makes it a gate rather than a habit. **The complete
`after_19_33_*` name list is the SUMMARY's handoff contract and `19-33`'s first action is confirming it.**

Build, then drive every guard row the way audits 10, 11 and 12 did: one JSON `PreToolUse` request on stdin
carrying `tool_name` `Bash` and a `tool_input.command`, piped to `./target/debug/gsd-meta-manager envelope
guard alpha` with a fresh `GSD_MM_ENVELOPE_ROOT` from `mktemp -d`, and the whole root walked afterwards.
Build the JSON with `python3 -c` using `json.dumps` so nested quoting survives as the bytes it is.
**Verify by eye that each driven row's argv is the one intended** — a mis-quoted `<`, `<<<` or `=` silently
changes the CLASS on these axes. Prove the walk non-blind on every pass with a positive control that leaves
exactly one ledger line.

**0. THE `dd17bfb` CONTROL BINARY, built WITHOUT touching the working tree.** `git archive dd17bfb | tar -x
-C <tmpdir>` outside the repository, `cargo build` there, and record the resulting binary's path. **Do not
check out, reset or stash.** This binary is what every "round 12 changed X" claim is measured against, and
it is the same control audit 12 used. Record its `--version` and confirm it answers a trivial guard request.

**1. `T-19-122` — driven END TO END, twice, with a control beside every leg.**

Rebuild the bare-remote fixture rather than citing audit 12's: a local bare upstream, a working clone on
`refs/heads/gsd-auto/alpha/w`, and `pre-push`/`pre-commit` written in `stub_body`'s exact three-line shape
and delivered exactly as the envelope delivers them, through the `GIT_CONFIG_COUNT` / `GIT_CONFIG_KEY_0` /
`GIT_CONFIG_VALUE_0` triplet naming `core.hooksPath`. Record the remote's SHA before and after every leg.
Drive, in this order: the plain force push with the hooks in place (expect REFUSED, remote unmoved);
`xargs rm -rf <<< <ENV>/alpha` through the guard (expect exit 0) **beside its twin `rm -rf <ENV>/alpha`
(expect exit 2), which is the whole finding in two lines**; the command performed under real `bash` with the
envelope directory confirmed GONE by a walk; the same push (expect COMPLETED, remote moved, both SHAs
recorded); hooks restored and the remote rewound, same push (expect REFUSED, unmoved).

Then the LEDGER, in ONE persistent root: four `gh pr create --title x` calls (expect 1 permit, 3
`pr_cap_exceeded`, four ledger lines); `rm -f <ledger>` (expect exit 2); `xargs rm -f <<< <ledger>` (expect
exit 0); the command performed; call 5 (expect PERMITTED again, one line). **If any leg does not reproduce,
that is a finding — record the measured behaviour and report it.**

**2. THE WORD-SET SWEEP — the measurement that decides where the FIRST new boundary is.** For each spelling,
record the guard's exit code AND whether real `bash` actually reaches the file, with the space-separated
twin beside it:

  - **all five NON-PATHNAME operators** over both protected paths — `<<<` (here-string), `<<` and `<<-`
    (heredoc delimiter), `>&` and `<&` (fd number) — driven so the class is measured rather than assumed.
    **A spelling whose operator cannot carry a path in any reaching form is RECORDED as such**, with the
    reason, rather than asserted;
  - **the HEREDOC BODY form** `xargs rm -rf <<EOF\n<ENV>/alpha\nEOF`. **`\n` is in `SEPARATORS`, so the
    body's words may already be ordinary tokens of a segment of their own — drive it, record the exit code
    AND the reason identifier, and state which it is.** If it is already exit 2, that is a CORRECTION to
    audit 12's sentence and it is recorded BESIDE audit 12's row, never as an edit to it; if it is exit 0,
    it is a second instance of the class. **Assert neither before measuring.**
  - **the BINARY in the same positions** — `xargs cp /bin/true <<< <BINARY>` and
    `xargs -I{} cp /bin/true {} <<< <BINARY>`, both of which audit 12 measured at exit 0;
  - **the composition with the interior scan** — a here-string word carrying the path INSIDE it, e.g.
    `xargs rm -rf <<< "x <ENV>/alpha"` — because the third word class and round 12's `/`-anchored scan must
    compose at the same site rather than being two rules;
  - **the CONTROLS that keep the class a PATH class rather than a here-string ban**: `cat <<<x` and
    `cat <<< /tmp/x`, both expected exit 0 BEFORE AND AFTER, and
    `git <<<x push --force origin main`, expected exit 2 `force_push_blocked` BEFORE AND AFTER — round 6's
    and `T-19-97`'s own row, which must not move by one character.

Each reaching row is measured exit 0 today and asserted **exit 2 `envelope_assertion_failed`** after, with
the derivation written beside it: `19-33` carries LITERAL non-pathname redirection target words on a SECOND
`Segment` field and chains them as a THIRD word class at the SAME existing reading site, so the identifier
is the general unresolvable one the sibling refusals already carry. **A spelling that real `bash` does NOT
resolve to the file is RECORDED rather than asserted** — a spelling the guard permits but the shell does not
reach is not a bypass, and this is the leg that tells them apart.

**3. `T-19-123` — driven END TO END, twice, and the ANCESTOR-STOP cost measured from BOTH sides.**

Re-drive the bare-remote fixture for the ancestor: **CONTROL** hooks present, force push REFUSED, remote
unmoved and its SHA recorded; **GUARD** `rm -rf <ENV>` exit 0 beside `rm -rf <ENV>/alpha` at exit 2; **LEG
B** performed, envelope directory GONE by walk, same push COMPLETED with both SHAs; **CONTROL** restored and
rewound, REFUSED. Then the cap, in ONE persistent root: fire it with four calls, reset it with `rm -rf
<ENV>` while `rm -f <ledger>` sits at exit 2 beside it, and drive call 5.

Then the SPELLINGS, each with its one-component-deeper twin: `rm -rf <ENV>`, `rm -rf <ENV>/`,
`mv <ENV> /tmp/gone`, `find <ENV> -delete`, `chmod 000 <ENV>`, `tar -C<ENV> --create --file /tmp/t .` (the
ancestor composed with the interior scan), and **`cd <ENV> && rm -rf alpha`, which defeats the composite
`protected_carrier_named`'s own doc claims as a narrowing** — drive the doc's own claimed row
(`cd <ENV>/alpha && rm -f pr-ledger.ndjson`, expected exit 2 by segment one) immediately beside it so the
pair is the finding.

**Then the STOP, which is what makes the design a boundary rather than an escalation.** Drive and assert
**exit 0 BEFORE AND AFTER**:

  - `ls <ENV>/unrelated-sibling` and `rm -f <ENV>/unrelated-sibling` — **the ANCESTOR-vs-PREFIX
    discriminator.** A prefix widened from `<ENV>/alpha` to `<ENV>` refuses these; the mandated ancestor
    clause does not. Say that in the comment, in those terms;
  - a second envelope directory under the same root, `<ENV>/beta`, for the same reason;
  - `ls <ENV-PARENT>` and the literal `/tmp` — **the STOP itself.** No rule is written above the root, and
    these rows are what a clause that walked the whole chain to `/` would turn red;
  - **the USER-SET-ROOT case**, which is reachable configuration and not a thought experiment: drive the
    guard with `GSD_MM_ENVELOPE_ROOT` set to a directory that also holds unrelated files, and assert a word
    naming one of those unrelated files stays exit 0. State in the comment that a prefix-to-the-root
    implementation refuses the whole subtree here;
  - `ls <BINPAR>` and `cp /bin/true <BINPAR>/some-other-file` — the EXACT-PATH controls, unchanged because
    **no ancestor clause is written for the binary.**

**And RECORD, never assert, `rm -rf <binary-parent>`** with its measured exit 0 and the statement that it is
a `T-19-116` route audit 12 added, that `word_is_exactly`'s permit is explicitly reasoned but the reasoning
never says the parent's own deletion removes the binary, and that the doc repair is `19-33`'s.

**4. `T-19-124` — the curve on THREE trees, and the pin is LINEARITY rather than speed.**

Drive one word of the form `/a/a/a…` at 1 000, 2 000, 5 000, 8 000 and 20 000 slashes against **the built
binary at HEAD** and against **the rebuilt `dd17bfb` control binary**, same input, same machine, and record
both wall times per row in a table. Drive audit 12's decisive control on both: **a 100 000-character word
with NO slashes.** Record `GUARD_TIMEOUT_SECS = 5` and `MAX_GUARD_REQUEST_BYTES = 1 MiB` beside the table
with the statement that the second bounds the INPUT and bounds no work.

**Assert exactly two rows and record the rest.**

  - **the LINEARITY RATIO**: `t(8 000 slashes) / t(2 000 slashes)` **< 8**. Derive the threshold in the
    comment — four times the input predicts ≈4× linear and ≈16× quadratic, so 8 leaves a factor of two of
    margin in both directions — and record the measured value today (expected ≈14, RED). State that process
    startup is a fixed additive term in both measurements and that it only ever moves the ratio DOWN, so it
    cannot manufacture a red;
  - **the no-slash control**: the 100 000-character word answers well inside the deadline, exit 0, BEFORE
    AND AFTER. **This is what attributes the cost to the SCAN rather than to the LENGTH**, and without it
    the ratio pin could be satisfied by a rule that simply refused long words.

**THE RATIO ROW IS COMMITTED IN A SEVERANCE-RETIRABLE FORM, AND THAT IS A CONTRACT RATHER THAN A
CONVENTION.** `19-33`'s work-bound task is SEVERABLE — `T-19-124` is `medium` and non-blocking — and a
severance that cannot be taken is not a severance. **So the ratio assertion lives in a `#[test]` fn OF ITS
OWN, carrying that assertion and NOTHING ELSE, named exactly**

```
after_19_33_the_candidate_scan_is_linear_in_the_words_length
```

**and no other row may ride in it.** The no-slash control and every other `T-19-124` row go in separate
fns, so that **under a severance the failure set is exactly this one name.** Record the name verbatim in
the SUMMARY under a heading that says **SEVERANCE-RETIRABLE ROW**, and state there that `19-33`'s two gates
each carry an explicit branch tolerating this one named failure and no other. **This is the seam that
halted `19-23` and needed a human authorisation twice since; it earns a named contract rather than a
precedent.**

**RECORD and do not assert**: the 50 000-slash row and **a 1 MiB slash-dense word**, each measured ONCE
under a hard external timeout with its wall time written down. **A test that is RED for hours is not a
test**; both are handed to `19-33` to assert once they are fast. Record the arithmetic showing audit 12's
parenthetical remedy is insufficient — a byte floor at the shortest protected path skips only the candidates
shorter than that floor, which for the 16 KB word is nine of eight thousand. **And state that the
BEHAVIOURAL half is UNMEASURED and claimed in NEITHER direction.**

**5. `T-19-125` — the normalising instances, asserted at the verdict and recorded at the wording.**

Drive `dd of=<ENV>/./alpha/x` and `dd of=<ENV>/zzz/../alpha/x` and assert **exit 2 BEFORE AND AFTER**, with
the mechanical note that **neither word contains `<ENV>/alpha` as a substring at all** — verify that
mechanically in the test rather than claiming it — beside their non-normalising twin `dd of=<ENV>/alpha/x`.
That pair is what makes the row discriminating. **RECORD** the disclosed cost's wording (`policy.rs:6044-6048`)
verbatim as one operation short, and **RECORD** that `envelope_carrier_refusal`'s *"To proceed: name a path
outside that directory"* is stale for this case, where the word merely CONTAINS the directory and the
program would never have opened it. **Both repairs are `19-33`'s and neither is performed here.**

**6. `T-19-126` — two REAL standing pins, green before and after, and labelled as such.**

  - **BEHAVIOURAL**: in ONE persistent root, place a regular file and symlink the ledger path to it, then
    drive `gh pr create --title x`; assert **exit 0 AND the line COUNTED** (read the tally, walk the root),
    BEFORE AND AFTER. **This is the control that keeps the KIND check a kind bound rather than a link ban**,
    and audit 12 had to measure it by hand because nothing asserted it. Drive the FIFO, directory and
    character-device-symlink rows beside it at exit 2 so the pin is not vacuous, and the fresh-root row at
    exit 0 so a check leaving the `Ok` arm turns this file red;
  - **SOURCE-SLICE**: slice `ledger.rs` from the top of the file to its first `#[cfg(test)]` sentinel and
    assert the link-non-following metadata API — the one `record_and_check_in`'s comment describes without
    spelling — appears **zero times** in that production slice, with the API's literal name written in the
    assertion, which sits BELOW the sentinel and therefore outside the slice. Add a POSITIVE control proving
    the slice is the right region (assert that a literal which IS in the production half is found). **State
    in the test's own doc that this is what dissolves the inversion `T-19-126`(i) records**: a whole-file
    grep cannot name what it forbids; a production-slice assertion can, because the name lives in the test
    half.

**Label both as STANDING PINS that are GREEN before and after**, so the SUMMARY does not count them as
evidence of a fix. The inverted comment is `19-33`'s to correct.

**7. THE FENCED-FILE ENUMERATION, RE-DERIVED MECHANICALLY.** Do not inherit this plan's answer. Run and
record verbatim:

```
rtk proxy grep -rnE '<<<|<<-|<<EOF|>&[0-9]|<&[0-9]' tests/ src/
rtk proxy grep -rnE '<ENV>([ "]|$)' tests/
rtk proxy grep -rn 'PARENT of the envelope directory' tests/ src/
rtk proxy grep -rn 'policy.rs:2297' tests/ src/ .planning/phases/19-*/
```

and state, for every hit, whether the new rules change its verdict. **The planning-time answer is: the
`<<<`/`<<`/`<<-` rows under `tests/` all carry targets (`x`, `EOF`, `here`) that name no protected path, so
NONE of them moves; NO corpus entry names the envelope root alone; and exactly TWO rows carry the ancestor
pin — `policy.rs:10022-10026` and `tests/envelope_interior_path.rs:2341` — both of which are `19-33`'s named
cross-fence exception and neither of which this plan touches.** **Confirm it or report the difference as a
finding.** Then check the `policy.rs` unit pins individually and record each: the `..`-collapse rows, the
`alphax` and `alpha2` sibling rows, the `./alpha/pr-ledger.ndjson` row, the binary's-parent row and both
`each_half_…` rows. **Record explicitly which of them the ancestor clause moves and which it does not**, and
name the three stale `:2297` citations under `tests/` as `19-33`'s to correct.

**8. `SEPARATORS`, RE-DERIVED AT ITS CURRENT LINE.** Run `git log -L 2422,2422:src/envelope/policy.rs` and
record that it returns **exactly one** commit for the whole phase, `84a9b05`; record that `git log -L
2297,2297` now returns `af72137` and says nothing about `SEPARATORS`, which is why the citation is stale
rather than the mechanism moved; and assert the constant byte-identical against its text at `84a9b05`,
`1e56389` and `dd17bfb`. Cite `:2422` everywhere in this file.

**9. The `cred.rs` alias-body route, MEASURED and RECORDED as `T-19-86`'s.** Reproduce the envelope's full
posture — `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` pointed at a `gitconfig` written by
`write_gitconfig_in`, the injected empty-`credential.helper` pair present, a fake `HOME` holding a
`.git-credentials` with a known username and password, `GIT_TERMINAL_PROMPT` as the envelope sets it — and
drive **the CONTROL first** (no `-c`, expect exit 128, secret ABSENT), then
`git -c alias.q='!git -c credential.helper=store credential fill' q` through the guard (expect exit 0) and
against real git (expect the secret PRESENT). **Record the secret as PRESENT/ABSENT rather than transcribing
it** (SAFE-04's reasoning applies to this corpus too). **Record `cred.rs:416-434`'s TWO-item WHAT IT DOES
NOT COVER list verbatim beside it** and state that the list item is `19-33`'s to add. **This row is
`T-19-86`'s route: RECORD it, assert nothing about a rule, and do NOT fold it into `T-19-86`'s declared harm
or use it to re-rate anything** — audit 12 declined to fold it and so does this plan.

**10. The two ORDERING pins, at deliberately DIFFERENT identifiers**, which are the mechanical proof
`19-33`'s widened word set and widened path set are still raised in the ONE per-segment walk rather than in a
second pass. Compose a segment carrying a here-string carrier beside a `--force` push, and a segment naming
the ancestor beside one, in both orders, each with an outside-the-envelope control. **Measure all orderings
before writing any**, and RECORD rather than assert any whose post-fix identifier cannot be derived.

**11. Carry forward every mechanism pin** from rounds 4 through 12, re-asserted in this file over the same
public functions rather than moved: round 5's literalness bit non-vacuous; round 6's deletion model and its
over-deletion control at exit 0; `SEPARATORS` byte-identical with `is_separator(">")` false; **the
SEGMENT-COUNT pins over `split_segments_with_heads`, asserting exactly ONE `Segment` with exactly the
surviving argv for both the `>/dev/null` and the `x2>` rows, with the failure message naming BOTH the SPLIT
and the DISPLACED variant**; round 7's four callee-grammar directions; round 8's confinement clause; round
9's re-parse clause with `aliasx.`/`notalias.` at 0 and both `T-19-86` `!`-bodied rows at 0; round 10's
clause; round 11's redirection and exact-path clauses with both near-miss controls permitted; **and round
12's own — the interior scan at a representative spread of attachment characters, the `credential.helper`
clause with its five near-miss controls PERMITTED at the same identifier, and the ledger KIND check.** All
green today and all green after `19-33`.

**12. The rows that are RECORDED and never asserted**, each with its measured verdict, its walk and the
reason no assertion is possible: `rm -rf <binary-parent>`; the ancestor ABOVE the root; `T-19-116`'s four
original residues; the alias-body credential route; every `C-11` … `C-15` row; `C-08`'s and `T-19-124`'s
behavioural halves; `GIT_CONFIG_PARAMETERS`; the 50 000-slash and 1 MiB rows. Print them under a heading
that says RECORDED, not asserted, and add the mechanical self-assertion this phase uses — a test that reads
this file's own text and proves no repo-side row is written as an assertion.

**13. RE-MEASURE `SECTION_ENVELOPE` and change nothing**: its whitespace-token count against the 215 cap,
its widest line against 80, and that the first `Guaranteed` clause is still TRUE — *"As started"* states
what the envelope ESTABLISHES and stops, so neither new class falsifies it. Record all three numbers.

Print the walk for at least one refused row and one counted row. Commit as its own commit, RED, with
**zero `src/` hunks and zero deletions under `tests/`**.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --test envelope_word_set --no-fail-fast &gt;/tmp/19-32-t1.log 2&gt;&amp;1; rtk proxy grep -q 'test result: FAILED' /tmp/19-32-t1.log || { echo "FAIL: the twelfth evidence file is GREEN against the pre-fix tree — the corpus cannot fail on the word set or on the ancestor"; exit 1; }; rtk proxy grep 'test result:' /tmp/19-32-t1.log; rtk proxy grep -A400 '^failures:$' /tmp/19-32-t1.log | rtk proxy grep -E '^ +[a-z0-9_]+$' | tr -d ' ' | sort -u &gt;/tmp/19-32-t1.names; test -s /tmp/19-32-t1.names || { echo "FAIL: could not extract the failing test names — the RED gate cannot assert rows it cannot read"; exit 1; }; rtk proxy grep -vc '^after_19_33_' /tmp/19-32-t1.names | rtk proxy grep -qx 0 || { echo "FAIL: a failing test is NOT named after_19_33_* — that is a broken fixture, a mis-quoted argv or a harness bug, not this round's derived RED:"; rtk proxy grep -v '^after_19_33_' /tmp/19-32-t1.names; exit 1; }; for n in after_19_33_a_here_string_word_naming_the_envelope_directory_is_refused after_19_33_a_here_string_word_naming_the_guards_own_binary_is_refused after_19_33_a_non_pathname_operator_other_than_a_here_string_reaches_the_same_way after_19_33_a_word_naming_the_ancestor_of_the_envelope_directory_is_refused after_19_33_the_two_segment_composite_naming_the_ancestor_is_refused after_19_33_the_candidate_scan_is_linear_in_the_words_length; do rtk proxy grep -qx "$n" /tmp/19-32-t1.names || { echo "FAIL: the required derived-post-fix row $n is not RED — either it was not written or it is already green"; exit 1; }; done; rtk proxy grep -qx after_19_33_the_candidate_scan_is_linear_in_the_words_length /tmp/19-32-t1.names &amp;&amp; test "$(rtk proxy grep -c 'after_19_33_the_candidate_scan_is_linear_in_the_words_length' tests/envelope_word_set.rs || true)" -ge 1 || { echo "FAIL: the SEVERANCE-RETIRABLE ratio row is missing or not named exactly"; exit 1; }; test -z "$(git show --numstat HEAD -- src/)" || { echo "FAIL: this commit carries src/ hunks"; exit 1; }; test "$(git show --numstat HEAD -- tests/ | awk '{s+=$2} END{print s+0}')" -eq 0 || { echo "FAIL: this commit deletes lines under tests/"; exit 1; }; for t in envelope_interior_path envelope_carrier_reach envelope_control_carrier envelope_wrapper_class envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_advisory envelope_credential envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: UNEXPECTED RED in $t"; exit 1; }; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; test "$(rtk proxy grep -c 'policy.rs:2422' tests/envelope_word_set.rs || true)" -ge 1 || { echo "FAIL: SEPARATORS is not cited at its current line in this file"; exit 1; }; test "$(rtk proxy grep -rl 'SEPARATORS' tests/envelope_wrapper_class.rs tests/envelope_carrier_reach.rs tests/envelope_interior_path.rs | wc -l)" -eq 3 || { echo "FAIL: a fenced file's SEPARATORS citation moved — the three stale sites are 19-33's to correct, not this plan's"; exit 1; }</automated>
  </verify>
  <done>`git diff --numstat f06d153..8659a18 -- src/ tests/` was confirmed empty before the first measurement; a `dd17bfb` control binary was built from a `git archive` extraction OUTSIDE the repository with the working tree never checked out, reset or stashed, and its path and `--version` recorded; `tests/envelope_word_set.rs` exists as the TWELFTH evidence file with its header stating what it is and which round produced it; **`T-19-122` was driven END TO END with the bare-remote fixture REBUILT, both remote SHAs recorded, the envelope directory confirmed GONE by a walk, and the `xargs … <<<` / space-separated twin pair driven side by side, and re-driven on the ledger from a FIRED cap in one persistent root**; **the WORD-SET SWEEP was driven over all five NON-PATHNAME operators, the HEREDOC BODY form with its guard exit code AND reason identifier RECORDED rather than assumed (with any correction to audit 12's sentence written BESIDE audit 12's row), the BINARY in the same positions, the composition with the interior scan, and the three controls — `cat <<<x`, `cat <<< /tmp/x` and `git <<<x push --force origin main` — each asserted at its unchanged verdict**; **`T-19-123` was driven END TO END twice with both remote SHAs and a fired-then-reset cap, over `rm -rf <ENV>`, `rm -rf <ENV>/`, `mv`, `find -delete`, `chmod 000`, `tar -C<ENV>` and `cd <ENV> && rm -rf alpha` with the doc's own claimed composite driven beside it**; **the ANCESTOR STOP was pinned from both sides — `ls <ENV>/unrelated-sibling`, `<ENV>/beta`, `ls <ENV-PARENT>`, the literal `/tmp`, the USER-SET-ROOT case and both binary EXACT-PATH controls all asserted exit 0 BEFORE AND AFTER — with the comment stating that a prefix widened to the root turns each of them red**; `rm -rf <binary-parent>` is RECORDED at its measured verdict and asserted in neither direction; **`T-19-124`'s curve was measured at five sizes on BOTH binaries with audit 12's no-slash control on both, exactly two rows asserted (the LINEARITY RATIO under 8 with its threshold derived in the comment, and the no-slash control), the 50 000-slash and 1 MiB rows RECORDED with wall times under a hard external timeout, the arithmetic showing a byte floor skips nine candidates of eight thousand written out, and the behavioural half stated UNMEASURED**; `T-19-125`'s two normalising instances are asserted exit 2 before and after with the absence of the substring verified mechanically and their non-normalising twin beside them, and both wordings RECORDED as `19-33`'s; **`T-19-126` has TWO REAL STANDING PINS — a symlinked-to-regular ledger driven PERMITTED and COUNTED beside FIFO, directory, character-device and fresh-root rows, and a source-slice pin over `ledger.rs`'s production half naming the forbidden API in an assertion below the sentinel with a positive control proving the slice — both labelled GREEN BEFORE AND AFTER**; the `T-19-126`(ii) reach failure was re-measured under real GNU tar and recorded; **the fenced-file enumeration was re-derived mechanically with all four greps' output recorded verbatim, every hit dispositioned, every `policy.rs` unit pin checked individually, and the TWO ancestor pins plus the THREE stale `:2297` citations named as `19-33`'s**; `SEPARATORS` was re-derived at `policy.rs:2422` with `git log -L` returning exactly `84a9b05` and the `:2297` staleness recorded; the alias-body credential route was driven with the no-`-c` control first, the secret recorded present/absent, `cred.rs`'s two-item list quoted verbatim, and the row RECORDED as `T-19-86`'s and NOT folded; the two ORDERING pins were measured in all orderings before any was written; every mechanism pin from rounds 4 through 12 is re-asserted and green, including the SEGMENT-COUNT pins with a failure message naming BOTH variants; every asserted row names its control and states what makes the pair discriminating; every RECORDED row is proved unasserted by a mechanical self-assertion; `SECTION_ENVELOPE`'s three numbers were re-measured and `advisory.rs` was not touched; the file is RED on exactly the derived-post-fix rows with the failing names recorded verbatim; and this commit shows ZERO `src/` hunks and ZERO deletions under `tests/`.</done>
</task>

<task type="auto">
  <name>Task 2: the AXIS — from twelve classes to fourteen, with classes 1, 2 and 7 all extended</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:7769-7830` (**section 17's header — the argument for why the
    control-carrier axis is a fifth axis, the mechanical reachability proof, and the paragraph on what is
    NOT claimed about text-level disjointness. This task EXTENDS that section and must keep its argument
    true**), `:7821-7876` (`REPRESENTATIVE_ENVELOPE_ROOT` `/tmp/gsd-envelope-root`,
    `REPRESENTATIVE_NEAR_MISS_ROOT`, `REPRESENTATIVE_BINARY_PATH`, `REPRESENTATIVE_BINARY_PARENT`, the four
    placeholders and `SYMLINK_MARKER` — **note that `<ENV>` is the ROOT and `<ENV>/alpha` is the envelope
    DIRECTORY, which is exactly the distinction class 14 is about**), `:7928-7961`
    (**`REDIRECTION_OPERATORS` is reused by `carrier_words`, and `is_redirection_target` is TRUE whenever
    the previous whitespace-separated word is ANY of the twelve — which is why a here-string entry
    satisfies class 2 today**), `:7968-7988` (**class 1, `draws_an_envelope_root_operand`, a
    `starts_with(REPRESENTATIVE_ENVELOPE_ROOT)` — read it and note that a word which IS the root starts
    with itself, which is the class-14 collision**), `:7990-8010` (**class 2,
    `draws_a_redirection_target_carrier` — read the predicate and note it asks only
    `is_redirection_target && starts_with(ROOT)` and does NOT distinguish a PATHNAME operator from a
    non-pathname one, which is the class-13 collision, and read round 11's own refinement note, which is
    the precedent for extending it**), `:8012-8231` (**all remaining class predicates, and in particular
    class 7 `draws_an_ordinary_operand` at `:8100-8112`, the COMPLEMENT, already extended twice; class 11
    `draws_the_guards_own_binary`, an EQUALITY; and class 12 `draws_an_interior_carrier_path` with its
    four exclusions**), `:8233-8340` (`CONTROL_CARRIER_CLASSES` and `CONTROL_CARRIER_REPRESENTATIVES`),
    `:8342-8484` (**every alphabet — `ENVELOPE_ROOT_OPERAND_CARRIERS`' KEPT-OUT-OF-THE-INVARIANCE-ARM doc,
    which is the exact argument this task reuses twice; `CONTROL_CARRIER_REDIRECTION_TARGETS`;
    `CONTROL_CARRIER_INTERIOR_PATH` with its twelve entries and its doc's claim that every entry was drawn
    from `19-30`'s measured sweep, which is `T-19-126`(ii); and
    `CONTROL_CARRIER_REDIRECTION_TARGETS_PRESERVING`'s own record of a DELIBERATE ABSENCE, which is the
    shape this task reuses**), `:8486-8700`
    (`CONTROL_CARRIER_ORDINARY_OPERANDS` and `CONTROL_CARRIER_FAIL_CLOSED_ALPHABETS` — FOUR today),
    `:8702-8722` (`CONTROL_CARRIER_INVARIANCE_ALPHABETS` — EIGHT), `:8724-8790` (the substitution helpers),
    `:8792-8930` (**the case, slot, per-alphabet and per-class arithmetic —
    `CONTROL_CARRIER_CASES = 84`, `CONTROL_CARRIER_SLOTS = 13`, `MIN_CONTROL_CARRIER_CLASSES = 12`,
    `MIN_CONTROL_CARRIER_INTERIOR_PATH = 12`, `MIN_CONTROL_CARRIER_ORDINARY_OPERANDS = 24` and
    `CONTROL_CARRIER_CLASS_COUNTS` with class 7 at 24 where the naive count is 36. ALL EXACT EQUALITIES
    that must be re-derived**), and every `#[test]` from `:8932` to the end of section 17 — the
    degenerate-proofing, the fences, the disjointness proof and the invariance property.
    The four earlier axes' class lists and floors — **read only to confirm they stay byte-identical and
    that `MIN_CONFIG_RESOLUTION_CLASSES` stays at 6**.
    `tests/envelope_wrapper_class.rs:3790-3800` (`REDIRECTION_OPERATORS`, all twelve) and `:4010-4170`
    (`DISPLACING_REDIRECTIONS`, which already names `<<<x` and `<<-EOF` **as a corpus-side operator list**,
    the precedent that a CLASS predicate may name operators where a production rule may not).
    `src/envelope/policy.rs:3193-3229` (**the seven/five split, read so the corpus-side class predicate
    reads bash's grammar rather than inventing a second one**).
    `tests/envelope_wrapper_class.rs`'s anti-vacuity stripper, `POLICY_MIN_PRODUCTION_BYTES`,
    `HOOKS_MIN_PRODUCTION_BYTES` and
    `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`.
    Task 1's measured verdicts, which are what every new entry's arm assignment is derived from.
  </read_first>
  <action>
**Extend section 17 in place. Nothing outside it moves, no existing alphabet ENTRY is deleted, reworded or
narrowed, and NOT ONE LINE anywhere under `tests/` is deleted.**

**THE NAMING CONTRACT APPLIES HERE TOO, in this file's own established form.** `19-30` named its
derived-post-fix axis row `a_command_carrying_a_carrier_path_inside_a_word_is_refused_after_19_31` — a
SUFFIX rather than a prefix, because section 17's names read as sentences. **Every `#[test]` this task adds
that is asserted at a DERIVED POST-FIX verdict carries the suffix `_after_19_33`, and no carried-forward
pin, fence, floor or invariance property does.** **Every floor, count and fence this task adds is satisfiable the moment the
entries exist, so it must be GREEN at this plan's end** — `19-30`'s own run left `envelope_wrapper_class`
at 54 passed / 1 failed, a single derived-verdict row. **The two new fail-closed alphabets are therefore
driven by NEW `#[test]` fns carrying the suffix, and adding them to `CONTROL_CARRIER_FAIL_CLOSED_ALPHABETS`
must NOT turn an existing generative property red.** If it does, that is a COLLISION FINDING — report it
with the property's name and the entry that reached it, exactly as `19-28` reported its class-2 collision —
rather than a red to be absorbed. **The gate asserts this mechanically**, which is what stops a
mis-derived floor or a broken predicate from being mistaken for this round's evidence.

**1. ADD CLASS 13 — a carrier path carried by a word after a NON-PATHNAME redirection operator.** Its
predicate decides on the word's TEXT, on the carrier paths, and on **which operator precedes it**, reading
bash's own seven/five split (`policy.rs:3203-3229`) rather than inventing a second list — the same licence
`DISPLACING_REDIRECTIONS` already exercises for a corpus-side classifier. It holds when a word follows one
of `<<<`, `<<`, `<<-`, `>&` or `<&`, carries a carrier path (at index zero **or** at a non-zero index, so it
composes with class 12's text fact rather than duplicating it), and carries no `$`, no leading `~`, no
pathname-expansion metacharacter and no brace list — **those four exclusions are what keep it disjoint from
classes 3, 8, 9 and 10**, for the reason class 12's doc already gives.

**2. EXTEND CLASS 2 — `draws_a_redirection_target_carrier` — to require a PATHNAME operator, and state the
reason AS a reason.** Today the predicate asks only `is_redirection_target && starts_with(ROOT)`, so a
here-string carrier satisfies it and would be filed under **the class round 11 CLOSED**. That is worse than
a wrong arm: it attributes a live, unruled bypass to a control the record says is closed. **This is
`19-18`'s `{v}>` blocker, `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split, `19-22`'s indirection/confined
split, `19-24`'s re-parsed/confined split, `19-28`'s class-2 collision and `19-30`'s complement extension, a
SEVENTH time — and the SECOND time through class 2 specifically.** Say that in the doc, not only in the
record. **RE-DERIVE mechanically that no existing entry of `CONTROL_CARRIER_REDIRECTION_TARGETS` or
`CONTROL_CARRIER_REDIRECTION_TARGETS_PRESERVING` falls out of class 2 as a result** — every one of them uses
`>` or `:` `>`, which are pathname operators — and record the check rather than the conclusion.

**3. ADD CLASS 14 — a carrier word naming an ANCESTOR of the envelope directory at or under the root.** Its
predicate holds when a word's normalised text is a PROPER ANCESTOR of `<ENV>/alpha` and is at or under
`<ENV>`, with the same four literalness exclusions. **It is DISJOINT from class 12 by construction** (class
12 requires a non-zero index; an ancestor word carries the path at index zero) **and from class 11 by
construction** (that is an equality over the binary).

**4. EXTEND CLASS 1 — `draws_an_envelope_root_operand` — to exclude a word naming the root EXACTLY, and
hand it to class 14.** `starts_with(REPRESENTATIVE_ENVELOPE_ROOT)` is true of the root itself, so without
this the ancestor entries land in **the class round 10 closed** — the same misattribution as class 2's, one
path over. This is the same shape of refinement round 11 made when it excluded globs and braces from class
1. **RE-DERIVE mechanically that no existing entry names the root exactly** (Task 1's second grep is the
evidence: every `<ENV>` in every alphabet is followed by `/alpha` or another component) **and record the
check**, because a refinement that emptied a class would push its entries into class 7, the invariance arm.

**5. EXTEND `draws_an_ordinary_operand` — class 7, the COMPLEMENT — to exclude classes 13 and 14, and state
why even though neither can currently reach it.** With classes 1 and 2 extended as above, an ancestor entry
and a here-string entry would otherwise fall out of every named class and into the complement, which is in
the INVARIANCE arm — asserting PERMITTED a row `19-33` REFUSES, permanently red in a file `19-33` may only
add to. **The extension is written so the exclusion does not depend on the ORDER in which classes 1 and 2
were narrowed**, which is the property that would otherwise have to be re-derived every round.

**6. ADD the two fail-closed alphabets, drawn from Task 1's MEASURED rows and only from those.**

  - **`CONTROL_CARRIER_NON_PATHNAME_TARGET`** must contain, at minimum: a here-string naming the envelope
    DIRECTORY; a here-string naming the BINARY; **at least one entry under an operator other than `<<<`**,
    because an alphabet that only ever draws `<<<` is an alphabet that certifies a here-string-only rule;
    and **at least one entry whose carrier path sits at a NON-ZERO index inside the here-string word**, so
    the composition with round 12's interior scan is drawn rather than assumed. At least one entry is drawn
    under a program name absent from both production halves. **Any operator Task 1 measured as unable to
    carry a reaching path is RECORDED in the doc as a deliberate absence with its reason**, the way
    `CONTROL_CARRIER_REDIRECTION_TARGETS_PRESERVING` already records one.
  - **`CONTROL_CARRIER_ANCESTOR`** must contain, at minimum: `rm -rf <ENV>`; a trailing-slash spelling; a
    non-`rm` spelling that reaches (`mv`, `find -delete` or `chmod 000`, whichever Task 1 measured
    reaching); **an ancestor carried at a non-zero index** (`tar -C<ENV> …`), so it composes with the
    interior scan; and **the two-segment composite `cd <ENV> && rm -rf alpha`**, which is the row that
    defeats the narrowing `protected_carrier_named`'s own doc claims.

Both alphabets state the arm they are in and why, in the shape `ENVELOPE_ROOT_OPERAND_CARRIERS`' own doc
already uses, and both are added to `CONTROL_CARRIER_FAIL_CLOSED_ALPHABETS` (four → six).

**7. GROW `CONTROL_CARRIER_ORDINARY_OPERANDS` with the measured ANCESTOR-STOP and WORD-SET controls** — a
sibling under the root, a second alias directory under the root, the root's own PARENT, the binary's parent,
a here-string carrying a path that is not protected, and a here-string carrying no path at all. Every one is
verdict-PRESERVING and stays in the invariance arm. **Add a fence asserting the ancestor pair BY NAME** —
`rm -rf <ENV>` in the fail-closed alphabet and `ls <ENV>/unrelated-sibling` permitted — **because the pair
differing in one component is what proves the clause is an ANCESTOR clause rather than a prefix widened to
the root.**

**8. ADD the two new fences, and make each COUNT rather than describe.**

  - the **WORD-SET fence**: assert mechanically that the axis draws at least one carrier path after a
    NON-PATHNAME redirection operator, **and that at least one such entry is drawn under an operator other
    than `<<<`** — counting entries, not prose. Its failure message says the correct response is to ADD the
    spelling, never to relax the fence, and names the here-string-only trap explicitly;
  - the **ANCESTOR-STOP fence**: assert mechanically that the axis draws at least one ANCESTOR entry in the
    fail-closed arm **and at least one word at or above the root's own parent in the invariance arm**, so
    an implementation that walked the whole ancestor chain to `/` turns this file red. Its failure message
    states that the stop is at the root and why.

**9. EXTEND the two existing mechanical fences.** The NO-PROGRAM-NAMES fence must now also draw at least one
class-13 and one class-14 row under a program name absent from both production halves. The
PATH-PREFIX-NOT-BASENAME fence keeps its entries and gains a statement that **the ancestor clause is a
comparison the OTHER WAY ROUND over the same component vectors** and is bounded at the root, which is the
mechanical reason it adds exactly one path.

**10. `T-19-126`(ii) — ADD, never re-spell.** Add a reaching `tar -C<ENV>/alpha …` entry beside entry 7 of
`CONTROL_CARRIER_INTERIOR_PATH`, drawn from Task 1's re-measurement under real GNU tar, and **APPEND** a
WR-02 note to the alphabet's doc recording that entry 7 as re-spelled does not reach the file, why (the
re-spelling moved `-C` last and dropped the `.` member), that the class is NOT vacuous because entries 8 and
9 reach, and that audit 12 measured the re-spelling's original motive moot — with a `c`-containing envelope
root BOTH forms answer exit 2, the carrier clause being raised above program resolution. **Entry 7 stays.
Nothing is deleted or reworded.** `MIN_CONTROL_CARRIER_INTERIOR_PATH` rises from 12 by exactly one.

**11. RE-DERIVE the arithmetic FROM ALL FOUR PREDICATE EXTENSIONS AS WELL AS THE TWO NEW CLASSES, and state
it as exact equalities.** `MIN_CONTROL_CARRIER_CLASSES` becomes **14**; `CONTROL_CARRIER_SLOTS` becomes
**15**; `CONTROL_CARRIER_CASES` becomes 84 plus the new fail-closed entries plus the new ordinary entries
plus the one added interior entry; `MIN_CONTROL_CARRIER_ORDINARY_OPERANDS` rises from 24 by exactly the
number added; `MIN_CONTROL_CARRIER_NON_PATHNAME_TARGET` and `MIN_CONTROL_CARRIER_ANCESTOR` are set to their
alphabets' exact lengths; and `CONTROL_CARRIER_CLASS_COUNTS` is recomputed per class over the whole
generated set **with each count's derivation written beside it — including class 2's and class 7's, which
FALL relative to the naive count because those predicates now exclude the new classes, and those falls are
the arithmetic proof the extensions took effect.** Every deliberate overlap is RECORDED as an overlap rather
than smoothed away, the way class 4's `ln -s` mitigation already is. **Audit 5 found `19-16` set a floor of
50 against a maximum of 40 by construction; state the arithmetic and CHECK it against the alphabets rather
than against the prose.**

**12. RE-PROVE disjointness and the class structure over all fourteen**, extending the existing
degenerate-proofing rather than replacing it: one representative per class, each satisfying its own
predicate and no other except where an overlap is RECORDED with its reason. **Keep section 17's honest
limitation intact** — the disjointness asserted is about REACHABILITY, not about text.

**13. Leave everything else byte-identical.** The four earlier axes, their predicates, their
degenerate-proofing and their floors do not move; `MIN_CONFIG_RESOLUTION_CLASSES` stays at 6; the
anti-vacuity stripper's three protections keep their present strictness; and
`wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` stays green.

Commit as its own commit, with **zero `src/` hunks and zero deletions under `tests/`**. Record which of this
file's tests are RED and why each is red — a floor that cannot be met until `19-33` lands is a different
kind of red from a verdict assertion, and the SUMMARY distinguishes them.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast &gt;/tmp/19-32-t2.log 2&gt;&amp;1; rtk proxy grep -q 'test result: FAILED' /tmp/19-32-t2.log || { echo "FAIL: the axis is GREEN against the pre-fix tree — the two new classes cannot fail on it"; exit 1; }; rtk proxy grep -A400 '^failures:$' /tmp/19-32-t2.log | rtk proxy grep -E '^ +[a-z0-9_]+$' | tr -d ' ' | sort -u &gt;/tmp/19-32-t2.names; test -s /tmp/19-32-t2.names || { echo "FAIL: could not extract the axis's failing test names"; exit 1; }; rtk proxy grep -vc '_after_19_33$' /tmp/19-32-t2.names | rtk proxy grep -qx 0 || { echo "FAIL: an axis failure is NOT suffixed _after_19_33 — that is a broken predicate, a mis-derived floor or a collision, not this round's derived RED:"; rtk proxy grep -v '_after_19_33$' /tmp/19-32-t2.names; exit 1; }; rtk proxy cargo test --test envelope_word_set --no-fail-fast &gt;/tmp/19-32-t2b.log 2&gt;&amp;1; rtk proxy grep -q 'test result: FAILED' /tmp/19-32-t2b.log || { echo "FAIL: the twelfth evidence file went GREEN without a production change"; exit 1; }; rtk proxy grep 'test result:' /tmp/19-32-t2.log /tmp/19-32-t2b.log; rtk proxy grep -nE 'MIN_CONTROL_CARRIER_CLASSES|MIN_CONFIG_RESOLUTION_CLASSES|CONTROL_CARRIER_CASES|CONTROL_CARRIER_SLOTS|MIN_CONTROL_CARRIER_NON_PATHNAME_TARGET|MIN_CONTROL_CARRIER_ANCESTOR|MIN_CONTROL_CARRIER_INTERIOR_PATH|MIN_CONTROL_CARRIER_ORDINARY_OPERANDS' tests/envelope_wrapper_class.rs; test "$(rtk proxy grep -c 'MIN_CONTROL_CARRIER_CLASSES: usize = 14' tests/envelope_wrapper_class.rs || true)" -eq 1 || { echo "FAIL: the class floor did not rise to fourteen"; exit 1; }; test "$(rtk proxy grep -c 'MIN_CONFIG_RESOLUTION_CLASSES: usize = 6' tests/envelope_wrapper_class.rs || true)" -eq 1 || { echo "FAIL: MIN_CONFIG_RESOLUTION_CLASSES moved"; exit 1; }; for t in envelope_interior_path envelope_carrier_reach envelope_control_carrier envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_pr_cap envelope_credential envelope_advisory envelope_wiring envelope_hook_refusals envelope_wrapper_bypass; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: UNEXPECTED RED in $t"; exit 1; }; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; test -z "$(git show --numstat HEAD -- src/)" || { echo "FAIL: this commit carries src/ hunks"; exit 1; }; test "$(git diff --numstat HEAD~1..HEAD -- tests/ | awk '{s+=$2} END{print s+0}')" -eq 0 || { echo "FAIL: this commit deletes lines under tests/"; exit 1; }</automated>
  </verify>
  <done>Section 17 names FOURTEEN classes with `MIN_CONTROL_CARRIER_CLASSES = 14` and `CONTROL_CARRIER_SLOTS = 15`; **class 13 decides on the word's TEXT, on the carrier paths and on which of bash's five NON-PATHNAME operators precedes the word — reading `policy.rs:3203-3229`'s split rather than inventing a second list — and excludes `$`, leading `~`, metacharacter and brace words so it is disjoint from classes 3, 8, 9 and 10**; **class 14 decides on a word whose normalised text is a PROPER ANCESTOR of `<ENV>/alpha` at or under `<ENV>`, disjoint from classes 11 and 12 by construction**; **class 2 is EXTENDED to require a PATHNAME operator and class 1 to exclude a word naming the root exactly, each with its reason stated AS a reason, each naming the `19-18`/`19-20`/`19-22`/`19-24`/`19-28`/`19-30` precedent and the SEVENTH-time count, and each RE-DERIVED mechanically to move no existing entry out of its class — with the check recorded rather than the conclusion**; **`draws_an_ordinary_operand` — the COMPLEMENT — is EXTENDED to exclude classes 13 and 14 in a form that does not depend on the order the other narrowings were applied**; two new fail-closed alphabets `CONTROL_CARRIER_NON_PATHNAME_TARGET` and `CONTROL_CARRIER_ANCESTOR` exist in `CONTROL_CARRIER_FAIL_CLOSED_ALPHABETS` (four → six), drawn only from Task 1's measured rows, the first carrying a here-string over BOTH protected paths, an entry under an operator other than `<<<`, an entry whose path sits at a non-zero index and any unreachable operator RECORDED as a deliberate absence, and the second carrying `rm -rf <ENV>`, a trailing-slash spelling, a non-`rm` reaching spelling, an ancestor at a non-zero index and the `cd <ENV> && rm -rf alpha` composite; `CONTROL_CARRIER_ORDINARY_OPERANDS` grew by the measured ANCESTOR-STOP and WORD-SET controls with the ancestor pair fenced BY NAME; **a WORD-SET fence and an ANCESTOR-STOP fence both assert by COUNT — the first that a non-`<<<` operator is drawn, the second that a word at or above the root's parent sits in the invariance arm — each with a failure message naming its trap**; the NO-PROGRAM-NAMES fence draws a class-13 and a class-14 row and the PATH-PREFIX-NOT-BASENAME fence states that the ancestor comparison is the same component vectors the other way round, bounded at the root; **`T-19-126`(ii)'s reaching `tar -C` entry was ADDED beside entry 7 with an APPENDED WR-02 note and entry 7 left in place, nothing deleted or reworded**; every `MIN_*`, `CONTROL_CARRIER_CASES`, `CONTROL_CARRIER_SLOTS` and per-class count is RE-DERIVED from the alphabets AND from all four predicate extensions and stated as an exact equality with its arithmetic beside it — **including class 2's and class 7's falls, which are the arithmetic proof the extensions took effect** — and every deliberate overlap is RECORDED; disjointness is re-proved over all fourteen with section 17's reachability-not-text limitation intact; the four earlier axes, their predicates, their degenerate-proofing and their floors are byte-identical and `MIN_CONFIG_RESOLUTION_CLASSES` is still 6; `git diff --numstat` over `tests/` shows ZERO deletions; and this commit shows ZERO `src/` hunks.</done>
</task>

<task type="auto">
  <name>Task 3: the three-axis condition, both design answers, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s "Threats found by audit 12" table (the `T-19-122` … `T-19-126` rows in full),
    "`T-19-119` and `T-19-120` — CLOSED, each on a CLASS sweep rather than a spelling list",
    "`T-19-121` — NARROWED HARD, NOT CLOSED", "The three mechanisms as attack surface in their own right",
    "The four execution-time judgements, assessed independently", "The known-open set, as audit 12 verified
    it", "The mechanism pins, verified non-dead", "Gates observed", "Audit 12's bookkeeping", "Audit 12 —
    what the round-12 control can and cannot fail on" and "Audit 12 Sign-Off" — **including its closing
    two paragraphs, whose finish-line sentence this plan's subsection quotes verbatim**; plus the appended
    execution records of plans 19-30 (`:12272`) and 19-31 (`:12761`), **which this plan's subsection
    FOLLOWS and does not edit, and beside which the correction to audit 11's non-pathname generalisation is
    written.** The Accepted Risks Log, and `AR-19-04` and `AR-19-05` in particular.
    `deferred-items.md`'s plan-19-30 and plan-19-31 sections and its "Still OPEN and unchanged" rows.
    `19-31-SUMMARY.md`'s per-binary counts and its recorded `passed + failed` total (**1871 over 47 result
    lines with 13 ignored, 18 `envelope_*` binaries**).
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-31 record and touching nothing that
precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan 19-32 —
carrying:

  - **FIRST, and in these words: this plan closes nothing and `/gsd-secure-phase 19` is NOT cleared.**
    `T-19-86`, `T-19-91`, `T-19-111`, `T-19-112`, `T-19-116`, `T-19-121`, `T-19-122` and `T-19-123` all
    remain open at `high`; `T-19-124` at `medium`; `T-19-125` and `T-19-126` at `low`; and whether `19-33`
    closes any of them is audit 13's judgement rather than either plan's claim.
  - **AUDIT 12'S FINISH LINE, QUOTED VERBATIM, AS THE REASON THIS ROUND EXISTS** — the paragraph beginning
    *"the open set is NOT yet the human-decision items plus the deliberately-unruled residues"* through
    *"When those two are ruled or accepted, the sentence becomes true — and it will be worth writing then."*
    State plainly that **this plan does not write that sentence and neither does `19-33`** — it is audit
    13's to write or to withhold.
  - **THE RESTATED RESIDUE CONDITION, OVER ALL THREE AXES, WITH NO COUNT AND HANDED TO NO CONTROL.** This
    is the round's central product. State it in the shape the predicate's doc will carry after `19-33`:

    > **AXIS 1 — WHICH WORDS REACH THE READER.** This predicate reads every LITERAL word of the segment
    > that is not an operator, every LITERAL PATHNAME redirection target, and every LITERAL NON-PATHNAME
    > redirection target. **Its silence on this axis is a word the tokenizer produced for no class at all**
    > — a production that failed to complete, which fails closed at `Token::redirection_unresolvable`
    > rather than being read.
    >
    > **AXIS 2 — WHAT THE READER SEES IN A WORD IT RECEIVED.** It reads every `/`-anchored substring of
    > that word's text, normalised. **Its silences are a word the SHELL MAY REWRITE**, because the guard
    > cannot know its final text; **a word whose TEXT CARRIES NO ABSOLUTE PATH ANYWHERE**, because there is
    > nothing in it to normalise; **and a word that reaches a protected path ONLY THROUGH A LINK**, because
    > no lexical reading of the text names it.
    >
    > **AXIS 3 — WHICH PATHS THE COMPARISON PROTECTS.** A candidate under this run's envelope directory, a
    > candidate that is a proper ANCESTOR of it at or under this run's envelope ROOT, and a candidate equal
    > to this run's own binary. **Its silences are an ancestor ABOVE the root — `$HOME`, `/tmp`, `/` — and
    > the binary's PARENT and every ancestor of it**, because refusing a word that names one of those is an
    > outage rather than a boundary (AR-19-11), which is the same argument the binary half is an EQUALITY
    > for.

    **State on each axis exactly what the measurement showed rather than what the plan predicted**, note
    that no count is written on any axis, and state that **none of it is handed to a pin, a schedule or a
    version witness** — `19-25`'s witness schedules a re-audit against a FUTURE GIT and every one of these
    is reachable by the driven agent TODAY.
  - **`T-19-122` DRIVEN END TO END, TWICE**, with both remote SHAs, the fired-then-reset cap, the
    `xargs … <<<` / space-separated twin pair, the binary half's two spellings, the composition with the
    interior scan, and **the heredoc form's guard verdict recorded as measured** — with any correction to
    audit 12's sentence stated BESIDE audit 12's row and never as an edit to it.
  - **THE `T-19-122` DESIGN ANSWER, AND THE DERIVATION THAT DECIDES IT.** The second `Segment` field and the
    third word class; the table of what design (A) turns red and what design (B) does not, naming
    `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator
    grammar pin; the rejected third design (*treat every deleted word as unresolvable*) with its cost
    stated; and **the principled restatement — the predicate's subject is what a line NAMES, not what the
    shell does with a word, so the word set is every LITERAL word the line contains.** Plus the disclosed
    over-refusal (`cat <<< <ledger>` becomes refused, the same family as `cat <ledger>`) and its two
    permitted controls.
  - **THE PROCESS FINDING, STATED PLAINLY AND WRITTEN BESIDE RATHER THAN AS AN EDIT.** Audit 11 recorded
    the five non-pathname operators as *"correctly NOT recorded as carriers, so the round bought its rule
    without an over-refusal"* with `cat <<<X` as the witness. **The observation was right about `cat` and
    the generalisation was wrong**, because a here-string is a DATA channel and `xargs` turns data into
    argv. Name it as the same shape as `19-28` calling `T-19-119`'s spelling *"Not a defect"* — **a
    recorded-and-misgeneralised observation is how a finding survives a round that already saw it** — and
    edit neither subsection.
  - **`T-19-123` DRIVEN END TO END, TWICE**, with both remote SHAs and the fired-then-reset cap, the full
    spelling table, and the composite `cd <ENV> && rm -rf alpha` beside the doc's own claimed narrowing.
  - **THE `T-19-123` DESIGN ANSWER, WITH THE PATH SET, THE STOP AND THE COST.** What the protected path set
    becomes, in its general form; **that it grows by EXACTLY ONE path because the alias is a plain single
    component and `ledger_path_in` refuses anything else**; **exactly where it stops — at `<root>` — and
    why, in ownership terms, with the chain above it named (`~/.local/share`, `$HOME`, `/tmp`, `/`) and the
    AR-19-11 argument stated**; **why it is an ANCESTOR clause and not the existing PREFIX widened to the
    root**, with the four-row discriminator table including the user-set-`GSD_MM_ENVELOPE_ROOT` case; and
    **the residue above the root, registered, disclosed and unaccepted.** State that **no ancestor clause is
    written for the binary**, that `rm -rf <binary-parent>` is a `T-19-116` route audit 12 added, and that
    `word_is_exactly`'s reasoning argues only that a PREFIX would be wrong and never that the parent's own
    deletion removes the binary — **an HONESTY gap `19-33` repairs in that doc, not a rule gap.**
  - **BOTH FENCED PINS NAMED IN ADVANCE, WITH THE EXCEPTION SPELLED OUT.** `policy.rs:10022-10026` and
    `tests/envelope_interior_path.rs:2341` both pin `T-19-123` PERMITTED by name and both are GREEN and
    NON-VACUOUS today. Audit 12: *"the code does exactly what the pin says. What is missing is the threat
    row and the residue clause, not the implementation."* State that **`19-33` moves both as a NAMED,
    BOUNDED, CROSS-FENCE exception with each pin's reasoning REWRITTEN rather than deleted** — the shape
    round 11 used for `direction_i_…_stays_permitted` — and that **this plan performs none of it.**
  - **`T-19-124`'s CURVE ON THREE TREES**, with the full table, audit 12's no-slash control, the two
    constants, **the arithmetic showing a byte floor skips nine candidates of eight thousand and does not
    restore linearity**, the statement that a per-word or per-segment candidate cap is amplified by the
    word or segment count, **the property `19-33` must satisfy (LINEARITY, with a fail-closed work ceiling
    beside it as the backstop)**, the reason the corpus pins a RATIO rather than a speed, why the 1 MiB row
    is RECORDED and not asserted, and **the behavioural half UNMEASURED and claimed in NEITHER direction.**
  - **`T-19-125` and `T-19-126` REGISTERED WITH THEIR REPAIRS NAMED AS `19-33`'s.** The disclosed cost's
    wording quoted verbatim with the two normalising instances beside it; `envelope_carrier_refusal`'s stale
    recovery line named; **the `ledger.rs` gate recorded as a one-shot rather than a standing control with
    its stated reason recorded INVERTED**, and the two REAL standing pins this plan wrote described as
    GREEN before and after so they are not counted as evidence of a fix; and entry 7's reach failure with
    the ADD-don't-re-spell repair.
  - **THE `SEPARATORS` CITATION RE-DERIVED at `policy.rs:2422`** with `git log -L 2422,2422` returning
    exactly `84a9b05`, `git log -L 2297,2297` returning `af72137` and saying nothing about `SEPARATORS`, the
    constant byte-identical against `84a9b05`, `1e56389` and `dd17bfb`, and **the three stale citations
    under `tests/` named as `19-33`'s to correct.**
  - **THE `cred.rs` ALIAS-BODY ROUTE, MEASURED AND RECORDED AS `T-19-86`'s.** The drive with its no-`-c`
    control, the secret recorded present/absent, `cred.rs:416-434`'s two-item list quoted verbatim, and the
    statement that **audit 12 declined to fold it into `T-19-86` and so does this plan** — the list item is
    `19-33`'s to add and the attribution does not move.
  - the axis arithmetic, fourteen classes with every floor re-derived and class 2's and class 7's falls
    stated; the mechanism pins and byte floors re-measured across rounds 4 through 12; **`SECTION_ENVELOPE`
    re-measured and NOT edited**, with its three numbers and `19-33` prohibited from opening it; the
    `glab --host` cell unchanged with `--hostname` KEPT and `glab` confirmed not installed; `C-08`'s,
    `T-19-120`'s and `T-19-124`'s behavioural halves UNMEASURED; `C-11` … `C-15` at control (e) with no
    `pr_cap_*` clamp; **`T-19-17r` OUTSTANDING for the seventeenth time with no `AR-19-13` and no use of
    the word "accepted"**; `AR-19-04`, `AR-19-05` and `T-19-23` untouched; and the gate.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any earlier
appended subsection.

**2. `deferred-items.md`.** Append a plan-19-32 section registering **`T-19-122` and `T-19-123` at `high`**
with their corpus recorded RED, **`T-19-124` at `medium`** with the work-bound property and the behavioural
half UNMEASURED, and **`T-19-125` and `T-19-126` at `low`** with their repairs registered as `19-33`'s
whether or not any rule lands. Replace the reading-only residue with the THREE-AXIS condition wherever this
plan's own section states it, and record that the docs still state it over one axis and that `19-33`
restates them. Record that **`T-19-116` gained TWO routes this audit** — the here-string spelling and
`rm -rf <binary-parent>` — **and that closing `T-19-122` closes only the first.** Carry `C-08`, `C-15`,
`T-19-110` and the `glab --host` cell forward unchanged; and leave `T-19-86`, `T-19-91`, `T-19-96`,
`T-19-111`, `T-19-112`, `T-19-113`, `T-19-115`, `T-19-121`, `T-19-74` and the `T-19-17r` bookkeeping gap
unchanged and open.

**3. The gate, with its arithmetic STATED and CHECKED.**

  - The command is `rtk proxy cargo test --no-fail-fast`, counted with `rtk proxy grep` over a redirected
    log, never a plain `grep` (D-34).
  - Baseline is **1871** on `passed + failed`, recorded by `19-31-SUMMARY.md` over **47** result lines with
    **13** ignored and independently re-measured by audit 12. After this plan `passed + failed` is strictly
    greater than 1871; **a red test RAN, so red→green leaves the total unchanged and every increase comes
    ONLY from new `#[test]` fns** — count them from `git show` over both code commits and check the
    identity. A disagreement is a finding.
  - **This plan's gate NAMES its expected failures rather than requiring zero.** Record the complete RED
    name list verbatim and the complete PERMITTED list — both are `19-33`'s handoff contract, and `19-33`'s
    first action is confirming both.
  - **All NINETEEN `envelope_*` binaries must RUN**, with their per-binary counts recorded. There are
    EIGHTEEN today. **A run reporting eighteen is a run in which this plan's own evidence file did not
    execute.**
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.
  - The four documented flakes are out of scope; record any firing with its verbatim output and state that
    absence is not evidence they are fixed. **The ETXTBSY race is `C-10`'s own seam and the here-string
    binary rows exercise it directly.**

**4. THE HANDOFF ANCHOR, WHICH IS A CONTRACT RATHER THAN TWELVE ROUNDS OF CONVENTION.** `19-33`'s final
gate must compare a diff range against this plan's last commit, and until now every round has resolved that
range from a commit-message SHAPE that no plan ever mandated. **Two anchors are established here and
`19-33` requires them to AGREE:**

  - **the record commit's message begins exactly `docs(19-32)`** — mandated, not assumed;
  - **the record commit is the ONE that adds `19-32-SUMMARY.md`**, so
    `git log --format=%H -n 1 -- .planning/phases/19-gitsafe-git-blast-radius-envelope/19-32-SUMMARY.md`
    resolves it from FILE CONTENT rather than from a message. **Commit the SUMMARY in this third commit
    together with the record**, and record both the SHA and the fact that the two anchors agree in the
    SUMMARY itself. **A gate that depends on a message shape is a gate one reworded commit disarms.**

**5. THE SEVERANCE-RETIRABLE ROW, DECLARED IN THE SUMMARY UNDER ITS OWN HEADING.** Record
`after_19_33_the_candidate_scan_is_linear_in_the_words_length` verbatim as the ONE row `19-33` may leave
red under a severance of its work-bound task, state that it carries that assertion and nothing else, and
state that **`19-33`'s two gates each carry an explicit branch tolerating exactly that name and no other.**
Every other `after_19_33_*` row and every `_after_19_33` axis row must go green.

Commit the record as its own commit, the third, with **zero `src/` hunks**.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: build"; exit 1; }; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || { echo "FAIL: clippy"; exit 1; }; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-32-gate.log 2&gt;&amp;1; echo "full_suite_exit=$? (RED is EXPECTED here — this plan names its failures)"; rtk proxy grep 'test result:' /tmp/19-32-gate.log | tail -55; git log --oneline -4; rtk proxy grep -q 'test result: FAILED' /tmp/19-32-gate.log || { echo "FAIL: the gate is fully GREEN — this plan is required to end RED"; exit 1; }; rtk proxy grep -A400 '^failures:$' /tmp/19-32-gate.log | rtk proxy grep -E '^ +[a-z0-9_]+$' | tr -d ' ' | sort -u &gt;/tmp/19-32-gate.names; rtk proxy grep -vcE '^after_19_33_|_after_19_33$' /tmp/19-32-gate.names | rtk proxy grep -qx 0 || { echo "FAIL: a suite failure is neither an after_19_33_* row nor an _after_19_33 axis row — a documented flake fired or something unrelated broke, and this plan's RED is not that:"; rtk proxy grep -vE '^after_19_33_|_after_19_33$' /tmp/19-32-gate.names; exit 1; }; rtk proxy grep -qx after_19_33_the_candidate_scan_is_linear_in_the_words_length /tmp/19-32-gate.names || { echo "FAIL: the SEVERANCE-RETIRABLE ratio row is not among the failures"; exit 1; }; ANCHOR=$(git log --format=%H -n 1 -- .planning/phases/19-gitsafe-git-blast-radius-envelope/19-32-SUMMARY.md); test "$ANCHOR" = "$(git rev-parse HEAD)" || { echo "FAIL: the SUMMARY anchor does not resolve to this record commit — 19-33's gate cannot find its base by file content"; exit 1; }; git log --format=%s -n 1 HEAD | rtk proxy grep -q '^docs(19-32)' || { echo "FAIL: the record commit's message does not begin docs(19-32) — the second handoff anchor is missing"; exit 1; }; test "$(rtk proxy grep -c 'Running tests/envelope_' /tmp/19-32-gate.log || true)" -eq 19 || { echo "FAIL: not all NINETEEN envelope_* binaries ran — this plan's own evidence file did not execute"; exit 1; }; test "$(rtk proxy grep -cE '^\| AR-19-13 \|' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md || true)" -eq 0 || { echo "FAIL: an AR-19-13 row exists"; exit 1; }; test -z "$(git diff --numstat HEAD~3..HEAD -- src/)" || { echo "FAIL: src/ hunks across this plan's three commits"; exit 1; }; test "$(git diff --numstat HEAD~3..HEAD -- tests/ | awk '{s+=$2} END{print s+0}')" -eq 0 || { echo "FAIL: deletions under tests/"; exit 1; }; test -z "$(git diff --numstat HEAD~3..HEAD -- src/envelope/advisory.rs)" || { echo "FAIL: advisory.rs was opened"; exit 1; }; test -z "$(git diff --name-only HEAD~3..HEAD -- Cargo.toml Cargo.lock)" || { echo "FAIL: a manifest moved (T-19-SC)"; exit 1; }</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-32 record stating FIRST that this plan closes nothing and clears no gate, with all eight `high` rows named; **audit 12's finish-line paragraph quoted verbatim with the statement that neither this plan nor `19-33` writes that sentence**; **the RESTATED RESIDUE CONDITION over ALL THREE AXES — which words reach the reader, what the reader sees in a word, and which paths the comparison protects — with the residue named on each axis from measurement, no count written on any axis, and nothing handed to a pin, a schedule or a version witness**; `T-19-122`'s two end-to-end drives with both remote SHAs and the fired-then-reset cap, the binary half, the composition with the interior scan, and **the heredoc form's guard verdict recorded as measured with any correction to audit 12 written BESIDE**; **the `T-19-122` DESIGN ANSWER with the table of what design (A) turns red and design (B) does not, naming both fenced pins, the rejected third design with its cost, the principled restatement of the word set, and the disclosed over-refusal with its two controls**; **the PROCESS finding that audit 11 generalised `cat <<<X` into a statement about all five non-pathname operators, written BESIDE and with neither subsection edited**; `T-19-123`'s two end-to-end drives with the full spelling table and the composite beside the doc's own claimed narrowing; **the `T-19-123` DESIGN ANSWER with what the protected path set becomes in its general form, that it grows by EXACTLY ONE path and why, exactly where it stops and why in ownership and AR-19-11 terms with the chain above it named, the four-row ANCESTOR-vs-PREFIX discriminator including the user-set-root case, the residue above the root registered, and the statement that no ancestor clause is written for the binary with `word_is_exactly`'s reasoning gap named as an HONESTY repair for `19-33`**; **both fenced ancestor pins NAMED with `19-33`'s cross-fence exception spelled out and this plan performing none of it**; **`T-19-124`'s three-tree curve with the arithmetic showing a byte floor skips nine of eight thousand, the amplification argument against per-word and per-segment caps, the LINEARITY property with a fail-closed backstop, the reason the pin is a ratio, why the 1 MiB row is recorded, and the behavioural half claimed in neither direction**; `T-19-125` and `T-19-126` registered with their repairs named as `19-33`'s, the `ledger.rs` gate recorded as a one-shot with its reason INVERTED, and the two new standing pins described as GREEN before and after; the `SEPARATORS` citation re-derived at `:2422` with both `git log -L` results and the three stale citations named; the alias-body credential route recorded as `T-19-86`'s and explicitly NOT folded; the axis arithmetic with fourteen classes and class 2's and class 7's falls; the mechanism pins and byte floors; `SECTION_ENVELOPE` re-measured, NOT edited, and `19-33` prohibited from opening it; `C-08`, `T-19-120` and `T-19-124`'s behavioural halves, `C-11` … `C-15`, `T-19-110` and the `glab --host` cell carried forward; and `T-19-17r` OUTSTANDING for the seventeenth time with no `AR-19-13` and no use of "accepted" — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection; `deferred-items.md` carries a plan-19-32 section registering all five rows at their severities, the three-axis condition, both low-severity repairs as `19-33`'s regardless of any rule, the statement that `T-19-116` gained two routes and that closing `T-19-122` closes only one, and every other open item unchanged; the `--no-fail-fast` run's `passed + failed` exceeds 1871 by exactly the number of new `#[test]` fns counted from `git show`, with the arithmetic stated and checked and the complete RED and PERMITTED name lists recorded verbatim as `19-33`'s handoff contract; ALL NINETEEN `envelope_*` binaries RAN with their per-binary counts recorded; `git diff --numstat HEAD~3..HEAD -- src/` is EMPTY across all three commits, `advisory.rs` is untouched, neither manifest moved, and `tests/` shows ZERO deletions; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, and to the files and binary the guard's own controls live in |
| **a word the LINE contains → a word the PREDICATE receives** | **This round's first boundary. `consume_redirection` deletes a redirection's target word, and records it on the `Segment` only when the operator takes a PATHNAME — so a here-string word is in neither of the two classes the predicate iterates, while being fully literal and absolute** |
| **a path the COMPARISON protects → a path whose destruction destroys it** | **This round's second boundary. The comparison is a PREFIX over `<root>/<alias>`, so every ANCESTOR is shorter and answers `false` — and `rm -rf <root>` takes the same nine carriers the prefix exists to protect** |
| a rule's stated residue → the spellings its silence permits | Stated over ONE axis — what the reader sees in a word. Measured as a condition over THREE: the word set, the reading, and the path set |
| a control's stated CAPABILITY → the quantity it bounds | `MAX_GUARD_REQUEST_BYTES` bounds the INPUT at 1 MiB. The candidate scan's work is quadratic in the input, so nothing bounds the work |
| an evidence artefact's claimed WEIGHT → what it can actually fail on | A plan-time grep gate is a one-shot; a corpus entry that does not reach the file certifies a shape rather than a bypass |
| a doc's disclosed COST → the surface the code actually refuses | The cost says *contains as a substring*; the comparison NORMALISES, and two measured refusals contain no such substring |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-122 | Elevation of Privilege / Tampering | `consume_redirection` (`policy.rs:3532-3609`) and the two word classes (`:6118-6128`) — a non-pathname redirection target is in NEITHER | high | **mitigate — CORPUS ONLY here; the rule is `19-33`'s** | Driven end to end twice against real git with a control on both sides and the envelope directory confirmed gone by a walk; the WORD-SET SWEEP driven over all five non-pathname operators, the heredoc body form, the binary half and the composition with the interior scan, with real `bash` confirming reach; the fail-closed corpus rows asserted at their derived post-fix verdict beside `cat <<<x`, `cat <<< /tmp/x` and `git <<<x push --force origin main` pinned unchanged, **so a design that widened `pathname_target` turns the two fenced pins red and a design that banned here-strings turns the controls red** |
| T-19-123 | Elevation of Privilege / Tampering | `word_is_within` (`policy.rs:5685-5711`), pinned PERMITTED by name at `policy.rs:10022` and `tests/envelope_interior_path.rs:2341` | high | **mitigate — CORPUS ONLY here; the DECISION and both pin moves are `19-33`'s** | Driven end to end twice with a control beside every leg and the doc's own claimed composite driven beside the row that defeats it; the ANCESTOR-STOP pinned from both sides — a sibling under the root, a second alias, the root's own parent, `/tmp`, the user-set-root case and both binary EXACT-PATH controls all asserted exit 0 before and after — **so a prefix widened to the root turns this file red rather than turning a driven run unusable**; the residue above the root and `rm -rf <binary-parent>` RECORDED and asserted in neither direction |
| T-19-124 | Denial of Service | `slash_anchored_candidates` (`policy.rs:5658-5662`) applied per candidate at `:5697` and `:5760` | medium | **mitigate — the measurement and the PROPERTY here; the bound is `19-33`'s** | The curve measured at five sizes on the built binary AND on a rebuilt `dd17bfb` control binary with audit 12's no-slash control on both; the pin is a LINEARITY RATIO with a derived threshold rather than a wall-clock speed, so it is a fact about the code rather than about the machine; the arithmetic showing a byte floor skips nine candidates of eight thousand is written out, and the amplification argument against per-word and per-segment caps with it. **The behavioural half is UNMEASURED and claimed in neither direction** |
| T-19-125 | Information Disclosure (documentation accuracy) | the disclosed cost (`policy.rs:6044-6048`) and `envelope_carrier_refusal`'s recovery line (`:6168`, `:6180`) | low | **mitigate — the BEHAVIOUR asserted here; both wordings are `19-33`'s to repair** | The two normalising instances asserted exit 2 before and after with the absence of the substring verified mechanically and their non-normalising twin beside them; both stale wordings quoted verbatim and registered, with the correction required whether or not any rule lands |
| T-19-126 | Tampering (evidence quality) | `ledger.rs:354-361`'s grep gate; `CONTROL_CARRIER_INTERIOR_PATH` entry 7 (`tests/envelope_wrapper_class.rs:8450`) | low | **mitigate — TWO REAL STANDING PINS written here; the inverted comment is `19-33`'s** | A behavioural pin driving a symlinked-to-regular ledger PERMITTED and COUNTED beside FIFO, directory, character-device and fresh-root rows; and a source-slice pin over `ledger.rs`'s production half that CAN name the forbidden API, because the name sits below the `#[cfg(test)]` sentinel and outside the slice — **which is what dissolves the inversion a whole-file grep creates**. Entry 7's reach failure repaired by an ADDED reaching entry and an APPENDED WR-02 note, never a re-spelling |
| T-19-116 | Elevation of Privilege | `C-10`, the binary every stub and `guard_command` exec | high | **carried forward OPEN — TWO NEW ROUTES this audit, and closing `T-19-122` closes only ONE** | `xargs cp /bin/true <<< <BINARY>` is closed by the third word class; `rm -rf <binary-parent>` is NOT, because no ancestor clause is written for the binary and `word_is_exactly` stays an EQUALITY. The four original residues stay at exit 0. Stated at the same weight as the narrowing |
| T-19-107-shape | Repudiation | a control's own text claiming a reach, a bound or an evidentiary weight the measurement denies | — | mitigate | Four claims measured and registered — the disclosed cost's substring wording, the refusal's recovery line, the `ledger.rs` gate's inverted reason, and the interior alphabet's *"every entry was drawn from a measured sweep"* — each with its repair required whether or not any rule lands; `SECTION_ENVELOPE` re-measured and left byte-identical so no headroom is spent |
| T-19-121 | Information Disclosure / Elevation of Privilege | the `credential.helper` clause's residue; `cred.rs:416-434`'s two-item list | high | **carried forward OPEN — a bounded residue of a control that genuinely landed** | The alias-body route measured with its no-`-c` control and RECORDED as `T-19-86`'s; the list item registered as `19-33`'s to add. **Not folded into `T-19-86`, not used to re-rate anything** — audit 12 declined and so does this plan |
| T-19-111 | Elevation of Privilege | the re-parse clause's CARRIER SET — `C-11`, a `.git/config` written by any command | high | **carried forward OPEN, NO rule, corpus rows RECORDED not asserted** | `19-27`'s five-site attribution correction stays as performed and attribution stays OUT of `T-19-86`; no rule, no acceptance, rows asserted in neither direction |
| T-19-112, T-19-113, T-19-115 | Tampering / Spoofing | the SAFE-06 ledger and the hook stubs as FILES; the tilde, glob and brace spellings | high / medium / medium | **carried forward NARROWED-NOT-CLOSED / NO RULE** | Both new classes are further routes to `T-19-112`'s cap reset and closing them narrows further without closing; `T-19-115` gets no rule and appears on axis 2 of the restated condition; **nothing here is described as closure** |
| T-19-86, T-19-91 | Elevation of Privilege | `classify_git`'s denylist default arm through a governed program's own operand; a decision operand assembled by expansion | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; rows at exit 0, pins green and UNMODIFIED. Because both remain open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-96, T-19-110, T-19-74 | Elevation of Privilege | a glob in a push flag; region 2's operand grammar; the frozen wrapper rows | medium | **accept for this plan (registered, not fixed)** | Left exactly as pinned |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — gap RECORDED, acceptance deliberately unmade for the seventeenth time** | No `AR-19-13`, no use of the word "accepted" |
| — | Tampering | `C-08`'s claimed-but-unwired second carrier; `C-15`'s unclamped `pr_cap_*`; `C-12`; `C-13`; `C-14` | planner-derived / control (e) | **carried forward UNFIXED, measured and RECORDED** | No rule, no clamp, no acceptance. `C-08`'s, `T-19-120`'s and `T-19-124`'s behavioural halves stay UNMEASURED |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added. `Cargo.toml` and `Cargo.lock` are not in `files_modified` and the gate asserts neither moved. Every probe uses `std::process::Command` inside `#[cfg(test)]` or the task's own shell |
</threat_model>

<verification>
- `git diff --numstat f06d153..8659a18 -- src/ tests/` is EMPTY, verified before the first measurement, so
  this plan's base is the tree audit 12 measured.
- **Every commit shows ZERO `src/` hunks**, verified by `git diff --numstat HEAD~3..HEAD -- src/` returning
  nothing, and `src/envelope/advisory.rs` is untouched. Neither `Cargo.toml` nor `Cargo.lock` moved.
- **`git diff --numstat` over `tests/` shows ZERO DELETIONS across all three commits.** Every test-side
  change is an ADDITION, an EXTENSION with its reason stated, or an APPENDED WR-02 note.
- The `dd17bfb` control binary was built from a `git archive` extraction OUTSIDE the repository; the working
  tree was never checked out, reset or stashed.
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`, and
  MUST be read as `passed + failed` against the exact total `19-31-SUMMARY.md` records — **1871**, over 47
  result lines with 13 ignored.
- The gate arithmetic is STATED and CHECKED: 1871 plus the NEW `#[test]` fns this plan adds, counted from
  `git show`. A disagreement is a finding.
- **The RED gates assert ROWS, not the presence of failure.** Every failing name in the evidence file
  carries the prefix `after_19_33_`, every failing name on the axis carries the suffix `_after_19_33`, a
  named minimum set of derived rows is present among them, and **a failure carrying neither marker fails
  the gate** — which is what distinguishes this round's derived RED from a broken fixture, a mis-quoted
  argv, a mis-derived floor or a class collision.
- **The SEVERANCE-RETIRABLE row exists and is isolated**:
  `after_19_33_the_candidate_scan_is_linear_in_the_words_length` carries the linearity ratio and nothing
  else, is recorded verbatim in the SUMMARY under its own heading, and is the ONE name `19-33`'s two gates
  branch on.
- **Both handoff anchors hold and agree**: the record commit's message begins `docs(19-32)`, and
  `git log -n 1 -- …/19-32-SUMMARY.md` resolves to that same commit, so `19-33` can find its base by file
  content rather than by message shape.
- **ALL NINETEEN `envelope_*` binaries RAN** and their per-binary counts are recorded. Eighteen means this
  plan's own evidence file did not execute.
- Every asserted row was MEASURED against the built binary before it was written, with a fresh
  `GSD_MM_ENVELOPE_ROOT` per row, the whole root walked afterwards and the walk proved non-blind by a
  positive control leaving exactly one ledger line — **except the two cap-reset drives and the ledger rows,
  which use ONE persistent root each.**
- **Every asserted row NAMES its control and states what makes the pair discriminating** — for `T-19-122`,
  whether the word stands as an operand or after a non-pathname operator; for `T-19-123`, whether the word
  names the directory or its parent; for the STOP, whether it names the root's parent or a sibling under
  the root. A row whose control fails the same way at the same identifier is `record_only`'d with its
  overlap stated.
- **The WORD-SET SWEEP was driven over all five non-pathname operators AND the heredoc body form, with the
  heredoc's guard exit code and reason identifier RECORDED rather than inherited from audit 12**, and real
  `bash` confirmed reach for every asserted row.
- **The ANCESTOR STOP is pinned from both sides**, including the user-set-`GSD_MM_ENVELOPE_ROOT` case, and
  `rm -rf <binary-parent>` is RECORDED in neither direction.
- **`T-19-124` is pinned as a LINEARITY RATIO with a derived threshold, not as a wall-clock speed**, with
  audit 12's no-slash control beside it and the 50 000-slash and 1 MiB rows RECORDED under a hard external
  timeout rather than asserted.
- **The FENCED-FILE ENUMERATION was re-derived mechanically**, all four greps' output recorded verbatim,
  every hit dispositioned, every `policy.rs` unit pin checked individually, and the TWO ancestor pins plus
  the THREE stale `:2297` citations named as `19-33`'s.
- **`SEPARATORS` is byte-identical and re-derived at `policy.rs:2422`** with `git log -L 2422,2422`
  returning exactly one commit for the phase, `84a9b05`; `is_separator(">")` is `false`; `segment.tokens` is
  byte-identical; the SEGMENT-COUNT pins are green in both the SPLIT and the DISPLACED variant; and every
  mechanism pin from rounds 4 through 12 is re-asserted and green, with `envelope_config_resolution` at 30/0.
- The axis names FOURTEEN classes; both new alphabets are in FAIL-CLOSED arms; classes 1, 2 and 7 are all
  EXTENDED with their reasons stated and their no-entry-lost checks re-derived mechanically; every floor and
  count is an exact equality re-derived from the alphabets AND the extensions, with class 2's and class 7's
  falls stated.
- The WORD-SET, ANCESTOR-STOP, NO-PROGRAM-NAMES and PATH-PREFIX-NOT-BASENAME fences all pass, and
  `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` stays green.
- The four earlier axes, their predicates, their degenerate-proofing and their floors are BYTE-IDENTICAL;
  `MIN_CONFIG_RESOLUTION_CLASSES` is 6; `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` are
  unchanged; the stripper's three protections keep their present strictness.
- **`SECTION_ENVELOPE` was RE-MEASURED and NOT edited** — token count against 215, widest line against 80,
  first `Guaranteed` clause still TRUE — and `advisory.rs` shows zero diff lines.
- `T-19-126`'s two new pins are GREEN before and after and are LABELLED as standing pins rather than counted
  as this round's RED.
- Every `C-11` … `C-15` row, `C-08`'s and `T-19-124`'s behavioural halves, `T-19-116`'s residues,
  `rm -rf <binary-parent>`, the ancestor above the root, the alias-body credential route and the 1 MiB row
  are RECORDED and never asserted, with a mechanical self-assertion proving it.
- The `AR-19-13` row count is zero, the word "accepted" is not applied to `T-19-17r`, no `AR-` row is added,
  edited, renumbered or un-accepted, and `T-19-23` is not marked closed.
- `git diff --stat` touches no file outside `files_modified`. The `19-SECURITY.md` diff is one appended
  subsection after the plan-19-31 record.
- Three commits exist in order: the evidence file, the axis, the record.
</verification>

<success_criteria>
1. The corpus is RED against the pre-fix tree on exactly the derived-post-fix rows, the failing names are
   recorded verbatim, and **every commit shows zero `src/` hunks and zero deletions under `tests/`** — the
   evidence a corpus can fail before a control exists.
2. **The RESIDUE CONDITION IS RESTATED OVER ALL THREE AXES** — which words reach the reader, what the reader
   sees in a word it received, and which paths the comparison protects — with the residue named on each axis
   from measurement, **no count written on any axis, and nothing handed to a pin, a schedule or a witness.**
3. **The `T-19-122` boundary is DECIDED and the decision is DERIVED from the fenced-file enumeration**: a
   second `Segment` field and a third word class, because widening `pathname_target` turns
   `only_a_literal_pathname_target_reaches_the_segment_and_the_tokens_do_not_move` and the twelve-operator
   grammar pin red and the second field turns neither red. The rejected third design is costed in writing.
4. **The `T-19-123` boundary is DECIDED, and what the protected path set becomes, how much it grows by,
   exactly where it stops and why are all stated**: a proper ancestor at or under `<root>`, exactly ONE more
   path because the alias is a plain single component, stopping at `<root>` on ownership and AR-19-11
   grounds with the chain above it named — **and it is an ANCESTOR clause rather than a prefix widened to
   the root, pinned by `ls <ENV>/unrelated-sibling` and the user-set-root case.**
5. **Both fenced ancestor pins — `policy.rs:10022-10026` and `tests/envelope_interior_path.rs:2341` — are
   NAMED IN ADVANCE as `19-33`'s cross-fence exception**, with the requirement that each reasoning be
   REWRITTEN rather than deleted, and this plan performs neither move.
6. **`T-19-124`'s bound is on WORK rather than input**, the property pinned is LINEARITY rather than speed,
   and **audit 12's own parenthetical remedy is derived INSUFFICIENT in writing** — a byte floor skips nine
   candidates of eight thousand — with the per-word and per-segment cap amplification argument beside it and
   the behavioural half claimed in neither direction.
7. **The axis draws a here-string carrier and an ancestor carrier in FAIL-CLOSED arms**, asserted by counting
   fences rather than claimed in prose; classes 1, 2 and 7 are all EXTENDED so no live unruled bypass is
   filed under a class the record says is closed, **the SEVENTH forced split and the SECOND through class 2**.
8. **`T-19-125` and `T-19-126` are registered with their repairs as `19-33`'s**, and `T-19-126`(i) gets TWO
   REAL STANDING PINS — behavioural and source-slice — with the source-slice pin naming the forbidden API
   below the sentinel, which is what dissolves the inversion a whole-file grep creates.
9. **The `SEPARATORS` citation is re-derived at `policy.rs:2422`** with both `git log -L` results recorded,
   and the three stale citations under `tests/` named for `19-33`.
10. **The `cred.rs` alias-body route is measured and RECORDED as `T-19-86`'s**, with the list item registered
    as `19-33`'s and the attribution explicitly NOT folded.
11. **`SECTION_ENVELOPE` is re-measured and untouched**, its headroom unspent, and `advisory.rs` shows zero
    diff lines.
12. `T-19-17r` is OUTSTANDING for the seventeenth time, no `AR-` row is added or un-accepted, `T-19-23` is
    not marked closed, and the SUMMARY states FIRST that this plan closes nothing, that eight threats remain
    open at `high`, and that **`/gsd-secure-phase 19` is NOT cleared** — with no unqualified "T-19-60 is
    closed" anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-32-SUMMARY.md` when done.
It must contain: the statement FIRST that this plan closes nothing, that `T-19-86`, `T-19-91`, `T-19-111`,
`T-19-112`, `T-19-116`, `T-19-121`, `T-19-122` and `T-19-123` remain open at `high`, and that
`/gsd-secure-phase 19` is NOT cleared; the confirmation that
`git diff --numstat f06d153..8659a18 -- src/ tests/` was empty and that the `dd17bfb` control binary was
built outside the repository without touching the working tree; the three commit SHAs in order with
`git show --stat` and the proof that each carries zero `src/` hunks and zero `tests/` deletions; **the
COMPLETE RED name list verbatim and the COMPLETE PERMITTED list, which together are `19-33`'s handoff
contract**; **a SEVERANCE-RETIRABLE ROW heading naming
`after_19_33_the_candidate_scan_is_linear_in_the_words_length` verbatim, stating that it carries the
linearity ratio and nothing else and that `19-33`'s two gates each branch on exactly that name and no
other**; **the HANDOFF ANCHOR — the record commit's SHA, that its message begins `docs(19-32)`, and that
`git log -n 1 -- …/19-32-SUMMARY.md` resolves to the same commit, with the two anchors confirmed to
agree**; the statement that every derived-post-fix row is named `after_19_33_*` in the evidence file and
`_after_19_33` on the axis and that nothing else is, with the observation that no failure lacked its
marker; the `--no-fail-fast` `passed + failed` totals before and after with the arithmetic stated and
checked against `git show`; the per-binary counts for all NINETEEN `envelope_*` binaries with every one
confirmed to have RUN; **the RESTATED THREE-AXIS RESIDUE CONDITION with the residue named on each axis**;
**the `T-19-122` DESIGN ANSWER with the design-(A)-versus-(B) table naming both fenced pins, the rejected
third design, and the disclosed over-refusal**; the WORD-SET SWEEP table with the `bash` reach column, the
space-separated twin beside every row, and **the heredoc body form's guard verdict recorded as measured with
any correction to audit 12 written BESIDE**; **the `T-19-123` DESIGN ANSWER with the protected path set in
its general form, that it grows by exactly one path and why, exactly where it stops and why, the four-row
ANCESTOR-vs-PREFIX discriminator including the user-set-root case, and the residue above the root**; **both
fenced ancestor pins named as `19-33`'s cross-fence exception**; `T-19-122`'s and `T-19-123`'s two
end-to-end drives each with both remote SHAs and the fired-then-reset caps; **`T-19-124`'s three-tree curve,
the linearity ratio with its derived threshold, the arithmetic showing a byte floor skips nine candidates of
eight thousand, the amplification argument, and the behavioural half claimed in neither direction**;
`T-19-125`'s two normalising instances with the substring absence verified mechanically; **`T-19-126`'s two
new STANDING pins labelled green-before-and-after, and entry 7's reach failure with the ADD-don't-re-spell
repair**; the `SEPARATORS` re-derivation at `:2422` with both `git log -L` results and the three stale
citations named; the alias-body credential route with its no-`-c` control, the secret recorded
present/absent, `cred.rs`'s two-item list quoted verbatim, and the explicit statement that it was NOT folded
into `T-19-86`; the FENCED-FILE ENUMERATION with all four greps' output verbatim and every hit
dispositioned; the two ordering pins at their identifiers; the axis arithmetic with all fourteen classes,
every re-derived floor and class 2's and class 7's falls; the four fences with the two new ones named; the
rows RECORDED and never asserted with the mechanical self-assertion that proves it; the mechanism pins and
byte floors re-measured across rounds 4 through 12 with `envelope_config_resolution` at 30/0; **the
`SECTION_ENVELOPE` re-measurement with its token count, widest line and clause truth, and the statement that
`advisory.rs` was not opened**; `AR-19-04`, `AR-19-05` and `T-19-23` carried forward untouched; `C-08`,
`C-15`, `T-19-110`, `T-19-115` and the `glab --host` cell carried forward unchanged; the `T-19-17r`
bookkeeping gap OUTSTANDING with the explicit statement that this plan did NOT accept it and added no
Accepted-Risks-Log row; any documented flake that fired recorded with its output and not fixed; and a "what
remains uncovered" section naming `T-19-86` FIRST — open at `high` by user scoping decision — followed by
`T-19-91`, `T-19-111`, `T-19-112`, `T-19-116`, `T-19-121`, `T-19-122` and `T-19-123` (open at `high`),
`T-19-113`, `T-19-115`, `T-19-124`, `T-19-125`, `T-19-126`, `T-19-96`, `T-19-110`, `T-19-74`, `T-19-84`,
`T-19-85` and `T-19-61`…`T-19-73`, with the explicit statement that only the WRAPPER-OPERAND sub-class of
`T-19-60` is closed.
</output>

