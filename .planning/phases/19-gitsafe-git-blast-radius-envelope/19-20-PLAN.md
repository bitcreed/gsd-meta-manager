---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 20
type: execute
wave: 10
gap_closure: true
gap_ids: [T-19-101, T-19-100, T-19-102]
depends_on: [19-19]
files_modified:
  - tests/envelope_callee_grammar.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-05, SAFE-06]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, and this is the FOURTH time that ordering is the reason a plan exists separately.** It worked three times — 19-14/19-15, 19-16/19-17, 19-18/19-19 — and the cross-plan carry-forward is what proves two rules separately load-bearing. This plan writes the corpus and the reproducers and STOPS. `19-21` writes the rule. `T-19-101` is `T-19-76`'s failure mode for the SIXTH consecutive round and this time the axis moved rather than the cell: `UNREADABLE_CLASSES` names seven word-ASSEMBLY classes and `DELETION_CLASSES` names five word-REMOVAL classes, **and both are axes of the SHELL's grammar**. Audit 6 verified mechanically that nothing anywhere in `tests/` models the CALLEE's — `grep -rn \"attr-source\\|shallow-file\\|GIT_GLOBAL_VALUE_OPTS\" tests/` returns nothing at all. The corpus is structurally incapable of generating, and so of failing on, `T-19-100`."
    - "**If the widened corpus does not go RED against the pre-fix tree, that is a FINDING — stop and report it, do not proceed.** A row already refused certifies nothing about the class this round is about, and a property green before the fix is a property that could not have failed on it. Every row here is measured against the built binary BEFORE it is written as an assertion, and any row that does not reproduce is reported with its measured verdict instead of being asserted anyway."
    - "**THE GAP MOVED AXIS AGAIN, AND THIS TIME IT LEFT THE SHELL ENTIRELY.** Audit 6 enumerated bash's transformations between the command string and `execve` one at a time — quote removal, every expansion, redirection, line continuation, assignment-prefix removal, control-operator splitting, here-document delimiters, pipeline and group nesting — and found every one modelled or failing closed, with **no reordering step in a simple command for a third rule to miss**. The command-line-to-argv boundary is CLOSED. Having reconstructed argv correctly, the guard must still decide **which word in it is the verb**, and it does that by asking a hand-maintained, unpinned enumeration of **git's own option grammar** which **fails OPEN when it is silent**. So the corpus gets a THIRD named axis, `CALLEE_GRAMMAR_CLASSES`, standing beside a byte-identical `UNREADABLE_CLASSES` and a byte-identical `DELETION_CLASSES`, with its own predicates, its own alphabet and its own floors. **Smuggling a leading git option into either shell axis would model the callee's grammar as if it were the shell's and lose exactly the distinction this round is about.**"
    - "**`T-19-100` is a DEMONSTRATED exploit and every row of it is measured, not argued.** Measured at `1583d20` against the built binary, one fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards and empty in every one: `git --attr-source HEAD push --force origin main` → **exit 0**; and at exit 0 also `git --attr-source HEAD stash`, `git --attr-source HEAD update-ref -d refs/heads/main`, `git --attr-source HEAD config core.hooksPath /tmp/x`, `git --attr-source HEAD reflog delete HEAD@{0}`, `git --attr-source HEAD symbolic-ref HEAD refs/heads/x`, `git --shallow-file /tmp/s push --force origin main`, `git --shallow-file /tmp/s stash`, and the layer-2 row `git --attr-source HEAD -c core.hooksPath=/dev/null push --force origin main`. **Four of them have no second carrier at all** — `stash`, `update-ref`, `reflog delete` and `config core.hooksPath`, where disarming the hook IS the loss of the carrier. The ATTACHED spelling is correctly refused today — `git --attr-source=HEAD push --force origin main` → **exit 2 `force_push_blocked`** — so this is the separate-value form specifically, and that asymmetry is the shape of the fix."
    - "**The mechanism, stated exactly: `scan_leading` walks leading `-`-initial tokens and asks `leading_git_option` how many words each occupies, and its answer for an option it does not recognise is `(None, 1)`.** The loop advances one word, lands on the option's VALUE, sees it does not start with `-`, and BREAKS with that value as the verb. `classify_git` then finds a verb in no denylist arm and answers `Allow`. Every word is literal and `Token.literal` is `true` for all of them — **the round-5 bit is right**; no word is deleted and the order is preserved — **the round-6 model is right**. What is wrong is the verb INDEX, and it is wrong because of a fact about git rather than a fact about bash."
    - "**The list is wrong in BOTH directions against the installed git, and that is the evidence the list itself is the defect.** Measured directly against `git version 2.43.0` in a temp directory, two-sided (`git <opt> version` versus `git <opt> XVALUE version`): `--attr-source` and `--shallow-file` DO consume a separate word — the 2-word form prints `git version 2.43.0` and the 1-word form prints git's usage — and both are ABSENT from `GIT_GLOBAL_VALUE_OPTS`; while `--super-prefix`, which the constant carries, prints **`unknown option: --super-prefix`** in both forms, so this git rejects it outright. The over-consuming direction is the dangerous one and it is demonstrated: `git --super-prefix push --force origin main` → **exit 0** at `1583d20`, because the guard swallows the real verb `push` as `--super-prefix`'s value and then reads `origin` as the verb. It is not a live bypass only because git itself rejects the option — a stale entry for an option git ACCEPTS would be one."
    - "**The corpus must be able to fail in BOTH directions, and the permitted half is what makes that true.** A rule that refused every leading option would close `T-19-100` and be unusable. All of these are measured at **exit 0** at `1583d20` and must still be exit 0 after `19-21`: `git --no-pager status`, `git --no-pager log --oneline`, `git -c user.name=\"$NAME\" commit -m x`, `git --git-dir=/tmp/g status`, `git -C /tmp status`, `git --bare status`, `git --literal-pathspecs status`, `git --no-optional-locks status`, `git --exec-path status`, `git --version`, `git --attr-source HEAD status` and `git --shallow-file /tmp/s log --oneline` — the last two confirmed against real git, which really runs them (`On branch gsd-auto/alpha/w` and `50d11d1 init`). **This axis's `ls {git,svn}-repo` is `git --no-pager status`**: the row that tells a grammar MODEL apart from a blanket refusal of anything beginning with `-`."
    - "**`T-19-102` is the same defect in the over-refusal direction, and its discrimination control is what stops the fix from being a copy of `git push -h`.** Measured in a fixture on `refs/heads/gsd-auto/alpha/w` with a bare upstream: `git push --recurse-submodules on-demand origin refs/heads/gsd-auto/alpha/w` → **exit 2 `push_outside_namespace`** while `git push origin refs/heads/gsd-auto/alpha/w` → **exit 0**, and real git runs the first line to completion (`Everything up-to-date`), so it is a false refusal of an ordinary in-namespace push. **But `git push --signed no origin refs/heads/gsd-auto/alpha/w` → exit 2 and must STAY exit 2**, because real git does NOT consume `no`: it reads `no` as the repository and `origin` as a refspec, and answers `error: src refspec origin does not match any`. `git push -h` spells them `--recurse-submodules (check|on-demand|no)` and `--signed[=(yes|no|if-asked)]` — a required value and an attached-only optional one — **and a fix that completed the list from that help text would add `signed` and introduce a real mis-parse.** Both rows are in the corpus."
    - "**Every post-fix expectation carries a written DERIVATION, and a verdict that cannot be derived is measured pre-fix and RECORDED rather than asserted.** This plan measures PRE-fix, so its measure-first discipline cannot catch a wrong POST-fix expectation, and a row pinned at a verdict the rule cannot produce lands RED with `19-21` forbidden to edit it — which stalls the round, and is the defect the `19-18` plan-check caught as blockers 2 and 3. **Three rows are undeliverable and are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `git --attr-source $T push --force origin main`, `git --attr-source *.x push --force origin main` and `git --attr-source HEAD {push,--force} origin main`, all three refused TODAY at `envelope_assertion_failed` because rounds 5 and 6's rules fire on what the guard currently reads as the VERB. After `19-21`, `--attr-source` consumes its value and those words are an option's OPERAND rather than a decision word, so whether the literalness bit still reaches them is a `19-21` design outcome this plan cannot know."
    - "**This seam needs NO replacement exception, and that is a deliberate correction to how the last three rounds handed off.** Three rounds running, this phase produced a two-plan handoff that was unsatisfiable as written — an alphabet entry the second plan breaks and the first cannot edit, and a false refusal pinned at a verdict the second plan is required to move under a prohibition allowing only additions. **The rule this plan adopts instead: assert the POST-fix verdict wherever it is derivable, so the row is RED now and GREEN after and no body ever needs replacing; record the PRE-fix measurement in the doc comment, the SUMMARY and the security record, where the evidence actually belongs.** `T-19-102`'s false refusal is therefore asserted at **exit 0**, not at its measured exit 2, and it is RED at this plan's end. `19-21` needs no named exception and none is granted."
    - "**The one place `19-21` WOULD have collided with this file is the anti-vacuity control, and it is re-expressed here BEFORE the rule exists rather than left to break.** `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` asserts `code.len() * 4 > raw.len()` per file. Re-derived independently at `1583d20`: `policy.rs` is 262,748 raw bytes and 65,933 stripped, a ratio of **25.0936%** against a 25% floor — **246 bytes of stripped-code headroom, or 984 bytes of comment**. `19-21` adds a production category, a refusal message and a drift pin's doc to that file and would turn it red. The control's PURPOSE is anti-vacuity — an absence assertion over `production_code` proves nothing if the stripper ate the file — and at that margin its red says *the stripper broke* while meaning *someone wrote comments*, pulling directly against this phase's own doc-disclosure pins such as `resolve_programs_own_doc_still_discloses_the_residual_it_does_not_cover`, which REQUIRE specific paragraphs to exist. **It is re-expressed as an ABSOLUTE floor on stripped production bytes, per file, and the ratio assertion is deleted rather than kept beside it** — keeping both would reproduce the collision this change exists to remove."
    - "**Nothing `19-14` through `19-19` settled is re-opened, and the mechanism pins say so mechanically rather than in prose.** Four pins are carried into the new file and all four must be GREEN at this plan's end and after `19-21`: Rule B's, over `policy::split_segments_with_heads` reporting `head_is_command_position == false` for the severed-prefix spellings and `true` for `{ git status; }`; **round 5's literalness bit asserted NON-VACUOUS**, `Token.literal` false for `pus?` and true for `push`; **round 6's deletion model asserted NON-DEAD**, `git >/dev/null push --force origin main` still refused and `git x2>/tmp/o push --force origin main` still PERMITTED; and **`policy::is_separator(\">\")` is `false` with `SEPARATORS` byte-identical**, the mechanical proof the route that constant's own doc forbids was not taken. The composition row `git --attr-so\\`+NL+`urce HEAD push --force origin main` — measured **exit 0** at `1583d20` — is the pin that proves round 6's model is load-bearing for this round: the continuation must be deleted before `--attr-source` is even spelled, and only then does the callee-grammar gap open."
    - "**This plan's gate names its expected failures rather than requiring zero, and it is the handoff contract `19-21` gates against.** Baseline is **1610 passed / 0 failed / 13 ignored**, `passed + failed = 1610`, recorded by `19-19-SUMMARY.md` and independently re-measured by audit 6. After this plan `passed + failed` is strictly greater than 1610; a red test RAN, so red→green leaves the total unchanged and every increase comes ONLY from new `#[test]` fns — count them from `git show` and check the identity. Every failure name is one of the three documented flakes or one of the RED names this plan creates; the RED set is non-empty and every one of its names is listed in the SUMMARY; and `--test envelope_expansion_slots`, `--test envelope_command_position`, `--test envelope_literal_decision` and `--test envelope_argv_deletion` are FULLY GREEN, because the widening must not disturb rounds 4, 5 and 6's evidence."
    - "**`T-19-17r` stays OUTSTANDING and this plan does not resolve it either.** `19-17-SUMMARY.md` calls it \"accepted\"; audits 5 and 6 both confirmed the measurement and both pins (`rg \"git status\" {src,tests}` → exit 2, `rg \"git status\" src/` → exit 0) and both deliberately declined to make the acceptance, because accepting a risk is a human decision. There is still no Accepted-Risks-Log row and no register row. This plan records the gap and makes no acceptance."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-21`, or by the two together.** `T-19-86` remains OPEN at `high` by explicit user scoping decision and its four rows must keep exiting 0; `T-19-91` remains OPEN at `high` with its arms unweakened — `reflog $S`, `reflog show $S` and `symbolic-ref $S` at exit 0 with no second carrier, and bare `git push $REF` at exit 0 in-namespace; `T-19-96` is registered open; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. The SUMMARY must say so FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-21` is the rule. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`, `19-16` and `19-18` met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT touch `UNREADABLE_CLASSES` or `DELETION_CLASSES`, their predicates, their degenerate-proofing assertions, or any of their floors — `MIN_UNREADABLE_GENERATED_CASES`, `MIN_GENERATED_CASES_PER_UNREADABLE_CLASS`, `MIN_UNREADABLE_FORGE_SLOT_CASES`, `MIN_DELETION_CLASSES`, `MIN_DISPLACING_REDIRECTIONS`, `DELETION_CASES`, `DELETION_REFUSED_CASES`, `DELETION_PERMITTED_CASES`, `DELETION_SLOTS` or `DELETION_CLASS_COUNTS`. The callee axis is a NEW named constant with NEW predicates and NEW floors standing beside them. Audit 6's framing is the reason: both existing axes are axes of the SHELL, the gap moved AXIS rather than one slot over, and an axis smuggled into a shell axis stops being legible as one — which is how a corpus comes to model exactly the control it certifies and nothing beside it."
      category: safety
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured or the one DERIVED in writing from `19-21`'s mandated design. If a row audit 6 recorded at exit 0 does not reproduce, report it as a finding with the measured verdict and stop — do not assert the audit's number over your own measurement, and do not quietly drop the row."
      category: honesty
    - statement: "MUST NOT claim a bypass against git without CONFIRMING IT AGAINST THE REAL `git` BINARY, as audit 6 did. Every row whose whole point is that git reaches a verb the guard did not identify must be confirmed by running real git — the two-sided probe `git <opt> version` versus `git <opt> XVALUE version` for the grammar, and an actual run in a throwaway fixture for the behaviour. A row real git rejects (`git --super-prefix …`, `git -pc user.name=x status`, `git -C/tmp status`, `git - push …`) is LABELLED as a mis-index of a command that does not run, never as a live bypass."
      category: honesty
    - statement: "MUST NOT claim the `glab --host` under-count as a live bypass. Measured at `1583d20`: `glab --host gitlab.com mr create --title x` → exit 0 with **zero** ledger lines while `glab --hostname gitlab.com mr create --title x` → exit 0 with ONE, because `FORGE_VALUE_OPTS` carries `--hostname` and not `--host`. **`glab` is not installed on this machine**, so whether glab accepts `--host` as a separate-value global flag is NOT confirmed against the callee. Record it as a planning-time cell needing callee confirmation. **Do NOT fix `FORGE_VALUE_OPTS`, `GH_API_VALUE_OPTS` or `subcommand_word_indices`** — they are a different component and outside the three-item scope."
      category: scope
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet entry or property in `tests/envelope_wrapper_class.rs`, with **exactly one named exception**: the ratio assertion `code.len() * 4 > raw.len()` inside `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`, which is REPLACED by a per-file absolute floor on stripped production bytes. Every other `MIN_*` constant keeps its current value or rises. A case that stops being drawn is a case that stops being able to fail, and this file's own section header says so."
      category: scope
    - statement: "MUST NOT keep the ratio assertion beside its replacement, and MUST NOT satisfy audit 6's finding by lowering the percentage. Keeping both reproduces the collision the change exists to remove — `19-21` adds comments to `policy.rs` and cannot edit this file — and a lower percentage is the same control with a bigger budget, still red for comment growth and still green under a proportional truncation. The replacement is an ABSOLUTE floor on `production_code(...).len()`, per file, independent of comment volume."
      category: safety
    - statement: "MUST NOT pin a POST-fix expectation that cannot be DERIVED from `19-21`'s mandated design, and MUST write the derivation beside every row that carries one. **Three rows are undeliverable and are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `git --attr-source $T push --force origin main`, `git --attr-source *.x push --force origin main` and `git --attr-source HEAD {push,--force} origin main`, each measured at exit 2 `envelope_assertion_failed` today. After the fix the value slot is an option's operand rather than the verb, and whether rounds 5 and 6's rules still reach it is `19-21`'s to measure. Assert only that they are REFUSED is also forbidden — record the measurement, assert nothing."
      category: honesty
    - statement: "MUST NOT put an entry whose verdict the fix CHANGES into the invariance-preserving arm of the generative property. This is `19-18`'s `{v}>` blocker one axis over and it is the single most likely way this seam breaks. **A leading option the installed git does NOT accept is refused after `19-21` even on a PERMITTED base** — `git --bogus-opt status` is exit 0 today and refused after — so the unknown class gets its OWN alphabet and its OWN fail-closed property, and the doc says why rather than leaving it to be inferred. The invariance arm draws only from classes whose verdict is preserved in BOTH states."
      category: safety
    - statement: "MUST NOT satisfy the callee-axis floors with an option drawn in a position that is already refused or already correct. The alphabet splices BETWEEN the governed program and its decision words, which is the only position `scan_leading` reads. An attached-value entry (`--attr-source=HEAD`) is already refused at `1583d20` and is a CONTROL rather than a reproducer; an option after the verb (`git push --attr-source HEAD --force origin main`) is not read by `scan_leading` at all."
      category: safety
    - statement: "MUST NOT modify `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs`, `tests/envelope_literal_decision.rs` or `tests/envelope_argv_deletion.rs`. Rounds 4, 5 and 6's evidence stays attributable to the round that produced it, which is why `19-14` created a third file, `19-16` a fourth and `19-18` a fifth; this plan creates a SIXTH for the same reason. The four carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place."
      category: scope
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS`, propose it, or write a row whose expected mechanism is that they were added; and MUST NOT weaken round 5's literalness bit or round 6's deletion model. All three are pinned in the new file and all three must stay green through `19-21`."
      category: safety
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Record the gap as OUTSTANDING: the cost is measured and pinned from both sides, and its ACCEPTANCE is not recorded anywhere an audit reads. Audits 5 and 6 both explicitly declined to make it on the developer's behalf; a plan making it would be forging the same signature."
      category: honesty
    - statement: "MUST NOT fix, close, narrow or re-scope `T-19-86`. Its four rows stay at exit 0 — `git submodule foreach git push --force origin main`, `git rebase -x \"git push --force origin main\" HEAD~3`, `git bisect run sh -c \"git push --force origin main\"`, `git -c alias.p='!git push --force origin main' p` — and `T-19-86` is named FIRST in the SUMMARY's 'what remains uncovered' section."
      category: scope
    - statement: "MUST NOT close, re-scope or add a remedy for `T-19-91`, and MUST NOT weaken its record. Its arms stay as audits 5 and 6 verified them — `git reflog $S`, `git reflog show $S` and `git symbolic-ref $S` at exit 0 with NO second carrier, and bare `git push $REF` at exit 0 in the in-namespace configuration. Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push`, and do not extend the denylist."
      category: scope
    - statement: "MUST NOT fix `T-19-96` (a glob in a push flag) or `T-19-74` (whose core rows are frozen and re-measured permitted). MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not touch `cred.rs`, `advisory.rs`, `scan.rs`, `config.rs`, `hooks.rs`, the honesty statement, the second-carrier table or the park-coverage surface."
      category: scope
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` — and every parser in this module is hand-rolled against `std` for exactly that reason. The real-git probes shell out through `std::process::Command`."
      category: scope
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised command wrote nothing must use a FRESH envelope root of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line. `gh pr create --title x` is measured at exit 0 with exactly ONE ledger line at `1583d20` and is a ready-made positive control."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against **1610**. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing — `19-19` first read 1364 instead of 1605 that way (D-34)."
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes — the two `tests/driver_reattach.rs` failures and the `tests/envelope_tracer.rs` ETXTBSY stub-write race — and MUST NOT treat their absence from a run as a regression. All three are pre-existing, environmental, documented in `deferred-items.md` since 19-07 for the tracer one, and out of scope."
      category: scope
    - statement: "MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lint errors in `src/browser.rs` and `src/project_creator.rs`. The gate is `cargo clippy -- -D warnings`. Do not fix those lints."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or the subsections plans 19-13 … 19-19 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-20 planning and execution record. Re-measuring and re-classifying these rows is `/gsd-secure-phase 19`'s job; an audit's own tables are its provenance and a plan writing into them forges it."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`, so this plan does not clear `/gsd-secure-phase 19` and neither does `19-21`."
      category: honesty
  artifacts:
    - path: "tests/envelope_callee_grammar.rs"
      provides: "the SIXTH evidence file — audit 6's `T-19-100` rows and `T-19-102`'s false refusal measured against the built binary and confirmed against real git, the cells found while planning round 7, the permitted half of the callee axis including `git --no-pager status` as this axis's blanket-refusal control, the unknown-option cost rows with their permitted twins, the `--signed` discrimination control that must stay refused, the three undeliverable rows recorded rather than asserted, the composition rows that prove rounds 5 and 6 are load-bearing here, and the four carried-forward mechanism pins"
      contains: "T-19-100"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "`CALLEE_GRAMMAR_CLASSES` — a THIRD named axis standing beside a byte-identical `UNREADABLE_CLASSES` and a byte-identical `DELETION_CLASSES`, with degenerate-proof predicates, a `GIT_GLOBAL_OPTIONS` alphabet spliced between the governed program and its decision words, a separate `GIT_GLOBAL_UNKNOWN_OPTIONS` alphabet kept out of the invariance arm, a generative property asserting refusal AND an empty walk over refused bases and an unchanged verdict over permitted ones, floors whose arithmetic is stated; and the anti-vacuity control re-expressed as a per-file ABSOLUTE floor on stripped production bytes"
      contains: "CALLEE_GRAMMAR_CLASSES"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-20 record rather than an audit finding, carrying every measured verdict with its walk, the real-git two-sided probe table for every candidate option, the fixture confirmation for `T-19-102` and its `--signed` twin, the cells found while planning, the design question answered with the rejected options named and costed, the measured over-refusal cost, the `glab --host` cell with its unconfirmed-callee caveat, the `T-19-17r` gap recorded as OUTSTANDING with the acceptance deliberately unmade, the `envelope_tracer` provenance correction, and the statement that this plan closes nothing and clears no gate"
      contains: "19-20"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "the `T-19-100`, `T-19-101` and `T-19-102` entries recording that their corpus exists and is RED, the planning cells folded into `T-19-100`'s class rather than registered as new threats, the `glab --host` forge cell recorded as needing callee confirmation and deliberately not fixed, and the `T-19-17r` bookkeeping gap still outstanding; `T-19-86`, `T-19-91` and `T-19-96` unchanged and open"
      contains: "T-19-101"
  key_links:
    - from: "audit 6's measured bypass rows and the real git binary's own option grammar"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary with a fresh root per row and a two-sided real-git probe, BEFORE any assertion is written and BEFORE any production line moves"
      pattern: "RED"
    - from: "a fact about the CALLEE rather than about the shell"
      to: "a THIRD named axis the corpus can draw, standing beside the two shell axes"
      via: "`CALLEE_GRAMMAR_CLASSES` with degenerate-proof predicates and its own floors, and a `GIT_GLOBAL_OPTIONS` alphabet spliced where `scan_leading` actually reads — between the governed program and its decision words"
      pattern: "CALLEE_GRAMMAR_CLASSES"
    - from: "a rule that refuses every leading option"
      to: "a red test rather than a guard nobody can use"
      via: "`git --no-pager status` and eleven other ordinary invocations pinned PERMITTED — this axis's `ls {git,svn}-repo`"
      pattern: "no-pager"
    - from: "a fix copied from `git push -h`"
      to: "a red test rather than a new mis-parse"
      via: "`git push --signed no origin refs/heads/gsd-auto/alpha/w` pinned REFUSED, because real git reads `no` as the repository — the discrimination between a required value and an attached-only optional one"
      pattern: "signed"
    - from: "`19-21` adding comments to `policy.rs` in a file it may not edit"
      to: "a control that measures what it names, and a seam that cannot break"
      via: "the ratio assertion replaced HERE by a per-file absolute floor on stripped production bytes, before the rule exists"
      pattern: "production_code"
---

<objective>
**The corpus for the axis that is not the shell — and it must go RED before a line of the rule is written.**

Audit 6 (`19-SECURITY.md`, the authority) answered the question it was set, and answered it in the
guard's favour:

> **The command-line-to-argv boundary is now correctly modelled. Audit 6 found no third shell
> mechanism, and says so plainly.** … there is **no reordering or relocation step in a simple
> command** for a third rule to miss.

And then found the class still open, one level past that boundary:

> Having reconstructed argv correctly, the guard must still decide **which word in it is the verb** —
> and it does that with a hand-maintained enumeration of *git's own option grammar* that **fails
> OPEN** on any option it does not know. … Rounds 1–6 all modelled the SHELL. Nothing in this phase
> models the CALLEE.

And found the corpus structurally incapable of catching it:

> `T-19-101` … **both axes are axes of the SHELL's grammar** … `grep -rn "attr-source\|shallow-file\|
> GIT_GLOBAL_VALUE_OPTS" tests/` returns **nothing at all**, and no alphabet carries a leading git
> option that consumes a separate word.

So: the corpus is widened here, observed failing here, and committed here. `19-21` writes the rule and
is gated on this plan's recorded RED state. **If the widened corpus does not go red against the
pre-fix tree, that is a finding — stop and report it.**

## The invariant this corpus is written against

**The word the guard calls the verb must be the word git calls the verb.** Rounds 5 and 6 established
the two halves of the boundary below it — a decision word must be provably LITERAL, and the words the
guard classifies must be exactly the words the program receives, in the same order. Audit 6 verified
both complete. This is the layer above: given the right words in the right order, **which one is the
command?** — and that is a question about git's grammar, not bash's.

## What must be RED at this plan's end

Measured by audit 6 and re-measured while planning at `1583d20` — fresh `GSD_MM_ENVELOPE_ROOT` per
row, the envelope directory walked afterwards and empty in every one:

```
exit=0  git --attr-source HEAD push --force origin main            <- T-19-100, ALL THREE LAYERS
exit=0  git --attr-source HEAD stash                               <- no second carrier
exit=0  git --attr-source HEAD update-ref -d refs/heads/main       <- no second carrier
exit=0  git --attr-source HEAD config core.hooksPath /tmp/x        <- disarms layer 3, no carrier
exit=0  git --attr-source HEAD reflog delete HEAD@{0}              <- no second carrier
exit=0  git --attr-source HEAD symbolic-ref HEAD refs/heads/x
exit=0  git --shallow-file /tmp/s push --force origin main
exit=0  git --shallow-file /tmp/s stash
exit=0  git --attr-source HEAD -c core.hooksPath=/dev/null push --force origin main   <- LAYER 2
exit=2  git push --recurse-submodules on-demand origin refs/heads/gsd-auto/alpha/w    <- T-19-102
```

The last one is the false refusal and it is asserted at **exit 0**, its post-fix verdict, so it is red
now and green after. All the others are asserted at exit 2.

## The cells found while PLANNING round 7

Same provenance caveat `19-14`, `19-16` and `19-18` established: found while planning, not by an
audit. All measured at `1583d20`, all inside `T-19-100`'s class rather than new threats.

| Cell | Line | Verdict | What it adds |
|---|---|---|---|
| **the stale entry, over-consuming** | `git --super-prefix push --force origin main` | **exit 0** | the list wrong in the direction that is a BYPASS rather than an over-refusal: the guard swallows the real verb `push` as `--super-prefix`'s value and reads `origin` as the verb. Not live only because real git prints `unknown option: --super-prefix`. Its twin `git --super-prefix x push --force origin main` → exit 2 |
| **the unknown option after a known one** | `git -c a=b --attr-source HEAD push --force origin main` | **exit 0** | the scan is a loop, so the gap is not confined to the first slot |
| **deletion then callee grammar** | `git >/dev/null --attr-source HEAD push --force origin main` | **exit 0** | round 6's model deletes the redirection and hands the callee gap a clean argv |
| **callee grammar then deletion** | `git --attr-source HEAD >/dev/null push --force origin main` | **exit 0** | the other order, same class |
| **a continuation INSIDE the option name** | `git --attr-so\`+NL+`urce HEAD push --force origin main` | **exit 0** | **the pin that proves round 6 is load-bearing here**: the continuation must be deleted before `--attr-source` is even spelled, and only then does the callee gap open |
| **the nested payload** | `bash -lc "git --attr-source HEAD push --force origin main"` | **exit 0** | reaches the same place through `NestedPayload`'s re-split |
| **behind a sequence** | `echo hi && git --attr-source HEAD push --force origin main` | **exit 0** | past the separator arm |
| **the short bundle** | `git -pc user.name=x status` | **exit 0** | verb read as `user.name=x`. Real git: `unknown option: -pc` — a mis-index of a command that does not run, LABELLED as such |
| **the bare dash** | `git - push --force origin main` | **exit 0** | `scan_leading` breaks on `-` before any option check, so the verb is `-`. Real git: `unknown option: -`. A control that the rule must NOT widen into non-option words |
| **the attached short `-C`** | `git -C/tmp push --force origin main` | exit 2 | already refused; real git rejects `-C/tmp` too, so there is no attached short form to model |
| **`--` end of options** | `git -- push --force origin main` | exit 2 `force_push_blocked` | already correct, pinned as a control |
| **the hooks key ahead of the gap** | `git -c core.hooksPath=/dev/null --attr-source HEAD push` | exit 2 `hook_bypass_blocked` | the key check fires before the verb is reached, pinned as a control |

## The forge cell — recorded, NOT fixed

`glab --host gitlab.com mr create --title x` → **exit 0 with ZERO ledger lines** at `1583d20`, while
`glab --hostname gitlab.com mr create --title x` → exit 0 with **one**. `FORGE_VALUE_OPTS` is
`["-R", "--repo", "--hostname"]` — the same hand-maintained enumeration of a callee's option grammar,
in a third component, failing in the UNDER-COUNTING direction. **`glab` is not installed on this
machine**, so whether glab accepts `--host` as a separate-value global flag is not confirmed against
the callee and this is NOT claimed as a live bypass. `gh` was swept and is clean: `gh --repo o/r`,
`gh -R o/r`, `gh --hostname h.example`, `gh api --hostname h.example` and `gh --version` all leave
exactly one ledger line. Recorded in `deferred-items.md` as a cell for the round after this one.
**Out of the three-item scope; do not fix it.**

## What this plan is NOT

It closes nothing. `T-19-100`, `T-19-101` and `T-19-102` stay open at this plan's end; `T-19-101` is
closed only when `19-21`'s rule is certified by the corpus written here, because a corpus is evidence
about a control and there is no control yet.

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-21`, or by the two together.** `T-19-86`
and `T-19-91` remain open at `high`. The SUMMARY must say so first.

Purpose: make the corpus capable of failing on the axis that is not the shell — whose grammar decides
which arriving word is the verb — and prove it does.
Output: a sixth evidence file, a third named class axis with its own floors, a re-expressed
anti-vacuity control, two committed RED states, one appended record, and an exact handoff number for
`19-21` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-19-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@tests/envelope_wrapper_class.rs
@tests/envelope_argv_deletion.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE against the guard AND against real git, then RED — the sixth evidence file</name>
  <files>tests/envelope_callee_grammar.rs</files>
  <precondition>
    Work in the main checkout at `1583d20` or later with a clean tree — ISOLATION=none is forced for
    this phase, so do NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`,
    which is what every guard measurement is driven against. A real `git` binary is on `PATH`
    (`git version 2.43.0` on the machine this plan was written on) and a writable temp directory is
    available; the real-git probes and the push fixture both need them. `glab` is NOT installed and
    must not be assumed. The three documented flakes are pre-existing.
  </precondition>
  <read_first>
    `tests/envelope_argv_deletion.rs:1-140` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, and the
    one-`TempDir`-per-row discipline — and its mechanism-pin section for the shape of an assertion
    made over a `policy::` function directly rather than over an exit code. `19-SECURITY.md`'s
    sections "Threats found by audit 6", "The known-open set, as audit 6 verified it" and
    "Audit 6 — what the round-6 control can and cannot fail on". `src/envelope/policy.rs:257-317`
    (`classify_git` and its denylist default arm), `:325-423` (`scan_leading`, its one-scan-two-callers
    doc, the `-` break, the `--` break and the `is_hooks_path_key` decision), `:438-453`
    (`push_needs_resolved_dests`, the second caller of the same scan), `:455-489`
    (`GIT_GLOBAL_VALUE_OPTS` and `leading_git_option` — **the `(None, 1)` on line 488 is the fail-open**),
    `:507-509` (`PUSH_VALUE_OPTS`), `:609-674` (`push_operands`), `:3219-3318`
    (`resolve_program_with_head` — the exhaustive post-filter and the **fail-closed wrapper axis whose
    treatment audit 6 says `scan_leading` was never given**), `:3649-3690` (`FORGE_VALUE_OPTS` and
    `subcommand_word_indices`, for the forge cell only — do not change them).
  </read_first>
  <action>
**Measure before you assert, and measure the CALLEE as well as the guard. Nothing in this file is
written until it has been driven.**

Build, then drive every guard row exactly as audit 6 did:

```
printf '{"tool_name":"Bash","tool_input":{"command":"<CMD>"}}' \
  | GSD_MM_ENVELOPE_ROOT=$(mktemp -d) ./target/debug/gsd-meta-manager envelope guard alpha
```

one fresh root per row, a walk over the whole root afterwards. Build the JSON with
`python3 -c json.dumps` so a backslash-newline survives as the two bytes it is.

**Then probe the REAL git binary, two-sided, and record the table.** This is the step that separates a
grammar claim from a guess, and it is what audit 6 did when it ran `git --attr-source XVALUE version`
against the control `git --bogus-opt XVALUE version`. In a throwaway directory, for every candidate
option:

  - `git <opt> version` — if it prints `git version …`, the option did NOT consume the next word;
  - `git <opt> XVALUE version` — if it prints `git version …`, the option DID consume `XVALUE`;
  - if both print `unknown option: <opt>`, the installed git does not accept the option at all.

Three options need a VALID value rather than `XVALUE` because they fail for their own reasons in both
forms, and the plan states the trap rather than letting it be discovered: `-C` needs an existing
directory (`git -C version` answers `cannot change to 'version'`, which is itself the proof that it
consumed the word), `--config-env` needs a `KEY=ENVVAR` pair, and the terminating family
(`--exec-path`, `--html-path`, `--man-path`, `--info-path`, `--version`) never prints a version at all
— for those the probe is that the 1-word and 2-word forms produce IDENTICAL first lines, which is the
proof that no following word can reach a verb slot. `--help` and `-h` satisfy no probe of this shape
(`git --help XVALUE version` answers `No manual entry for gitXVALUE`) and are recorded as UNPROBED
with that reason.

Measured on `git version 2.43.0` while planning, and to be re-measured rather than copied:

```
CONSUMES A SEPARATE WORD:  -c  --git-dir  --work-tree  --namespace  --attr-source  --shallow-file
                           -C (via `cannot change to 'XVALUE'`)   --config-env (via `invalid config format: XVALUE`)
SELF-CONTAINED (boolean):  --no-pager  -p  --paginate  -P  --bare  --no-replace-objects
                           --literal-pathspecs  --glob-pathspecs  --noglob-pathspecs
                           --icase-pathspecs  --no-optional-locks
TERMINATING (identical):   --exec-path  --html-path  --man-path  --info-path  --version
NOT ACCEPTED by this git:  --super-prefix   --no-lazy-fetch   --no-advice   --bogus-opt
UNPROBED, reason stated:   --help  -h
```

**`--super-prefix` being rejected by the installed git is the measurement that names the defect**, and
`--no-lazy-fetch` and `--no-advice` are real global options in later git releases that this git
rejects — they are the concrete, measured representatives of the future-git over-refusal cost, and
they belong in the record.

Any audit-6 row that does not reproduce at the recorded verdict: STOP, record the measured verdict,
and report it as a finding rather than asserting the audit's number over your own measurement.

Then create `tests/envelope_callee_grammar.rs`. Its header states what the file is: audit 6's
`T-19-100` rows and `T-19-102`'s false refusal plus the cells found while planning round 7, measured
against the built binary AND against real git BEFORE the rule was written so the corpus cannot have
been shaped to agree with the code, and written as a SIXTH file rather than appended to
`tests/envelope_argv_deletion.rs` so round-6 and round-7 evidence stay attributable to the round that
produced them. It states the invariant the file is about — **the word the guard calls the verb must be
the word git calls the verb** — that rounds 5 and 6 closed the two halves of the boundary below it,
and that this file is about the layer above. It states that the file is RED at this plan's end by
design and that `19-21` closes it.

Carry the harness forward from `tests/envelope_argv_deletion.rs` unchanged in shape, including the
walk. Print the walk for at least one refused row and one counted row so the SUMMARY records listings
that were measured.

**Section 1 — the `T-19-100` rows, asserting exit 2 with a named reason identifier.** A test whose doc
records the mechanism in git's own terms: `scan_leading` skips leading `-`-initial tokens and asks
`leading_git_option` how many words each occupies; its answer for an unrecognised option is `(None, 1)`
at `policy.rs:488`, so the loop advances one word, lands on the option's VALUE, sees it does not start
with `-` and breaks with that value as the verb. The nine rows in the objective's RED table, each with
its derived post-fix identifier: the eight `--attr-source`/`--shallow-file` rows land on the identifier
their VERB earns once the value is consumed — `force_push_blocked` for `push --force`, `stash`,
`update-ref -d` and `symbolic-ref HEAD <ref>`, `hook_bypass_blocked` for `config core.hooksPath` — and
the layer-2 row lands on `hook_bypass_blocked` from the `-c` key check, which fires as soon as the
scan reaches it. **Write the derivation for each beside the row**; a verdict you cannot derive belongs
in section 6, not here. The comment records the three-leg argument audit 6 measured: layer 1's
`Bash(git push:*)` prefix rule does not match a line beginning `git --attr-source`; layer 2 is
defeated on the same line; layer 3 is disarmed because a command-line `-c` outranks the envelope's
env-injected `core.hooksPath` (D-09). It records separately that **four rows have no second carrier at
all**, and that audit 6 confirmed this class by performing a REAL destructive force push that rewrote
a bare remote's `main`.

**Section 2 — `T-19-102`, asserted at its POST-fix verdict.**
`git push --recurse-submodules on-demand origin refs/heads/gsd-auto/alpha/w` asserted at **exit 0**,
beside its permitted twin `git push origin refs/heads/gsd-auto/alpha/w` at exit 0, both driven with a
fixture repository on `refs/heads/gsd-auto/alpha/w` with a bare upstream as the guard's working
directory. It is RED at this plan's end because the row measures exit 2 `push_outside_namespace`
today, and the doc comment records that measured pre-fix verdict with its message — *the refspec
`origin` resolves to `refs/heads/origin`* — as prose rather than as a second assertion.
**Asserting the post-fix verdict rather than the pre-fix one is deliberate and it is this plan's
correction to how the last three rounds handed off**: a pin the next plan must MOVE, under a
prohibition allowing only additions, is a plan that cannot be executed, and this seam therefore grants
`19-21` no replacement exception and needs none.

Beside it, in the same test, **the discrimination control that must STAY refused**:
`git push --signed no origin refs/heads/gsd-auto/alpha/w` at exit 2 `push_outside_namespace`, with the
real-git evidence in the comment — `git push --dry-run --signed no origin refs/heads/gsd-auto/alpha/w`
answers `error: src refspec origin does not match any`, because `--signed[=(yes|no|if-asked)]` takes
an ATTACHED-ONLY optional value while `--recurse-submodules (check|on-demand|no)` takes a required one.
**A fix that completed `PUSH_VALUE_OPTS` from `git push -h` would add `signed` and introduce a real
mis-parse**, and this row is what turns that red. Add the two short spellings as controls at exit 0:
`git push -o ci.skip origin refs/heads/gsd-auto/alpha/w` and `--push-option ci.skip`, and the
attached form `--recurse-submodules=on-demand` at exit 0 — permitted today and after.

**Section 3 — the cells found while PLANNING**, in a test of their own and named for the class, each
with the sentence saying what it adds, taken from the objective's table. The nine exit-0 cells assert
exit 2 after the fix; the three already-correct cells (`-C/tmp`, `--`, the hooks key ahead of the gap)
assert the verdicts they already have, as controls. **Two of them are LABELLED as mis-indexes of
commands real git does not run** — `git -pc user.name=x status` (`unknown option: -pc`) and
`git - push --force origin main` (`unknown option: -`) — never as live bypasses. `git - push …` is
additionally pinned at **exit 0 after the fix**, because `scan_leading` breaks on `-` before any
option check and `19-21` is forbidden to widen the rule into non-option words.

**Section 4 — the PERMITTED half, which is what makes this a grammar MODEL rather than a blanket
refusal of anything beginning with `-`.** All measured at exit 0 today and asserted at exit 0 after:
`git --no-pager status`, `git --no-pager log --oneline`, `git -c user.name="$NAME" commit -m x`,
`git --git-dir=/tmp/g status`, `git -C /tmp status`, `git --bare status`,
`git --literal-pathspecs status`, `git --no-optional-locks status`, `git --exec-path status`,
`git --version`, `git --attr-source HEAD status` and `git --shallow-file /tmp/s log --oneline`.
**`git --no-pager status` is this axis's `ls {git,svn}-repo`** and the comment says so: a corpus
without the permitted half cannot fail on an implementation that refuses every leading option, which
is the shape this fix is one wrong step away from. The last two carry their real-git confirmation in
the comment — `git --attr-source HEAD status` prints `On branch gsd-auto/alpha/w` and
`git --shallow-file /tmp/s log --oneline` prints `50d11d1 init` in the fixture, so the value really is
consumed and the verb really is reached. Add the positive walk control here:
`gh pr create --title x` at exit 0 with exactly ONE ledger line in a walked fresh root, measured at
`1583d20`, which is what makes an empty walk elsewhere mean something.

**Section 5 — the COST rows, each beside its permitted twin, because the round ADDS over-refusal and
the plan must not hide it.** `git --bogus-opt status` is exit 0 today and asserted **exit 2
`envelope_assertion_failed`** after, beside `git status` at exit 0. `git --no-advice status` and
`git --no-lazy-fetch status` are the same, and their comment records what makes them the important
ones: **both are real global options in git releases after 2.43 and both are rejected by the installed
git**, so they are the measured stand-ins for the future-git cost this design accepts. And
`git --super-prefix x status`, exit 0 today and exit 2 after, whose comment records that the entry is
being REMOVED because the installed git prints `unknown option: --super-prefix`.

**Section 6 — the THREE rows whose POST-fix verdict this plan cannot derive.** Each is measured
pre-fix and the measurement is **RECORDED IN A COMMENT AND A PRINT — never written as an assertion** —
because this plan measures pre-fix and cannot catch a wrong post-fix expectation, and a row pinned at
a verdict the rule cannot produce lands RED with `19-21` forbidden to edit it. That is exactly how
`19-16` and `19-18` handled their undeliverable rows. `19-21` measures each against the rule and
appends the pin.

  - `git --attr-source $T push --force origin main`, `git --attr-source *.x push --force origin main`
    and `git --attr-source HEAD {push,--force} origin main` — all three at exit 2
    `envelope_assertion_failed` today, because rounds 5 and 6's rules fire on what the guard currently
    reads as the VERB or as a splice into it. After the fix `--attr-source` consumes its value, so
    those words are an option's OPERAND rather than a decision word, and whether the literalness bit
    still reaches them — and therefore whether the identifier stays `envelope_assertion_failed` or
    becomes `force_push_blocked` — is a `19-21` design outcome.

**Section 7 — the composition rows, which prove rounds 5 and 6 are load-bearing for this round rather
than superseded by it.** `git >/dev/null --attr-source HEAD push --force origin main`,
`git --attr-source HEAD >/dev/null push --force origin main`,
`git --attr-so`+NL+`urce HEAD push --force origin main`,
`bash -lc "git --attr-source HEAD push --force origin main"` and
`echo hi && git --attr-source HEAD push --force origin main`, all exit 0 today and all asserted exit 2
`force_push_blocked` after — the redirection is deleted by round 6's model, the continuation is deleted
by round 6's model, and only then is there a `--attr-source` for this round's rule to recognise. The
continuation row's comment states that plainly: **it is the row that turns red if round 6's deletion
model is ever removed, whatever this round's rule does.**

**Section 8 — the four carried-forward MECHANISM pins**, re-asserted here over the same public
functions rather than moved or edited in place, because this round touches the verb-index decision for
the first time since round 3:

  - **Rule B** — `policy::split_segments_with_heads` still reports `head_is_command_position == false`
    for `C=GIT_CONFIG; env -u ${C}_COUNT git fetch origin` and the two other severed spellings, and
    still reports `true` for `{ git status; }` so the pin cannot be satisfied by marking every segment
    severed;
  - **round 5's literalness bit, asserted NON-VACUOUS** — over the tokens of
    `git pus? --force origin main` and `git --attr-source HEAD push --force origin main`,
    `Token.literal` is `false` for `pus?` and `true` for `--attr-source`, `HEAD` and `push`. **The bit
    is RIGHT about every word of the bypass line and this pin says so**, which is what stops `19-21`
    from closing `T-19-100` by making the bit wrong;
  - **round 6's deletion model, asserted NON-DEAD** — `git >/dev/null push --force origin main` still
    refused, and the over-deletion control `git x2>/tmp/o push --force origin main` still PERMITTED;
  - **`policy::is_separator(">")` is `false`** and the `SEPARATORS` line is unchanged — the mechanical
    proof that the route that constant's own doc forbids was not taken.
    `separators_are_named_in_one_list_that_the_predicate_reads` in `policy.rs` asserts the same fact
    and must stay GREEN and UNMODIFIED.

Commit this as its own commit, RED. State the per-binary counts in the SUMMARY.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_callee_grammar --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo test --test envelope_argv_deletion &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_literal_decision &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_expansion_slots &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_command_position &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "ZERO-SRC-HUNKS-RED-COMMITTED"</automated>
  </verify>
  <done>Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before it was written as an assertion, and every grammar claim was confirmed by a two-sided probe of the REAL `git` binary whose full table is recorded — including that the installed git rejects `--super-prefix`, `--no-lazy-fetch` and `--no-advice`, and that `-C`, `--config-env` and the terminating family need the probe variants the plan names; `tests/envelope_callee_grammar.rs` exists as a SIXTH file with its eight sections — the nine `T-19-100` rows asserting exit 2 with a DERIVED reason identifier each; `T-19-102` asserted at its POST-fix exit 0 beside its permitted twin with the `--signed` discrimination control pinned REFUSED and its real-git evidence in the comment; the twelve planning cells with the two real-git-rejected ones LABELLED as mis-indexes rather than bypasses and `git - push …` pinned at exit 0 after; the permitted half of twelve ordinary invocations with `git --no-pager status` named as this axis's blanket-refusal control and `gh pr create --title x` as the walk's non-blindness control at one ledger line; the four cost rows beside their permitted twins with `--no-advice` and `--no-lazy-fetch` recorded as the measured future-git stand-ins; the five composition rows with the continuation row documented as round 6's load-bearing pin; and the four mechanism pins; the THREE undeliverable rows are RECORDED IN COMMENTS AND PRINTS and are **NOT written as assertions**, verified by reading the file for an `assert` on any of them; `--test envelope_argv_deletion`, `--test envelope_literal_decision`, `--test envelope_expansion_slots` and `--test envelope_command_position` are fully GREEN and unmodified; the new file is RED with every failing name recorded verbatim; `git show --stat` on this commit shows ZERO `src/` hunks; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: the THIRD AXIS — `CALLEE_GRAMMAR_CLASSES`, its alphabets and floors, and the anti-vacuity control re-expressed</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:700-900`
    (`POLICY_SOURCE`, `HOOKS_SOURCE`, `POLICY_ANCHOR`, `HOOKS_ANCHOR`, `production_code` and
    `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` — **the four positive
    controls, of which the fourth is the ratio assertion `code.len() * 4 > raw.len()` this task
    replaces**), `:1785-1830` (`resolve_programs_own_doc_still_discloses_the_residual_it_does_not_cover`
    — the doc-disclosure control the ratio floor pulls against), `:3000-3230` (section 13:
    `UNREADABLE_CLASSES`, its seven predicates, `the_corpus_can_draw_every_one_of_the_seven_unreadable_classes`
    and its floors — **read to mirror the SHAPE, not to edit**), `:3860-4000` (section 14:
    `DELETION_CLASSES` and `the_corpus_can_draw_every_one_of_the_five_deletion_classes`), `:4042-4235`
    (`DISPLACING_REDIRECTIONS`, `CONTINUATION_SPLICES` and the generative property), `:4236-4360`
    (section 14b: the floors, **the stated-arithmetic discipline audit 5 imposed after `19-16` set a
    floor of 50 against a maximum of 40**, and the `{v}>` exclusion assertion — the closest precedent
    for this task's unknown-option exclusion), `:2545-2604` (`SEVERED_PREFIXES` /
    `SEVERED_BRACE_PREFIXES` — the precedent for giving a new class its OWN alphabet rather than
    widening one whose property it cannot satisfy).
  </read_first>
  <action>
**Neither shell axis is touched.** `UNREADABLE_CLASSES` and `DELETION_CLASSES`, their predicates,
their degenerate-proofing assertions and all of their floors stay byte-identical. This task adds a
THIRD axis beside them, in the shape sections 13 and 14 already establish, and the file's own section
header records why it is a third axis rather than more entries in an existing one: audit 6 enumerated
bash's transformations one at a time and found the command-line-to-argv boundary complete, so the gap
moved off the shell entirely — from *how a word is written* and *which words arrive* to **whose
grammar decides which arriving word is the verb**.

**1. `CALLEE_GRAMMAR_CLASSES` — five classes, each with a degenerate-proof predicate.** Named once so
the per-alphabet floor, the per-class floor and the counted floor all count the same thing. Each
predicate is evaluated on a REPRESENTATIVE SPLICED COMMAND rather than on the bare entry, the way
section 14's are, because these entries are splice fragments:

  1. **a leading option that consumes a SEPARATE word** — an option token followed by a word that is
     the option's value rather than the verb (`git --attr-source HEAD push …`, `git -C /tmp status`);
  2. **a leading option that consumes NO word** — an option token immediately followed by the verb
     (`git --no-pager status`). **Class 1 cannot satisfy this and class 2 cannot satisfy class 1**,
     which is the split that makes the pair non-degenerate and it is the whole of the grammar question;
  3. **a leading option carrying an ATTACHED value** — a `--`-prefixed token containing `=`, or a
     `-c`-prefixed token with a non-empty remainder (`--git-dir=/tmp/g`, `--attr-source=HEAD`,
     `-cuser.name=x`). **This class needs no knowledge of git at all** and the doc says so: git's own
     grammar makes an attached value self-contained whatever the option is, which is why
     `git --attr-source=HEAD push --force origin main` is already correctly refused at `1583d20`;
  4. **a leading option the installed git does NOT accept** — the unknown class, drawn from
     `GIT_GLOBAL_UNKNOWN_OPTIONS` and never from `GIT_GLOBAL_OPTIONS`;
  5. **the end-of-options marker or a bare `-`** — `--` and `-`, the two tokens `scan_leading` breaks
     on before any option check runs.

**2. The degenerate-proofing block, asserted rather than described**, in the shape
`the_corpus_can_draw_every_one_of_the_seven_unreadable_classes` already uses:
`git --attr-source HEAD push` satisfies class 1 and NOT class 2; `git --no-pager push` satisfies
class 2 and NOT class 1; `git --attr-source=HEAD push` satisfies class 3 and neither 1 nor 2;
`git --bogus-opt push` satisfies class 4; `git -- push` satisfies class 5; and **`git commit -m "--attr-source x"`
satisfies NONE**, which is the quoting-and-position control — an option-shaped word that is neither
leading nor unquoted-in-the-scan's-view must not satisfy any class, or the floors become satisfiable
by a row the rule must never touch.

**3. `GIT_GLOBAL_OPTIONS` — the alphabet, tagged by class, spliced where `scan_leading` reads.** A
struct array in the shape `CONTINUATION_SPLICES` uses, each entry carrying its spelling, its class and
whether it occupies one word or two, so one alphabet feeds every floor. Entries are spliced **between
the governed program and its decision words**, because that is the only position `scan_leading` reads:
the attached-value position is already refused today and an option after the verb is not read by this
scan at all, so an alphabet drawing either would be green before the fix and would certify nothing.
Minimum entries, and each must be one the two-sided real-git probe in Task 1 CLASSIFIED rather than
one copied from this plan:

  - class 1 — `--attr-source HEAD`, `--shallow-file /tmp/s`, `-C /tmp`, `--git-dir /tmp/g`,
    `--work-tree /tmp/w`, `--namespace n`;
  - class 2 — `--no-pager`, `--bare`, `--literal-pathspecs`, `--no-optional-locks`, `-p`;
  - class 3 — `--git-dir=/tmp/g`, `--attr-source=HEAD`, `--namespace=n`;
  - class 5 — `--`.

**`--attr-source HEAD` and `--shallow-file /tmp/s` are MINIMUM entries because they are the two
spellings the rule's new knowledge uniquely adds**, exactly as `&>>` and `<<-` were for round 6, and a
corpus without them cannot fail on the two options `T-19-100` is about. Assert their presence by name.

**4. `GIT_GLOBAL_UNKNOWN_OPTIONS` — a SEPARATE alphabet, and the doc says why rather than leaving it
to be inferred.** `--bogus-opt`, `--no-advice`, `--no-lazy-fetch`, `--super-prefix x`. **Every entry in
`GIT_GLOBAL_OPTIONS` must be verdict-PRESERVING**, because the property's permitted arm asserts the
wrapped verdict EQUALS the unwrapped one — and an option the installed git does not accept is REFUSED
after `19-21` even on a permitted base, so `git --bogus-opt status` would be STRICTER than its base and
would turn that property permanently red in a file `19-21` may not edit. **This is `19-18`'s `{v}>`
blocker one axis over**, it is the single most likely way this seam breaks, and it is prevented here
by construction: assert mechanically that no `GIT_GLOBAL_OPTIONS` entry appears in
`GIT_GLOBAL_UNKNOWN_OPTIONS` and that the unknown alphabet is never drawn by the invariance arm. The
unknown class gets its own fail-closed property instead — spliced into both refused and permitted
bases, asserting refusal and an empty walk in both.

**5. The generative property**, modelled directly on section 14's: for each governed base whose
unwrapped form is REFUSED, splice each `GIT_GLOBAL_OPTIONS` entry at the position between the program
and its decision words, assert **refusal AND an empty walk**, and record the slots seen; for each base
whose unwrapped form is PERMITTED, splice the same entries and assert the verdict is **unchanged**,
with the positive walk control preceding the loop so an empty walk means something. **Assert the exit
code and the walk, never the reason identifier** — a class-4 row on a refused base is refused today for
its verb and after the fix for its option, which is a reason-identifier change on a row whose verdict
never moves, and identifiers belong in the named per-row pins of Task 1 where each carries its own
derivation. Floors in the file's existing shape — `MIN_CALLEE_GRAMMAR_CLASSES`,
`MIN_GIT_GLOBAL_OPTIONS`, `MIN_GIT_GLOBAL_UNKNOWN_OPTIONS`, `CALLEE_GRAMMAR_CASES`,
`CALLEE_GRAMMAR_REFUSED_CASES`, `CALLEE_GRAMMAR_PERMITTED_CASES`, `CALLEE_GRAMMAR_SLOTS` and
`CALLEE_GRAMMAR_CLASS_COUNTS` — **each computed from the generation arithmetic (entries × bases ×
slots) and STATED in the constant's doc rather than guessed**, as exact equalities so losing one case
turns them red. Audit 5 found `19-16` set a floor of 50 against a maximum of 40 by construction; state
the arithmetic and check it.

**6. The per-alphabet floor for the new axis**, in the shape of
`every_alphabet_this_round_widens_can_draw_a_word_the_shell_deletes` — every alphabet this round widens
must be able to draw at least one callee-grammar class, with a failure message that says the correct
response is to RESTORE entries and never to delete the floor, and that names `T-19-101` as the sixth
consecutive instance of this failure mode.

**7. The anti-vacuity control, RE-EXPRESSED.** Inside
`wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`, the fourth positive control
is currently `code.len() * 4 > raw.len()` — a 25% ratio floor. **Delete that assertion and put a
per-file ABSOLUTE floor on `production_code(...).len()` in its place.** The other three positive
controls (the two anchor assertions and `code.contains(anchor)`) are untouched: they are the real
anti-truncation controls and this one is their belt.

Re-derived independently while planning, at `1583d20`, using the same stripper:

```
src/envelope/policy.rs   raw 262,748   stripped 65,933   ratio 25.0936%   headroom 246 stripped bytes / 984 comment bytes
src/envelope/hooks.rs    raw  99,753   stripped 33,434   ratio 33.5168%
```

Audit 6's judgement, quoted in the constant's doc rather than paraphrased: *the control has stopped
measuring what it was written to measure … the floor now functions as a documentation budget whose red
says "the stripper broke" while meaning "someone wrote comments" … keep the anti-vacuity property,
re-express it as an absolute floor on stripped production bytes rather than a ratio against a file
whose comment volume is itself a deliberate security artifact.*

The new constants, with their derivation in the doc:

```
POLICY_MIN_PRODUCTION_BYTES = 40_000    // 61% of the 65,933 measured at 1583d20
HOOKS_MIN_PRODUCTION_BYTES  = 20_000    // 60% of the 33,434 measured at 1583d20
```

The doc states the derivation and the direction of each margin: **ordinary refactoring that deletes a
third of either file still passes, while a stripper that ate the logic — kept only comments, broke on
the `#[cfg(test)]` sentinel, or truncated the include — drops to near zero and turns red.** It states
that the floor is deliberately INDEPENDENT of comment volume, and names
`resolve_programs_own_doc_still_discloses_the_residual_it_does_not_cover` as the control the ratio
pulled against — a control that REQUIRES specific paragraphs to exist cannot coexist with one that
penalises writing them. The failure message says the correct response to a red is to fix
`production_code` or restore the deleted logic, **never to lower the floor**.

**The ratio assertion is REPLACED, not supplemented**, and the reason is a seam rather than taste:
`19-21` adds a production category, a refusal message and a drift pin's doc to `policy.rs` and may not
edit this file, so a ratio left in place would be red at the end of a round nobody could fix without
breaking the prohibition. This is the ONE existing assertion this plan is permitted to delete, it is
named here, and the replacement is stated to be strictly more specific to the failure the original
names: an absolute floor cannot go red for comment growth and cannot go green under a proportional
truncation.

This whole task is RED by design except the permitted arm, the degenerate-proofing block and the
re-expressed anti-vacuity control: the refusal assertions cannot pass until `19-21` exists. Commit as
its own commit, second, with ZERO `src/` hunks.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c 'UNREADABLE_CLASSES\|DELETION_CLASSES' tests/envelope_wrapper_class.rs; rtk proxy grep -c 'code.len() \* 4' tests/envelope_wrapper_class.rs; git diff HEAD~1 --numstat -- tests/envelope_wrapper_class.rs; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "THIRD-AXIS-RED-ZERO-SRC"</automated>
  </verify>
  <done>`UNREADABLE_CLASSES` and `DELETION_CLASSES`, their predicates, their degenerate-proofing assertions and all of their floors are byte-identical — verified by `git diff` showing no deletion inside either region; `CALLEE_GRAMMAR_CLASSES` names five classes with degenerate-proof predicates and the assertion block proving each pair non-degenerate with `git commit -m "--attr-source x"` satisfying NONE; `GIT_GLOBAL_OPTIONS` exists as a class-tagged alphabet spliced BETWEEN the governed program and its decision words, carries `--attr-source HEAD` and `--shallow-file /tmp/s` as assert-by-name minimum entries, and every entry is one the real-git probe classified; **`GIT_GLOBAL_UNKNOWN_OPTIONS` is a SEPARATE alphabet with its own fail-closed property, is asserted disjoint from `GIT_GLOBAL_OPTIONS`, and is never drawn by the invariance arm** — with the doc recording that an option the installed git rejects is refused after `19-21` even on a permitted base, which is `19-18`'s `{v}>` blocker one axis over; the generative property asserts refusal AND an empty walk over refused bases and an UNCHANGED verdict over permitted ones, asserts exit codes and walks but never reason identifiers, and has a positive walk control preceding the loop; every new floor's arithmetic is stated in its doc and equals the generation count; the anti-vacuity control's ratio assertion is DELETED and replaced by `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` with their derivations, the audit-6 judgement quoted, the doc-disclosure control named, and a failure message that forbids lowering them — with a grep confirming the ratio expression no longer appears; no other existing floor, alphabet entry or property was lowered, deleted or narrowed; the new properties are RED with every failing name recorded; `git show --stat` shows ZERO `src/` hunks; and this is the second commit.</done>
</task>

<task type="auto">
  <name>Task 3: the registrations, the outstanding gap, the provenance correction, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s sections "Threats found by audit 6", "The known-open set, as audit 6 verified
    it", "The four execution-time judgements, assessed independently", "Audit 6 — what the round-6
    control can and cannot fail on" and "Audit 6 Sign-Off", plus the appended execution records of
    plans 19-18 and 19-19 which this plan's subsection FOLLOWS and does not edit.
    `deferred-items.md`'s round-6 closure section, its `T-19-96` entry, and its
    `tests/envelope_tracer.rs` ETXTBSY entry carried since 19-07.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after `19-19`'s and touching nothing that precedes
it, titled so a reader cannot mistake it for an audit finding — a record made by plan 19-20 — carrying:

  - **the invariant this round is about**, stated once: the word the guard calls the verb must be the
    word git calls the verb; rounds 5 and 6 closed the boundary below it and audit 6 verified that
    closure, so this round is a layer above rather than a seventh spelling of the same class;
  - **every measured row with its walk**, in the column shape the audit uses: the nine `T-19-100`
    rows, `T-19-102` and its twins, the twelve planning cells, the twelve permitted rows, the four
    cost rows, the three undeliverable rows and the five composition rows;
  - **the two-sided real-git probe table in full**, every candidate option with both forms' output,
    including the three that need the probe variants (`-C`, `--config-env`, the terminating family)
    and the two recorded UNPROBED with their reason. **The rejections are the point and must be
    prominent**: this git prints `unknown option: --super-prefix`, `unknown option: --no-lazy-fetch`
    and `unknown option: --no-advice`;
  - **the fixture confirmation for `T-19-102`**, including that real git runs
    `git push --recurse-submodules on-demand origin refs/heads/gsd-auto/alpha/w` to completion while
    answering `error: src refspec origin does not match any` for the `--signed no` spelling — the
    measurement that distinguishes a required value from an attached-only optional one;
  - **the design question, answered, with every rejected option named and costed.** Four options were
    considered and the record states each with its measured reason:
      (i) **complete the list** — rejected: it closes today's two cells and is wrong again at the next
      git release, and the list is already wrong in both directions against the installed git;
      (ii) **derive the grammar from git at guard time** — rejected on two independent grounds, and
      the record states both: the guard runs synchronously on the agent's `PreToolUse` critical path
      and `push_needs_resolved_dests` exists precisely because a reproduced 180–240 second hang made
      shelling out per call unacceptable; and a guard that asks the program it is guarding to describe
      its own grammar can be lied to by a `git` earlier on `PATH`, which is the same attacker surface
      this phase's own shims demonstrate. There is also no machine-readable enumeration to read:
      git's global options are documented in prose and `--list-cmds=` lists commands, not options;
      (iii) **derive it at build or envelope-construction time** — rejected: the probing binary is not
      the guarded binary, the result is non-hermetic, and a probe that fails would have to fail closed,
      which is a guard nobody can build;
      (iv) **ADOPTED — invert the failure direction and pin the list against real git in a TEST.** The
      knowledge required is exactly one bit per option — does it consume the next word — and the fix is
      to make the ABSENCE of that bit a refusal instead of a guess, the same treatment
      `resolve_program`'s wrapper axis already has. The structural rules that need no knowledge
      (an attached `=` value, `--`, a non-`-` token) are used first so the list has less to know. **The
      better source of truth is the installed git binary itself, consulted at TEST time rather than at
      guard time**, which catches drift in both directions and already catches `--super-prefix` today;
  - **the measured over-refusal cost, stated plainly and from both sides.** On the installed git
    2.43.0 the cost is **ZERO**: every option this git accepts is classified by the probe and
    enumerated, and the only rows moving permitted → refused are ones git itself rejects
    (`--bogus-opt`, `--super-prefix`, `-pc`, `-C/tmp`), so they are refusals of commands that already
    do nothing. On a FUTURE git the cost is **one refusal per newly added global option until the
    constant learns it**, with `--no-advice` and `--no-lazy-fetch` as the measured stand-ins — real
    options in later releases, rejected by this git. **How a user recovers, in the order the refusal
    message should offer it**: spell a value-taking option with its value attached (`--option=value`),
    which needs no list change at all because the attached form is self-contained by git's own
    grammar; or drop the option; or add it to the constant, which the drift pin will name. State that
    the guard's refusal names the OPTION TOKEN, on the same footing as the existing `git -c
    {assignment}` refusals, so the message is actionable without quoting the command back (SAFE-04);
  - **why the asymmetry between `scan_leading` and `push_operands` is deliberate**, since one gets the
    fail-closed treatment and the other does not: `scan_leading`'s unknown-option failure direction is
    a BYPASS (the value becomes the verb, and the verb is what the denylist reads), while
    `push_operands`' is an over-refusal (an unconsumed value becomes an extra refspec, which refuses).
    A fail-closed `push_operands` would refuse `git push --dry-run origin <ref>` and every other
    one-word push flag, which is a control that fails into unusability and gets switched off
    (AR-19-11). **But `PUSH_VALUE_OPTS` is fail-open in the OVER-consuming direction exactly as
    `GIT_GLOBAL_VALUE_OPTS` is** — an entry git does not treat as value-taking makes the guard skip a
    word git reads as a refspec — so it gets the same real-git drift pin without the fail-closed
    default;
  - **the `glab --host` forge cell**, with its measurement, its `gh` sweep showing that side clean,
    and the explicit caveat that glab is not installed so the callee's grammar is unconfirmed and this
    is not claimed as a live bypass. Recorded as the next round's candidate, deliberately not fixed;
  - **the `T-19-17r` bookkeeping gap, recorded as OUTSTANDING and deliberately NOT resolved.** State
    that `19-17-SUMMARY.md` calls it "accepted"; that audits 5 and 6 both confirmed the measurement and
    both pins at `tests/envelope_literal_decision.rs:1355-1379`; that there is **no Accepted-Risks-Log
    row and no register row**; and that **this plan does not make the acceptance, because accepting a
    risk is a human decision and both audits explicitly declined to make it.** The next round either
    adds the log row or drops the word — and this plan does neither;
  - **the `envelope_tracer` provenance correction, made once and not re-litigated.**
    `19-19-SUMMARY.md` presents the `a_relocated_copy_of_the_stub_refuses_instead_of_acting`
    ETXTBSY flake as newly observed; `deferred-items.md` has carried it since 19-07 under
    "`tests/envelope_tracer.rs` — ETXTBSY when a just-copied stub is exec'd", with the same symptom
    string, the same write-then-exec diagnosis and the same "once in ~6 full-suite runs" frequency.
    Record it as a provenance slip rather than a defect, note that audit 6 ran the test 8/8 green in
    isolation, and note that it fails closed by ERRORING and therefore cannot mask a regression. Do
    not edit `19-19-SUMMARY.md`;
  - **the statement that this plan CLOSES NOTHING**, that `T-19-100`, `T-19-101` and `T-19-102` all
    stay open, that `T-19-86` and `T-19-91` remain OPEN at `high` so `/gsd-secure-phase 19` is not
    cleared, and that re-measuring and re-classifying these rows is that command's job rather than a
    plan's.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Record that the round-7 corpus exists and is RED for `T-19-100`,
`T-19-101` and `T-19-102`; fold the twelve planning cells into `T-19-100`'s entry as spellings of its
class rather than registering new threat IDs, with the provenance caveat `19-14` established; add the
`glab --host` forge cell as its own entry with the unconfirmed-callee caveat and the explicit note
that it is out of this round's scope; leave the `T-19-17r` bookkeeping gap entry present and
unresolved. Leave `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` unchanged and open.

**3. The handoff gate.**

  - The command is `rtk proxy cargo test --no-fail-fast`. A plain `cargo test` fail-fasts at
    `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a
    single envelope suite; `rtk proxy` is required because the RTK hook strips exactly the `warning:`
    and `test result:` lines any count is read from — `19-19` first read 1364 instead of 1605 that way
    (D-34). Read the counts with `rtk proxy grep` over the redirected log, never a plain `grep`.
  - **State the gate arithmetic explicitly rather than assuming it.** `passed + failed` is strictly
    greater than **1610**, the total `19-19-SUMMARY.md` records and audit 6 re-measured. A red test
    RAN, so red→green leaves the total unchanged and the increase comes ONLY from the NEW `#[test]`
    fns this plan adds — count them from `git show` and check the identity against the observed
    number. A disagreement is a finding.
  - Every failure name is one of the three documented flakes
    (`a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`,
    `a_run_killed_without_an_ending_is_reported_crashed_and_nothing_on_disk_is_repaired`,
    `a_relocated_copy_of_the_stub_refuses_instead_of_acting`) or one of the RED names this plan
    creates. **The RED set is non-empty and every one of its names is listed in the SUMMARY** — that
    list is `19-21`'s handoff contract and the one number a run that did nothing cannot forge.
  - `--test envelope_argv_deletion`, `--test envelope_literal_decision`,
    `--test envelope_expansion_slots` and `--test envelope_command_position` are FULLY GREEN and
    unmodified.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate: it
    already fails at base on four pre-existing lints in `src/browser.rs` and `src/project_creator.rs`.
  - Record the per-binary counts for all seven envelope suites this round touches or carries.

Commit the record as its own commit, the third.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-20-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -c '^| AR-19-13 |' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-20-gate.log | tail -45; git diff --stat HEAD~3..HEAD | tail -12; git log --oneline -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-20 record with the invariant, every measured row and its walk, the full two-sided real-git probe table including the three options this git rejects, the fixture confirmation for `T-19-102` and its `--signed` twin, the design question answered with all four options named and the three rejected ones costed, the measured over-refusal cost stated as zero on the installed git and one refusal per future global option with `--no-advice`/`--no-lazy-fetch` as the stand-ins and the three-step user recovery path, the deliberate `scan_leading`/`push_operands` asymmetry with its reason, the `glab --host` forge cell with its unconfirmed-callee caveat, the `T-19-17r` gap recorded as OUTSTANDING with the acceptance deliberately unmade, the `envelope_tracer` provenance correction made once, and the statement that nothing is closed and no gate is cleared — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection, and the Accepted-Risks-Log row count for that risk ID still zero; `deferred-items.md` records the RED corpus, folds the twelve planning cells into `T-19-100`'s class, adds the forge cell out of scope, and leaves `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` unchanged; the `--no-fail-fast` run reports `passed + failed` strictly greater than 1610 with the arithmetic stated and checked against `git show`, counted with `rtk proxy grep` over a redirected log, every failure name is one of the three documented flakes or one of this plan's RED names, and the complete RED list is in the SUMMARY; the four prior evidence files are green and unmodified; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, and then to the words that decide, before it runs |
| written word → word the shell runs | Round 5 closed this half: which BYTES reach `execve` is decided by expansions the guard does not perform |
| words read → words that ARRIVE | Round 6 closed this half: the shell DELETES words and characters the guard reads. Audit 6 verified the pair COMPLETE over bash |
| **reconstructed argv → which word in it is the VERB** | **This round's boundary, and it is not a property of the shell at all: the guard asks a hand-maintained enumeration of GIT's option grammar which word is the command, and answers `Allow` from the denylist's default arm when the enumeration is silent** |
| corpus → the control it certifies | An alphabet that cannot DRAW a class is a property that cannot FAIL on it, and six consecutive rounds have shipped one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-101 | Spoofing | every generative alphabet, and both named class axes (`UNREADABLE_CLASSES`, `DELETION_CLASSES`) | medium | mitigate | The corpus is widened FIRST and observed RED, the ordering that worked three times. `CALLEE_GRAMMAR_CLASSES` stands as a THIRD named axis beside two untouched shell axes, with five degenerate-proof predicates, a class-tagged `GIT_GLOBAL_OPTIONS` alphabet spliced where `scan_leading` actually reads, a SEPARATE `GIT_GLOBAL_UNKNOWN_OPTIONS` alphabet kept out of the invariance arm because its entries are not verdict-preserving, and floors whose arithmetic is stated rather than guessed. Closed only by `19-21`, because a corpus is evidence about a control and there is no control yet |
| T-19-100 | Elevation of Privilege | `scan_leading` / `leading_git_option` / `GIT_GLOBAL_VALUE_OPTS` (`policy.rs:336-489`) — an incomplete, unpinned enumeration of git's own global-option grammar which fails OPEN at line 488 | high | mitigate (corpus only; rule is `19-21`) | Nine audit-6 rows plus twelve cells found while planning are measured at their verdicts, confirmed against the REAL git binary with a two-sided probe, and written as RED assertions with a DERIVED post-fix identifier each. The PERMITTED half is written beside them — twelve ordinary invocations with `git --no-pager status` as this axis's blanket-refusal control — so the corpus can fail on a rule that refuses every leading option, not only on the bypass. The COST rows are written too, so the round's over-refusal is measured rather than asserted |
| T-19-102 | Denial of Service | `push_operands` / `PUSH_VALUE_OPTS` (`policy.rs:509`) — the same enumeration defect, in the over-refusal direction | low | mitigate (corpus only; rule is `19-21`) | The false refusal is asserted at its POST-fix exit 0 beside its permitted twin, so it is RED now and GREEN after and no body needs replacing. The `--signed no` discrimination control is pinned REFUSED with its real-git evidence, because `git push -h` spells a required value and an attached-only optional one identically and a fix copied from that help text would introduce a real mis-parse |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — bookkeeping gap RECORDED, acceptance deliberately unmade** | `19-17-SUMMARY.md` calls it accepted; there is no Accepted-Risks-Log row and no register row. Audits 5 and 6 both confirmed the measurement and both pins and both explicitly declined to add the acceptance because that is a human decision. This plan records the gap as OUTSTANDING and makes no acceptance |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; four rows still at exit 0 and their pin stays green UNMODIFIED. Because it remains open at `high`, this plan does NOT clear `/gsd-secure-phase 19`, and the SUMMARY says so first |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (registered, arms unweakened, not remedied)** | `reflog $S`, `reflog show $S` and `symbolic-ref $S` at exit 0 with no second carrier; bare `git push $REF` at exit 0 in-namespace. Record unweakened, no remedy added, counts toward `threats_open` |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG, one slot outside the decision region | medium | **accept for this plan (registered by `19-16`, not fixed)** | Left exactly as pinned. Audit 6 notes it is one of four places a decision is made on a word the decision region does not cover; widening the region is a later round's to decide |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added; this plan touches only `tests/` and `.planning/`. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. The real-git probes shell out through `std::process::Command` |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`,
  and MUST be read as `passed + failed` against **1610**.
- The gate arithmetic is STATED and CHECKED: expected `passed + failed` = 1610 plus the number of NEW
  `#[test]` fns this plan adds, counted from `git show`. A red test RAN, so red→green contributes
  nothing; a disagreement is a finding.
- The RED set is NON-EMPTY, every one of its names is listed in the SUMMARY, and every other failure is
  one of the three documented flakes.
- Every commit shows ZERO `src/` hunks under `git show --stat`. This is the evidence the split exists
  to produce.
- Every grammar claim is backed by a recorded two-sided probe of the REAL `git` binary, and the probe
  table includes the options this git REJECTS.
- `UNREADABLE_CLASSES` and `DELETION_CLASSES`, their predicates, their degenerate-proofing blocks and
  all their floors are byte-identical; no existing `MIN_*` constant was lowered.
- `GIT_GLOBAL_UNKNOWN_OPTIONS` is disjoint from `GIT_GLOBAL_OPTIONS` and is never drawn by the
  invariance arm, asserted mechanically.
- The anti-vacuity ratio assertion no longer appears and the two absolute byte floors do, each with its
  derivation and a failure message forbidding a lower value.
- `--test envelope_argv_deletion`, `--test envelope_literal_decision`, `--test envelope_expansion_slots`
  and `--test envelope_command_position` are fully GREEN and byte-identical.
- The four mechanism pins are present and non-vacuous: `split_segments_with_heads` still reports
  `head_is_command_position == false` for the severed spellings and `true` for `{ git status; }`;
  `Token.literal` is `false` for `pus?` and `true` for `--attr-source`, `HEAD` and `push`;
  `git >/dev/null push --force origin main` is still refused and `git x2>/tmp/o push --force origin
  main` still permitted; and `is_separator(">")` is `false` with `SEPARATORS` unchanged.
- `git --no-pager status` and the other eleven ordinary invocations are pinned at exit 0.
- `git push --signed no origin refs/heads/gsd-auto/alpha/w` is pinned REFUSED.
- `gh pr create --title x` leaves exactly ONE ledger line in a walked fresh root, which is the walk's
  non-blindness control.
- The Accepted-Risks-Log row count for the `T-19-17r` risk ID is zero and the word "accepted" is not
  applied to `T-19-17r`.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after `19-19`'s and nothing
  else.
- Three commits exist in order: the sixth evidence file, the third axis, the record.
</verification>

<success_criteria>
1. Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before
   it was written as an assertion, and every grammar claim was confirmed by a two-sided probe of the
   REAL git binary whose full table is recorded.
2. The corpus goes RED against the pre-fix tree, the RED set is non-empty, and every failing name is
   recorded verbatim in the SUMMARY as `19-21`'s handoff contract.
3. The callee axis is legible as a THIRD axis: `CALLEE_GRAMMAR_CLASSES` with five degenerate-proof
   predicates stands beside a byte-identical `UNREADABLE_CLASSES` and a byte-identical
   `DELETION_CLASSES`, and nothing was smuggled into either shell axis.
4. The corpus can fail in BOTH directions — the bypass rows, and twelve ordinary invocations pinned
   PERMITTED with `git --no-pager status` named as this axis's `ls {git,svn}-repo`.
5. `T-19-102` is asserted at its POST-fix verdict beside the `--signed` discrimination control, so the
   round removes a false refusal without acquiring a new mis-parse.
6. **The handoff needs no replacement exception and none is granted**: every derivable row is asserted
   at its post-fix verdict, the three undeliverable rows are RECORDED rather than asserted, the unknown
   alphabet is kept out of the invariance arm, and the anti-vacuity control is re-expressed HERE so
   `19-21`'s comments cannot break a file it may not edit.
7. The design question is answered in writing with all four options named, the three rejected ones
   costed, and the measured over-refusal cost stated from both sides with the user's recovery path.
8. The `T-19-17r` bookkeeping gap is recorded as OUTSTANDING, no Accepted-Risks-Log row is added, and
   the word "accepted" is not used for it.
9. No `src/` line changed; no existing floor, alphabet entry, property or assertion was lowered,
   deleted or narrowed apart from the one named ratio assertion; and `T-19-86`, `T-19-91`, `T-19-96`
   and `T-19-74` are untouched.
10. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared — `T-19-86` and `T-19-91`
    remain OPEN at `high` — and no unqualified "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-20-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared and that this plan
closes nothing; the three commit SHAs in order with `git show --stat` proving ZERO `src/` hunks in
each; the `--no-fail-fast` `passed + failed` totals before and after with the gate arithmetic stated
and checked against `git show`; the COMPLETE list of RED test names, which is `19-21`'s handoff
contract; the per-binary counts for all seven envelope suites; a table of every measured row with its
exit code, reason identifier and walk listing, split into the `T-19-100` rows, `T-19-102` and its
twins, the twelve planning cells, the permitted half, the cost rows and the composition rows; the full
two-sided real-git probe table with the options this git rejects called out; the three undeliverable
rows with the reason each is left for `19-21`; the design question answered with the three rejected
options and their costs; the measured over-refusal cost from both sides with the user recovery path;
the `glab --host` forge cell with its unconfirmed-callee caveat and the note that it is out of scope;
the anti-vacuity re-expression with the measured ratio it replaces and the derivation of both floors;
the `T-19-17r` bookkeeping gap recorded as OUTSTANDING with the explicit statement that this plan did
NOT accept it and added no Accepted-Risks-Log row; the `envelope_tracer` provenance correction; and a
"what remains uncovered" section naming `T-19-86` FIRST — open at `high` by user scoping decision —
followed by `T-19-91` (open at `high`, arms unweakened, `reflog` and `symbolic-ref` with no second
carrier), `T-19-96`, `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit
statement that only the WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>
