---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 09
type: execute
wave: 1
gap_closure: true
gap_ids: [G-19-1]
depends_on: []
files_modified:
  - src/envelope/advisory.rs
  - tests/envelope_advisory.rs
autonomous: true
requirements: [SAFE-01, SAFE-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "**`SECTION_ENVELOPE` opens with ONE sentence stating the ceiling, then explains the guaranteed case with its examples, then the not-guaranteed case with its examples, then closes on the server-side branch-protection recommendation as the conclusion.** That is the shape the user asked for in the UAT, verbatim: *\"One sentence, then an explanation for both cases and examples?\"* The current text is three ~75-word paragraphs that each fuse a claim with several qualifications and put no visual separation anywhere; the failure G-19-1 records is that nobody finishes reading it (D-27)."
    - "**Every one of the SEVEN substrings `tests/envelope_advisory.rs:175` pins survives VERBATIM, each sitting wholly inside one rendered line, in the pinned byte-offset order.** `the_honesty_statement_carries_each_of_its_three_required_parts` is not edited — not one character. That test staying green against an unedited pin list is the mechanical proof that the shortening did not become a softening (D-27; 19-06 coverage D5, 19-08 coverage D2)."
    - "**Every substantive residual disclosure the statement carries today is still carried after the rewrite.** Named individually so none can be lost by inattention: the SSH agent socket is REMOVED rather than emptied; git's global and system configuration are redirected to a generated file naming NO credential helper; the pre-push hook judges the refs git hands it rather than the command line it was asked about; the PR cap is enforced from an append-only ledger this repository does not contain; client-side hooks, tool denies and env-injected git configuration are all defeatable by an agent that can spawn an unsupervised shell; an agent that unsets `GIT_CONFIG_COUNT` in a subshell is past the last layer; an agent that runs the askpass responder itself reads the token; a settings file the agent's own CLI silently ignores leaves the pull-request cap unenforced because no git hook observes a pull request; the only boundaries that do not depend on the agent's cooperation are the remote's own ruleset and the scope of the credential this run was given (D-27, D-07, D-09)."
    - "**The legibility is held by a control, not by an intention.** A new test in `tests/envelope_advisory.rs` caps the whole constant's whitespace-token count and caps its rendered line width, and its failure message prints the ACTUAL count so the number is evidence rather than an assertion about itself. The cap was observed RED against the pre-fix constant at its measured count before the rewrite landed. Without this the statement re-grows and G-19-1 is re-opened by the next well-meaning clarification."
    - "**The constant's doc comment tells the next editor the three things that will otherwise silently break the rewrite:** the `\\x20` escape requirement (Rust's backslash-continuation strips ALL leading whitespace on the next source line, so an indented example line written with literal spaces silently collapses flush-left), the ordering trap (the pin test compares FIRST occurrences, so the opening ceiling sentence must not contain the part-2 or part-3 anchor phrases), and the rule that a pinned phrase straddling a `\\n` cannot match. All three were learned the expensive way; none is discoverable from the code (D-27)."
  prohibitions:
    - statement: "MUST NOT edit `the_honesty_statement_carries_each_of_its_three_required_parts` in `tests/envelope_advisory.rs` — not the seven phrases, not the ordering assertion, not the failure messages. That test is the phase's whole defence against a refactor tidying an honesty statement into something softer, and a rewrite that has to move the pin is a rewrite that dropped something. If the new text genuinely cannot carry a pinned phrase, STOP and report it as a blocked task rather than adjusting the pin; the trade was already weighed and rejected during the UAT."
      category: honesty
    - statement: "MUST NOT drop, generalise or soften any residual disclosure listed in the third must-have truth in order to hit a word target. Shortening is a LAYOUT change and a redundancy change, never a claim change: the specifics are exactly what make this candour rather than a hedge, and an overstated safety claim is worse than a stated limitation because it gets trusted. Two secondary clauses may be dropped as redundant and only these two — the ledger clause `so the run cannot reset its own limit by deleting a file it can see` (implied by a ledger the repository does not contain) and the closing clause `it is this envelope's conclusion rather than its footnote` (the new layout demonstrates it instead of asserting it)."
      category: safety
    - statement: "MUST NOT chase a word count below the ~161-word body the UAT draft reached. The seven pinned phrases alone are ~48 words; going lower requires rewording pins, which is the prohibited edit above. The user asked for 120-140 and that target was already judged the wrong trade for this specific constant during the UAT — do not silently re-litigate it, and do not report the ~161 as a shortfall."
      category: scope
    - statement: "MUST NOT change what `envelope_notice` composes, what `dry_run` renders, or the constant's identity as the single producer both the preview and the run journal consume. Two assemblies are two things that can drift, and a preview and a journal disagreeing about what was claimed is the worst drift available here (T-19-42)."
      category: safety
  artifacts:
    - path: "src/envelope/advisory.rs"
      provides: "`SECTION_ENVELOPE` rewritten to the one-sentence-then-two-explained-cases-then-conclusion shape, carrying all seven pinned substrings verbatim and every residual disclosure, with the constant's doc comment extended with the `\\x20` indentation rule, the first-occurrence ordering trap and the one-phrase-per-line rule"
      contains: "SECTION_ENVELOPE"
    - path: "tests/envelope_advisory.rs"
      provides: "a new legibility control capping the constant's whitespace-token count and its maximum rendered line width, whose failure message prints the measured values; the pre-existing seven-phrase pin test present and byte-unchanged"
      contains: "split_whitespace"
  key_links:
    - from: "`SECTION_ENVELOPE` in `src/envelope/advisory.rs`"
      to: "`the_honesty_statement_carries_each_of_its_three_required_parts` in `tests/envelope_advisory.rs`"
      via: "seven verbatim substrings plus a first-occurrence byte-offset ordering assertion — the one control that makes a dropped part a build failure rather than a quiet loss"
      pattern: "advisory::SECTION_ENVELOPE.contains"
    - from: "`SECTION_ENVELOPE` in `src/envelope/advisory.rs`"
      to: "the dry-run preview and the run journal"
      via: "`envelope_notice`, the single producer both consume — asserted by `the_journal_notice_and_the_rendered_preview_carry_the_same_claim_text`, which must stay green through the rewrite"
      pattern: "envelope_notice"
---

<objective>
**Gap G-19-1. The honesty statement is accurate and nobody reads it.**

The human UAT pass on 2026-08-28 judged `SECTION_ENVELOPE` (`src/envelope/advisory.rs:192-214`)
**not** a candour failure. All three required parts are present, correctly ordered, and no claim was
found overstated. The defect is legibility: 250 whitespace-separated tokens across three dense
paragraphs, each fusing a claim with several embedded qualifications, with no visual separation
between the guaranteed case, the not-guaranteed case, and their examples.

The user's words: *"it reads like it's too long.. it's hard to follow. Can we shorten it
significantly. One sentence, then an explanation for both cases and examples? Doable?"*

That is a real defect against this phase's own reasoning, not a style preference. D-27 is a
`[USER-HARD-REQUIREMENT]` whose closing line is *"an overstated safety claim is worse than a stated
limitation, because it gets trusted"* — and PITFALLS' concern is precisely that safety claims get
trusted **without being understood**. An honesty statement that is not finished is not read, and an
honesty statement that is not read is doing none of the work D-27 commissioned it for.

Purpose: make the statement legible without making it softer, and leave behind a control so it
cannot silently re-grow.
Output: a rewritten `SECTION_ENVELOPE` that lands with **zero** edits to the pin test, plus a new
legibility control that was observed RED against the pre-fix constant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-UAT.md

@src/envelope/advisory.rs
@tests/envelope_advisory.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The legibility control, observed RED against the constant as it stands</name>
  <files>tests/envelope_advisory.rs</files>
  <read_first>
    `tests/envelope_advisory.rs:174-222` — `the_honesty_statement_carries_each_of_its_three_required_parts`,
    the pin this plan may not touch. Read it before writing anything so the seven phrases and the
    ordering assertion are in front of you while you work.
  </read_first>
  <behavior>
    - The whole constant, counted with `split_whitespace().count()`, is at most 190 tokens.
      Measured today: 250. The UAT draft reached a 161-token body; the header line
      `== What this envelope guarantees, and what it does not ==` is 11 tokens, so the target
      whole-constant count is ~172 and the cap leaves ~18 tokens of ordinary-rewording headroom
      while a return to the pre-fix density fails by 60.
    - No line of the constant exceeds 80 characters. Measured today: 76. This clause is GREEN
      before the rewrite and is a guard rather than a repair — say so in the test's own comment.
      It protects the property the pin test depends on: a phrase that straddles a `\n` cannot
      match, so an unwrapped rewrite would break the pins in a way whose error message points
      at the wrong thing.
    - Both failure messages print the MEASURED value (the count, and the offending line with its
      length), never a bare "too long". A control whose red output does not carry the number is a
      control the next person has to re-measure by hand.
  </behavior>
  <action>
    Add ONE new `#[test]` to `tests/envelope_advisory.rs`, named for the property it holds — a
    legibility cap on `advisory::SECTION_ENVELOPE` — placed immediately AFTER the existing pin test
    so a reader meets the contract before its budget.

    Its doc comment states why a word cap belongs on a safety text at all, and states it in the
    terms the UAT recorded: the three required parts being present is necessary and was already
    proved by the test above; the failure this one catches is different in kind — a statement long
    enough that nobody finishes it, which is a safety claim trusted without being understood. Cite
    G-19-1 and D-27.

    Two assertions, in this order:
    1. `advisory::SECTION_ENVELOPE.split_whitespace().count()` is at most the cap. Bind the cap to
       a named `const` inside the test with a comment carrying the arithmetic above (250 measured,
       ~172 targeted, 190 chosen), so the number is auditable rather than magic.
    2. Every line of `advisory::SECTION_ENVELOPE.lines()` is at most 80 characters. Report the
       first offender with its length and its content.

    Do NOT touch the pin test, and do not touch the other four tests that reference
    `SECTION_ENVELOPE`.

    Then RUN IT AND OBSERVE IT RED, before any change to `advisory.rs`. Capture the assertion output
    verbatim — it carries the measured 250 — and quote it in the SUMMARY. A cap that has never been
    seen failing certifies nothing about the rewrite that follows it.

    Note for every cargo invocation in this plan: a global RTK hook rewrites bare cargo commands and
    strips `warning:` and `test result:` lines from the output, so a check that reads raw output must
    be issued as `rtk proxy cargo ...`. A grep for a stripped line succeeds vacuously.

    Commit this task on its own, as a `test(19-09):` commit, so the RED is a reviewable point in
    history and not something the reader has to take on trust.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_advisory 2>&amp;1 | tail -40</automated>
  </verify>
  <done>The new legibility test FAILS against the unmodified `SECTION_ENVELOPE`, its message names the measured token count (250) and the cap (190), and the pre-existing tests in `tests/envelope_advisory.rs` are all still green. The RED output is captured verbatim for the SUMMARY. `advisory.rs` is unchanged at this point.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SECTION_ENVELOPE to the requested shape, with no test edit</name>
  <files>src/envelope/advisory.rs</files>
  <read_first>
    `src/envelope/advisory.rs:156-214` — the constant and the doc comment above it, including the
    "The three parts, in this order and for this reason" list and the closing **Do not soften part
    2** rule. Both survive this task; the doc gains material and loses none.
  </read_first>
  <action>
    Rewrite `SECTION_ENVELOPE` to the shape the user asked for. Keep the existing header line
    `== What this envelope guarantees, and what it does not ==` exactly as it is — it is the section
    header `dry_run` renders and it already names the two cases.

    The body, in this order:

    1. **One sentence stating the ceiling.** New, and the thing the user asked for first. It says
       what the whole envelope amounts to: the run's reach into the developer's credentials and the
       shape of what it may push are constrained by mechanism, and everything below the remote is
       defeatable by an agent that chooses to. One sentence, not two.

    2. **The guaranteed case, explained, with its examples visibly separated.** Carries the three
       part-1 claims that are pinned and their concrete examples: the run cannot reach ambient git
       credentials or the SSH agent (the agent socket is REMOVED rather than emptied; global and
       system git configuration are redirected into a generated file naming no credential helper);
       a push reaching git through the driven process tree passes the pre-push hook, which judges
       the refs git itself hands it rather than the command line it was asked about; the
       pull-request cap is enforced from an append-only ledger this repository does not contain.

    3. **The not-guaranteed case, explained, with its examples visibly separated.** Carries the
       part-2 claims and their examples with no softening: client-side hooks, tool denies and
       env-injected git configuration are all defeatable by an agent that can spawn an unsupervised
       shell and chooses to; an agent that unsets `GIT_CONFIG_COUNT` in a subshell is past the last
       layer; an agent that runs the askpass responder itself reads the token; a settings file the
       agent's own CLI silently ignores leaves the pull-request cap unenforced, because no git hook
       observes a pull request; the only boundaries that do not depend on the agent's cooperation
       are the remote's own ruleset and the scope of the credential this run was given.

    4. **The conclusion.** `Therefore: enable server-side branch protection on this repository.` —
       and the one clause that says why: it is the one control here an agent cannot talk its way
       past. Position, not assertion, is what now makes it the conclusion, which is why the
       self-describing clause about being a conclusion rather than a footnote is the second of the
       two clauses this plan permits dropping.

    Use the visual separation the current text lacks: a labelled case line followed by indented
    example lines, blank-line separated between the four movements. That separation IS the fix —
    the words below it are largely the words already there.

    **Three implementation traps, each of which silently produces a wrong result:**

    - **Indentation.** Rust's backslash-continuation strips ALL leading whitespace on the next
      source line. Every indented example line must be written with `\x20` escapes for its leading
      spaces or the indent silently vanishes and the list collapses flush-left, producing exactly
      the undifferentiated block this task exists to remove. This was verified with rustc during
      the UAT; do not re-derive it, and do not verify the indentation by reading the source — read
      the RENDERED constant.
    - **Ordering.** The pin test compares FIRST occurrences:
      `find("cannot reach your ambient git credentials")` must be less than
      `find("defeatable by an agent that can spawn an unsupervised")` must be less than
      `find("enable server-side branch protection")`. The opening ceiling sentence therefore must
      NOT contain the second or third of those phrases verbatim, or the ordering assertion fails on
      a text whose parts are in the right order.
    - **Line breaks.** Each of the seven pinned phrases must sit wholly within one rendered line. A
      phrase split across a `\n` cannot match, and the resulting failure message names the phrase
      rather than the wrapping, which sends the reader at the wrong thing. The longest pinned
      phrase is `defeatable by an agent that can spawn an unsupervised` at 53 characters; wrap
      around the phrases, not through them.

    Then extend the constant's doc comment. Keep every existing paragraph including **Do not soften
    part 2** and the pinned-contract framing. Add: the new four-movement layout in place of the bare
    "three parts" list (the three parts are still there and still ordered — the opening sentence is
    the addition); the three traps above, stated as rules for the next editor, because none of them
    is discoverable from the code and each costs an afternoon; and a line recording that G-19-1
    shortened this text for legibility on 2026-08-28 without moving a single pinned phrase, so a
    future reader knows the compression was audited rather than accidental.

    Do not touch `envelope_notice`, `protection_line`, `PROTECTION_WARNING`, `PROTECTED_CLAIM`, or
    anything in `src/driver/dry_run.rs`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_advisory 2>&amp;1 | tail -30 &amp;&amp; rtk proxy git diff --stat -- tests/envelope_advisory.rs src/envelope/advisory.rs &amp;&amp; rtk proxy cargo test --lib envelope 2>&amp;1 | tail -20 &amp;&amp; rtk proxy cargo test --lib dry_run 2>&amp;1 | tail -20 &amp;&amp; rtk proxy cargo clippy -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All tests in `tests/envelope_advisory.rs` pass, including the pin test and the new legibility cap. `git diff` shows `tests/envelope_advisory.rs` changed ONLY by Task 1's added test — `the_honesty_statement_carries_each_of_its_three_required_parts` is byte-identical to its pre-plan state. The `dry_run` in-source tests that consume `SECTION_ENVELOPE` by identity are green. `cargo clippy -- -D warnings` exits 0. The rendered constant, printed and read, shows four visually separated movements with the example lines actually indented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| envelope → operator | The honesty statement is the only place this system tells a human what it does and does not enforce. Everything downstream of this text is a trust decision the operator makes on the strength of it. |
| this edit → the pinned contract | A text edit is the exact shape of change D-27 built the pin to survive; the pin is the boundary that makes an edit auditable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-09-01 | Repudiation | `SECTION_ENVELOPE` | high | mitigate | A shortening that quietly drops a residual disclosure produces a text that reads as a stronger claim than the mechanism supports — the failure D-27 exists to prevent, and worse than the verbose original because it gets trusted. Mitigated by the enumerated disclosure list in must-have 3, the seven unedited pins, and the two-clause allowlist for permitted drops. |
| T-19-09-02 | Tampering | `tests/envelope_advisory.rs` | high | mitigate | Editing the pin to fit a rewrite converts the control into a record of whatever was written. Mitigated by an explicit prohibition and by a `git diff` acceptance check on that file. |
| T-19-09-03 | Information disclosure | rendered preview / run journal | low | accept | The statement's content is unchanged in substance; it is already user-facing on both surfaces and carries no secret. No new information crosses a boundary. |
| T-19-09-04 | Denial of service | legibility cap | low | accept | A future legitimate addition could hit the 190-token cap and block a commit. Accepted: that is the cap doing its job, and the failure is loud, immediate and carries the measured number. |

No package-manager installs occur in this plan and no dependency is added, so the package-legitimacy
gate does not apply and no `T-19-SC` row is carried.
</threat_model>

<verification>
1. `rtk proxy cargo test --test envelope_advisory` — all tests green, including the unedited pin.
2. `rtk proxy git diff -- tests/envelope_advisory.rs` — the ONLY hunk is Task 1's added test.
3. `rtk proxy cargo test` — full suite; any `driver_reattach` red is the documented pre-existing
   flake and is recorded as such, never as this plan's regression.
4. `rtk proxy cargo clippy -- -D warnings` — exit 0. `--all-targets` still fails with the documented
   pre-existing lints; that count is unchanged by this plan.
5. Print the rendered `SECTION_ENVELOPE` and read it. Confirm by eye: one opening sentence; two
   labelled cases each with visibly indented examples; the recommendation last. If the example lines
   are flush-left, the `\x20` trap was hit.
</verification>

<success_criteria>
- `SECTION_ENVELOPE` is at most 190 whitespace tokens (from 250) and reads as one sentence, then
  each case explained with its own examples, then the recommendation.
- All seven pinned substrings appear verbatim, each within one rendered line, in the pinned order.
- `tests/envelope_advisory.rs`'s pin test is byte-unchanged.
- Every residual disclosure enumerated in must-have 3 is still carried.
- The legibility control was observed RED at 250 before the rewrite and is green after.
- The constant's doc comment carries the `\x20` rule, the first-occurrence ordering trap and the
  one-phrase-per-line rule.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-09-SUMMARY.md` when done.

The SUMMARY must quote: the verbatim RED output of the legibility control against the pre-fix
constant, the final measured token count, and the full rendered text of the new `SECTION_ENVELOPE`
so a reader can judge the candour question the UAT reserved for a human without opening the source.
</output>
