---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 22
type: execute
wave: 12
gap_closure: true
gap_ids: [T-19-105, T-19-103, T-19-104, T-19-106, T-19-107]
depends_on: [19-21]
files_modified:
  - tests/envelope_config_resolution.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-05, SAFE-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, and this is the FIFTH time that ordering is the reason a plan exists separately.** It worked four times — 19-14/19-15, 19-16/19-17, 19-18/19-19, 19-20/19-21 — and the cross-plan carry-forward is what proves two rules separately load-bearing. This plan writes the corpus and the reproducers and STOPS. `19-23` writes the rules. `T-19-105` is `T-19-76`'s failure mode for the EIGHTH consecutive round, and for the third round running the axis moved rather than the cell: `UNREADABLE_CLASSES` names seven word-ASSEMBLY classes, `DELETION_CLASSES` names five word-REMOVAL classes, `CALLEE_GRAMMAR_CLASSES` names five classes of **git's OPTION grammar** — and audit 7 verified mechanically that nothing anywhere models git's CONFIG resolution: `grep -rn \"include\\.path\\|includeIf\" src/ tests/` returns **nothing at all** and `grep -rn \"GIT_CONFIG_PARAMETERS\" src/ tests/` returns **nothing at all**. Re-verified while planning at `15c40d6`: both greps still return nothing. The corpus is structurally incapable of generating, and so of failing on, `T-19-103` and `T-19-104`."
    - "**If the widened corpus does not go RED against the pre-fix tree, that is a FINDING — stop and report it, do not proceed.** A row already refused certifies nothing about the class this round is about, and a property green before the fix is a property that could not have failed on it. Every row here is measured against the built binary BEFORE it is written as an assertion, and any row that does not reproduce is reported with its measured verdict instead of being asserted anyway."
    - "**THE MOST IMPORTANT CONSTRAINT IN THIS PLAN, found by measurement while planning and stated FIRST because a corpus that misses it certifies nothing: THE BASE MUST BE A COMMAND LAYER 2 PERMITS.** The whole of `T-19-103` is that an indirection disarms layer 3 on a line layer 2 lets through — so a reproducer whose base is *already refused for its verb* proves nothing about config resolution. Measured at `15c40d6`, fresh root per row, walk EMPTY on every one: `git -c include.path=/tmp/evil.cfg push --force origin main` → **exit 2 `force_push_blocked`**, `bash -lc \"git -c include.path=/tmp/evil.cfg push --force origin main\"` → **2 `force_push_blocked`**, `echo hi && git -c include.path=… push --force origin main` → **2**, `git >/dev/null -c include.path=… push --force origin main` → **2**, `git --attr-source HEAD -c include.path=… push --force origin main` → **2**. **Every one of those is a CONTROL, not a reproducer.** The reproducers are the three layer-2-permitted bases audit 7 itself used: `commit -m x`, `status`, and the IN-NAMESPACE push `push origin refs/heads/gsd-auto/alpha/w`. This is asserted mechanically, not left to care."
    - "**THE AXIS MOVED AGAIN, AND THIS TIME IT LEFT THE COMMAND LINE.** Rounds 1–6 modelled the shell — how a word is written, which words arrive. Round 7 modelled the callee's option grammar — which arriving word is the verb. Audit 7's framing, adopted verbatim: **git turns a command line into behaviour in three stages, and this phase now models one.** Stage 2 is *what configuration is in effect while the verb runs* and stage 3 is *what the process it inherits tells it*, and **both reach `core.hooksPath`** — the single setting the envelope's entire third layer is, the setting `T-19-09` denies by name, and the setting `cred.rs:241-248` claims only one form outranks. So the corpus gets a FOURTH named axis, `CONFIG_RESOLUTION_CLASSES`, standing beside a byte-identical `UNREADABLE_CLASSES`, a byte-identical `DELETION_CLASSES` and a byte-identical `CALLEE_GRAMMAR_CLASSES`, with its own predicates, its own alphabets and its own floors. **Smuggling a config carrier into the callee-grammar axis would model resolution as if it were option grammar and lose exactly the distinction this round is about.**"
    - "**`T-19-103` IS A DEMONSTRATED EXPLOIT AND EVERY ROW OF IT IS MEASURED, NOT ARGUED.** Re-measured while planning at `15c40d6` against the built binary, one fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards and EMPTY in every one — all seven audit-7 rows reproduce exactly: `git -c include.path=/tmp/evil.cfg commit -m x` → **exit 0**; `git -c include.path=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w` → **0**; `git -c include.path=/tmp/evil.cfg status` → **0**; `git -c includeIf.gitdir:/tmp/.path=/tmp/evil.cfg status` → **0**; `git --config-env=include.path=EVILVAR status` → **0**; `git --config-env include.path=EVILVAR status` → **0**; `git -c INCLUDE.PATH=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w` → **0**. **The paired discriminators still refuse, which is the evidence the mechanism is present and this is a gap in its REACH**: `git -c core.hooksPath=/dev/null commit -m x` → 2 `hook_bypass_blocked`, `git --config-env=core.hooksPath=EVILVAR status` → 2, `git --config-env core.hooksPath=EVILVAR status` → 2, `git -c CORE.HOOKSPATH=/dev/null status` → 2. Audit 7 confirmed the class against real git end to end: an in-namespace push the `pre-push` hook REFUSES **completed and moved a bare remote's ref** under `-c include.path=`, and a persisted alias carrying the same **rewrote the bare remote's `main`**."
    - "**`T-19-104` IS A GAP IN A LIST, NOT IN THE MECHANISM, AND THE PAIRED DISCRIMINATOR IS WHAT PROVES IT.** `GIT_CONFIG_PARAMETERS` is git's own internal carrier for `-c` — git EXPORTS it, so one prefix disarms every git subprocess of the command — and it is absent from `ENVELOPE_ENV_KEYS`. Re-measured at `15c40d6`, walk EMPTY: `GIT_CONFIG_PARAMETERS=\"'core.hooksPath=/tmp/nohooks'\" git commit -m x` → **0**; the same prefix before `git push origin refs/heads/gsd-auto/alpha/w` → **0**; the `export …; git push …` spelling → **0**; the `env … git push …` spelling → **0**; the composed `GIT_CONFIG_PARAMETERS=\"'include.path=/tmp/evil.cfg'\" git push …` → **0**. **All three spellings of the paired discriminator refuse at `hook_bypass_blocked` on BOTH a refused and a permitted base** — `GIT_CONFIG_COUNT=0 git push origin refs/heads/gsd-auto/alpha/w`, `export GIT_CONFIG_COUNT=0; git push …`, `env GIT_CONFIG_COUNT=0 git push …`, and the same three before `git status` — which is what makes every post-fix verdict on this axis DERIVABLE rather than guessed."
    - "**THE DISCRIMINATION CONTROLS ARE THIS ROUND'S `--signed no`, AND THEY ARE WHAT STOP THE FIX FROM BEING A SUBSTRING MATCH ON `include`.** Measured at `15c40d6`, all **exit 0** and all pinned at **exit 0 AFTER** as well: `git -c includepath=/tmp/evil.cfg status` (no `.` at all, so no section) and `git -c notinclude.path=/tmp/evil.cfg status` (section `notinclude`). A rule written as `key.contains(\"include\")` turns both red, and a rule written as a SECTION comparison keeps both green. **Beside them, the row whose verdict the rule DOES move and whose cost is therefore disclosed rather than discovered**: `git -c include.pathx=/tmp/evil.cfg status` is exit 0 today and is pinned **exit 2 `envelope_assertion_failed`** after, because the mandated rule reads the SECTION and deliberately does not read the variable — an over-refusal of a key git ignores, in the safe direction, stated in the corpus rather than found by an audit."
    - "**THE SEAM DEFECT THAT WOULD HAVE BLOCKED THIS ROUND, CAUGHT AT PLAN TIME AND FENCED MECHANICALLY: A DOTLESS `-c` KEY MUST STAY CONFINED.** `tests/envelope_wrapper_class.rs:5197` defines `CALLEE_KNOWN_LEADING_PREFIX: &str = \"-c a=b\"` and round 7's ENTIRE callee-grammar generative property is spliced behind it. Measured at `15c40d6`: `git -c a=b status` → **exit 0**, `git -c a=b push --force origin main` → **2 `force_push_blocked`**, `git -c a=b --attr-source HEAD push --force origin main` → **2 `force_push_blocked`**, and real git RUNS `git -c a=b version` at rc 0 (it errors only when something reads the key: `git -c a=b config --get a` → `error: key does not contain a section: a`). **A rule that refused a key it cannot decompose into a section would refuse `-c a=b`, turn round 7's whole property permanently red in a file `19-23` may not edit, and reproduce `19-18`'s `{v}>` blocker one axis over.** `-c a=b` is asserted PRESENT in the confined alphabet and ABSENT from the indirection alphabet, with the failure message naming `CALLEE_KNOWN_LEADING_PREFIX` and the consequence."
    - "**CASE-INSENSITIVITY IS ANSWERED BY MEASUREMENT, IN BOTH HALVES OF THE KEY, AND IT CHANGES NO EXISTING PIN.** Against `git version 2.43.0` with the envelope's own injection as the control (`GIT_CONFIG_COUNT=1`/`KEY_0=core.hooksPath`/`VALUE_0=/ENV_WINS` printing `/ENV_WINS`): `git -c INCLUDE.PATH=<file> config --get core.hooksPath` prints **`/INCLUDE_WINS`** and `git -c INCLUDEIF.gitdir:<p>.PATH=<file> config --get core.hooksPath` prints **`/INCLUDE_WINS`** — so git folds the SECTION and the VARIABLE to lower case while the SUBSECTION is case-sensitive. The corpus therefore carries a case-varied class, and it carries the guard-side control that the existing behaviour is already right: `git -c CORE.HOOKSPATH=/dev/null status` → **2 `hook_bypass_blocked`** today, because `is_hooks_path_key` (`policy.rs:750-752`) is already `eq_ignore_ascii_case(\"core.hookspath\")`. **No existing pin changes**, and that row is asserted UNCHANGED."
    - "**THE CELL FOUND WHILE PLANNING THAT IS A DEFEAT WITHOUT A HARM, AND IT IS RECORDED HONESTLY RATHER THAN CLAIMED.** `GIT_CONFIG_NOSYSTEM` is a second key the envelope does not set which DEFEATS one it does: measured against real git, `GIT_CONFIG_SYSTEM=<file with credential.helper=evil> git config --get credential.helper` prints `evil` while adding `GIT_CONFIG_NOSYSTEM=1` makes it exit 1 having read nothing. Guard-side it is permitted: `GIT_CONFIG_NOSYSTEM=1 git push origin refs/heads/gsd-auto/alpha/w` → **exit 0** at `15c40d6`. **But its HARM is not demonstrated and must not be claimed**: `cred::write_gitconfig`'s own doc records that the generated helper-free file is pointed at by **both** `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM`, so suppressing the system read removes a deny the global pointer duplicates. It is recorded as a measured defeat with an INERT effect and the reason for the inertness stated, in the same shape the `glab --host` cell was recorded — folded into `T-19-104`'s class rather than registered as a new ID, with the provenance caveat `19-14` established. **Whether it earns an `ENVELOPE_ENV_KEYS` entry is `19-23`'s design decision and this plan asserts no post-fix verdict for it.**"
    - "**THE CORPUS MUST BE ABLE TO FAIL IN BOTH DIRECTIONS, AND THE PERMITTED HALF IS WHAT MAKES THAT TRUE.** A rule that refused every `-c` would close `T-19-103` and be unusable. All of these are measured **exit 0** at `15c40d6` and must still be exit 0 after `19-23`: `git -c user.name=\"$NAME\" commit -m x`, `git -c core.pager=cat log`, `git -c a=b status`, `git -c includepath=/tmp/evil.cfg status`, `git -c notinclude.path=/tmp/evil.cfg status`, `git --git-dir=/tmp/g status`, `git -C /tmp status`, `git --no-pager status`. **`git -c user.name=\"$NAME\" commit -m x` is this axis's `ls {git,svn}-repo`** and the comment says so: the row that tells a resolution MODEL apart from a blanket refusal of anything spelled `-c`. Beside them the verdict-preserving refusals: `git -c a=b push --force origin main` and `git -c a=b --attr-source HEAD push --force origin main` stay at `force_push_blocked`."
    - "**Every post-fix expectation carries a written DERIVATION, and a verdict that cannot be derived is measured pre-fix and RECORDED rather than asserted.** Rounds 3, 4 and 5 each shipped a two-plan handoff that was unsatisfiable as written; `19-20` corrected it by asserting every derivable row at its POST-fix verdict so no body ever needs replacing, and granting `19-21` no exception because none was needed. **This plan does the same and this round is unusually derivable**, because the refusal `19-23` must produce is raised inside `scan_leading` before any verb is classified and because all three env spellings of the paired `GIT_CONFIG_COUNT` discriminator are measured. **Two rows are RECORDED rather than asserted**: `GIT_CONFIG_NOSYSTEM=1 git push …` (whose `19-23` treatment is a design decision, above) and `git -c include.path=$F push --force origin main`, which is exit 2 **`force_push_blocked`** today — refused for its VERB, not for its carrier — and whose post-fix IDENTIFIER moves while its verdict does not, which is a clause-ordering outcome `19-23` measures."
    - "**Nothing rounds 4 through 7 settled is re-opened, and the mechanism pins say so mechanically rather than in prose.** Five pins are carried into the new file and all five must be GREEN at this plan's end and after `19-23`: **round 5's literalness bit asserted NON-VACUOUS** (`Token.literal` false for `pus?`, true for `push`); **round 6's deletion model asserted NON-DEAD** (`git >/dev/null push --force origin main` still refused, `git x2>/tmp/o push --force origin main` still PERMITTED); **`policy::is_separator(\">\")` is `false` with `SEPARATORS` byte-identical**; **round 7's fail-closed grammar asserted NON-DEAD** (`git --attr-source HEAD push --force origin main` refused at `force_push_blocked`, `git --bogus-opt status` refused at `envelope_assertion_failed`, `git --no-pager status` and `git - push --force origin main` both still exit 0); and Rule B's severed-head geometry. The composition rows carry the same load-bearing proof one axis further out: `git >/dev/null -c include.path=… status` reaches the config gap only after round 6 deletes the redirection."
    - "**The BYTE FLOORS are load-bearing, not cosmetic, and audit 7 proved it.** `POLICY_MIN_PRODUCTION_BYTES = 40_000` and `HOOKS_MIN_PRODUCTION_BYTES = 20_000` stay at their values or rise; re-measured while planning at `15c40d6` with the file's own `production_code`, `policy.rs` is raw 307,496 / stripped **68,785** and `hooks.rs` is raw 99,909 / stripped **33,460**. **`policy.rs`'s ratio is now 22.37%, so the 25% ratio assertion `19-20` deleted would be RED TODAY** — `19-21`'s own doc additions would have spent it. The floors are what this file's anti-vacuity control now rests on and `19-23` adds more prose to `policy.rs` than `19-21` did."
    - "**This plan's gate names its expected failures rather than requiring zero, and it is the handoff contract `19-23` gates against.** Baseline is **1639 passed / 0 failed / 13 ignored**, `passed + failed = 1639`, recorded by `19-21-SUMMARY.md` and independently re-measured by audit 7 over 42 result lines. After this plan `passed + failed` is strictly greater than 1639; **a red test RAN, so red→green leaves the total unchanged and every increase comes ONLY from new `#[test]` fns** — count them from `git show` and check the identity. Every failure name is one of the three documented flakes or one of the RED names this plan creates; the RED set is non-empty and every one of its names is listed in the SUMMARY; and `--test envelope_expansion_slots`, `--test envelope_command_position`, `--test envelope_literal_decision`, `--test envelope_argv_deletion` and `--test envelope_callee_grammar` are FULLY GREEN, because the widening must not disturb rounds 4, 5, 6 and 7's evidence. **All FOURTEEN `envelope_*` binaries must run** — the thirteen audit 7 observed plus the one this plan adds."
    - "**`T-19-17r` stays OUTSTANDING and this plan does not resolve it either.** `19-17-SUMMARY.md` calls it \"accepted\"; audits 5, 6 and 7 all confirmed the measurement and both pins and all three deliberately declined to make the acceptance, because accepting a risk is a human decision. There is still no Accepted-Risks-Log row and no register row. This plan records the gap and makes no acceptance."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-23`, or by the two together.** `T-19-86` remains OPEN at `high` by explicit user scoping decision and its four rows must keep exiting 0 — **plus a fifth arm audit 7 measured and the register does not name, reproduced while planning at `15c40d6`: `git config alias.p \"!git push --force origin HEAD:refs/heads/main\"` → exit 0 and `git p` → exit 0, in two separately-permitted tool calls the stateless guard cannot correlate.** Layer 3 catches the inner push TODAY, which is `AR-19-03` working and precisely what `T-19-103` removes; the interaction is RECORDED and `T-19-86` is NOT closed. `T-19-91` remains OPEN at `high` with its arms unweakened; `T-19-96` is registered open; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. The SUMMARY must say so FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-23` is the rules. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`, `19-16`, `19-18` and `19-20` met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT build a `T-19-103` reproducer on a base layer 2 already refuses. Measured at `15c40d6`: `git -c include.path=/tmp/evil.cfg push --force origin main` and every `bash -lc`, `echo hi &&`, `>/dev/null` and `--attr-source HEAD` composition of it are ALREADY exit 2 `force_push_blocked` for their VERB. Those are CONTROLS. The reproducers use the layer-2-permitted bases audit 7 used — `commit -m x`, `status`, and the in-namespace `push origin refs/heads/gsd-auto/alpha/w` — and the corpus asserts mechanically that at least one permitted base is drawn, with a failure message stating that a `push --force` base certifies nothing about config resolution."
      category: safety
    - statement: "MUST NOT put an entry whose verdict the fix CHANGES into the invariance-preserving arm of the generative property. This is `19-18`'s `{v}>` blocker and `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split, one axis over, and it is the single most likely way this seam breaks. **An indirection carrier is refused after `19-23` even on a PERMITTED base** — `git -c include.path=… status` is exit 0 today and refused after — and so is every environment carrier. Both therefore get their OWN alphabets and their OWN fail-closed properties, disjoint from the confined alphabet the invariance arm draws, and the doc says why rather than leaving it to be inferred."
      category: safety
    - statement: "MUST NOT put a bare `-c a=b`, or any other dotless-key spelling, into `CONFIG_INDIRECTION_CARRIERS`, and MUST NOT write a row whose expected mechanism is that a key the guard cannot decompose into a section is refused. `tests/envelope_wrapper_class.rs:5197` splices round 7's ENTIRE callee-grammar generative property behind `CALLEE_KNOWN_LEADING_PREFIX = \"-c a=b\"`, and real git RUNS `git -c a=b version` at rc 0. A refusal there would be permanently red in properties `19-23` may not edit. Assert the exclusion mechanically, in the shape section 14b's `{v}>` exclusion already uses, with a failure message naming `CALLEE_KNOWN_LEADING_PREFIX` and the consequence."
      category: safety
    - statement: "MUST NOT touch `UNREADABLE_CLASSES`, `DELETION_CLASSES` or `CALLEE_GRAMMAR_CLASSES`, their predicates, their degenerate-proofing assertions, or any of their floors — including `MIN_CALLEE_GRAMMAR_CLASSES`, `MIN_GIT_GLOBAL_OPTIONS`, `MIN_GIT_GLOBAL_UNKNOWN_OPTIONS`, `CALLEE_GRAMMAR_CASES`, `CALLEE_GRAMMAR_REFUSED_CASES`, `CALLEE_GRAMMAR_PERMITTED_CASES`, `CALLEE_GRAMMAR_SLOTS`, `CALLEE_GRAMMAR_CLASS_COUNTS`, `GIT_GLOBAL_OPTIONS`, `GIT_GLOBAL_UNKNOWN_OPTIONS` and `CALLEE_KNOWN_LEADING_PREFIX`. The config axis is a NEW named constant with NEW predicates and NEW floors standing beside them. Audit 7's framing is the reason: all three existing axes are axes of how a COMMAND LINE becomes an argv and which word in it is the verb; this one is about what that verb RUNS UNDER, and an axis smuggled into an existing one stops being legible as one — which is how a corpus comes to model exactly the control it certifies and nothing beside it."
      category: safety
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet entry, property or assertion anywhere under `tests/`. **`19-20` was granted exactly one named deletion — the ratio assertion — and this plan is granted NONE.** Every `MIN_*` constant keeps its current value or rises; `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` keep their values or rise. A case that stops being drawn is a case that stops being able to fail, and this file's own section header says so."
      category: scope
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured or the one DERIVED in writing from `19-23`'s mandated design. If any audit-7 row does not reproduce, report it as a finding with the measured verdict and stop — do not assert the audit's number over your own measurement, and do not quietly drop the row."
      category: honesty
    - statement: "MUST NOT claim a config-resolution fact against git without CONFIRMING IT AGAINST THE REAL `git` BINARY, using the envelope's OWN delivery mechanism as the control rather than a stand-in. Every precedence claim is measured as audit 7 measured it: `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath GIT_CONFIG_VALUE_0=/ENV_WINS git config --get core.hooksPath` must print `/ENV_WINS`, and the carrier under test must make the same command print the INCLUDED or PARAMETER value instead. A carrier that does not outrank the injection is not an indirection and is not a reproducer."
      category: honesty
    - statement: "MUST NOT inherit audit 7's END-TO-END behavioural claim by citation. The bare-remote fixture is rebuilt and all four legs re-measured independently — the plain in-namespace push refused with the remote's ref unmoved, the `-c include.path=` push completing and moving it, the `GIT_CONFIG_PARAMETERS` push completing and moving it, and the `pre-commit` point falling the same way — with the remote's SHA recorded BEFORE and AFTER each, because a SHA is what makes \"the remote moved\" a measurement. A claim in this codebase about which forms outrank the injection is already known false (`cred.rs:241-248`), so this round does not carry forward a behavioural claim it has not reproduced. A leg that does not reproduce is a finding to report, not a sentence to repeat."
      category: honesty
    - statement: "MUST NOT pin a POST-fix expectation that cannot be DERIVED from `19-23`'s mandated design, and MUST write the derivation beside every row that carries one. **Two rows are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `GIT_CONFIG_NOSYSTEM=1 git push origin refs/heads/gsd-auto/alpha/w`, whose `ENVELOPE_ENV_KEYS` treatment is `19-23`'s design decision, and `git -c include.path=$F push --force origin main`, measured exit 2 `force_push_blocked` today for its VERB and whose post-fix identifier is a clause-ordering outcome. Asserting only that they are REFUSED is also forbidden — record the measurement, assert nothing."
      category: honesty
    - statement: "MUST NOT write the rule, sketch it in `src/`, or add any production line. In particular MUST NOT add `include.path` to `is_hooks_path_key`, MUST NOT add `GIT_CONFIG_PARAMETERS` to `ENVELOPE_ENV_KEYS`, MUST NOT touch `CONFIG_VALUE_OPTS`, `GIT_GLOBAL_SELF_CONTAINED_OPTS` or the drift pin, and MUST NOT correct `cred.rs`. All of those are `19-23`'s, and the correction to `cred.rs:241-248` is a deliberate, narrow, named exception to this phase's `cred.rs` fence that belongs in the plan that can measure the rule it describes."
      category: scope
    - statement: "MUST NOT modify `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_argv_deletion.rs` or `tests/envelope_callee_grammar.rs`. Rounds 4, 5, 6 and 7's evidence stays attributable to the round that produced it, which is why `19-14` created a third file, `19-16` a fourth, `19-18` a fifth and `19-20` a sixth; this plan creates a SEVENTH for the same reason. The five carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place."
      category: scope
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS`, propose it, or write a row whose expected mechanism is that they were added; and MUST NOT weaken round 5's literalness bit, round 6's deletion model or round 7's fail-closed callee grammar. All four are pinned in the new file and all four must stay green through `19-23`."
      category: safety
    - statement: "MUST NOT claim `GIT_CONFIG_NOSYSTEM` as a live bypass. The DEFEAT is measured against real git and is real; the HARM is not, because `cred::write_gitconfig`'s generated helper-free file is pointed at by BOTH `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM`, so suppressing the system read removes a deny the global pointer duplicates. Record it as a measured defeat with an inert effect and the reason stated, in the same shape the `glab --host` cell was recorded — folded into `T-19-104`'s class with the `19-14` provenance caveat, never registered as a new threat ID and never called a bypass."
      category: honesty
    - statement: "MUST NOT claim the `glab --host` under-count as a live bypass and MUST NOT fix `FORGE_VALUE_OPTS`, `GH_API_VALUE_OPTS` or `subcommand_word_indices`. **`glab` is confirmed NOT INSTALLED on this machine**, so whether glab accepts `--host` as a separate-value global flag is not confirmed against the callee. Audit 7 explicitly declined to upgrade it; do not upgrade it without evidence, and do not expand into that component."
      category: scope
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Record the gap as OUTSTANDING. Audits 5, 6 and 7 all explicitly declined to make it on the developer's behalf; a plan making it would be forging the same signature. There is no `AR-19-13`."
      category: honesty
    - statement: "MUST NOT fix, close, narrow or re-scope `T-19-86`. Its four registered rows stay at exit 0 — `git submodule foreach git push --force origin main`, `git rebase -x \"git push --force origin main\" HEAD~3`, `git bisect run sh -c \"git push --force origin main\"`, `git -c alias.p='!git push --force origin main' p` — and so does the PERSISTED-alias arm audit 7 measured and the register does not name (`git config alias.p '!…'` then `git p`, both exit 0). **Record the interaction with `T-19-103`; do not close `T-19-86`.** It is named FIRST in the SUMMARY's 'what remains uncovered' section."
      category: scope
    - statement: "MUST NOT close, re-scope or add a remedy for `T-19-91`, and MUST NOT weaken its record. Its arms stay as audits 5, 6 and 7 verified them — `git reflog $S`, `git reflog show $S` and `git symbolic-ref $S` at exit 0 with NO second carrier, and bare `git push $REF` at exit 0 in the in-namespace configuration. Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push`, and do not extend the denylist."
      category: scope
    - statement: "MUST NOT fix `T-19-96` (a glob in a push flag) or `T-19-74` (whose core rows are frozen and re-measured permitted). MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not touch `cred.rs`, `advisory.rs`, `scan.rs`, `config.rs`, `mod.rs`, `hooks.rs`, the honesty statement, the second-carrier table or the park-coverage surface."
      category: scope
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` — and every parser in this module is hand-rolled against `std` for exactly that reason. The real-git precedence probes shell out through `std::process::Command` inside tests."
      category: scope
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised command wrote nothing must use a FRESH envelope root of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line. `gh pr create --title x` is the ready-made positive control: one line on call 1, `pr_cap_exceeded` with two on call 2."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against **1639**. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing — `19-19` first read 1364 instead of 1605 that way (D-34). **A run reporting thirteen `envelope_*` binaries after this plan is a run in which this plan's own evidence file did not execute.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes — the two `tests/driver_reattach.rs` failures and the `tests/envelope_tracer.rs` ETXTBSY stub-write race — and MUST NOT treat their absence from a run as a regression. None fired in audit 7's run and absence is not evidence they are fixed. All three are pre-existing, environmental and out of scope."
      category: scope
    - statement: "MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lint errors in `src/browser.rs` and `src/project_creator.rs`. The gate is `cargo clippy -- -D warnings`. Do not fix those lints."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or the subsections plans 19-13 … 19-21 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-22 planning and execution record. Re-measuring and re-classifying these rows is `/gsd-secure-phase 19`'s job; an audit's own tables are its provenance and a plan writing into them forges it."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`, so this plan does not clear `/gsd-secure-phase 19` and neither does `19-23`."
      category: honesty
  artifacts:
    - path: "tests/envelope_config_resolution.rs"
      provides: "the SEVENTH evidence file — audit 7's seven `T-19-103` rows and five `T-19-104` rows re-measured against the built binary on LAYER-2-PERMITTED bases and confirmed against real git with the envelope's own injection as the control, the paired discriminators that prove the mechanism present, the three-spelling environment derivation, the discrimination controls (`includepath`, `notinclude.path`) that stop a substring fix, the disclosed cost row (`include.pathx`), the case-varied rows in both halves of the key, the permitted half with `git -c user.name=\"$NAME\" commit -m x` as this axis's blanket-refusal control, the composition rows that prove rounds 6 and 7 load-bearing here, the persisted-alias arm of `T-19-86` recorded not closed, the two rows recorded rather than asserted, and the five carried-forward mechanism pins"
      contains: "T-19-103"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "`CONFIG_RESOLUTION_CLASSES` — a FOURTH named axis standing beside a byte-identical `UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES`, with degenerate-proof predicates, a `CONFIG_INDIRECTION_CARRIERS` alphabet spliced where `scan_leading` reads, a `CONFIG_ENV_CARRIERS` alphabet spliced as an assignment prefix, a separate verdict-preserving `CONFIG_CONFINED_CARRIERS` alphabet that is the only one the invariance arm draws, the mechanical exclusion of dotless keys naming `CALLEE_KNOWN_LEADING_PREFIX`, the mechanical assertion that a layer-2-PERMITTED base is drawn, and floors whose arithmetic is stated"
      contains: "CONFIG_RESOLUTION_CLASSES"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-22 record rather than an audit finding, carrying every measured verdict with its walk, the real-git precedence table with the envelope's own injection as the control, the layer-2-permitted-base finding stated first, the case-insensitivity answer measured in both halves of the key, the `GIT_CONFIG_NOSYSTEM` cell recorded as a defeat with an inert effect, the persisted-alias arm of `T-19-86` recorded not closed, the design question stated with the options `19-23` must cost, the `glab --host` cell unchanged and unfixed, the `T-19-17r` gap recorded as OUTSTANDING with the acceptance deliberately unmade, and the statement that this plan closes nothing and clears no gate"
      contains: "19-22"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "the `T-19-103`, `T-19-104`, `T-19-105`, `T-19-106` and `T-19-107` entries recording that their corpus exists and is RED, the cells found while planning folded into `T-19-103`'s and `T-19-104`'s classes rather than registered as new threats, the persisted-alias arm recorded under `T-19-86` without closing it, the `glab --host` forge cell unchanged, and the `T-19-17r` bookkeeping gap still outstanding; `T-19-86`, `T-19-91` and `T-19-96` unchanged and open"
      contains: "T-19-105"
  key_links:
    - from: "audit 7's measured bypass rows and real git's own config resolution"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary with a fresh root per row, and a real-git precedence probe using the envelope's OWN `GIT_CONFIG_COUNT` triplet as the control, BEFORE any assertion is written and BEFORE any production line moves"
      pattern: "RED"
    - from: "a fact about what the verb RUNS UNDER rather than about which word the verb is"
      to: "a FOURTH named axis the corpus can draw, standing beside three axes of the command line"
      via: "`CONFIG_RESOLUTION_CLASSES` with degenerate-proof predicates and its own floors, and carrier alphabets spliced where the guard actually reads — between the governed program and its decision words, and as an assignment prefix"
      pattern: "CONFIG_RESOLUTION_CLASSES"
    - from: "a reproducer built on `push --force origin main`, which is already refused for its verb"
      to: "a corpus that can actually fail on config resolution"
      via: "a mechanical assertion that a layer-2-PERMITTED base is drawn, with the measured `force_push_blocked` verdicts of the `--force` compositions recorded as controls"
      pattern: "permitted_base"
    - from: "a rule that refuses every `-c`"
      to: "a red test rather than a guard nobody can use"
      via: "`git -c user.name=\"$NAME\" commit -m x` and seven other ordinary invocations pinned PERMITTED — this axis's `ls {git,svn}-repo`"
      pattern: "user.name"
    - from: "a fix written as a substring match on `include`"
      to: "a red test rather than a rule that cannot say what it means"
      via: "`git -c includepath=…` and `git -c notinclude.path=…` pinned PERMITTED before and after, beside `git -c include.pathx=…` pinned REFUSED as the disclosed cost of reading the SECTION and not the variable"
      pattern: "notinclude"
    - from: "a rule that refuses a key it cannot decompose into a section"
      to: "a permanently red generative property in a file `19-23` may not edit"
      via: "`-c a=b` asserted present in the confined alphabet and absent from the indirection alphabet, with the failure message naming `CALLEE_KNOWN_LEADING_PREFIX`"
      pattern: "CALLEE_KNOWN_LEADING_PREFIX"
---

<objective>
**The corpus for the axis that is not the command line — and it must go RED before a line of either rule is written.**

Audit 7 (`19-SECURITY.md`, the authority) answered the question it was set, and answered it in the
guard's favour on the axis round 7 closed:

> **Round 7 is genuinely complete on the axis it set out to close** … the callee's option grammar is
> now modelled and fail-closed, and audit 7 says so plainly.

And then found the class still open, one stage further into what git does:

> Git turns a command line into behaviour in **three** stages, and this phase now models the first:
> (1) **which arriving word is the verb** — closed by `19-21`; (2) **what configuration is in effect
> while that verb runs** — **unmodelled**; (3) **what the process it inherits tells it** —
> **unmodelled at one key**.

And found the corpus structurally incapable of catching either:

> `T-19-105` … **all five classes are classes of git's OPTION GRAMMAR** … `grep -rn "include\.path\|
> includeIf" src/ tests/` returns **nothing at all**, and `grep -rn "GIT_CONFIG_PARAMETERS" src/
> tests/` returns **nothing at all**.

Both greps were re-run while planning at `15c40d6` and both still return nothing at all.

So: the corpus is widened here, observed failing here, and committed here. `19-23` writes the rules and
is gated on this plan's recorded RED state. **If the widened corpus does not go red against the
pre-fix tree, that is a finding — stop and report it.**

## The invariant this corpus is written against

**The configuration the guard assumes is in effect must be the configuration git will actually
resolve.** Rounds 5, 6 and 7 closed the three questions below it — a decision word must be provably
LITERAL, the words the guard classifies must be exactly the words the program receives in the same
order, and the word the guard calls the verb must be the word git calls the verb. Audit 7 verified all
three answered. This is the layer above: having built the right argv and found the right verb, **what
will that verb run under?** — and that is a question about git's config graph and about the
environment git reads as config, not about the command line at all.

## The finding that shapes the whole corpus, stated first

**A `T-19-103` reproducer must be built on a base layer 2 PERMITS.** The entire point of an
indirection is that it disarms layer 3 on a line layer 2 lets through. Measured at `15c40d6`, fresh
root per row, walk EMPTY:

```
exit=2 [force_push_blocked]  git -c include.path=/tmp/evil.cfg push --force origin main
exit=2 [force_push_blocked]  bash -lc "git -c include.path=/tmp/evil.cfg push --force origin main"
exit=2 [force_push_blocked]  echo hi && git -c include.path=/tmp/evil.cfg push --force origin main
exit=2 [force_push_blocked]  git >/dev/null -c include.path=/tmp/evil.cfg push --force origin main
exit=2 [force_push_blocked]  git --attr-source HEAD -c include.path=/tmp/evil.cfg push --force origin main
```

**Every one of those is a CONTROL.** A corpus built out of them would be green before the fix and
would certify nothing — the eighth consecutive instance of `T-19-76`'s failure mode, produced by this
plan rather than found by the next audit.

## What must be RED at this plan's end

Re-measured while planning at `15c40d6` — fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory
walked afterwards and EMPTY in every one:

```
exit=0  git -c include.path=/tmp/evil.cfg commit -m x                          <- T-19-103, layer 3 gone
exit=0  git -c include.path=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w
exit=0  git -c include.path=/tmp/evil.cfg status
exit=0  git -c includeIf.gitdir:/tmp/.path=/tmp/evil.cfg status
exit=0  git --config-env=include.path=EVILVAR status
exit=0  git --config-env include.path=EVILVAR status
exit=0  git -c INCLUDE.PATH=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w
exit=0  git -c "include.path=/tmp/evil.cfg" status                             <- the quoted spelling
exit=0  git -c include.pathx=/tmp/evil.cfg status                              <- the DISCLOSED COST
exit=0  GIT_CONFIG_PARAMETERS="'core.hooksPath=/tmp/nohooks'" git commit -m x  <- T-19-104
exit=0  GIT_CONFIG_PARAMETERS="'core.hooksPath=/tmp/nohooks'" git push origin refs/heads/gsd-auto/alpha/w
exit=0  export GIT_CONFIG_PARAMETERS="'core.hooksPath=/tmp/nohooks'"; git push origin refs/heads/gsd-auto/alpha/w
exit=0  env GIT_CONFIG_PARAMETERS="'core.hooksPath=/tmp/nohooks'" git push origin refs/heads/gsd-auto/alpha/w
exit=0  GIT_CONFIG_PARAMETERS="'include.path=/tmp/evil.cfg'" git push origin refs/heads/gsd-auto/alpha/w
exit=0  echo GIT_CONFIG_PARAMETERS                                             <- the DISCLOSED COST
```

All are asserted at their POST-fix verdicts, so they are red now and green after: the ten
`include`/`--config-env` rows and `include.pathx` at exit 2 `envelope_assertion_failed`; the five
`GIT_CONFIG_PARAMETERS` rows and `echo GIT_CONFIG_PARAMETERS` at exit 2 `hook_bypass_blocked`.

**One further row is RED without being exit 0 today** — the ordering pin, whose IDENTIFIER moves while
its verdict does not:

```
exit=2 [hook_bypass_blocked]  git -c include.path=/tmp/evil.cfg -c core.hooksPath=/dev/null push --force origin main
                              -> asserted at envelope_assertion_failed, because the scan is LEFT TO
                                 RIGHT and 19-23's clause is raised at the first assignment it cannot
                                 bound. Its twin in the other order stays hook_bypass_blocked.
```

## The paired discriminators, which prove this is a gap in REACH and not in MECHANISM

Measured at `15c40d6`, all exit 2, all walks EMPTY, all asserted UNCHANGED:

```
hook_bypass_blocked  git -c core.hooksPath=/dev/null commit -m x
hook_bypass_blocked  git --config-env=core.hooksPath=EVILVAR status
hook_bypass_blocked  git --config-env core.hooksPath=EVILVAR status
hook_bypass_blocked  git -c CORE.HOOKSPATH=/dev/null status        <- case, already right
hook_bypass_blocked  GIT_CONFIG_COUNT=0 git push origin refs/heads/gsd-auto/alpha/w
hook_bypass_blocked  export GIT_CONFIG_COUNT=0; git push origin refs/heads/gsd-auto/alpha/w
hook_bypass_blocked  env GIT_CONFIG_COUNT=0 git push origin refs/heads/gsd-auto/alpha/w
hook_bypass_blocked  GIT_CONFIG_COUNT=0 git status                 <- permitted base
hook_bypass_blocked  export GIT_CONFIG_COUNT=0; git status
hook_bypass_blocked  env GIT_CONFIG_COUNT=0 git status
hook_bypass_blocked  echo GIT_CONFIG_COUNT                         <- the disclosed bare-word cost
```

**These are what make every `T-19-104` post-fix verdict DERIVABLE rather than guessed**: all three
environment spellings already refuse for a key that IS in the list, on both a refused and a permitted
base, so adding one entry derives all five rows and their cost twin.

## The cells found while PLANNING round 8

Same provenance caveat `19-14`, `19-16`, `19-18` and `19-20` established: found while planning, not by
an audit. All measured at `15c40d6`, all inside `T-19-103`'s or `T-19-104`'s class rather than new
threats.

| Cell | Line | Verdict | What it adds |
|---|---|---|---|
| **the quoted carrier** | `git -c "include.path=/tmp/evil.cfg" status` | **exit 0** | the quoting is removed by the tokenizer before the scan reads the key, so quoting is not an evasion and not a defence |
| **the near miss, no section** | `git -c includepath=/tmp/evil.cfg status` | **exit 0**, stays 0 | this round's `--signed no`: a rule written as a substring match on `include` turns it red |
| **the near miss, wrong section** | `git -c notinclude.path=/tmp/evil.cfg status` | **exit 0**, stays 0 | the same control from the other side — the SECTION is what decides, not the letters |
| **the disclosed cost** | `git -c include.pathx=/tmp/evil.cfg status` | **exit 0** → refused | the mandated rule reads the SECTION and deliberately does not read the variable, so a key git ignores is refused. Safe direction, stated here rather than found by audit 8 |
| **ordering, indirection first** | `git -c include.path=… -c core.hooksPath=/dev/null push --force origin main` | exit 2 `hook_bypass_blocked` | today the hooks key wins because the include carrier is invisible; after the fix the SCAN ORDER decides, and `19-23` must pin which |
| **ordering, hooks key first** | `git -c core.hooksPath=/dev/null -c include.path=… push --force origin main` | exit 2 `hook_bypass_blocked` | unchanged either way — the left-to-right scan reaches the hooks key first |
| **the `~` collision** | `git -c includeIf.gitdir:~/p/.path=/tmp/evil.cfg status` | exit 2 `envelope_assertion_failed` | **already refused TODAY**, by the existing rewriting-character clause on the key half, not by this round's rule. A CONTROL, and it must be labelled one |
| **a dotless key** | `git -c a=b status` / `git -c a=b push --force origin main` | exit 0 / exit 2 `force_push_blocked` | **the seam that would have blocked this round** — `CALLEE_KNOWN_LEADING_PREFIX` splices round 7's whole property behind `-c a=b`, and real git RUNS it |
| **the environment defeat with no harm** | `GIT_CONFIG_NOSYSTEM=1 git push origin refs/heads/gsd-auto/alpha/w` | **exit 0** | a second key the envelope does not set that DEFEATS one it does (measured against real git); harm INERT because `write_gitconfig`'s file is pointed at by both `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM`. RECORDED, not asserted |
| **the persisted alias** | `git config alias.p "!git push --force origin HEAD:refs/heads/main"` then `git p` | **exit 0** and **exit 0** | the `T-19-86` arm the register does not name, in two tool calls the stateless guard cannot correlate. Layer 3 catches it today — which is what `T-19-103` removes. RECORDED, `T-19-86` NOT closed |

## The real-git precedence table, measured with the envelope's own injection as the control

```
control:  GIT_CONFIG_COUNT=1 KEY_0=core.hooksPath VALUE_0=/ENV_WINS  git config --get core.hooksPath
          -> /ENV_WINS
+ -c include.path=<file>                                            -> /INCLUDE_WINS
+ -c INCLUDE.PATH=<file>                                            -> /INCLUDE_WINS
+ -c includeIf.gitdir:<p>.path=<file>                               -> /INCLUDE_WINS
+ -c INCLUDEIF.gitdir:<p>.PATH=<file>                               -> /INCLUDE_WINS
+ --config-env=include.path=EVILVAR                                 -> /INCLUDE_WINS
+ GIT_CONFIG_PARAMETERS="'core.hooksPath=/PARAM_WINS'"              -> /PARAM_WINS
+ GIT_CONFIG_PARAMETERS="'include.path=<file>'"                     -> /PARAM_INCLUDE_WINS
```

**The case answer, measured in both halves of the key.** git folds the SECTION and the VARIABLE to
lower case and leaves the SUBSECTION case-sensitive: `INCLUDE.PATH` resolves, and so does
`INCLUDEIF.gitdir:<p>.PATH`. **No existing pin changes** — `is_hooks_path_key` is already
`eq_ignore_ascii_case("core.hookspath")` and `git -c CORE.HOOKSPATH=/dev/null status` is measured at
`hook_bypass_blocked` today.

## The design question `19-23` must answer, stated here so the corpus can fail on the wrong answer

**The guard must not ask "does this assignment spell `core.hooksPath`" but "can I establish what
`core.hooksPath` will be."** This corpus is written so that a rule which reads the SECTION of a `-c`
key — and deliberately reads neither the subsection nor the variable — is green, and a rule which
substring-matches `include`, or refuses every `-c`, or refuses a key it cannot decompose into a
section, is red. The costing is `19-23`'s; the discrimination is this plan's.

## What this plan is NOT

It closes nothing. `T-19-103`, `T-19-104`, `T-19-105`, `T-19-106` and `T-19-107` stay open at this
plan's end; `T-19-105` is closed only when `19-23`'s rules are certified by the corpus written here,
because a corpus is evidence about a control and there are no controls yet.

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-23`, or by the two together.** `T-19-86`
and `T-19-91` remain open at `high`. The SUMMARY must say so first.

Purpose: make the corpus capable of failing on the axis that is not the command line — what the verb
runs under — and prove it does.
Output: a seventh evidence file, a fourth named class axis with its own floors, two committed RED
states, one appended record, and an exact handoff number for `19-23` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-21-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@tests/envelope_wrapper_class.rs
@tests/envelope_callee_grammar.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE against the guard AND against real git, then RED — the seventh evidence file</name>
  <files>tests/envelope_config_resolution.rs</files>
  <precondition>
    Work in the main checkout at `15c40d6` or later with a clean tree — ISOLATION=none is forced for
    this phase, so do NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`,
    which is what every guard measurement is driven against. A real `git` binary is on `PATH`
    (`git version 2.43.0` on the machine this plan was written on) and a writable temp directory is
    available; the precedence probes and the push fixture both need them. `glab` is NOT installed and
    must not be assumed. The three documented flakes are pre-existing.
  </precondition>
  <read_first>
    `tests/envelope_callee_grammar.rs:1-140` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, and the
    one-`TempDir`-per-row discipline — and its mechanism-pin section for the shape of an assertion made
    over a `policy::` function directly rather than over an exit code. `19-SECURITY.md`'s sections
    "Threats found by audit 7", "The known-open set, as audit 7 verified it", "The four execution-time
    judgements, assessed independently" and "Audit 7 — what the round-7 control can and cannot fail
    on". `src/envelope/policy.rs:325-471` (`scan_leading` — its one-scan-two-callers doc, the `-` and
    `--` breaks, **the `if let Some(assignment)` block at `:397-466` that carries the rewriting-key
    refusal and then `is_hooks_path_key`, which is where `19-23`'s new clause goes**), `:486-501`
    (`push_needs_resolved_dests`, the second caller), `:614-703` (`leading_git_option`'s seven arms —
    note arm (a) at `:664` returns the NEXT TOKEN as the assignment for a bare `-c`/`--config-env`, and
    arm (b) at `:669` returns the attached one, so BOTH carriers reach the same key check), `:738-752`
    (`config_key_of` and `is_hooks_path_key` — **already `eq_ignore_ascii_case`**), `:2785-2874`
    (`ENVELOPE_ENV_KEYS`, its prefix-matching rule and `envelope_env_key`), `:2901-2928`
    (`tampers_with_envelope_env` and its disclosed bare-word false positive),
    `src/envelope/cred.rs:197-251` (`config_env` and `hooks_path_env` — **the injection every
    precedence probe must use as its control, and the doc at `:241-248` whose claim `19-23` corrects;
    do NOT edit it here**), `:253-271` (`write_gitconfig`'s doc — the double pointer that makes
    `GIT_CONFIG_NOSYSTEM` inert).
  </read_first>
  <action>
**Measure before you assert, and measure the CALLEE as well as the guard. Nothing in this file is
written until it has been driven.**

Build, then drive every guard row exactly as audit 7 did:

```
printf '{"tool_name":"Bash","tool_input":{"command":"<CMD>"}}' \
  | GSD_MM_ENVELOPE_ROOT=$(mktemp -d) ./target/debug/gsd-meta-manager envelope guard alpha
```

one fresh root per row, a walk over the whole root afterwards. Build the JSON with
`python3 -c json.dumps` so the nested quoting of the `GIT_CONFIG_PARAMETERS` rows survives as the
bytes it is.

**Then probe REAL git for every precedence claim, with the envelope's OWN delivery mechanism as the
control.** This is the step that separates a resolution claim from a guess, and it is what audit 7 did.
In a throwaway directory with a planted include file carrying `[core] hooksPath = /INCLUDE_WINS`:

  - the control is `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath
    GIT_CONFIG_VALUE_0=/ENV_WINS git config --get core.hooksPath`, which must print `/ENV_WINS`;
  - each carrier under test is added to that same command line or environment, and the carrier is an
    INDIRECTION only if the same command then prints the INCLUDED or PARAMETER value instead.

The full table is in the objective and is to be RE-MEASURED rather than copied, including both case
rows (`INCLUDE.PATH` and `INCLUDEIF.gitdir:<p>.PATH`), which are what answer the case question in both
halves of the key. Record it in full.

**Also measure `GIT_CONFIG_NOSYSTEM` against real git and record it honestly.** With a system file
carrying `credential.helper = evil`: `GIT_CONFIG_SYSTEM=<file> git config --get credential.helper`
prints `evil`, and adding `GIT_CONFIG_NOSYSTEM=1` makes the same command exit 1 having read nothing —
a measured DEFEAT of a key the envelope sets. **Then measure why it is INERT**: `cred::write_gitconfig`
points BOTH `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` at the same helper-free file, so suppressing
the system read removes a deny the global pointer duplicates. Record the defeat, record the inertness
and its reason, and **do not call it a bypass**.

**Then CONFIRM THE CLASS END TO END AGAINST REAL GIT WITH A BARE REMOTE, independently rather than by
citing audit 7.** A claim in this codebase about which forms outrank the injection is already known
false, so this round does not inherit a behavioural claim it has not reproduced. In a throwaway fixture
on `refs/heads/gsd-auto/alpha/w` with a **local bare upstream** and a `pre-push` hook installed exactly
as the envelope installs one (`core.hooksPath` delivered through the `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n`
triplet `cred::hooks_path_env` emits), measure and record all four legs:

  - the plain in-namespace push is **REFUSED by the hook** and the bare remote's ref **does not move** —
    record the remote's SHA before and after;
  - `git -c include.path=<evil> push origin refs/heads/gsd-auto/alpha/w` — a line the guard permits at
    exit 0 — **completes and MOVES the remote's ref**. Record both SHAs;
  - the same with the environment carrier, `GIT_CONFIG_PARAMETERS="'core.hooksPath=…'"` before the
    push — **completes and moves the ref**;
  - the `pre-commit` enforcement point falls the same way: the control refuses, the carrier commits.

**If any leg does not reproduce, that is a finding — record the measured behaviour and report it rather
than repeating audit 7's sentence.** The SHAs are what make "the remote moved" a measurement rather
than a claim, and they go in the record.

Any audit-7 row that does not reproduce at the recorded verdict: STOP, record the measured verdict, and
report it as a finding rather than asserting the audit's number over your own measurement.

Then create `tests/envelope_config_resolution.rs`. Its header states what the file is: audit 7's
`T-19-103` and `T-19-104` rows plus the cells found while planning round 8, measured against the built
binary AND against real git BEFORE either rule was written so the corpus cannot have been shaped to
agree with the code, and written as a SEVENTH file rather than appended to
`tests/envelope_callee_grammar.rs` so round-7 and round-8 evidence stay attributable to the round that
produced them. It states the invariant the file is about — **the configuration the guard assumes is in
effect must be the configuration git will actually resolve** — that rounds 5, 6 and 7 closed the three
questions below it and audit 7 verified all three answered, and that this file is about the layer
above. It states that the file is RED at this plan's end by design and that `19-23` closes it.

**It states the layer-2-permitted-base finding in the header, prominently**, with the five measured
`force_push_blocked` control rows beside it, because that is the single design constraint a later
reader most needs in order to understand why the reproducers look the way they do.

Carry the harness forward from `tests/envelope_callee_grammar.rs` unchanged in shape, including the
walk. Print the walk for at least one refused row and one counted row so the SUMMARY records listings
that were measured.

**Section 1 — the `T-19-103` rows, asserting exit 2 `envelope_assertion_failed`, each with its
derivation.** A test whose doc records the mechanism in git's own terms: `scan_leading` decides ONE
question about a `-c` assignment — is the KEY half `core.hooksPath` (D-09) — and git's config
resolution has an INDIRECTION that check does not model, because a key in the `include` or `includeIf`
section names a file whose contents are spliced in **at the precedence of the directive that named
them**, i.e. at command-line precedence, without the string `core.hooksPath` appearing anywhere. The
ten rows in the objective's RED table that carry an include directive, each asserted at exit 2
`envelope_assertion_failed`. **Write the derivation for each beside the row**: `19-23`'s mandated
clause is raised inside `scan_leading` before any verb is classified, so the identifier is the
unresolvable-command one and not the one the verb would have earned. The comment records the
three-leg argument audit 7 measured — layer 1's `Bash(git push:*)` prefix rule does not match a line
beginning `git -c`; layer 2 is defeated on the same line; layer 3 is disarmed, measured, with
`GIT_ASKPASS`, `GIT_CONFIG_GLOBAL/SYSTEM` and `GIT_SSH_COMMAND` untouched so the push authenticates —
and records that `pre-push` is the ONLY carrier of the worktree credential scan (`hooks.rs:342`,
SAFE-05, `T-19-15`/`T-19-16`/`T-19-18`) and the second carrier `AR-19-03` rests on, so disarming it
widens `T-19-86`, `T-19-91` and `T-19-96` at once. It records that audit 7 confirmed the class end to
end against real git: an in-namespace push the `pre-push` hook REFUSES completed and moved a bare
remote's ref, and a persisted alias carrying the same rewrote the bare remote's `main`.

**Section 2 — the `T-19-104` rows, asserting exit 2 `hook_bypass_blocked`, derived from the paired
discriminator rather than guessed.** The five `GIT_CONFIG_PARAMETERS` rows of the objective's RED
table, plus `echo GIT_CONFIG_PARAMETERS` as the disclosed bare-word cost, all at exit 2
`hook_bypass_blocked`. The derivation is beside them: all three environment spellings of
`GIT_CONFIG_COUNT=0` — the bare prefix, `export …;` and `env …` — are measured at exit 2
`hook_bypass_blocked` on BOTH a refused and a permitted base, and `echo GIT_CONFIG_COUNT` is measured
refused too, so one `ENVELOPE_ENV_KEYS` entry derives every row. The doc records what makes this a gap
in the LIST rather than the mechanism, and why the existing drift pin **structurally cannot see it**:
the pin is sourced from `cred::EnvelopeEnv::with_run_id(build_env_in(…))`, i.e. from the keys the
envelope SETS or REMOVES, and this is a key the envelope neither sets nor removes but which DEFEATS one
it sets. It records the third-omission history — `GIT_SSH_COMMAND`, `SSH_AUTH_SOCK`/`SSH_AGENT_PID`
(`T-19-82`), `GSD_MM_RUN_ID` (`T-19-90`) — and that git EXPORTS this variable, so one prefix disarms
every git subprocess of the command, which is how it composes with `T-19-86`.

**Section 3 — the paired discriminators, asserted UNCHANGED as controls.** The eleven rows of the
objective's discriminator table, each at its measured exit 2 and reason identifier, with the comment
stating that they are what make this a gap in reach rather than absence. `git -c
CORE.HOOKSPATH=/dev/null status` carries the case note: `is_hooks_path_key` is ALREADY
`eq_ignore_ascii_case`, this row is green today, and **no existing pin changes**.

**Section 4 — the DISCRIMINATION controls, which stop the fix from being a substring match.**
`git -c includepath=/tmp/evil.cfg status` and `git -c notinclude.path=/tmp/evil.cfg status`, both
measured exit 0 and both asserted **exit 0 after**. The comment says plainly that these are this
round's `--signed no`: a rule written as `key.contains("include")` turns both red, and only a rule that
compares the SECTION keeps them green. Beside them, in the same test, the row whose verdict the rule
DOES move: `git -c include.pathx=/tmp/evil.cfg status`, exit 0 today and asserted **exit 2
`envelope_assertion_failed`** after, with the comment recording that the mandated rule reads the
section and deliberately does not read the variable — so a key git ignores is refused, in the safe
direction, and the cost is stated in the corpus rather than found by audit 8.

**Section 5 — the PERMITTED half, which is what makes this a resolution MODEL rather than a blanket
refusal of anything spelled `-c`.** All measured at exit 0 today and asserted at exit 0 after:
`git -c user.name="$NAME" commit -m x`, `git -c core.pager=cat log`, `git -c a=b status`,
`git --git-dir=/tmp/g status`, `git -C /tmp status`, `git --no-pager status`. **`git -c
user.name="$NAME" commit -m x` is this axis's `ls {git,svn}-repo`** and the comment says so: a corpus
without the permitted half cannot fail on an implementation that refuses every `-c`, which is the shape
this fix is one wrong step away from (AR-19-11). Beside them the verdict-preserving refusals
`git -c a=b push --force origin main` and `git -c a=b --attr-source HEAD push --force origin main`,
both at exit 2 `force_push_blocked` before and after. Add the positive walk control here:
`gh pr create --title x` at exit 0 with exactly ONE ledger line in a walked fresh root, and the cap
firing with two on call 2, which is what makes an empty walk elsewhere mean something.

**Section 6 — the DOTLESS-KEY pin, which is the seam this plan exists to fence.** A test of its own,
named for the fact rather than the row: a `-c` key with no `.` at all names no config section, real git
RUNS the command (`git -c a=b version` → `git version 2.43.0`, rc 0; it errors only when something
READS the key — `git -c a=b config --get a` → `error: key does not contain a section: a`), and
`tests/envelope_wrapper_class.rs:5197` splices round 7's ENTIRE callee-grammar generative property
behind `CALLEE_KNOWN_LEADING_PREFIX = "-c a=b"`. The three rows above are asserted, and the doc states
the consequence in one sentence: **a rule that refused a key it cannot decompose into a section would
turn round 7's whole property permanently red in a file `19-23` may not edit**, which is `19-18`'s
`{v}>` blocker one axis over.

**Section 7 — the CONTROLS that are already refused today, LABELLED as such.** `git -c
includeIf.gitdir:~/p/.path=/tmp/evil.cfg status` is exit 2 `envelope_assertion_failed` TODAY, by the
existing rewriting-character clause on the key half and NOT by this round's rule; it is asserted at its
present verdict with the comment naming which clause produces it and stating that the identifier is
unchanged after the fix while the CLAUSE may not be.

**The two ORDERING rows go here too, and they are the pin on `19-23`'s mandated scan order rather than
controls.** Both are exit 2 `hook_bypass_blocked` today, and they are asserted at DIFFERENT post-fix
verdicts because the scan is left to right and `19-23`'s clause is raised inside the same loop:

  - `git -c include.path=/tmp/evil.cfg -c core.hooksPath=/dev/null push --force origin main` — the
    scan reaches the INDIRECTION first, so it is asserted at **exit 2 `envelope_assertion_failed`**,
    which is RED today. **Its derivation, written beside it**: `19-23`'s clause is raised at the first
    assignment the loop cannot bound, and no assignment after it is read, so a line carrying both earns
    the unresolvable identifier and not the hooks-path one — which is D-24's requirement that a refusal
    name the mechanism that produced it;
  - `git -c core.hooksPath=/dev/null -c include.path=/tmp/evil.cfg push --force origin main` — the scan
    reaches the HOOKS KEY first, so it is asserted **UNCHANGED at `hook_bypass_blocked`**.

Together they are the mechanical proof that the two clauses are ordered by the scan and not by a
priority the reader has to infer, and a `19-23` that raised its refusal in a second pass over the
leading tokens would turn one of them red.

**Section 8 — the composition rows, which prove rounds 6 and 7 load-bearing for this round rather than
superseded by it.** Built on PERMITTED bases, because that is this axis's whole constraint:
`git >/dev/null -c include.path=/tmp/evil.cfg status`,
`git --attr-source HEAD -c include.path=/tmp/evil.cfg status`,
`bash -lc "git -c include.path=/tmp/evil.cfg status"` and
`echo hi && git -c include.path=/tmp/evil.cfg status` — each measured first, and each asserted at the
verdict the measurement plus the derivation supports. The redirection is deleted by round 6's model and
`--attr-source` consumes its value under round 7's rule, and only then is there a `-c include.path=`
for this round's rule to read; the comments say so, and say that these are the rows that turn red if
either earlier model is ever removed, whatever this round's rules do.

**Section 9 — the two rows RECORDED rather than asserted.** Each is measured and the measurement is
**RECORDED IN A COMMENT AND A PRINT — never written as an assertion** — because this plan measures
pre-fix and cannot catch a wrong post-fix expectation, and a row pinned at a verdict the rules cannot
produce lands RED with `19-23` forbidden to edit it. That is exactly how `19-16`, `19-18` and `19-20`
handled their undeliverable rows. `19-23` measures each against the rules and appends the pin.

  - `GIT_CONFIG_NOSYSTEM=1 git push origin refs/heads/gsd-auto/alpha/w` — exit 0 today; whether it
    earns an `ENVELOPE_ENV_KEYS` entry is `19-23`'s design decision, because the defeat is real and the
    harm is inert;
  - `git -c include.path=$F push --force origin main` — exit 2 **`force_push_blocked`** today, refused
    for its VERB rather than for its carrier, so its verdict does not move while its IDENTIFIER may;
    which of the two it lands on is a clause-ordering outcome `19-23` measures.

**Section 10 — the `T-19-86` persisted-alias arm, RECORDED and explicitly NOT closing anything.**
`git config alias.p "!git push --force origin HEAD:refs/heads/main"` at exit 0 and `git p` at exit 0,
asserted at those verdicts as `T-19-86` rows that must keep exiting 0 — with the doc recording that
they are two separately-permitted tool calls the stateless guard cannot correlate, that layer 3 catches
the inner push TODAY (which is `AR-19-03` working), and that **this is precisely what `T-19-103`
removes**. The doc states in terms that `T-19-86` is NOT closed, narrowed or re-scoped by recording it.

**Section 11 — the five carried-forward MECHANISM pins**, re-asserted here over the same public
functions rather than moved or edited in place, because this round touches the `-c` key decision for
the first time since round 5:

  - **round 5's literalness bit, asserted NON-VACUOUS** — over the tokens of
    `git pus? --force origin main` and `git -c include.path=/tmp/evil.cfg status`, `Token.literal` is
    `false` for `pus?` and `true` for `-c`, `include.path=/tmp/evil.cfg` and `status`. **The bit is
    RIGHT about every word of the bypass line and this pin says so**, which is what stops `19-23` from
    closing `T-19-103` by making the bit wrong;
  - **round 6's deletion model, asserted NON-DEAD** — `git >/dev/null push --force origin main` still
    refused, and the over-deletion control `git x2>/tmp/o push --force origin main` still PERMITTED;
  - **`policy::is_separator(">")` is `false`** and the `SEPARATORS` line is unchanged — the mechanical
    proof that the route that constant's own doc forbids was not taken.
    `separators_are_named_in_one_list_that_the_predicate_reads` in `policy.rs` asserts the same fact and
    must stay GREEN and UNMODIFIED;
  - **round 7's fail-closed callee grammar, asserted NON-DEAD** —
    `git --attr-source HEAD push --force origin main` refused at `force_push_blocked`,
    `git --bogus-opt status` refused at `envelope_assertion_failed`, and both
    `git --no-pager status` and `git - push --force origin main` still at exit 0;
  - **Rule B** — `policy::split_segments_with_heads` still reports `head_is_command_position == false`
    for `C=GIT_CONFIG; env -u ${C}_COUNT git fetch origin` and the two other severed spellings, and
    still reports `true` for `{ git status; }` so the pin cannot be satisfied by marking every segment
    severed.

Commit this as its own commit, RED. State the per-binary counts in the SUMMARY.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_config_resolution --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo test --test envelope_callee_grammar &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_argv_deletion &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_literal_decision &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_expansion_slots &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_command_position &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "ZERO-SRC-HUNKS-RED-COMMITTED"</automated>
  </verify>
  <done>Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before it was written as an assertion, and every precedence claim was confirmed against the REAL `git` binary using the envelope's own `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` injection as the control — with the full table recorded, including both case rows that answer the case question in the SECTION and the VARIABLE halves, and including the `GIT_CONFIG_NOSYSTEM` defeat with its INERT effect and the `write_gitconfig` double-pointer reason; `tests/envelope_config_resolution.rs` exists as a SEVENTH file whose header states the layer-2-PERMITTED-base finding prominently with the five measured `force_push_blocked` control rows beside it, and carries its eleven sections — the ten `T-19-103` rows at exit 2 `envelope_assertion_failed` with a derivation each; the five `T-19-104` rows plus `echo GIT_CONFIG_PARAMETERS` at exit 2 `hook_bypass_blocked` derived from the three-spelling paired discriminator; the eleven discriminators asserted UNCHANGED with the case row's note that no existing pin changes; the two discrimination controls (`includepath`, `notinclude.path`) pinned at exit 0 before and after beside the disclosed cost row `include.pathx` pinned refused; the permitted half with `git -c user.name="$NAME" commit -m x` named as this axis's blanket-refusal control and `gh pr create --title x` as the walk's non-blindness control; the DOTLESS-KEY pin naming `CALLEE_KNOWN_LEADING_PREFIX` and its consequence; the already-refused rows LABELLED with the clause that produces them; the four composition rows on PERMITTED bases; the `T-19-86` persisted-alias arm recorded at exit 0 with `T-19-86` explicitly not closed; and the five mechanism pins; the TWO undeliverable rows are RECORDED IN COMMENTS AND PRINTS and are **NOT written as assertions**, verified by reading the file for an `assert` on either; `--test envelope_callee_grammar`, `--test envelope_argv_deletion`, `--test envelope_literal_decision`, `--test envelope_expansion_slots` and `--test envelope_command_position` are fully GREEN and unmodified; the new file is RED with every failing name recorded verbatim; `git show --stat` on this commit shows ZERO `src/` hunks; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: the FOURTH AXIS — `CONFIG_RESOLUTION_CLASSES`, its three alphabets and its two mechanical fences</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:700-914`
    (`POLICY_SOURCE`, `HOOKS_SOURCE`, `POLICY_ANCHOR`, `HOOKS_ANCHOR`, `production_code`,
    `POLICY_MIN_PRODUCTION_BYTES`, `HOOKS_MIN_PRODUCTION_BYTES` and
    `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` — **read to leave
    UNTOUCHED; the floors are `19-20`'s and this plan is granted no deletion**), `:3000-3230`
    (section 13: `UNREADABLE_CLASSES`, its seven predicates and its floors — **read to mirror the
    SHAPE, not to edit**), `:3860-4000` (section 14: `DELETION_CLASSES`), `:4042-4360` (the deletion
    generative property, the floors and **the `{v}>` exclusion assertion — the precedent for both of
    this task's mechanical fences**), `:4900-5230` (section 15: `CALLEE_GRAMMAR_CLASSES`,
    `GIT_GLOBAL_OPTIONS`, `GIT_GLOBAL_UNKNOWN_OPTIONS`, the invariance/fail-closed split and
    **`CALLEE_KNOWN_LEADING_PREFIX` at `:5197`, which this task's dotless fence protects**),
    `:2545-2604` (`SEVERED_PREFIXES` / `SEVERED_BRACE_PREFIXES` — the precedent for giving a new class
    its OWN alphabet rather than widening one whose property it cannot satisfy).
  </read_first>
  <action>
**No existing axis is touched.** `UNREADABLE_CLASSES`, `DELETION_CLASSES` and
`CALLEE_GRAMMAR_CLASSES`, their predicates, their degenerate-proofing assertions and all of their
floors stay byte-identical, and so do `POLICY_MIN_PRODUCTION_BYTES`, `HOOKS_MIN_PRODUCTION_BYTES` and
`wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`. **This plan is granted no
deletion of any kind** — `19-20`'s ratio deletion was a named one-off and there is no equivalent here.
This task adds a FOURTH axis beside the three, in the shape sections 13, 14 and 15 already establish,
and the file's own section header records why it is a fourth axis rather than more entries in an
existing one: audit 7 found git turns a command line into behaviour in three stages and this phase now
models one, so the gap moved off the command line entirely — from *how a word is written*, to *which
words arrive*, to *which arriving word is the verb*, to **what that verb runs under**.

**1. `CONFIG_RESOLUTION_CLASSES` — five classes, each with a degenerate-proof predicate.** Named once
so the per-alphabet floor, the per-class floor and the counted floor all count the same thing. Each
predicate is evaluated on a REPRESENTATIVE SPLICED COMMAND rather than on the bare entry, the way
sections 14 and 15's are, because these entries are splice fragments:

  1. **a command-line carrier of a config INDIRECTION** — a `-c` or `--config-env` assignment whose
     key names a section that pulls further configuration in at the precedence of the directive that
     named it (`-c include.path=<file>`, `-c includeIf.gitdir:<p>.path=<file>`);
  2. **a command-line carrier of a CONFINED assignment** — a `-c` assignment whose effect is bounded to
     the key it names (`-c user.name=x`, `-c core.pager=cat`, `-c a=b`). **Class 1 cannot satisfy this
     and class 2 cannot satisfy class 1**, which is the split that makes the pair non-degenerate and it
     is the whole of the resolution question;
  3. **an ENVIRONMENT carrier of configuration** — a word that sets configuration git reads with no
     `-c` on the line at all (`GIT_CONFIG_PARAMETERS=…`, in its bare-prefix, `export …;` and `env …`
     spellings). **The command line is not involved**, which is why this class cannot live in any of
     the three existing axes;
  4. **a CASE-VARIED spelling of class 1 or 3** — `INCLUDE.PATH`, `INCLUDEIF.gitdir:<p>.PATH`, because
     git folds the SECTION and the VARIABLE to lower case while leaving the SUBSECTION case-sensitive,
     measured in Task 1 in both halves of the key;
  5. **an OPTION-carrier delivery of class 1** — `--config-env=include.path=<VAR>` and its
     separate-word form `--config-env include.path=<VAR>`, the second carrier `scan_leading` reads and
     the one whose value is an environment variable name rather than a value.

**2. The degenerate-proofing block, asserted rather than described**, in the shape
`the_corpus_can_draw_every_one_of_the_seven_unreadable_classes` already uses:
`git -c include.path=/tmp/e status` satisfies class 1 and NOT class 2; `git -c user.name=x status`
satisfies class 2 and NOT class 1; `GIT_CONFIG_PARAMETERS="'core.hooksPath=/x'" git status` satisfies
class 3 and neither 1 nor 2; `git -c INCLUDE.PATH=/tmp/e status` satisfies class 4;
`git --config-env=include.path=V status` satisfies class 5; and **`git commit -m "include.path=/tmp/e"`
satisfies NONE**, which is the quoting-and-position control — a config-shaped word that is neither a
carrier nor in the scan's view must not satisfy any class, or the floors become satisfiable by a row
the rules must never touch.

**3. Three alphabets, and the split between them is the seam fence rather than a tidy-up.**

  - **`CONFIG_CONFINED_CARRIERS`** — the ONLY alphabet the invariance-preserving arm draws. Entries are
    spliced **between the governed program and its decision words**, because that is the position
    `scan_leading` reads: `-c user.name=x`, `-c core.pager=cat`, **`-c a=b`**, `-c notinclude.path=/tmp/e`,
    `-c includepath=/tmp/e`. Every entry is verdict-PRESERVING — measured permitted on a permitted base
    and measured at the base's own identifier on a refused base, before and after;
  - **`CONFIG_INDIRECTION_CARRIERS`** — a SEPARATE alphabet with its own FAIL-CLOSED property, spliced
    at the same position: `-c include.path=/tmp/evil.cfg`, `-c includeIf.gitdir:/tmp/.path=/tmp/evil.cfg`,
    `-c INCLUDE.PATH=/tmp/evil.cfg`, `--config-env=include.path=EVILVAR`,
    `--config-env include.path=EVILVAR`;
  - **`CONFIG_ENV_CARRIERS`** — a SEPARATE alphabet with the same fail-closed property, spliced as an
    ASSIGNMENT PREFIX rather than between the program and its decision words, because that is where
    `resolve_program`'s step-1 check reads: the bare `GIT_CONFIG_PARAMETERS="'core.hooksPath=/tmp/nohooks'"`
    prefix, the `env GIT_CONFIG_PARAMETERS=… ` prefix, and the `export GIT_CONFIG_PARAMETERS=…; `
    two-segment spelling.

**Why the split, written in the doc rather than left to be inferred.** The property's invariance arm
asserts the wrapped verdict EQUALS the unwrapped one. **An indirection carrier and an environment
carrier are both refused after `19-23` even on a PERMITTED base** — `git -c include.path=… status` and
`GIT_CONFIG_PARAMETERS=… git status` are both exit 0 today and both refused after — so either in the
invariance arm would be STRICTER than its base and would turn that property permanently red in a file
`19-23` may not edit. **This is `19-18`'s `{v}>` blocker and `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS`
split, one axis over**, it is the single most likely way this seam breaks, and it is prevented here by
construction: assert mechanically that `CONFIG_CONFINED_CARRIERS` is disjoint from both other
alphabets and that neither of the other two is ever drawn by the invariance arm.

**4. The DOTLESS FENCE — the second mechanical assertion, and the one that would otherwise have
blocked the round.** Assert that `CONFIG_INDIRECTION_CARRIERS` contains no entry whose key half lacks a
`.`, and assert by name that `-c a=b` IS an entry of `CONFIG_CONFINED_CARRIERS`. The failure message
names the reason rather than leaving it to be inferred: `CALLEE_KNOWN_LEADING_PREFIX` at
`tests/envelope_wrapper_class.rs:5197` splices round 7's ENTIRE callee-grammar generative property
behind `-c a=b`; real git RUNS `git -c a=b version` at rc 0 and errors only when something READS the
key; so a rule that refused a key it cannot decompose into a section would be permanently red in a
property `19-23` may not edit. Written in the shape section 14b's `{v}>` exclusion already uses.

**5. The PERMITTED-BASE FENCE — the third mechanical assertion, and it is this axis's own version of
the `{v}>` lesson.** The base set the indirection and environment properties draw from must contain at
least one base whose UNWRAPPED form is PERMITTED — assert it by name (`status`, `commit -m x`, and the
in-namespace `push origin refs/heads/gsd-auto/alpha/w`). The failure message states the measurement:
`git -c include.path=/tmp/evil.cfg push --force origin main` is ALREADY exit 2 `force_push_blocked` at
`15c40d6` for its VERB, so a property drawing only refused bases is green before the fix and certifies
nothing about config resolution — the eighth consecutive instance of `T-19-76`'s failure mode,
produced by the corpus rather than found by the next audit.

**6. The generative properties**, modelled directly on sections 14 and 15's:

  - the INVARIANCE arm — for each base, splice each `CONFIG_CONFINED_CARRIERS` entry and assert the
    verdict is **unchanged**, with the positive walk control preceding the loop so an empty walk means
    something;
  - the FAIL-CLOSED arm — for each base, refused or permitted, splice each
    `CONFIG_INDIRECTION_CARRIERS` and each `CONFIG_ENV_CARRIERS` entry and assert **refusal AND an
    empty walk**, recording the slots seen.

**Assert the exit code and the walk, never the reason identifier.** An indirection carrier on a refused
base is refused today for its verb and after the fix for its carrier, which is a reason-identifier
change on a row whose verdict never moves; identifiers belong in the named per-row pins of Task 1 where
each carries its own derivation.

Floors in the file's existing shape — `MIN_CONFIG_RESOLUTION_CLASSES`, `MIN_CONFIG_CONFINED_CARRIERS`,
`MIN_CONFIG_INDIRECTION_CARRIERS`, `MIN_CONFIG_ENV_CARRIERS`, `CONFIG_RESOLUTION_CASES`,
`CONFIG_RESOLUTION_REFUSED_CASES`, `CONFIG_RESOLUTION_PERMITTED_CASES`, `CONFIG_RESOLUTION_SLOTS` and
`CONFIG_RESOLUTION_CLASS_COUNTS` — **each computed from the generation arithmetic (entries × bases ×
slots) and STATED in the constant's doc rather than guessed**, as exact equalities so losing one case
turns them red. Audit 5 found `19-16` set a floor of 50 against a maximum of 40 by construction; state
the arithmetic and check it.

**7. The per-alphabet floor for the new axis**, in the shape of
`every_alphabet_this_round_widens_can_draw_a_word_the_shell_deletes` — every alphabet this round widens
must be able to draw at least one config-resolution class, with a failure message that says the correct
response is to RESTORE entries and never to delete the floor, and that names `T-19-105` as the eighth
consecutive instance of this failure mode and the THIRD consecutive round in which the axis moved
rather than the cell.

This whole task is RED by design except the invariance arm, the degenerate-proofing block and the three
mechanical fences: the fail-closed assertions cannot pass until `19-23` exists. Commit as its own
commit, second, with ZERO `src/` hunks.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c 'UNREADABLE_CLASSES\|DELETION_CLASSES\|CALLEE_GRAMMAR_CLASSES\|CALLEE_KNOWN_LEADING_PREFIX' tests/envelope_wrapper_class.rs; rtk proxy grep -c 'POLICY_MIN_PRODUCTION_BYTES: usize = 40_000\|HOOKS_MIN_PRODUCTION_BYTES: usize = 20_000' tests/envelope_wrapper_class.rs; git diff HEAD~1 --numstat -- tests/envelope_wrapper_class.rs; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "FOURTH-AXIS-RED-ZERO-SRC"</automated>
  </verify>
  <done>`UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES`, their predicates, their degenerate-proofing assertions and all of their floors are byte-identical — verified by `git diff` showing no deletion inside any of the three regions — and so are `POLICY_MIN_PRODUCTION_BYTES` (40_000), `HOOKS_MIN_PRODUCTION_BYTES` (20_000) and `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`, with `git diff --numstat` over this file showing that no line was deleted anywhere; `CONFIG_RESOLUTION_CLASSES` names five classes with degenerate-proof predicates and an assertion block proving each pair non-degenerate with `git commit -m "include.path=/tmp/e"` satisfying NONE; three alphabets exist with `CONFIG_CONFINED_CARRIERS` spliced between the governed program and its decision words as the ONLY one the invariance arm draws, `CONFIG_INDIRECTION_CARRIERS` spliced at the same position and `CONFIG_ENV_CARRIERS` spliced as an ASSIGNMENT PREFIX, both with their own fail-closed property, all three asserted mutually disjoint and the latter two asserted never drawn by the invariance arm, with the doc recording that an indirection or environment carrier is refused after `19-23` even on a permitted base and that this is `19-18`'s `{v}>` blocker one axis over; **the DOTLESS FENCE asserts mechanically that no `CONFIG_INDIRECTION_CARRIERS` entry has a dotless key half and that `-c a=b` IS an entry of `CONFIG_CONFINED_CARRIERS`**, with the failure message naming `CALLEE_KNOWN_LEADING_PREFIX`, its line, and the consequence; **the PERMITTED-BASE FENCE asserts mechanically that a layer-2-permitted base is drawn**, with the failure message recording that `git -c include.path=… push --force origin main` is already `force_push_blocked` at `15c40d6` so a refused-only base set certifies nothing; the two generative properties assert an UNCHANGED verdict over the confined alphabet and refusal AND an empty walk over the other two, assert exit codes and walks but never reason identifiers, and have a positive walk control preceding each loop; every new floor's arithmetic is stated in its doc and equals the generation count; no existing floor, alphabet entry, property or assertion was lowered, deleted or narrowed anywhere; the new properties are RED with every failing name recorded; `git show --stat` shows ZERO `src/` hunks; and this is the second commit.</done>
</task>

<task type="auto">
  <name>Task 3: the registrations, the outstanding gap, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s sections "Threats found by audit 7", "The mechanism pins, verified non-dead",
    "The known-open set, as audit 7 verified it", "The four execution-time judgements, assessed
    independently", "Audit 7 — what the round-7 control can and cannot fail on" and "Audit 7 Sign-Off",
    plus the appended execution records of plans 19-20 and 19-21 and the plan-19-21 blocker-row
    resolution record, which this plan's subsection FOLLOWS and does not edit.
    `deferred-items.md`'s round-7 closure section, its `T-19-96` entry, its `glab --host` forge cell,
    its `T-19-17r` bookkeeping gap entry, and its `tests/envelope_tracer.rs` ETXTBSY entry carried
    since 19-07.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-21 records and touching nothing
that precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan
19-22 — carrying:

  - **the invariant this round is about**, stated once: the configuration the guard assumes is in
    effect must be the configuration git will actually resolve. Rounds 5, 6 and 7 closed the three
    questions below it and audit 7 verified all three answered, so this round is a stage further into
    what git does with a command line rather than an eighth spelling of the same class;
  - **the layer-2-PERMITTED-base finding, stated FIRST among the measurements** and with its five
    measured `force_push_blocked` control rows, because it is the design constraint that shapes the
    whole corpus and the one a later reader most needs;
  - **every measured row with its walk**, in the column shape the audit uses: the ten `T-19-103` rows,
    the six `T-19-104` rows, the eleven paired discriminators, the discrimination controls and the
    disclosed cost row, the permitted half, the dotless-key rows, the already-refused controls, the
    four composition rows, the two recorded-not-asserted rows and the `T-19-86` persisted-alias arm;
  - **the real-git precedence table in full**, every carrier measured with the envelope's OWN
    `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` injection as the control rather than a stand-in, with the
    control's `/ENV_WINS` printed beside each carrier's value. **The case rows are the point and must
    be prominent**: `INCLUDE.PATH` and `INCLUDEIF.gitdir:<p>.PATH` both resolve, so git folds the
    SECTION and the VARIABLE and leaves the SUBSECTION case-sensitive;
  - **the case-insensitivity question, ANSWERED EXPLICITLY**, in its own short paragraph: yes, key
    comparison must be ASCII-case-insensitive on the section and the variable; the subsection must not
    be read at all; and **no existing pin changes**, because `is_hooks_path_key` (`policy.rs:750-752`)
    is already `eq_ignore_ascii_case("core.hookspath")` and `git -c CORE.HOOKSPATH=/dev/null status` is
    measured at `hook_bypass_blocked` today. Record that the guard's whole-key case-fold is MORE
    permissive than git for a subsectioned key, which is the safe direction;
  - **the design question, stated with what the corpus can now fail on**, and explicitly NOT answered
    here because the costing belongs to the plan that measures the rule: the guard must not ask *does
    this assignment spell `core.hooksPath`* but *can I establish what `core.hooksPath` will be*. Record
    which shapes this corpus turns red — a substring match on `include` (red at `includepath` and
    `notinclude.path`), a blanket refusal of `-c` (red at the permitted half), and a refusal of a key
    that cannot be decomposed into a section (red at `-c a=b` and at round 7's whole generative
    property) — and record the disclosed cost the corpus pins in the other direction
    (`include.pathx`);
  - **the `GIT_CONFIG_NOSYSTEM` cell**, with its measured real-git defeat, its guard-side exit 0, and
    the explicit statement that its harm is INERT because `cred::write_gitconfig` points both
    `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` at the same helper-free file. Folded into `T-19-104`'s
    class with the `19-14` provenance caveat, **not registered as a new threat ID and not called a
    bypass**, and left to `19-23` to decide whether it earns a list entry;
  - **the `T-19-86` persisted-alias arm**, recorded with both rows at exit 0 and the explicit statement
    that layer 3 catches the inner push today — which is `AR-19-03` working — and that this is exactly
    what `T-19-103` removes. **`T-19-86` is NOT closed, narrowed or re-scoped**;
  - **why the existing `ENVELOPE_ENV_KEYS` drift pin structurally cannot see `T-19-104`**: it is
    sourced from `cred::EnvelopeEnv::with_run_id(build_env_in(…))`, i.e. from the keys the envelope
    SETS or REMOVES, and every floor it carries is a floor over the envelope's own entries — while both
    cells here are keys the envelope neither sets nor removes but which DEFEAT ones it does. State that
    the fix is a SECOND SOURCE rather than a wider filter, and that it is `19-23`'s;
  - **the `glab --host` forge cell**, carried forward unchanged with its unconfirmed-callee caveat and
    the note that `glab` is confirmed NOT installed, so audit 7 declined to upgrade it and this plan
    declines too. Deliberately not fixed and out of scope;
  - **the `T-19-17r` bookkeeping gap, recorded as OUTSTANDING and deliberately NOT resolved.** State
    that `19-17-SUMMARY.md` calls it "accepted"; that audits 5, 6 and 7 all confirmed the measurement
    and both pins at `tests/envelope_literal_decision.rs:1355-1379`; that there is **no
    Accepted-Risks-Log row, no `AR-19-13` and no register row**; and that **this plan does not make the
    acceptance, because accepting a risk is a human decision and three audits running explicitly
    declined to make it.** The next round either adds the log row or drops the word — and this plan
    does neither;
  - **the byte floors, re-measured and reported**: `policy.rs` raw 307,496 / stripped 68,785 against
    40,000, `hooks.rs` raw 99,909 / stripped 33,460 against 20,000, with the note that `policy.rs`'s
    current ratio is **22.37%** so the 25% ratio assertion `19-20` deleted would be RED today — the
    floors are load-bearing and `19-23` adds more prose to that file than `19-21` did;
  - **the statement that this plan CLOSES NOTHING**, that `T-19-103`, `T-19-104`, `T-19-105`,
    `T-19-106` and `T-19-107` all stay open, that `T-19-86` and `T-19-91` remain OPEN at `high` so
    `/gsd-secure-phase 19` is not cleared, and that re-measuring and re-classifying these rows is that
    command's job rather than a plan's.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Record that the round-8 corpus exists and is RED for `T-19-103`,
`T-19-104` and `T-19-105`, and that `T-19-106` and `T-19-107` are registered with their evidence
carried into `19-23`; fold the cells found while planning into `T-19-103`'s and `T-19-104`'s entries as
spellings of their classes rather than registering new threat IDs, with the provenance caveat `19-14`
established; add the `GIT_CONFIG_NOSYSTEM` cell with its inert-harm caveat; record the persisted-alias
arm under `T-19-86` **without closing it**; leave the `glab --host` forge cell and the `T-19-17r`
bookkeeping gap present and unresolved. Leave `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` unchanged
and open.

**3. The handoff gate.**

  - The command is `rtk proxy cargo test --no-fail-fast`. A plain `cargo test` fail-fasts at
    `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a
    single envelope suite; `rtk proxy` is required because the RTK hook strips exactly the `warning:`
    and `test result:` lines any count is read from — `19-19` first read 1364 instead of 1605 that way
    (D-34). Read the counts with `rtk proxy grep` over the redirected log, never a plain `grep`.
  - **State the gate arithmetic explicitly rather than assuming it.** `passed + failed` is strictly
    greater than **1639**, the total `19-21-SUMMARY.md` records and audit 7 re-measured over 42 result
    lines. **A red test RAN, so red→green leaves the total unchanged and the increase comes ONLY from
    the NEW `#[test]` fns this plan adds** — count them from `git show` and check the identity against
    the observed number. A disagreement is a finding.
  - Every failure name is one of the three documented flakes
    (`a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`,
    `a_run_killed_without_an_ending_is_reported_crashed_and_nothing_on_disk_is_repaired`,
    `a_relocated_copy_of_the_stub_refuses_instead_of_acting`) or one of the RED names this plan
    creates. None fired in audit 7's run and their absence is not evidence they are fixed. **The RED
    set is non-empty and every one of its names is listed in the SUMMARY** — that list is `19-23`'s
    handoff contract and the one number a run that did nothing cannot forge.
  - `--test envelope_callee_grammar`, `--test envelope_argv_deletion`,
    `--test envelope_literal_decision`, `--test envelope_expansion_slots` and
    `--test envelope_command_position` are FULLY GREEN and unmodified.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate: it
    already fails at base on four pre-existing lints in `src/browser.rs` and `src/project_creator.rs`.
  - Record the per-binary counts for **all FOURTEEN `envelope_*` binaries** — the thirteen audit 7
    observed (`advisory`, `argv_deletion`, `callee_grammar`, `command_position`, `credential`,
    `expansion_slots`, `hook_refusals`, `literal_decision`, `pr_cap`, `tracer`, `wiring`,
    `wrapper_bypass`, `wrapper_class`) plus the `envelope_config_resolution` this plan adds — and
    confirm every one RAN. **A run reporting thirteen is a run in which this plan's own evidence file
    did not execute.**

Commit the record as its own commit, the third.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-22-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -c '^| AR-19-13 |' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-22-gate.log | tail -50; rtk proxy grep -c 'Running tests/envelope_' /tmp/19-22-gate.log; git diff --stat HEAD~3..HEAD | tail -12; git diff --numstat HEAD~3..HEAD -- tests/ | tail -6; git log --oneline -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-22 record with the invariant, the layer-2-permitted-base finding stated first with its five control rows, every measured row and its walk, the full real-git precedence table measured with the envelope's own injection as the control and both case rows prominent, the case-insensitivity question answered explicitly with the statement that no existing pin changes, the design question stated with the shapes the corpus turns red and the disclosed cost it pins, the `GIT_CONFIG_NOSYSTEM` cell recorded as a defeat with an INERT harm and its `write_gitconfig` reason, the `T-19-86` persisted-alias arm recorded with `T-19-86` explicitly not closed, the structural reason the existing `ENVELOPE_ENV_KEYS` drift pin cannot see `T-19-104` and the statement that the fix is a second source, the `glab --host` cell carried forward unfixed with its unconfirmed-callee caveat, the `T-19-17r` gap recorded as OUTSTANDING with the acceptance deliberately unmade, the re-measured byte floors with the 22.37% note, and the statement that nothing is closed and no gate is cleared — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection, and the Accepted-Risks-Log row count for that risk ID still zero; `deferred-items.md` records the RED corpus, folds the planning cells into `T-19-103`'s and `T-19-104`'s classes, adds the `GIT_CONFIG_NOSYSTEM` cell, records the persisted-alias arm without closing `T-19-86`, and leaves `T-19-86`, `T-19-91`, `T-19-96`, `T-19-74`, the forge cell and the `T-19-17r` gap unchanged; the `--no-fail-fast` run reports `passed + failed` strictly greater than 1639 with the arithmetic stated and checked against `git show`, counted with `rtk proxy grep` over a redirected log, every failure name is one of the three documented flakes or one of this plan's RED names, and the complete RED list is in the SUMMARY; all FOURTEEN `envelope_*` binaries RAN with their per-binary counts recorded; the five prior evidence files are green and unmodified; `git diff --numstat` over `tests/` shows ZERO deletions across all three commits; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, and now to the configuration those words will run under |
| written word → word the shell runs | Round 5 closed this half: which BYTES reach `execve` is decided by expansions the guard does not perform |
| words read → words that ARRIVE | Round 6 closed this half; audit 6 verified the pair COMPLETE over bash |
| reconstructed argv → which word is the VERB | Round 7 closed this half: an unestablished verb slot is a refusal, pinned against the installed git in both directions |
| **the verb → the CONFIGURATION it runs under** | **This round's boundary, and it is not a property of the command line at all: git's config graph has an INDIRECTION (`include.path`, `includeIf.<cond>.path`) that reaches `core.hooksPath` at command-line precedence without the string appearing, and git's own environment carrier `GIT_CONFIG_PARAMETERS` outranks the triplet the envelope injects** |
| corpus → the control it certifies | An alphabet that cannot DRAW a class is a property that cannot FAIL on it, and seven consecutive rounds have shipped one |
| **a reproducer's BASE → what it can prove** | **A row already refused for its verb certifies nothing about what the verb runs under, and five measured `force_push_blocked` rows are the evidence** |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-105 | Spoofing | every generative alphabet, and all THREE named class axes (`UNREADABLE_CLASSES`, `DELETION_CLASSES`, `CALLEE_GRAMMAR_CLASSES`) | medium | mitigate | The corpus is widened FIRST and observed RED, the ordering that worked four times. `CONFIG_RESOLUTION_CLASSES` stands as a FOURTH named axis beside three untouched command-line axes, with five degenerate-proof predicates, a `CONFIG_CONFINED_CARRIERS` alphabet that is the only one the invariance arm draws, SEPARATE `CONFIG_INDIRECTION_CARRIERS` and `CONFIG_ENV_CARRIERS` alphabets with their own fail-closed property because their entries are not verdict-preserving, a mechanical DOTLESS fence protecting `CALLEE_KNOWN_LEADING_PREFIX`, a mechanical PERMITTED-BASE fence, and floors whose arithmetic is stated rather than guessed. Closed only by `19-23`, because a corpus is evidence about a control and there are no controls yet |
| T-19-103 | Elevation of Privilege | git's own CONFIG RESOLUTION — `scan_leading`'s single-key check `is_hooks_path_key` (`policy.rs:750`), reached through `include.path` / `includeIf.<cond>.path` | high | mitigate (corpus only; rules are `19-23`) | Ten rows measured at their verdicts on LAYER-2-PERMITTED bases, confirmed against the REAL git binary with the envelope's own injection as the control, and written as RED assertions with a DERIVED identifier each. The PERMITTED half is written beside them — six ordinary invocations with `git -c user.name="$NAME" commit -m x` as this axis's blanket-refusal control — so the corpus can fail on a rule that refuses every `-c`. The two DISCRIMINATION controls (`includepath`, `notinclude.path`) are pinned permitted so a substring fix lands red, and the disclosed cost row (`include.pathx`) is pinned refused so the round's over-refusal is measured rather than discovered |
| T-19-104 | Elevation of Privilege | `ENVELOPE_ENV_KEYS` / `tampers_with_envelope_env` (`policy.rs:2816-2928`) — `GIT_CONFIG_PARAMETERS` is absent | high | mitigate (corpus only; rules are `19-23`) | Six rows measured and asserted at `hook_bypass_blocked`, every post-fix verdict DERIVED from the three-spelling paired discriminator (`GIT_CONFIG_COUNT=0` bare, `export …;` and `env …`, on both a refused and a permitted base) rather than guessed. The disclosed bare-word cost (`echo GIT_CONFIG_PARAMETERS`) is pinned beside its twin. The record states why the existing drift pin structurally cannot see this class and that the fix is a SECOND SOURCE rather than a wider filter |
| T-19-106 | Spoofing | the callee-grammar drift pin's REACH — `CONFIG_VALUE_OPTS` (`policy.rs:997`) and the four structural arms of `leading_git_option` | medium | mitigate (registered; rule is `19-23`) | Registered here with `git config --comment core.hooksPath /dev/null` measured at exit 0 and real git measured answering `error: unknown option 'comment'`. Not fixed by this plan |
| T-19-107 | Repudiation | the "ZERO over-refusal cost on git 2.43.0" claim | low | mitigate (registered; rule is `19-23`) | Registered here with `git -v` measured at exit 2 `envelope_assertion_failed` against real git's `git version 2.43.0` at rc 0. Not fixed by this plan |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — bookkeeping gap RECORDED, acceptance deliberately unmade** | `19-17-SUMMARY.md` calls it accepted; there is no Accepted-Risks-Log row, no `AR-19-13` and no register row. Audits 5, 6 and 7 all confirmed the measurement and both pins and all three explicitly declined to add the acceptance because that is a human decision. This plan records the gap as OUTSTANDING and makes no acceptance |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; four registered rows still at exit 0 and their pin stays green UNMODIFIED, and the PERSISTED-alias arm audit 7 measured is RECORDED at exit 0 without closing, narrowing or re-scoping the threat. Because it remains open at `high`, this plan does NOT clear `/gsd-secure-phase 19`, and the SUMMARY says so first |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (registered, arms unweakened, not remedied)** | `reflog $S`, `reflog show $S` and `symbolic-ref $S` at exit 0 with no second carrier; bare `git push $REF` at exit 0 in-namespace. Record unweakened, no remedy added, counts toward `threats_open` |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG, one slot outside the decision region | medium | **accept for this plan (registered by `19-16`, not fixed)** | Left exactly as pinned. Widening the decision region is a later round's to decide |
| — | Elevation of Privilege | `FORGE_VALUE_OPTS` / `subcommand_word_indices` — the same enumeration defect in a third component, under-counting direction | — | **out of scope, carried forward unfixed** | `glab` is confirmed NOT installed, so the callee's grammar is unconfirmed and it is not claimed as a live bypass. Audit 7 declined to upgrade it; this plan declines too |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added; this plan touches only `tests/` and `.planning/`. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. The real-git precedence probes shell out through `std::process::Command` inside tests |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`,
  and MUST be read as `passed + failed` against **1639**.
- The gate arithmetic is STATED and CHECKED: expected `passed + failed` = 1639 plus the number of NEW
  `#[test]` fns this plan adds, counted from `git show`. A red test RAN, so red→green contributes
  nothing; a disagreement is a finding.
- All FOURTEEN `envelope_*` binaries RAN and their per-binary counts are recorded. Thirteen means this
  plan's own evidence file did not execute.
- The RED set is NON-EMPTY, every one of its names is listed in the SUMMARY, and every other failure is
  one of the three documented flakes.
- Every commit shows ZERO `src/` hunks under `git show --stat`. This is the evidence the split exists
  to produce.
- Every precedence claim is backed by a recorded real-git probe using the envelope's own
  `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` injection as the control, with `/ENV_WINS` printed beside each
  carrier's value.
- **At least one reproducer base is layer-2 PERMITTED, asserted mechanically**, and the five
  `force_push_blocked` `--force` compositions are recorded as controls rather than reproducers.
- `UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES`, their predicates, their
  degenerate-proofing blocks and all their floors are byte-identical; `POLICY_MIN_PRODUCTION_BYTES` and
  `HOOKS_MIN_PRODUCTION_BYTES` are unchanged; no existing `MIN_*` constant was lowered; and
  `git diff --numstat` over `tests/` shows ZERO deletions across all three commits.
- `CONFIG_CONFINED_CARRIERS`, `CONFIG_INDIRECTION_CARRIERS` and `CONFIG_ENV_CARRIERS` are mutually
  disjoint, and neither of the latter two is ever drawn by the invariance arm, asserted mechanically.
- **`-c a=b` is asserted PRESENT in `CONFIG_CONFINED_CARRIERS` and no dotless-key entry is in
  `CONFIG_INDIRECTION_CARRIERS`**, with the failure message naming `CALLEE_KNOWN_LEADING_PREFIX`.
- `git -c includepath=…` and `git -c notinclude.path=…` are pinned at exit 0 before and after;
  `git -c include.pathx=…` is pinned REFUSED after as the disclosed cost.
- The five mechanism pins are present and non-vacuous: `Token.literal` false for `pus?` and true for
  `-c`, `include.path=…` and `status`; `git >/dev/null push --force origin main` still refused and
  `git x2>/tmp/o push --force origin main` still permitted; `is_separator(">")` false with `SEPARATORS`
  unchanged; `git --attr-source HEAD push --force origin main` refused and `git --no-pager status` and
  `git - push --force origin main` still at exit 0; and `split_segments_with_heads` reporting
  `head_is_command_position == false` for the severed spellings and `true` for `{ git status; }`.
- `gh pr create --title x` leaves exactly ONE ledger line in a walked fresh root and the cap fires with
  two on call 2, which is the walk's non-blindness control.
- The Accepted-Risks-Log row count for the `T-19-17r` risk ID is zero and the word "accepted" is not
  applied to `T-19-17r`.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-21 records
  and nothing else.
- Three commits exist in order: the seventh evidence file, the fourth axis, the record.
</verification>

<success_criteria>
1. Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before
   it was written as an assertion, and every precedence claim was confirmed against the REAL git binary
   with the envelope's own injection as the control.
2. The corpus goes RED against the pre-fix tree, the RED set is non-empty, and every failing name is
   recorded verbatim in the SUMMARY as `19-23`'s handoff contract.
3. **The reproducers are built on layer-2-PERMITTED bases, asserted mechanically**, and the five
   `--force` compositions that are already refused for their verb are recorded as controls.
4. The config axis is legible as a FOURTH axis: `CONFIG_RESOLUTION_CLASSES` with five degenerate-proof
   predicates stands beside three byte-identical command-line axes, and nothing was smuggled into any
   of them.
5. The corpus can fail in BOTH directions — the bypass rows, six ordinary invocations pinned PERMITTED
   with `git -c user.name="$NAME" commit -m x` named as this axis's `ls {git,svn}-repo`, and the two
   discrimination controls that turn a substring fix red.
6. **The handoff needs no replacement exception and none is granted**: every derivable row is asserted
   at its post-fix verdict, the two undeliverable rows are RECORDED rather than asserted, the
   indirection and environment alphabets are kept out of the invariance arm, and the DOTLESS and
   PERMITTED-BASE fences are asserted mechanically so `19-23` cannot be handed an unsatisfiable seam.
7. The case-insensitivity question is answered explicitly and by measurement in both halves of the key,
   with the statement that no existing pin changes.
8. The `T-19-17r` bookkeeping gap is recorded as OUTSTANDING, no Accepted-Risks-Log row is added, and
   the word "accepted" is not used for it.
9. No `src/` line changed; no existing floor, alphabet entry, property or assertion was lowered,
   deleted or narrowed anywhere; and `T-19-86` (including its newly recorded persisted-alias arm),
   `T-19-91`, `T-19-96` and `T-19-74` are untouched and open.
10. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared — `T-19-86` and `T-19-91`
    remain OPEN at `high` — and no unqualified "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-22-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared and that this plan
closes nothing; the three commit SHAs in order with `git show --stat` proving ZERO `src/` hunks in
each; the `--no-fail-fast` `passed + failed` totals before and after with the gate arithmetic stated
and checked against `git show`; the COMPLETE list of RED test names, which is `19-23`'s handoff
contract; the per-binary counts for all FOURTEEN `envelope_*` binaries with every one confirmed to have
RUN; the layer-2-PERMITTED-base finding stated prominently with its five measured `force_push_blocked`
control rows; a table of every measured row with its exit code, reason identifier and walk listing,
split into the `T-19-103` rows, the `T-19-104` rows, the paired discriminators, the discrimination
controls and the disclosed cost row, the permitted half, the dotless-key rows, the already-refused
controls, the composition rows and the `T-19-86` persisted-alias arm; the full real-git precedence
table with the envelope's own injection as the control and both case rows called out; the explicit
answer to the case-insensitivity question with the statement that no existing pin changes; the two
undeliverable rows with the reason each is left for `19-23`; the design question stated with the shapes
this corpus turns red and the disclosed cost it pins; the `GIT_CONFIG_NOSYSTEM` cell with its measured
defeat and its INERT harm and the `write_gitconfig` reason; the structural reason the existing
`ENVELOPE_ENV_KEYS` drift pin cannot see `T-19-104`; the re-measured byte floors with the 22.37% note;
the `glab --host` forge cell carried forward unfixed with its unconfirmed-callee caveat; the `T-19-17r`
bookkeeping gap recorded as OUTSTANDING with the explicit statement that this plan did NOT accept it
and added no Accepted-Risks-Log row; and a "what remains uncovered" section naming `T-19-86` FIRST —
open at `high` by user scoping decision, with its persisted-alias arm recorded and NOT closed —
followed by `T-19-91` (open at `high`, arms unweakened, `reflog` and `symbolic-ref` with no second
carrier), `T-19-96`, `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit
statement that only the WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>