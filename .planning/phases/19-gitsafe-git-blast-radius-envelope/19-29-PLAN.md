---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 29
type: execute
wave: 19
gap_closure: true
gap_ids: [T-19-116, T-19-118, T-19-117, T-19-115]
depends_on: [19-28]
files_modified:
  - src/envelope/policy.rs
  - src/envelope/hooks.rs
  - src/envelope/ledger.rs
  - src/envelope/cred.rs
  - src/envelope/advisory.rs
  - src/envelope/mod.rs
  - tests/envelope_carrier_reach.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-04, SAFE-05, SAFE-06]

estimate:
  tokens: 152000
  raw_tokens: 152000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "**THE DESIGN QUESTION, ANSWERED: BOTH, PLUS A THIRD THING THAT IS NEITHER — AND EACH HALF IS CHOSEN BY MEASUREMENT RATHER THAN BY SYMMETRY.** Rule (a) reads a path and refuses if it resolves under the envelope root. **`C-10` is outside the root, so the PATH SET widens**: `{envelope_dir_in(root, alias)} ∪ {the binary this guard is running as}`, the first a PREFIX boundary and the second an **EXACT PATH**. **`C-05` is inside the root but reached through a direction the rule cannot see, so what the rule SEES widens**: pathname REDIRECTION TARGETS, carried on a channel `Segment::tokens` never holds. **`T-19-115`'s tilde, glob and brace spellings get NO rule**, because a rule would have to perform expansions the guard is forbidden to perform — a tilde needs the environment and a glob needs the filesystem — so they get the residue's ARITHMETIC corrected instead. **And `T-19-117` is not a path question at all**: the ledger's SIZE is work on the guard's registered critical path, and it gets a size bound that fails CLOSED."
    - "**THE TWO BOUNDARY KINDS DIFFER AND THE REASON IS MECHANICAL RATHER THAN AESTHETIC.** The envelope directory is a PREFIX boundary because this envelope owns every byte under it and `rm -rf <root>/<alias>` takes nine carriers in one call. **The binary is an EXACT-PATH boundary because its directory is shared with everything else the user installed**: a prefix over `~/.cargo/bin` would refuse `ls ~/.cargo/bin` and every `cargo install`, and `19-28` pinned `cp /bin/true <BINARY-PARENT>/some-other-file` and `ls <BINARY-PARENT>` PERMITTED as this round's `--signed no` from the other side. **A clause written as a directory prefix over the binary's parent turns both red, and that is the point of them.**"
    - "**CLOSING DIRECTION (i) DOES *NOT* REQUIRE THE DELETION MODEL TO READ REDIRECTION TARGETS AS ARGV, AND THAT IS THE ROUND'S CENTRAL MECHANICAL CLAIM.** Round 6's model answers *which words ARRIVE at the program*; a redirection target does not arrive, and that answer does not change. Rule (a) asks a different question about the same walk: *does this line NAME a path this run's controls live in?* Three facts, read out of the source: **(1)** `skip_redirection_target` (`policy.rs:3007-3057`) already walks the target's full extent applying the target's own quoting rules, in order to skip it — its doc's *\"Its text is never needed, only its extent\"* is the sentence that becomes false, in the WR-02 shape this codebase already names; **(2)** `redirection_operator_len` (`:2968-2992`) is a FINITE CLOSED grammar of twelve operators and only some take a PATHNAME — `<<` and `<<-` take a heredoc DELIMITER, `<<<` a here-STRING and `>&`/`<&` an fd number, none of them a path; **(3)** `split_segments_with_heads` (`:2466-2514`) EXCLUDES every operator token from `segment.tokens` and FLUSHES `current` into a `Segment` the instant one arrives, **so a redirection-target token pushed into the stream would split `git >/dev/null push --force origin main` into `[git]` and `[push, --force, origin, main]`, and the first resolves `Governed` with an empty argv, which `classify_git` answers `Allow` for.** `19-28`'s SEGMENT-COUNT pins assert exactly one `Segment` with exactly the surviving argv for that row and for `git x2>/tmp/o push --force origin main`; **they are GREEN today and a red one is a finding about the FIX, reported with the observed segment list, never an assertion to edit.**"
    - "**THE CHANNEL FOLLOWS A PRECEDENT ALREADY IN THE FILE, WHICH IS WHY IT IS NOT A SECOND SCAN.** `Segment::redirection_unresolvable` (`policy.rs:2402-2418`) is a fact about the COMMAND rather than a word, accumulated in the operator arm of `split_segments_with_heads`' ONE walk (`:2461-2505`), applied RETROACTIVELY over `segments[command_start..]` because a redirection belongs to the whole simple command, and reset at each REAL command operator. **The redirection-target channel is that shape exactly**: found by the walk that already finds it, attributed by the machinery that already attributes, and delivered on a `Segment` field while **`segment.tokens` stays byte-for-byte what it is today** — which is what makes it mechanically impossible for `resolve_program`, `resolve_program_with_head`, `first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api` or `scan_leading` to see anything new. Round 3's principle is discharged rather than weakened: **rule (a) is still raised at ONE site on ONE segment, and reading two fields of that segment at that one site is one reading site.** `SEPARATORS` does not move, `is_separator(\">\")` stays `false`, and round 6's over-deletion control stays PERMITTED. **If the fix needs a second scan or a second reading site, STOP and report it as a finding.**"
    - "**`Token.literal` IS NOW READ IN A THIRD PLACE AND THE BIT IS THE ONE THING BOTH NEW HALVES REST ON.** Round 5 computes it while a word is consumed and it can never be recovered from `Token::text` afterwards; round 10 read it for the first time outside a governed program's decision words; **this round reads it for a REDIRECTION TARGET, computed by the same quote-walk that already parses the target.** So a change that cleared, repurposed or widened it would silently move THREE rules at once as well as turning rounds 5 and 6's verdict pins vacuous. `Token::literal`, `Token::expansion`, `Token::word_splitting_flush`, `Token::brace_splice`, `Token::redirection_unresolvable`, `Segment::head_is_command_position` and `Segment::brace_spliced` all keep their present meanings exactly."
    - "**THE RESIDUE IS SEVEN DIRECTIONS, NOT FOUR, AND THE ARITHMETIC IS CORRECTED IN FOUR PLACES.** Rule (a) decides on four conditions and each is also a silence. The docs name FOUR silences and every cited spelling is `$`-shaped; **round 5's own literalness table names EXPANSION, PATHNAME, TILDE and BRACE as the four classes that clear the bit**, so the rule inherits round 5's whole class list by construction. `19-28` measured every one live, with the absolute-literal twin at exit 2 beside each and `bash` confirming the shell reaches the file. **After this round the residue is stated as a CONDITION rather than an enumeration** — a word the shell may rewrite, a word that is not absolute, a word that reaches the path only through a link — with the seven spellings named as instances rather than as a complete list. **`T-19-115` is `T-19-76`'s failure mode for the tenth consecutive round and for the first time it is in the cell the previous round just filled**, which is worth saying in the doc rather than only in the record."
    - "**DIRECTION (i) STOPS BEING A DIRECTION OF ITS OWN AND BECOMES A SECOND WORD CLASS THE OTHER SILENCES APPLY OVER, AND THAT IS THE HONEST FORM.** After this round an ABSOLUTE LITERAL pathname redirection target under either protected path is REFUSED, and a target that is expansion-borne, tilde-borne, glob-borne, brace-borne, relative or reached through a link is not. **The path set grows, the word set grows, and the silences do not shrink** — each now applies over two word classes and two paths. Saying *\"direction (i) is closed\"* would be false and saying *\"it is unreachable\"* would be the sentence audit 10 disproved. **NARROWED is the word, and the doc, the refusal, `cred.rs` and the record all use it.**"
    - "**`C-10` GETS A RULE, AND THE RULE IS PARTIAL AND SAYS SO.** `cp /bin/true <binary>` at its absolute literal spelling is refused after this round, and that is the spelling audit 10 drove end to end — the one that moved a bare remote's `main` `01f9664` → `2537e97` and made the guard itself answer exit 0. **What it does not reach is stated with the same weight**: `$(command -v gsd-meta-manager)`, a tilde spelling, a relative spelling, and a link on `PATH` whose target `current_exe()` reports instead — `19-28` measured whether `current_exe()` agrees with the path the stub and `guard_command` baked in, and the answer is on the record. **`current_exe()` is resolved ONCE, in `guard`, and handed to `guard_in` explicitly the way `install_in` already takes its `binary`** — because under `cargo test` `current_exe()` is the test binary, which `hooks.rs:103-108` already records. **The predicate itself calls NOTHING on the filesystem or the process**, and an unresolvable exe path makes the binary half SILENT, which is fail-open and is stated rather than left to be discovered."
    - "**`SECTION_ENVELOPE`'S *FIRST* `Guaranteed` CLAUSE MUST BE TRUE WHEN THIS ROUND ENDS, UNDER THE SAME WORDING DISCIPLINE `19-27` USED FOR THE CAP CLAUSE ONE CLAUSE OVER.** Today it reads *\"This run cannot reach your ambient git credentials or SSH agent: the socket is removed, not emptied, and global and system git config is a generated file naming no credential helper\"* (`advisory.rs:245-247`), and `T-19-118` falsifies both halves by measurement. **State what the clause COVERS, make NO completeness claim, and do NOT enumerate** — a clause listing directions and stopping implies a completeness the measurement denies, which is `T-19-107`'s shape in a shorter sentence. **And the mechanical constraint is tighter here than it was for the cap clause**: the pinned phrase `cannot reach your ambient git credentials` sits INSIDE the clause being repaired and must survive verbatim, first, and unwrapped; the constant is at **213 whitespace tokens against a 215 cap**, so the repair has TWO tokens of headroom and **MUST NOT raise the cap.** If it does not fit, shorten by dropping a CLAIM and never a LIMITATION — and a LIMITATION may be GENERALISED to free tokens, which is the direction that makes it both shorter and wider."
    - "**`T-19-118` GETS THE HONESTY REPAIR AND, IF `19-28` MEASURED IT REPRODUCING, A MECHANISM CONTROL — AND NEITHER ALONE IS ENOUGH.** The clause repair alone leaves a live `high`-severity credential reach admitted and uncontrolled, in the one thing the honesty statement leads with. The rule alone closes the ABSOLUTE LITERAL redirection spelling and leaves six others. **`19-28` measured, and RECORDED rather than asserted, whether adding a `credential.helper` pair with an EMPTY value to the `GIT_CONFIG_COUNT` triplet the envelope already injects resets the helper list — a control that reads no command line at all and therefore defends against EVERY spelling** — together with its cost to `GIT_ASKPASS`, to `gh`, and against a later `-c credential.helper=…` on the same line. **Take it if and only if `19-28` recorded it reproducing with an acceptable cost; if it did not, take the clause repair alone and say plainly which branch was taken and why.** Either way `AR-19-04` is RECORDED and NOT un-accepted, and `T-19-23` is not silently re-closed."
    - "**`T-19-117` GETS A BOUND THAT FAILS CLOSED, DERIVED FROM THE DEADLINE RATHER THAN FROM THE CAPS.** `record_and_check_in` reads the ledger WHOLE (`ledger.rs:225-227`) with no size bound, on the guard's registered critical path against `GUARD_TIMEOUT_SECS = 5`; `19-28` measured 190 MB at 7.69 s. **A TAIL read is the WRONG bound** and `tally`'s own doc says why: *\"a malformed line in a cap ledger is an attempt that demonstrably happened — forgetting it under-counts, and under-counting is the one failure this file exists to prevent.\"* So the bound REFUSES rather than reads less. **Its value is derived from `19-28`'s measured curve against the deadline with the margin stated**, not from the caps — the caps are unclamped (`C-15`) and a long-lived envelope accumulates entries outside the window forever, so a cap-derived bound would refuse a legitimately old ledger. The refusal carries `ParkReason::EnvelopeAssertionFailed` — **never `PrCapExceeded`, which names a mechanism this refusal does not use (D-24)** — and it is ACTIONABLE (AR-19-11): it names the file and says the ledger can be archived. **The over-refusal is disclosed from both sides and `19-28` pinned the just-under-the-bound control that still permits and still counts.**"
    - "**THIS PLAN OPENS SIX `src/` FILES AND EVERY ONE IS A NAMED, NARROW, BOUNDED EXCEPTION STATED AS AN EXCEPTION.** `19-23` was granted one, `19-25` another, `19-27` three on the same standard. **`policy.rs`** — the tokenizer's redirection channel, the `Token`/`Segment` fields, the widened predicate and its doc, the refusal, the floor comment's `842`→`421`, and additions inside the existing `mod tests`; `resolve_program`, `resolve_program_with_head`, `first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api`, `scan_leading`, `forbidden_repo_path`, `SEPARATORS` and `is_separator` show ZERO diff lines. **`hooks.rs`** — `guard`, `guard_in`'s and `classify_segments`' signatures, and the ONE call site at `:1018-1029`; `pre_push`, `pre_commit`, `deny`, `read_guard_request`, `park_refusal`, `write_stub`, `stub_body`, `install_in`, `hooks_dir_in`, `assert_provenance_in`, `settings_value`, `settings_json`, `guard_command`, `write_settings_in` and the D-07 second-carrier table show ZERO diff lines, and **the `settings_json` second-carrier row is NOT repaired.** **`ledger.rs`** — ONLY `record_and_check_in`'s read and its new bound; `tally`, `append_entry`, `ends_mid_line`, `parse_stamp`, `saturating_bump` and `ledger_path_in` untouched. **`cred.rs`** — the reach paragraph and WHAT IS NOT COVERED, plus the injected pair IF the branch is taken. **`advisory.rs`** — ONLY `SECTION_ENVELOPE`'s literal. **`mod.rs`** — ONLY `ENVELOPE_ROOT_ENV`'s residual-limit paragraph at `:165-168`, **zero non-doc lines.** `T-19-61` … `T-19-73`, `T-19-84` and `T-19-85` are NOT taken on, and `scan.rs` and `config.rs` are not opened."
    - "**`mod.rs:165-168` IS D-09'S LAST UNCORRECTED HOME AND `T-19-116` CONTRADICTS ITS ARGUMENT DIRECTLY.** It says a party who already controls the environment the TUI starts in can relocate the envelope, and *\"that party can equally unset `GIT_CONFIG_COUNT`, which is D-09's stated ceiling, so this override does not lower a boundary that was standing.\"* **`T-19-116` is a route that lowers a standing boundary — layer 2 AND layer 3 — without controlling the environment the TUI starts in at all.** `19-27` read this paragraph and correctly declined because `mod.rs` was outside its `files_modified`, taking its own stated fallback rather than silently widening the diff; audit 10 endorsed that and named the paragraph for whoever picked it up. **This plan picks it up as a named exception with zero non-doc lines changed.** `GSD_MM_ENVELOPE_ROOT` itself stays as it is — audits 9 and 10 both measured it INERT from inside a Bash command."
    - "**EVERY ROW `19-28` LEFT RED WAS OBSERVED RED IN COMMITS WITH ZERO `src/` HUNKS, AND THIS PLAN'S FIRST ACTION IS CONFIRMING THEY ARE STILL RED — AND ITS SECOND IS CONFIRMING THE PERMITTED SET IS STILL PERMITTED.** If any row is already green against `19-28`'s post-state, the rule is a rule nobody has shown is needed and that is a finding to report with the measured verdict, not a task to complete. This is the mechanism `19-15`, `19-17`, `19-19`, `19-21`, `19-23`, `19-25` and `19-27` used and it is the one number a run that did nothing cannot forge. **And the converse check is load-bearing twice this round**: `19-28` pinned the tilde, glob, brace, expansion-borne, symlinked, relative, ordinary and EXACT-PATH-near-miss rows PERMITTED, so **a rule that turns any of them red is a rule that quietly widened past the boundary it declares** — a finding about the RULE, never an assertion to edit."
    - "**EVERY POST-FIX EXPECTATION CARRIES A WRITTEN DERIVATION AND EVERY NEW ROW IS CHECKED FOR A DISCRIMINATING CONTROL.** `19-26` refused a false converse degenerate-proof its own plan mandated and wrote the true one instead; `19-27` measured that its plan's own certifying row was refused at the SAME identifier as its control and `record_only`'d it. **Name the control for every row this plan adds and state what makes the pair discriminating.** A row whose control fails the same way at the same identifier is RECORDED with its overlap stated, never asserted — and a verdict that cannot be derived is measured and recorded rather than claimed."
    - "**ROUNDS 4 THROUGH 10'S MECHANISMS MUST NOT BECOME DEAD CODE, AND THIS ROUND PUTS TWO OF THEM UNDER DIRECT PRESSURE.** `SEPARATORS` stays **byte-identical** at `policy.rs:2297` — ONE commit in the whole phase, `84a9b05` — and `policy::is_separator(\">\")` stays `false`. **Round 6's deletion model must be UNCHANGED in behaviour**: `git >/dev/null push --force origin main` stays exit 2 `force_push_blocked` AS EXACTLY ONE SEGMENT, `git x2>/tmp/o push --force origin main` stays exit 0 AS EXACTLY ONE SEGMENT, `git 2>/dev/null push --force origin main` stays exit 2, `git >` stays failing closed, and the heredoc row stays as `19-28` pinned it. Round 5's literalness bit stays non-vacuous, round 7's four callee-grammar directions hold, round 8's confinement clause and round 9's re-parse clause stay non-dead with `aliasx.` / `notalias.` and both `T-19-86` `!`-bodied rows at exit 0, and round 10's clause stays green over all nineteen envelope-root operand rows. **Not one config-resolution verdict may move.**"
    - "**THE BYTE FLOORS AND THE STRIPPER'S THREE PROTECTIONS KEEP THEIR STRICTNESS, AND THE FLOOR COMMENT IS ALREADY TRUE EXCEPT FOR ONE EDITORIAL NUMBER.** Audit 10 re-derived every number in it independently — production half 277,570 bytes / 5,414 lines, `hooks.rs` 78,465 / 1,636, one `#[cfg(test)]` sentinel per file, deep anchor at 5,157, `fn scan_leading` at 416, 40,000 at 752, 180,000 at 3,613, 64.85% — and found each exactly right. **Do not re-break it.** The one defect is that *\"842 multi-byte characters\"* is the byte-minus-character DIFFERENCE; **the count is 421.** `policy.rs:7450` is corrected here; `deferred-items.md:2172` sits inside the plan-19-27 section and was corrected BESIDE by `19-28`. `POLICY_MIN_PRODUCTION_BYTES = 40_000`, `HOOKS_MIN_PRODUCTION_BYTES = 20_000`, the proportional floor's VALUE and strictness, the deep anchor and the sentinel counts do not move, and this plan adds prose to `policy.rs`, which only grows the production half."
    - "**`T-19-17r` STAYS OUTSTANDING AND THIS PLAN DOES NOT RESOLVE IT EITHER.** `grep -cE '^\\| AR-19-13 \\|'` over `19-SECURITY.md` is **0**. Audits 5 through 10 and plans 19-24 … 19-28 all confirmed the measurement and both pins and all eleven explicitly declined. **This plan is the twelfth to decline, because accepting a risk is a human decision.** `AR-19-04` and `AR-19-05` are NOT un-accepted, re-rated or renumbered; `AR-19-04`'s reasoning gap is RECORDED beside it with `T-19-118`'s measured consequence, and nothing more. **`T-19-23` is not silently re-closed either** — whether `T-19-118` closes it is audit 11's judgement."
    - "**`/gsd-secure-phase 19` IS NOT CLEARED BY THIS PLAN.** `T-19-86` remains OPEN at `high` by explicit user scoping decision, with `T-19-111` moved OUT of it as `19-27` moved it. `T-19-91` remains OPEN at `high`. `T-19-111` remains OPEN at `high` with NO rule. `T-19-112` and `T-19-113` are narrowed FURTHER and still not closed — `C-15` is a route this rule cannot reach and the deferred option (b) another. **`T-19-115` gets no rule at all**, and **whether `T-19-116`, `T-19-117` and `T-19-118` close is audit 11's judgement rather than this plan's claim: this plan says NARROWED and states what remains open.** `T-19-96` and `T-19-110` are registered open at `medium`; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched apart from the six named exceptions. The SUMMARY must say so FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT begin any fix before confirming, against `19-28`'s recorded post-state, that every RED row `19-28-SUMMARY.md` names is STILL RED and that every PERMITTED row is STILL PERMITTED, and recording both observations verbatim. If any RED row is already green, report it as a finding with the measured verdict and the reason identifier rather than proceeding — a rule certified by a corpus that could not fail on its class is the exact defect eleven rounds of this phase have been about."
      category: honesty
    - statement: "MUST NOT push a redirection-target token into `tokenize`'s token stream. `split_segments_with_heads` (`policy.rs:2466-2514`) excludes operator tokens from `segment.tokens` and FLUSHES `current` into a `Segment` the instant one arrives, so such a token would split `git >/dev/null push --force origin main` into `[git]` and `[push, --force, origin, main]`, and the leading `git` resolves `Governed` with an empty argv, which `classify_git` answers `Allow` for. **`19-28`'s SEGMENT-COUNT pins assert exactly ONE `Segment` with exactly the surviving argv for that row and for the `x2>` row; a red one is a finding about the FIX, reported with the observed segment list, and never an assertion to edit.** The target travels on the SEGMENT, the way `Segment::redirection_unresolvable` already does, and `segment.tokens` stays byte-for-byte what it is today."
      category: safety
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS`, remove any character from it, or change `is_separator`. `policy.rs:2297` has ONE commit in the whole phase (`84a9b05`), `policy::is_separator(\">\")` stays FALSE by construction, `separators_are_named_in_one_list_that_the_predicate_reads` stays GREEN and UNMODIFIED, and round 6's over-deletion control `git x2>/tmp/o push --force origin main` stays PERMITTED. **The deletion model's BEHAVIOUR does not change at all — the target still produces no argv word and the scan index still advances exactly as far.** Only its by-product is kept."
      category: safety
    - statement: "MUST NOT record a heredoc DELIMITER, a here-STRING or an fd NUMBER as a pathname redirection target. `redirection_operator_len` (`policy.rs:2968-2992`) is a finite closed grammar and the pathname/non-pathname split is derived AT THE OPERATOR in the same match that already computes its length — `<<`, `<<-`, `<<<`, `>&` and `<&` take no path. Recording one would over-refuse on a shape that names no file, and `19-28`'s heredoc row pins what `git <<EOF push --force origin main` answers."
      category: safety
    - statement: "MUST NOT shell out to `git` or anything else from the guard's own path; MUST NOT call `canonicalize`, `read_link`, `metadata`, `exists`, `symlink_metadata`, `current_dir`, `current_exe` or any other filesystem or process API from the carrier predicate, from `tokenize`, from `split_segments_with_heads` or from `classify_segments`' per-segment loop; and MUST NOT read a config file or consult the environment at guard time. **`std::env::current_exe()` is resolved ONCE in `guard` and handed to `guard_in` EXPLICITLY**, the way `install_in` already takes its `binary` (`hooks.rs:103-108`: under `cargo test` `current_exe()` is the test binary). The predicate stays PURE and normalises LEXICALLY, following no link. Every probe lives in a TEST."
      category: safety
    - statement: "MUST NOT implement the widened refusal as a second pass over the segments, a second scan of the argv, a filter after `resolve_program_with_head` returned, a new arm in `resolve_program`, a change to `ProgramResolution`, or a THIRD reading site of any kind. It is raised ONCE at the top of `classify_segments`' existing per-segment loop, on the segment that loop already holds, BEFORE the resolution match, reading two fields of that one segment. `resolve_program`, `resolve_program_with_head`, `first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api`, `scan_leading`, `forbidden_repo_path` and `forbidden_repo_prefixes` are NOT touched. **If the fix needs a second site or a second scan, STOP and report it as a finding.**"
      category: safety
    - statement: "MUST NOT write the BINARY half as a DIRECTORY PREFIX. It is an EXACT-PATH comparison against the one path `guard` resolved, component-wise after the same lexical normalisation the directory half uses. `19-28` pinned `cp /bin/true <BINARY-PARENT>/some-other-file` and `ls <BINARY-PARENT>` PERMITTED, and a prefix over `~/.cargo/bin` would refuse `ls ~/.cargo/bin` and every `cargo install`. **A red near-miss control is a finding about the RULE.**"
      category: safety
    - statement: "MUST NOT refuse a word that is not ABSOLUTE, not LITERAL, or that reaches either protected path only through a symlink the rule would have to follow — in EITHER word class. `19-28` pinned the tilde, glob, brace, expansion-borne, symlinked and relative rows PERMITTED in both operand and redirection-target position, and **a rule that turns any of them red is a rule that quietly widened**: report it as a finding about the RULE and never edit the assertion. Refusing every non-literal operand of an ungoverned command would deny `rm $TMPDIR/x`; following a link is I/O and TOCTOU on the guard path."
      category: safety
    - statement: "MUST NOT write a rule, a pin or a doc claim for the TILDE, GLOB or BRACE spellings. A rule would have to perform expansions the guard is forbidden to perform — a tilde needs the environment, which `19-25`'s standing prohibition forbids at guard time, and a glob needs the filesystem, which the latency and TOCTOU rules forbid. **`T-19-115`'s remedy is the residue's ARITHMETIC: state the CONDITION rather than enumerating spellings, or enumerate all seven.** Recording it as unfixed at its measured severity is the honest answer and no acceptance is made for it."
      category: honesty
    - statement: "MUST NOT add a new `ParkReason` variant, and MUST NOT use `HookBypassBlocked` or `PrCapExceeded` for either new refusal. A command naming a path this run's controls live in, and a ledger too large for the count to be established, are both `ParkReason::EnvelopeAssertionFailed` — the general unresolvable identifier the sibling refusals already carry and the one `19-28`'s rows are asserted at. `HookBypassBlocked` names a config-KEY mechanism and `PrCapExceeded` names a cap that fired; attributing either refusal to a mechanism that did not produce it is D-24, the defect `19-21`'s executor reported as a blocker."
      category: safety
    - statement: "MUST NOT quote the command back in any refusal message (SAFE-04). Naming the envelope DIRECTORY, the BINARY's own path and the LEDGER's path is permitted and required, on the same footing as `scan_leading`'s refusals naming a key and a section: all three are paths this binary generated or resolved for itself and none carries a secret. **Each recovery step must be one the agent or a human can act on** (AR-19-11) — name a path outside the protected set; archive the oversized ledger — and `git config --get core.hooksPath` still reports the directory."
      category: safety
    - statement: "MUST NOT claim in any doc, message or record that the widened rule is fail-closed, that it CLOSES `T-19-112`, `T-19-113`, `T-19-115`, `T-19-116`, `T-19-117` or `T-19-118`, or that its silence is a refusal. **It fails OPEN in SEVEN named directions over TWO word classes and TWO paths, and NOT ONE of them has an automated control.** Direction (i) is NARROWED to those seven and is not closed; directions (iii) and (iv) keep their measured partial mitigations and the word is NARROWED. All of it goes in the predicate's doc, the refusal's doc, `cred.rs`'s WHAT IS NOT COVERED and the record, in those terms. **MUST NOT hand any of them to a pin, a schedule, a version witness or any other control**, and MUST NOT create a revisit condition for them — `19-25`'s witness schedules a re-audit against a FUTURE GIT, and these are reachable by the agent TODAY."
      category: honesty
    - statement: "MUST NOT leave `SECTION_ENVELOPE`'s FIRST `Guaranteed` clause making an unqualified affirmative claim about ambient credentials, and MUST NOT repair it by enumerating the residue. **MUST NOT raise the token cap** asserted by `the_honesty_statement_stays_short_enough_that_a_reader_finishes_it` — the constant is at 213 of 215 and the headroom is TWO tokens. MUST NOT move or wrap through any of the three phrases whose ORDER is pinned at `advisory.rs:206-217`, and **`cannot reach your ambient git credentials` sits INSIDE the clause being repaired and must survive verbatim, first and unwrapped.** MUST NOT indent with literal spaces rather than `\\x20` escapes, and check the RENDERED constant rather than the source. If the repair does not fit, shorten by dropping a CLAIM and never a LIMITATION; a LIMITATION may be GENERALISED to free tokens, which makes it shorter and wider at once."
      category: honesty
    - statement: "MUST NOT edit any assertion over `SECTION_ENVELOPE` other than the ONE `19-28` named in advance and any `tests/envelope_advisory.rs` assertion MEASURED to pin the credential clause literally. Enumerate the complete hit set with `grep -rn` BEFORE the text moves. The phrase-ORDER assertion, the token cap, `envelope_notice`'s pin and `driver/dry_run.rs`'s consumers must all pass UNMODIFIED. An assertion that cannot pass is a finding about the WORDING to report and re-word — never an assertion to edit."
      category: honesty
    - statement: "MUST NOT bound the ledger read by reading LESS of the file — no tail read, no line cap, no sampling. `tally`'s own doc states the invariant: *\"a malformed line in a cap ledger is an attempt that demonstrably happened — forgetting it under-counts, and under-counting is the one failure this file exists to prevent.\"* **The bound REFUSES rather than reads less**, its value is DERIVED from `19-28`'s measured curve against `GUARD_TIMEOUT_SECS` with the margin stated — never from the caps, which are unclamped (`C-15`) and which a long-lived envelope legitimately exceeds over months — and the over-refusal is disclosed from both sides against `19-28`'s just-under-the-bound control."
      category: safety
    - statement: "MUST NOT open any part of `src/envelope/ledger.rs` other than `record_and_check_in`'s read and the new bound with its constant and doc. `tally`, `append_entry`, `ends_mid_line`, `parse_stamp`, `saturating_bump`, `ledger_path`, `ledger_path_in`, `LedgerEntry`, `Tally`, `CapVerdict` and `WINDOW_SECS` are untouched, and the exception is stated in the SUMMARY and the record AS an exception. This is the first plan in the phase to open this file."
      category: scope
    - statement: "MUST NOT open any part of `src/envelope/mod.rs` other than `ENVELOPE_ROOT_ENV`'s residual-limit paragraph at `:165-168`, changing **zero non-doc lines**, verified by `git diff --unified=0` over the file filtered for lines that are not `///` and not blank returning NOTHING. `envelope_root`, `envelope_dir`, `envelope_dir_in`, `APP_SUBDIR`, `ENVELOPE_SUBDIR`, `ParkOutcome` and every other item are untouched, and `ENVELOPE_ROOT_ENV`'s VALUE and behaviour do not change — audits 9 and 10 both measured the variable INERT from inside a Bash command."
      category: scope
    - statement: "MUST NOT open any part of `src/envelope/cred.rs` other than the `hooks_path_env` reach and limit paragraphs and, IF AND ONLY IF `19-28` recorded the empty-helper control reproducing at an acceptable cost, the one injected pair and its doc. `write_gitconfig`, `write_gitconfig_in`, `gitconfig_body`, `write_askpass_stub_in`, `configured_remote_host`, `EnvelopeEnv` and every other item are untouched. **If the branch is NOT taken, `cred.rs` changes ZERO non-doc lines**, verified by the filtered `git diff --unified=0` returning nothing, and the SUMMARY says which branch was taken and why."
      category: scope
    - statement: "MUST NOT open any part of `src/envelope/advisory.rs` other than `SECTION_ENVELOPE`'s literal and its doc comment. `envelope_notice`, `protection_line`, `PROTECTION_WARNING`, `PROTECTED_CLAIM`, `ProtectionState` and every other item are untouched. **MUST NOT delete a residual disclosure** — G-19-1's compression dropped exactly two secondary clauses as redundant and dropped no limitation."
      category: scope
    - statement: "MUST NOT open any part of `src/envelope/hooks.rs` other than `guard`, the signatures of `guard_in` and `classify_segments`, and the call site inside `classify_segments`' per-segment loop with its comment. `pre_push`, `pre_commit`, `deny`, `read_guard_request`, `park_refusal`, `write_stub`, `stub_body`, `install`, `install_in`, `hooks_dir_in`, `assert_provenance`, `assert_provenance_in`, `settings_value`, `settings_json`, `guard_command`, `write_settings`, `write_settings_in`, the D-07 second-carrier table and `mod tests` must show ZERO diff lines, asserted by reading the diff. **In particular MUST NOT repair the `settings_json` second-carrier row** — repairing a delivery is a change to the SPAWN SEAM and is out of this round's scope, and `C-08`'s behavioural half stays UNMEASURED and claimed in neither direction."
      category: scope
    - statement: "MUST NOT edit or delete a row of `tests/envelope_carrier_reach.rs`, `tests/envelope_control_carrier.rs`, `tests/envelope_wrapper_class.rs`, `tests/envelope_reparsed_value.rs`, `tests/envelope_config_resolution.rs`, `tests/envelope_callee_grammar.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs` or `tests/envelope_pr_cap.rs` to make a fix pass. Those are `19-28`'s and earlier rounds' evidence. If a row cannot be satisfied, that is a finding about the RULE: report it with the row, the expected verdict and the measured one, and change the rule or report the plan as inconclusive — never the assertion. **The permitted test writes are ADDITIONS ONLY**: new `#[test]` fns in `tests/envelope_carrier_reach.rs` and in `src/envelope/policy.rs`'s own `#[cfg(test)] mod tests`, plus the ONE `SECTION_ENVELOPE` assertion `19-28` named in advance. A deletion anywhere under `tests/` is a finding about the rule."
      category: honesty
    - statement: "MUST NOT fold the new pins into an existing `#[test]` fn. This is load-bearing arithmetic rather than style: turning a failing test green leaves `passed + failed` unchanged, so NEW `#[test]` fns are the ONLY thing that makes this plan's gate arithmetically satisfiable. Folding them in makes the gate unsatisfiable — round 3's failure mode, which only execution caught."
      category: honesty
    - statement: "MUST NOT place a `#[cfg(test)]` line anywhere in `src/envelope/policy.rs` above the existing sentinel, or a second one anywhere in `src/envelope/hooks.rs` or `src/envelope/ledger.rs` above theirs. The anti-vacuity stripper truncates its view at the FIRST one and each file's sentinel-count assertion forbids a second. New test-only constants belong inside the existing `#[cfg(test)] mod tests`."
      category: safety
    - statement: "MUST NOT clear, repurpose or widen `Token::literal`, `Token::expansion`, `Token::word_splitting_flush`, `Token::brace_splice`, `Token::redirection_unresolvable`, `Segment::head_is_command_position`, `Segment::brace_spliced`, `Segment::splice_can_produce_governed` or `Segment::redirection_unresolvable`. **`Token::literal` is READ IN A THIRD PLACE by this round**, so a change there now silently moves three rules at once as well as turning rounds 5 and 6's verdict pins vacuous. New fields are ADDITIVE and every existing field keeps its present meaning exactly."
      category: safety
    - statement: "MUST NOT lower `POLICY_MIN_PRODUCTION_BYTES` or `HOOKS_MIN_PRODUCTION_BYTES`, MUST NOT weaken the proportional floor's VALUE, the deep anchor or the sentinel-count assertions, and MUST NOT economise on `policy.rs`'s prose to protect a floor. **Correcting `842` to `421` in the floor's COMMENT is permitted and required; changing the floor's value or its strictness is not**, and every other number in that comment was re-derived by audit 10 and is right."
      category: safety
    - statement: "MUST NOT fix, narrow, re-scope or re-classify `T-19-86`, and MUST NOT fold `T-19-111` into it — `19-27`'s five-site attribution correction stays as performed. Its four registered rows and its persisted-alias arm stay at exit 0 and its pins stay green UNMODIFIED. MUST NOT close, re-scope or add a remedy for `T-19-91`, and do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push` or extend the denylist. MUST NOT write a rule for `T-19-111`. MUST NOT fix `T-19-96` or `T-19-110`, and MUST NOT re-open `T-19-74`, whose core rows are frozen."
      category: scope
    - statement: "MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85`. Do not move `park_refusal` above the `?` writes in `deny`, do not add park-coverage controls, and do not touch `scan.rs` or `config.rs`. The six named exceptions above are the whole of this plan's reach into `src/`."
      category: scope
    - statement: "MUST NOT write a rule, a pin or a doc claim for `.git/config`, `.claude/settings.json`, `.git/info/exclude`, the run journal or `~/.config/gsd-meta-manager/config.json`, and MUST NOT clamp `pr_cap_per_24h` or `pr_cap_per_run`. `C-11` … `C-15` keep control (e); `19-28` RECORDED every one of those rows precisely because no rule is planned, and a rule appearing here would make those recorded rows wrong in a file this plan may only ADD to. Clamping a configured cap is a PRODUCT decision, not a guard rule."
      category: scope
    - statement: "MUST NOT remove `--hostname` from `FORGE_VALUE_OPTS`, and MUST NOT fix, pin or expand into the `glab --host` cell or `subcommand_word_indices`. Audit 9 re-measured and overturned audit 8's suggestion: removal moves a counted creation form to UNCOUNTED (`T-19-35`). **`glab` is confirmed NOT INSTALLED**, so a pin over that cell cannot run against its real callee and a pin that skips is a fail-open pin."
      category: scope
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, MUST NOT create `AR-19-13`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere. MUST NOT un-accept, re-rate or renumber `AR-19-04` or `AR-19-05`; `AR-19-04`'s reasoning gap is RECORDED beside it and nothing more. **MUST NOT mark `T-19-23` closed or re-closed** — whether `T-19-118`'s repair closes it is audit 11's judgement. Accepting or un-accepting a risk is a human decision and eleven agents have deliberately left `T-19-17r`'s unmade."
      category: honesty
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock`. Every probe uses `std::process::Command` inside `#[cfg(test)]`."
      category: scope
    - statement: "MUST NOT report a green run of any control without `19-28`'s recorded RED output beside it in the SUMMARY, and MUST NOT create a control and its fix in the same commit. The four commits here are all fixes; the RED is `19-28`'s and is confirmed still red before the first of them."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Gate on `passed + failed` against the exact total `19-28-SUMMARY.md` records, and state the arithmetic: a red test RAN, so red→green leaves the total unchanged and any increase comes ONLY from new `#[test]` fns. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy` (D-34). **All SEVENTEEN `envelope_*` binaries must have RUN.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes, and MUST NOT treat their absence from a run as a regression. **The `envelope_tracer` ETXTBSY race is `C-10`'s own seam and this round changes what the guard does about that seam**, so a firing here is RECORDED with its output and its relation to this round's change stated in neither direction. MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lints in `src/browser.rs` and `src/project_creator.rs`; the gate is `cargo clippy -- -D warnings`."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, the plan-19-21 blocker-row resolution record, or any subsection plans 19-13 … 19-28 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-29 execution record."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere, and MUST NOT write that `T-19-116`, `T-19-117` or `T-19-118` is CLOSED. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86`, `T-19-91`, `T-19-111` and `T-19-112` all remain OPEN at `high`. **State plainly that this plan does NOT clear `/gsd-secure-phase 19`.**"
      category: honesty
  artifacts:
    - path: "src/envelope/policy.rs"
      provides: "a pathname REDIRECTION-TARGET channel produced by the same walk that already skips the target — `skip_redirection_target` returning the target's TEXT and LITERALNESS alongside its extent with its stale doc sentence corrected, a pathname/non-pathname split derived at the operator in `redirection_operator_len`'s own closed grammar, and a `Segment` field accumulated in `split_segments_with_heads`' existing operator arm exactly the way `Segment::redirection_unresolvable` already travels, with `segment.tokens` byte-for-byte unchanged; the carrier predicate widened to TWO word classes and TWO paths — the envelope directory as a PREFIX and the guard's own binary as an EXACT PATH — still pure, still lexical, still following no link; the residue restated as SEVEN directions over two word classes and two paths, stated as a CONDITION rather than an enumeration and handed to no control; the refusal naming whichever protected path matched, never quoting the command back, with an actionable recovery step; the floor comment's `842` corrected to `421`; and new `#[test]` fns inside the existing `mod tests`"
      contains: "envelope_carrier"
    - path: "src/envelope/hooks.rs"
      provides: "`guard` resolving `std::env::current_exe()` ONCE and handing it to `guard_in` explicitly the way `install_in` already takes its `binary`, `guard_in` and `classify_segments` threading it, and the ONE existing call site widened to read the segment's redirection targets and the two-path set — raised at the same place before the resolution match, on the same channel at `ParkReason::EnvelopeAssertionFailed`, with no new variant, no second pass and no third reading site — and NOTHING ELSE in the file, verified by reading the diff for zero lines in every other named item"
      contains: "current_exe"
    - path: "src/envelope/ledger.rs"
      provides: "a size bound on `record_and_check_in`'s read that fails CLOSED, with its value DERIVED from `19-28`'s measured `tally` curve against `GUARD_TIMEOUT_SECS` and the margin stated, its refusal carrying `ParkReason::EnvelopeAssertionFailed` rather than `PrCapExceeded` (D-24) and naming the file with an actionable recovery step (AR-19-11), and a doc stating why a TAIL read is the wrong bound against `tally`'s own no-under-count invariant — and NOTHING ELSE in the file"
      contains: "GUARD_TIMEOUT"
    - path: "src/envelope/cred.rs"
      provides: "the reach and limit paragraphs carrying the FILE-carrier family's SEVEN fail-open directions over two word classes and two paths, D-09's ceiling naming the BINARY route as well as the file and environment routes, and `T-19-118` recorded with `AR-19-04`'s reasoning gap beside it — plus, IF AND ONLY IF `19-28` recorded it reproducing at an acceptable cost, one injected `credential.helper` pair with an empty value and its doc; if the branch is not taken the file changes zero non-doc lines and the SUMMARY says which branch and why"
      contains: "T-19-118"
    - path: "src/envelope/advisory.rs"
      provides: "`SECTION_ENVELOPE` with the FIRST `Guaranteed` clause repaired so it is TRUE against measurement — stating what it covers, making no completeness claim and enumerating nothing — with `cannot reach your ambient git credentials` surviving verbatim, first and unwrapped, the three pinned phrases in their pinned order, `\\x20` indentation, the token cap UNRAISED at 215 and no residual disclosure dropped, and any limitation GENERALISED rather than removed if tokens must be freed"
      contains: "ambient git credentials"
    - path: "src/envelope/mod.rs"
      provides: "`ENVELOPE_ROOT_ENV`'s residual-limit paragraph at `:165-168` corrected — D-09's last uncorrected home — so it no longer argues that a relocation *\"does not lower a boundary that was standing\"* on the ground that the same party could unset `GIT_CONFIG_COUNT`, since `T-19-116` lowers layer 2 and layer 3 without controlling the environment the TUI starts in at all; **zero non-doc lines changed** and the variable's value and behaviour unaltered"
      contains: "T-19-116"
    - path: "tests/envelope_carrier_reach.rs"
      provides: "ADDITIONS ONLY — new `#[test]` fns pinning the ORDERING of the widened refusal against the existing ones at deliberately different identifiers, the redirection-target row that an operand-only rule would miss, the binary row that a directory-prefix rule would over-refuse, the ledger-bound row beside its just-under-the-bound control, the `SECTION_ENVELOPE` assertion `19-28` named in advance as this plan's one pre-authorised edit, and — if the branch was taken — the empty-helper rows measured before they were written"
      contains: "ordering"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-29 execution record, carrying the statement FIRST that the gate is not cleared and that nothing here is described as a closure; `19-28`'s RED rows with their before and after verdicts and its PERMITTED rows re-checked; the design answer with each half's reason and the direction-(i) question answered with the SEGMENT-COUNT measurements that make it falsifiable; the SEVEN fail-open directions over two word classes and two paths, handed to no control; `C-10`'s partial rule with what it does not reach stated at the same weight; the `SECTION_ENVELOPE` repair with the complete pin hit set enumerated before the text moved and the token arithmetic stated; which `T-19-118` branch was taken and why; the ledger bound with its derivation and its over-refusal disclosed from both sides; `mod.rs:165-168` corrected; the SIX named file exceptions stated AS exceptions with their bounds and the verification each held; the `421` correction; `AR-19-04`, `AR-19-05` and `T-19-23` carried forward untouched; `T-19-17r` OUTSTANDING for the twelfth time; and the gate arithmetic"
      contains: "19-29"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-116`, `T-19-117` and `T-19-118` recorded NARROWED-NOT-CLOSED with the clause that narrowed each and the directions that remain; `T-19-115` recorded OPEN at `medium` with NO rule written and the reason; the seven-direction residue replacing the four everywhere this plan states it; `C-10`'s control letter updated to (a)-exact-path-partial; the `T-19-118` branch recorded; the ledger bound recorded with its derivation; `mod.rs:165-168` recorded corrected; and `T-19-86`, `T-19-91`, `T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-74`, `C-08`, `C-15`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open"
      contains: "T-19-116"
  key_links:
    - from: "a path named after a redirection operator on a line the guard reads"
      to: "`ParkReason::EnvelopeAssertionFailed` before any classifier and before the ledger write"
      via: "a target text and literalness kept by the walk that already skips it, carried on the `Segment` the way `Segment::redirection_unresolvable` travels, with `segment.tokens` byte-for-byte unchanged"
      pattern: "redirection_target"
    - from: "a redirection-target token pushed into the token stream"
      to: "a red SEGMENT-COUNT pin rather than `git >/dev/null push --force origin main` silently becoming a permit"
      via: "`19-28`'s assertions over `split_segments_with_heads` that the redirected simple command is exactly ONE `Segment` with exactly the surviving argv, green before and after"
      pattern: "segment_count"
    - from: "the binary every hook stub and the guard registration execs"
      to: "an EXACT-PATH clause that refuses its absolute literal spelling and says what it does not reach"
      via: "`current_exe()` resolved once in `guard` and threaded explicitly, compared component-wise by a predicate that touches no filesystem, with the near-miss controls over the binary's parent pinned PERMITTED"
      pattern: "exact_path"
    - from: "a `Guaranteed` clause about ambient credentials that measurement falsifies"
      to: "an honesty statement that is true when this round ends and claims no completeness"
      via: "the repair under the pinned-phrase order, the `\\x20` rule and an unraised 215-token cap, with the complete pin hit set enumerated before the text moved and a limitation generalised rather than dropped if tokens must be freed"
      pattern: "ambient git credentials"
    - from: "an append-only ledger a permitted command can inflate past the guard's registered deadline"
      to: "a refusal derived from the deadline rather than a read that sees less"
      via: "a fail-CLOSED size bound in `record_and_check_in` whose value comes from `19-28`'s measured curve, with the just-under-the-bound control still permitting and still counting"
      pattern: "GUARD_TIMEOUT"
---

<objective>
**The rule: two paths and two word classes — and the honest statement that its silence is a permit in seven directions, not four.**

`19-28` widened the corpus onto the cells audit 10 left on the tenth plane, observed it RED and committed
it with zero `src/` hunks. This plan writes the rules and the honesty repairs and is gated on that
recorded RED state. **Its first action is confirming those rows are still red, and its second is
confirming `19-28`'s PERMITTED rows are still permitted** — because a rule that turns any of them red is
a rule that quietly widened past the boundary it declares.

Audit 10 named the defect, the order and the honest limit:

> **`T-19-116` FIRST among the fixes, because it is the widest and the cheapest to REGISTER.** … A rule
> is genuinely hard — the binary is outside the envelope root and its path is `current_exe()` — and may
> honestly end in an acceptance; **that acceptance is a human decision and this audit does not make it.**

> **`T-19-118`.** Either the `Guaranteed` credential clause gains the same kind of repair the cap clause
> just got, or the generated `gitconfig` gains a protection; **the current pair cannot both stand.** Note
> that rule (a) already refuses four of its six write spellings, so the remaining gap is direction (i) —
> **which means this row and `T-19-112` share a residue and a fix for one is a fix for the other.**

> **`T-19-115`**: correct the residue's own arithmetic. Say what the condition is (a word the shell may
> rewrite) rather than enumerating four spellings of it, or enumerate all seven.

## The design question, answered — BOTH, plus a third thing that is neither

| Cell | Widen the PATH SET | Widen what the rule SEES | Neither |
|---|---|---|---|
| `T-19-116` (`C-10`) | **YES** — the guard's own binary, as an **EXACT PATH** | inherits every existing silence over a second path | — |
| `T-19-118` (`C-05` via (i)) | — | **YES** — pathname REDIRECTION TARGETS, on a channel `Segment::tokens` never holds | plus the `Guaranteed` clause repair, and the injected empty-helper control **if `19-28` measured it reproducing** |
| `T-19-115` (tilde/glob/brace) | — | **NO** — a rule would need expansions the guard is forbidden to perform | the residue's **ARITHMETIC**: four becomes seven, stated as a CONDITION rather than a list |
| `T-19-117` (ledger size) | — | — | a size bound in `ledger.rs` that fails **CLOSED**, derived from the DEADLINE |

**The two boundary kinds differ for a measured reason.** The envelope directory is a PREFIX boundary
because this envelope owns every byte under it. **The binary is an EXACT-PATH boundary because its
directory is shared with everything else the user installed** — a prefix over `~/.cargo/bin` would refuse
`ls ~/.cargo/bin` and every `cargo install`, and `19-28` pinned `cp /bin/true
<BINARY-PARENT>/some-other-file` and `ls <BINARY-PARENT>` PERMITTED so that a clause written the wrong
way turns red.

## Does closing direction (i) mean the deletion model must read redirection targets? No

**Round 6's model answers *which words ARRIVE at the program*. A redirection target does not arrive, and
that answer does not change.** Rule (a) asks a different question about the same walk: *does this line
NAME a path this run's controls live in?*

1. **`skip_redirection_target` (`policy.rs:3007-3057`) already walks the target's full extent**, applying
   the target's own quoting rules, in order to skip it. Its doc's *"Its text is never needed, only its
   extent"* is the sentence that becomes false — and correcting a stated reason that went stale, rather
   than quietly outgrowing it, is the WR-02 discipline this file already names.
2. **`redirection_operator_len` (`:2968-2992`) is a finite, closed grammar of twelve operators**, and
   only some take a PATHNAME. `<<` and `<<-` take a heredoc DELIMITER, `<<<` a here-STRING, `>&` and
   `<&` an fd number. The split is derived AT THE OPERATOR, in the match that already computes its
   length.
3. **`split_segments_with_heads` (`:2466-2514`) excludes every operator token from `segment.tokens` and
   flushes `current` into a `Segment` the instant one arrives.** So a redirection-target token pushed
   into the stream splits `git >/dev/null push --force origin main` into `[git]` and
   `[push, --force, origin, main]`, and the leading `git` resolves `Governed` with an empty argv, which
   `classify_git` answers `Allow` for. **The obvious implementation silently converts round 6's headline
   refusal into a permit.**

**So the target travels on the SEGMENT instead, in a shape already in the file.**
`Segment::redirection_unresolvable` (`:2402-2418`) is a fact about the COMMAND rather than a word,
accumulated in the operator arm of the ONE walk (`:2461-2505`), applied retroactively over
`segments[command_start..]`, and reset at each real command operator. The redirection-target channel is
that shape exactly — **and `segment.tokens` stays byte-for-byte what it is today**, which is what makes
it mechanically impossible for `resolve_program`, `resolve_program_with_head`,
`first_unreadable_decision_word`, `config_key_operand_index`, `subcommand_word_indices`, `scan_gh_api` or
`scan_leading` to see anything new.

**The cost, stated rather than discovered:** one additive `Token` field and one additive `Segment` field,
compiler-enforced across six `Token { … }` and two `Segment { … }` construction sites; a return-type
change on `tokenize`, private with four call sites; a corrected doc sentence; and a pathname/non-pathname
split of a grammar the file already enumerates. **`SEPARATORS` does not move, `is_separator(">")` stays
`false`, the deletion model's behaviour is unchanged, and round 6's over-deletion control stays
PERMITTED.** Round 3's principle is discharged rather than weakened: rule (a) is still raised at ONE
site, on ONE segment, reading two of its fields. **If the fix needs a second scan or a second reading
site, that is a finding.**

`19-28`'s SEGMENT-COUNT pins are what make that claim falsifiable, and they are green today.

## And the honest part — the residue is SEVEN, not four

Rule (a) decides on four conditions and each is also a silence. **Round 5's own literalness table names
EXPANSION, PATHNAME, TILDE and BRACE as the four classes that clear `Token.literal`**, so the rule
inherits round 5's whole class list by construction — and the docs name only the `$`-shaped one.

1. **An EXPANSION-BORNE word** — `D=$(git config --get core.hooksPath); rm -f $D/../pr-ledger.ndjson`,
   `cp /bin/true $(command -v gsd-meta-manager)`. The location is fetched by a PERMITTED GOVERNED READ
   and acted on by an ungoverned command.
2. **A SYMLINK is not followed.** NARROWED: `ln -s <env>/… /tmp/l` names an envelope path as its own
   operand and is refused, so a link must predate the run or be made by a means naming no protected path.
3. **A RELATIVE word is not resolved.** NARROWED the same way and no further.
4. **A TILDE.** `rm -rf ~/.local/share/gsd-meta-manager/envelope/alpha` is exit 0 while its absolute twin
   is exit 2. Resolving it needs the environment, which the guard may not read.
5. **A GLOB.** `rm -rf <env>/alph?` takes nine carriers in one call. Resolving it needs the filesystem.
6. **A BRACE LIST.** `rm -f <env>/alpha/{pr-ledger.ndjson,x}`.
7. **A REDIRECTION whose target is any of the six above** — because direction (i) stops being a direction
   of its own and becomes a **second WORD CLASS the other six apply over**.

**Each silence now applies over TWO word classes and TWO paths. The path set grew, the word set grew, and
the silences did not shrink.** They go in the predicate's doc, the refusal's doc, `cred.rs`'s WHAT IS NOT
COVERED and the record — **stated as a CONDITION with instances rather than as a complete list**, which
is what `T-19-115` is about — and **none is handed to a pin, a schedule or a witness.** `19-25`'s witness
schedules a re-audit against a FUTURE GIT; these are reachable by the agent TODAY, so a schedule would
observe the wrong thing entirely.

## `C-10` gets a rule, and the rule is partial and says so

`cp /bin/true <binary>` at its absolute literal spelling is refused after this round — the spelling audit
10 drove end to end, the one that moved a bare remote's `main` `01f9664` → `2537e97` and made the guard
itself answer exit 0. **What it does not reach is stated with the same weight**: an expansion-borne
spelling, a tilde spelling, a relative spelling, and a link on `PATH` whose target `current_exe()`
reports instead. `19-28` measured whether `current_exe()` agrees with the path the stub and
`guard_command` baked in, and the answer is on the record. **An unresolvable exe path makes the binary
half silent, which is fail-open and is stated rather than left to be discovered.**

## What this plan is NOT

**`/gsd-secure-phase 19` is NOT cleared by this plan.** `T-19-86` remains open at `high` — with
`T-19-111` moved OUT of it as `19-27` moved it — `T-19-91` remains open at `high`, and `T-19-111` remains
open at `high` because no rule is written for it. `T-19-112` and `T-19-113` are narrowed FURTHER and
still not closed: `C-15` is a route this rule cannot reach and the deferred option (b) another.
**`T-19-115` gets no rule at all. And whether `T-19-116`, `T-19-117` and `T-19-118` close is audit 11's
judgement rather than this plan's claim — this plan says NARROWED and states what remains open.** The
SUMMARY must say all of that first, and must never write an unqualified "T-19-60 is closed".

Purpose: give the widest carrier of the fifteen a control, make the path after a redirection operator a
fact the guard establishes, stop the guard's own deadline being set by a file a permitted command can
inflate — and make the envelope's own first guarantee true when it does.
Output: the redirection-target channel and the two-path predicate at one existing decision point, a
fail-closed ledger bound, four narrow doc corrections in fenced files, a repaired `Guaranteed` claim, the
appended execution record, and a green `--no-fail-fast` gate whose arithmetic is stated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-28-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@src/envelope/hooks.rs
@src/envelope/ledger.rs
@tests/envelope_carrier_reach.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm still RED and still PERMITTED, then the redirection-target channel and the two-path predicate in `policy.rs`</name>
  <files>src/envelope/policy.rs</files>
  <precondition>
    `19-28` is committed and its SUMMARY records the complete RED name list, the complete PERMITTED list,
    the `passed + failed` total, the per-binary counts for all SEVENTEEN `envelope_*` binaries, the
    SEGMENT-COUNT pin measurements, the ledger latency curve and the empty-helper candidate control's
    recorded result. Work in the main checkout with a clean tree — **ISOLATION=none is forced for this
    phase, so do NOT create a worktree.** `cargo build` produces `./target/debug/gsd-meta-manager`. A
    real `git` binary is on `PATH` and `gh` 2.45.0 is installed; no pin may be written to skip when
    either is absent, because a skipped pin is a fail-open pin. `glab` is NOT installed and no pin may
    depend on it.
  </precondition>
  <read_first>
    `19-28-SUMMARY.md` in full — **the RED list, the PERMITTED list, the SEGMENT-COUNT measurements, the
    ledger curve and the empty-helper result are this plan's handoff contract**, and the rule is built
    from those measurements rather than from this plan's text.
    `src/envelope/policy.rs:2158-2355` (`Token` — **`literal`'s own table naming expansion, pathname,
    tilde and brace, which is where the residue's arithmetic comes from** — and
    `redirection_unresolvable`), `:2357-2418` (`Segment` — **`redirection_unresolvable`'s doc is the
    shape this task copies, and `brace_spliced`'s is the same argument one field over**), `:2420-2526`
    (`split_segments_with_heads` — **the operator arm, the flush above it, the retroactive
    `segments[command_start..]` loop and the reset at a real command operator; this is the machinery,
    not a new one**), `:2954-2992` (`redirection_operator_len` — **the twelve-operator closed grammar
    and which of them take a PATHNAME**), `:2994-3057` (`skip_redirection_target` — **the quote handling
    that already produces both the text and the literalness, and the stale doc sentence**), `:3078-3230`
    (`tokenize`'s preamble and `consume_redirection!` — the IO_NUMBER arm, the over-deletion control it
    names, and the unresolvable arm that already pushes a token), `:2297` (`SEPARATORS`, read to leave
    byte-identical), `:3542-3546` (`is_separator`), `:5179-5234` (`lexical_absolute_components`,
    `word_is_within` — **the component-wise comparison the directory half already makes and the
    EXACT-PATH comparison the binary half needs beside it**), `:5236-5413`
    (`envelope_carrier_operand` and `envelope_carrier_refusal` in full — **the four conditions, the FOUR
    directions whose count becomes seven, the no-revisit-condition paragraph, the disclosed cost and the
    rejected options; this is the doc this task rewrites in place rather than replacing**),
    `:5100-5177` (`forbidden_repo_prefixes`, `forbidden_repo_path` — read to leave untouched),
    `:7415-7485` (**the floor comment's `842`, which is the byte-minus-character DIFFERENCE; every other
    number in it was re-derived by audit 10 and is right**).
    `src/envelope/hooks.rs:87-115` (**`install_in`'s doc at `:103-108` on why it takes `binary`
    EXPLICITLY — the precedent for how `guard` hands `current_exe()` to `guard_in`; do NOT edit here,
    that is Task 2**), `:771-816` (the three latency rules), `:954-1030` (`classify_segments` and round
    10's clause, read to confirm the signature change Task 2 makes).
  </read_first>
  <action>
**FIRST, and before any production line moves: confirm `19-28`'s RED set is still RED and its PERMITTED
set is still PERMITTED.** Re-run the two evidence suites and the axis suite against the unmodified tree
with `--no-fail-fast` and record the failing names verbatim. Diff that list against `19-28-SUMMARY.md`'s
recorded RED set. **If any row is already green, STOP and report it as a finding** with the measured
verdict and the reason identifier. Then drive `19-28`'s PERMITTED rows — the seven spelling rows in both
word positions, the expansion-borne, symlinked and relative rows, the near-misses and the two
EXACT-PATH-not-PREFIX binary controls — against the unmodified tree and record them PERMITTED. **That is
the baseline the widening check compares against at the end of this task.** Put the verbatim pre-fix
output in the SUMMARY.

**Then the production work, in `policy.rs` only.**

**1. The pathname redirection-target channel, produced by the walk that already skips the target.**

  - Split `redirection_operator_len`'s twelve operators into those whose target is a PATHNAME and those
    whose target is a heredoc DELIMITER, a here-STRING or an fd NUMBER. **Derive it at the operator, in
    the match that already computes the length**, and state the split in the doc as a property of bash's
    grammar rather than as a list somebody chose.
  - Have `skip_redirection_target` return the target's TEXT and its LITERALNESS alongside its extent,
    computed by the quote handling it already performs. **Correct its doc's *"Its text is never needed,
    only its extent"* sentence rather than leaving it standing false**, and say in the correction why
    the sentence was right when it was written and what changed.
  - Carry the target onto the `Segment` in `split_segments_with_heads`' EXISTING operator arm — the same
    accumulate-and-apply-retroactively-over-`segments[command_start..]` machinery
    `Segment::redirection_unresolvable` and `Segment::brace_spliced` already use, reset at each REAL
    command operator because a redirection belongs to the whole simple command. **`segment.tokens` must
    be byte-for-byte what it is today**, and the field's doc says so and says why: every argv consumer
    reads that vector and a new element in it would move rules four rounds settled.
  - **No token enters the token stream and `SEPARATORS` does not move.** `19-28`'s SEGMENT-COUNT pins are
    the mechanical proof; run them after this step and before going further. **A red one means the target
    reached the stream — a finding about the FIX, reported with the observed segment list, never an
    assertion to edit.**

**2. The predicate widened to TWO word classes and TWO paths.** It takes the segment's own words AND its
pathname redirection targets, and a path SET rather than a single directory: the envelope directory as a
**PREFIX** boundary, compared component-wise exactly as today, and **the guard's own binary as an EXACT
PATH** — equality after the same lexical normalisation, never a prefix over its parent. It stays PURE:
no `canonicalize`, no `read_link`, no `metadata`, no `current_exe`, no process call, no program name and
no filename. **An absent binary path makes that half silent**, which is fail-open and is stated on the
signature rather than left to be inferred.

**3. The doc, rewritten in place in the shape it already has.** Keep every section it carries and change
what measurement changed:

  - **the one-sentence purpose**, now naming both word classes: the `Ungoverned` arm is right about the
    PROGRAM and this predicate narrows it in the PATHS a line NAMES, whether the path stands in an
    operand or after a redirection operator;
  - **the two boundary KINDS and why they differ** — a directory this envelope owns entirely versus a
    file in a directory shared with everything else the user installed, with the near-miss controls named;
  - **the RESIDUE as SEVEN directions over two word classes and two paths, stated as a CONDITION rather
    than an enumeration**: a word the shell may rewrite, a word that is not absolute, a word that reaches
    a protected path only through a link — with the seven spellings named as INSTANCES and the sentence
    that says so, because a list that stops implies a completeness the measurement denies. Say that
    directions (iii) and (iv) keep their measured partial mitigations and that the word is NARROWED; say
    that **direction (i) is NARROWED to these seven and is not closed**; and say that `T-19-115`'s three
    spellings get no rule because a tilde needs the environment and a glob needs the filesystem, both of
    which the guard is forbidden;
  - **the NO REVISIT CONDITION paragraph kept and extended to all seven**, with its reason unchanged:
    `19-25`'s witness schedules a re-audit against a FUTURE GIT and these are reachable TODAY;
  - **the DISCLOSED COST**, now with the binary half's own over-refusal beside the directory half's: a
    run cannot read or write its own envelope directory, and cannot name the binary it is driven by,
    while `git config --get core.hooksPath` and `command -v` both still report their paths;
  - **the REJECTED OPTIONS re-costed**: option (b) for the ledger still costed and DEFERRED with no
    schedule; option (b) for the STUB still structurally unavailable; **option (b) for the BINARY —
    a digest carried in the stub — costed and NOT taken, with both reasons stated**: it would hash the
    binary on every push against the latency rule, and it is carried by `C-02`, the very file a
    replacement can also rewrite, so it defends layer 3 only and defends nothing at layer 2; option (c)
    still dead on a single-uid model;
  - **and the statement that `Token::literal` is now read in a THIRD place**, so a change there moves
    three rules at once.

**4. The refusal, naming whichever protected path matched.** Never quote the command back (SAFE-04);
name the envelope directory or the binary's own path — both are paths this binary generated or resolved
for itself and neither carries a secret, on the same footing as `scan_leading`'s refusals naming a key
and a section. **The recovery step must be actionable** (AR-19-11). It must not reuse
`unbounded_config_assignment_refusal`'s wording or the hooks-path deny's, and the identifier stays
`ParkReason::EnvelopeAssertionFailed` with **no new variant**.

**5. The floor comment.** Correct `842` to `421` at `:7450`, stating that 842 is the byte-minus-character
DIFFERENCE and that each three-byte character contributes two extra bytes. **Every other number in that
comment was re-derived independently by audit 10 and is right — do not touch one of them, and do not
change the floor's value or its strictness.**

**6. The pins, in `policy.rs`'s own `#[cfg(test)] mod tests`** — never above the sentinel and never
introducing a second `#[cfg(test)]` line. **Each a NEW `#[test]` fn**, because turning a failing test
green leaves `passed + failed` unchanged and new fns are the only thing that makes the gate
arithmetically satisfiable:

  - **the pathname/non-pathname operator split, in BOTH directions** — a `>` target and a `>>` target are
    pathname targets; a `<<` delimiter, a `<<<` here-string and a `>&` fd number are not, and the second
    half is what proves the split is real rather than a filter that happened to match;
  - **the target's TEXT and LITERALNESS**, over a quoted target, an unquoted one, a tilde one, a glob one
    and an expansion-borne one, asserting the literalness bit each way;
  - **the two-path predicate in BOTH directions**: every envelope carrier and the binary answer `true`;
    the binary's PARENT, a sibling file beside the binary, `/tmp/x`, `/tmp/pr-ledger.ndjson`, a
    one-character-different root, a sibling directory extending the alias, a relative word, a tilde word,
    a glob word, a brace word and a non-literal token all answer `false`;
  - **the `..`-collapse pins** carried forward in both directions over both word classes;
  - **a NO-FILESYSTEM source pin extended to the new code**: the predicate's and the tokenizer's own
    production text names none of `canonicalize`, `read_link`, `symlink_metadata`, `metadata`,
    `current_dir`, `current_exe` or `exists`, with a failure message saying the correct response is to
    remove the call and never to relax the assertion, and naming the latency and TOCTOU reasons.

Commit as its own commit, the first fix commit. **`hooks.rs` is NOT touched in this commit** — the
channel and the predicate exist and the call site still passes one path, which is deliberate: it makes
Task 2's diff a reviewable one.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --lib --no-fail-fast 2&gt;&amp;1 | tail -30; rtk proxy cargo test --test envelope_carrier_reach --no-fail-fast 2&gt;&amp;1 | tail -30; for t in envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_pr_cap envelope_advisory envelope_control_carrier; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || echo "UNEXPECTED RED: $t"; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c '^#\[cfg(test)\]' src/envelope/policy.rs; rtk proxy grep -n 'const SEPARATORS' src/envelope/policy.rs; rtk proxy grep -c '842 multi-byte' src/envelope/policy.rs; git diff --numstat HEAD~1..HEAD</automated>
  </verify>
  <done>`19-28`'s complete RED set was re-run against the unmodified tree BEFORE any production line moved, the failing names recorded verbatim and diffed against `19-28-SUMMARY.md`, with any already-green row reported as a finding rather than proceeded past; **`19-28`'s PERMITTED rows were driven against the unmodified tree and recorded as the widening baseline**; `redirection_operator_len`'s twelve operators are split into pathname and non-pathname targets AT THE OPERATOR with the split stated as a property of bash's grammar; `skip_redirection_target` returns the target's TEXT and LITERALNESS from the quote handling it already performs, with its stale doc sentence CORRECTED rather than left standing and the correction saying what changed; the target is carried onto the `Segment` in `split_segments_with_heads`' EXISTING operator arm by the same retroactive machinery `Segment::redirection_unresolvable` uses, **with `segment.tokens` byte-for-byte unchanged and no token entering the stream**, and `19-28`'s SEGMENT-COUNT pins were re-run immediately after that step and are GREEN; the predicate now takes both word classes and a path SET with the envelope directory as a PREFIX and the binary as an EXACT PATH, stays pure with no filesystem or process call, and is SILENT when the binary path is absent with that stated on the signature; its doc carries the two boundary kinds and why they differ, **the residue as SEVEN directions over two word classes and two paths stated as a CONDITION with the spellings as instances**, direction (i) marked NARROWED and not closed, `T-19-115`'s three spellings recorded as getting no rule with the reason, the no-revisit-condition paragraph extended to all seven, the disclosed cost with the binary half's own over-refusal, the rejected options re-costed including **option (b) for the BINARY costed and NOT taken with both reasons**, and the statement that `Token::literal` is now read in a third place; the refusal names whichever protected path matched, never quotes the command back, gives an actionable recovery step and keeps `ParkReason::EnvelopeAssertionFailed` with no new variant; `policy.rs:7450` reads 421 with the difference explained and every other number in that comment untouched; the new pins are NEW `#[test]` fns inside the existing `mod tests` covering the operator split in both directions, the target's text and literalness across five spellings, the two-path predicate in both directions, the `..` collapse over both word classes and an extended NO-FILESYSTEM source assertion; `policy.rs` still has exactly ONE `#[cfg(test)]` line and `SEPARATORS` is byte-identical; `hooks.rs` shows ZERO diff lines in this commit; the nine prior evidence files are GREEN and unmodified; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: the ONE `hooks.rs` exception — the binary threaded, the call site widened, and the ordering it establishes</name>
  <files>src/envelope/hooks.rs, tests/envelope_carrier_reach.rs</files>
  <read_first>
    `src/envelope/hooks.rs:87-115` (**`install`/`install_in` and the doc at `:103-108`: under `cargo test`
    `current_exe()` is the TEST binary, which is exactly why `install_in` takes `binary` explicitly. That
    is the precedent this task follows for `guard`/`guard_in`**), `:216-260` (`assert_provenance_in` —
    **read what `T-19-01` covers and what it does not, because the record must state that it is
    unweakened and that it was never a defence of the exec target's bytes**), `:771-846` (the three
    latency rules and `guard`, which already resolves the root and hands `Config::default_path()` down —
    **the site that resolves `current_exe()` ONCE**), `:848-900` (`guard_in`'s signature and its
    `#[allow(clippy::too_many_arguments)]`), `:939-1030` (`classify_segments`' doc, its signature, the
    per-segment loop, round 10's clause with its three-consequence comment — **the one site this task
    widens** — and the paragraph about the segment's CONTEXT travelling with it rather than being
    re-derived), `:1031-1140` (the `Refuse`, `NestedPayload` and `Governed` arms and
    `first_unreadable_decision_word`'s placement comment — **"placing it before the `match` is
    load-bearing in two ways", the argument this clause already reuses**), `:1390-1534`
    (`GUARD_TIMEOUT_SECS`, the D-07 second-carrier table, `settings_value`, `guard_command`,
    `settings_json` — **read to leave untouched, and in particular do NOT repair the `settings_json`
    row**).
    `19-28-SUMMARY.md`'s ordering rows, its SEGMENT-COUNT measurements, its binary rows with their
    EXACT-PATH-not-PREFIX controls, and its redirection-target rows.
    `tests/envelope_carrier_reach.rs`'s sections — **the rows this task must not perturb, and the ONE
    `SECTION_ENVELOPE` assertion `19-28` named in advance as Task 3's permitted edit.**
  </read_first>
  <action>
**1. Thread the binary, once, at the top.** `guard` resolves `std::env::current_exe()` and hands the
result to `guard_in` explicitly, the way `install_in` already takes its `binary` — **and the doc says
why in one sentence: under `cargo test` `current_exe()` is the test binary, so a guard that resolved it
internally could not be driven by the fixture against the built binary.** An unresolvable path is passed
through as absent rather than made an error: the guard's job is to answer, and a guard that refused
because it could not name itself would be a new denial-of-service surface. `guard_in` and
`classify_segments` take it through to the clause. **This is the whole of the signature change and it
introduces no new I/O anywhere below `guard`.**

**2. Widen the ONE existing call site**, at the top of `classify_segments`' per-segment loop, on the
segment the loop already holds, still BEFORE `resolve_program_with_head`, still returning on the existing
channel at `ParkReason::EnvelopeAssertionFailed` with no new variant, no second pass and no third reading
site. It now asks the Task 1 predicate against the segment's words AND its pathname redirection targets,
over the two-path set.

Its comment extends the three placement consequences it already states to a fourth and a fifth, in the
shape `first_unreadable_decision_word`'s own comment uses:

  - the three it already carries — before the resolution match so a GOVERNED carrier is reached, before
    the ledger write so a refusal consumes no PR-cap budget, and re-raised at depth through
    `NestedPayload`'s re-entry;
  - **fourth: it reads the segment's redirection targets from a field the ONE walk filled**, not from a
    second scan, and `segment.tokens` is unchanged, which is why no rule four rounds settled can move;
  - **fifth: the two paths carry two different boundary KINDS**, a prefix and an exact path, with the
    reason and the near-miss controls named.

**Nothing else in `hooks.rs` moves.** Verify by reading the diff that `pre_push`, `pre_commit`, `deny`,
`read_guard_request`, `park_refusal`, `write_stub`, `stub_body`, `install`, `install_in`, `hooks_dir_in`,
`assert_provenance`, `assert_provenance_in`, `settings_value`, `settings_json`, `guard_command`,
`write_settings`, `write_settings_in`, the D-07 second-carrier table and `mod tests` all show ZERO diff
lines. **In particular do NOT repair the `settings_json` second-carrier row.**

**3. Then re-run `19-28`'s corpus and check BOTH directions of the handoff.** Every RED row green at the
identifier the corpus derived, and **every PERMITTED row still permitted** — the seven spelling rows in
both word positions, the expansion-borne, symlinked and relative rows, the near-misses, and the two
EXACT-PATH-not-PREFIX binary controls. A permitted row that has turned red is a rule that quietly
widened — **report it as a finding about the RULE with the measured verdict, and never edit the
assertion.** Re-run the SEGMENT-COUNT pins and every round-4-through-10 mechanism pin and record them.

**4. ADDITIONS ONLY to `tests/envelope_carrier_reach.rs`** — new `#[test]` fns, never folded into an
existing one, because the gate arithmetic depends on it. **Measure each before writing it, and RECORD
rather than assert any whose post-fix identifier cannot be derived from the placement:**

  - **the ORDERING pins, at deliberately DIFFERENT identifiers**, which are the mechanical proof the
    clause is still raised in the one per-segment walk rather than in a second pass: a redirection target
    under the envelope root composed with a `--force` push in both orders, and a binary operand composed
    with a `--force` push in both orders, each with an outside-the-protected-set control;
  - **the REDIRECTION-TARGET row an operand-only rule misses**, with a comment stating that this row
    exists to turn an operand-only rule red;
  - **the BINARY row a directory-prefix rule over-refuses**, paired by name with the two near-miss
    controls, with a comment stating that this pair exists to turn a prefix rule red;
  - **a NESTED-PAYLOAD row over the redirection channel**, recording that the same clause reached it
    through `NestedPayload`'s re-entry rather than a second mechanism.

Commit as its own commit, the second fix commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_carrier_reach --no-fail-fast 2&gt;&amp;1 | tail -35; rtk proxy cargo test --test envelope_control_carrier --no-fail-fast 2&gt;&amp;1 | tail -25; rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -25; rtk proxy cargo test --test envelope_hook_refusals --test envelope_wiring --test envelope_tracer &gt;/dev/null 2&gt;&amp;1 || echo "CHECK: hook/wiring/tracer"; for t in envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position envelope_reparsed_value envelope_config_resolution envelope_callee_grammar envelope_pr_cap; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || echo "UNEXPECTED RED: $t"; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c '^#\[cfg(test)\]' src/envelope/hooks.rs; git diff --numstat HEAD~1..HEAD -- src/envelope/hooks.rs; git diff HEAD~1..HEAD -- src/envelope/hooks.rs | rtk proxy grep -c '^@@'</automated>
  </verify>
  <done>`guard` resolves `std::env::current_exe()` ONCE and hands it to `guard_in` explicitly with the one-sentence reason recorded, an unresolvable path passed through as absent rather than made an error, and `guard_in` and `classify_segments` threading it with no new I/O anywhere below `guard`; the ONE existing call site at the top of `classify_segments`' per-segment loop now asks the Task 1 predicate against the segment's words AND its pathname redirection targets over the two-path set, still before `resolve_program_with_head`, still on the existing channel at `ParkReason::EnvelopeAssertionFailed`, with no new variant, no second pass and no third reading site; its comment states FIVE placement consequences — the original three plus that the redirection targets come from a field the ONE walk filled with `segment.tokens` unchanged, and that the two paths carry two different boundary kinds with their reason and near-miss controls named; **`hooks.rs` shows diff lines in no other item**, verified by reading the diff against the named list, and the `settings_json` second-carrier row is NOT repaired; `19-28`'s complete RED set is now GREEN at the derived identifiers and **every one of its PERMITTED rows is still PERMITTED**, including the seven spelling rows in both word positions and both EXACT-PATH-not-PREFIX binary controls, with any newly-red permitted row reported as a finding about the RULE rather than edited; **the SEGMENT-COUNT pins and every round-4-through-10 mechanism pin were re-run and are green**, with `git >/dev/null push --force origin main` still exit 2 `force_push_blocked` as exactly ONE segment and `git x2>/tmp/o push --force origin main` still exit 0 as exactly ONE segment; `tests/envelope_carrier_reach.rs` gains ADDITIONS ONLY as new `#[test]` fns — the ordering pins measured in both orders before either was written and landing at deliberately different identifiers, the redirection-target row that turns an operand-only rule red, the binary row paired by name with its two near-miss controls, and a nested-payload row over the redirection channel — with any underivable identifier RECORDED; `hooks.rs` still has exactly ONE `#[cfg(test)]` line; no row anywhere under `tests/` was edited or deleted; and this is the second commit.</done>
</task>

<task type="auto">
  <name>Task 3: the honesty repairs and the ledger bound — `advisory.rs`, `cred.rs`, `mod.rs`, `ledger.rs`</name>
  <files>src/envelope/advisory.rs, src/envelope/cred.rs, src/envelope/mod.rs, src/envelope/ledger.rs, tests/envelope_carrier_reach.rs</files>
  <read_first>
    `src/envelope/advisory.rs:156-264` in full — **the four movements, the THREE RULES FOR THE NEXT
    EDITOR (the `\x20` indentation rule, the pinned-phrase-ordering rule and the never-wrap-through
    rule), the "Do not soften part 2" paragraph, the G-19-1 compression record naming exactly which two
    secondary clauses were dropped and why, and `SECTION_ENVELOPE` itself — noting that
    `cannot reach your ambient git credentials` is the FIRST pinned phrase and sits INSIDE the clause
    being repaired.** `:266-290` (`envelope_notice`, `PROTECTION_WARNING`, `PROTECTED_CLAIM` — read to
    leave untouched). `src/driver/dry_run.rs`'s re-export and its three consumers.
    `tests/envelope_advisory.rs` in full — **the smaller evidence file, and every assertion over the
    honesty statement must be enumerated before the text moves, including the token cap and the
    phrase-ORDER assertion.**
    `src/envelope/cred.rs:200-215` (`config_env` — **the `GIT_CONFIG_COUNT` triplet builder the injected
    pair would extend, IF the branch is taken**), `:224-306` (the D-09 four-things list, the ceiling
    paragraph, the REGIONS paragraph, the reach bullet `19-27` restored and the closing
    not-a-guarantee paragraph — **the doc-only region this task edits**), `:355-434` (`hooks_path_env`,
    **`write_gitconfig`'s doc naming the three helper routes it puts out of reach — the claim `T-19-118`
    falsifies** — `write_gitconfig_in`, `gitconfig_body`), `:480-560` (`build_env_in` — read to leave
    untouched unless the branch is taken).
    `src/envelope/mod.rs:146-226` (**`ENVELOPE_ROOT_ENV`'s residual-limit paragraph at `:165-168`, D-09's
    LAST uncorrected home**, `envelope_root`, `envelope_dir_in` — read to leave every non-doc line
    untouched).
    `src/envelope/ledger.rs:1-100` (the module doc's *"a cap the driven agent can reset is not a cap"*),
    **`:180-250`** (`record_and_check`, `record_and_check_in` and the unbounded read at `:225-227` —
    **and note that `policy` is already in scope there**), **`:280-303`** (`ends_mid_line` — **its doc's
    *"a whole-file read to answer a one-byte question is the sort of thing that turns a guard into a
    hang"* is the reasoning three functions above the unbounded read, and quoting the contrast is part
    of the fix**), `:305-377` (`tally` — **its no-under-count invariant, which is why the bound must
    REFUSE rather than read less**), and `src/envelope/hooks.rs:1390-1410` (`GUARD_TIMEOUT_SECS` and the
    reproduced-hang paragraph the bound is derived against).
    `19-28-SUMMARY.md`'s ledger latency curve, its derived bound row with the just-under-the-bound
    control, its empty-helper candidate result, and its `SECTION_ENVELOPE` pin section.
  </read_first>
  <action>
**1. Enumerate every pin over the honesty statement BEFORE the text moves.** Run a `grep -rn` over `src/`
and `tests/` for `SECTION_ENVELOPE`, `envelope_notice`, `ambient git credentials`, `credential helper`
and `socket is removed`, and record the COMPLETE hit set in the SUMMARY. **Update only the assertions
whose subject is the credential clause**: the one `19-28` named in advance, plus any
`tests/envelope_advisory.rs` assertion measured to pin the literal — **a FURTHER named, bounded exception
recorded in the SUMMARY by test function name.** Every other assertion must pass UNMODIFIED, including
the phrase-ORDER assertion, the token cap and `driver/dry_run.rs`'s three consumers. **An assertion that
cannot pass is a finding about the WORDING to report and re-word — never an assertion to edit.**
**Measure the rendered constant's whitespace-token count first and record it**; the headroom is two.

**2. Repair `SECTION_ENVELOPE`'s FIRST `Guaranteed` clause so the text is TRUE.** Today it claims the run
cannot reach ambient git credentials because the global and system git config is a generated file naming
no credential helper. `T-19-118` falsifies both halves. The wording is yours under **seven constraints,
every one of them mechanical**:

  - it must be TRUE against Task 2's measured boundary and against whichever `T-19-118` branch step 4
    takes;
  - **it must say what the clause COVERS and MUST NOT enumerate the residue.** A clause listing
    directions and stopping implies a completeness the measurement denies — the `T-19-107` shape in a
    shorter sentence — and the residue here is genuinely seven directions over two word classes, plus
    `C-15`, plus the deferred options;
  - **`cannot reach your ambient git credentials` must survive VERBATIM, FIRST and UNWRAPPED**, because
    it is the first of the three phrases whose ORDER is pinned and it sits inside the clause being
    repaired;
  - the other two pinned phrases keep their order and none is moved or wrapped through — the longest is
    `defeatable by an agent that can spawn an unsupervised`, at 53 characters;
  - indentation is `\x20` escapes, never literal spaces, and **the RENDERED constant is what you check**;
  - **the token cap is NOT raised.** The constant sits at 213 of 215. If the repair does not fit, shorten
    by dropping a CLAIM and never a LIMITATION — **and a LIMITATION may be GENERALISED to free tokens**,
    which makes it shorter and wider at once and is the direction G-19-1's own compression went;
  - **no residual disclosure is deleted.**

Then extend the `Not guaranteed` half so it names the BINARY route as well as the environment and file
routes, within the same cap.

**3. `mod.rs:165-168` — D-09's last uncorrected home, and a NAMED exception with ZERO non-doc lines.**
That paragraph argues that relocating the envelope root *"does not lower a boundary that was standing"*
because the same party *"can equally unset `GIT_CONFIG_COUNT`, which is D-09's stated ceiling."*
**`T-19-116` is a route that lowers layer 2 and layer 3 without controlling the environment the TUI
starts in at all**, so the ground of that argument is gone. Correct it: name the file and binary routes
as ceilings the environment route does not bound, keep the variable's own residual limit intact — audits
9 and 10 both measured `GSD_MM_ENVELOPE_ROOT` INERT from inside a Bash command — and change **zero
non-doc lines**, verified by `git diff --unified=0` over the file filtered for lines that are not `///`
and not blank returning NOTHING.

**4. `cred.rs` — the reach paragraph, and the `T-19-118` BRANCH.**

Doc first, and doc-only unless the branch is taken: add the FILE-carrier family's **SEVEN** fail-open
directions over two word classes and two paths to WHAT IS NOT COVERED, replacing the four; correct D-09's
ceiling paragraph to name the BINARY route as well as the file and environment routes; and record
`T-19-118` beside `write_gitconfig`'s own claim, with `AR-19-04` named and **NOT un-accepted** and with
the note that whether this repair closes `T-19-23` is audit 11's judgement.

**Then the branch, decided by `19-28`'s recorded measurement rather than by preference.** If `19-28`
recorded that adding a `credential.helper` pair with an EMPTY value to the injected `GIT_CONFIG_COUNT`
triplet resets the helper list, **and** recorded an acceptable cost — `GIT_ASKPASS` still answering and
`gh` still operating — then add that one pair through `config_env`, with a doc stating exactly what it
covers (every write spelling, because it reads no command line at all), exactly what it does not (an
agent that unsets `GIT_CONFIG_COUNT`, which is D-09's stated ceiling and already disclosed; and a later
`-c credential.helper=…` on the same line, at whatever `19-28` measured), and the measurement it rests
on. Add the corresponding rows to `tests/envelope_carrier_reach.rs` as ADDITIONS, each measured before it
is written and each with its control. **If `19-28` recorded that it did NOT reproduce, or recorded an
unacceptable cost, do not add it** — `cred.rs` then changes zero non-doc lines, and the SUMMARY states
which branch was taken and why in one paragraph. **Either way `AR-19-04` is not un-accepted and
`T-19-23` is not marked closed.**

**5. `ledger.rs` — the bound, and it is the first time this phase opens this file.** Add a size check
before `record_and_check_in`'s read, refusing when the ledger exceeds a bound whose value is **DERIVED
from `19-28`'s measured `tally` curve against `GUARD_TIMEOUT_SECS`** with the margin stated in the doc —
never from the caps, which are unclamped (`C-15`) and which a long-lived envelope legitimately exceeds
over months. The doc states, in the shape this module's other docs already use:

  - **why the bound REFUSES rather than reads less**, quoting `tally`'s own no-under-count invariant: a
    tail read, a line cap or a sample would forget attempts that demonstrably happened, and under-counting
    is the one failure this file exists to prevent;
  - **the contrast three functions above**, where `ends_mid_line` already refuses a whole-file read for a
    one-byte question on exactly this reasoning, and the note that the read below it was unbounded until
    now;
  - **the derivation**, with `19-28`'s measured sizes and times and the margin under the registered
    deadline;
  - **the over-refusal, disclosed from both sides**: a ledger past the bound refuses the forge commands
    the cap governs while every other command is unaffected, and `19-28` pinned the just-under-the-bound
    control still permitting and still counting.

The refusal carries `ParkReason::EnvelopeAssertionFailed` — **never `PrCapExceeded`, which names a cap
that fired (D-24)** — names the ledger's own path and gives an actionable recovery step (AR-19-11): the
file can be archived. **`tally`, `append_entry`, `ends_mid_line`, `parse_stamp`, `saturating_bump`,
`ledger_path`, `ledger_path_in`, `LedgerEntry`, `Tally`, `CapVerdict` and `WINDOW_SECS` are untouched.**

**6. Update the ONE `SECTION_ENVELOPE` assertion `19-28` named**, plus any `tests/envelope_advisory.rs`
update the enumeration in step 1 showed to be necessary, and record both in the SUMMARY as exceptions by
test function name.

Commit as its own commit, the third fix commit.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_advisory --no-fail-fast 2&gt;&amp;1 | tail -25; rtk proxy cargo test --test envelope_carrier_reach --no-fail-fast 2&gt;&amp;1 | tail -30; rtk proxy cargo test --test envelope_pr_cap --test envelope_credential --no-fail-fast 2&gt;&amp;1 | tail -25; rtk proxy cargo test --lib --no-fail-fast 2&gt;&amp;1 | tail -25; rtk proxy cargo test --test driver_dry_run &gt;/dev/null 2&gt;&amp;1 || echo "CHECK: driver_dry_run"; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git diff --unified=0 HEAD~1..HEAD -- src/envelope/mod.rs | rtk proxy grep -E '^[+-]' | rtk proxy grep -vE '^[+-]{3}' | rtk proxy grep -vE '^[+-]\s*///' | rtk proxy grep -vcE '^[+-]\s*$'; git diff --numstat HEAD~1..HEAD; rtk proxy grep -rn 'SECTION_ENVELOPE' src/ tests/ | wc -l</automated>
  </verify>
  <done>Every pin over the honesty statement was ENUMERATED with `grep -rn` and its complete hit set recorded BEFORE the text moved, together with the rendered constant's measured whitespace-token count; `SECTION_ENVELOPE`'s FIRST `Guaranteed` clause is TRUE against Task 2's boundary and against whichever `T-19-118` branch was taken, states what it COVERS, makes NO completeness claim and enumerates nothing, with `cannot reach your ambient git credentials` surviving verbatim, first and unwrapped, the other two pinned phrases in order and unwrapped, `\x20` indentation checked on the RENDERED constant, **the token cap NOT raised**, nothing shortened except by dropping a claim or GENERALISING a limitation, and no residual disclosure deleted; the `Not guaranteed` half names the BINARY route as well as the environment and file routes; **`mod.rs:165-168` is corrected — D-09's last uncorrected home — naming the file and binary routes as ceilings the environment route does not bound, with `GSD_MM_ENVELOPE_ROOT`'s own residual limit intact and ZERO non-doc lines changed**, verified by the filtered `git diff --unified=0` returning nothing; `cred.rs`'s WHAT IS NOT COVERED carries the SEVEN fail-open directions over two word classes and two paths, D-09's ceiling names the binary route, and `T-19-118` is recorded beside `write_gitconfig`'s own claim with `AR-19-04` named and NOT un-accepted and `T-19-23` not marked closed; **the `T-19-118` branch was decided by `19-28`'s recorded measurement, taken or declined, and the SUMMARY states which and why in one paragraph** — with the injected pair's doc stating exactly what it covers and does not if taken, and `cred.rs` changing zero non-doc lines if not; `ledger.rs` carries a size bound before `record_and_check_in`'s read whose value is DERIVED from `19-28`'s curve against `GUARD_TIMEOUT_SECS` with the margin stated, whose doc explains why it REFUSES rather than reads less against `tally`'s no-under-count invariant, quotes `ends_mid_line`'s contrasting reasoning three functions above, and discloses the over-refusal from both sides, with the refusal carrying `ParkReason::EnvelopeAssertionFailed` rather than `PrCapExceeded`, naming the ledger's path and giving an actionable recovery step — and every other item in the file untouched; only the ONE `SECTION_ENVELOPE` assertion `19-28` named plus any measured `tests/envelope_advisory.rs` assertion was updated, both recorded by test function name, and every other assertion over the constant passes UNMODIFIED including the phrase-order assertion, the token cap and `driver/dry_run.rs`'s consumers; and this is the third commit.</done>
</task>

<task type="auto">
  <name>Task 4: the record, the registrations, and the gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s "Threats found by audit 10" table, "The four fail-open directions — re-measured",
    "`SECTION_ENVELOPE`'s repaired cap clause", "The known-open set, as audit 10 verified it", "The
    mechanism pins, verified non-dead", "Audit 10's bookkeeping", "The four execution-time judgements",
    "Audit 10 — what the round-10 control can and cannot fail on" and "Audit 10 Sign-Off"; plus the
    appended execution records of plans 19-26, 19-27 and **19-28, which this plan's subsection FOLLOWS
    and does not edit.** The Accepted Risks Log and `AR-19-03`, `AR-19-04`, `AR-19-05` in particular, and
    the `T-19-23` row.
    `deferred-items.md`'s plan-19-27 section, **its `842` at `:2172` which `19-28` corrected BESIDE**, its
    plan-19-28 section, and its "Still OPEN and unchanged" rows.
    `19-28-SUMMARY.md`'s recorded RED and PERMITTED lists, its `passed + failed` total, its per-binary
    counts for all SEVENTEEN binaries and its new-`#[test]`-fn count.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-28 record and touching nothing that
precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan 19-29 —
carrying:

  - **FIRST, and in these words: `/gsd-secure-phase 19` is NOT cleared, and nothing here is described as
    a closure.** `T-19-86`, `T-19-91` and `T-19-111` remain open at `high`; `T-19-112` and `T-19-113` are
    narrowed FURTHER and still not closed; `T-19-115` gets NO rule; and **whether `T-19-116`,
    `T-19-117` and `T-19-118` close is audit 11's judgement rather than this plan's claim.**
  - **`19-28`'s RED rows with their before and after verdicts and identifiers**, and **`19-28`'s
    PERMITTED rows re-checked and still permitted**, with the statement that a permitted row turning red
    would have been a finding about the RULE rather than an assertion to edit.
  - **THE DESIGN ANSWER — BOTH, plus a third thing that is neither** — with each half's reason, the two
    boundary KINDS and why they differ, and the near-miss controls that would turn a directory-prefix
    binary clause red.
  - **THE DIRECTION-(i) QUESTION ANSWERED IN FULL**, in the terms the mandate asked for: closing it does
    NOT require the deletion model to read redirection targets as argv; round 6's model answers which
    words ARRIVE and rule (a) asks whether the line NAMES a control's path; the three source-read facts;
    **what it cost** — two additive fields, a private return-type change with four call sites, a
    corrected stale doc sentence, and a pathname/non-pathname split of a closed grammar; and **the
    SEGMENT-COUNT pin measurements before and after, which are what make the claim falsifiable rather
    than asserted.**
  - **THE SEVEN FAIL-OPEN DIRECTIONS over TWO word classes and TWO paths, in the docs' own words, handed
    to NO control**, with directions (iii) and (iv) recorded as NARROWED by their measured partial
    mitigations, **direction (i) recorded as NARROWED to those seven and NOT closed**, and the explicit
    statement that no revisit condition and no version witness was created and why these differ in kind
    from `19-25`'s future-git residue.
  - **`C-10`'s partial rule**, with what it refuses and **what it does not reach stated at the same
    weight**, `current_exe()`'s measured agreement or disagreement with the baked-in path, the
    silent-when-absent behaviour recorded as fail-open, and `T-19-01`'s mitigation recorded as correct,
    unweakened and never a defence of the exec target's bytes. **Update `C-10`'s control letter to (a),
    EXACT PATH, PARTIAL**, so the fifteen-carrier table still has a letter for every one.
  - **`T-19-115` carried forward with NO rule at its measured severity**, with the reason — a tilde needs
    the environment and a glob needs the filesystem — and the residue's arithmetic corrected from four to
    seven wherever this plan states it.
  - **the `SECTION_ENVELOPE` repair**, with the COMPLETE pin hit set enumerated before the text moved,
    the before-and-after of the credential clause, **the whitespace-token arithmetic stated** with the
    cap unraised, the confirmation that the phrase order and `driver/dry_run.rs`'s consumers passed
    unmodified, and the list of assertions updated with the reason each was permitted.
  - **which `T-19-118` branch was taken and why**, quoting `19-28`'s recorded measurement, with
    `AR-19-04` recorded and NOT un-accepted and `T-19-23` explicitly not marked closed.
  - **the ledger bound with its derivation** from `19-28`'s curve against `GUARD_TIMEOUT_SECS`, its
    margin, why it refuses rather than reads less, and the over-refusal disclosed from both sides —
    **and the behavioural half still UNMEASURED and claimed in neither direction.**
  - **`mod.rs:165-168` corrected**, with the before-and-after and the statement that `19-27` correctly
    declined it as outside its `files_modified` and that this round took it as a named exception.
  - **the SIX NAMED FILE EXCEPTIONS, stated AS exceptions**, each with its mechanical bound and the
    verification that the bound held, and the note that `T-19-61` … `T-19-73`, `T-19-84` and `T-19-85`
    were not taken on and `scan.rs` and `config.rs` were not opened.
  - the over-refusal cost measured from both sides for both new halves; the `421` correction at
    `policy.rs:7450` with every other number in that comment untouched; **`AR-19-04`, `AR-19-05` and
    `T-19-23` carried forward, RECORDED and NOT un-accepted or re-closed**; the planner-derived carriers
    `C-05`'s alias half, `C-07`, `C-08` and `C-12` carried forward unfixed with **`C-08`'s behavioural
    half UNMEASURED**; `C-15` carried forward at control (e) with no clamp; **`T-19-17r` OUTSTANDING for
    the twelfth time with no `AR-19-13` and no use of the word "accepted"**; and the `glab --host` cell
    unfixed with `--hostname` KEPT and `T-19-110` at `medium`.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Append a plan-19-29 section recording `T-19-116`, `T-19-117` and `T-19-118`
as **NARROWED, NOT CLOSED**, each naming the clause or bound that narrowed it and the directions that
remain; `T-19-115` **OPEN at `medium` with NO rule written** and the reason; the seven-direction residue
replacing the four; `C-10`'s control letter updated; the `T-19-118` branch recorded with its
measurement; the ledger bound recorded with its derivation; `mod.rs:165-168` recorded corrected; and
`T-19-86`, `T-19-91`, `T-19-96`, `T-19-110`, `T-19-111`, `T-19-112`, `T-19-113`, `T-19-74`, `C-08`,
`C-15`, the `glab --host` forge cell and the `T-19-17r` bookkeeping gap unchanged and open.

**3. The gate.**

  - The command is `rtk proxy cargo test --no-fail-fast`, counted with `rtk proxy grep` over a redirected
    log, never a plain `grep` (D-34).
  - **State the arithmetic explicitly.** `passed + failed` equals the exact total `19-28-SUMMARY.md`
    records plus the number of NEW `#[test]` fns this plan adds, counted from `git show`. **A red test
    RAN, so red→green leaves the total unchanged** and every increase comes only from new fns. A
    disagreement is a finding.
  - Every failure name is one of the three documented flakes. **The RED set from `19-28` is now EMPTY**,
    and `19-28`'s recorded RED output goes in the SUMMARY beside this plan's green output — a green run
    without the RED beside it is a run that certifies nothing.
  - **All SEVENTEEN `envelope_*` binaries RAN**, with their per-binary counts recorded. Sixteen means an
    evidence file did not execute.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.
  - **The `envelope_tracer` ETXTBSY race is `C-10`'s own seam and this round changed what the guard does
    about that seam**, so a firing is RECORDED with its output and its relation to this round's change
    stated in neither direction rather than assumed either way.

Commit the record as its own commit, the fourth.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-29-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -cE '^\| AR-19-13 \|' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-29-gate.log | tail -55; rtk proxy grep -c 'Running tests/envelope_' /tmp/19-29-gate.log; rtk proxy grep -n 'const SEPARATORS' src/envelope/policy.rs; git diff --stat HEAD~4..HEAD | tail -14; git diff --numstat HEAD~4..HEAD -- tests/ | tail -8; git log --oneline -4</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-29 record stating FIRST that `/gsd-secure-phase 19` is NOT cleared and that nothing here is described as a closure, that `T-19-86`, `T-19-91` and `T-19-111` remain open at `high`, that `T-19-112` and `T-19-113` are narrowed further and not closed, that `T-19-115` gets no rule, and that whether `T-19-116`, `T-19-117` and `T-19-118` close is audit 11's judgement; carrying `19-28`'s RED rows with before-and-after verdicts AND its PERMITTED rows re-checked still permitted; the design answer with each half's reason and the two boundary kinds; **the direction-(i) question answered in full with the three source-read facts, the stated cost and the SEGMENT-COUNT pin measurements before and after**; the SEVEN fail-open directions over two word classes and two paths in the docs' own words, handed to no control, with (iii) and (iv) NARROWED, (i) NARROWED-not-closed, and the explicit statement that no revisit condition or version witness was created and why; **`C-10`'s partial rule with what it does not reach stated at the same weight, its control letter updated to (a)-exact-path-partial, `current_exe()`'s measured agreement recorded and the silent-when-absent behaviour recorded as fail-open**; `T-19-115` carried forward with no rule and the reason; the `SECTION_ENVELOPE` repair with the complete pin hit set enumerated first, the before-and-after, **the whitespace-token arithmetic with the cap unraised**, and the assertions updated by name; **which `T-19-118` branch was taken and why, quoting `19-28`'s measurement, with `AR-19-04` NOT un-accepted and `T-19-23` NOT marked closed**; the ledger bound with its derivation, margin, refuse-rather-than-read-less reasoning and both-sides over-refusal, with the behavioural half still UNMEASURED; `mod.rs:165-168` corrected with before-and-after and `19-27`'s correct decline recorded; the SIX named file exceptions stated AS exceptions with their bounds and the verification each held; the `421` correction; `AR-19-04`, `AR-19-05`, `C-05`'s alias half, `C-07`, `C-08`, `C-12`, `C-15`, `T-19-110` and the `glab --host` cell carried forward; and `T-19-17r` OUTSTANDING for the twelfth time with no `AR-19-13` and no use of "accepted" — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection; `deferred-items.md` carries a plan-19-29 section recording `T-19-116`, `T-19-117` and `T-19-118` NARROWED-NOT-CLOSED with their remaining directions, `T-19-115` OPEN at `medium` with no rule, the seven-direction residue, `C-10`'s updated letter, the branch, the bound and `mod.rs`'s correction, leaving every other open item unchanged; the `--no-fail-fast` run is GREEN apart from documented flakes with `passed + failed` equal to `19-28`'s recorded total plus the new `#[test]` fns counted from `git show`, the arithmetic stated and checked, counted with `rtk proxy grep` over a redirected log, and `19-28`'s recorded RED output printed beside it; **all SEVENTEEN `envelope_*` binaries RAN** with their per-binary counts recorded; `SEPARATORS` is byte-identical; `git diff --numstat` over `tests/` shows ZERO deletions across all four commits; and this is a fourth commit distinct from the three before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, to what git does with a value the guard approved, and to the files the guard's own controls live in |
| **a control → a FILE OUTSIDE the envelope root** | **This round's first boundary. The path set grows from one directory to a directory and a file, with two different boundary KINDS** |
| **a path NAMED on the line → a path the rule can SEE** | **This round's second boundary. A pathname redirection target is now a word class the rule reads, on a channel `Segment::tokens` never carries** |
| **the walk that DELETES → the walk that DECIDES** | **Round 6 answers which words arrive; rule (a) asks what the line names. One walk, two questions, `segment.tokens` unchanged** |
| the guard's registered DEADLINE → the work it must finish | `GUARD_TIMEOUT_SECS = 5` against a ledger read a permitted command can inflate. Bounded here, failing CLOSED |
| a rule's stated residue → the spellings its silence permits | Stated as four. Measured as seven, over two word classes and two paths |
| **a control's own text → the reach it claims** | **`SECTION_ENVELOPE`'s FIRST `Guaranteed` clause claimed ambient credentials were out of reach and measurement falsified it. The repair is mandatory whether or not any rule lands — `T-19-107`'s registered shape, one clause over from the repair `19-27` made** |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-116 | Elevation of Privilege | `C-10` — the binary the hook stubs and `guard_command` exec (`hooks.rs:196-204`, `:1518-1524`) | high | **mitigate — NARROWED, explicitly NOT closed** | The carrier predicate's path set grows to `{envelope directory as a PREFIX} ∪ {the guard's own binary as an EXACT PATH}`, with `current_exe()` resolved once in `guard` and threaded explicitly the way `install_in` already takes its `binary`. **It refuses the absolute literal spelling and nothing else**, and the expansion-borne, tilde, relative and link spellings are stated as remaining open and handed to no control. `T-19-01`'s relocated-copy mitigation is unweakened. `C-10` gains a control letter so the fifteen-carrier table has one for every carrier |
| T-19-118 | Information Disclosure / Elevation of Privilege | `C-05` — the generated `gitconfig` on both `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` (`cred.rs:519-526`), reached by direction (i) | high | **mitigate — NARROWED, plus a mandatory honesty repair** | A pathname redirection-target channel produced by the walk that already skips the target, carried on the `Segment` with `segment.tokens` unchanged, so an absolute literal target under either protected path is refused. **`SECTION_ENVELOPE`'s FIRST `Guaranteed` clause is repaired so the text is TRUE, stating what it covers and enumerating nothing.** The injected empty-`credential.helper` pair is taken **if and only if `19-28` measured it reproducing at an acceptable cost**, and the branch is recorded either way. `AR-19-04` is NOT un-accepted and `T-19-23` is NOT marked closed |
| T-19-117 | Denial of Service / Elevation of Privilege | `C-01` as unbounded work — `ledger::tally` over `std::fs::read` (`ledger.rs:225-227`) against `GUARD_TIMEOUT_SECS = 5` | medium | **mitigate — NARROWED, bound derived from the DEADLINE** | A fail-CLOSED size bound before the read, valued from `19-28`'s measured curve with the margin stated, refusing rather than reading less because `tally`'s no-under-count invariant forbids a tail read. `ParkReason::EnvelopeAssertionFailed`, never `PrCapExceeded` (D-24). Over-refusal disclosed from both sides against a just-under-the-bound control. **The behavioural half stays UNMEASURED and is claimed in neither direction** |
| T-19-115 | Spoofing / Elevation of Privilege | rule (a)'s `LITERAL` condition versus its own four-direction disclosure | medium | **mitigate — the RESIDUE'S ARITHMETIC only; NO RULE, stated rather than implied** | The residue is restated as a CONDITION — a word the shell may rewrite, a word that is not absolute, a word that reaches a protected path only through a link — with the seven spellings as instances, in the predicate's doc, the refusal's doc, `cred.rs` and the record. **No rule is written and no acceptance is made**: a tilde needs the environment and a glob needs the filesystem, both forbidden at guard time |
| T-19-107-shape | Repudiation | a control's own text claiming a reach the measurement denies | — | mitigate | Every pin over `SECTION_ENVELOPE` enumerated before the text moves; the credential clause repaired under seven mechanical constraints including a 215-token cap with two tokens of headroom and a pinned phrase INSIDE the clause; **no completeness claim and no enumeration**; the seven directions live in four other places and are handed to no pin, schedule or witness |
| SAFE-04 | Information Disclosure | the widened refusal messages | — | mitigate | Each names whichever protected path matched — the envelope directory, the binary or the ledger, all paths this binary generated or resolved for itself — and never quotes the command back, on the same footing as `scan_leading`'s refusals naming a key and a section. Each recovery step is actionable (AR-19-11) |
| — | Denial of Service | over-refusal from the widened rule and from the ledger bound | medium | **accept, with the cost pinned from both sides and disclosed** | Two legible families: a run cannot name the binary it is driven by, and a forge command against an oversized ledger is refused. `19-28` pinned `cp /bin/true <BINARY-PARENT>/some-other-file`, `ls <BINARY-PARENT>`, `command -v gsd-meta-manager` and the just-under-the-bound ledger PERMITTED, and every ordinary-operand row stays at exit 0 |
| — | Tampering | round 6's deletion model, under direct pressure from the redirection channel | high | **mitigate — mechanically pinned, not argued** | `19-28`'s SEGMENT-COUNT pins assert exactly ONE `Segment` with exactly the surviving argv for `git >/dev/null push --force origin main` and `git x2>/tmp/o push --force origin main`. `SEPARATORS` byte-identical, `is_separator(">")` false, no token in the stream, `segment.tokens` unchanged. **A red pin is a finding about the FIX and never an assertion to edit** |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — gap RECORDED, acceptance deliberately unmade for the twelfth time** | No `AR-19-13`, no use of the word "accepted" |
| T-19-86, T-19-91 | Elevation of Privilege | `classify_git`'s denylist default arm through a governed program's own operand; a git classifier's decision operand assembled by expansion | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated, rows at exit 0 and pins green UNMODIFIED, with `T-19-111` still moved OUT of `T-19-86`. Because both remain open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-111 | Elevation of Privilege | the re-parse clause's CARRIER SET — `C-11` | high | **accept for this plan (registered, NO rule, no acceptance made)** | `19-27`'s five-site attribution correction stays as performed; corpus rows RECORDED in neither direction |
| T-19-96, T-19-110, T-19-74 | Elevation of Privilege | a glob in a push flag; region 2's operand grammar; the frozen wrapper rows | medium | **accept for this plan (registered, not fixed)** | Left exactly as pinned |
| — | Tampering | `C-05`'s alias half, `C-07`, `C-08`, `C-12`, `C-15` | planner-derived / control (e) | **carried forward UNFIXED and stated** | No rule, no clamp, no acceptance. `C-08`'s behavioural half stays UNMEASURED; repairing a delivery is a spawn-seam change and is out of scope |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. Every probe uses `std::process::Command` inside `#[cfg(test)]` |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`, and
  MUST be read as `passed + failed` against the exact total `19-28-SUMMARY.md` records.
- The gate arithmetic is STATED and CHECKED: `19-28`'s total plus the NEW `#[test]` fns this plan adds,
  counted from `git show`. A red test RAN, so red→green contributes nothing; a disagreement is a finding.
- **`19-28`'s RED set was confirmed STILL RED and its PERMITTED set STILL PERMITTED before any production
  line moved**, and both lists are in the SUMMARY beside this plan's green output.
- **Every one of `19-28`'s PERMITTED rows is still PERMITTED after the fix** — the seven spelling rows in
  both word positions, the expansion-borne, symlinked and relative rows, the near-misses, and both
  EXACT-PATH-not-PREFIX binary controls. A newly-red permitted row is a finding about the RULE.
- **The SEGMENT-COUNT pins are GREEN after the channel lands**: `git >/dev/null push --force origin main`
  is exactly ONE `Segment` with exactly the surviving argv and still exit 2 `force_push_blocked`, and
  `git x2>/tmp/o push --force origin main` is exactly ONE and still exit 0.
- All SEVENTEEN `envelope_*` binaries RAN and their per-binary counts are recorded.
- The rule is raised at ONE site, at the top of `classify_segments`' existing per-segment loop, BEFORE
  `resolve_program_with_head`, with no new `ParkReason` variant, no second pass and no third reading
  site. The ordering pins land at deliberately different identifiers; the redirection-target row, the
  binary row with its two named controls, and the nested-payload row all pass.
- **`segment.tokens` is byte-for-byte unchanged** and no redirection-target token enters `tokenize`'s
  stream; `SEPARATORS` is byte-identical at `policy.rs:2297` and `policy::is_separator(">")` is `false`.
- The predicate and the tokenizer call NOTHING on the filesystem or the process, asserted mechanically
  over their own production text against `canonicalize`, `read_link`, `symlink_metadata`, `metadata`,
  `current_dir`, `current_exe` and `exists`. `current_exe()` is called ONCE, in `guard`.
- The binary half is an EXACT-PATH comparison; `19-28`'s near-miss controls over the binary's parent
  stay PERMITTED.
- Heredoc delimiters, here-strings and fd numbers are NOT recorded as pathname targets, pinned in both
  directions.
- **The SEVEN fail-open directions appear in the predicate's doc, the refusal's doc, `cred.rs`'s WHAT IS
  NOT COVERED and the record, stated as a CONDITION with the spellings as instances, and no pin, schedule
  or version witness is created for any of them.** Direction (i) is described as NARROWED, never closed.
- **`SECTION_ENVELOPE`'s FIRST `Guaranteed` clause is TRUE, makes no completeness claim and enumerates
  nothing**, the complete pin hit set was enumerated before the text moved, `cannot reach your ambient
  git credentials` survives verbatim/first/unwrapped, the three phrases keep their order, indentation
  uses `\x20`, **the token cap was not raised**, and no residual disclosure was dropped.
  `driver/dry_run.rs`'s consumers and the phrase-order assertion pass UNMODIFIED.
- The `T-19-118` branch was decided by `19-28`'s recorded measurement and the SUMMARY says which and why;
  `AR-19-04` is not un-accepted and `T-19-23` is not marked closed.
- The ledger bound REFUSES rather than reads less, its value is derived from `19-28`'s curve against
  `GUARD_TIMEOUT_SECS` with the margin stated, it carries `EnvelopeAssertionFailed` rather than
  `PrCapExceeded`, and `19-28`'s just-under-the-bound control still permits and still counts.
- **The SIX named file exceptions held their bounds**: `hooks.rs` shows diff lines only in `guard`,
  `guard_in`'s and `classify_segments`' signatures and the one call site; `mod.rs` shows ZERO non-doc
  lines changed, verified by the filtered `git diff --unified=0`; `cred.rs` shows zero non-doc lines if
  the branch was declined; `advisory.rs` shows diff lines only in `SECTION_ENVELOPE`; `ledger.rs` shows
  them only in `record_and_check_in` and the new bound; `policy.rs` shows none in `resolve_program`,
  `resolve_program_with_head`, `first_unreadable_decision_word`, `config_key_operand_index`,
  `subcommand_word_indices`, `scan_gh_api`, `scan_leading`, `forbidden_repo_path`, `SEPARATORS` or
  `is_separator`.
- `POLICY_MIN_PRODUCTION_BYTES`, `HOOKS_MIN_PRODUCTION_BYTES`, the proportional floor's VALUE, the deep
  anchor and the sentinel counts are unchanged; only `842` → `421` moved in the comment.
- Every mechanism pin from rounds 4 through 10 is green and non-dead, and **not one config-resolution
  verdict moved**.
- No row anywhere under `tests/` was edited or deleted except the ONE `SECTION_ENVELOPE` assertion
  `19-28` named in advance and any `tests/envelope_advisory.rs` assertion measured to pin the credential
  clause; `git diff --numstat` over `tests/` shows ZERO deletions across all four commits.
- The `AR-19-13` row count is zero, the word "accepted" is not applied to `T-19-17r`, and no `AR-` row is
  added, edited, renumbered or un-accepted.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml` nor
  `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-28 record.
- Four commits exist in order: the channel and the predicate, the call site, the honesty repairs and the
  bound, the record.
</verification>

<success_criteria>
1. `19-28`'s RED set was confirmed still RED **and its PERMITTED set still PERMITTED** before any
   production line moved, both recorded verbatim, and every already-green or newly-red row would have
   been reported as a finding rather than proceeded past.
2. **The direction-(i) question is answered by construction rather than by argument**: the pathname
   redirection targets are produced by the walk that already skips them and carried on the `Segment` the
   way `Segment::redirection_unresolvable` travels, **`segment.tokens` is byte-for-byte unchanged, no
   token enters the stream, `SEPARATORS` does not move, and `19-28`'s SEGMENT-COUNT pins are green.**
3. The rule is raised at the SAME one site over TWO word classes and TWO paths, with no filesystem or
   process call in the predicate, no new `ParkReason`, no second pass and no third reading site — and the
   two boundary KINDS are pinned from both sides by the EXACT-PATH-not-PREFIX controls.
4. **`T-19-116` is NARROWED and the record says NARROWED, not closed** — the absolute literal spelling is
   refused, `C-10` has a control letter, and the expansion-borne, tilde, relative and link spellings are
   stated as open at the same weight.
5. **`SECTION_ENVELOPE`'s FIRST `Guaranteed` clause is TRUE when this round ends** — repaired under all
   seven mechanical constraints, with the pinned phrase surviving verbatim inside it, the token cap
   unraised, no limitation dropped, and **no completeness claim and no enumeration.**
6. **The residue is stated as SEVEN directions over two word classes and two paths, as a CONDITION rather
   than a list**, in four places, handed to no pin, schedule or witness — and `T-19-115` gets no rule,
   with the reason stated and no acceptance made.
7. The ledger bound fails CLOSED, is derived from the DEADLINE rather than from the caps, refuses rather
   than reads less against `tally`'s own invariant, and its over-refusal is disclosed from both sides.
8. **`mod.rs:165-168` is corrected** — D-09's last uncorrected home — with zero non-doc lines changed and
   `19-27`'s correct decline recorded rather than criticised.
9. The `T-19-118` branch was decided by `19-28`'s recorded measurement, taken or declined, and the
   SUMMARY says which and why; `AR-19-04` is not un-accepted and `T-19-23` is not marked closed.
10. The six named file exceptions held their mechanical bounds and are stated in the SUMMARY AS
    exceptions; `T-19-61` … `T-19-73`, `T-19-84` and `T-19-85` are untouched and `scan.rs` and
    `config.rs` were not opened.
11. `T-19-17r` is OUTSTANDING for the twelfth time, no `AR-` row is added or un-accepted, and the byte
    floors, the stripper's three protections and every round-4-through-10 mechanism pin are unchanged
    apart from `842` → `421`.
12. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared, that `T-19-86`, `T-19-91` and
    `T-19-111` remain open at `high`, and that **whether `T-19-116`, `T-19-117` and `T-19-118` close is
    audit 11's judgement rather than this plan's claim** — and no unqualified "T-19-60 is closed" appears
    anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-29-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared, that `T-19-86`,
`T-19-91` and `T-19-111` remain open at `high`, that `T-19-112` and `T-19-113` are narrowed further and
not closed, that `T-19-115` gets no rule, and that whether `T-19-116`, `T-19-117` and `T-19-118` close is
audit 11's judgement; the four commit SHAs in order with `git show --stat`; **`19-28`'s verbatim RED
output beside this plan's green output, plus `19-28`'s PERMITTED rows re-checked still permitted**; the
`--no-fail-fast` `passed + failed` totals before and after with the arithmetic stated and checked against
`git show`; the per-binary counts for all SEVENTEEN `envelope_*` binaries with every one confirmed to
have RUN; **the direction-(i) answer with its three source-read facts, its stated cost, and the
SEGMENT-COUNT pin measurements before and after**; the rule's conditions over both word classes and both
paths with the two boundary KINDS and why they differ; the placement with its five stated consequences
and the ordering pins at their identifiers; **the SEVEN fail-open directions in the docs' own words,
stated as a CONDITION with the spellings as instances, with (iii) and (iv) NARROWED, (i) NARROWED-not-
closed, and the explicit statement that no revisit condition or version witness was created and why**;
`C-10`'s partial rule with what it does not reach at the same weight, `current_exe()`'s measured
agreement with the baked-in path, the silent-when-absent behaviour recorded as fail-open, and
`T-19-01` recorded unweakened; **the `SECTION_ENVELOPE` repair with the COMPLETE pin hit set enumerated
before the text moved, the before-and-after of the credential clause, the whitespace-token arithmetic
with the cap unraised, and the confirmation that the phrase order and `driver/dry_run.rs`'s consumers
passed unmodified**; which `T-19-118` branch was taken and why, quoting `19-28`'s measurement, with
`AR-19-04` NOT un-accepted and `T-19-23` NOT marked closed; the ledger bound with its derivation, margin,
refuse-rather-than-read-less reasoning, both-sides over-refusal and the behavioural half still
UNMEASURED; `mod.rs:165-168`'s before-and-after with `19-27`'s correct decline recorded; the SIX NAMED
FILE EXCEPTIONS stated AS exceptions with their bounds and the verification each held, including the
assertions updated and by which test function name; the over-refusal cost from both sides for both new
halves; the `421` correction with every other number in that comment untouched; the mechanism pins and
byte floors re-measured with `SEPARATORS` byte-identical; `C-05`'s alias half, `C-07`, `C-08`, `C-12`,
`C-15`, `T-19-110` and the `glab --host` cell carried forward unfixed with `C-08`'s behavioural half
UNMEASURED; the `T-19-17r` bookkeeping gap OUTSTANDING with the explicit statement that this plan did NOT
accept it and added no Accepted-Risks-Log row; any documented flake that fired recorded with its output
and its relation to this round's change stated in neither direction; and a "what remains uncovered"
section naming `T-19-86` FIRST — open at `high` by user scoping decision, with `T-19-111` moved OUT of it
— followed by `T-19-91` and `T-19-111` (open at `high`), `T-19-112` and `T-19-113` (narrowed further,
directions open), `T-19-115` (no rule), `T-19-116`, `T-19-117` and `T-19-118` (narrowed, residue stated),
`T-19-96`, `T-19-110`, `T-19-74`, `T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit
statement that only the WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>
