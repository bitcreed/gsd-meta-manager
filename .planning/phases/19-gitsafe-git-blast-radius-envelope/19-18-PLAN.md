---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 18
type: execute
wave: 8
gap_closure: true
gap_ids: [T-19-99, T-19-97, T-19-98]
depends_on: [19-17]
files_modified:
  - tests/envelope_argv_deletion.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-05, SAFE-06]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, and this is the SECOND time that ordering is the reason a plan exists separately.** It worked twice — 19-14/19-15 and 19-16/19-17 — and the cross-plan carry-forward is what proves two rules separately load-bearing. This plan writes the corpus and the reproducers and STOPS. `19-19` writes the rule. `T-19-99` is `T-19-76`'s failure mode for the FIFTH consecutive round: `UNREADABLE_CLASSES` (`tests/envelope_wrapper_class.rs:3011`) names seven classes, each with a degenerate-proof predicate and three kinds of floor, and it genuinely fails on the class round 5 closed — but **all seven are word-ASSEMBLY classes**, and `grep -rnE '\"(git|gh|glab)[^\"]*[<>][^\"]*\"' tests/ src/` returns NOTHING while neither corpus file holds a backslash-newline. The corpus is structurally incapable of generating, and so of failing on, `T-19-97` and `T-19-98`."
    - "**If the widened corpus does not go RED against the pre-fix tree, that is a FINDING — stop and report it, do not proceed.** A row already refused is a row that certifies nothing about the class this round is about, and a property green before the fix is a property that could not have failed on it. `19-15` hit exactly this and handled it correctly by substituting a spelling that did reproduce and disclosing the substitution. Every row here is measured against the built binary BEFORE it is written as an assertion, and any row that does not reproduce is reported with its measured verdict instead of being asserted anyway."
    - "**THE GAP MOVED AXIS, NOT ONE SLOT OVER, AND THE CORPUS MUST BE LEGIBLE AS A SECOND AXIS.** Rounds 1–4's gap was always the cell beside the one the corpus could draw. Audit 5 enumerated bash's word expansions one at a time against round 5's inverted rule and found **no gap in word ASSEMBLY** — `T-19-92`…`T-19-95` closed, `T-19-93` on the harder COUNT bar. What the inversion answers is *is this word handed over as written*. What it does not answer is *is this word handed over at all*. So the corpus gets a SECOND named axis, `DELETION_CLASSES`, standing BESIDE `UNREADABLE_CLASSES` with its own predicates, its own per-alphabet floor, its own per-class floor and its own counted floor. **The seven assembly classes are not touched, not widened and not relaxed** — smuggling a `>` into them would model deletion as if it were assembly and lose exactly the distinction this round is about."
    - "**The invariant the corpus is written against, stated once: THE WORDS THE GUARD CLASSIFIES MUST BE EXACTLY THE WORDS THE PROGRAM RECEIVES, IN THE SAME ORDER — no more and no fewer.** `Token.literal` is one half of it, established positively while the word is consumed. The other half is that the word SURVIVES into argv, in its slot. Two mechanisms sit outside the literalness bit and both words are perfectly literal by its own test, correctly so: a **redirection** is a word the guard reads and the shell DELETES from argv, displacing every decision word one slot right (`T-19-97`); a **backslash-newline** is two characters bash deletes and `tokenize` keeps, so the word the guard reads is not the word the program receives (`T-19-98`)."
    - "**The corpus must be able to fail in BOTH directions, and the over-deletion control is the row that makes that true.** A rule that deletes too much displaces decision words LEFT and is the same defect mirrored. Measured at `596480c` and confirmed under the shims: `git x2>/tmp/o push --force origin main` gives bash `ARGV[git]: [x2] [push] [--force] [origin] [main]` — `x2` IS argv, because an IO_NUMBER is a digits-only run since the start of the word, and git itself then rejects `x2` as not a git command. It is PERMITTED today and **must stay permitted**, because after the fix the guard's argv equals git's argv. It is this axis's `ls {git,svn}-repo`: the control that tells a deletion MODEL apart from a blanket refusal, and a corpus without it cannot fail on over-deletion."
    - "**Two ordinary commands are ALREADY falsely refused today by this same defect, and pinning that pre-fix is what makes the round's cost argument checkable rather than asserted.** Measured at `596480c`, fresh root per row: `git push origin refs/heads/gsd-auto/alpha/w > log.txt` → **exit 2 `push_outside_namespace`** while its one-line twin `git push origin refs/heads/gsd-auto/alpha/w` → **exit 0** — the redirection word is read as an EXTRA refspec, measured message *the refspec `>` resolves to `refs/heads/>`*. And `git push \\`+NEWLINE+` origin refs/heads/gsd-auto/alpha/w` → **exit 2 `push_outside_namespace`** against the same permitted twin, by a DIFFERENT mechanism: the whitespace after the continuation flushes it, so it becomes its own WORD in the REMOTE slot and every operand shifts one slot right — measured message *the refspec `origin` resolves to `refs/heads/origin`*, which is `T-19-97`'s displacement arriving through `T-19-98`'s mechanism. **The round REMOVES two measured over-refusals rather than adding any**, and both are pinned here at their pre-fix verdicts in two `#[test]` fns this plan NAMES — `the_redirected_in_namespace_push_is_falsely_refused_today_and_19_19_must_move_it` and `the_continued_in_namespace_push_is_falsely_refused_today_and_19_19_must_move_it` — which are the ONLY assertions in this file `19-19` is permitted to REPLACE rather than only add to. A pin the next plan must move, under a prohibition allowing only additions, is a plan that cannot be executed."
    - "**Every claimed bypass is confirmed under `bash` against argv-printing shims, and a row the shell does not actually run is DISCARDED or LABELLED, never pinned as a reproducer.** Audit 5 already discarded three on this step and they must NOT be re-spent: single-quoted `\\`+newline (bash performs no continuation inside single quotes; measured `ARGV[git]: [pu\\`+NL+`sh]`), `\\`+CR and `\\`+TAB (genuine escapes of those characters). Two rows here are PRECONDITION rows rather than discards and must be labelled as such in the test file and in the SUMMARY: `git <input.txt push --force origin main` runs only when `input.txt` exists — bash fails the redirection and runs nothing otherwise — which is the same standard audit 4 applied to `touch push && git pus? …`, and `git >$F push --force origin main` needs `F` bound or bash answers `ambiguous redirect`."
    - "**The cells found while PLANNING round 6 are measured and pinned at whatever verdict they actually have, with the same provenance caveat `19-14` and `19-16` established.** Seven were probed, every one at **exit 0** at `596480c` with an empty walk and every one confirmed under the shims to assemble the dangerous command. All seven are inside `T-19-97`'s class — a word the shell deletes — rather than new threats: **`git &>/tmp/o push --force origin main`**, where `&` is in `SEPARATORS` so the guard splits ONE simple command into TWO and a redirection parser running after the separator arm never sees `&>`; **`git {v}>/tmp/o push --force origin main`**, bash 4.1 fd allocation, carried into the verb slot by round 5's own literal-brace branch because `{v}` has no comma and no range; **`git >|/tmp/o …`** and **`git <>/tmp/o …`**, two-character operators no single-`>` rule reaches; **`git <<EOF push --force origin main`**, which bash runs even as a single line; **`git > /tmp/o push --force origin main`**, the operator as its own whitespace-delimited word; and **`git x2>/tmp/o push --force origin main`**, the over-deletion control above."
    - "**A row whose POST-fix verdict cannot be DERIVED is measured pre-fix, recorded, and left for `19-19` to measure and pin.** This plan measures pre-fix, so its measure-first discipline cannot catch a wrong post-fix expectation, and a row pinned at a verdict the rule cannot produce lands RED with `19-19` forbidden to edit it — which stalls the round. Two shapes are in that category and both are named rather than guessed: `git >$F push --force origin main` and `git >*.log push --force origin main` are refused TODAY at `envelope_assertion_failed`, because round 4's literalness bit fires on the `$` and the glob in what it reads as the VERB; after deletion the surviving argv is `push --force origin main`, so the row stays refused but its REASON IDENTIFIER changes to `force_push_blocked`. That is a derivation `19-19` must state and measure, not one this plan asserts."
    - "**Where the hole actually is, stated precisely so the corpus draws the right position.** A redirection BEFORE the program is already refused: `>/dev/null git push --force origin main`, `2>/dev/null git …` and `env >/dev/null git …` all measure **exit 2 `force_push_blocked`** today, because `resolve_program` walks past the redirection word as it would any wrapper operand and finds `git`. A redirection AFTER the decision words is also already right: `git push --force origin main >/dev/null` → exit 2, and `>out gh pr create --title x` → exit 0 **with a ledger line**, correctly COUNTED. **The hole is a redirection standing BETWEEN the governed program and its decision words**, and the displacement alphabet must splice there rather than at a wrapper prefix or an operand slot — a corpus that drew the prefix position would be green before the fix and would certify nothing."
    - "**A deletion entry is VERDICT-PRESERVING after the fix, which is the opposite of the brace axis and changes which alphabets may carry it.** A brace expansion spliced into a permitted governed command is refused after `19-17`, so `19-16` had to keep it out of the invariance-preserving alphabets. A redirection is different in kind: after `19-19` it changes no verdict at all, because the surviving argv is what the classifier already answers about. `git >/dev/null status` and `git >/dev/null log --oneline` are permitted today and must stay permitted; `git >/dev/null push --force origin main` is permitted today and must become refused. So deletion entries belong wherever the class is drawn, and the corpus must carry the PERMITTED half beside the refused half or it cannot fail on a blanket-refusal implementation."
    - "**Nothing `19-14`, `19-15`, `19-16` or `19-17` settled is re-opened, and the mechanism pins say so mechanically rather than in prose — because this round touches the tokenizer for the third time.** Three pins are carried into the new file and all three must be GREEN at this plan's end and after `19-19`: Rule B's, asserted over `policy::split_segments_with_heads` reporting `head_is_command_position == false` for the severed-prefix spellings; round 4's literalness bit asserted NON-VACUOUS, so the inversion cannot become dead code behind a deletion rule that refuses the same rows for a different reason; and **`separators_are_named_in_one_list_that_the_predicate_reads` (`policy.rs:4497`), whose `assert!(!is_separator(\">\"))` is the mechanical proof that the fix did NOT take the route `SEPARATORS`' own doc forbids.**"
    - "**This plan's gate names its expected failures rather than requiring zero, and it is the handoff contract `19-19` gates against.** `19-17-SUMMARY.md` records `1584 passed / 0 failed / 13 ignored`, `passed + failed = 1584`, and audit 5 independently re-measured that number. After this plan `passed + failed` is strictly greater than 1584; every failure name is either one of the two flaky `driver_reattach` names or one of the RED names this plan creates; the RED set is non-empty and every one of its names is listed in the SUMMARY; and `--test envelope_expansion_slots`, `--test envelope_command_position` and `--test envelope_literal_decision` are FULLY GREEN, because the widening must not disturb rounds 4 and 5's evidence."
    - "**The `T-19-17r` bookkeeping gap is RECORDED and the acceptance is NOT made.** `19-17-SUMMARY.md` calls `T-19-17r` \"the new over-refusal cost, **accepted** and pinned from both sides\". Audit 5 confirmed the measurement and the pins — `rg \"git status\" {src,tests}` → exit 2, `rg \"git status\" src/` → exit 0, both in `tests/envelope_literal_decision.rs:1355-1379` with the producing clause named — but there is **no `AR-19-13` row in the Accepted Risks Log and no register row**, so it is described as accepted while documented nowhere an audit reads. Audit 5 deliberately did not add it: accepting a risk is a human decision. **This plan records the gap and leaves it open**, and does not use the word \"accepted\" for `T-19-17r` anywhere."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-19`, or by the two together.** `T-19-86` remains OPEN at `high` by explicit user scoping decision and its four rows must keep exiting 0; `T-19-91` remains OPEN at `high` with three arms — `reflog $S`, `reflog show $S` and `symbolic-ref $S` at exit 0 with no second carrier, and `git push $REF` cwd-dependent and permitted in-namespace; `T-19-96` is registered open; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. The SUMMARY must say so FIRST, and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-19` is the rule. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`'s and `19-16`'s RED commits met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT touch `UNREADABLE_CLASSES`, its seven predicates, its degenerate-proofing assertions, `MIN_UNREADABLE_GENERATED_CASES`, `MIN_GENERATED_CASES_PER_UNREADABLE_CLASS` or `MIN_UNREADABLE_FORGE_SLOT_CASES`. The deletion axis is a NEW named constant with NEW predicates and NEW floors standing beside them. Audit 5's own framing is the reason: the seven are ASSEMBLY classes, the gap moved AXIS rather than one slot over, and an axis smuggled into the assembly classes is an axis that stops being legible as one — which is how a corpus comes to model exactly the control it certifies and nothing beside it."
      category: safety
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured. If a row audit 5 recorded at exit 0 does not reproduce, report it as a finding with the measured verdict and stop — do not assert the audit's number over your own measurement, and do not quietly drop the row. If a row is refused for a DIFFERENT reason identifier than the class expects, pin it at its true identifier in a test of its own and disclose the interaction (D-24)."
      category: honesty
    - statement: "MUST NOT pin a bypass row that `bash` does not actually run. Confirm every claimed permit under `bash` with shims named `git`, `gh` and `glab` first on `PATH` printing their own argv, and for the backslash-newline rows drive bash from a FILE whose bytes were verified with `od -c`. **MUST NOT re-spend audit 5's three discarded measurements** — single-quoted `\\`+newline, `\\`+CR and `\\`+TAB — and MUST NOT re-add them as rows. Rows that need a precondition (`<input.txt`, `>$F`) are LABELLED as precondition rows in the test file and in the SUMMARY, never as bare reproducers."
      category: honesty
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet entry or property in `tests/envelope_wrapper_class.rs`. `MIN_REFUSED_CASES`, `MIN_DISTINCT_CHAINS`, `MIN_DISTINCT_REASONS`, `MIN_DECOY_OPERANDS`, `MIN_DECOY_CASES`, `MIN_DISTINCT_DECOY_CHAINS`, `MIN_EXPANSION_WRAPPERS`, `MIN_EXPANSION_WRAPPER_CASES`, `MIN_DISTINCT_EXPANSION_CHAINS`, `MIN_LIVE_EXPANSION_CASES`, `MIN_FORGE_SLOT_CASES`, `MIN_SEVERED_PREFIXES`, `MIN_SEVERED_CASES`, `MIN_DISTINCT_SEVERED_CHAINS`, `MIN_SEVERED_BRACE_CASES` and `MIN_UNREADABLE_FORGE_SLOT_CASES` keep their current values or rise. A case that stops being drawn is a case that stops being able to fail, and this file's own section header says so."
      category: scope
    - statement: "MUST NOT pin a POST-fix expectation that cannot be DERIVED from `19-19`'s design, and MUST write the derivation beside every row that carries one. This plan measures PRE-fix, so its measure-first discipline cannot catch a wrong post-fix expectation — a row pinned at a verdict the rule cannot produce lands RED and `19-19` is forbidden to edit it, which stalls the round. **Three rows are undeliverable and are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `git >$F push --force origin main` and `git >*.log push --force origin main`, whose reason identifier changes when deletion removes the word round 4's bit fires on; and `git {v}>/tmp/o push --force origin main` with its permitted-half twin, because `19-19` deliberately does NOT model a `{name}` fd prefix and marks it unresolvable, so it lands on a different identifier from its six siblings by a different clause. Assert only the PRE-fix verdict each was measured at."
      category: honesty
    - statement: "MUST NOT add a deletion entry to an alphabet whose property asserts a verdict the entry does not preserve, and MUST NOT satisfy the deletion floors with an entry drawn in a position that is already refused. A redirection BEFORE the program (`>/dev/null git push --force origin main`) and one AFTER the decision words (`git push --force origin main >/dev/null`) are BOTH already refused at `596480c` — measured — so an alphabet drawing only those positions is green before the fix and certifies nothing. The displacement alphabet splices BETWEEN the governed program and its decision words, which is where the hole is."
      category: safety
    - statement: "MUST NOT modify `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs` or `tests/envelope_literal_decision.rs`. Round-4's and round-5's evidence stays attributable to the round that produced it, which is why `19-14` created a third file and `19-16` a fourth; this plan creates a FIFTH for the same reason. The three carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place."
      category: scope
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS`, propose it, or write a row whose expected mechanism is that they were added. `SEPARATORS`' own doc records the decision — a redirection does not start a new command, so treating it as a separator would hide the command it redirects — and audit 5 judged that reasoning RIGHT and only its unexamined consequence wrong. `separators_are_named_in_one_list_that_the_predicate_reads` asserting `!is_separator(\">\")` is carried forward as the mechanical proof and must stay GREEN and UNMODIFIED through `19-19`."
      category: safety
    - statement: "MUST NOT add an `AR-19-13` row to the Accepted Risks Log, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Record the gap as OUTSTANDING: the cost is measured and pinned from both sides, and its ACCEPTANCE is not recorded anywhere an audit reads. Accepting a risk is a human decision and audit 5 explicitly declined to make it on the developer's behalf; a plan making it would be forging the same signature."
      category: honesty
    - statement: "MUST NOT fix, close, narrow or re-scope `T-19-86`. `the_t_19_86_residual_is_permitted_today_and_this_plan_leaves_it_permitted` stays green UNMODIFIED with all four rows exiting 0, and `T-19-86` is named FIRST in the SUMMARY's 'what remains uncovered' section."
      category: scope
    - statement: "MUST NOT close, re-scope or add a remedy for `T-19-91`, and MUST NOT weaken its record. Its three arms stay as audit 5 verified them — `git reflog $S`, `git reflog show $S` and `git symbolic-ref $S` at exit 0 with NO second carrier, and the bare `git push $REF` cwd-dependent and permitted in the in-namespace configuration, which is a THIRD arm and not \"already fails closed\". Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push`, and do not extend the denylist."
      category: scope
    - statement: "MUST NOT fix `T-19-96` (a glob in a push flag) or `T-19-74` (whose core rows are frozen and re-measured permitted). MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not touch `cred.rs`, `advisory.rs`, `scan.rs`, `config.rs`, the honesty statement, the second-carrier table or the park-coverage surface."
      category: scope
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` — and every parser in this module is hand-rolled against `std` for exactly that reason."
      category: scope
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised forge command wrote nothing must use a FRESH envelope root of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line. `>out gh pr create --title x` is measured at exit 0 WITH one ledger line at `596480c` and is a ready-made positive control."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against 1584, the number `19-17-SUMMARY.md` records and audit 5 re-measured. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing (D-34)."
      category: honesty
    - statement: "MUST NOT fix, edit or work around the two `tests/driver_reattach.rs` failures, and MUST NOT treat their absence from a run as a regression. They are pre-existing, flaky, documented in `deferred-items.md`, and out of scope."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or the subsections plans 19-13 … 19-17 appended. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-18 execution record. Re-measuring and re-classifying these rows is `/gsd-secure-phase 19`'s job; an audit's own tables are its provenance and a plan writing into them forges it."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`, so this plan does not clear `/gsd-secure-phase 19` and neither does `19-19`."
      category: honesty
  artifacts:
    - path: "tests/envelope_argv_deletion.rs"
      provides: "the FIFTH evidence file — audit 5's `T-19-97` and `T-19-98` reproducers measured against the built binary and confirmed under bash shims, the seven cells found while planning round 6, the PERMITTED half of the deletion axis (`git >/dev/null status`, `git >/dev/null log --oneline`, `>out gh pr create --title x` with its ledger line), the over-deletion control `git x2>/tmp/o push --force origin main`, the two pre-existing FALSE REFUSALS pinned at their pre-fix verdicts beside their permitted one-line twins, the two precondition rows labelled as such, the two rows whose post-fix verdict `19-19` must derive, and the three carried-forward mechanism pins"
      contains: "T-19-97"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "`DELETION_CLASSES` — a SECOND named axis standing beside the untouched `UNREADABLE_CLASSES`, with five degenerate-proof predicates, a displacement alphabet spliced between the governed program and its decision words, a generative property asserting refusal AND an empty walk, and per-alphabet, per-class and counted floors in the shape section 13a already establishes"
      contains: "DELETION_CLASSES"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-18 execution record rather than an audit finding, carrying every measured verdict with its walk, the bash-shim argv for every claimed bypass, any audit-5 row that failed to reproduce, the seven cells found while planning, the two pre-existing false refusals, the `T-19-17r` bookkeeping gap recorded as OUTSTANDING with the acceptance deliberately unmade, and the statement that this plan closes nothing and clears no gate"
      contains: "19-18"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "the `T-19-97`, `T-19-98` and `T-19-99` entries recording that their corpus exists and is RED, the seven planning cells folded into `T-19-97`'s class rather than registered as new threats, and the `T-19-17r` bookkeeping gap as its own entry with the acceptance unmade; `T-19-86`, `T-19-91` and `T-19-96` unchanged and open"
      contains: "T-19-99"
  key_links:
    - from: "audit 5's measured bypass rows"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary with a fresh root per row and a bash-shim confirmation, BEFORE any assertion is written and BEFORE any production line moves"
      pattern: "RED"
    - from: "the two mechanisms that delete rather than rewrite"
      to: "a SECOND named axis the corpus can draw, standing beside the seven assembly classes"
      via: "`DELETION_CLASSES` with degenerate-proof predicates and its own floors, and a displacement alphabet spliced where the hole actually is — between the governed program and its decision words"
      pattern: "DELETION_CLASSES"
    - from: "a rule that deletes TOO MUCH"
      to: "a red test rather than a silent left-shift of every decision word"
      via: "`git x2>/tmp/o push --force origin main` pinned PERMITTED, because bash gives git `[x2] [push] …` and git itself rejects `x2` — this axis's `ls {git,svn}-repo`"
      pattern: "x2"
    - from: "round 4's literalness bit and Rule B's severed-head geometry"
      to: "pins `19-19` cannot make dead code silently while every verdict stays green"
      via: "assertions over `policy::split_segments_with_heads` and `policy::is_separator` directly, not over exit codes"
      pattern: "head_is_command_position"
---

<objective>
**The corpus for the OTHER HALF of the boundary — and it must go RED before a line of the rule is written.**

Audit 5 (`19-SECURITY.md`, the authority) verified that round 5's inversion is genuine and that it
closed the whole word-ASSEMBLY class, not merely the rows the corpus drew:

> The inversion is genuine, and it is the first control in this phase that is not an enumeration of
> shell syntax. … Audit 5 enumerated bash's word expansions one at a time against the bit's trigger
> set and clause 2 and found **no gap in word ASSEMBLY**. … **And the class is still open, because
> word assembly was never the whole class.** The inversion answers *"is this word handed over as
> written"*. It does not answer *"is this word handed over at all"*.

And then it found the corpus could not have caught either mechanism:

> `T-19-99` … **all seven classes are word-ASSEMBLY classes** … `grep -rnE
> '"(git|gh|glab)[^"]*[<>][^"]*"' tests/ src/` finds **no guard-driven row carrying a redirection
> anywhere in the repository**. The corpus is therefore structurally incapable of generating, and so
> of failing on, `T-19-97` and `T-19-98`.

So: the corpus is widened here, observed failing here, and committed here. `19-19` writes the rule and
is gated on this plan's recorded RED state. **If the widened corpus does not go red against the
pre-fix tree, that is a finding — stop and report it.**

## The invariant this corpus is written against

**The words the guard classifies must be exactly the words the program receives, in the same order —
no more and no fewer.** Round 4 established one half positively: a decision word must be provably
LITERAL, evidenced while it is consumed. This is the other half: the word must SURVIVE into argv, in
its slot. Both `T-19-97` and `T-19-98` are fully literal by round 4's own test, **and the bit is right
about them** — it is the model that is incomplete.

## What must be RED at this plan's end

Measured by audit 5 at `ad847b4` and re-measured while planning at `596480c` — fresh
`GSD_MM_ENVELOPE_ROOT` per row, envelope directory walked afterwards, every one confirmed under
`bash` against argv-printing `git`/`gh`/`glab` shims:

```
exit=0  git >/dev/null push --force origin main            <- T-19-97, ALL THREE LAYERS
exit=0  git 2>/dev/null push --force origin main           <- T-19-97, fd number
exit=0  git 1>/dev/null push --force origin main           <- T-19-97
exit=0  git >>/tmp/x push --force origin main              <- T-19-97, append
exit=0  git > /tmp/o push --force origin main              <- T-19-97, operator as its own word
exit=0  git <<<x push --force origin main                  <- T-19-97, herestring
exit=0  git push>/dev/null --force origin main             <- T-19-97, ATTACHED to the verb
exit=0  git >/dev/null stash                               <- T-19-97, no second carrier
exit=0  git >/dev/null update-ref -d refs/heads/main       <- T-19-97, no second carrier
exit=0  git >/dev/null config core.hooksPath /tmp/x        <- T-19-97, disarms layer 3
exit=0  gh >/dev/null pr create --title x    (EMPTY WALK)  <- T-19-97, SAFE-06 UNCOUNTED
exit=0  gh >/dev/null api repos/o/r/pulls -f title=x (EMPTY WALK)  <- T-19-97, SAFE-06 UNCOUNTED
exit=0  glab >/dev/null mr create --title x                <- T-19-97
exit=0  bash -lc "git >/dev/null push --force origin main" <- T-19-97, nested payload
exit=0  echo hi && git >/dev/null push --force origin main <- T-19-97, behind a sequence
exit=0  git >/dev/null -c core.hooksPath=/dev/null push --force origin main   <- T-19-97, LAYER 2
exit=0  git pu\<NL>sh --force origin main                  <- T-19-98
exit=0  git \<NL>push --force origin main                  <- T-19-98
exit=0  git sta\<NL>sh                                     <- T-19-98, no second carrier
exit=0  gh p\<NL>r create --title x          (EMPTY WALK)  <- T-19-98, SAFE-06 UNCOUNTED
exit=0  git config core.hooks\<NL>Path /tmp/x              <- T-19-98, disarms layer 3
exit=0  git "pu\<NL>sh" --force origin main                <- T-19-98, double-quoted
exit=0  bash -lc "git pu\<NL>sh --force origin main"       <- T-19-98, nested payload
exit=0  git -c core.hooks\<NL>Path=/dev/null pu\<NL>sh --force origin main    <- T-19-98, LAYER 2
```

Every `<NL>` above is a REAL newline preceded by a REAL backslash. Confirmed under bash from a file
whose bytes were checked with `od -c`: `git pu\`+NL+`sh --force origin main` prints
`ARGV[git]: [push] [--force] [origin] [main]`.

## The cells found while PLANNING round 6

Same provenance caveat `19-14` and `19-16` established: found while planning, not by an audit. All
seven measured at **exit 0** at `596480c` with empty walks, all seven confirmed under the shims, and
all seven are inside `T-19-97`'s class rather than new threats.

| Cell | Line | Why the audit's rows do not reach it as written |
|---|---|---|
| **`&>` — the guard SPLITS a command bash does not** | `git &>/tmp/o push --force origin main` | `&` is in `SEPARATORS`, so the tokenizer's separator arm fires first and the line becomes TWO segments — `git`, then `>/tmp/o push --force origin main`. Bash's `&>` is ONE redirection operator: `ARGV[git]: [push] [--force] [origin] [main]`. A redirection parser that runs after the separator arm never sees it, so this is a DIFFERENT mechanism inside the same class |
| **`{v}>` — bash 4.1 fd allocation** | `git {v}>/tmp/o push --force origin main` | `{v}` has no comma and no range, so round 5's own literal-brace branch absorbs it into the word and `{v}>/tmp/o` reaches the verb slot. Round 5's correct fix is what carries this one. **The one cell `19-19` does not MODEL** — it marks a `{name}` fd prefix unresolvable — so its post-fix identifier differs from its six siblings' and this plan records the pre-fix measurement without asserting a post-fix verdict, and keeps it out of the verdict-preserving alphabet |
| **`>\|` — clobber** | `git >\|/tmp/o push --force origin main` | a two-character operator no single-`>` rule reaches |
| **`<>` — read-write** | `git <>/tmp/o push --force origin main` | same, and it is the one operator whose first character is `<` and second is `>` |
| **`<<` — heredoc** | `git <<EOF push --force origin main` | bash runs it even as a single line; the delimiter word is deleted from argv and the body is not argv at all |
| **`<file` needs the file** | `touch input.txt && git <input.txt push --force origin main` | without the file bash fails the redirection and runs NOTHING. A PRECONDITION row, the same standard audit 4 applied to `touch push && git pus? …` — not a discard |
| **`x2>` — the OVER-DELETION control** | `git x2>/tmp/o push --force origin main` | an IO_NUMBER is a digits-only run since the START of the word, so `x2` IS argv: `ARGV[git]: [x2] [push] [--force] [origin] [main]`, and git itself rejects `x2`. **Permitted today and it must STAY permitted.** A parser that deleted any word part before `>` would delete a real argv word and displace every decision word LEFT — the same defect mirrored |

## Where the hole actually is

Measured at `596480c`, so the displacement alphabet splices in the right place rather than in a
position that is already closed:

```
exit=2  force_push_blocked   >/dev/null git push --force origin main       (already refused)
exit=2  force_push_blocked   2>/dev/null git push --force origin main      (already refused)
exit=2  force_push_blocked   env >/dev/null git push --force origin main   (already refused)
exit=2  force_push_blocked   git push --force origin main >/dev/null       (already refused)
exit=0  + ONE LEDGER LINE    >out gh pr create --title x                   (already COUNTED)
```

`resolve_program` walks past a leading redirection as it would any wrapper operand and finds `git`.
**The hole is a redirection standing BETWEEN the governed program and its decision words**, and a
corpus that drew the prefix position would be green before the fix and would certify nothing.

## The two pre-existing FALSE REFUSALS this round removes

Measured at `596480c`, each beside its permitted one-line twin. Both are the SAME defect in the
opposite direction — the guard reads a word the program never receives, and answers about it:

```
exit=2 push_outside_namespace   git push origin refs/heads/gsd-auto/alpha/w > log.txt
exit=0                          git push origin refs/heads/gsd-auto/alpha/w      (twin)

exit=2 push_outside_namespace   git push \<NL>  origin refs/heads/gsd-auto/alpha/w
exit=0                          git push origin refs/heads/gsd-auto/alpha/w      (twin)
```

Both derivations come from the measured message, and they are DIFFERENT mechanisms. The redirection
word is read as an extra refspec — *the refspec `>` resolves to `refs/heads/>`*. The `\`+newline is
flushed by the whitespace after it and becomes its OWN WORD in the REMOTE slot, displacing every
operand one slot right — *the refspec `origin` resolves to `refs/heads/origin`*, which is `T-19-97`'s
displacement arriving through `T-19-98`'s mechanism.

**The round REMOVES over-refusal rather than adding it**, and pinning both here pre-fix — in two
`#[test]` fns this plan NAMES, which `19-19` is required to replace — is what makes that claim
checkable instead of asserted.

## What this plan is NOT

It closes nothing. `T-19-97`, `T-19-98` and `T-19-99` stay open at this plan's end; `T-19-99` is
closed only when `19-19`'s rule is certified by the corpus written here, because a corpus is evidence
about a control and there is no control yet.

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-19`, or by the two together.** `T-19-86`
remains open at `high` by explicit user scoping decision and `T-19-91` remains open at `high` with
three arms. The SUMMARY must say so first.

Purpose: make the corpus capable of failing on the SECOND AXIS of the boundary — which words arrive —
and prove it does.
Output: a fifth evidence file, a second named class axis with its own floors, two committed RED
states, one appended execution record, and an exact handoff number for `19-19` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-17-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@tests/envelope_wrapper_class.rs
@tests/envelope_literal_decision.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE, then RED — the fifth evidence file, both directions of the deletion axis</name>
  <files>tests/envelope_argv_deletion.rs</files>
  <precondition>
    Work in the main checkout at `596480c` or later with a clean tree — ISOLATION=none is forced for
    this phase, so do NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`,
    which is what every measurement is driven against. A `bash` and a writable directory first on
    `PATH` are available for the shim confirmations. The two `tests/driver_reattach.rs` failures are
    pre-existing and FLAKY.
  </precondition>
  <read_first>
    `tests/envelope_literal_decision.rs:1-140` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, and the
    one-`TempDir`-per-row discipline — and `:1355-1379` and `:1506-1560` for the two mechanism-pin
    shapes (an assertion over `policy::split_segments_with_heads` directly rather than over an exit
    code, and its non-vacuity partner). `19-SECURITY.md`'s sections "Threats found by audit 5" and
    "Audit 5 — what the round-5 control can and cannot fail on", for the measured rows and the
    discarded ones. `src/envelope/policy.rs:1072-1180` (`Token` with its `literal` doc, and
    `SEPARATORS` with the doc whose reasoning is right and whose consequence is not), `:1806-2108`
    (`tokenize` — the separator arm, the two quote loops and the backslash arm at `:2063-2072`),
    `:336-423` (`scan_leading`, which skips only tokens beginning with `-`), `:4492-4498`
    (`separators_are_named_in_one_list_that_the_predicate_reads`).
  </read_first>
  <action>
**Measure before you assert. Nothing in this file is written until it has been driven.**

Build, then drive every row below exactly as audit 5 did:

```
printf '{"tool_name":"Bash","tool_input":{"command":"<CMD>"}}' \
  | GSD_MM_ENVELOPE_ROOT=$(mktemp -d) ./target/debug/gsd-meta-manager envelope guard alpha
```

one fresh root per row, a walk over the whole root afterwards. Build the JSON with
`python3 -c json.dumps` so a backslash-newline survives as the two bytes it is. Then confirm every
claimed permit under `bash` with shims named `git`, `gh` and `glab` first on `PATH` printing their own
argv, and record the printed argv. **For the `T-19-98` rows, drive bash from a FILE and verify the
file's bytes with `od -c` first** — audit 5 did exactly that, and the three rows it discarded were
discarded on this step.

Any audit-5 row that does not reproduce at the recorded verdict: STOP, record the measured verdict,
and report it as a finding rather than asserting the audit's number over your own measurement.

Then create `tests/envelope_argv_deletion.rs`. Its header states what the file is: audit 5's
`T-19-97` and `T-19-98` reproducers plus the cells found while planning round 6, measured against the
built binary BEFORE the rule was written so the corpus cannot have been shaped to agree with the code,
and written as a FIFTH file rather than appended to `tests/envelope_literal_decision.rs` so round-5
and round-6 evidence stay attributable to the round that produced them. It states the invariant the
file is about — **the words the guard classifies must be exactly the words the program receives, in
the same order** — and that round 4's literalness bit is one half of it and this file is the other.
It also states that the whole file except its controls is RED at this plan's end, by design, and that
`19-19` closes it.

Carry the harness forward from `tests/envelope_literal_decision.rs` unchanged in shape, including the
walk. Print the walk for at least one refused forge row and one COUNTED row so the SUMMARY records
listings that were measured.

**Rows that must be REFUSED after `19-19`, each asserting exit code 2 and a named reason identifier:**

  - **the `T-19-97` class**, in a test whose doc records the mechanism in bash's own terms — a
    redirection is a word the guard reads and `execve` never sees, so it displaces every decision word
    one slot right, and `scan_leading` skips only tokens beginning with `-` so `>/dev/null` becomes
    the git VERB and `classify_git`'s denylist finds it in no arm. All sixteen `T-19-97` rows above.
    The comment records the three-leg argument audit 5 measured: layer 1's `Bash(git push:*)` prefix
    rule does not match a line beginning `git >`; layer 2 is defeated on the same line
    (`git >/dev/null -c core.hooksPath=/dev/null push --force origin main` → exit 0); and layer 3 is
    disarmed because the command-line `-c` outranks the envelope's env-injected `core.hooksPath`
    (D-09), re-measured with `/CLI_WINS` printed against the paired `/ENV_WINS` control. It records
    separately that four rows have **no second carrier at all** — `git stash`, `git update-ref`,
    `git reflog delete` and the SAFE-06 PR cap — so for those the guard is the only control and the
    cap is bypassed **uncounted rather than exceeded**;
  - **the `T-19-98` class**, in a test of its own, whose doc records that `\`+newline is not an escape
    but a LINE CONTINUATION bash deletes BOTH characters of before the word is assembled, that
    `tokenize`'s backslash arm (`policy.rs:2063-2072`) keeps the newline and deliberately does not
    clear `literal` on the stated reasoning that escaping is what makes a character literal — **which
    is true of every character except a newline** — and that the recovered word is therefore `pu\nsh`
    where the program receives `push`. All eight `T-19-98` rows above, including the DOUBLE-QUOTED
    spelling, which reaches the same place through the quote loop's own backslash branch;
  - **six of the seven cells found while PLANNING**, in a test of their own and named for the class,
    each with the sentence saying why the audit's rows do not reach it: `&>` because the guard's own
    separator arm splits a simple command bash does not split, `>|` and `<>` because they are
    two-character operators, `<<EOF` because bash runs it even as a single line,
    `touch input.txt && git <input.txt …` **labelled as a PRECONDITION row**, and `x2>` which is the
    over-deletion control below rather than a refusal row. **`{v}>` is the seventh and it is NOT
    asserted here** — see the deferred list below;
  - **the position rows that show where the hole is NOT**, pinned at the verdicts they already have so
    a later reader can see the class was drawn where it lives: `>/dev/null git push --force origin
    main`, `2>/dev/null git …` and `env >/dev/null git …` at exit 2 `force_push_blocked`, and
    `git push --force origin main >/dev/null` at exit 2 `force_push_blocked`. These are GREEN today
    and must stay green — they are controls, not reproducers, and the comment says so.

**Rows that must be PERMITTED after `19-19`, which are what make this a deletion MODEL rather than a
blanket refusal.** A deletion entry is verdict-PRESERVING, and a corpus without this half cannot fail
on an implementation that simply refuses everything carrying a `>`:

  - `git >/dev/null status` and `git >/dev/null log --oneline` — exit 0 today and after;
  - `>out gh pr create --title x` — exit 0 **with exactly one ledger line in a walked fresh root**
    today and after. This row doubles as the walk's non-blindness control;
  - `git log > out`, `git status > /tmp/s.txt`, `git diff > /tmp/d.patch`,
    `git commit -m "x" >> build.log`, `gh pr list 2>/dev/null` and
    `git fetch origin 2>&1 | tee log` — the ordinary-redirection corpus, all exit 0 today and all of
    which must still be exit 0 after. Their comment records that these are the measured cost of the
    REJECTED design option: refusing any governed simple command containing a token the shell deletes
    would refuse every one of them, and would turn `gh pr create --title x > /tmp/o` — **exit 0 with a
    ledger line today** — into a refusal, trading a correct COUNT for a false positive on ordinary
    syntax, which is the exact trade `T-19-93`'s bar forbids;
  - **`git x2>/tmp/o push --force origin main`** — the OVER-DELETION control, in a test of its own
    with the bash argv `ARGV[git]: [x2] [push] [--force] [origin] [main]` recorded in the comment.
    Permitted today, and it must STAY permitted, because after the fix the guard's argv equals git's
    argv and git itself rejects `x2` as not a git command. **This is this axis's `ls {git,svn}-repo`**:
    the row that tells a deletion model apart from a rule that deletes any word part before a `>` and
    displaces every decision word LEFT;
  - `git commit -m ">"`, `git log --grep='>'`, `git commit -m "a > b"`, `rg ">" src/` and
    `git push origin refs/heads/gsd-auto/alpha/w --push-option="a>b"` — a QUOTED redirection character
    is an ordinary character and must never be treated as an operator. All exit 0 today and after.

**The two pre-existing FALSE REFUSALS, pinned at their PRE-FIX verdicts beside their permitted twins,
in TWO `#[test]` fns whose names are FIXED BY THIS PLAN because `19-19` is required to replace their
bodies:**

```
the_redirected_in_namespace_push_is_falsely_refused_today_and_19_19_must_move_it
the_continued_in_namespace_push_is_falsely_refused_today_and_19_19_must_move_it
```

Both assert exit 2 `push_outside_namespace` today, each against
`git push origin refs/heads/gsd-auto/alpha/w` at exit 0.

**These two fns are the ONLY assertions in this file that `19-19` is permitted to REPLACE rather than
only add to**, and each carries a doc comment saying so in its own words: *this pin records a FALSE
REFUSAL measured before the rule existed; `19-19` replaces this body with the exit-0 assertion and
that replacement is the evidence the round removed over-refusal rather than adding it.* Naming them
here is what makes the handoff expressible — a pin `19-19` must move, under a prohibition allowing
only additions, is a plan that cannot be executed, which is the same class of defect as round 3's
arithmetically-unsatisfiable gate and round 4's stale handoff guard.

**Each carries its DERIVATION, and the two are different mechanisms — take them from the measured
message, not from a description of it:**

  - `git push origin refs/heads/gsd-auto/alpha/w > log.txt` → `REFUSED (reason:
    push_outside_namespace) — the refspec `>` resolves to `refs/heads/>`, which is outside
    `refs/heads/gsd-auto/alpha/``. The redirection word is read as an EXTRA refspec the program never
    receives;
  - `git push \`+NL+` origin refs/heads/gsd-auto/alpha/w` → `REFUSED (reason:
    push_outside_namespace) — the refspec `origin` resolves to `refs/heads/origin`, which is outside
    `refs/heads/gsd-auto/alpha/``. **The mechanism is DISPLACEMENT, not a mangled word**: the
    whitespace after the continuation flushes it, so `\`+newline becomes its own WORD occupying the
    REMOTE slot and every operand shifts one slot right — `origin` is then read as a refspec. This is
    the same displacement `T-19-97` produces, arriving through `T-19-98`'s mechanism, and the comment
    says so.

**`19-19` must move both to exit 0**, matching their twins, and the SUMMARY reports them as
over-refusal REMOVED rather than added.

**The THREE rows whose POST-fix verdict this plan cannot derive.** Each is measured pre-fix and the
measurement is **RECORDED IN A COMMENT AND A PRINT — never written as an assertion** — because this
plan measures pre-fix and its measure-first discipline cannot catch a wrong POST-fix expectation, and
a row pinned at a verdict the rule cannot produce lands RED with `19-19` forbidden to edit it. That
is exactly how `19-16` handled its two undeliverable rows. `19-19` measures each against the rule and
appends the pin.

  - `git >$F push --force origin main` and `git >*.log push --force origin main` — refused TODAY at
    `envelope_assertion_failed`, because round 4's literalness bit fires on the `$` and the glob in
    what the guard reads as the VERB. After deletion the surviving argv is `push --force origin main`,
    so each stays refused with a DIFFERENT reason identifier. Both are also PRECONDITION rows: bash
    answers `ambiguous redirect` when `F` is unbound and when the glob matches more than one file;
  - **`git {v}>/tmp/o push --force origin main`** — exit 0 today, `ARGV[git]: [push] [--force]
    [origin] [main]` under the shims. Its six siblings in the planning-cell group land on
    `force_push_blocked` after the fix because their redirection is DELETED and the surviving argv is
    the dangerous one; **this row does not, because `19-19` deliberately does not model a `{name}` fd
    prefix and marks it unresolvable instead**, which is a different identifier
    (`envelope_assertion_failed`) reached by a different clause. That is a `19-19` design outcome this
    plan cannot know, so this plan asserts only the PRE-fix exit 0 it measured and leaves the post-fix
    identifier to `19-19`. Its permitted-half twin `git {v}>/tmp/o status` is measured and recorded
    the same way, and for the same reason it is kept OUT of `DISPLACING_REDIRECTIONS` in Task 2.

**The three carried-forward MECHANISM pins**, re-asserted here over the same public functions rather
than moved or edited in place, because this round touches the tokenizer for the third time:

  - **Rule B** — `policy::split_segments_with_heads` still reports `head_is_command_position == false`
    for `C=GIT_CONFIG; env -u ${C}_COUNT git fetch origin` and the two other severed spellings, and
    still reports `true` for `{ git status; }` so the pin cannot be satisfied by marking every segment
    severed;
  - **round 4's literalness bit, asserted NON-VACUOUS** — over the tokens of
    `git pus? --force origin main` and `git >/dev/null push --force origin main`,
    `Token.literal` is `false` for `pus?` and `true` for both `>/dev/null` and `push`. This is the pin
    that turns red if `19-19` closes these rows by clearing `literal` for a redirection word instead
    of by modelling deletion — the bit is RIGHT about them, and a fix that made it wrong to get a
    refusal would be the inversion quietly becoming dead code with every verdict green;
  - **`policy::is_separator(">")` is `false`** — the mechanical proof that the fix did not take the
    route `SEPARATORS`' own doc forbids. `separators_are_named_in_one_list_that_the_predicate_reads`
    in `policy.rs` asserts the same fact and must stay GREEN and UNMODIFIED; this file asserts it too
    so the requirement is visible in the round's own evidence.

Commit this as its own commit, RED. State the per-binary counts in the SUMMARY.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_argv_deletion --no-fail-fast 2>&amp;1 | tail -40; rtk proxy cargo test --test envelope_literal_decision &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_expansion_slots &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_command_position &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy --tests -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 && echo "ZERO-SRC-HUNKS-RED-COMMITTED"</automated>
  </verify>
  <done>Every row was MEASURED against the built binary with a fresh envelope root and a walk before it was written as an assertion, and every claimed permit was confirmed under bash shims with the `T-19-98` rows driven from a file whose bytes `od -c` verified; `tests/envelope_argv_deletion.rs` exists as a FIFTH file carrying the sixteen `T-19-97` rows, the eight `T-19-98` rows, the seven planning cells, the four position controls that are already refused, the permitted half including the ordinary-redirection corpus and the quoted-`>` rows, the over-deletion control `git x2>/tmp/o push --force origin main` pinned PERMITTED with its bash argv in the comment, the two pre-existing false refusals pinned at their pre-fix verdicts beside their permitted twins in the two `#[test]` fns this plan NAMES with their doc comments saying `19-19` replaces those bodies, the precondition rows labelled as such, and the three mechanism pins; the THREE undeliverable-verdict rows — `>$F`, `>*.log` and **`{v}>`** with its permitted-half twin — are RECORDED IN COMMENTS AND PRINTS and are **NOT written as assertions**, verified by reading the file for an `assert` on any of them; `--test envelope_literal_decision`, `--test envelope_expansion_slots` and `--test envelope_command_position` are fully GREEN and unmodified; the new file is RED with every failing name recorded verbatim; `git show --stat` on this commit shows ZERO `src/` hunks; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: the SECOND AXIS — `DELETION_CLASSES`, the displacement alphabet, and its floors</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:2780-3030` (section 13's header, `brace_pairs`, the seven
    predicates and `UNREADABLE_CLASSES` — **read to mirror the SHAPE, not to edit**), `:3029-3250`
    (the per-alphabet floor, the per-class floor with its degenerate-proofing block, and the counted
    floors `MIN_UNREADABLE_GENERATED_CASES` / `MIN_GENERATED_CASES_PER_UNREADABLE_CLASS`),
    `:3240-3400` (`FORGE_SLOTS`, `DISPLACING_OPTS` and
    `a_forge_decision_slot_carrying_a_splice_or_a_glob_is_refused_in_every_generated_slot` — the
    DISPLACEMENT property this task's new property is modelled on, including its
    `MIN_UNREADABLE_FORGE_SLOT_CASES` floor which audit 5 re-derived as exactly equal to the
    generation count), `:2545-2604` (`SEVERED_PREFIXES`, `SEVERED_BRACE_PREFIXES` and the precedent
    for giving a new axis its OWN alphabet and OWN property rather than widening one whose floor the
    new class cannot satisfy).
  </read_first>
  <action>
**The seven assembly classes are not touched.** `UNREADABLE_CLASSES`, its predicates, its
degenerate-proofing assertions and all three of its floors stay byte-identical. This task adds a
SECOND axis beside them, in the shape section 13a already establishes, and the file's own section
header records why it is a second axis rather than two more entries: audit 5 enumerated bash's word
expansions one at a time and found no gap in word ASSEMBLY, so the gap moved AXIS — from *how a word
is written* to *which words arrive* — and an axis smuggled into the assembly classes stops being
legible as one.

**1. `DELETION_CLASSES` — five classes, each with a degenerate-proof predicate.** Named once so the
per-alphabet floor, the per-class floor and the counted floor all count the same thing:

  1. **redirection with a separate target word** — an unquoted redirection operator standing as its
     own whitespace-delimited word or leading one, with a following word (`git >/dev/null push …`);
  2. **ATTACHED redirection** — an unquoted redirection operator with a NON-EMPTY literal run before
     it inside the same whitespace-delimited word (`git push>/dev/null --force …`). **Class 1 cannot
     satisfy this**, which is the whole reason it is separate: the operator need not be its own word,
     and a whitespace-delimited predicate walks straight around it;
  3. **a multi-character or fd-carrying operator** — `>>`, `2>`, `1>`, `<<<`, `<<`, **`<<-`**, `>|`,
     `<>`, `>&`, `<&`, `&>`, **`&>>`**. **A bare `>` cannot satisfy this**, and the class exists
     because five of the seven planning cells are exactly here. `<<-` and `&>>` are named in the
     class list AND drawn by the alphabet, because they are the two spellings `19-19`'s production
     uniquely adds and a class list that named them without an entry drawing them would be a floor
     nothing satisfies;
  4. **character-pair deletion INSIDE a word** — a backslash-newline with a non-empty literal run on
     BOTH sides inside one word (`git pu\`+NL+`sh …`);
  5. **character-pair deletion at a word BOUNDARY** — a backslash-newline with whitespace or a word
     boundary on at least one side (`git \`+NL+`push …`). **Class 4 cannot satisfy this and class 5
     cannot satisfy class 4**, which is the split that makes the pair non-degenerate.

Every predicate respects quoting the way `draws_a_glob` and `draws_a_tilde` already do — a `>` inside
single quotes is an ordinary character and must NOT satisfy any class, because
`git log --grep='>'` is pinned permitted in Task 1 and a predicate that counted it would make the
floors satisfiable by a row the rule must never touch.

**2. The degenerate-proofing block, asserted rather than described**, in the shape
`the_corpus_can_draw_every_one_of_the_seven_unreadable_classes` already uses:
`git >/dev/null push` satisfies class 1 and NOT class 2; `git push>/dev/null` satisfies class 2;
`git > /tmp/o push` satisfies class 1 and NOT class 3; `git 2>/dev/null push` satisfies class 3;
`git pu\`+NL+`sh` satisfies class 4 and NOT class 5; `git \`+NL+`push` satisfies class 5 and NOT
class 4; and **`git log --grep='>'` satisfies NONE**, which is the quoting control.

**3. `DISPLACING_REDIRECTIONS` — the alphabet, spliced where the hole is.** Not a wrapper prefix and
not an operand: the entries are spliced **between the governed program and its decision words**,
because the prefix position is already refused at `596480c` and drawing it would give a property that
is green before the fix. Entries span the classes and the measured cells — at minimum `>/dev/null`,
`2>/dev/null`, `1>/dev/null`, `>>/tmp/x`, `> /tmp/o`, `<<<x`, `>|/tmp/o`, `<>/tmp/o`, `&>/tmp/o`,
**`&>>/tmp/o`** and **`<<-EOF`** — and a separate `CONTINUATIONS` alphabet carries the
backslash-newline splice points. The doc above each alphabet records that a redirection entry is
**verdict-PRESERVING** after the fix, which is the opposite of the brace entries `19-16` had to keep
out of the invariance-preserving alphabets, and that this is why the property below asserts refusal
over refused bases and PERMISSION over permitted ones.

**`&>>` and `<<-` are minimum entries because they are the two spellings the rule's production
uniquely adds**, and both are measured at `596480c`: `git &>>/tmp/o push --force origin main` and
`git <<-EOF push --force origin main` are at exit 0 with `ARGV[git]: [push] [--force] [origin] [main]`
under the shims, and `git &>>/tmp/o status` and `git <<-EOF status` are at exit 0 with
`ARGV[git]: [status]` — live on the refused half and verdict-preserving on the permitted half, so both
arms of the property can draw them. A corpus without them cannot fail on the two operators the fix
adds beyond what any other row exercises.

**`{v}>/tmp/o` is DELIBERATELY NOT in this alphabet, and the doc says why rather than leaving it to be
inferred.** Every entry here must be verdict-PRESERVING, because the property's permitted arm asserts
the wrapped verdict EQUALS the unwrapped one. A `{name}` fd-allocation prefix is the one spelling
`19-19` does **not** model — it is marked unresolvable and refuses — so `git {v}>/tmp/o status` would
be STRICTER than its base and would break invariance in exactly the direction `19-16` recorded for the
`GIT_CONFIG_COUNT=0` prefix, turning this property permanently red in a file `19-19` may not edit. It
is pinned instead as a single fail-closed row in Task 1, with its POST-fix verdict left for `19-19` to
derive. This is the same call `19-16` made when it gave `SEVERED_BRACE_PREFIXES` its own alphabet
rather than lowering a floor its class could not satisfy.

**4. The generative property**, modelled directly on
`a_forge_decision_slot_carrying_a_splice_or_a_glob_is_refused_in_every_generated_slot`: for each
governed base whose unwrapped form is REFUSED, splice each alphabet entry at each position between the
program and its decision words, assert **refusal AND an empty walk**, and record the slots seen; for
each base whose unwrapped form is PERMITTED, splice the same entries and assert the verdict is
**unchanged**, with the positive walk control preceding the loop so an empty walk means something.
Floors in the file's existing shape — `MIN_DELETION_CLASSES`, `MIN_DISPLACING_REDIRECTIONS`,
`MIN_DELETION_CASES`, `MIN_GENERATED_CASES_PER_DELETION_CLASS` and a `slots_seen.len()` floor —
**each computed from the generation arithmetic and stated in the doc rather than guessed**. Audit 5
found `19-16` set a floor of 50 against a maximum of 40 by construction; state the arithmetic
(entries × bases × slots × depths) in the constant's doc, and make the floor exactly equal to the
count so losing one case turns it red.

**5. The per-alphabet floor for the new axis**, in the shape of
`every_alphabet_this_round_widens_can_draw_a_word_the_guard_cannot_read` — every alphabet this round
widens must be able to draw at least one deletion class, with a failure message that says the correct
response is to RESTORE entries and never to delete the floor.

This whole task is RED by design except the permitted half and the degenerate-proofing block: the
refusal assertions cannot pass until `19-19` exists. Commit as its own commit, second, with ZERO
`src/` hunks.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2>&amp;1 | tail -40; rtk proxy cargo clippy --tests -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c 'UNREADABLE_CLASSES' tests/envelope_wrapper_class.rs; git diff HEAD~1 --numstat -- tests/envelope_wrapper_class.rs; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 && echo "SECOND-AXIS-RED-ZERO-SRC"</automated>
  </verify>
  <done>`UNREADABLE_CLASSES`, its seven predicates, its degenerate-proofing assertions and all three of its floors are byte-identical — verified by `git diff` showing no deletion inside that region; `DELETION_CLASSES` names five classes with degenerate-proof predicates that respect quoting, with the assertion block proving each pair non-degenerate and `git log --grep='>'` satisfying NONE; `DISPLACING_REDIRECTIONS` and `CONTINUATIONS` exist and splice BETWEEN the governed program and its decision words rather than at a wrapper prefix; `&>>/tmp/o` and `<<-EOF` are entries and `<<-`/`&>>` are named in class 3, so the corpus can fail on the two operators `19-19`'s production uniquely adds; **`{v}>/tmp/o` is NOT an entry in `DISPLACING_REDIRECTIONS` and the doc records why** — every entry there must be verdict-PRESERVING and `19-19` does not model a `{name}` fd prefix, so including it would break the permitted arm in a file `19-19` cannot edit; the generative property asserts refusal AND an empty walk over refused bases and an UNCHANGED verdict over permitted ones, with a positive walk control preceding the loop; every new floor's arithmetic is stated in its doc and equals the generation count; no existing floor, alphabet entry or property was lowered, deleted or narrowed; the new property is RED with every failing name recorded; `git show --stat` shows ZERO `src/` hunks; and this is the second commit.</done>
</task>

<task type="auto">
  <name>Task 3: the registrations, the `T-19-17r` bookkeeping gap, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s sections "Threats found by audit 5", "A bookkeeping gap in `T-19-17r` — flagged,
    not counted as a threat", "Audit 5 — what the round-5 control can and cannot fail on" and
    "Audit 5 Sign-Off", plus the appended execution records of plans 19-16 and 19-17 which this plan's
    subsection FOLLOWS and does not edit. `deferred-items.md`'s "Round-5 closure status (recorded by
    19-17)" section and its `T-19-96` entry.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after `19-17`'s and touching nothing that precedes
it, titled so a reader cannot mistake it for an audit finding — an execution record made by plan
19-18 — carrying:

  - **the invariant this round is about**, stated once: the words the guard classifies must be exactly
    the words the program receives, in the same order; round 4's `Token.literal` is one half and
    survival into argv is the other;
  - **every measured row with its walk**, in the column shape the audit uses: the sixteen `T-19-97`
    rows, the eight `T-19-98` rows, the seven planning cells, the four position controls that are
    already refused, the permitted half, and the two pre-existing FALSE REFUSALS beside their twins;
  - **the bash-shim argv for every claimed bypass**, and for the `T-19-98` rows the `od -c` output
    proving the file's bytes;
  - **any audit-5 row that failed to reproduce**, with the measured verdict — or the explicit
    statement that all of them did;
  - **the design question, answered**, with the rejected option named and its measured cost: refusing
    any governed simple command containing a token the shell deletes would refuse `git log > out`,
    `git status > /tmp/s.txt`, `git diff > /tmp/d.patch`, `git commit -m "x" >> build.log`,
    `gh pr list 2>/dev/null` and `git fetch origin 2>&1 | tee log`, all permitted today, and would
    turn `gh pr create --title x > /tmp/o` — exit 0 **with a ledger line** today — into a refusal.
    `19-19` models deletion instead, and the record says so here so the choice is auditable before the
    rule exists;
  - **the `T-19-17r` bookkeeping gap, recorded as OUTSTANDING and deliberately NOT resolved.** State
    that `19-17-SUMMARY.md` calls it "accepted"; that audit 5 confirmed the measurement and both pins
    at `tests/envelope_literal_decision.rs:1355-1379`; that there is **no `AR-19-13` row in the
    Accepted Risks Log and no register row**; and that **this plan does not add the acceptance,
    because accepting a risk is a human decision and audit 5 explicitly declined to make it.** The
    next round either adds the log row or drops the word "accepted" — and this plan does neither;
  - **audit 5's disclosed corpus limit, forwarded to `19-19` with its reason**: `sh {-c,"git push
    --force …"}` is refused because a quote inside an alternative is unenumerable, which is correct and
    fail-closed, but the corpus cannot distinguish that refusal from an enumerated one. It is
    addressed in `19-19` rather than here because the only place the distinction is observable is a
    unit assertion over the private whole-word product scan in `policy.rs`'s own test module, and this
    plan may not touch `src/`;
  - **the statement that this plan CLOSES NOTHING**, that `T-19-97`, `T-19-98` and `T-19-99` all stay
    open, that `T-19-86` and `T-19-91` remain OPEN at `high` so `/gsd-secure-phase 19` is not cleared,
    and that re-measuring and re-classifying these rows is that command's job rather than a plan's.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Record that the round-6 corpus exists and is RED for `T-19-97`, `T-19-98`
and `T-19-99`; fold the seven planning cells into `T-19-97`'s entry as spellings of its class rather
than registering new threat IDs, with the provenance caveat `19-14` established; add the `T-19-17r`
bookkeeping gap as its own entry with the acceptance explicitly unmade. Leave `T-19-86`, `T-19-91`,
`T-19-96` and `T-19-74` unchanged and open.

**3. The handoff gate.**

  - The command is `rtk proxy cargo test --no-fail-fast`. A plain `cargo test` fail-fasts at
    `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a
    single envelope suite; `rtk proxy` is required because the RTK hook strips exactly the `warning:`
    and `test result:` lines any count is read from (D-34).
  - **State the gate arithmetic explicitly rather than assuming it.** `passed + failed` is strictly
    greater than **1584**, the total `19-17-SUMMARY.md` records and audit 5 re-measured. A red test
    RAN, so the increase comes only from the NEW `#[test]` fns this plan adds — count them from
    `git show` and check the identity against the observed number. A disagreement is a finding.
  - Every failure name is either one of the two flaky `driver_reattach` names
    (`a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`,
    `a_run_killed_without_an_ending_is_reported_crashed_and_nothing_on_disk_is_repaired`) or one of
    the RED names this plan creates. **The RED set is non-empty and every one of its names is listed
    in the SUMMARY** — that list is `19-19`'s handoff contract and the one number a run that did
    nothing cannot forge.
  - `--test envelope_literal_decision`, `--test envelope_expansion_slots` and
    `--test envelope_command_position` are FULLY GREEN and unmodified.
  - `cargo build` and `cargo clippy --tests -- -D warnings` exit 0.
  - Record the per-binary counts for all six envelope suites.

Commit the record as its own commit, the third.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy --tests -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-18-gate.log 2>&amp;1; echo "full_suite_exit=$?"; tail -80 /tmp/19-18-gate.log; rtk proxy grep -c 'AR-19-13' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; git diff --stat HEAD~3..HEAD | tail -12; git log --oneline -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-18 execution record with the invariant, every measured row and its walk, the bash-shim argv, the `od -c` bytes for the `T-19-98` rows, the design question answered with the rejected option's measured cost, the `T-19-17r` gap recorded as OUTSTANDING with the acceptance deliberately unmade, audit 5's disclosed corpus limit forwarded to `19-19` with its reason, and the statement that nothing is closed and no gate is cleared — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection, and NO `AR-19-13` row anywhere; `deferred-items.md` records the RED corpus, folds the seven planning cells into `T-19-97`'s class, and adds the `T-19-17r` gap with `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` unchanged; the `--no-fail-fast` run reports `passed + failed` strictly greater than 1584 with the arithmetic stated and checked against `git show`, every failure name is a `driver_reattach` name or one of this plan's RED names, and the complete RED list is in the SUMMARY; the three prior evidence files are green and unmodified; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, and then to the words that decide, before it runs |
| written word → word the shell runs | Round 4 closed this half: which BYTES reach `execve` is decided by expansions the guard does not perform |
| **words read → words that ARRIVE** | **The other half, and this round's boundary: the shell DELETES words (a redirection) and characters (a line continuation) that the guard reads, so the argv a classifier answers about is not the argv that runs** |
| corpus → the control it certifies | An alphabet that cannot DRAW a class is a property that cannot FAIL on it, and five consecutive rounds have shipped one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-99 | Spoofing | the 19-16/19-17 generative alphabets and `UNREADABLE_CLASSES` | medium | mitigate | The corpus is widened FIRST and observed RED, which is the ordering that worked twice. `DELETION_CLASSES` stands as a SECOND named axis beside the seven untouched assembly classes, with five degenerate-proof predicates, a displacement alphabet spliced where the hole actually is — between the governed program and its decision words, the prefix position being already refused — and per-alphabet, per-class and counted floors whose arithmetic is stated rather than guessed. Closed only by `19-19`, because a corpus is evidence about a control and there is no control yet |
| T-19-97 | Elevation of Privilege | `policy::tokenize` / `SEPARATORS` — a redirection is an ordinary word to the guard and is REMOVED from argv by the shell | high | mitigate (corpus only; rule is `19-19`) | Sixteen audit-5 rows plus seven cells found while planning are measured at exit 0 with empty walks, confirmed under bash shims, and written as RED assertions. The PERMITTED half is written beside them — `git >/dev/null status`, the ordinary-redirection corpus, the quoted-`>` rows, and the over-deletion control `git x2>/tmp/o push --force origin main` — so the corpus can fail on a blanket refusal and on over-deletion, not only on the bypass. `SEPARATORS` is not changed and `is_separator(">") == false` is pinned as the mechanical proof that the forbidden route was not taken |
| T-19-98 | Elevation of Privilege | `policy::tokenize`'s backslash arm — a line continuation is KEPT in the word instead of deleted | high | mitigate (corpus only; rule is `19-19`) | Eight rows measured at exit 0, every one driven from a file whose bytes `od -c` verified, including the double-quoted spelling that reaches the same place through the quote loop's own backslash branch. Audit 5's three discarded rows are NOT re-added and are recorded as discarded so audit 6 does not spend the measurement again. The pre-existing FALSE REFUSAL `git push \`+NL+` origin refs/heads/gsd-auto/alpha/w` is pinned beside its permitted twin, so `19-19` has to REMOVE an over-refusal rather than add one |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — bookkeeping gap RECORDED, acceptance deliberately unmade** | `19-17-SUMMARY.md` calls it accepted; there is no `AR-19-13` row and no register row. Audit 5 confirmed the measurement and both pins and explicitly declined to add the acceptance because that is a human decision. This plan records the gap as OUTSTANDING and makes no acceptance, adds no `AR-19-13` row, and does not use the word "accepted" for it |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; four rows still at exit 0 and `the_t_19_86_residual_is_permitted_today_and_this_plan_leaves_it_permitted` stays green UNMODIFIED. Because it remains open at `high`, this plan does NOT clear `/gsd-secure-phase 19`, and the SUMMARY says so first |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (registered, three arms, not remedied)** | `reflog $S`, `reflog show $S` and `symbolic-ref $S` at exit 0 with no second carrier; `git push $REF` cwd-dependent and permitted in-namespace, a THIRD arm and not "already fails closed". Record unweakened, no remedy added, counts toward `threats_open` |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG, one slot outside the decision region | medium | **accept for this plan (registered by `19-16`, not fixed)** | Left exactly as pinned. Widening the decision region to `classify_push`'s flags is the same move as closing `T-19-91` and both are a later round's to decide |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added; this plan touches only `tests/` and `.planning/`. `Cargo.toml` and `Cargo.lock` are not in `files_modified` |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy --tests -- -D warnings` (D-34).
  The suite number MUST come from a `--no-fail-fast` run and MUST be read as `passed + failed` against
  1584, the exact total `19-17-SUMMARY.md` records and audit 5 re-measured.
- The gate arithmetic is STATED and CHECKED: expected `passed + failed` = 1584 plus the number of NEW
  `#[test]` fns this plan adds, counted from `git show`. A red test RAN, so red→green contributes
  nothing; a disagreement is a finding.
- The RED set is NON-EMPTY, every one of its names is listed in the SUMMARY, and every other failure
  is one of the two named `driver_reattach` tests.
- Every commit shows ZERO `src/` hunks under `git show --stat`. This is the evidence the split exists
  to produce.
- `UNREADABLE_CLASSES`, its seven predicates, its degenerate-proofing block and all three of its
  floors are byte-identical; no existing `MIN_*` constant was lowered.
- `--test envelope_literal_decision`, `--test envelope_expansion_slots` and
  `--test envelope_command_position` are fully GREEN and byte-identical.
- The three mechanism pins are present and non-vacuous: `split_segments_with_heads` still reports
  `head_is_command_position == false` for the severed spellings and `true` for `{ git status; }`;
  `Token.literal` is `false` for `pus?` and `true` for `>/dev/null` and `push`; and
  `is_separator(">")` is `false`.
- The over-deletion control `git x2>/tmp/o push --force origin main` is pinned PERMITTED with its bash
  argv recorded, and the permitted half of the deletion axis — `git >/dev/null status`, the ordinary
  redirection corpus and the quoted-`>` rows — is pinned at exit 0.
- `>out gh pr create --title x` leaves exactly ONE ledger line in a walked fresh root, which is the
  walk's non-blindness control.
- The two pre-existing false refusals are pinned at exit 2 `push_outside_namespace` beside their
  permitted one-line twins at exit 0.
- No `AR-19-13` row exists anywhere and the word "accepted" is not applied to `T-19-17r`.
- `SEPARATORS` is byte-for-byte unchanged and no change to it is proposed.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after `19-17`'s and nothing
  else.
- Three commits exist in order: the fifth evidence file, the second axis, the record.
</verification>

<success_criteria>
1. Every row was MEASURED against the built binary with a fresh envelope root and a walk before it was
   written as an assertion, and every claimed permit was confirmed under bash shims — the `T-19-98`
   rows from a file whose bytes `od -c` verified.
2. The corpus goes RED against the pre-fix tree, the RED set is non-empty, and every failing name is
   recorded verbatim in the SUMMARY as `19-19`'s handoff contract.
3. The deletion axis is legible as a SECOND axis: `DELETION_CLASSES` with five degenerate-proof
   predicates stands beside a byte-identical `UNREADABLE_CLASSES`, and nothing was smuggled into the
   seven assembly classes.
4. The corpus can fail in BOTH directions — the bypass rows, and the over-deletion control
   `git x2>/tmp/o push --force origin main` pinned PERMITTED with the bash argv `[x2] [push] …`
   recorded beside it.
5. The displacement alphabet splices BETWEEN the governed program and its decision words; the prefix
   and trailing positions are pinned at the verdicts they already have, as controls rather than
   reproducers.
6. The two pre-existing FALSE REFUSALS are pinned pre-fix beside their permitted twins in the two
   `#[test]` fns this plan NAMES, each documented as a body `19-19` is required to replace — so the
   handoff is expressible rather than a pin the next plan is forbidden to move.
6a. The THREE undeliverable rows — `>$F`, `>*.log` and `{v}>` with its permitted twin — are RECORDED
   rather than asserted, and `{v}>` is kept out of `DISPLACING_REDIRECTIONS` because that alphabet's
   property asserts verdict PRESERVATION and `19-19` does not model a `{name}` fd prefix.
7. The `T-19-17r` bookkeeping gap is recorded as OUTSTANDING, no `AR-19-13` row is added, and the word
   "accepted" is not used for it.
8. No `src/` line changed; no existing floor, alphabet entry, property or assertion was lowered,
   deleted or narrowed; and `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` are untouched.
9. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared — `T-19-86` and `T-19-91`
   remain OPEN at `high` — and no unqualified "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-18-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared and that this plan
closes nothing; the three commit SHAs in order with `git show --stat` proving ZERO `src/` hunks in
each; the `--no-fail-fast` `passed + failed` totals before and after with the gate arithmetic stated
and checked against `git show`; the COMPLETE list of RED test names, which is `19-19`'s handoff
contract; the per-binary counts for all six envelope suites; a table of every measured row with its
exit code, reason identifier, walk listing and bash-shim argv, split into the `T-19-97` rows, the
`T-19-98` rows, the seven planning cells, the position controls, the permitted half and the two
pre-existing false refusals; the `od -c` bytes for the backslash-newline rows; the rows LABELLED as
precondition rows and the explicit note that audit 5's three discarded rows were not re-added; the two
rows whose post-fix verdict is left for `19-19` with the reason; the design question answered, naming
the rejected option and its measured cost including `gh pr create --title x > /tmp/o` being COUNTED
today; the `T-19-17r` bookkeeping gap recorded as OUTSTANDING with the explicit statement that this
plan did NOT accept it and added no `AR-19-13` row; audit 5's disclosed corpus limit forwarded to
`19-19` with its reason; and a "what remains uncovered" section naming `T-19-86` FIRST — open at
`high` by user scoping decision — followed by `T-19-91` (open at `high`, three arms, `reflog` and
`symbolic-ref` with no second carrier), `T-19-96`, `T-19-74`, `T-19-84`, `T-19-85` and
`T-19-61`…`T-19-73`, with the explicit statement that only the WRAPPER-OPERAND sub-class of `T-19-60`
is closed.
</output>
