---
phase: 19-gitsafe-git-blast-radius-envelope
plan: 24
type: execute
wave: 14
gap_closure: true
gap_ids: [T-19-105, T-19-108, T-19-109, T-19-106]
depends_on: [19-23]
files_modified:
  - tests/envelope_reparsed_value.rs
  - tests/envelope_wrapper_class.rs
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
  - .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md
autonomous: true
requirements: [SAFE-02, SAFE-05, SAFE-06]

estimate:
  tokens: 118000
  raw_tokens: 118000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**THE CORPUS IS WIDENED FIRST AND IT MUST GO RED AGAINST THE PRE-FIX CODE, and this is the SIXTH time that ordering is the reason a plan exists separately.** It worked five times — 19-14/19-15, 19-16/19-17, 19-18/19-19, 19-20/19-21, 19-22/19-23 — and the cross-plan carry-forward is what proves two rules separately load-bearing. This plan writes the corpus and the reproducers and STOPS. `19-25` writes the rules. `T-19-105` is `T-19-76`'s failure mode for the NINTH consecutive round, and for the SECOND round running the gap is not one axis further out but one REGION over on the SAME axis: audit 8 verified mechanically that `CONFIG_RESOLUTION_CLASSES`'s five classes are **all argv or environment carriers** and that `grep -rn \"alias\\.[a-z]*=\\\"-c\\|alias.*include\\.path\" src/ tests/` returns **nothing at all**. Re-verified while planning at `21655c5`: the grep still returns nothing. The corpus is structurally incapable of generating, and so of failing on, `T-19-108`."
    - "**If the widened corpus does not go RED against the pre-fix tree, that is a FINDING — stop and report it, do not proceed.** A row already refused certifies nothing about the class this round is about, and a property green before the fix is a property that could not have failed on it. Every row here is measured against the built binary BEFORE it is written as an assertion, and any row that does not reproduce is reported with its measured verdict instead of being asserted anyway."
    - "**THE MOST IMPORTANT CONSTRAINT IN THIS PLAN, FOUND BY MEASUREMENT WHILE PLANNING AND STATED FIRST BECAUSE A BLANKET `alias.*` REFUSAL IS UNDISCHARGEABLE: GIT'S SHELL-ALIAS RULE IS A ONE-BYTE FACT AND THE CORPUS MUST FENCE IT MECHANICALLY.** A rule that refused every `alias.*` assignment turns **two evidence files this round may not edit** permanently RED: `tests/envelope_command_position.rs:550` asserts `permits(\"git -c alias.p='!git push --force origin main' p\")` — round 3's registered `T-19-86` row — and `tests/envelope_config_resolution.rs:1539-1543` asserts `permits(\"git config alias.p \\\"!git push --force origin HEAD:refs/heads/main\\\"\")` — `19-22`'s persisted `T-19-86` arm. Both must keep exiting 0 by explicit user scoping decision. **The carve-out is not a concession, it is git's own documented rule and it is MEASURED in both directions at `21655c5` against `git version 2.43.0`, with the envelope's own injection as the control (`/ENV_WINS`)**: `git -c alias.b='!git config --get core.hooksPath' b` → **`/ENV_WINS`** — a `!` body runs through the SHELL as a CHILD which **INHERITS the injection**, so layer 3 is intact — while `git -c alias.a='-c include.path=<f> config --get core.hooksPath' a` → **`/INCLUDE_WINS`** — a non-`!` body is re-parsed by git **IN-PROCESS** as its own argv **including its leading options**, so the `-c` inside it is applied by git itself at command-line precedence. The boundary is the FIRST BYTE and nothing else: `git -c alias.g='config --get core.hooksPath !x' g` → `/ENV_WINS` (a `!` that is not first is not a shell body) and `git -c alias.d=' !git config --get core.hooksPath' d` → `expansion of alias 'd' failed; '' is not a git command` (a leading space makes git refuse to expand at all). **This is `19-22`'s `-c a=b` seam and `19-18`'s `{v}>` blocker, one round over, caught at PLAN time, and it is fenced mechanically in this corpus rather than left to care.**"
    - "**THE AXIS DID NOT MOVE — THE REGION DID, AND THAT IS AUDIT 8'S OWN FRAMING, ADOPTED.** Round 8's confinement clause is the right SHAPE of rule: it asks whether an assignment can be BOUNDED, not whether it spells a name. **But it reads config assignments in TWO regions — the leading-option region `scan_leading` walks, and the environment — and git resolves configuration from a THIRD: a value the guard itself CONFINED and let through.** `alias.q` names the section `alias`, which is not an indirection, so `config_key_names_an_indirection_section` correctly answers `false`, the assignment is CONFINED, and the carrier rides inside its VALUE into a position `scan_leading` never reads. So `CONFIG_RESOLUTION_CLASSES` gets a **SIXTH class on the SAME axis** — *a carrier delivered inside a config VALUE the guard confines* — rather than a fifth named axis. **Standing this up as a new axis would model a region as if it were a stage and lose exactly the distinction audit 8 drew**; the axis is still *what the verb runs under*, and the class is the region the round-8 rule does not reach."
    - "**`T-19-108` IS A DEMONSTRATED EXPLOIT AND EVERY ROW OF IT IS MEASURED, NOT ARGUED.** Re-measured while planning at `21655c5` against the built binary, one fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards and EMPTY in every one: `git -c alias.q=\"-c include.path=/tmp/evil.cfg push --force origin HEAD:refs/heads/main\" q` → **exit 0 on a SINGLE line**; `git config alias.p '-c include.path=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w'` → **0** and `git p` → **0**, in two separately-permitted tool calls the stateless guard cannot correlate; `git -c alias.z=\"-c core.hooksPath=/dev/null push origin refs/heads/gsd-auto/alpha/w\" z` → **0**, which reaches the by-name deny plan 19-02 built for exactly that setting. **The paired discriminators still refuse on the same line shapes, which is the evidence the mechanism is present and this is a gap in REACH**: `git -c include.path=/tmp/evil.cfg push --force origin main` → 2 `envelope_assertion_failed` and `git -c core.hooksPath=/dev/null status` → 2 `hook_bypass_blocked`. Against real git the class resolves in both deliveries and at both keys: `-c alias.x='-c include.path=<f> …'` → `/INCLUDE_WINS`, the persisted `git config alias.y '-c include.path=<f> …'` then `git y` → `/INCLUDE_WINS`, and `-c alias.z='-c core.hooksPath=/ALIAS_WINS …'` → `/ALIAS_WINS`."
    - "**THE PERSISTED ARM IS NOT A PRECEDENCE CONTEST, AND THAT ASYMMETRY WITH `include.path` IS THIS ROUND'S SHARPEST MEASURED FACT.** Audit 8 measured that persisted `git config include.path <evil>` is INERT at every repository-side level — repo-local, `--worktree` and GLOBAL — because the envelope's injection outranks it, and concluded correctly that the `git config` subcommand path needs no clause for the include family. **An alias is different in kind: it does not have to WIN a precedence contest, it only has to EXIST.** Measured while planning under the envelope's own `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` redirect at a generated helper-free file: `git config --global alias.g '-c include.path=<f> config --get core.hooksPath'` then `git g` → **`/INCLUDE_WINS`**, and the repo-local spelling → **`/INCLUDE_WINS`**. So the persisted delivery is live at both levels, including the one the envelope itself controls, and closing `T-19-108`'s second leg requires the question to be asked at the `git config` key operand as well as at the `-c` carrier. **The corpus must therefore be able to fail at BOTH regions**, and it must record the include/alias asymmetry so `19-25` cannot mistake one for the other and add an inert clause."
    - "**WHAT ELSE GIT RE-PARSES FROM A CONFIG VALUE IS ENUMERATED AND MEASURED, NOT ASSUMED, AND THE ENUMERATION HAS EXACTLY THREE KINDS.** (K1) **re-parsed as a GIT command line, in-process, including its own leading options** — `alias.<name>` with a non-`!` body, and measurement says it is the ONLY member; (K2) **re-parsed as a SHELL command line, executed as a CHILD that INHERITS the envelope's injection** — `alias.<name>` with a `!` body, `core.pager`, `pager.<cmd>`, `core.editor`, `sequence.editor`, `core.askPass`, `credential.helper`, `core.sshCommand`, `core.gitProxy`, `core.fsmonitor`, `core.alternateRefsCommand`, `diff.external`, `diff.<d>.command`, `diff.<d>.textconv`, `merge.<d>.driver`, `mergetool.<t>.cmd`, `difftool.<t>.cmd`, `filter.<n>.clean`/`.smudge`/`.process`, `gpg.program`, `uploadpack.packObjectsHook`, `protocol.<n>.command`, `remote.<n>.uploadpack`/`.receivepack`, `trailer.<t>.command`, `submodule.<n>.update`, `web.browser`/`browser.<t>.cmd`; (K3) **re-parsed as a FILE PATH spliced at the directive's precedence** — `include.path`, `includeIf.<cond>.path`, closed by `19-23`. **K2 is INERT for layer 3 and that is a measurement, not a category argument**: `git -c alias.b='!git config --get core.hooksPath' b` prints `/ENV_WINS`, i.e. the child inherits the triplet, which is audit 8's own finding for `GIT_PAGER`/`GIT_EDITOR`/`GIT_SSH` restated on the config side. **At least three K2 representatives are re-measured here rather than accepted from this list, and a member that does NOT inherit is a FINDING to report, not a row to add.**"
    - "**THE CORPUS MUST BE ABLE TO FAIL IN BOTH DIRECTIONS, AND THIS ROUND'S PERMITTED HALF IS UNUSUALLY LOAD-BEARING BECAUSE THE `!` CARVE-OUT LIVES IN IT.** All measured **exit 0** at `21655c5` and all asserted **exit 0 AFTER**: `git -c alias.p='!git push --force origin main' p` and `git config alias.p \"!git push --force origin HEAD:refs/heads/main\"` (both `T-19-86` rows); `git -c aliasx.q=\"-c include.path=/tmp/evil.cfg status\" q` and `git -c notalias.q=\"…\" q` (**this round's `--signed no`** — a rule written as `key.starts_with(\"alias\")` turns both red, and only a SECTION comparison keeps them green); `git -c a=b status` and `git -c a=b version` (the dotless fence, `CALLEE_KNOWN_LEADING_PREFIX`); and the whole of round 8's permitted half — `git -c user.name=\"$NAME\" commit -m x`, `git -c core.pager=cat log`, `git -c includepath=/tmp/evil.cfg status`, `git -c notinclude.path=/tmp/evil.cfg status`. Beside them the verdict-preserving refusal `git -c a=b push --force origin main` at `force_push_blocked`."
    - "**THE OVER-REFUSAL COST IS A WHOLE LEGIBLE FAMILY AND IT IS DISCLOSED HERE RATHER THAN DISCOVERED BY AUDIT 9 — ORDINARY ALIASES DO NOT KEEP WORKING, AND SAYING SO PLAINLY IS THE POINT.** Measured exit 0 today and pinned **exit 2 `envelope_assertion_failed`** after: `git -c alias.st=status st`, `git -c alias.lg=\"log --oneline\" lg`, `git -c alias.co=checkout co`, `git config alias.co checkout`. **Every one has a permitted twin that does the same work and is pinned beside it** — `git status`, `git log --oneline`, `git checkout`, and for the persisted form simply running the command the alias would have run. **The one thing that does NOT move is INVOCATION**: `git co` and `git p` stay at exit 0, because the guard is stateless and argv-only and cannot see an alias it did not watch being defined. The refused half is *defining* a non-shell alias; the permitted half is *using* one. That boundary is the round's whole cost and the corpus states it in both directions."
    - "**Every post-fix expectation carries a written DERIVATION, and a verdict that cannot be derived is measured pre-fix and RECORDED rather than asserted.** `19-22` asserted an identifier its own comment, its SUMMARY and `19-22-PLAN-CHECK.md` all said should be RECORDED rather than asserted, and it halted `19-23` mid-plan. This plan does not repeat it. This round is unusually derivable because the refusal `19-25` must produce is raised inside `scan_leading` before any verb is classified and because the `git config` key operand is a decision region `classify_config` already reads with `is_hooks_path_key`. **Two rows are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `git --config-env alias.q=BODYVAR status` in its SEPARATE-WORD spelling, whose post-fix identifier depends on whether `19-25` reads the value half of a carrier whose value is an environment variable NAME; and `git -c alias.q status` with no `=` at all, where `config_key_of` returns the whole token and there is no value half to read, so whether the fail-closed answer is raised by this round's clause or by an existing one is a `19-25` measurement."
    - "**`T-19-108` WILL BE CLOSED ONLY AS SCOPED, AND THE PART THAT STAYS OPEN IS NAMED HERE RATHER THAN IN AUDIT 9.** The rule `19-25` writes closes the **non-shell** alias body — audit 8's leg 1 (`-c alias.q=\"-c include.path=… push --force …\" q`), its leg 2 (the persisted non-shell form that moved `refs/heads/gsd-auto/alpha/w` from `3b84f30` to `1ab87f6`) and its `alias.z` variant that reaches the by-name `core.hooksPath` deny. **It does NOT close audit 8's leg 3** — `git config alias.q '!git -c include.path=<evil> push --force origin HEAD:refs/heads/main'` then `git q`, the pair that rewrote the bare remote's `main` from `ac303dc` to `9f62444` — because a `!` body is a whole command line handed to a governed program as data, which is `T-19-86`, open at `high` and out of scope by explicit user decision, and because its two rows are pinned PERMITTED in files this round may not edit. **Audit 7's destructive demonstration therefore still works after this round, and the SUMMARY must say so in those words, first.** This is audit 8's own boundary paragraph, which lists the re-parsed config value and the command-line-as-data as two separate bullets; the corpus records the composition and closes neither by accident."
    - "**Nothing rounds 4 through 8 settled is re-opened, and the mechanism pins say so mechanically rather than in prose.** Six pins are carried into the new file and all six must be GREEN at this plan's end and after `19-25`, all re-measured at `21655c5`: **round 5's literalness bit asserted NON-VACUOUS** (`Token.literal` false for `pus?`, true for `-c`, `alias.q=…` and `status`; `git pus? --force origin main` → 2 `envelope_assertion_failed`); **round 6's deletion model asserted NON-DEAD** (`git >/dev/null push --force origin main` → 2 `force_push_blocked`, `git x2>/tmp/o push --force origin main` → **0**); **`policy::is_separator(\">\")` is `false` with `SEPARATORS` byte-identical** at `policy.rs:1786`; **round 7's fail-closed callee grammar asserted NON-DEAD** (`git --attr-source HEAD push --force origin main` → 2 `force_push_blocked`, `git --bogus-opt status` → 2 `envelope_assertion_failed`, `git - push --force origin main` → **0**, `git -- push --force origin main` → 2); **round 8's confinement clause asserted NON-DEAD** (`git -c include.path=/tmp/evil.cfg status` → 2 `envelope_assertion_failed` beside `git -c includepath=…` and `git -c notinclude.path=…` at **0**); and Rule B's severed-head geometry."
    - "**The BYTE FLOORS and the anti-vacuity stripper's THREE protections are load-bearing and all three are carried.** `POLICY_MIN_PRODUCTION_BYTES = 40_000` (`tests/envelope_wrapper_class.rs:831`) and `HOOKS_MIN_PRODUCTION_BYTES = 20_000` (`:838`) stay at their values or rise; re-measured while planning at `21655c5` by splitting each file at its `#[cfg(test)]` sentinel, `policy.rs` is raw 372,920 / production **228,101** and `hooks.rs` is raw 99,753 / production **74,358**. The three round-8 repairs stay: the **proportional** floor `production.len() >= 180_000` at `policy.rs:6643` (78.9% of the current production half), the **deep anchor** `fn forbidden_repo_path` at `policy.rs:6653` (the function sits at line **4,592** of a 4,613-line production half), and the **sentinel-count** assertion at `policy.rs:6665` — `grep -c '^#\\[cfg(test)\\]' src/envelope/policy.rs` is **exactly 1**, at line 4,614. All three keep their present strictness."
    - "**This plan's gate names its expected failures rather than requiring zero, and it is the handoff contract `19-25` gates against.** Baseline is **1680** (`passed + failed`), recorded by `19-23-SUMMARY.md:221` and independently re-measured by audit 8 over **43** result lines as 1679 passed / 1 failed / 13 ignored. After this plan `passed + failed` is strictly greater than 1680; **a red test RAN, so red→green leaves the total unchanged and every increase comes ONLY from new `#[test]` fns** — count them from `git show` and check the identity. **All FIFTEEN `envelope_*` binaries must run** — the fourteen audit 8 observed (`advisory` 10, `argv_deletion` 20, `callee_grammar` 19, `command_position` 18, `config_resolution` 30, `credential` 6, `expansion_slots` 32, `hook_refusals` 7, `literal_decision` 43, `pr_cap` 11, `tracer` 6, `wiring` 14, `wrapper_bypass` 13, `wrapper_class` 40) plus the one this plan adds. **A run reporting fourteen is a run in which this plan's own evidence file did not execute.** The three documented flakes are out of scope and `driver_reattach::a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step` **FIRED during audit 8** and passed on re-run — absence is not evidence they are fixed and one firing is not evidence of a new defect."
    - "**`T-19-17r` stays OUTSTANDING and this plan does not resolve it either.** `19-17-SUMMARY.md` calls it \"accepted\"; audits 5, 6, 7 and 8 all confirmed the measurement and both pins and all four deliberately declined to make the acceptance, because accepting a risk is a human decision. The Accepted Risks Log runs `AR-19-01` … `AR-19-12`; there is still no `AR-19-13` row and no register row. This plan records the gap and makes no acceptance."
    - "**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-25`, or by the two together.** `T-19-86` remains OPEN at `high` by explicit user scoping decision and its four rows plus the persisted-alias arm must keep exiting 0 — **and audit 8 found it measurably WIDER than the record credits, which this plan RECORDS and does not close**: the layer-3 catch three documents attribute to closing `T-19-103` is absent when the alias body carries a carrier of its own. `T-19-91` remains OPEN at `high` with its arms unweakened; `T-19-96` is registered open; `T-19-61` through `T-19-73`, `T-19-84` and `T-19-85` are open, unaccepted and untouched. The SUMMARY must say so FIRST and must never write an unqualified \"T-19-60 is closed\"."
  prohibitions:
    - statement: "MUST NOT change a single line of `src/`. This plan is the corpus and the reproducers; `19-25` is the rules. Every commit here must show ZERO `src/` hunks under `git show --stat`, the same discipline `19-15`, `19-16`, `19-18`, `19-20` and `19-22` met. A plan that wrote the fix beside the corpus would destroy the one piece of evidence this split exists to produce: that the corpus was capable of failing before the code changed."
      category: honesty
    - statement: "MUST NOT write a row whose expected mechanism is that EVERY `alias.*` assignment is refused, and MUST NOT put a `!`-bodied alias into any fail-closed alphabet. `tests/envelope_command_position.rs:550` and `tests/envelope_config_resolution.rs:1539-1543` both pin a `!`-bodied alias PERMITTED as a registered `T-19-86` row, and this round may edit neither file. Git's own rule is the FIRST BYTE and it is measured in both directions here. Assert the exclusion mechanically, in the shape section 14b's `{v}>` exclusion and `19-22`'s dotless fence already use, with a failure message naming both files, both line numbers, and the consequence."
      category: safety
    - statement: "MUST NOT build a `T-19-108` reproducer on a base layer 2 already refuses. `19-22` established this and it still holds: `git -c include.path=/tmp/evil.cfg push --force origin main` is exit 2 `envelope_assertion_failed` for its CARRIER and every `--force` composition is refused for its VERB. Those are CONTROLS. The reproducers use layer-2-permitted bases — `commit -m x`, `status`, the in-namespace `push origin refs/heads/gsd-auto/alpha/w`, and the alias INVOCATION forms `git p` / `git q` / `git z` — and the corpus asserts mechanically that at least one permitted base is drawn."
      category: safety
    - statement: "MUST NOT stand the new class up as a FIFTH named axis. Audit 8's finding is that the axis did NOT move — the REGION did. `CONFIG_RESOLUTION_CLASSES` gains a SIXTH class and `MIN_CONFIG_RESOLUTION_CLASSES` rises from 5 to 6; `UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES` and every one of their predicates, degenerate-proofing assertions and floors stay byte-identical. A fifth axis would model a region as if it were a stage and lose the distinction audit 8 drew."
      category: safety
    - statement: "MUST NOT put an entry whose verdict the fix CHANGES into the invariance-preserving arm of any generative property. This is `19-18`'s `{v}>` blocker, `19-20`'s `GIT_GLOBAL_UNKNOWN_OPTIONS` split and `19-22`'s indirection/confined split, one region over. A NON-SHELL alias carrier is refused after `19-25` even on a PERMITTED base, so it gets its OWN alphabet and its OWN fail-closed property, disjoint from `CONFIG_CONFINED_CARRIERS`; a `!`-BODIED alias carrier is verdict-PRESERVING and belongs in the confined alphabet beside `-c a=b`. The doc says why rather than leaving it to be inferred."
      category: safety
    - statement: "MUST NOT touch `CONFIG_CONFINED_CARRIERS`', `CONFIG_INDIRECTION_CARRIERS`' or `CONFIG_ENV_CARRIERS`' existing entries, the five existing `CONFIG_RESOLUTION_CLASSES` predicates, the dotless fence over `CALLEE_KNOWN_LEADING_PREFIX`, or the permitted-base fence. Entries may be ADDED; nothing may be removed, reworded away or narrowed. Every `MIN_*` constant keeps its current value or rises, and `CONFIG_RESOLUTION_CASES`, `CONFIG_RESOLUTION_SLOTS` and the per-class counts are RE-DERIVED from the new generation arithmetic and STATED in the constant's doc — audit 5 found `19-16` set a floor of 50 against a maximum of 40 by construction, so state the arithmetic and check it."
      category: scope
    - statement: "MUST NOT lower, delete or narrow any existing floor, alphabet entry, property or assertion anywhere under `tests/`. `19-20` was granted exactly one named deletion and this plan is granted NONE. `POLICY_MIN_PRODUCTION_BYTES` and `HOOKS_MIN_PRODUCTION_BYTES` keep their values or rise, and the anti-vacuity stripper's three protections — the proportional floor, the deep anchor and the sentinel count — keep their present strictness. A case that stops being drawn is a case that stops being able to fail."
      category: scope
    - statement: "MUST NOT write a row as an assertion before MEASURING it against the built binary at this plan's base commit, and MUST NOT assert a verdict other than the one measured or the one DERIVED in writing from `19-25`'s mandated design. If any audit-8 row does not reproduce, report it as a finding with the measured verdict and stop — do not assert the audit's number over your own measurement, and do not quietly drop the row."
      category: honesty
    - statement: "MUST NOT claim a config-resolution fact against git without CONFIRMING IT AGAINST THE REAL `git` BINARY, using the envelope's OWN delivery mechanism as the control rather than a stand-in. Every precedence claim is measured as audit 8 measured it: `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath GIT_CONFIG_VALUE_0=/ENV_WINS git config --get core.hooksPath` must print `/ENV_WINS`, and the carrier under test must make the same command print the INCLUDED or ALIAS value instead. A carrier that does not outrank the injection is not a reproducer."
      category: honesty
    - statement: "MUST NOT inherit audit 8's END-TO-END behavioural claim by citation. The bare-remote fixture is rebuilt and every leg re-measured independently, with the remote's SHA recorded BEFORE and AFTER each and a CONTROL beside every leg — the plain in-namespace push refused with the ref unmoved, the persisted NON-SHELL alias carrying the include completing and MOVING it, and the `pre-commit` point falling the same way. **Claims in this codebase about what outranks the injection have been wrong twice**, so this round does not carry forward a behavioural claim it has not reproduced. A leg that does not reproduce is a finding to report, not a sentence to repeat."
      category: honesty
    - statement: "MUST NOT pin a POST-fix expectation that cannot be DERIVED from `19-25`'s mandated design, and MUST write the derivation beside every row that carries one. **Two rows are RECORDED IN COMMENTS AND PRINTS rather than asserted**: `git --config-env alias.q=BODYVAR status` in its separate-word spelling, and `git -c alias.q status` with no `=` at all. Asserting only that they are REFUSED is also forbidden — record the measurement, assert nothing. `19-22` asserted an identifier three of its own documents said should be recorded, and it halted `19-23` mid-plan."
      category: honesty
    - statement: "MUST NOT write the rule, sketch it in `src/`, or add any production line. In particular MUST NOT add an `alias` entry to `INDIRECTION_SECTIONS`, MUST NOT create the re-parse constant or predicate, MUST NOT touch `classify_config`, `config_key_operand_index`, `CONFIG_VALUE_OPTS`, `FORGE_VALUE_OPTS`, `GH_API_VALUE_OPTS` or any drift pin, and MUST NOT correct `cred.rs`. All of those are `19-25`'s, and the `cred.rs` correction is a deliberate, narrow, named exception to this phase's `cred.rs` fence that belongs in the plan that can measure the rule it describes."
      category: scope
    - statement: "MUST NOT modify `tests/envelope_expansion_slots.rs`, `tests/envelope_command_position.rs`, `tests/envelope_literal_decision.rs`, `tests/envelope_argv_deletion.rs`, `tests/envelope_callee_grammar.rs` or `tests/envelope_config_resolution.rs`. Rounds 3 through 8's evidence stays attributable to the round that produced it, which is why `19-14` created a third file, `19-16` a fourth, `19-18` a fifth, `19-20` a sixth and `19-22` a seventh; this plan creates an EIGHTH for the same reason. The six carried-forward mechanism pins are RE-ASSERTED in the new file over the same public functions, not moved and not edited in place."
      category: scope
    - statement: "MUST NOT add `>` or `<` to `SEPARATORS`, propose it, or write a row whose expected mechanism is that they were added; and MUST NOT weaken round 5's literalness bit, round 6's deletion model, round 7's fail-closed callee grammar or round 8's confinement clause. All are pinned in the new file and all must stay green through `19-25`."
      category: safety
    - statement: "MUST NOT claim the `glab --host` under-count as a live bypass and MUST NOT fix `FORGE_VALUE_OPTS`, `GH_API_VALUE_OPTS` or `subcommand_word_indices`. **`glab` is confirmed NOT INSTALLED** — re-verified while planning, `command -v glab` finds nothing — so whether glab accepts `--host` as a separate-value global flag is not confirmed against the callee. Audits 7 and 8 both explicitly declined to upgrade it; do not upgrade it without evidence. The `T-19-106` work is `19-25`'s."
      category: scope
    - statement: "MUST NOT add an Accepted-Risks-Log row for `T-19-17r`, and MUST NOT write the word \"accepted\" about `T-19-17r` anywhere — in a test name, a comment, the SUMMARY or the appended subsection. Record the gap as OUTSTANDING. Audits 5, 6, 7 and 8 all explicitly declined to make it on the developer's behalf; a plan making it would be forging the same signature. There is no `AR-19-13`."
      category: honesty
    - statement: "MUST NOT fix, close, narrow or re-scope `T-19-86`. Its four registered rows stay at exit 0 — `git submodule foreach git push --force origin main`, `git rebase -x \"git push --force origin main\" HEAD~3`, `git bisect run sh -c \"git push --force origin main\"`, `git -c alias.p='!git push --force origin main' p` — and so does the persisted-alias arm. **RECORD that audit 8 found it measurably WIDER than the register credits and that audit 7's destructive `!`-bodied pair still works; do not close it.** It is named FIRST in the SUMMARY's 'what remains uncovered' section."
      category: scope
    - statement: "MUST NOT close, re-scope or add a remedy for `T-19-91`, and MUST NOT weaken its record. Its arms stay as audits 5 through 8 verified them — `git reflog $S`, `git reflog show $S` and `git symbolic-ref $S` at exit 0 with NO second carrier. Do not add a decision-operand rule for `reflog`, `symbolic-ref` or `push`, and do not extend the denylist."
      category: scope
    - statement: "MUST NOT fix `T-19-96` (a glob in a push flag) or `T-19-74` (whose core rows are frozen and re-measured permitted). MUST NOT take on `T-19-61` through `T-19-73`, `T-19-84` or `T-19-85` — open and unaccepted by explicit user decision. Do not touch `cred.rs`, `advisory.rs`, `scan.rs`, `config.rs`, `mod.rs`, `hooks.rs`, the honesty statement, the second-carrier table or the park-coverage surface."
      category: scope
    - statement: "MUST NOT add a crate. `T-19-SC` holds phase-wide — no phase-19 commit has touched `Cargo.toml` or `Cargo.lock` — and every parser in this module is hand-rolled against `std` for exactly that reason. The real-git precedence probes and the bare-remote fixture shell out through `std::process::Command` inside tests."
      category: scope
    - statement: "MUST NOT infer a missing ledger line. Every row claiming a refused or unrecognised command wrote nothing must use a FRESH envelope root of its own and WALK the whole envelope directory afterwards, and the walk must be proved non-blind in the same run by a positive control that leaves a ledger line. `gh pr create --title x` is the ready-made positive control — re-measured while planning, it leaves exactly one `alpha/pr-ledger.ndjson` line."
      category: honesty
    - statement: "MUST NOT report a suite number from a run without `--no-fail-fast`, and MUST NOT gate on the passed count alone. Plain `cargo test` fail-fasts at `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a single envelope suite. Gate on `passed + failed` against **1680**. MUST NOT grep raw `cargo` output for `warning:` or `test result:` without `rtk proxy`: the RTK hook strips exactly those lines, so the grep succeeds having read nothing — `19-19` first read 1364 instead of 1605 that way (D-34). **A run reporting fourteen `envelope_*` binaries after this plan is a run in which this plan's own evidence file did not execute.**"
      category: honesty
    - statement: "MUST NOT fix, edit or work around the three documented flakes — the two `tests/driver_reattach.rs` failures and the `tests/envelope_tracer.rs` ETXTBSY stub-write race — and MUST NOT treat their absence from a run as a regression. `a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step` FIRED in audit 8's run and passed on an immediate re-run. All three are pre-existing, environmental and out of scope."
      category: scope
    - statement: "MUST NOT gate on `cargo clippy --tests -- -D warnings`, which already fails at base on four pre-existing lint errors in `src/browser.rs` and `src/project_creator.rs`. The gate is `cargo clippy -- -D warnings`. Do not fix those lints."
      category: scope
    - statement: "MUST NOT edit `19-SECURITY.md`'s audit tables, its Security Audit Trail, its Accepted Risks Log, its Sign-Off, or the subsections plans 19-13 … 19-23 appended or the plan-19-21 blocker-row resolution record. The only permitted change is ONE further appended subsection, clearly attributed as a plan-19-24 planning and execution record. Audit 8 checksummed the body below the frontmatter before writing; that provenance is the file's value and a plan writing into an audit's own tables forges it."
      category: honesty
    - statement: "MUST NOT write an unqualified `T-19-60 is closed` anywhere. Only the WRAPPER-OPERAND sub-class of `T-19-60` is closed; `T-19-86` and `T-19-91` are both sub-classes of it and both remain OPEN at `high`, so this plan does not clear `/gsd-secure-phase 19` and neither does `19-25`."
      category: honesty
  artifacts:
    - path: "tests/envelope_reparsed_value.rs"
      provides: "the EIGHTH evidence file — audit 8's `T-19-108` rows re-measured against the built binary on layer-2-PERMITTED bases in BOTH deliveries (`-c alias.<n>=` and the persisted `git config alias.<n>`), the real-git K1/K2/K3 enumeration with at least three K2 representatives measured INERT, the bare-remote fixture rebuilt with a control beside every leg, the paired discriminators that prove the mechanism present, the SHELL-ALIAS fence measured on both sides of the first byte, the discrimination controls (`aliasx.`, `notalias.`) that stop a prefix match, the whole disclosed over-refusal family with a permitted twin beside each row, the invocation rows pinned UNCHANGED, the two ordering pins at deliberately different post-fix identifiers, the `T-19-86` rows recorded WIDER and not closed, the two rows recorded rather than asserted, and the six carried-forward mechanism pins"
      contains: "T-19-108"
    - path: "tests/envelope_wrapper_class.rs"
      provides: "a SIXTH class in `CONFIG_RESOLUTION_CLASSES` — *a carrier delivered inside a config VALUE the guard confines* — with `MIN_CONFIG_RESOLUTION_CLASSES` raised from 5 to 6, a `CONFIG_REPARSED_VALUE_CARRIERS` alphabet with its OWN fail-closed property in both deliveries, `!`-bodied entries added to `CONFIG_CONFINED_CARRIERS` as verdict-preserving, a mechanical SHELL-ALIAS fence naming both files that pin a `!` body permitted, degenerate-proofing that the sixth class is satisfied by no existing entry and that no existing class is satisfied by a re-parsed-value carrier, and floors whose arithmetic is re-derived and stated"
      contains: "CONFIG_REPARSED_VALUE_CARRIERS"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md"
      provides: "one further appended subsection, attributed as a plan-19-24 record rather than an audit finding, carrying the shell-alias seam finding FIRST, every measured row with its walk, the K1/K2/K3 enumeration with its measurements, the include/alias persistence asymmetry, the bare-remote legs with SHAs and controls, the disclosed over-refusal family with its permitted twins, the design question stated with what the corpus can now fail on, the statement that `T-19-108` will close only AS SCOPED and that audit 7's `!`-bodied destructive pair still works, `T-19-86` recorded WIDER and not closed, the `T-19-106` `deferred-items.md` mis-closure corrected, the `glab --host` cell unchanged, the `T-19-17r` gap OUTSTANDING with the acceptance deliberately unmade, and the statement that this plan closes nothing and clears no gate"
      contains: "19-24"
    - path: ".planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md"
      provides: "`T-19-108` and `T-19-109` registered with their corpus recorded RED, `T-19-105` recorded open for the ninth consecutive round with the region-not-axis finding, the `T-19-106` entry CORRECTED from CLOSED to OPEN at `medium` with audit 8's `gh`-is-installed finding, the `T-19-86` entry recorded WIDER without closing it, the `glab --host` forge cell unchanged and the `T-19-17r` bookkeeping gap still outstanding; `T-19-91`, `T-19-96` and `T-19-74` unchanged and open"
      contains: "T-19-108"
  key_links:
    - from: "audit 8's measured bypass rows and git's own alias re-parsing"
      to: "committed RED assertions that could not have been shaped to agree with the code"
      via: "measurement against the built binary with a fresh root per row, and a real-git probe using the envelope's OWN `GIT_CONFIG_COUNT` triplet as the control, BEFORE any assertion is written and BEFORE any production line moves"
      pattern: "RED"
    - from: "a carrier riding inside a value the round-8 clause CONFINED and let through"
      to: "a SIXTH class the corpus can draw on the axis it already has"
      via: "`CONFIG_REPARSED_VALUE_CARRIERS` spliced in both deliveries, with `MIN_CONFIG_RESOLUTION_CLASSES` raised to 6 and the region-not-axis reason stated in the section header"
      pattern: "CONFIG_REPARSED_VALUE_CARRIERS"
    - from: "a rule that refuses every `alias.*` assignment"
      to: "two permanently red evidence files this round may not edit"
      via: "a mechanical SHELL-ALIAS fence naming `tests/envelope_command_position.rs:550` and `tests/envelope_config_resolution.rs:1539`, with git's one-byte rule measured on both sides"
      pattern: "shell_alias"
    - from: "a rule written as a prefix match on `alias`"
      to: "a red test rather than a rule that cannot say what it means"
      via: "`git -c aliasx.q=…` and `git -c notalias.q=…` pinned PERMITTED before and after — this round's `--signed no`"
      pattern: "notalias"
    - from: "an over-refusal family discovered by audit 9"
      to: "an over-refusal family disclosed by the corpus that produced it"
      via: "every refused alias-definition row pinned beside the permitted twin that does the same work, and every alias INVOCATION row pinned UNCHANGED at exit 0"
      pattern: "permitted_twin"
---

<objective>
**The corpus for the REGION the round-8 rule does not reach — a carrier inside a value the guard itself confined — and it must go RED before a line of either rule is written.**

Audit 8 (`19-SECURITY.md`, the authority) answered the question it was set, and answered it in the
guard's favour on the shape of the rule:

> **The confinement clause is the right SHAPE of rule and audit 8 says so plainly** … it asks whether
> an assignment can be bounded rather than whether it spells a name.

And then found the class still open, one region over:

> **But it reads config assignments in ONE region — the leading-option region `scan_leading` walks —
> and git resolves configuration from a second place the guard hands it: a config VALUE the clause
> itself CONFINES.**

And found the corpus structurally incapable of catching it:

> `CONFIG_RESOLUTION_CLASSES`'s five classes are **all argv or environment carriers** …
> `grep -rn "alias\.[a-z]*=\"-c\|alias.*include\.path" src/ tests/` returns **nothing at all** … The
> corpus is therefore structurally incapable of generating, and so of failing on, `T-19-108`.

That grep was re-run while planning at `21655c5` and still returns nothing at all.

So: the corpus is widened here, observed failing here, and committed here. `19-25` writes the rules and
is gated on this plan's recorded RED state. **If the widened corpus does not go red against the
pre-fix tree, that is a finding — stop and report it.**

## The invariant this corpus is written against

**A value the guard read, CONFINED and let through is still an input to git's configuration.** Round 8
closed the question below it — an assignment the guard cannot bound to the key it names makes the
command unresolvable — and audit 8 verified every one of its rows. This is the layer above: having
decided an assignment is bounded, **what does git do with the value?**

## The finding that shapes the whole corpus, stated first

**Git's shell-alias rule is a ONE-BYTE fact, and a corpus that does not fence it hands `19-25` an
undischargeable seam.** Two evidence files this round may not edit pin a `!`-bodied alias PERMITTED as
a registered `T-19-86` row:

```
tests/envelope_command_position.rs:550       permits("git -c alias.p='!git push --force origin main' p")
tests/envelope_config_resolution.rs:1539     permits("git config alias.p \"!git push --force origin HEAD:refs/heads/main\"")
```

A blanket `alias.*` refusal turns both permanently red. **The carve-out is not a concession — it is
git's own rule, and it is measured in both directions** against `git version 2.43.0` with the
envelope's own injection as the control (`/ENV_WINS`):

```
-c alias.a='-c include.path=<f> config --get core.hooksPath'  -> /INCLUDE_WINS   K1: re-parsed IN-PROCESS
-c alias.b='!git config --get core.hooksPath'                 -> /ENV_WINS       K2: CHILD inherits injection
-c alias.g='config --get core.hooksPath !x'                   -> /ENV_WINS       a `!` not first is not a shell body
-c alias.d=' !git config --get core.hooksPath'                -> expansion of alias 'd' failed
-c alias.q='"!git …"'   (the QUOTED-body spelling)            -> re-parsed IN-PROCESS: the first byte
                                                                 is `"`, not `!`, so the rule correctly
                                                                 REFUSES it
-c alias.t='<TAB>!git …'                                      -> git refuses to expand
```

**No spelling was found in which the first byte is `!` yet git re-parses in-process**, which is what
makes the one-byte rule safe to lean on. The last two rows are the plan-check's own additions and are
carried as rows rather than as prose.

**This is `19-22`'s `-c a=b` seam and `19-18`'s `{v}>` blocker one region over, caught at PLAN time.**

## What must be RED at this plan's end

Measured at `21655c5` — fresh `GSD_MM_ENVELOPE_ROOT` per row, the envelope directory walked afterwards
and EMPTY in every one:

```
exit=0  git -c alias.q="-c include.path=/tmp/evil.cfg push --force origin HEAD:refs/heads/main" q   <- T-19-108, ONE line
exit=0  git -c alias.z="-c core.hooksPath=/dev/null push origin refs/heads/gsd-auto/alpha/w" z      <- reaches 19-02's deny
exit=0  git config alias.p '-c include.path=/tmp/evil.cfg push origin refs/heads/gsd-auto/alpha/w'  <- the persisted arm
exit=0  git config --global alias.p '-c include.path=/tmp/evil.cfg push'
exit=0  git -c ALIAS.q="-c include.path=/tmp/evil.cfg status" q                                     <- the case row
exit=0  git --config-env=alias.q=BODYVAR status                                                     <- the second carrier
exit=0  git -c alias.st=status st                                                                   <- the DISCLOSED COST
exit=0  git -c alias.lg="log --oneline" lg                                                          <- the DISCLOSED COST
exit=0  git -c alias.co=checkout co                                                                 <- the DISCLOSED COST
exit=0  git config alias.co checkout                                                                <- the DISCLOSED COST
```

All are asserted at their POST-fix verdict, exit 2 `envelope_assertion_failed`, so they are red now and
green after. **One further row is RED without being exit 0 today** — the ordering pin, whose IDENTIFIER
moves while its verdict does not:

```
exit=2 [hook_bypass_blocked]  git -c alias.q="-c include.path=/tmp/evil.cfg status" -c core.hooksPath=/dev/null push --force origin main
                              -> asserted at envelope_assertion_failed, because the scan is LEFT TO
                                 RIGHT and 19-25's clause is raised at the first assignment it cannot
                                 bound. Its twin in the other order stays hook_bypass_blocked.
```

## The rows that must NOT move, and the `!` carve-out lives here

```
exit=0  git -c alias.p='!git push --force origin main' p                        <- T-19-86, may not move
exit=0  git config alias.p "!git push --force origin HEAD:refs/heads/main"       <- T-19-86, may not move
exit=0  git p  /  git co                                                         <- INVOCATION is not DEFINITION
exit=0  git -c aliasx.q="-c include.path=/tmp/evil.cfg status" q                 <- this round's --signed no
exit=0  git -c notalias.q="-c include.path=/tmp/evil.cfg status" q               <- the same, from the other side
exit=0  git -c a=b status  /  git -c a=b version                                 <- the dotless fence
exit=0  git -c user.name="$NAME" commit -m x  /  git -c core.pager=cat log
exit=0  git -c includepath=/tmp/evil.cfg status  /  git -c notinclude.path=… status
exit=2 [force_push_blocked]  git -c a=b push --force origin main
```

## The persistence asymmetry with `include.path`, which `19-25` must not get backwards

Audit 8 measured persisted `git config include.path <evil>` INERT at repo-local, `--worktree` and
GLOBAL, because the injection outranks it — so the include family correctly needs no clause at the
`git config` write site. **An alias does not have to WIN a precedence contest; it only has to EXIST.**
Measured while planning, under the envelope's own `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` redirect at a
generated helper-free file:

```
git config --global alias.g '-c include.path=<f> config --get core.hooksPath'; git g  -> /INCLUDE_WINS
git config         alias.l '-c include.path=<f> config --get core.hooksPath'; git l  -> /INCLUDE_WINS
```

Both deliveries are live, including the one the envelope itself controls.

## What else git re-parses from a config value — three kinds, and only one reaches layer 3

* **K1 — re-parsed as a GIT command line, in-process, INCLUDING its leading options.** `alias.<name>`
  with a non-`!` body. Measurement says it is the only member, and it is the whole of `T-19-108`.
* **K2 — re-parsed as a SHELL command line, executed as a CHILD that INHERITS the injection.**
  `alias.<name>` with a `!` body, `core.pager`, `pager.<cmd>`, `core.editor`, `sequence.editor`,
  `core.askPass`, `credential.helper`, `core.sshCommand`, `core.gitProxy`, `core.fsmonitor`,
  `core.alternateRefsCommand`, `diff.external`, `diff.<d>.command`, `diff.<d>.textconv`,
  `merge.<d>.driver`, `mergetool.<t>.cmd`, `difftool.<t>.cmd`, `filter.<n>.clean`/`.smudge`/`.process`,
  `gpg.program`, `uploadpack.packObjectsHook`, `protocol.<n>.command`,
  `remote.<n>.uploadpack`/`.receivepack`, `trailer.<t>.command`, `submodule.<n>.update`,
  `web.browser`/`browser.<t>.cmd`. **Measured INERT for layer 3**, which is audit 8's `GIT_PAGER` /
  `GIT_EDITOR` / `GIT_SSH` finding restated on the config side.
* **K3 — re-parsed as a FILE PATH spliced at the directive's precedence.** `include.path`,
  `includeIf.<cond>.path`. Closed by `19-23`.

## The design question `19-25` must answer, stated here so the corpus can fail on the wrong answer

**Is the fix (a) to scan a confined value that git will re-parse as a command line, applying the same
clause recursively with a stated depth bound, or (b) to treat any assignment whose key names a
re-parsed value as unbounded and refuse?** This corpus is written so that a rule reading the SECTION
plus the value's FIRST BYTE is green, and a rule that prefix-matches `alias`, or refuses every
`alias.*` including `!` bodies, or refuses a key it cannot decompose into a section, is red. **The
costing is `19-25`'s; the discrimination is this plan's**, and the corpus records the two measurements
that decide it: an alias body can itself carry `-c alias.<n>=…` (measured at depth 2 →
`/INCLUDE_WINS`), and git splits an alias body with its OWN quoting rules, not the shell's.

## What this plan is NOT

It closes nothing. `T-19-105`, `T-19-106`, `T-19-108` and `T-19-109` stay open at this plan's end.

**`/gsd-secure-phase 19` is NOT cleared by this plan, by `19-25`, or by the two together.** `T-19-86`
and `T-19-91` remain open at `high`, and `T-19-108` will close only AS SCOPED — audit 7's `!`-bodied
destructive pair still works. The SUMMARY must say so first.

Purpose: make the corpus capable of failing on the region the round-8 rule does not reach — a carrier
inside a value the guard confined — and prove it does.
Output: an eighth evidence file, a sixth class on the existing axis with re-derived floors, two
committed RED states, one appended record, and an exact handoff number for `19-25` to gate against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/19-23-SUMMARY.md
@.planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md

@src/envelope/policy.rs
@tests/envelope_wrapper_class.rs
@tests/envelope_config_resolution.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE against the guard AND against real git, then RED — the eighth evidence file</name>
  <files>tests/envelope_reparsed_value.rs</files>
  <precondition>
    Work in the main checkout at `21655c5` or later with a clean tree — ISOLATION=none is forced for
    this phase, so do NOT create a worktree. `cargo build` produces `./target/debug/gsd-meta-manager`,
    which is what every guard measurement is driven against. A real `git` binary is on `PATH`
    (`git version 2.43.0` on the machine this plan was written on) and a writable temp directory is
    available; the precedence probes and the bare-remote fixture both need them. `gh` 2.45.0 IS
    installed; `glab` is NOT installed and must not be assumed. The three documented flakes are
    pre-existing.
  </precondition>
  <read_first>
    `tests/envelope_config_resolution.rs:1-140` for the harness this file carries forward — `Answer`,
    `ask`, `envelope_files`, `ledger_lines_anywhere`, `listing`, `refuses`, `permits`, and the
    one-`TempDir`-per-row discipline — and its section 11 for the shape of a mechanism pin asserted
    over a `policy::` function directly rather than over an exit code. **`:1513-1550` in particular:
    that is `19-22`'s `T-19-86` persisted-alias arm, it pins a `!` body PERMITTED, and this plan may
    not edit it.** `tests/envelope_command_position.rs:515-551` — round 3's `T-19-86` pin, same
    constraint. `19-SECURITY.md`'s sections "Threats found by audit 8", "What audit 8 measured and
    found INERT", "The closures, re-measured rather than accepted from the summaries", "The known-open
    set, as audit 8 verified it", "The fail-open residue" and "Audit 8 — what the round-8 control can
    and cannot fail on". `src/envelope/policy.rs:402-533` (`scan_leading` — its one-scan-two-callers
    doc, the `-` and `--` breaks, **the `if let Some(assignment)` block at `:425-528` carrying the
    rewriting-key refusal, then the confinement clause at `:508-514`, then `is_hooks_path_key` at
    `:515-527`, which is where `19-25`'s new clause goes**), `:855-1008` (`INDIRECTION_SECTIONS` and
    its residue paragraph, `config_key_section`, `config_key_names_an_indirection_section` and
    `unbounded_config_assignment_refusal` — **the shape `19-25`'s new constant, predicate and refusal
    must mirror**), `:1010-1030` (`is_hooks_path_key`), `:732-840` (`leading_git_option`'s arms — arm
    (a) returns the NEXT token as the assignment for a bare `-c`/`--config-env` and arm (b) the
    attached one, so BOTH carriers reach the same key check), `:842-855` (`config_key_of` — **it
    returns the WHOLE token when there is no `=`, which is why `git -c alias.q status` has no value
    half to read**), `:1380-1468` (`scan_config`, `config_key_operand_index` and `classify_config` —
    **the SECOND decision region, which already reads the key operand with `is_hooks_path_key` and is
    where `19-25` must ask the same question for the persisted arm**), `src/envelope/cred.rs:224-275`
    (`hooks_path_env` — the injection every precedence probe must use as its control, and the
    "**FIVE forms**" table at `:246-260` whose claim `19-25` corrects; do NOT edit it here — and
    `write_gitconfig`'s doc, the generated helper-free file both `GIT_CONFIG_GLOBAL` and
    `GIT_CONFIG_SYSTEM` point at, which is why a `--global` alias written under the envelope is live).
  </read_first>
  <action>
**Measure before you assert, and measure the CALLEE as well as the guard. Nothing in this file is
written until it has been driven.**

Build, then drive every guard row exactly as audit 8 did:

```
printf '{"tool_name":"Bash","tool_input":{"command":"<CMD>"}}' \
  | GSD_MM_ENVELOPE_ROOT=$(mktemp -d) ./target/debug/gsd-meta-manager envelope guard alpha
```

one fresh root per row, a walk over the whole root afterwards. Build the JSON with
`python3 -c json.dumps` so the nested quoting of the alias-body rows survives as the bytes it is.
**Quoting is load-bearing on this axis and getting it wrong silently changes the class**: an unquoted
`git -c alias.p=!git push --force origin main p` tokenizes into a bare `push --force` the guard refuses
for its verb, which measures nothing about aliases. Verify by eye that each driven row's argv is the
one intended.

**Then probe REAL git for every re-parse claim, with the envelope's OWN delivery mechanism as the
control.** In a throwaway directory with a planted include file carrying `[core] hooksPath =
/INCLUDE_WINS`:

  - the control is `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath
    GIT_CONFIG_VALUE_0=/ENV_WINS git config --get core.hooksPath`, which must print `/ENV_WINS`;
  - each carrier under test is added to that same command line, and it is a re-parse carrier only if
    the same command then prints the INCLUDED or ALIAS value instead.

**The K1/K2 split is the measurement this whole round rests on and it must be made in BOTH
directions**: a non-`!` alias body carrying `-c include.path=<f>` resolves `/INCLUDE_WINS`, and a `!`
body carrying nothing of its own resolves `/ENV_WINS` because the child INHERITS the triplet. Measure
the boundary too — a `!` that is not the first byte, a leading space before `!`, a **TAB** before `!`, a
**QUOTED body** (`'"!git …"'`, whose first byte is `"` and which git re-parses in-process), an empty
body, and a body that is only an option with no verb — and record git's exact words for each. **The
quoted and tab spellings are the plan-check's own additions and both go in as ROWS**: the quoted one is
the more interesting, because the rule refuses it and that refusal is CORRECT — git re-parses it
in-process — which is the case that shows reading one byte is not a loophole. **Report as a finding any
spelling in which the first byte IS `!` and git nonetheless re-parses in-process**; none was found while
planning or at plan-check, and the whole rule leans on that.

**Then measure at least THREE K2 representatives from the enumeration in the objective** (`core.editor`,
`core.pager`, `credential.helper`, `diff.external` and `filter.<n>.clean` are the readiest) and confirm
each is INERT for `core.hooksPath`. **Record the MECHANISM, not just the verdict**: the child's own
environment still carries `GIT_CONFIG_COUNT=1` / `KEY_0=core.hooksPath` / `VALUE_0=/ENV_WINS`, which is
why it inherits — dump the child's environment in at least one row so the inertness is explained rather
than observed. **A K2 member that does NOT inherit is a FINDING — record the measured behaviour and
report it rather than folding it into a row.**

**Measure the PERSISTENCE asymmetry, because `19-25` adding an inert clause is the likeliest way this
round wastes a rule.** Under an environment that reproduces the envelope's own config posture —
`GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` both pointed at a generated helper-free file, and the
`core.hooksPath` triplet injected — persist an alias at `--global` and at repo-local, invoke each, and
record that both resolve the included value. Beside them record audit 8's INERT rows for
`git config include.path` at repo-local, `--worktree` and GLOBAL, **which are carried forward as
ESTABLISHED and are not re-derived**. The asymmetry is the finding: an include must WIN a precedence
contest and loses; an alias only has to EXIST.

**Measure the DEPTH and the QUOTING facts that decide the design question**, because `19-25` must cost
option (a) against them rather than against an argument: an alias body can itself carry
`-c alias.<n>="…"` and git re-parses it — measured at **depth 2 AND at depth 3**, both `/INCLUDE_WINS`,
the depth-3 row being the plan-check's own addition — and git splits an alias body with its own
`split_cmdline` rules rather than the shell's, dequoting confirmed
(`-c alias.m='config --get "core.hooksPath"' m` resolves). Measure the `--config-env` alias delivery too,
which also resolves. Record all of them verbatim. **Depth 3 is the row that matters**: it shows the
recursion is not a depth-2 curiosity, so any stated depth bound in option (a) is a residue an attacker
reaches by adding one more nesting level, not a theoretical one.

**Then CONFIRM THE CLASS END TO END AGAINST REAL GIT WITH A BARE REMOTE, independently rather than by
citing audit 8.** Claims in this codebase about what outranks the injection have been wrong twice, so
this round does not inherit a behavioural claim it has not reproduced. In a throwaway fixture on
`refs/heads/gsd-auto/alpha/w` with a local bare upstream and `pre-push`/`pre-commit` hooks installed
exactly as the envelope installs them (`core.hooksPath` delivered through the
`GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` triplet `cred::hooks_path_env` emits), measure and record, with
the remote's SHA before and after each and a CONTROL beside every leg:

  - the plain in-namespace push is REFUSED by the hook and the remote's ref does not move;
  - the persisted NON-SHELL alias carrying the include — `git config alias.p '-c include.path=<evil>
    push origin refs/heads/gsd-auto/alpha/w'` then `git p` — completes and MOVES the ref;
  - the same alias WITHOUT the include is refused by the hook and the ref does not move;
  - the `pre-commit` point falls the same way: control refuses, carrier commits.

**If any leg does not reproduce, that is a finding — record the measured behaviour and report it.**

Any audit-8 row that does not reproduce at the recorded verdict: STOP, record the measured verdict, and
report it as a finding rather than asserting the audit's number over your own measurement.

Then create `tests/envelope_reparsed_value.rs`. Its header states what the file is: audit 8's
`T-19-108` rows plus the cells found while planning round 9, measured against the built binary AND
against real git BEFORE either rule was written, and written as an EIGHTH file rather than appended to
`tests/envelope_config_resolution.rs` so round-8 and round-9 evidence stay attributable to the round
that produced them. It states the invariant — **a value the guard read, confined and let through is
still an input to git's configuration** — that round 8 closed the question below it and audit 8
verified every one of its rows, and that this file is about the region that clause does not reach. It
states that the file is RED at this plan's end by design and that `19-25` closes it.

**It states the SHELL-ALIAS seam in the header, prominently**, with both file paths, both line numbers
and git's one-byte rule measured on both sides, because that is the single design constraint a later
reader most needs in order to understand why the rows look the way they do.

Carry the harness forward from `tests/envelope_config_resolution.rs` unchanged in shape, including the
walk. Print the walk for at least one refused row and one counted row so the SUMMARY records listings
that were measured.

**Section 1 — the `T-19-108` rows in the `-c` delivery, asserting exit 2 `envelope_assertion_failed`,
each with its derivation.** The `alias.q`, `alias.z`, `ALIAS.q` and `--config-env=alias.q=` rows. The
doc records the mechanism in git's own terms: git re-parses a non-`!` alias body as a command line
INCLUDING its leading options, so `alias.q` — a key that correctly answers `false` to
`config_key_names_an_indirection_section` — carries the same indirection into a position `scan_leading`
never reads. **Write the derivation beside each row**: `19-25`'s clause is raised inside `scan_leading`
before any verb is classified, so the identifier is the unresolvable one and not the one the verb would
have earned. Record that `alias.z` reaches the by-name `core.hooksPath` deny plan 19-02 built, that
`T-19-09` exists to enforce it, and that `cred.rs` names it as the one form 19-02 closes.

**Section 2 — the `T-19-108` rows in the PERSISTED delivery, asserting exit 2
`envelope_assertion_failed`.** `git config alias.p '<non-shell body>'` and its `--global`, `--worktree`,
`--file`, `--add` and `--replace-all` spellings, each measured first. The derivation beside them:
`classify_config` already reads this exact operand with `is_hooks_path_key` and refuses
`git config core.hooksPath /dev/null` at `hook_bypass_blocked` today — measured — so asking the same
question there is the same decision region and not a second reading site. **Record the persistence
asymmetry here in full**, with audit 8's INERT include rows carried forward beside the measured live
alias rows, and state that a clause for `git config include.path` would be inert and must not be added.

**Section 3 — the paired discriminators, asserted UNCHANGED as controls.** `git -c
include.path=/tmp/evil.cfg push --force origin main` at `envelope_assertion_failed`, `git -c
core.hooksPath=/dev/null status` and `git config core.hooksPath /dev/null` at `hook_bypass_blocked`.
The comment states that they are what make this a gap in REACH rather than in mechanism.

**Section 4 — the SHELL-ALIAS FENCE, which is the seam this plan exists to fence.** A test of its own,
named for the fact rather than the row. The two `T-19-86` rows asserted PERMITTED before and after, each
carrying the file and line of the pin it mirrors and the statement that this round may edit neither.
Beside them git's one-byte rule measured in both directions and at its boundary, and the consequence in
one sentence: **a rule that refused every `alias.*` assignment would turn two evidence files permanently
red**, which is `19-18`'s `{v}>` blocker and `19-22`'s `-c a=b` seam one region over.

**Two boundary spellings the plan-check added go in as ROWS here, not as prose.** The **QUOTED body**
`-c alias.q='"!git -c include.path=<f> push --force origin HEAD:refs/heads/main"' q` — its first byte is
`"`, git re-parses it IN-PROCESS, and the rule therefore **REFUSES it, correctly**; it is asserted at
exit 2 `envelope_assertion_failed` after, and its comment says why this is the row that shows reading one
byte is not a loophole. And the **TAB** spelling `-c alias.t='<TAB>!git …' t`, which git refuses to
expand at all. **Add an assertion that no measured spelling exists in which the first byte IS `!` and
git nonetheless re-parses in-process** — none was found while planning or at plan-check, the whole rule
leans on it, and finding one is a FINDING to report rather than a row to add. **And the honest
half, in the same test**: because the `!` body stays permitted, audit 7's destructive pair —
`git config alias.q '!git -c include.path=<evil> push --force origin HEAD:refs/heads/main'` then
`git q`, which rewrote a bare remote's `main` at `fb43577` — **still works after this round**, and that
is `T-19-86`, out of scope, not closed, and named as such.

**Section 5 — the DISCRIMINATION controls, which stop the fix from being a prefix match.**
`git -c aliasx.q="…" q` and `git -c notalias.q="…" q`, both measured exit 0 and both asserted **exit 0
after**. The comment says plainly that these are this round's `--signed no`: a rule written as
`key.starts_with("alias")` turns both red, and only a rule that compares the SECTION keeps them green.
Add the dotless rows beside them — `git -c a=b status` and `git -c a=b version` — with the note that
`CALLEE_KNOWN_LEADING_PREFIX` splices round 7's whole property behind `-c a=b`.

**Section 6 — the DISCLOSED OVER-REFUSAL family, every row beside its permitted twin.** `git -c
alias.st=status st`, `git -c alias.lg="log --oneline" lg`, `git -c alias.co=checkout co` and
`git config alias.co checkout` measured exit 0 today and asserted **exit 2
`envelope_assertion_failed`** after, each paired in the same test with the invocation that does the
same work and is asserted **exit 0 before and after** — `git status`, `git log --oneline`,
`git checkout`. **And the boundary that makes this legible**: `git p`, `git co` and `git q` —
INVOCATION of an already-defined alias — asserted exit 0 before and after, because the guard is
stateless and argv-only. The comment states the round's cost in one sentence: **defining a non-shell
alias is refused; using one is not**, and every refused form has a permitted twin.

**Section 7 — the PERMITTED half carried forward from round 8, asserted unchanged.** `git -c
user.name="$NAME" commit -m x`, `git -c core.pager=cat log`, `git -c includepath=/tmp/evil.cfg status`,
`git -c notinclude.path=/tmp/evil.cfg status`, `git --git-dir=/tmp/g status`, `git -C /tmp status`,
`git --no-pager status`, with the verdict-preserving refusals `git -c a=b push --force origin main` and
`git -c a=b --attr-source HEAD push --force origin main` at `force_push_blocked`. Add the positive walk
control here: `gh pr create --title x` at exit 0 with exactly ONE ledger line in a walked fresh root,
and the cap firing with two on call 2.

**Section 8 — the two ORDERING pins, at deliberately DIFFERENT post-fix identifiers.** Both are exit 2
`hook_bypass_blocked` today:

  - `git -c alias.q="-c include.path=/tmp/evil.cfg status" -c core.hooksPath=/dev/null push --force
    origin main` — the scan reaches the ALIAS assignment first, so it is asserted at **exit 2
    `envelope_assertion_failed`**, which is RED today;
  - `git -c core.hooksPath=/dev/null -c alias.q="…" push --force origin main` — the scan reaches the
    HOOKS KEY first, so it is asserted **UNCHANGED at `hook_bypass_blocked`**.

Together they are the mechanical proof that the new clause is raised inside the ONE left-to-right walk
and not in a second pass, exactly as round 8's pair is for the confinement clause.

**Section 9 — the two rows RECORDED rather than asserted.** Each is measured and the measurement is
**RECORDED IN A COMMENT AND A PRINT — never written as an assertion** — because this plan measures
pre-fix and cannot catch a wrong post-fix expectation, and a row pinned at a verdict the rules cannot
produce lands RED with `19-25` forbidden to edit it. `19-22` asserted such a row against its own
comment, its own SUMMARY and its own plan-check, and it halted `19-23` mid-plan.

  - `git --config-env alias.q=BODYVAR status` in its SEPARATE-WORD spelling — the value half is an
    environment variable NAME rather than a body, so whether `19-25` can read a first byte at all is a
    design decision;
  - `git -c alias.q status` with no `=` — `config_key_of` returns the whole token, so there is no value
    half, and which clause raises the fail-closed answer is a `19-25` measurement.

**Section 10 — the `T-19-86` rows, RECORDED WIDER and explicitly NOT closing anything.** The four
registered rows plus the persisted-alias arm asserted at exit 0, with the doc recording audit 8's
finding that `T-19-86` is **measurably wider than the register credits** — the layer-3 catch three
documents attribute to closing `T-19-103` is absent when the alias body carries a carrier of its own —
and that recording it does not close, narrow or re-scope it.

**Section 11 — the six carried-forward MECHANISM pins**, re-asserted here over the same public
functions rather than moved or edited in place: round 5's literalness bit asserted NON-VACUOUS over the
tokens of `git pus? --force origin main` and `git -c alias.q="-c include.path=/tmp/evil.cfg status" q`;
round 6's deletion model asserted NON-DEAD with its over-deletion control; `policy::is_separator(">")`
false with `SEPARATORS` byte-identical; round 7's fail-closed callee grammar asserted NON-DEAD in all
four directions; **round 8's confinement clause asserted NON-DEAD** with its two discrimination
controls; and Rule B's severed-head geometry.

Commit this as its own commit, RED. State the per-binary counts in the SUMMARY.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --test envelope_reparsed_value --no-fail-fast 2&gt;&amp;1 | tail -40; for t in envelope_config_resolution envelope_callee_grammar envelope_argv_deletion envelope_literal_decision envelope_expansion_slots envelope_command_position; do rtk proxy cargo test --test $t &gt;/dev/null 2&gt;&amp;1 || exit 1; done; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "ZERO-SRC-HUNKS-RED-COMMITTED"; git diff --numstat HEAD~1..HEAD -- tests/envelope_command_position.rs tests/envelope_config_resolution.rs | wc -l</automated>
  </verify>
  <done>Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before it was written as an assertion, with each driven row's argv verified by eye because quoting decides the class on this axis; every re-parse claim was confirmed against the REAL `git` binary using the envelope's own `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` injection as the control, with the K1/K2 split measured in BOTH directions, git's one-byte shell-alias rule measured at its boundary in four spellings, at least THREE K2 representatives measured INERT with the inheritance reason recorded, the persistence asymmetry measured at `--global` and repo-local under the envelope's own config posture with audit 8's INERT include rows carried forward as established, and the depth-2 and alias-body-quoting facts recorded verbatim for `19-25` to cost option (a) against; the bare-remote fixture was REBUILT and every leg re-measured with the remote's SHA before and after and a CONTROL beside each; `tests/envelope_reparsed_value.rs` exists as an EIGHTH file whose header states the SHELL-ALIAS seam prominently with both file paths, both line numbers and the measured one-byte rule, and carries its eleven sections — the `-c`-delivery `T-19-108` rows at exit 2 `envelope_assertion_failed` with a derivation each; the PERSISTED-delivery rows at the same identifier with the persistence asymmetry recorded and the statement that a `git config include.path` clause would be inert; the paired discriminators asserted UNCHANGED; the SHELL-ALIAS FENCE pinning both `T-19-86` rows permitted with the consequence stated and audit 7's `!`-bodied pair recorded as still working; the two discrimination controls plus the dotless rows pinned exit 0 before and after; the disclosed over-refusal family with a permitted twin beside every row and the invocation rows pinned unchanged; round 8's permitted half unchanged with `gh pr create --title x` as the walk's non-blindness control; the two ordering pins at deliberately different post-fix identifiers; the `T-19-86` rows recorded WIDER and not closed; and the six mechanism pins; the TWO undeliverable rows are RECORDED IN COMMENTS AND PRINTS and are **NOT written as assertions**, verified by reading the file for an `assert` on either; `tests/envelope_command_position.rs` and `tests/envelope_config_resolution.rs` show ZERO diff lines; the six prior evidence files are fully GREEN and unmodified; the new file is RED with every failing name recorded verbatim; `git show --stat` on this commit shows ZERO `src/` hunks; and this is its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: the SIXTH class on the EXISTING axis — `CONFIG_REPARSED_VALUE_CARRIERS` and its mechanical shell-alias fence</name>
  <files>tests/envelope_wrapper_class.rs</files>
  <read_first>
    `tests/envelope_wrapper_class.rs:700-914` (`POLICY_SOURCE`, `HOOKS_SOURCE`, `production_code`,
    `POLICY_MIN_PRODUCTION_BYTES` at `:831`, `HOOKS_MIN_PRODUCTION_BYTES` at `:838` and
    `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic` — **read to leave
    UNTOUCHED**), `:5791-6060` (section 16's header and `CONFIG_RESOLUTION_CLASSES` at `:6038` — the
    five existing classes and the section header's own statement of why it is a fourth axis, which this
    task EXTENDS rather than replaces), `:6060-6200` (the degenerate-proofing block and the per-class
    predicate loop at `:6161`), `:6198-6270` (`CONFIG_CONFINED_CARRIERS` at `:6209`,
    `CONFIG_INDIRECTION_CARRIERS` at `:6234`, `CONFIG_ENV_CARRIERS` at `:6255` and the doc at `:6220`
    recording why the fail-closed alphabets are kept out of the invariance arm), `:6270-6395` (the two
    generative properties and the splice positions), `:6395-6410`
    (`CONFIG_RESOLUTION_CASES = 161`, `CONFIG_RESOLUTION_SLOTS = 21`,
    `MIN_CONFIG_RESOLUTION_CLASSES = 5` and the three per-alphabet floors, **whose arithmetic this task
    re-derives**), `:6440-6700` (the floor assertions, the DOTLESS fence at `:6501-6535` naming
    `CALLEE_KNOWN_LEADING_PREFIX`, the disjointness assertions at `:6543-6570`, the permitted-base
    fence, and the case-count identities at `:6663`), `:5197` (`CALLEE_KNOWN_LEADING_PREFIX`),
    `:3000-3230` and `:3860-4360` (sections 13 and 14 — **read to mirror the SHAPE, not to edit**).
  </read_first>
  <action>
**No existing axis is touched and no fifth axis is created.** `UNREADABLE_CLASSES`, `DELETION_CLASSES`
and `CALLEE_GRAMMAR_CLASSES`, their predicates, their degenerate-proofing assertions and all of their
floors stay byte-identical, and so do `POLICY_MIN_PRODUCTION_BYTES`, `HOOKS_MIN_PRODUCTION_BYTES` and
`wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`. **This plan is granted no
deletion of any kind.** Section 16's five existing classes, its three alphabets' existing entries, its
dotless fence and its permitted-base fence are all kept and extended.

**Extend the section header with audit 8's finding, in its own words**: rounds 1–5 were about how a word
is written, round 6 about which words arrive, round 7 about which arriving word is the verb, round 8
about what the verb runs under — **and round 9 is on the SAME axis as round 8, one REGION over.** The
guard reads config assignments in the argv region and in the environment; git resolves configuration
from a third place, a value the guard itself confined and handed on. That is why this is a SIXTH class
here and not a fifth axis of its own.

**1. The SIXTH class, with a degenerate-proof predicate.** *A carrier delivered inside a config VALUE
the guard confines* — a `-c` or `git config` assignment whose key names an ordinary, bounded section and
whose VALUE git re-parses as a git command line including its own leading options. Evaluated on a
REPRESENTATIVE SPLICED COMMAND as the other five are. `MIN_CONFIG_RESOLUTION_CLASSES` rises from 5 to 6.

**2. Extend the degenerate-proofing block rather than replacing it.** Assert that the new class is
satisfied by `git -c alias.q="-c include.path=/tmp/e status" q` and by
`git config alias.q "-c include.path=/tmp/e status"`, and by **none** of the five existing classes;
assert that every existing class's representative satisfies the new class **not at all**; and keep the
existing negative control (`git commit -m "include.path=/tmp/e"` satisfies NONE) with a new one beside
it — **`git -c alias.q='!git push' q` must satisfy the new class NOT AT ALL**, because a `!` body is a
shell command handed to a child and is `T-19-86`'s class, not this one.

**3. A FOURTH alphabet, `CONFIG_REPARSED_VALUE_CARRIERS`, with its own FAIL-CLOSED property in BOTH
deliveries.** Entries are the non-`!` alias bodies — `-c alias.q="-c include.path=/tmp/evil.cfg status"`,
`-c alias.z="-c core.hooksPath=/dev/null status"`, `-c ALIAS.q="-c include.path=/tmp/evil.cfg status"`,
`--config-env=alias.q=EVILBODY`, `-c alias.st=status` — spliced **between the governed program and its
decision words**, plus a separate persisted-delivery arm splicing `git config alias.<n> "<body>"` as a
whole command. Its property asserts **refusal AND an empty walk**, and it is kept OUT of the invariance
arm for the reason section 16's doc already gives for the other two fail-closed alphabets: these entries
are refused after `19-25` even on a PERMITTED base, so an entry in the invariance arm would be stricter
than its base and turn that property permanently red in a file `19-25` may not edit.

**4. `!`-bodied entries join `CONFIG_CONFINED_CARRIERS`, and that is the verdict-preserving half.**
`-c alias.p='!git push --force origin main'` and `-c alias.b='!git status'` are added to the confined
alphabet beside `-c a=b`, because their verdict does not move. **This is the alphabet the invariance arm
draws**, so adding them there is what makes the corpus able to fail on a rule that refuses them.

**5. The SHELL-ALIAS FENCE — the mechanical assertion this task exists to add.** Assert that no entry of
`CONFIG_REPARSED_VALUE_CARRIERS` carries a value whose first byte is `!`, and assert BY NAME that at
least one `!`-bodied entry IS in `CONFIG_CONFINED_CARRIERS`. The failure message names the reason rather
than leaving it to be inferred: `tests/envelope_command_position.rs:550` and
`tests/envelope_config_resolution.rs:1539-1543` both pin a `!`-bodied alias PERMITTED as a registered
`T-19-86` row; git treats a body whose FIRST BYTE is `!` as a shell command run in a CHILD that inherits
the envelope's injection (measured `/ENV_WINS`) while a non-`!` body is re-parsed IN-PROCESS at
command-line precedence (measured `/INCLUDE_WINS`); so a rule that refused both would be permanently red
in files `19-25` may not edit. Written in the shape the DOTLESS fence at `:6501-6535` already uses.

**6. Extend the disjointness and dotless assertions to the new alphabet.** `CONFIG_REPARSED_VALUE_CARRIERS`
must be disjoint from all three existing alphabets, must never be drawn by the invariance arm, and must
carry no dotless-key entry — the same `CALLEE_KNOWN_LEADING_PREFIX` reasoning, restated for the fourth
alphabet.

**7. Extend the PERMITTED-BASE fence to the new property.** The base set the re-parsed-value property
draws from must contain at least one base whose unwrapped form is PERMITTED, asserted by name, with the
failure message carrying the measurement: a property drawing only refused bases is green before the fix
and certifies nothing — the ninth consecutive instance of `T-19-76`'s failure mode, produced by the
corpus rather than found by the next audit.

**8. Re-derive the floors and STATE the arithmetic.** `CONFIG_RESOLUTION_CASES`,
`CONFIG_RESOLUTION_SLOTS`, `CONFIG_RESOLUTION_CLASS_COUNTS` and the per-alphabet floors are recomputed
from the new generation arithmetic (entries × bases × slots) and stated in each constant's doc as exact
equalities, so losing one case turns them red. Add `MIN_CONFIG_REPARSED_VALUE_CARRIERS`. Audit 5 found
`19-16` set a floor of 50 against a maximum of 40 by construction; state the arithmetic and check it.

**9. Extend the per-alphabet floor** in the shape of the existing one: every alphabet this round widens
must be able to draw at least one config-resolution class, with a failure message saying the correct
response is to RESTORE entries and never to delete the floor, and naming `T-19-105` as the NINTH
consecutive instance of this failure mode and the SECOND consecutive round in which the gap was one
region over on the same axis rather than one axis further out.

This whole task is RED by design except the invariance arm, the degenerate-proofing block and the four
mechanical fences: the fail-closed assertions cannot pass until `19-25` exists. Commit as its own
commit, second, with ZERO `src/` hunks.
  </action>
  <verify>
    <automated>rtk proxy cargo test --test envelope_wrapper_class --no-fail-fast 2&gt;&amp;1 | tail -40; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy grep -c 'UNREADABLE_CLASSES\|DELETION_CLASSES\|CALLEE_GRAMMAR_CLASSES\|CALLEE_KNOWN_LEADING_PREFIX' tests/envelope_wrapper_class.rs; rtk proxy grep -c 'POLICY_MIN_PRODUCTION_BYTES: usize = 40_000\|HOOKS_MIN_PRODUCTION_BYTES: usize = 20_000' tests/envelope_wrapper_class.rs; rtk proxy grep -c 'MIN_CONFIG_RESOLUTION_CLASSES: usize = 6' tests/envelope_wrapper_class.rs; git diff HEAD~1 --numstat -- tests/envelope_wrapper_class.rs; git show --stat HEAD | grep -c '^ src/' | grep -qx 0 &amp;&amp; echo "SIXTH-CLASS-RED-ZERO-SRC"</automated>
  </verify>
  <done>`UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES`, their predicates, their degenerate-proofing assertions and all of their floors are byte-identical, and so are `POLICY_MIN_PRODUCTION_BYTES` (40_000), `HOOKS_MIN_PRODUCTION_BYTES` (20_000) and `wrapper_names_the_fix_must_not_know_are_absent_from_the_production_logic`, with `git diff --numstat` over this file showing that no line was deleted anywhere; section 16 gains a SIXTH class rather than a fifth axis, with the section header extended to record audit 8's region-not-axis finding; `MIN_CONFIG_RESOLUTION_CLASSES` is 6; the degenerate-proofing block is EXTENDED — the new class is satisfied by both deliveries of a non-`!` alias carrier, by none of the five existing classes, and NOT AT ALL by `git -c alias.q='!git push' q`, with the existing negative control kept; `CONFIG_REPARSED_VALUE_CARRIERS` exists as a FOURTH alphabet with its own fail-closed property in both the `-c` and the persisted deliveries, asserted disjoint from all three existing alphabets, never drawn by the invariance arm, and carrying no dotless-key entry; `!`-bodied entries are ADDED to `CONFIG_CONFINED_CARRIERS` as verdict-preserving; **the SHELL-ALIAS FENCE asserts mechanically that no `CONFIG_REPARSED_VALUE_CARRIERS` entry has a `!` first byte and that a `!`-bodied entry IS in `CONFIG_CONFINED_CARRIERS`**, with the failure message naming both pinning files, both line numbers, git's measured one-byte rule and the consequence; the permitted-base fence covers the new property; every floor's arithmetic is re-derived, stated in its doc and equal to the generation count; no existing floor, alphabet entry, property or assertion was lowered, deleted or narrowed anywhere; the new properties are RED with every failing name recorded; `git show --stat` shows ZERO `src/` hunks; and this is the second commit.</done>
</task>

<task type="auto">
  <name>Task 3: the registrations, the corrected `T-19-106` entry, the outstanding gap, and the handoff gate</name>
  <files>.planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md, .planning/phases/19-gitsafe-git-blast-radius-envelope/deferred-items.md</files>
  <read_first>
    `19-SECURITY.md`'s sections "Threats found by audit 8", "What audit 8 measured and found INERT",
    "The closures, re-measured rather than accepted from the summaries", "The over-refusal cost,
    re-measured from BOTH sides", "The mechanism pins, verified non-dead", "The known-open set, as
    audit 8 verified it", "The fail-open residue", "The four execution-time judgements", "Gates
    observed", "Audit 8 — what the round-8 control can and cannot fail on" and "Audit 8 Sign-Off",
    plus the appended execution records of plans 19-22 and 19-23, which this plan's subsection FOLLOWS
    and does not edit. `deferred-items.md`'s round-8 closure section at `:1329-1430` — **in particular
    the `T-19-106` entry at `:1399`, which records it CLOSED and which audit 8 re-opened at `medium`**
    — its `T-19-96` entry, its `glab --host` forge cell at `:1285`, its `T-19-17r` bookkeeping gap at
    `:1292`, and its `tests/envelope_tracer.rs` ETXTBSY entry carried since 19-07.
  </read_first>
  <action>
**1. One appended subsection to `19-SECURITY.md`**, after the plan-19-23 record and touching nothing
that precedes it, titled so a reader cannot mistake it for an audit finding — a record made by plan
19-24 — carrying:

  - **the invariant this round is about**, stated once: a value the guard read, confined and let
    through is still an input to git's configuration. Round 8 closed the question below it and audit 8
    verified every one of its rows, so this round is one REGION over on the same axis rather than a
    ninth spelling of the same class;
  - **the SHELL-ALIAS seam, stated FIRST among the findings**, with both pinning files, both line
    numbers, git's one-byte rule measured in four spellings, and the statement that a blanket `alias.*`
    refusal would be permanently red in files `19-25` may not edit;
  - **every measured row with its walk**, in the column shape the audit uses: the `-c`-delivery
    `T-19-108` rows, the persisted-delivery rows, the paired discriminators, the discrimination
    controls, the disclosed over-refusal family with its permitted twins, the invocation rows, the
    dotless rows, the two ordering rows, the two recorded-not-asserted rows and the `T-19-86` rows;
  - **the K1/K2/K3 enumeration with its measurements**, stating that `alias.<name>` with a non-`!` body
    is the only member of K1, that at least three K2 representatives were measured INERT because the
    child inherits the injection, and that K3 is closed by `19-23`;
  - **the persistence asymmetry with `include.path`**, with audit 8's INERT rows carried forward as
    ESTABLISHED and this plan's live alias rows beside them, and the explicit statement that a
    `git config include.path` clause would be inert and must not be added;
  - **the depth and quoting facts**, so `19-25` costs option (a) against measurement: an alias body can
    itself carry `-c alias.<n>=…` and git re-parses it at depth 2, and git splits an alias body with its
    own quoting rules rather than the shell's;
  - **the bare-remote legs in full**, with the SHA before and after each and the CONTROL beside each;
  - **the design question, stated with what the corpus can now fail on**, and explicitly NOT answered
    here because the costing belongs to the plan that measures the rule. Record which shapes this corpus
    turns red — a prefix match on `alias` (red at `aliasx.` and `notalias.`), a blanket `alias.*`
    refusal including `!` bodies (red at both `T-19-86` rows), and a refusal of a key that cannot be
    decomposed into a section (red at `-c a=b` and at round 7's whole generative property) — and record
    the disclosed cost this corpus pins in the other direction;
  - **`T-19-108` will close only AS SCOPED**, stated plainly: the non-shell alias body closes; audit 7's
    `!`-bodied destructive pair — the one that rewrote a bare remote's `main` from `ac303dc` to
    `9f62444` at `fb43577` — **still works after this round**, because closing it means taking on
    `T-19-86`, which is out of scope by explicit user decision and whose two rows are pinned PERMITTED
    in files this round may not edit. This is audit 8's own boundary paragraph, which lists the
    re-parsed config value and the command-line-as-data as two separate bullets;
  - **`T-19-86` recorded WIDER and NOT closed**, with audit 8's finding that the layer-3 catch three
    documents attribute to closing `T-19-103` is absent for a carrier-bearing alias body;
  - **`T-19-109` registered** — the counted-completeness claim at `cred.rs:246` and the "RESTORATION of
    layer 3's catch" claim in the 19-22 record, the 19-23 record and `19-23-SUMMARY.md` — with the
    correction recorded as a plan-19-24 finding BESIDE those subsections rather than as an edit to
    them, which is the provenance discipline audit 8 used for this exact shape. The fix itself is
    `19-25`'s;
  - **the `T-19-106` mis-closure**, recorded: `deferred-items.md` marks it CLOSED at round 8, and audit
    8 re-opened it at `medium` because `GH_API_VALUE_OPTS` is a pure `gh api` constant, `gh` 2.45.0 IS
    installed, and the stated reason for leaving it unpinned is a fact about a different callee.
    Re-verified while planning, and the probe form is **ENDPOINT-LESS** (`gh api <opt>`) so that no
    row touches the network: all seventeen entries answer `flag needs an argument`, `--bogus-opt`
    answers `unknown flag: --bogus-opt`, and **`gh api --paginate` answers `accepts 1 arg(s),
    received 0`** — a POSITIONAL error, not a flag-level one, which is exactly what makes it the second
    negative control: the flag was ACCEPTED and consumed NO value, and `gh` got as far as complaining
    about the missing endpoint. **Record that measured string rather than a paraphrase**, and record
    that the endpoint-bearing form `gh api repos/o/r --paginate` makes a real HTTP request (measured, a
    404 body) — a pin written that way would be non-hermetic and would fail on a machine without
    network or auth, which is a flaky pin in the fail-open direction. **And a correction to audit 8's own
    suggestion, measured**: removing `--hostname` from `FORGE_VALUE_OPTS` would be a REGRESSION in the
    under-counting direction, because `glab --hostname gitlab.com mr create --title x` leaves exactly
    one ledger line today while an option NOT in that constant leaves ZERO. The fix is `19-25`'s and it
    is "record why", not "remove";
  - **the fail-open residue's missing revisit condition**, recorded as audit 8 recorded it: the
    admission is complete and correctly unclaimed in five places, the residual is acceptable and
    properly bounded, and the single gap is that its only control is a human reading a future git's
    release notes and **nothing schedules that**. `19-25` adds the revisit condition and a version
    witness. **Record, in the same bullet, that the witness `19-25` adds is a SCHEDULE and not a
    CONTROL** — it says WHEN to look and cannot say WHAT changed, it does not observe a new indirection
    or re-parsed section appearing, so the residues stay uncovered by any automated control and audit
    8's judgement of them is unchanged. **This plan invents no acceptance and adds no `AR-` row**;
  - **the stale proportional-floor comment, RECORDED and deliberately not fixed.** `policy.rs:6644`'s
    own comment cites "228,785 / 78.7%" against a measured 228,100 / 78.9%. The floor itself is correct
    and load-bearing; only the arithmetic in its comment has drifted. **This plan may not edit
    `policy.rs` at all**, so it is recorded here and in `deferred-items.md` and left for a later round;
  - **the `glab --host` forge cell**, carried forward unchanged with its unconfirmed-callee caveat and
    the note that `glab` is confirmed NOT installed — re-verified while planning. Deliberately not
    fixed and out of scope;
  - **the `T-19-17r` bookkeeping gap, recorded as OUTSTANDING and deliberately NOT resolved.** State
    that `19-17-SUMMARY.md` calls it "accepted"; that audits 5, 6, 7 and 8 all confirmed the measurement
    and both pins and all four explicitly declined to make the acceptance; that there is no
    Accepted-Risks-Log row, no `AR-19-13` and no register row; and that **this plan does not make the
    acceptance, because accepting a risk is a human decision and four audits running have deliberately
    left it unmade.** The next round either adds the log row or drops the word — and this plan does
    neither;
  - **the byte floors and the stripper's three protections, re-measured and reported**: `policy.rs` raw
    372,920 / production 228,101 against 40,000 and against the proportional floor of 180,000 (78.9%),
    `hooks.rs` raw 99,753 / production 74,358 against 20,000, the deep anchor `fn forbidden_repo_path`
    at production line 4,592 of 4,613, and exactly ONE `#[cfg(test)]` line at 4,614 with the count
    itself asserted;
  - **the statement that this plan CLOSES NOTHING**, that `T-19-105`, `T-19-106`, `T-19-108` and
    `T-19-109` all stay open, that `T-19-86` and `T-19-91` remain OPEN at `high` so
    `/gsd-secure-phase 19` is not cleared, and that re-measuring and re-classifying these rows is that
    command's job rather than a plan's.

Do not touch any audit table, the Security Audit Trail, the Accepted Risks Log, the Sign-Off, or any
earlier appended subsection.

**2. `deferred-items.md`.** Register `T-19-108` and `T-19-109` with their corpus recorded RED and the
scoped-closure boundary stated; record `T-19-105` open for the ninth consecutive round with the
region-not-axis finding; **CORRECT the `T-19-106` entry from CLOSED to OPEN at `medium`**, citing audit
8's finding and this plan's re-measurement, and record the `FORGE_VALUE_OPTS` regression measurement
beside it; record the `T-19-86` entry as WIDER without closing it; record the fail-open residue's
missing revisit condition **with the note that `19-25`'s version witness is a schedule and not a control
over it**; record the stale `policy.rs:6644` proportional-floor comment (228,785 / 78.7% against a
measured 228,100 / 78.9%) as a documentation drift this plan may not fix; leave the `glab --host` forge
cell and the `T-19-17r` bookkeeping gap present and unresolved. Leave `T-19-86`, `T-19-91`, `T-19-96` and `T-19-74` unchanged and open.

**3. The handoff gate.**

  - The command is `rtk proxy cargo test --no-fail-fast`. A plain `cargo test` fail-fasts at
    `driver_reattach` and `envelope_*` sorts after `driver_*`, so an unqualified run never executes a
    single envelope suite; `rtk proxy` is required because the RTK hook strips exactly the `warning:`
    and `test result:` lines any count is read from — `19-19` first read 1364 instead of 1605 that way
    (D-34). Read the counts with `rtk proxy grep` over the redirected log, never a plain `grep`.
  - **State the gate arithmetic explicitly rather than assuming it.** `passed + failed` is strictly
    greater than **1680**, the total `19-23-SUMMARY.md:221` records and audit 8 re-measured over 43
    result lines as 1679 passed / 1 failed / 13 ignored. **A red test RAN, so red→green leaves the total
    unchanged and the increase comes ONLY from the NEW `#[test]` fns this plan adds** — count them from
    `git show` and check the identity against the observed number. A disagreement is a finding.
  - Every failure name is one of the three documented flakes
    (`a_fresh_scan_finds_the_orphaned_run_live_with_its_last_journal_step`,
    `a_run_killed_without_an_ending_is_reported_crashed_and_nothing_on_disk_is_repaired`,
    `a_relocated_copy_of_the_stub_refuses_instead_of_acting`) or one of the RED names this plan creates.
    The first FIRED during audit 8 and passed on an immediate re-run; one firing is not evidence of a
    new defect and an absence is not evidence they are fixed. **The RED set is non-empty and every one
    of its names is listed in the SUMMARY** — that list is `19-25`'s handoff contract and the one number
    a run that did nothing cannot forge.
  - `--test envelope_config_resolution`, `--test envelope_callee_grammar`,
    `--test envelope_argv_deletion`, `--test envelope_literal_decision`,
    `--test envelope_expansion_slots` and `--test envelope_command_position` are FULLY GREEN and
    unmodified.
  - `cargo build` and `cargo clippy -- -D warnings` exit 0. `cargo clippy --tests` is NOT the gate.
  - Record the per-binary counts for **all FIFTEEN `envelope_*` binaries** — the fourteen audit 8
    observed plus the `envelope_reparsed_value` this plan adds — and confirm every one RAN. **A run
    reporting fourteen is a run in which this plan's own evidence file did not execute.**

Commit the record as its own commit, the third.
  </action>
  <verify>
    <automated>rtk proxy cargo build &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo clippy -- -D warnings &gt;/dev/null 2&gt;&amp;1 || exit 1; rtk proxy cargo test --no-fail-fast &gt;/tmp/19-24-gate.log 2&gt;&amp;1; echo "full_suite_exit=$?"; rtk proxy grep -c '^| AR-19-13 |' .planning/phases/19-gitsafe-git-blast-radius-envelope/19-SECURITY.md; rtk proxy grep 'test result:' /tmp/19-24-gate.log | tail -50; rtk proxy grep -c 'Running tests/envelope_' /tmp/19-24-gate.log; git diff --stat HEAD~3..HEAD | tail -12; git diff --numstat HEAD~3..HEAD -- tests/ | tail -6; git log --oneline -3</automated>
  </verify>
  <done>`19-SECURITY.md` carries ONE further appended, clearly-attributed plan-19-24 record with the invariant, the SHELL-ALIAS seam stated first with both pinning files and line numbers and git's measured one-byte rule, every measured row and its walk, the K1/K2/K3 enumeration with at least three K2 representatives measured INERT, the persistence asymmetry with audit 8's INERT include rows carried forward and the statement that a `git config include.path` clause would be inert, the depth-2 and alias-body-quoting facts, the bare-remote legs with SHAs and controls, the design question stated with the shapes this corpus turns red, the statement that `T-19-108` will close only AS SCOPED and that audit 7's `!`-bodied destructive pair still works, `T-19-86` recorded WIDER and not closed, `T-19-109` registered with its correction recorded BESIDE the 19-22 and 19-23 subsections rather than as an edit, the `T-19-106` mis-closure recorded with the re-measured seventeen-entry probe and the measured `FORGE_VALUE_OPTS` regression that makes "record why" the correct disposition, the fail-open residue's missing revisit condition recorded with no acceptance invented, the `glab --host` cell carried forward unfixed, the `T-19-17r` gap OUTSTANDING with the acceptance deliberately unmade, the re-measured byte floors and all three stripper protections, and the statement that nothing is closed and no gate is cleared — with NO edit to any audit table, the trail, the risk log, the sign-off or any earlier appended subsection, and the Accepted-Risks-Log row count for that risk ID still zero; `deferred-items.md` registers `T-19-108` and `T-19-109`, records `T-19-105` open for the ninth round, **CORRECTS the `T-19-106` entry from CLOSED to OPEN at `medium`**, records `T-19-86` as WIDER without closing it, and leaves `T-19-91`, `T-19-96`, `T-19-74`, the forge cell and the `T-19-17r` gap unchanged; the `--no-fail-fast` run reports `passed + failed` strictly greater than 1680 with the arithmetic stated and checked against `git show`, counted with `rtk proxy grep` over a redirected log, every failure name is one of the three documented flakes or one of this plan's RED names, and the complete RED list is in the SUMMARY; all FIFTEEN `envelope_*` binaries RAN with their per-binary counts recorded; the six prior evidence files are green and unmodified; `git diff --numstat` over `tests/` shows ZERO deletions across all three commits; and this is a third commit distinct from the two before it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driven agent → `PreToolUse` guard | The agent supplies an arbitrary shell command string the guard must attribute to a program, to the words that decide, to the configuration those words run under, and now to what git does with a value the guard approved |
| written word → word the shell runs | Round 5 closed this half |
| words read → words that ARRIVE | Round 6 closed this half; audit 6 verified the pair COMPLETE over bash |
| reconstructed argv → which word is the VERB | Round 7 closed this half |
| the verb → the CONFIGURATION it runs under, in the ARGV and ENVIRONMENT regions | Round 8 closed these two; audit 8 verified every row |
| **a value the guard CONFINED → what git does with it** | **This round's boundary. Git re-parses a non-`!` `alias.<name>` body as a command line INCLUDING its leading options, in-process, so a `-c include.path=` inside the value is applied by git itself at command-line precedence — reaching `core.hooksPath` through a key that correctly answers `false` to the round-8 clause** |
| corpus → the control it certifies | An alphabet that cannot DRAW a class is a property that cannot FAIL on it, and eight consecutive rounds have shipped one |
| **a rule's REACH → a pin in a file it may not edit** | **A blanket `alias.*` refusal turns two registered `T-19-86` rows permanently red; git's one-byte shell rule is the fence and it is measured, not assumed** |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-105 | Spoofing | every generative alphabet, and `CONFIG_RESOLUTION_CLASSES`'s five argv-and-environment classes | medium | mitigate | The corpus is widened FIRST and observed RED, the ordering that worked five times. A SIXTH class joins the EXISTING axis — audit 8's finding is that the region moved, not the axis — with a degenerate-proof predicate, a fourth `CONFIG_REPARSED_VALUE_CARRIERS` alphabet carrying its own fail-closed property in both deliveries, `!`-bodied entries added to the confined alphabet as verdict-preserving, a mechanical SHELL-ALIAS fence, extended disjointness, dotless and permitted-base fences, and floors whose arithmetic is re-derived and stated. Closed only by `19-25` |
| T-19-108 | Elevation of Privilege | the CONFINEMENT clause's REACH — `config_key_names_an_indirection_section` (`policy.rs:976`) and `scan_leading` (`policy.rs:402`) read config assignments in the leading-option region only; a value the clause CONFINES carries the same indirection into a region the scan never reads | high | mitigate (corpus only; rules are `19-25`) | Every row measured at its verdict on layer-2-PERMITTED bases in BOTH deliveries, confirmed against the REAL git binary with the envelope's own injection as the control and end to end against a rebuilt bare remote with a control beside every leg, and written as RED assertions with a DERIVED identifier each. The disclosed over-refusal family is pinned with a permitted twin beside every row, the invocation rows are pinned UNCHANGED, and the two ordering rows are pinned at deliberately different post-fix identifiers so a second-pass implementation lands red |
| T-19-109 | Repudiation | `cred::hooks_path_env`'s counted "FIVE forms" table (`cred.rs:246-260`) and the "RESTORATION of layer 3's catch" claim in three documents | low | mitigate (registered; rule is `19-25`) | Registered here with the sixth form measured — a non-`!` `alias.<name>` body carrying `-c include.path=<f>`, which resolves `/INCLUDE_WINS` against the control's `/ENV_WINS` and moved a bare remote's ref. The correction is recorded BESIDE the 19-22 and 19-23 subsections rather than as an edit to them. Not fixed by this plan |
| T-19-106 | Spoofing | the drift pins' REACH — `GH_API_VALUE_OPTS` (`policy.rs:4374`) unpinned for a reason that is a fact about `glab`, and `FORGE_VALUE_OPTS`'s `--hostname` (`policy.rs:4328`) rejected by `gh pr create` | medium | mitigate (entry CORRECTED here; rule is `19-25`) | The `deferred-items.md` entry is corrected from CLOSED to OPEN. All seventeen `gh api` entries re-measured answering `flag needs an argument` with two negative controls; and a correction to audit 8's own suggestion, measured — removing `--hostname` would be a REGRESSION in the under-counting direction, so the disposition is "record why", not "remove" |
| T-19-17r | Denial of Service | over-refusal from the round-5 literalness rule | medium | **NOT accepted here — bookkeeping gap RECORDED, acceptance deliberately unmade** | Four audits confirmed the measurement and both pins and all four explicitly declined to add the acceptance because that is a human decision. This plan records the gap as OUTSTANDING and makes no acceptance. There is no `AR-19-13` |
| T-19-86 | Elevation of Privilege | `classify_git`'s denylist default arm reached through a governed program's own operand | high | **accept for this round (out of scope by explicit user decision)** | Unchanged and unremediated; four registered rows plus the persisted-alias arm still at exit 0 and their pins stay green UNMODIFIED. **RECORDED as measurably WIDER than the register credits**, and the `!`-bodied destructive pair is recorded as still working after this round. Because it remains open at `high`, this plan does NOT clear `/gsd-secure-phase 19` |
| T-19-91 | Elevation of Privilege | a git classifier's own decision operand, assembled by expansion | high | **accept for this plan (registered, arms unweakened, not remedied)** | Record unweakened, no remedy added, counts toward `threats_open` |
| T-19-96 | Elevation of Privilege | a glob in a PUSH FLAG, one slot outside the decision region | medium | **accept for this plan (registered by `19-16`, not fixed)** | Left exactly as pinned |
| — | Elevation of Privilege | the `glab --host` forge cell | — | **out of scope, carried forward unfixed** | `glab` is confirmed NOT installed — re-verified while planning — so the callee's grammar is unconfirmed and it is not claimed as a live bypass. Audits 7 and 8 both declined to upgrade it; this plan declines too |
| T-19-SC | Tampering | package-manager installs | high | mitigate | No crate is added; this plan touches only `tests/` and `.planning/`. `Cargo.toml` and `Cargo.lock` are not in `files_modified`. The real-git probes and the bare-remote fixture shell out through `std::process::Command` inside tests |
</threat_model>

<verification>
- `rtk proxy cargo build && rtk proxy cargo test --no-fail-fast && rtk proxy cargo clippy -- -D warnings`
  (D-34). The suite number MUST come from a `--no-fail-fast` run, MUST be read with `rtk proxy grep`,
  and MUST be read as `passed + failed` against **1680**.
- The gate arithmetic is STATED and CHECKED: expected `passed + failed` = 1680 plus the number of NEW
  `#[test]` fns this plan adds, counted from `git show`. A red test RAN, so red→green contributes
  nothing; a disagreement is a finding.
- All FIFTEEN `envelope_*` binaries RAN and their per-binary counts are recorded. Fourteen means this
  plan's own evidence file did not execute.
- The RED set is NON-EMPTY, every one of its names is listed in the SUMMARY, and every other failure is
  one of the three documented flakes.
- Every commit shows ZERO `src/` hunks under `git show --stat`. This is the evidence the split exists
  to produce.
- Every re-parse claim is backed by a recorded real-git probe using the envelope's own
  `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` injection as the control, with `/ENV_WINS` printed beside each
  carrier's value; the K1/K2 split is measured in BOTH directions and at the one-byte boundary; at
  least three K2 representatives are measured INERT.
- The bare-remote fixture is REBUILT and every leg re-measured with the remote's SHA before and after
  and a CONTROL beside each. A leg that does not reproduce is reported, not repeated.
- **At least one reproducer base is layer-2 PERMITTED, asserted mechanically.**
- `UNREADABLE_CLASSES`, `DELETION_CLASSES` and `CALLEE_GRAMMAR_CLASSES`, their predicates, their
  degenerate-proofing blocks and all their floors are byte-identical; `POLICY_MIN_PRODUCTION_BYTES` and
  `HOOKS_MIN_PRODUCTION_BYTES` are unchanged; the stripper's proportional floor, deep anchor and
  sentinel count keep their present strictness; no existing `MIN_*` constant was lowered; and
  `git diff --numstat` over `tests/` shows ZERO deletions across all three commits.
- `tests/envelope_command_position.rs` and `tests/envelope_config_resolution.rs` are UNMODIFIED.
- **The SHELL-ALIAS FENCE is asserted mechanically**, naming both pinning files, both line numbers and
  git's measured one-byte rule; `CONFIG_REPARSED_VALUE_CARRIERS` carries no `!`-bodied entry and a
  `!`-bodied entry IS in `CONFIG_CONFINED_CARRIERS`.
- `git -c aliasx.q=…` and `git -c notalias.q=…` are pinned at exit 0 before and after; every disclosed
  over-refusal row is pinned beside a permitted twin; every alias INVOCATION row is pinned unchanged.
- The six mechanism pins are present and non-vacuous, including round 8's confinement clause with its
  two discrimination controls.
- `gh pr create --title x` leaves exactly ONE ledger line in a walked fresh root and the cap fires with
  two on call 2, which is the walk's non-blindness control.
- The Accepted-Risks-Log row count for the `T-19-17r` risk ID is zero and the word "accepted" is not
  applied to `T-19-17r`.
- `git diff --stat` touches no file outside `files_modified`, and in particular neither `Cargo.toml`
  nor `Cargo.lock`. The `19-SECURITY.md` diff is one appended subsection after the plan-19-23 record
  and nothing else.
- Three commits exist in order: the eighth evidence file, the sixth class, the record.
</verification>

<success_criteria>
1. Every guard row was MEASURED against the built binary with a fresh envelope root and a walk before
   it was written as an assertion, and every re-parse claim was confirmed against the REAL git binary
   with the envelope's own injection as the control.
2. The corpus goes RED against the pre-fix tree, the RED set is non-empty, and every failing name is
   recorded verbatim in the SUMMARY as `19-25`'s handoff contract.
3. **The SHELL-ALIAS seam is fenced mechanically**, git's one-byte rule is measured in both directions
   and at its boundary, and both `T-19-86` rows are pinned PERMITTED with the files and lines that
   already pin them named in the failure message.
4. The config axis stays ONE axis: a SIXTH class joins `CONFIG_RESOLUTION_CLASSES` with
   `MIN_CONFIG_RESOLUTION_CLASSES` at 6, and nothing was smuggled into any other axis or stood up as a
   fifth.
5. The corpus can fail in BOTH directions — the bypass rows in both deliveries, the two discrimination
   controls that turn a prefix match red, the `!`-bodied entries in the invariance arm, and every
   disclosed over-refusal row pinned beside the permitted twin that does the same work.
6. **The handoff needs no replacement exception and none is granted**: every derivable row is asserted
   at its post-fix verdict, the two undeliverable rows are RECORDED rather than asserted, the
   re-parsed-value alphabet is kept out of the invariance arm, and the SHELL-ALIAS, DOTLESS and
   PERMITTED-BASE fences are asserted mechanically so `19-25` cannot be handed an unsatisfiable seam.
7. The K1/K2/K3 enumeration is measured rather than argued, and the persistence asymmetry with
   `include.path` is recorded so `19-25` cannot add an inert clause.
8. The `T-19-106` entry is CORRECTED from CLOSED to OPEN, and the measured `FORGE_VALUE_OPTS`
   regression is recorded so `19-25` records why rather than removing.
9. The `T-19-17r` bookkeeping gap is recorded as OUTSTANDING, no Accepted-Risks-Log row is added, and
   the word "accepted" is not used for it.
10. No `src/` line changed; no existing floor, alphabet entry, property or assertion was lowered,
    deleted or narrowed anywhere; and `T-19-86` (recorded WIDER), `T-19-91`, `T-19-96` and `T-19-74`
    are untouched and open.
11. The SUMMARY states FIRST that `/gsd-secure-phase 19` is NOT cleared — `T-19-86` and `T-19-91`
    remain OPEN at `high` — that `T-19-108` will close only AS SCOPED, and that audit 7's `!`-bodied
    destructive pair still works; and no unqualified "T-19-60 is closed" appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/19-gitsafe-git-blast-radius-envelope/19-24-SUMMARY.md` when done.
It must contain: the statement FIRST that `/gsd-secure-phase 19` is NOT cleared, that this plan closes
nothing, and that `T-19-108` will close only AS SCOPED with audit 7's `!`-bodied destructive pair still
working; the three commit SHAs in order with `git show --stat` proving ZERO `src/` hunks in each; the
`--no-fail-fast` `passed + failed` totals before and after with the gate arithmetic stated and checked
against `git show`; the COMPLETE list of RED test names, which is `19-25`'s handoff contract; the
per-binary counts for all FIFTEEN `envelope_*` binaries with every one confirmed to have RUN; the
SHELL-ALIAS seam stated prominently with both pinning files, both line numbers and git's one-byte rule
measured in four spellings; a table of every measured row with its exit code, reason identifier and walk
listing, split into the `-c`-delivery `T-19-108` rows, the persisted-delivery rows, the paired
discriminators, the discrimination controls, the disclosed over-refusal family with its permitted twins,
the invocation rows, the dotless rows, the two ordering rows, the two recorded-not-asserted rows and the
`T-19-86` rows; the K1/K2/K3 enumeration with its measurements and at least three K2 representatives
measured INERT; the persistence asymmetry with `include.path`, with audit 8's INERT rows carried forward
as established; the depth-2 and alias-body-quoting facts; the full bare-remote table with SHAs and a
control beside every leg; the design question stated with the shapes this corpus turns red; the two
undeliverable rows with the reason each is left for `19-25`; the `T-19-106` mis-closure with the
re-measured seventeen-entry probe and the measured `FORGE_VALUE_OPTS` regression; the fail-open
residue's missing revisit condition recorded with no acceptance invented; the re-measured byte floors
and all three stripper protections; the `glab --host` forge cell carried forward unfixed with its
unconfirmed-callee caveat; the `T-19-17r` bookkeeping gap recorded as OUTSTANDING with the explicit
statement that this plan did NOT accept it and added no Accepted-Risks-Log row; and a "what remains
uncovered" section naming `T-19-86` FIRST — open at `high` by user scoping decision, recorded WIDER,
and NOT closed — followed by `T-19-91` (open at `high`, arms unweakened), `T-19-96`, `T-19-74`,
`T-19-84`, `T-19-85` and `T-19-61`…`T-19-73`, with the explicit statement that only the
WRAPPER-OPERAND sub-class of `T-19-60` is closed.
</output>
