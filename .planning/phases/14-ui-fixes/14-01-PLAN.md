---
phase: 14-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/screens/normal.rs
  - src/state_reader/mod.rs
autonomous: true
requirements: [UIFIX-01, UIFIX-02]
tags: [rust, ratatui, tui, regression-test, display-defect]

must_haves:
  truths:
    # --- ROADMAP success criteria owned by this plan ---
    - "A project whose .planning/ holds a non-empty HANDOFF.md is reported as paused and renders the cyan pause badge on its dashboard row"
    - "The D-R-P-E-V status cell renders exactly `D  R  P  E  V` (13 terminal cells) and never begins with whitespace"
    # --- UI-SPEC `## UI Considerations` lift (rows 1-5, all `covered`) ---
    - "An empty or whitespace-only HANDOFF.md/HANDOFF.json produces no pause badge — the row renders exactly as an unpaused project"
    - "A non-empty but unparseable HANDOFF.json still reports paused with no pause context — the badge never depends on successful JSON parsing"
    - "At most one badge renders per dashboard row; priority is pause > async-job > session; two glyphs never appear in the alias cell"
    - "The D-R-P-E-V cell renders with no leading blank at terminal widths >=80, >=60, and <60 — one construction site feeds all three width tiers"
    - "`D` occupies the same starting column as the first character of sibling Status values such as `executing`, `verifying`, and `v1.0 Complete`"
    # --- Edge-probe fallback lift (UIFIX-02: `empty` and `encoding`, both auto-covered) ---
    - "compact_pipeline for DiskStatus::NoDirectory and DiskStatus::Empty still renders all five stage letters as `D  R  P  E  V` with no leading blank — a project with no workflow data on disk never produces a short or blank pipeline cell"
    - "The no-leading-blank assertion is made on the rendered Line's first character (char-level), not a byte index, so multi-byte glyphs elsewhere in the row cannot invalidate it"
  artifacts:
    - path: "src/ui/screens/normal.rs"
      provides: "alias_badge badge-priority function, whitespace-free compact_pipeline, first #[cfg(test)] mod tests under src/ui/"
      contains: "fn alias_badge"
    - path: "src/state_reader/mod.rs"
      provides: "HANDOFF pause-detection regression tests inside the existing #[cfg(test)] mod tests block"
      contains: "HANDOFF.md"
  key_links:
    - from: "src/state_reader/mod.rs"
      to: "src/ui/screens/normal.rs"
      via: "parse_project_state sets ProjectState.paused (mod.rs:218-220); the dashboard row reads it and selects the badge"
      pattern: "state\\.paused"
    - from: "src/ui/screens/normal.rs (alias_cell construction)"
      to: "src/ui/screens/normal.rs (alias_badge)"
      via: "the row builder delegates the three-tier badge priority to the extracted pure function"
      pattern: "alias_badge\\("
  prohibitions:
    - statement: "MUST NOT render HANDOFF file body text in the dashboard alias or status cells — the dashboard surfaces a glyph only; pause context stays confined to the detail view"
      category: privacy
---

<objective>
Land verified regression coverage for the shipped HANDOFF pause badge (UIFIX-01) and remove the
leading pad cell from the D-R-P-E-V status display at its construction site (UIFIX-02).

Purpose: two of the four Phase 14 display defects live in the dashboard row. UIFIX-01 is believed
already fixed by Phase 11 and needs proof, not reimplementation. UIFIX-02 is a one-line
construction bug with no test coverage anywhere under `src/ui/`.

Output: `alias_badge` extracted from the shipped badge-priority rule, a whitespace-free
`compact_pipeline`, the first `#[cfg(test)] mod tests` under `src/ui/`, and five HANDOFF
regression tests inside the state reader's existing test block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-fixes/14-CONTEXT.md
@.planning/phases/14-ui-fixes/14-PATTERNS.md
@.planning/phases/14-ui-fixes/14-UI-SPEC.md
@CLAUDE.md
</context>

## Artifacts this phase produces

New symbols created by THIS plan (excluded from drift verification — they do not exist yet):

| Symbol | File | Kind |
|--------|------|------|
| `alias_badge` | `src/ui/screens/normal.rs` | private free fn — `(bool, bool, bool) -> Option<(&'static str, Color)>` |
| `mod tests` (new module) | `src/ui/screens/normal.rs` | `#[cfg(test)]` module — the first one under `src/ui/` |
| `test_paused_project_shows_pause_badge_end_to_end` | `src/ui/screens/normal.rs` | test fn |
| `test_pause_badge_wins_over_session` | `src/ui/screens/normal.rs` | test fn |
| `test_pause_badge_wins_over_async_job` | `src/ui/screens/normal.rs` | test fn |
| `test_async_job_badge_when_not_paused` | `src/ui/screens/normal.rs` | test fn |
| `test_no_badge_when_nothing_active` | `src/ui/screens/normal.rs` | test fn |
| `test_badge_is_never_two_glyphs` | `src/ui/screens/normal.rs` | test fn |
| `test_compact_pipeline_has_no_leading_blank` | `src/ui/screens/normal.rs` | test fn |
| `test_compact_pipeline_exact_cells_and_width` | `src/ui/screens/normal.rs` | test fn |
| `test_compact_pipeline_stage_colors_preserved` | `src/ui/screens/normal.rs` | test fn |
| `test_compact_pipeline_no_workflow_data_still_renders_five_stages` | `src/ui/screens/normal.rs` | test fn |
| `test_handoff_md_non_empty_sets_paused` | `src/state_reader/mod.rs` | test fn |
| `test_handoff_json_non_empty_sets_paused_with_context` | `src/state_reader/mod.rs` | test fn |
| `test_handoff_md_whitespace_only_is_not_paused` | `src/state_reader/mod.rs` | test fn |
| `test_no_handoff_file_is_not_paused` | `src/state_reader/mod.rs` | test fn |
| `test_handoff_json_invalid_is_paused_without_context` | `src/state_reader/mod.rs` | test fn |

No new files. No new Cargo dependencies. `tempfile` is already a runtime dependency and is the
fixture crate used by 7 existing unit-test modules.

## Multi-Source Coverage Audit (phase-wide)

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|-----|----------------------|------|--------|-------|
| GOAL | — | Four long-standing display defects stop misreporting project state | 14-01, 14-02, 14-03 | COVERED | |
| GOAL | — | SC1: non-empty HANDOFF.md shows the pause badge | 14-01 | COVERED | verify-first |
| GOAL | — | SC2: D-R-P-E-V renders with no leading blank at any terminal width | 14-01 | COVERED | |
| GOAL | — | SC3: markdown edit key enters edit mode on first press | 14-02 | COVERED | |
| GOAL | — | SC4: PageDown at end leaves the last line on screen | 14-02 | COVERED | |
| REQ | UIFIX-01 | Paused projects show a pause badge derived from HANDOFF.md | 14-01 | COVERED | |
| REQ | UIFIX-02 | The DRPEV status display has no leading blank | 14-01 | COVERED | |
| REQ | UIFIX-03 | Markdown edit mode activates on key press | 14-02 | COVERED | |
| REQ | UIFIX-04 | PageDown scroll offset is clamped to the end of content | 14-02 | COVERED | |
| RESEARCH | — | — | — | n/a | ROADMAP marks Phase 14 `Research: skip`; no RESEARCH.md exists |
| CONTEXT | D-01 | UIFIX-01 is verify-first, not build-first; do not re-implement; fix only a gap | 14-01 | COVERED | task 1 + task 2 |
| CONTEXT | D-02 | The shipped cyan pause glyph wins; the todo's dim-yellow proposal is stale | 14-01 | COVERED | glyph/color frozen |
| CONTEXT | D-03 | UIFIX-02 fixed at the construction site, not by trimming at the call site | 14-01 | COVERED | task 3 |
| CONTEXT | D-04 | UIFIX-03 resolve which of the three causes it is, then wire the existing editor path | 14-02 | COVERED | root cause pre-located |
| CONTEXT | D-05 | UIFIX-04 clamp in the handler using the render path's existing bound formula | 14-02 | COVERED | task 2 |
| CONTEXT | D-06 | Adjacent unclamped scroll paths in the same handler are in scope | 14-02 | COVERED | 4 sites |
| CONTEXT | D-07 | Each fix lands with a regression test in the module it covers | 14-01, 14-02 | COVERED | |
| CONTEXT | D-08 | The four source todos are marked completed when the phase closes | 14-03 | COVERED | |
| UI-SPEC | rows 1-5 | UI Considerations, `covered` | 14-01 | COVERED | lifted to `must_haves.truths` |
| UI-SPEC | rows 6-8 | UI Considerations, `covered` | 14-02 | COVERED | lifted to `must_haves.truths` |
| UI-SPEC | rows 9-10 | UI Considerations, `backstop` | 14-02 | COVERED | lifted as `verification: backstop` markers |

All rows COVERED. No `⚠ MISSING` rows. Deferred (not gaps, per CONTEXT.md `## Deferred Ideas`):
the 5 pre-existing `--all-targets` clippy lints; any broader scroll/keybinding refactor.

## Flagged Assumption — edge-probe fallback (UIFIX-01)

The deterministic edge probe returned UIFIX-01 as `category: unclassified, status: unresolved`.
Per the fallback protocol an `unclassified` row is never auto-backstopped and never silently
dropped, so it is surfaced here as an explicit assumption rather than an authored truth:

> **A-01 (UIFIX-01, unresolved):** the probe could not classify the shape of "paused projects show
> a pause badge derived from HANDOFF.md" into a boundary/adjacency/encoding category. The assumed
> shape is *boolean-flag-from-file-presence*, which is what tasks 1 and 2 test. If the real
> requirement has a dimension the probe would have named (e.g. an ordering or concurrency
> dimension across multiple HANDOFF files), it is NOT covered by this plan. Review manually.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a paused project shows the pause badge" — one path only</name>
  <reversibility rating="reversible">Extracting the shipped badge-priority rule into a pure fn is a
  one-file, behavior-preserving refactor; undoing it is inlining four match arms back into the row
  builder.</reversibility>
  <files>src/ui/screens/normal.rs</files>
  <read_first>
    - src/ui/screens/normal.rs (whole file — 616 lines; the badge priority lives at lines 409-441 and compact_pipeline at 55-79)
    - src/state_reader/mod.rs (lines 1-230 — ProjectState fields, detect_handoff at 57-99, the paused/pause_context assignment at 218-220)
    - .planning/phases/14-ui-fixes/14-UI-SPEC.md (the `### Badge Priority Rule (UIFIX-01)` table and the `### UIFIX-01` interaction contract)
    - .planning/phases/14-ui-fixes/14-PATTERNS.md (sections 1 and 2 — fixture idioms and the ratatui Line assertion pattern)
  </read_first>
  <action>
    This is the phase tracer: it wires the single thinnest path that touches every layer UIFIX-01
    spans — a real HANDOFF file on disk, the state reader, ProjectState, and the dashboard row's
    badge decision — and proves it end-to-end before any expansion task runs.

    STEP 1 — establish empirically, do NOT re-implement (CONTEXT.md D-01). Read the two source
    ranges above and confirm the shipped behavior: detect_handoff checks HANDOFF.json first, then
    HANDOFF.md, treats a trim-empty file as not paused, and returns a (bool, Option&lt;String&gt;)
    pair that parse_project_state assigns to ProjectState.paused / ProjectState.pause_context. The
    dashboard row then applies a three-tier priority: paused wins, else external_job_waiting, else
    an active Claude session, else no badge. Record what you found.

    STEP 2 — extract the badge decision so it is testable without an AppContext. Add a private
    free function `alias_badge(is_paused: bool, external_job_waiting: bool, has_session: bool)
    -> Option&lt;(&amp;'static str, Color)&gt;` next to `status_color` and `prev_status`. It returns
    the pause glyph U+23F8 followed by one space with Color::Cyan when is_paused; the hourglass
    glyph U+23F3 followed by one space with Color::Yellow when external_job_waiting; the play glyph
    U+25B6 followed by one space with Color::Green when has_session; and None otherwise. Use the
    exact escape forms already present at the current call site so the byte content is unchanged.
    Rewrite the `alias_cell` construction to match on `alias_badge(...)`, building a two-span Line
    (styled glyph + raw alias) on Some and a bare `Line::from(alias.clone())` on None. This is a
    behavior-preserving extraction: the glyphs, the colors, the priority order, and the
    one-badge-per-row rule are all frozen (UI-SPEC Non-Goals). Do not change them.

    STEP 3 — create the first `#[cfg(test)] mod tests` block under `src/ui/` at the bottom of the
    file, following the `src/app.rs:490` template (`use super::*;` plus targeted imports). Add the
    single end-to-end tracer test `test_paused_project_shows_pause_badge_end_to_end`: build a
    `tempfile::TempDir`, create a `.planning` directory inside it, write a non-empty HANDOFF.md
    containing a heading line and a resume-instruction line, call
    `crate::state_reader::parse_project_state` on that `.planning` path, assert the returned state
    is paused, assert its pause context is the resume-instruction line, then feed that same
    `state.paused` value into `alias_badge` with both other flags false and assert the result is
    the cyan pause glyph pair. Assert booleans with the plain truthiness macro and assert the
    Option tuple by equality — never compare a boolean to a literal, which trips the clippy lint
    already flagged in the pre-existing baseline.

    Do NOT modify detect_handoff, parse_project_state, or the badge glyph/color/priority in this
    task. If — and only if — the tracer test fails against the unmodified code, fix the narrowest
    gap it exposes and say so explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test --lib ui::screens::normal::tests::test_paused_project_shows_pause_badge_end_to_end</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib ui::screens::normal::tests::test_paused_project_shows_pause_badge_end_to_end` exits 0
    - `grep -c 'fn alias_badge' src/ui/screens/normal.rs` outputs 1
    - `grep -c 'alias_badge(' src/ui/screens/normal.rs` outputs at least 2 (definition plus the row call site)
    - `grep -c 'cfg(test)' src/ui/screens/normal.rs` outputs 1
    - `cargo build` exits 0 and `cargo clippy -- -D warnings` exits 0
    - The SUMMARY records whether the tracer passed against unmodified shipped code (expected) or exposed a gap, and — if it passed — states that the 2026-03-27 todo was stale
  </acceptance_criteria>
  <done>A non-empty HANDOFF.md written to a temp `.planning/` directory flows through the real state reader into a cyan pause badge decision, proven by one committed test, with no change to the shipped detection or badge vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand UIFIX-01 to the full eight-row HANDOFF contract</name>
  <files>src/state_reader/mod.rs, src/ui/screens/normal.rs</files>
  <read_first>
    - src/state_reader/mod.rs (lines 255-300 — the existing `mod tests` block and its `make_planning` fixture helper, which is reused verbatim)
    - src/state_reader/mod.rs (lines 52-99 — detect_handoff, the function under test)
    - .planning/phases/14-ui-fixes/14-UI-SPEC.md (the eight-row `### UIFIX-01` state table)
    - .planning/phases/14-ui-fixes/14-PATTERNS.md (section 1 — the `test_async_jobs_sets_external_job_waiting` shape to copy, and the local `test_<subject>_<expected>` naming convention)
  </read_first>
  <behavior>
    - HANDOFF.md present and non-empty after trim → paused true, pause context is the first non-empty non-heading line
    - HANDOFF.json present and non-empty with a valid `next_action` string → paused true, pause context is that string
    - HANDOFF.md present but whitespace-only → paused false, pause context None
    - No HANDOFF file at all → paused false, pause context None
    - HANDOFF.json non-empty but unparseable → paused true, pause context None
    - Paused plus an active session → the pause badge, session glyph suppressed
    - Paused plus an async job waiting → the pause badge, hourglass suppressed
    - Async job waiting and not paused → the hourglass badge
    - Nothing active → no badge at all
  </behavior>
  <action>
    Expand outward from the proven tracer path. Two files, no production-code changes expected.

    In the state reader's existing `#[cfg(test)] mod tests` block (the one that already defines
    `make_planning`), add five tests that reuse that helper verbatim — do not write a second
    fixture helper and do not open a second test module (the module deliberately sits above
    `count_backlog_items`, so append inside it). Name them
    `test_handoff_md_non_empty_sets_paused`, `test_handoff_json_non_empty_sets_paused_with_context`,
    `test_handoff_md_whitespace_only_is_not_paused`, `test_no_handoff_file_is_not_paused`, and
    `test_handoff_json_invalid_is_paused_without_context`. Each builds a planning tree containing a
    minimal STATE.md plus the relevant HANDOFF fixture, calls the public `parse_project_state` (not
    the private detector) so the detector-to-ProjectState wiring is covered too, and asserts on
    `state.paused` and `state.pause_context`. Use `assert!(state.paused)` and
    `assert!(!state.paused)` — never a boolean equality comparison.

    In the dashboard module's new test block, add four badge-priority tests over `alias_badge`:
    `test_pause_badge_wins_over_session` (paused plus session yields the cyan pause pair),
    `test_pause_badge_wins_over_async_job` (paused plus async job, and paused plus both, each yield
    the cyan pause pair), `test_async_job_badge_when_not_paused` (not paused, async job waiting,
    session active yields the yellow hourglass pair), and `test_no_badge_when_nothing_active` (all
    three flags false yields None). Add `test_badge_is_never_two_glyphs`, which asserts that every
    Some-variant badge string is exactly two characters wide by character count — the glyph plus
    its single trailing space — so the zero-one-many contract is enforced structurally rather than
    by reading the call site.

    These nine tests are expected to pass against unmodified production code. If one fails, fix
    only the narrow gap it exposes and leave the glyph, the color, and the priority order alone.
  </action>
  <verify>
    <automated>cargo test --lib state_reader::tests:: &amp;&amp; cargo test --lib ui::screens::normal::tests::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib` exits 0
    - `grep -c 'HANDOFF' src/state_reader/mod.rs` outputs at least 8 (the shipped detector references plus the new fixtures)
    - `grep -c 'fn test_handoff' src/state_reader/mod.rs` outputs 3
    - `grep -c 'fn test_no_handoff_file_is_not_paused' src/state_reader/mod.rs` outputs 1
    - `grep -c 'fn test_pause_badge_wins' src/ui/screens/normal.rs` outputs 2
    - `grep -c 'assert_eq!(.*, true)' src/ui/screens/normal.rs` outputs 0 and the same for `src/state_reader/mod.rs`
    - `cargo clippy -- -D warnings` exits 0
  </acceptance_criteria>
  <done>All eight UI-SPEC UIFIX-01 rows plus the no-badge row have a committed regression test, split between the state reader (file-to-flag) and the dashboard module (flag-to-badge).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove the D-R-P-E-V pad cells at the construction site</name>
  <files>src/ui/screens/normal.rs</files>
  <read_first>
    - src/ui/screens/normal.rs (lines 42-79 — prev_status and compact_pipeline, the construction site; and lines 283-320 and 443-460 — the three width tiers that all consume the same status cell)
    - src/state_reader/disk_status.rs (lines 1-30 — the DiskStatus enum and its PartialOrd derive, which the color rule depends on)
    - .planning/phases/14-ui-fixes/14-UI-SPEC.md (the `### UIFIX-02` property table and the `### Spacing changes in this phase` row)
    - .planning/phases/14-ui-fixes/14-PATTERNS.md (section 2 — the ratatui Line assertion pattern)
  </read_first>
  <behavior>
    - compact_pipeline for any DiskStatus renders exactly `D  R  P  E  V`, 13 cells, first char `D`, last char `V`
    - Inter-stage separator is exactly two spaces; no pad cell before `D` or after `V`
    - Per-stage color is unchanged: Green when status is at or past the stage threshold, Yellow when status is exactly one step below it, DarkGray otherwise
    - DiskStatus::NoDirectory renders all five stages DarkGray; DiskStatus::Empty renders `D` Yellow and the rest DarkGray
  </behavior>
  <action>
    Fix where the string is built, not where it is rendered (CONTEXT.md D-03). In
    `compact_pipeline`, the current mapping wraps every stage label in a format that pads both
    sides, so the assembled cell carries a pad cell before `D` and after `V`. Replace the
    `.map(...).collect()` chain with an explicit loop or fold that pushes, for each stage, a styled
    span whose content is the bare stage letter with no surrounding spaces, and pushes an unstyled
    two-space separator span between consecutive stages only — never before the first stage and
    never after the last. The per-stage color computation (the three-way Green / Yellow / DarkGray
    rule driven by the threshold comparison and `prev_status`) is unchanged; each stage letter must
    remain its own span so per-letter color survives. The return type stays `Line<'static>`, so the
    span content must be owned or a `'static` literal.

    Do NOT touch the Pipeline tab's expanded rendering in the detail screen — its two-cell left
    indent is deliberate panel padding inside a bordered block and is explicitly out of scope
    (UI-SPEC Non-Goals). Do NOT trim at the row-assembly call site.

    Add four tests to the module's test block.
    `test_compact_pipeline_has_no_leading_blank` iterates every DiskStatus variant, concatenates the
    span contents into a String, and asserts the first character is `D` and that the string does not
    start with a whitespace character — assert on the first char, not on a byte index, so a
    multi-byte glyph elsewhere in the row cannot invalidate the check.
    `test_compact_pipeline_exact_cells_and_width` asserts the concatenated content equals the exact
    five-letter double-spaced contract string, that the Line's width is 13, and that the last
    character is `V`.
    `test_compact_pipeline_stage_colors_preserved` uses DiskStatus::Planned and asserts D, R and P
    are Green, E is Yellow and V is DarkGray — locate the stage spans by finding the spans whose
    content is a single stage letter rather than by hard-coded index, so the assertion is
    independent of where separator spans land.
    `test_compact_pipeline_no_workflow_data_still_renders_five_stages` asserts the NoDirectory and
    Empty variants still produce the full 13-cell contract string, with NoDirectory all DarkGray and
    Empty showing D in Yellow.
  </action>
  <verify>
    <automated>cargo test --lib ui::screens::normal::tests::test_compact_pipeline</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib` exits 0
    - `grep -c 'fn test_compact_pipeline' src/ui/screens/normal.rs` outputs 4
    - `grep -c 'format!(" {} ", label)' src/ui/screens/normal.rs` outputs 0
    - A test asserts the rendered Line width equals 13 and the concatenated content equals the five stage letters separated by two spaces each
    - `cargo build` exits 0 and `cargo clippy -- -D warnings` exits 0
  </acceptance_criteria>
  <done>compact_pipeline emits a 13-cell cell starting at `D` and ending at `V`, per-stage colors are unchanged, and four tests pin the contract for every DiskStatus variant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → app | `.planning/HANDOFF.md` and `HANDOFF.json` are read from disk. The files belong to the local user who also runs this TUI; there is no network surface, no authentication, and no multi-tenant input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Information Disclosure | HANDOFF file body rendered on the dashboard row (`alias_badge` / alias cell, `normal.rs`) | low | mitigate | The dashboard cell carries a glyph and the alias only. The extracted `alias_badge` returns a fixed `&'static str` glyph, never file-derived text, so no HANDOFF body can reach the row. Pause context stays in the detail view where the shipped Phase 11 design put it. Enforced by `must_haves.prohibitions`. |
| T-14-05 | Denial of Service | `detect_handoff` reads whole files into memory with `read_to_string` | low | accept | Pre-existing shipped behavior (Phase 11), unchanged by this phase. A HANDOFF file is written by the user's own GSD run and is kilobytes. Re-engineering it to a streaming read would be scope expansion with no threat reduction for a single-user local tool. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This phase adds **zero** dependencies (UI-SPEC `## Registry Safety`). There is no package-manager install task in any Phase 14 plan, so the Package Legitimacy Gate does not apply and no legitimacy checkpoint is required. |

Canon-referral breadcrumbs (not minted as prohibitions, per the prohibition-probe canon rule):
path-traversal on `.planning/` reads is canon — covered by `/gsd-secure-phase`; not minted here.

ASVS level 1, block-on `high`. No `high` or `critical` threat in this plan.
</threat_model>

<verification>
1. `cargo build` exits 0
2. `cargo test` exits 0
3. `cargo clippy -- -D warnings` exits 0 (the project's lib-target gate from CLAUDE.md)
4. `cargo test --lib ui::screens::normal::tests::` runs at least 9 tests, all passing
5. `cargo test --lib state_reader::tests::` includes the five new HANDOFF tests, all passing
6. No new clippy warning originates in either file this plan edits — verified phase-wide in plan 14-03
</verification>

<success_criteria>
- ROADMAP success criterion 1 (non-empty HANDOFF.md shows the pause badge) is proven by a committed end-to-end test
- ROADMAP success criterion 2 (no leading blank at any terminal width) is proven by a committed test asserting the exact 13-cell contract
- All eight UI-SPEC UIFIX-01 rows and all seven UI-SPEC UIFIX-02 properties have a corresponding assertion
- Zero changes to the pause glyph, its color, the badge priority order, or the inter-stage two-cell rhythm
- `src/ui/` gains its first `#[cfg(test)] mod tests`
</success_criteria>

<output>
Create `.planning/phases/14-ui-fixes/14-01-SUMMARY.md` when done. It MUST state explicitly whether
UIFIX-01 was already satisfied by the shipped Phase 11 implementation (making the 2026-03-27 todo
stale) or whether a gap was found and narrowly fixed.
</output>
