---
phase: 14-ui-fixes
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - .planning/todos/pending/2026-03-27-detect-paused-projects-via-handoff-md-badge.md
  - .planning/todos/pending/2026-04-05-fix-leading-blank-in-drpev-status-display.md
  - .planning/todos/pending/2026-04-05-fix-markdown-edit-mode-not-activating-on-key-press.md
  - .planning/todos/pending/2026-04-05-fix-pagedown-scroll-offset-not-clamped-to-content-end.md
  - .planning/todos/completed/2026-03-27-detect-paused-projects-via-handoff-md-badge.md
  - .planning/todos/completed/2026-04-05-fix-leading-blank-in-drpev-status-display.md
  - .planning/todos/completed/2026-04-05-fix-markdown-edit-mode-not-activating-on-key-press.md
  - .planning/todos/completed/2026-04-05-fix-pagedown-scroll-offset-not-clamped-to-content-end.md
autonomous: true
requirements: [UIFIX-01, UIFIX-02, UIFIX-03, UIFIX-04]
tags: [rust, quality-gate, clippy, todo-hygiene]

must_haves:
  truths:
    - "The full project gate passes: build, test, and lib-target clippy with warnings denied all exit 0"
    - "The phase adds no new clippy warning — the `--all-targets` warning count is no higher than the pre-existing baseline recorded in 14-CONTEXT.md, and none of the pre-existing warnings has been touched"
    - "All four ROADMAP success criteria are traceable to a named, passing test or a named source assertion"
    - "The four source todos tagged `resolves_phase: 14` are no longer pending"
  artifacts:
    - path: ".planning/todos/completed/2026-03-27-detect-paused-projects-via-handoff-md-badge.md"
      provides: "UIFIX-01 todo, marked completed by relocation"
    - path: ".planning/todos/completed/2026-04-05-fix-leading-blank-in-drpev-status-display.md"
      provides: "UIFIX-02 todo, marked completed by relocation"
    - path: ".planning/todos/completed/2026-04-05-fix-markdown-edit-mode-not-activating-on-key-press.md"
      provides: "UIFIX-03 todo, marked completed by relocation"
    - path: ".planning/todos/completed/2026-04-05-fix-pagedown-scroll-offset-not-clamped-to-content-end.md"
      provides: "UIFIX-04 todo, marked completed by relocation"
  key_links:
    - from: ".planning/todos/completed/"
      to: ".planning/phases/14-ui-fixes/"
      via: "each relocated todo's `resolves_phase: 14` front-matter key ties it to this phase's SUMMARYs"
      pattern: "resolves_phase: 14"
  prohibitions:
    - statement: "MUST NOT modify or 'fix' the five pre-existing `--all-targets` clippy warnings in the browser, project-creator, and state-reader modules — they are explicitly deferred in 14-CONTEXT.md and silently absorbing them into this phase would misattribute unrelated churn to a display-defect fix"
      category: transparency
---

<objective>
Close Phase 14: prove all four defects are fixed together on one tree, prove the phase added no new
lint debt, and retire the four source todos.

Purpose: plans 14-01 and 14-02 each gate their own files. Nothing yet asserts that the four fixes
coexist, that the clippy baseline did not grow, or that the todos that motivated the phase are
retired.

Output: a green full-project gate, a recorded clippy delta, a traceability table from each ROADMAP
success criterion to a named test, and four todos moved from pending to completed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-fixes/14-CONTEXT.md
@.planning/phases/14-ui-fixes/14-UI-SPEC.md
@.planning/phases/14-ui-fixes/14-01-SUMMARY.md
@.planning/phases/14-ui-fixes/14-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates no new source symbols. It relocates four existing markdown files and produces the
phase SUMMARY. The full roll-up of symbols created by Phase 14 (all of which are new and therefore
excluded from drift verification) is:

| Symbol | File | Plan |
|--------|------|------|
| `alias_badge` | `src/ui/screens/normal.rs` | 14-01 |
| `mod tests` (new module) | `src/ui/screens/normal.rs` | 14-01 |
| `browse_edit_target` | `src/ui/screens/detail.rs` | 14-02 |
| `footer_spans` | `src/ui/screens/detail.rs` | 14-02 |
| `clamp_scroll` | `src/ui/screens/detail.rs` | 14-02 |
| `ViewportMetrics` | `src/ui/screens/detail.rs` | 14-02 |
| `DetailScreen.browser_viewport`, `DetailScreen.archive_viewport` | `src/ui/screens/detail.rs` | 14-02 |
| 10 `test_*` fns in the dashboard module, 5 in the state reader, 13 in the detail screen | see 14-01 / 14-02 | 14-01, 14-02 |

No new files under `src/`. No new Cargo dependencies anywhere in Phase 14.

## No-silent-drop tallies

**Edge-probe fallback:** 5 probe items = 2 authored into `must_haves.truths` (14-01: the UIFIX-02
`empty` and `encoding` rows, both auto-`covered`) + 3 surfaced as flagged assumptions (A-01 in
14-01, A-02 and A-03 in 14-02 — all `unclassified`, therefore left `unresolved` and never
auto-backstopped). 2 + 3 = 5. ✓

**UI-SPEC `## UI Considerations`:** 10 rows = 8 `covered` lifted as plain-string truths (rows 1-5 in
14-01, rows 6-8 in 14-02) + 2 `backstop` lifted as flat-scalar `verification: backstop` markers
(rows 9-10 in 14-02). 8 + 2 = 10. ✓

**Prohibitions:** 3 kept, authored descriptor-less (P1 privacy in 14-01, P2 safety in 14-02, P3
transparency in this plan). 3 canon items breadcrumbed and dropped: path traversal on `.planning/`
reads, command injection via the editor spawn, and generic fairness — all covered by
`/gsd-secure-phase`; not minted here.

<tasks>

<task type="auto">
  <name>Task 1: Full project gate and cross-cutting success-criterion trace</name>
  <files>.planning/phases/14-ui-fixes/14-03-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md (the `## Release Process` gate — `cargo build && cargo test && cargo clippy -- -D warnings` — and the GSD workflow enforcement rules)
    - .planning/phases/14-ui-fixes/14-CONTEXT.md (the `## Existing Code Insights` clippy note: five pre-existing `--all-targets` lints in three named modules, explicitly not this phase's to fix)
    - .planning/phases/14-ui-fixes/14-01-SUMMARY.md (what actually landed, and whether UIFIX-01 turned out stale)
    - .planning/phases/14-ui-fixes/14-02-SUMMARY.md (what actually landed, and which discretionary decisions held)
    - .planning/ROADMAP.md (the `### Phase 14: UI Fixes` success-criteria list, which this task traces against)
  </read_first>
  <action>
    Run the whole project gate on the combined tree, in this order, and record each result:
    the release build, the full test run, and lib-target clippy with warnings denied. All three must
    exit 0. If any fails, fix the failure inside the files Phase 14 already owns; do not widen scope.

    Then measure the lint delta. Run all-targets clippy with the short message format and count the
    warning lines. 14-CONTEXT.md and 14-PATTERNS.md both record five pre-existing warnings, in the
    browser, project-creator and state-reader modules. The observed count must be no higher than
    that recorded baseline. If it is higher, the phase introduced new lints — fix only the new ones,
    in the files Phase 14 edited, and re-run. If it is lower or the file attribution differs from
    the record, note the actual observed value and per-file attribution in the SUMMARY and change
    nothing: the pre-existing warnings are deferred by CONTEXT.md and are out of scope. Paste the
    per-file attribution into the SUMMARY so a reader can see the phase added none.

    Finally build a traceability table in the SUMMARY mapping each of the four ROADMAP success
    criteria to the named test or source assertion that proves it: criterion 1 to the end-to-end
    pause-badge tracer test plus the five state-reader HANDOFF tests; criterion 2 to the four
    compact-pipeline tests; criterion 3 to the seven browse-edit-target tests plus the footer test
    plus the source assertion that the `e` key arm has a Browse branch; criterion 4 to the five
    clamp-scroll tests plus the source assertion that all four handler sites clamp. Any criterion
    that cannot be traced to a named artifact is a gap — record it explicitly rather than asserting
    completion.
  </action>
  <verify>
    <automated>cargo build &amp;&amp; cargo test &amp;&amp; cargo clippy -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0
    - `cargo test` exits 0
    - `cargo clippy -- -D warnings` exits 0
    - `cargo clippy --all-targets --message-format short 2>&amp;1 | grep -c ': warning'` outputs a number no greater than 5, and the SUMMARY records the observed number together with its per-file attribution
    - `cargo test --lib 2>&amp;1 | grep -E '[0-9]+ passed'` shows a higher passing count than the pre-phase tree, and the SUMMARY records both numbers
    - The SUMMARY contains a four-row traceability table, one row per ROADMAP success criterion, each naming at least one passing test or one source assertion
    - The SUMMARY states that none of the pre-existing clippy warnings was modified
  </acceptance_criteria>
  <done>The combined tree passes the project's own gate, the clippy baseline did not grow, and every ROADMAP success criterion is traceable to a named passing artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the four Phase 14 source todos</name>
  <files>.planning/todos/pending/, .planning/todos/completed/</files>
  <read_first>
    - .planning/todos/pending/2026-03-27-detect-paused-projects-via-handoff-md-badge.md
    - .planning/todos/pending/2026-04-05-fix-leading-blank-in-drpev-status-display.md
    - .planning/todos/pending/2026-04-05-fix-markdown-edit-mode-not-activating-on-key-press.md
    - .planning/todos/pending/2026-04-05-fix-pagedown-scroll-offset-not-clamped-to-content-end.md
    - .planning/todos/completed/2026-04-05-fix-folder-appears-empty-after-returning-from-markdown-view.md (the one already-completed todo — it establishes the convention: the file is relocated with its body unchanged and no status key is appended)
  </read_first>
  <action>
    CONTEXT.md `## Specific Ideas` requires the four source todos tagged `resolves_phase: 14` to be
    marked completed when the phase closes. Follow the convention the single existing completed todo
    establishes: relocate each file from the pending directory to the completed directory with its
    body and front matter unchanged. Use `git mv` for each of the four so the move is recorded as a
    rename rather than a delete-plus-add. Do not edit the file contents; in particular do not rewrite
    the 2026-03-27 todo's stale `||` dim-yellow proposal — the record of what was originally proposed
    is worth keeping, and 14-01's SUMMARY already documents why the shipped cyan glyph superseded it.

    Note in the phase SUMMARY that the 2026-03-27 todo's proposed solution was superseded before this
    phase began, and cite 14-01's finding about whether the underlying behavior was already shipped.
  </action>
  <verify>
    <automated>ls .planning/todos/completed/ | grep -c -e handoff-md-badge -e leading-blank-in-drpev -e markdown-edit-mode -e pagedown-scroll-offset</automated>
  </verify>
  <acceptance_criteria>
    - `ls .planning/todos/completed/ | grep -c -e handoff-md-badge -e leading-blank-in-drpev -e markdown-edit-mode -e pagedown-scroll-offset` outputs 4
    - `test ! -e .planning/todos/pending/2026-03-27-detect-paused-projects-via-handoff-md-badge.md` succeeds, and the same holds for the other three Phase 14 todo filenames
    - `git status --porcelain .planning/todos/` shows four rename entries and no content modification
    - `grep -c 'resolves_phase: 14' .planning/todos/completed/*.md | grep -c ':1'` outputs 4 — each relocated todo kept its phase tag
    - The phase SUMMARY records that the 2026-03-27 todo's proposed badge design was superseded by the shipped implementation
  </acceptance_criteria>
  <done>All four `resolves_phase: 14` todos are recorded as completed, with contents unmodified and the moves tracked as git renames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repository | This plan runs the project's own build/test/lint toolchain and relocates four markdown files inside `.planning/`. No new code, no runtime surface, no external input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Repudiation | Silent absorption of unrelated lint churn into a display-defect phase | low | mitigate | The lint gate compares against the baseline recorded in 14-CONTEXT.md and forbids touching the five pre-existing warnings; the observed count and per-file attribution are written into the SUMMARY, so the phase's lint footprint is auditable rather than asserted. Enforced by `must_haves.prohibitions`. |
| T-14-08 | Tampering | Todo relocation losing the original problem statements | low | mitigate | Files are moved with `git mv` and their contents are explicitly not edited, so the original proposals — including the superseded 2026-03-27 badge design — remain in history and in the completed directory. |
| T-14-SC | Tampering | npm/pip/cargo installs | low | accept | This phase adds **zero** dependencies (UI-SPEC `## Registry Safety`). There is no package-manager install task in any Phase 14 plan, so the Package Legitimacy Gate does not apply and no legitimacy checkpoint is required. |

ASVS level 1, block-on `high`. No `high` or `critical` threat in this plan.
</threat_model>

<verification>
1. `cargo build` exits 0
2. `cargo test` exits 0
3. `cargo clippy -- -D warnings` exits 0
4. The `--all-targets` clippy warning count is no greater than the 5 recorded in 14-CONTEXT.md, with per-file attribution written into the SUMMARY
5. `ls .planning/todos/pending/` no longer lists any of the four Phase 14 todos
6. The SUMMARY carries a four-row success-criterion traceability table
</verification>

<success_criteria>
- All four ROADMAP success criteria hold simultaneously on one tree, each traceable to a named passing test or source assertion
- The project's own release gate (`cargo build && cargo test && cargo clippy -- -D warnings`) is green
- The phase added zero clippy warnings and modified zero pre-existing ones
- The four source todos that motivated Phase 14 are recorded as completed
- Zero new Cargo dependencies across the whole phase
</success_criteria>

<output>
Create `.planning/phases/14-ui-fixes/14-03-SUMMARY.md` when done. It MUST contain (a) the four-row
success-criterion traceability table, (b) the observed `--all-targets` clippy warning count with
per-file attribution and an explicit statement that no pre-existing warning was touched, and (c) the
finding from 14-01 about whether the 2026-03-27 pause-badge todo was stale.
</output>