---
phase: 24-roadmap-tab-redesign-and-detail-tab-consolidation
plan: 06
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04, 24-05]
files_modified:
  - src/ui/screens/detail.rs
  - src/ui/screens/render_escape_guard.rs
  - src/ui/roadmap_graph.rs
autonomous: true
requirements: [D-A01, D-A02, D-A03, D-A08, D-A09, D-A12, D-A14, D-A15, D-B02, D-B08, D-B12]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Roadmap tab's default view is the master/detail list (`RoadmapView` over `roadmap_model_for`) with the cursor 24-05 stores; `v` still toggles the old box view (D-A01, D-A10)."
    - "SC-1 on real data: in the rendered list pane, daily-vow's phase `20` occupies exactly one row, sentriq's phase `9` occupies exactly one row, no rendered cell sequence of the old reference-row arrow appears anywhere on screen, and when `23` (daily-vow) or `11` (sentriq) is selected the implied dependency's row carries the dim `·` and the detail pane says `(implied via 21)` / `(implied via 10)` (D-A08, D-A14)."
    - "Each milestone label appears on exactly one list row (its band) — never appended to phase rows (D-A07, D-A08, D-A14)."
    - "ttbook's build phases 14-18 appear as ordinary selectable list rows under bands M3, M4 and M5, and no band is named after a build phase (D-A12, SC-4)."
    - "sentriq's phase 12 detail pane says `nothing declared` and `can run any time` instead of `Nothing in this milestone` (D-A14)."
    - "The Roadmap tab's top carries the former PhaseList header: a summary line `{alias} · {milestone} · phase {n} {status} · {k} of {n} phases done`, `Path:`, and — only when present — the unreadable-state, recovered-state, Paused and change-tracker lines; the selected phase's disk `[stage]` badge is in the detail pane's status line; no state reads `No state data available for this project.` and no phases reads `No roadmap data available` (D-B02, D-B08)."
    - "Status glyphs come from `ProjectState::phase_marker` (disk-inferred done/current), so a phase `Complete` on disk with a stale `- [ ]` checkbox draws `●`, not `○`/`◌` — the folded todo's disagreement cannot recur (D-B12)."
    - "The layout works at 80×24 (detail stacked below) and 120×30 (side by side) with no horizontal scrolling (D-A09, D-A13, SC-3)."
    - "The escape-guard probe covers the new text paths — goal, planned build-phase name, `milestone_name` synthetic band — and the legacy left-to-right graph code is deleted, leaving `roadmap_graph.rs`'s kept dependency/cycle/wave logic plus the list model (D-A15)."
  artifacts:
    - path: "src/ui/screens/detail.rs"
      provides: "render_roadmap drawing RoadmapView with the compact header, roadmap_summary_line, fixture render tests, render_detail_to_text_at and roadmap_list_pane test helpers"
      contains: "RoadmapView"
    - path: "src/ui/screens/render_escape_guard.rs"
      provides: "hostile phase_goals / planned_phases / milestone_name, rewritten RoadmapViz arrival reason, planned-phase cursor sub-state"
      contains: "phase_goals"
    - path: "src/ui/roadmap_graph.rs"
      provides: "list model only; legacy layout, RoadmapGraphWidget and their tests removed"
      contains: "pub fn layout_list"
  key_links:
    - from: "src/ui/screens/detail.rs render_roadmap"
      to: "src/ui/roadmap_view.rs RoadmapView"
      via: "render_stateful_widget with roadmap_list_offset / roadmap_list_viewport write-back"
      pattern: "roadmap_view::RoadmapView"
    - from: "src/ui/screens/detail.rs render_roadmap"
      to: "src/ui/screens/detail.rs roadmap_model_for"
      via: "the same adapter the keys use"
      pattern: "roadmap_model_for\\("
  prohibitions:
    - "No file I/O in `render_roadmap` (in-memory state only)."
    - "No per-row milestone label and no row type that repeats a phase id."
---

<objective>
Draw the new Roadmap: wire `RoadmapView` into `render_roadmap` with the cursor and folds 24-05
stores, move the former PhaseList header content to the top of the tab, prove the reported
defects gone on the three real fixtures at 80 and 120 columns, extend the escape-guard probe to
the new text paths, and delete the legacy left-to-right layout.

Purpose: this is where success criteria 1-4 become visible and pinned end to end (parser →
adapter → model → widget → screen).

Dependency: `depends_on: [24-02, 24-03, 24-04, 24-05]`. **Semantic** on 24-04 (renders
`RoadmapView`) and 24-05 (uses `roadmap_model_for`, the cursor cells and fixture helper).
**Shared files**: `src/ui/screens/detail.rs` (24-03, 24-05), `src/ui/screens/render_escape_guard.rs`
(24-03), `src/ui/roadmap_graph.rs` (24-02 — this plan deletes its legacy half).

Output: the new Roadmap tab, end-to-end fixture tests, a stronger escape probe, a smaller
`roadmap_graph.rs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/MOCKUPS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-03-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-04-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-05-SUMMARY.md
</context>

<interfaces>
From 24-04 (`crate::ui::roadmap_view`): `RoadmapView { model, cursor }`, `RoadmapViewState { offset, list_rows }`, `panes(area) -> (Rect, Rect)`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`.
From 24-05 (`detail.rs` / `mod.rs`): `roadmap_model_for(state, cache, show_badges)`, `ProjectViewCache.roadmap_cursor`, `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`, test helper `fixture_state(name)`.
Created here: `fn roadmap_summary_line(alias, state, model) -> Line<'static>`; test helpers `render_detail_to_text_at(screen, ctx, width, height) -> String` and `roadmap_list_pane(text: &str) -> Vec<String>` (the rows inside the ` Roadmap ` block's borders, located by its title and its bottom-left corner; at ≥ 100 columns only the columns left of the ` Phase ` block).
</interfaces>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 24 plan; newly-created names are not drift).

**New files:** `tests/fixtures/roadmaps/README.md`, `tests/fixtures/roadmaps/{daily-vow,sentriq,ttbook}-{ROADMAP,STATE}.md` (24-01), `src/ui/roadmap_view.rs` (24-04).
**Parser functions (24-01):** `parse_phase_goals`, `parse_planned_build_phases`, `shipped_milestones`, `declared_phase_count`; `ProjectState.{phase_goals, planned_phases, milestone_name}`.
**Engine types and functions (24-02):** `ListNode`, `BandInput`, `ListInput`, `RoadmapModel`, `ListRow`, `PhaseFacts`, `BandFacts`, `PhaseStatus`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, `layout_list`, `is_folded`, `lane_text`, the `RoadmapModel` navigation methods, the glyph consts.
**Widget (24-04):** `RoadmapView`, `RoadmapViewState`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`, `LANE_CAP_NARROW`, `LANE_CAP_WIDE`, `LANE_OVERFLOW`, `panes`, `keep_visible`.
**View state (24-05):** `ProjectViewCache.{roadmap_cursor, roadmap_fold_toggles, roadmap_edge_walk}`; `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`; `roadmap_model_for`.
**Detail screen (24-06/24-07):** `roadmap_summary_line`, `switch_to_sub_view`, `docs_sub_tab_strip`; test helpers `render_detail_to_text_at`, `roadmap_list_pane`.
**Keys:** Roadmap `j`/`k`/`Up`/`Down`/`PageUp`/`PageDown`/`g`/`G`/`h`/`l`/`[`/`]`/`Space`/`Enter` (24-05); Docs `m` (24-07).
**Constants changed:** `TAB_COUNT` 11 → 10 → 9, `DRIVER_TAB_INDEX` 10 → 9 → 8, `TAB_BAR_*` re-derived, label arrays reordered (24-03, 24-07).
**Removed:** `DetailSubView` variant `PhaseList`, `render_phase_list` (24-03); the old graph layout and `RoadmapGraphWidget` (24-06); stale help row for `r` (24-05); detail-view digits `9`/`0` (24-03/24-07).
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: ─► -->

<tasks>

<task type="tracer">
  <name>Task 1: The new Roadmap on screen — header, RoadmapView, cursor; daily-vow shows 20 once</name>
  <reversibility rating="reversible">Render-path change; the box view and the model are untouched, and the header layout is discretion.</reversibility>
  <files>src/ui/screens/detail.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — re-read after 24-05 merged: `render_roadmap` (~3795-3932: header block, box-view branch, legacy graph branch), `unreadable_state_line` (~90), `recovered_state_line` (~124), `classify_status` / `status_color`, `shown` (~78), the `DetailScreen` cells from 24-05, `roadmap_model_for`, `fixture_state`, `render_detail_to_text` (~11631), and the tests `roadmap_graph_tab_draws_the_graph_by_default_and_v_toggles_the_box_list` / `roadmap_graph_tab_v_is_inert_on_other_tabs` / `roadmap_graph_tab_footer_advertises_v` (~14447-14521).
    - `src/ui/roadmap_view.rs` — `RoadmapView`, `RoadmapViewState`, `panes`.
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/MOCKUPS.md` — the header line of Mockups A/B/C.
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md` — § Pattern 5 (80×24 vertical budget, header line).
  </read_first>
  <action>
**Header (D-B02, D-B08).** Replace the bordered header block with borderless lines (the list and
detail blocks carry their own borders, and 80×24 has no rows to spare): line 1 is
`roadmap_summary_line` — `{alias} · {milestone label} · phase {active phase} {status} · {k} of {n}
phases done`, where the milestone label is the active roadmap milestone's `label.shown()`, else
`{state.milestone} {milestone_name.shown()}` (sentriq), else omitted; `{status}` is
`shown(&state.status)` in `status_color(classify_status(..))`; `k`/`n` are `model.done`/`model.total`.
Line 2 is `Path: {shown(path)}`. Then, only when present, the unreadable-state line, the
recovered-state line, the Paused line (existing wording) and the change-tracker banner (existing
wording) — these are the PhaseList-only items D-B08 lists. Without a project state draw
`No state data available for this project.` (D-B08 wording).

**List + detail.** In the non-box branch, build `roadmap_model_for(state,
ctx.view_cache.get(alias), ctx.config.preferences.gsd_integration)`, resolve the stored cursor
with `model.resolve_cursor(..)` (do not write it back from render — render takes `&self`), render
`RoadmapView { model: &model, cursor }` with `frame.render_stateful_widget` and a
`RoadmapViewState` seeded from `roadmap_list_offset`, then store the updated `offset` and
`list_rows` into `roadmap_list_offset` / `roadmap_list_viewport`. Remove the legacy graph call
and its `generic_viewport` write in this branch (the box view keeps its own).

**Tests.** Add `render_detail_to_text_at` (the existing 120×30 helper becomes a call to it) and
`roadmap_list_pane`. Rewrite `roadmap_graph_tab_draws_the_graph_by_default_and_v_toggles_the_box_list`
to assert the default render contains the ` Roadmap ` block and `Start now:`, and that `v` switches
to the box view and back. New: `daily_vow_roadmap_renders_phase_20_once_at_120` — `fixture_state("daily-vow")`,
RoadmapViz, cursor unset: in `roadmap_list_pane` exactly one row has `20` in its number column
(match the token with a word boundary), exactly one row contains `v1.5 Closing the Loop`, one row
contains `5 milestones · 17 phases shipped`; the whole screen contains no `─►`; the header line
contains `v1.5 Closing the Loop · phase 23` and `5 of 6 phases done`; and with the cursor on `23`
the row for `20` contains `·` and the detail pane contains `(implied via 21)`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::daily_vow_roadmap_renders_phase_20_once_at_120</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed"</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::roadmap_graph_tab</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - Both commands pass and `daily_vow_roadmap_renders_phase_20_once_at_120` appears as `ok`.
    - `render_roadmap` no longer references `layout_for_state`, `split_areas` or `RoadmapGraphWidget` (`rtk proxy git grep -n -F -e 'layout_for_state' -e 'split_areas' -e 'RoadmapGraphWidget' -- src/ui/screens/detail.rs` prints nothing).
    - `render_roadmap` contains no `std::fs` or `read_to_string` call.
  </acceptance_criteria>
  <done>The Roadmap tab draws the header, the lane list and the detail pane from live state, and daily-vow's 20 appears once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The defects stay gone on all three fixtures at 80 and 120 columns; preserved PhaseList content</name>
  <files>src/ui/screens/detail.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — Task 1's render and helpers; `fixture_state`; the Paused / banner / unreadable / recovered tests that already exist for the old PhaseList header (grep `Paused (HANDOFF file present)` and `latest_change`) — port any that rendered `PhaseList` to `RoadmapViz`.
    - `src/state_reader/disk_status.rs` — `DiskInference` / `DiskStatus` fields for building a disk status in a test.
    - `.planning/todos/pending/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md` — the folded todo's failing shape (disk `Complete`, checkbox `[ ]`).
  </read_first>
  <behavior>
    - sentriq at 80×24, cursor unset: the list block sits above the ` Phase ` block (stacked); exactly one list row has `9` in its number column; no `─►` on screen; the band row `v0.12 Actuation Routines` appears once; with the cursor on `12` the detail pane contains `nothing declared` and `can run any time`; with the cursor on `11` the `9` row contains `·` and the detail pane contains `(implied via 10)`.
    - sentriq at 120×30 and daily-vow at 80×24: the same single-row and single-band assertions hold.
    - ttbook at 120×30: list rows exist for 14, 15, 16, 17 and 18, each once; band rows whose short id is `M3`, `M4`, `M5` each appear once; no list row contains `Build phase 14` as a band; after `G` the detail block's title reads `Phase 18` and its status line contains `planned (not a GSD phase)`.
    - A phase whose disk inference is `Complete` while its ROADMAP checkbox is unticked draws `●` in its list row (D-B12); a phase with disk status `Partial` 2/3 shows `[Executing 2/3]` in the detail pane when selected (D-B08).
    - A state with no phases renders `No roadmap data available`; no state renders `No state data available for this project.`; a paused state renders the Paused line; a change-tracker event renders its banner.
  </behavior>
  <action>
Write the tests for the behaviours: `sentriq_roadmap_at_80_columns_is_stacked_and_shows_9_once`,
`sentriq_phase_12_explains_it_has_no_deps`, `sentriq_implied_dep_is_a_dim_marker_not_a_row`,
`sentriq_and_daily_vow_hold_at_both_widths`, `ttbook_build_phases_are_list_rows_not_bands`,
`a_disk_complete_phase_with_an_unticked_box_draws_done`, `the_selected_phase_shows_its_stage_badge`,
`roadmap_empty_states_explain_themselves`, `roadmap_header_keeps_paused_and_change_banner`.
Fix any render detail the tests expose in `render_roadmap` (header height accounting at 24 rows,
the stacked split). Port any pre-existing test that exercised the Paused line, change banner,
unreadable or recovered lines through the removed PhaseList tab so it now targets `RoadmapViz`
(record them in the SUMMARY).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - The nine tests named in this task appear as `ok`.
    - `sentriq_roadmap_at_80_columns_is_stacked_and_shows_9_once` renders at exactly 80×24.
  </acceptance_criteria>
  <done>SC-1 through SC-4 hold on sanitised real data at both breakpoints, and everything the PhaseList tab uniquely showed is on the Roadmap.</done>
</task>

<task type="auto">
  <name>Task 3: Escape-guard coverage for the new text paths, then delete the legacy graph layout</name>
  <files>src/ui/screens/render_escape_guard.rs, src/ui/roadmap_graph.rs, src/ui/screens/detail.rs</files>
  <read_first>
    - `src/ui/screens/render_escape_guard.rs` — `hostile_project_state` (~859-948, the `milestones` addition as the template), `DETAIL_TAB_ARRIVAL`'s `"RoadmapViz tab"` row, `DETAIL_SUB_STATES` (~1651+, the `"RoadmapViz tab, box view"` arrange closure), `PROBE_WIDTH`/`PROBE_HEIGHT` (~2016), and the arrival assertion (~1521-1553). Pitfall 4 in RESEARCH.
    - `src/ui/roadmap_graph.rs` — the legacy half: `GraphNode`, `MilestoneTag`, `HeaderItem`, `Segment`, `GraphRow`, `CellSpan`, `GraphLayout`, the placement machinery (`Group` … `paint_reference_row`), `layout_graph`, `milestone_decorations`, `milestone_tags`, `render_text`, `split_areas`, `nodes_from_phases`, `phase_markers`, `layout_for_state`, `RoadmapGraphWidget`, and the legacy tests (`roadmap_graph_o*`, `roadmap_graph_wr01_*`, `roadmap_graph_escapes_*`, `roadmap_graph_*_tiny_sizes`, `roadmap_graph_widget_*`, `roadmap_graph_auto_offset_*`).
  </read_first>
  <action>
**Probe.** In `hostile_project_state`, populate the three 24-01 fields with the identity:
`phase_goals` = `{phase_key("1") → Untrusted(identity)}`, one `planned_phases` entry (a build
phase with an id not used elsewhere, named `identity`, with a goal keyed to it too), and
`milestone_name: Some(Untrusted(identity))`. Rewrite the `"RoadmapViz tab"` arrival reason to name
what the list and detail pane now draw (phase row names — truncatable — the band label, and the
selected phase's name and goal whole in the detail pane). Add a `DETAIL_SUB_STATES` entry
`"RoadmapViz tab, cursor on the planned phase"` whose arrange closure stores
`roadmap_cursor = Some(CursorTarget::Phase(<its key>))`, with a matching arrival row. At the
probe's 200 columns the detail pane is side by side; confirm the identity arrives WHOLE there — if
its escaped form is wider than the pane's name line, make the name line wrap rather than truncate
(record the choice). Update the state-count prose. Do not mark arrival that does not happen.

**Delete the legacy layout** from `roadmap_graph.rs` — every item listed in read_first and its
tests — keeping `esc`, `width_of`, `ParentLists`, `ExternalLists`, `resolve_deps`, `break_cycles`,
`MAX_CYCLES_SHOWN`, `cycle_note`, `longest_path_layers` and the whole list model (D-A15). Rewrite
the module doc for the single remaining half. Remove now-unused imports in `roadmap_graph.rs` and
`detail.rs`. The 24-02 list tests already cover the legacy O-topologies; name in the SUMMARY which
new test covers each deleted legacy test.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::render_escape_guard</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer `test result:` lines than the 49-suite baseline</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rtk proxy git grep -n -F -e 'RoadmapGraphWidget' -e 'fn layout_graph' -e 'fn layout_for_state' -e 'fn split_areas' -e 'fn milestone_decorations' -e 'Segment::Reference' -- src` prints nothing.
    - `rtk proxy git grep -n 'phase_goals' -- src/ui/screens/render_escape_guard.rs` prints at least one line.
    - The five `app::tests` archive regression tests appear as `ok` in the full run.
  </acceptance_criteria>
  <done>The probe proves the new text paths escaped, and only the list model remains in `roadmap_graph.rs`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProjectState (third-party ROADMAP/STATE text) → Roadmap tab cells | goal, planned phase names, synthetic band label, status, path, pause context |
| render loop | must stay I/O-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-19 | Tampering / Spoofing | goal, planned-phase name, `milestone_name` reaching cells | high | mitigate | hostile identity added to all three fields; RoadmapViz arrival and a planned-cursor sub-state asserted by `render_escape_guard`; header strings through `shown()` / `Untrusted::shown()` |
| T-24-20 | Denial of Service | file I/O or heavy compute on the render thread | medium | mitigate | `roadmap_model_for` reads in-memory state only; `render_roadmap` has no `std::fs` call (acceptance grep); model cost O(n·e) |
| T-24-21 | Repudiation (silent regression) | the reported defects returning later | medium | mitigate | fixture render tests pin 20-once, 9-once, no reference arrow, one band label, build phases as rows |
| T-24-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `rtk proxy cargo test --no-fail-fast`: only the known git-version witness fails; 49 suites ran; the five archive regression tests pass unchanged.
- `rtk proxy cargo clippy -- -D warnings` clean.
- The legacy-layout git-grep prints nothing.
</verification>

<success_criteria>
- SC-1: every phase once, each milestone label once, implied deps as a dim marker, both reported defects gone on real data.
- SC-2 (visual half): cursor, Goal/Needs/Unblocks/Parallel and the no-deps explanation render.
- SC-3: 80×24 stacked, 120×30 side by side, no horizontal scroll.
- SC-4: ttbook build phases render as selectable rows.
- SC-5 (header half): PhaseList's unique content lives on the Roadmap.
</success_criteria>

<output>
Create `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-06-SUMMARY.md` when done
</output>
