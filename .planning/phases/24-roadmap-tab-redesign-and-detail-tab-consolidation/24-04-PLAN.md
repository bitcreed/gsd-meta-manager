---
phase: 24-roadmap-tab-redesign-and-detail-tab-consolidation
plan: 04
type: execute
wave: 2
depends_on: [24-02]
files_modified:
  - src/ui/roadmap_view.rs
  - src/ui/mod.rs
autonomous: true
requirements: [D-A01, D-A02, D-A04, D-A05, D-A06, D-A07, D-A09, D-A13, D-A14]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`RoadmapView` draws the list pane: a `Start now:` line of active (◉) and ready (○) phases joined by `║` (D-A06), a `lanes  #  Phase … plans  wave` header, then one row per visible model row — status glyph in its lane, marker column, phase number, full name truncated with `…`, plans done/total (`—` when unknown), `W<n>` (D-A01)."
    - "Glyph colours follow D-A04 (● dim, ◉ yellow bold, ○ green, ◌ default) and done rows are dimmed; the selected row is reverse video with `▶`, the selection's needs carry `↑` (cyan), what it unblocks `↓` (magenta), its implied deps a dim `·` (D-A05, D-A08)."
    - "The detail pane shows the selected phase's name, milestone and `wave W of M`, status (glyph, word, plans, stage badge), Goal (wrapped), Needs (with `(implied via N)` rows and external deps), Unblocks (with the other band's short id when different, or `nothing (last in <band>)`), and Parallel = the other phases in the same wave (`none in wave W` when empty) (D-A02)."
    - "A phase with no declared deps says `nothing declared` under Needs and its Parallel line says it can run any time — `(no edge either way; can run any time)` when nothing depends on it either (Mockup C wording), `(no deps; can run any time)` otherwise — instead of the old `Nothing in this milestone` (D-A14). [INFERRED — audit: the second wording]"
    - "At ≥ 100 columns (`ROADMAP_SIDE_BY_SIDE_MIN_COLS`) the detail pane sits beside the list; below that it stacks under the list; 80 columns render without horizontal scrolling — the name column absorbs width and lanes beyond the cap collapse into one `┆` column (D-A09, D-A13, SC-3)."
    - "Milestone band rows (`▾ label done/total ━━━`) and the shipped summary (`▸ v1.0 … v1.4   5 milestones · 17 phases shipped`) are drawn once each; a band or summary under the cursor gets its own detail pane (D-A07)."
    - "Every string the widget draws comes from the escaped model or a static literal; truncation is char-based on escaped text; the widget never panics or draws outside its `Rect` at any size."
  artifacts:
    - path: "src/ui/roadmap_view.rs"
      provides: "RoadmapView StatefulWidget (list + detail pane, side-by-side / stacked), RoadmapViewState, panes, keep_visible, lane cap"
      contains: "impl StatefulWidget for RoadmapView"
    - path: "src/ui/mod.rs"
      provides: "`pub mod roadmap_view;` and the escape test ported from the legacy graph widget to RoadmapView"
      contains: "pub mod roadmap_view;"
  key_links:
    - from: "src/ui/roadmap_view.rs"
      to: "src/ui/roadmap_graph.rs"
      via: "renders RoadmapModel rows and PhaseFacts; reuses the glyph consts"
      pattern: "roadmap_graph::"
  prohibitions:
    - "No horizontal scroll state or key is introduced (D-A13)."
    - "No `str::len` / byte slicing on displayed text."
    - "No mouse handling (D-A11)."
---

<objective>
Build the Roadmap tab's master/detail widget: a phase list with a narrow git-log lane column and a
detail pane for the selected row, side-by-side at 100+ columns and stacked below 100, drawing only
the escaped `RoadmapModel` from plan 24-02.

Purpose: success criteria 2 and 3 are visual — cursor, Goal/Needs/Unblocks/Parallel, no-deps
explanation, 80-column stacking — and a pure widget over the model can pin them with `TestBackend`
buffers before any screen wiring exists.

Dependency: `depends_on: [24-02]` — **semantic**: this widget renders the `RoadmapModel`,
`ListRow`, `PhaseFacts` and glyph consts that plan defines. Files (`src/ui/roadmap_view.rs`,
`src/ui/mod.rs`) are disjoint from its wave-2 sibling 24-05.

Output: `src/ui/roadmap_view.rs` and the ported escape test in `src/ui/mod.rs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/MOCKUPS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-02-SUMMARY.md
</context>

<interfaces>
Consumed from plan 24-02 (`src/ui/roadmap_graph.rs`; if 24-02's SUMMARY records a deviation, the
code wins): `RoadmapModel { rows, phases, bands, start_now, max_wave, done, total, notes }`,
`ListRow::{ShippedSummary, Band, Connector, Phase { node, lane, lanes }}`, `PhaseFacts`,
`BandFacts`, `PhaseStatus`, `CursorTarget`, `BandKey`, `layout_list`, `lane_text`, and the glyph
consts `GLYPH_*`, `MARK_*`, `BAND_*`, `PARALLEL_SEP`.

Created here:
- `pub const ROADMAP_SIDE_BY_SIDE_MIN_COLS: u16 = 100;` `pub const LANE_CAP_NARROW: usize = 4;` `pub const LANE_CAP_WIDE: usize = 6;` `pub const LANE_OVERFLOW: &str = "\u{2506}";` (┆)
- `pub struct RoadmapView<'a> { pub model: &'a RoadmapModel, pub cursor: Option<&'a CursorTarget> }`
- `#[derive(Debug, Default, Clone, Copy)] pub struct RoadmapViewState { pub offset: usize, pub list_rows: u16 }` — `offset` in/out (first visible model row), `list_rows` out (visible model rows, for PageUp/PageDown).
- `impl StatefulWidget for RoadmapView<'_> { type State = RoadmapViewState; }`
- `pub fn panes(area: Rect) -> (Rect, Rect)` — (list, detail) for that area per the breakpoint.
- `pub fn keep_visible(offset: usize, row: usize, visible: usize) -> usize`
</interfaces>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 24 plan; newly-created names are not drift).

**New files:** `tests/fixtures/roadmaps/README.md`, `tests/fixtures/roadmaps/{daily-vow,sentriq,ttbook}-{ROADMAP,STATE}.md` (24-01), `src/ui/roadmap_view.rs` (24-04).
**Parser functions (24-01):** `parse_phase_goals`, `parse_planned_build_phases`, `shipped_milestones`, `declared_phase_count`; `ProjectState.{phase_goals, planned_phases, milestone_name}`.
**Engine types and functions (24-02):** `ListNode`, `BandInput`, `ListInput`, `RoadmapModel`, `ListRow`, `PhaseFacts`, `BandFacts`, `PhaseStatus`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, `layout_list`, `is_folded`, `lane_text`, the `RoadmapModel` navigation methods, the glyph consts.
**Widget (24-04):** `RoadmapView`, `RoadmapViewState`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`, `LANE_CAP_NARROW`, `LANE_CAP_WIDE`, `LANE_OVERFLOW`, `panes`, `keep_visible`.
**View state (24-05):** `ProjectViewCache.{roadmap_cursor, roadmap_fold_toggles, roadmap_edge_walk}`; `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`; `roadmap_model_for`.
**Detail screen (24-06/24-07):** `roadmap_summary_line`, `switch_to_sub_view`, `docs_sub_tab_strip`; test helpers `render_detail_to_text_at`, `roadmap_list_pane`.
**Keys:** Roadmap `j`/`k`/`Up`/`Down`/`PageUp`/`PageDown`/`g`/`G`/`h`/`l`/`[`/`]`/`Space`/`Enter` (24-05); Docs `m` (24-07).
**Constants changed:** `TAB_COUNT` 11 → 10 → 9, `DRIVER_TAB_INDEX` 10 → 9 → 8, `TAB_BAR_*` re-derived, label arrays reordered (24-03, 24-07).
**Removed:** `DetailSubView` variant `PhaseList`, `render_phase_list` (24-03); the old graph layout and `RoadmapGraphWidget` (24-06); stale help row for `r` (24-05); detail-view digits `9`/`0` (24-03/24-07).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Side-by-side list + detail pane for Mockup A at 120 columns</name>
  <reversibility rating="reversible">A new widget module with no caller yet; the breakpoint and column budget are discretion (D-A09 rated reversible).</reversibility>
  <files>src/ui/roadmap_view.rs, src/ui/mod.rs</files>
  <read_first>
    - `src/ui/roadmap_graph.rs` — the 24-02 model types, `lane_text`, glyph consts, and the legacy `RoadmapGraphWidget` (zero-area guard, `Paragraph` rendering, marker→style mapping ~1049-1156) as the widget-skeleton analog.
    - `src/ui/screens/detail.rs` — `render_pipeline_tab` (~4301-4370: master/detail split, `List` + `ListState`), `truncate_subject` (~301-317: char-wise `…` truncation to copy — it is private there), `shown` (~78).
    - `src/ui/mod.rs` — module list (lines 1-3) and the legacy escape test at the end of the file (~975-1033).
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/MOCKUPS.md` — Mockup A (list and detail pane rows).
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md` — § Pattern 5 (breakpoint, row column budget), § Detail-pane content, § Code Examples (glyph consts), § Don't Hand-Roll (Wrap with hanging indent).
  </read_first>
  <action>
Add `pub mod roadmap_view;` to `src/ui/mod.rs` and create `src/ui/roadmap_view.rs` with a module
doc stating its contract: it draws only the escaped model from `roadmap_graph` and static literals,
measures by chars, clips to its `Rect`.

**Split.** `panes(area)`: when `area.width >= ROADMAP_SIDE_BY_SIDE_MIN_COLS`, horizontal — detail
width `clamp(area.width * 2 / 5, 40, 56)`, list the rest; otherwise vertical — detail height
`min(9, area.height / 2)`, list the rest. A zero-size area returns zero rects.

**List pane.** Bordered `Block`, title ` Roadmap `, right-aligned hint ` v list ` (the `v` key
toggles the box list). Inner line 1: `Start now: ` then `start_now` items as `{glyph}{id} {name}`
with ` (active)` on the active one, joined by ` ║ ` (`PARALLEL_SEP`), truncated with `…`; with none,
`Start now: nothing ready`. Line 2: the column header (`lanes`, `#`, `Phase`, `plans`, `wave`).
Then the visible model rows from `state.offset`: Phase rows as
`{lanes}{marker}{id right-aligned to the widest id}  {name}{plans right-aligned in 5}  {W<wave>}`
with the name column taking the remaining width (char-truncated with `…`); plans `d/t` or `—`.
Band and summary rows in Task 2 (draw them plainly for now). Lane cells are measured from the
widest visible lanes string (minimum 8 cells).

**Styles (D-A04, D-A05).** Status glyph cell: Done `Color::DarkGray`, Active `Color::Yellow` +
BOLD, Ready `Color::Green`, Blocked default; a Done row's text is DarkGray. Marker column relative
to the cursor phase `s`: `s` itself → `▶` and the whole row `Modifier::REVERSED`; a node in
`s.needs` → `↑` cyan; in `s.unblocks` → `↓` magenta; an implied dep of `s` → `·` DarkGray.

**Detail pane (side-by-side form, Mockups A/B).** Bordered block titled ` Phase {id} `. Lines:
name (bold); `{band label} · wave {w} of {max_wave}` (omit the band part when there is none);
`{glyph} {done|active|ready|blocked}` + ` · {d}/{t} plans` or ` · plans TBD` + two spaces and the badge verbatim (it carries its own brackets)
when present + ` · planned (not a GSD phase)` for a planned node; blank; `Goal` then the goal
wrapped with `Paragraph::wrap(Wrap { trim: true })` (or `no goal in ROADMAP.md`); blank;
`Needs     ` + one line per need `{glyph} {id} {name…} {d/t}`, each implied dep as
`{glyph} {id} {name…}` followed by an indented `(implied via {via id})` line, each external dep as
`{id} (outside this roadmap)`, or `nothing declared` when `no_deps`; `Unblocks  ` + one line per
unblocked phase (append the other band's short id when it differs), or
`nothing (last in {short})` when `last_in_band`, else `nothing`; `Parallel  ` + the same-wave
peers `{glyph} {id} {name…}` (append ` done` for a Done peer), or `none in wave {w}`, followed —
when `no_deps` — by `(no edge either way; can run any time)` if `no_edges`, else
`(no deps; can run any time)`; blank; footer hint `⏎ open in Phases   h/l follow edge`.

**Test (tracer).** `roadmap_graph`'s test helpers are private to its test module, so give
`roadmap_view`'s tests their own small spec builder (`(id, name, deps, marker, band)` tuples →
`ListInput`). `mockup_a_side_by_side_at_120_columns`: build the Mockup A model with
`roadmap_graph::layout_list` (the bookly spec of 24-02's `lanes_mockup_a_with_bands`), cursor
`Phase("12")`, render into a 120×28 `Buffer` via `StatefulWidget::render`; with
`panes(area)` split the buffer into list and detail text (helper `rect_text(buf, rect) ->
Vec<String>`). Assert: the list and detail rects share `y` and the detail starts right of the
list; detail text contains `Phase 12`, `Checkout & payments`, `wave 4 of 7`, the Needs line
does not contain `nothing declared`, Needs lines contain `10` and `11`, Unblocks lines contain
`13` and `18` and `M5`, and `none in wave 4`; in the list text the row containing `Checkout & payments` contains
`▶`, the rows for `Slot picker UI` and `Calendar sync` contain `↑`, the rows for
`Confirmation flow` and `Web booking portal` contain `↓`; the `Start now:` line contains
`Slot picker UI (active)`, `║` and `Calendar sync`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::roadmap_view</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - `mockup_a_side_by_side_at_120_columns` appears as `ok`.
    - `src/ui/mod.rs` contains the line `pub mod roadmap_view;`.
    - `rtk proxy cargo build` succeeds with `src/ui/screens/` unmodified by this plan.
  </acceptance_criteria>
  <done>A Mockup A model renders as list + detail pane side by side with the cursor, dep/unblock markers and the full detail content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stacked 80-column layout, bands and shipped summary, lane cap, scrolling and empty states</name>
  <files>src/ui/roadmap_view.rs</files>
  <read_first>
    - `src/ui/roadmap_view.rs` — Task 1.
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/MOCKUPS.md` — Mockup B (band row, shipped summary, `(implied via 21)`, `nothing (last in v1.5)`, truncated name `Notification Scheduling Extraction & Det…`) and Mockup C (stacked 80-col detail pane, `Needs     nothing declared   Unblocks  nothing`, `Parallel  … (no edge either way; can run any time)`, `⏎ open in Phases   h/l follow edge   j/k move`).
    - `src/ui/screens/detail.rs` — `clamp_scroll` / `ViewportMetrics` (~211-225) for the scroll-clamp idiom.
  </read_first>
  <behavior>
    - Mockup C model (sentriq spec with the synthetic `v0.12 Actuation Routines` band and a shipped `v0.11`) at 80×20, cursor `Phase("12")`: `panes` stacks (detail below list, both full width); detail text contains `nothing declared`, `no edge either way; can run any time`, `j/k move`.
    - Mockup B model at 120×28, cursor `Phase("23")`: detail contains `(implied via 21)`, `nothing (last in v1.5)` and a Parallel line containing `22`; the list row of phase 20 contains `·` and ends its name with `…`; the shipped summary row contains `▸`, `5 milestones · 17 phases shipped`.
    - Every band label occurs on exactly one list row; the `▾`/`▸` glyph reflects the fold state.
    - A model with 8 simultaneous lanes renders at 80 columns with `┆` in the lane column and no lane glyph past `2 * LANE_CAP_NARROW + 1` cells; at 120 columns the cap is `LANE_CAP_WIDE`.
    - `keep_visible(0, 30, 10) == 21`, `keep_visible(25, 3, 10) == 3`, `keep_visible(5, 8, 10) == 5`; rendering with the cursor below the viewport moves `state.offset` so the cursor row is drawn; `state.list_rows` reports the visible row count.
    - A model with no phases draws `No roadmap data available`; a planned node's status line contains `planned (not a GSD phase)`; a node with badge `[Executing 2/3]` shows `[Executing 2/3]` (single brackets) in its status line; a cursor on a band shows `{done}/{total} phases done` and `Space fold/unfold`; a cursor on the shipped summary lists each shipped band label with its declared phase count and `⏎ open milestones`.
  </behavior>
  <action>
**Stacked form (Mockup C).** Below the breakpoint the detail pane uses the compact line set:
`{name}   {band short} · wave {w} · {glyph} {status word} · {plans}` on one line (truncated),
`Goal  ` with a hanging-indent wrapped goal (`Layout::horizontal([Length(6), Min(0)])` + `Wrap`),
`Needs     …   Unblocks  …` on one line when both are short (else two lines), `Parallel  …`,
and the hint `⏎ open in Phases   h/l follow edge   j/k move`. Lines that do not fit the pane's
height are dropped from the bottom up except the hint, which is dropped first.

**Band rows.** `{lanes}{▾ or ▸} {label}  {done}/{total} ` then `━` fill to the pane edge
(`BAND_OPEN`/`BAND_FOLDED`/`BAND_FILL`). **Shipped summary.** `{lanes}{▸ or ▾} {text}   {n} milestone(s)
· {m} phases shipped`. **Band / summary under the cursor** → reversed row, and the detail pane
per the behaviours (title ` {short} ` for a band, ` Shipped ` for the summary).

**Lane cap.** Cap = `LANE_CAP_WIDE` when the widget area is at least
`ROADMAP_SIDE_BY_SIDE_MIN_COLS` wide, else `LANE_CAP_NARROW`; lanes past the cap are replaced by
one `LANE_OVERFLOW` cell when any of them is non-blank.

**Scrolling.** The two header lines never scroll; model rows scroll with `state.offset` clamped
by `keep_visible` against the cursor's `row_of`; write `state.list_rows`. Add a `Notes` line at
the bottom of the list pane when `model.notes` is non-empty (one line, truncated).

Write the tests named by the behaviours: `mockup_c_stacked_at_80_columns`,
`mockup_b_implied_dep_and_shipped_summary_at_120`, `band_labels_are_drawn_once`,
`lanes_past_the_cap_collapse_into_one_overflow_column`, `keep_visible_clamps_both_ways`,
`the_cursor_row_is_scrolled_into_view`, `an_empty_model_explains_itself`,
`a_planned_phase_says_so`, `the_stage_badge_is_in_the_status_line`,
`a_band_cursor_has_its_own_detail_pane`, `the_shipped_summary_cursor_lists_its_milestones`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::roadmap_view</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - The eleven tests named in this task appear as `ok`, and Task 1's test still passes.
    - `mockup_c_stacked_at_80_columns` asserts the detail rect's `y` is greater than the list rect's `y` and both are 80 wide.
  </acceptance_criteria>
  <done>The widget matches Mockups B and C, stacks below 100 columns, keeps the cursor visible, caps lanes and explains empty, planned and no-deps cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Escaping, tiny sizes, no bleed — and the ported escape test in src/ui/mod.rs</name>
  <files>src/ui/roadmap_view.rs, src/ui/mod.rs</files>
  <read_first>
    - `src/ui/roadmap_graph.rs` — legacy tests `roadmap_graph_never_panics_or_bleeds_at_tiny_sizes` and `buffer_rows` (~1558-1610) to port.
    - `src/ui/mod.rs` — `the_roadmap_graph_renders_a_clean_phase_name_unchanged_and_a_control_one_differently` (~975-1033).
    - `src/text.rs` — `render_for_terminal` (~487).
  </read_first>
  <behavior>
    - A model whose id, name, goal and band label carry `\u{1b}[31m` and `\u{202E}` renders no cell containing either raw character at 120×28 and at 80×20, and the escaped forms appear.
    - Rendering at (1,1), (5,3), (10,3), (20,5) and (40,8) never panics, for both a cursor on a phase and on a band.
    - Rendering into `Rect::new(3, 2, 5, 3)` of a 20×10 buffer pre-filled with `x` changes no cell outside that rect.
    - A 70-character name mixing multibyte characters (`Überprüfung`, `日本語`, `✓`) renders at 80×20 without panicking and its list row ends the name with `…`.
  </behavior>
  <action>
Write `roadmap_view_stores_and_draws_only_escaped_text`, `roadmap_view_never_panics_at_tiny_sizes`,
`roadmap_view_never_draws_outside_its_rect` and `a_multibyte_name_truncates_on_char_boundaries`
per the behaviours (port the legacy helpers).

In `src/ui/mod.rs`, rewrite the legacy escape test to exercise `RoadmapView` instead of the
legacy graph widget — rename it `the_roadmap_view_renders_a_clean_phase_name_unchanged_and_a_control_one_differently`
and keep both directions: a clean name reaches the cells verbatim; a name carrying ESC and
`U+202E` reaches them as its `render_for_terminal` form, differs from the clean render, and no raw
control character survives. Build the one-node model with `layout_list` (marker `Current`,
cursor on it) and assert on the detail pane's name line. After this, `src/ui/mod.rs` no longer
imports any legacy graph type (plan 24-06 deletes them).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line under `ui::`</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `roadmap_view_stores_and_draws_only_escaped_text`, `roadmap_view_never_panics_at_tiny_sizes`, `roadmap_view_never_draws_outside_its_rect`, `a_multibyte_name_truncates_on_char_boundaries` and `the_roadmap_view_renders_a_clean_phase_name_unchanged_and_a_control_one_differently` appear as `ok`.
    - `rtk proxy git grep -n -F -e 'RoadmapGraphWidget' -e 'layout_graph' -e 'GraphNode' -- src/ui/mod.rs` prints nothing.
  </acceptance_criteria>
  <done>The widget is escape-safe and size-safe, and the only test outside `roadmap_graph.rs` that used the legacy graph widget now covers the new one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| escaped model strings → terminal cells | the last hop of third-party ROADMAP text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-12 | Tampering / Spoofing | escape or bidi sequences reaching cells via name, goal, band label | high | mitigate | the widget draws only model strings (escaped in 24-02) and literals; `roadmap_view_stores_and_draws_only_escaped_text` plus the ported `src/ui/mod.rs` two-direction test |
| T-24-13 | Denial of Service | panic on multibyte truncation or tiny terminal | medium | mitigate | char-based truncation on escaped text; zero-area guard; tiny-size and no-bleed tests |
| T-24-14 | Denial of Service | lane explosion on a wide fan-out pushing names off screen | low | mitigate | lane cap 4 / 6 with a single overflow column |
| T-24-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `rtk proxy cargo test --lib ui::` green.
- `rtk proxy cargo test --no-fail-fast`: only the known git-version witness fails; 49 suites ran.
- `rtk proxy cargo clippy -- -D warnings` clean.
</verification>

<success_criteria>
- Mockups A (120 cols) and C (80 cols) render with the specified content; B's implied dep and shipped row render.
- No horizontal scroll mechanism exists; 80 columns works by stacking and truncation.
</success_criteria>

<output>
Create `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-04-SUMMARY.md` when done
</output>
