---
phase: 24-roadmap-tab-redesign-and-detail-tab-consolidation
plan: 07
type: execute
wave: 4
depends_on: [24-03, 24-05, 24-06]
files_modified:
  - src/ui/screens/detail.rs
  - src/app.rs
  - src/ui/screens/render_escape_guard.rs
  - src/ui/screens/help.rs
  - README.md
  - docs/ARCHITECTURE.md
  - .planning/todos/pending/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md
  - .planning/todos/completed/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md
autonomous: true
requirements: [D-B01, D-B04, D-B05, D-B06, D-B09, D-B10, D-B11]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The detail view has exactly eight tabs plus Driver — `1:Roadmap 2:Phases 3:Backlog 4:Git 5:Queue 6:Sess 7:Cfg 8:Docs` (compact `1:Rd 2:Ph 3:Bk 4:Gt 5:Qu 6:Ss 7:Cf 8:Dc`), `D:Drive` last behind the experimental flag; `TAB_COUNT` 9, `DRIVER_TAB_INDEX` 8, the four widths re-derived from the arrays (D-B01, D-B11)."
    - "Archive is the Docs tab's `Milestones` sub-tab: `tab_index(&DetailSubView::Archive) == tab_index(&DetailSubView::Browse)`, `sub_view_from_index` of that index is `Browse`, a `Files | Milestones` strip marks the active sub-tab (`[Files]` / `[Milestones]`) on both, and `m` switches between them; the archive drill-down behaves exactly as before (D-B04). [INFERRED — audit: the `DetailSubView::Archive` variant is kept as the sub-view rather than removed (RESEARCH Pattern 6, deviating from agent-inferred D-B09), and `m` is the discretionary switch key]"
    - "`switch_to_sub_view` holds the one arrival rule (Archive milestone discovery included); `switch_to_tab` resolves an index then delegates; `DetailScreen::opened_on` calls `switch_to_sub_view` directly, so `opened_on(.., Archive, ..)` lands on Docs › Milestones, not Docs › Files."
    - "The five debug-fix regression tests in `src/app.rs` — `archive_milestone_view_keeps_its_content_across_the_periodic_prune`, `the_same_milestone_version_in_two_projects_does_not_share_a_cache_entry`, `archive_milestone_view_reloads_in_place_when_planning_files_change`, `the_archive_milestone_list_picks_up_a_newly_archived_milestone_in_place`, `an_in_place_reload_that_shrinks_the_listing_keeps_the_cursor_on_a_row` — keep their names and every assertion byte-for-byte, open the archive THROUGH the Docs tab's Milestones sub-tab (Docs, then `m`), and pass. No title string changes: the breadcrumb stays `Archive > v1.2` (D-B06)."
    - "`Enter` on the Roadmap's collapsed shipped-milestones row lands on Docs › Milestones with milestone discovery scheduled (D-B04)."
    - "Digits `9` and `0` are inert in the detail view; the footer's digit range reads `[1-8/D]` / `[1-8]` and the Docs footers advertise `[m]` (D-B10)."
    - "Every tab-index consumer agrees with the final order — round-trip tests, width anti-drift test, escape-guard probe, help text, README and `docs/ARCHITECTURE.md` — and a stored `DetailSubView::Archive` (in-memory per-project view state) simply renders Docs with Milestones active: no migration (D-B05, D-B09)."
    - "The work landed after the Archive `Loading...` debug fix (`3c0e38f`, `581aa7d`) and was written against a fresh read of `detail.rs` and `app.rs` (D-B06)."
  artifacts:
    - path: "src/ui/screens/detail.rs"
      provides: "final tab constants and mappings, switch_to_sub_view, opened_on via switch_to_sub_view, `m` switch, docs_sub_tab_strip in both Docs renders, shipped-row Enter to Docs › Milestones, final footer prefix"
      contains: "fn switch_to_sub_view"
    - path: "src/app.rs"
      provides: "Archive/Driver enum docs, the index test at 8, the five archive tests ported to open via Docs › Milestones, the shipped-row App test"
      contains: "fn open_docs_milestones"
    - path: "src/ui/screens/help.rs"
      provides: "the `m` row"
      contains: "Docs tab: switch Files / Milestones (detail view)"
    - path: "README.md"
      provides: "eight-tab detail view description with Docs › Milestones"
      contains: "8-tab detail view"
  key_links:
    - from: "DetailScreen::opened_on"
      to: "switch_to_sub_view"
      via: "direct call with the requested sub-view (no index round trip)"
      pattern: "switch_to_sub_view\\(&screen.alias"
    - from: "src/app.rs open_docs_milestones"
      to: "the Docs tab's `m` key"
      via: "opened_on(.., Browse, ..) then a real `m` key event through App::update"
      pattern: "KeyCode::Char\\('m'\\)"
    - from: "Roadmap Enter on the shipped summary"
      to: "switch_to_sub_view(.., DetailSubView::Archive, ..)"
      via: "the RoadmapViz Enter branch"
      pattern: "DetailSubView::Archive"
  prohibitions:
    - "No assertion in the five archive regression tests is weakened, removed or reworded."
    - "The archive cache keying and in-place reload logic from `3c0e38f` / `581aa7d` is not modified."
---

<objective>
Fold the Archive tab into Docs as its `Milestones` sub-tab (D-B04), completing the eight-tab
layout (D-B01), and finish every remaining tab-index consumer — digits, footer, help, README,
architecture doc (D-B05, D-B10).

Purpose: the last half of success criterion 5. It is sequenced LAST by the user's decision D-B06:
the Archive drill-down just received a debug fix (`3c0e38f` keys the archive cache by alias then
milestone and prunes it by project; `581aa7d` reloads the open archive in place on `.planning`
changes; `46d7e82` resolved the session), and its five new regression tests must survive the move.

Dependency: `depends_on: [24-03, 24-05, 24-06]` — **shared files** `src/ui/screens/detail.rs`,
`src/app.rs`, `src/ui/screens/render_escape_guard.rs` and `src/ui/screens/help.rs`, plus a
**semantic** dependency on 24-05's Roadmap `Enter` branch (re-routed here) — and the user's D-B06
ordering (last wave).

**Executor instruction (mandatory, D-B06):** re-read `src/ui/screens/detail.rs` (tab constants,
`opened_on`, `tab_index`, `sub_view_from_index`, `switch_to_tab`, the Archive arrival block, every
`DetailSubView::Archive` key arm, `render_archive_tab`, `archive_breadcrumb`, `render_browser_tab`,
`footer_spans`, the RoadmapViz `Enter` branch) and `src/app.rs` (the whole archive regression block
~4725-5040 and `open_archive_tab`) at execution time. Every line number in this plan is a hint.

Output: Docs with Files | Milestones, the final tab constants, ported tests, updated docs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-03-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-05-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-06-SUMMARY.md
@.planning/debug/archive-milestone-view-loading.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 24 plan; newly-created names are not drift).

**New files:** `tests/fixtures/roadmaps/README.md`, `tests/fixtures/roadmaps/{daily-vow,sentriq,ttbook}-{ROADMAP,STATE}.md` (24-01), `src/ui/roadmap_view.rs` (24-04).
**Parser functions (24-01):** `parse_phase_goals`, `parse_planned_build_phases`, `shipped_milestones`, `declared_phase_count`; `ProjectState.{phase_goals, planned_phases, milestone_name}`.
**Engine types and functions (24-02):** `ListNode`, `BandInput`, `ListInput`, `RoadmapModel`, `ListRow`, `PhaseFacts`, `BandFacts`, `PhaseStatus`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, `layout_list`, `is_folded`, `lane_text`, the `RoadmapModel` navigation methods, the glyph consts.
**Widget (24-04):** `RoadmapView`, `RoadmapViewState`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`, `LANE_CAP_NARROW`, `LANE_CAP_WIDE`, `LANE_OVERFLOW`, `panes`, `keep_visible`.
**View state (24-05):** `ProjectViewCache.{roadmap_cursor, roadmap_fold_toggles, roadmap_edge_walk}`; `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`; `roadmap_model_for`.
**Detail screen (24-06/24-07):** `roadmap_summary_line`, `switch_to_sub_view`, `docs_sub_tab_strip`; test helpers `render_detail_to_text_at`, `roadmap_list_pane`; app test helper `open_docs_milestones`.
**Keys:** Roadmap `j`/`k`/`Up`/`Down`/`PageUp`/`PageDown`/`g`/`G`/`h`/`l`/`[`/`]`/`Space`/`Enter` (24-05); Docs `m` (24-07).
**Constants changed:** `TAB_COUNT` 11 → 10 → 9, `DRIVER_TAB_INDEX` 10 → 9 → 8, `TAB_BAR_*` re-derived, label arrays reordered (24-03, 24-07).
**Removed:** `DetailSubView` variant `PhaseList`, `render_phase_list` (24-03); the old graph layout and `RoadmapGraphWidget` (24-06); stale help row for `r` (24-05); detail-view digits `9`/`0` (24-03/24-07).
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: 9:Arch -->
<!-- planner-discipline-allow: [1-9/D] -->

<tasks>

<task type="tracer">
  <name>Task 1: Docs › Milestones end to end — final tab set, switch_to_sub_view, opened_on, `m`, the strip, and the five archive tests ported</name>
  <reversibility rating="costly">Completes D-B01 (rated costly in CONTEXT): the final tab set is consumed by every index consumer. Locked and user-approved — flagged, not gated.</reversibility>
  <precondition>The debug fix is in history: `git merge-base --is-ancestor 3c0e38f HEAD` and `git merge-base --is-ancestor 581aa7d HEAD` both exit 0 (D-B06). If either fails, halt and report — the Archive move must not land before that fix.</precondition>
  <files>src/ui/screens/detail.rs, src/app.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — FULL fresh read of: tab constants and width docs (~347-453), `opened_on` and its doc (~698-730), `tab_index` / `sub_view_from_index` / `effective_sub_view` (~778-847), `switch_to_tab` (~1283-1428, including the Archive arrival block that schedules milestone discovery), the Browse and Archive key arms (grep `DetailSubView::Archive` and `DetailSubView::Browse`), the guarded `g` on Browse (~2721) as the arm pattern, `render_archive_tab` (~4619) and `render_browser_tab` (~4804) with their 1-row breadcrumb headers, `archive_breadcrumb` (~4957) and both render dispatches. The fix commits changed this file: trust the tree, not these numbers.
    - `src/app.rs` — `DetailSubView` docs (~15-40) and the archive regression block (~4725-5040): `archive_fixture`, `open_archive_tab`, `press`, `pump_archive_until`, `is_discovery_for`, `is_load_for`, `render_top_screen`, `drill_into_v1_2`, and the five tests.
    - `.planning/debug/archive-milestone-view-loading.md` — what the fix guarantees (cache keyed by alias then milestone; pruned by project; in-place reload).
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md` — § Pattern 6, § Tab-Index Consumer Inventory.
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md` — `switch_to_tab` split, `opened_on` note, the app.rs guard section.
  </read_first>
  <action>
**Final tab set (D-B01, D-B11).** Remove the interim Archive entry from both label arrays (full
ends `"7:Cfg", "8:Docs", "D:Drive"`, compact `"7:Cf", "8:Dc", "D:Dr"`); `TAB_COUNT = 9`;
`DRIVER_TAB_INDEX = 8`; re-derive the four widths with the documented arithmetic and rewrite each
doc's worked sum (expected full 89, compact 63, full-no-driver 78, compact-no-driver 55 — the
anti-drift test is the authority). `tab_index(&Archive)` returns the same index as `Browse` (7);
`sub_view_from_index(7)` stays `Browse`; nothing else maps to 8 except the Driver. Update the
comments that name the last index.

**`switch_to_sub_view(alias, view: DetailSubView, scroll_offset, ctx) -> ScreenAction`.** Move the
body of `switch_to_tab` into it unchanged — same resets, same arrival work, the Archive discovery
block exactly as the fix left it. `switch_to_tab(alias, index, ..)` becomes
`switch_to_sub_view(alias, sub_view_from_index(index, ctx.experimental), ..)`. `opened_on` calls
`switch_to_sub_view(&screen.alias, sub_view, &mut screen.scroll_offset, ctx)` directly; rewrite
its doc paragraph: the index argument no longer holds for sub-tabs (two sub-views share Docs'
index), so the sub-view form is the one entry point and `tab_index` stays the index authority for
digits and arrows. `switching_to_the_backlog_tab_returns_no_screen_action` must still pass.

**`m` and the strip.** A guarded arm `KeyCode::Char('m') if matches!(current_view,
DetailSubView::Browse | DetailSubView::Archive)` calls `switch_to_sub_view` with the other one
(check first that no Browse/Archive text-input mode consumes `m`; there is none at planning time).
`docs_sub_tab_strip(active: &DetailSubView) -> Line<'static>` renders `[Files] │ Milestones   m switch`
on Browse and `Files │ [Milestones]   m switch` on Archive (active one also REVERSED); draw it in
the first row of BOTH `render_browser_tab` and `render_archive_tab` above their existing breadcrumb
row, shrinking the body by one row. Keep `archive_breadcrumb`'s `Archive` literal: **no title
string changes**, so `Archive > v1.2` stays exact. [INFERRED — audit]

**Port the five archive tests (orchestrator requirement 1).** In `src/app.rs`, replace
`open_archive_tab` with `open_docs_milestones(app, alias)`: push
`DetailScreen::opened_on(alias, DetailSubView::Browse, &mut app.ctx)` and then deliver a real
`m` key through `press(app, KeyCode::Char('m'))`, so milestone discovery is scheduled on the real
channel by the Docs sub-tab switch. Rewrite the helper docs ("the way a user does: `8` for Docs,
then `m` for Milestones") and `drill_into_v1_2`'s doc (`8`, `m`, `Down`, `Enter`). Point every
caller at the new helper. Change nothing else in the five tests: names, fixtures, pumps, key
sequences after opening, and every assertion (`Archive > v1.2`, `Phase 01: Core Flow`,
`Loading...` absent, `>   v1.2-ROADMAP.md`, the cursor index checks) stay byte-identical.

**New detail.rs tests:** `opened_on_archive_lands_on_docs_milestones` (stored view `Archive`,
render contains `[Milestones]` and the `8:Docs` tab active), `m_switches_docs_between_files_and_milestones`,
`m_is_inert_outside_docs` (Roadmap), and in `every_tab_index_round_trips_through_its_sub_view`
add `assert_eq!(tab_index(&DetailSubView::Archive), tab_index(&DetailSubView::Browse))`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib -- --exact app::tests::archive_milestone_view_keeps_its_content_across_the_periodic_prune app::tests::the_same_milestone_version_in_two_projects_does_not_share_a_cache_entry app::tests::archive_milestone_view_reloads_in_place_when_planning_files_change app::tests::the_archive_milestone_list_picks_up_a_newly_archived_milestone_in_place app::tests::an_in_place_reload_that_shrinks_the_listing_keeps_the_cursor_on_a_row</automated>
    <fails_when>non-zero exit, or the summary line does not read "5 passed" (a renamed or filtered-away test shows as fewer than 5)</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - The first command reports exactly `5 passed`; the five names appear as `ok`.
    - `git diff $PLAN_BASE -- src/app.rs` changes no line containing `assert` inside the five tests — only the opening helper and doc comments change (`$PLAN_BASE` is the commit this plan's worktree started from; diffing against it covers committed and uncommitted work).
    - `opened_on_archive_lands_on_docs_milestones`, `m_switches_docs_between_files_and_milestones`, `m_is_inert_outside_docs` and `every_tab_index_round_trips_through_its_sub_view` appear as `ok`.
    - `rtk proxy git grep -n -F -e '"9:Arch"' -e '"9:Ar"' -- src` prints nothing.
  </acceptance_criteria>
  <done>Archive is reached only as Docs › Milestones, and the debug fix's five regression tests pass unchanged through that path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shipped row → Docs › Milestones, inert 9/0, final footer, remaining tab tests, enum docs, probe prose</name>
  <files>src/ui/screens/detail.rs, src/app.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — the RoadmapViz `Enter` branch (24-05: shipped summary → Archive via `tab_index`), the digit arms, `driver_footer_spans` / `footer_spans` (Browse and Archive arms), the tab tests renamed count-agnostic in 24-03 (see 24-03-SUMMARY for the old → new names), `the_tabs_hint_*` and `the_driver_footer_has_three_measured_width_forms`.
    - `src/app.rs` — `the_driver_sub_view_is_the_last_tab_index_in_both_directions` (renamed in 24-03), `obs_app` (~2557-2580), and the ported archive harness from Task 1.
    - `src/ui/screens/render_escape_guard.rs` — the Archive arrival rows and sub-states, `ALL_SUB_VIEWS`, the tab/state-count prose.
  </read_first>
  <behavior>
    - An `App` built with `obs_app` over a tempdir holding `archive_fixture` plus a ROADMAP whose `## Milestones` list marks `v1.1` and `v1.2` shipped and `v1.3` in progress (with a STATE.md naming `v1.3` and its state parsed into `ctx.project_states`): a detail screen on `RoadmapViz`, `g` (cursor to the shipped summary), `Enter` → the stored view is `Archive`, milestone discovery for that alias arrives (`pump_archive_until(.., is_discovery_for(..))`), and `render_top_screen` contains `[Milestones]` and `v1.2`.
    - Pressing `9` or `0` in the detail view leaves the stored view unchanged; `8` lands on Docs › Files.
    - The flag-on footer starts its tabs hint with `[1-8/D]tabs`, flag-off with `  [Esc]back  [1-8]tabs  `; the Browse and Archive footers contain `[m]`.
    - A stored `DetailSubView::Archive` renders the tab bar with `8:Docs` active (in-memory view state; no migration).
    - From Browse, `Right` reaches the Driver tab with the flag on and stays put with it off.
  </behavior>
  <action>
In the RoadmapViz `Enter` branch, route the shipped summary through `switch_to_sub_view(&self.alias,
DetailSubView::Archive, &mut self.scroll_offset, ctx)` (D-B04), and update 24-05's
`roadmap_enter_on_the_shipped_row_opens_the_archive_view` to also assert the render shows
`[Milestones]`. Add the App-level test `enter_on_the_shipped_milestones_row_opens_docs_milestones`
per the behaviours (copy the archive harness; do not modify the five ported tests).

Delete the `9` digit arm (D-B10) so `9` and `0` both fall through to the no-op arm; digits
`1`-`8` map to indices 0-7. Footer prefixes become `[1-8/D]` / `[1-8]`; in the experimental
footer test update the stale "digits-only hint" negative check to the new range's digits-only
form. Add `[m]` + `ilestones  ` (Browse) and `[m]` + ` files  ` (Archive) to those footer arms;
re-check the measured Driver footer widths (the prefix length is unchanged).

Update the count-agnostic tab tests where a literal remains (the app.rs index test asserts `8`
and `9 → RoadmapViz`); add `nine_and_zero_are_inert_in_the_detail_view`,
`a_stored_archive_view_is_the_docs_tab_with_milestones_active`,
`the_docs_footers_advertise_the_milestones_switch`. In `src/app.rs`, rewrite the `Driver` doc
("Index 8 … `TAB_COUNT` is 9") and the `Archive` doc (the Docs tab's Milestones sub-view, sharing
Docs' index; reached with `m` or from the Roadmap's shipped row). In `render_escape_guard.rs`,
rewrite the Archive arrival reasons to say Docs › Milestones and update the tab/state-count prose
(nine tabs; Docs has two sub-views); `ALL_SUB_VIEWS` keeps `Archive` and the set-equality test
stays green.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib app::tests</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line under `app::tests::`</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - `enter_on_the_shipped_milestones_row_opens_docs_milestones`, `nine_and_zero_are_inert_in_the_detail_view`, `a_stored_archive_view_is_the_docs_tab_with_milestones_active`, `the_docs_footers_advertise_the_milestones_switch` and `the_tab_bar_widths_are_the_label_arrays_own_arithmetic` appear as `ok`.
    - `rtk proxy git grep -n "KeyCode::Char('9') =>" -- src/ui/screens/detail.rs` prints nothing.
    - `rtk proxy git grep -n 'tab_index(&DetailSubView::Archive)' -- src/ui/screens/detail.rs` prints no line inside the RoadmapViz `Enter` branch.
  </acceptance_criteria>
  <done>The Roadmap's shipped row opens Docs › Milestones, the digit set is 1-8, and every tab test, enum doc and probe agrees with the final layout.</done>
</task>

<task type="auto">
  <name>Task 3: Help, README, architecture doc, folded todo, and the phase-wide gates</name>
  <files>src/ui/screens/help.rs, README.md, docs/ARCHITECTURE.md, .planning/todos/pending/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md, .planning/todos/completed/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md</files>
  <read_first>
    - `src/ui/screens/help.rs` — `help_lines` detail-view rows (the 24-05 Roadmap rows and the `v` row) and the whole-row test pattern.
    - `README.md` — the Features list (~44-60: the tab-list bullet and the "Milestone archive browser" bullet) and the highlights bullet "Milestone archive browsing" (~28).
    - `docs/ARCHITECTURE.md` ~160-170 — the `detail.rs` sub-view list.
    - `.planning/todos/pending/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md` and one file in `.planning/todos/completed/` for the completed-todo convention.
  </read_first>
  <action>
**Help.** Add `row("m", "Docs tab: switch Files / Milestones (detail view)")` beside the Roadmap
rows; add `the_docs_milestones_switch_is_documented` (whole-row match). Keep the popup-scroll test
green.

**README (D-B05).** Replace the tab-list bullet with: `8-tab detail view (plus the experimental
Driver tab): Roadmap (phase list with git-log-style dependency lanes and a detail pane showing
each phase's goal, needs, unblocks and parallel phases), Phases, Backlog, Git History, Queue,
Sessions, Config, Docs (Files: rendered .planning/ browser rooted at the active phase, with quick
jumps to .planning/ and back; Milestones: the shipped-milestone archive)`, preserving the list's
markdown style and wrapping. Make both "milestone archive" bullets say it lives in Docs ›
Milestones. **`docs/ARCHITECTURE.md`**: the `detail.rs` line lists the sub-views `RoadmapViz,
Pipeline (Phases), Backlog, GitHistory, Queue, Sessions, Defaults, Browse + Archive (Docs: Files /
Milestones), Driver`.

**Folded todo.** `git mv` the phase-list marker todo from `pending/` to `completed/` and append a
`## Resolution` section: the PhaseList panel was removed (24-03) and the Roadmap's glyphs derive
from `ProjectState::phase_marker` (D-B12), pinned by
`a_disk_complete_phase_with_an_unticked_box_draws_done` (24-06).

**Phase-wide gates.** Run the final stale-literal sweep, the full suite, the lib clippy gate and
the all-targets clippy location check (see verify / acceptance); fix anything this phase caused.
  </action>
  <verify>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer `test result:` lines than the 49-suite baseline</fails_when>
    <automated>rtk proxy cargo clippy -- -D warnings</automated>
    <fails_when>non-zero exit, or any line beginning with "error" or "warning:"</fails_when>
    <automated>rtk proxy cargo clippy --keep-going --all-targets -- -D warnings</automated>
    <fails_when>an error location outside the eleven pre-existing ones recorded in 24-RESEARCH.md § Validation Architecture (src/browser.rs:156,157,158; src/project_creator.rs:146; tests/envelope_carrier_reach.rs:1711; tests/envelope_config_resolution.rs:2206; tests/envelope_control_carrier.rs:978; tests/envelope_wrapper_class.rs:6127,6213,10795,11232)</fails_when>
    <automated>rtk proxy git grep -n -F -e '[1-0' -e '[1-9/D]' -e '1:Phases' -e '5:Pipe' -e '8:Arch' -e '9:Arch' -e '0:Docs' -e 'DetailSubView::PhaseList' -e '10-tab' -e 'ten tabs' -e 'eleven tabs' -- src tests README.md docs</automated>
    <fails_when>any matching line is printed (a stale tab literal or count survived)</fails_when>
  </verify>
  <acceptance_criteria>
    - `the_docs_milestones_switch_is_documented` appears as `ok`.
    - `README.md` contains `8-tab detail view` and `Milestones`; `docs/ARCHITECTURE.md` no longer names `PhaseList` as a detail sub-view.
    - `.planning/todos/completed/2026-08-22-phase-list-grey-marker-disagrees-with-disk-inferred-stage.md` exists with a `## Resolution` section and the `pending/` copy is gone.
    - The four verify commands meet their stated pass conditions; the five archive regression tests appear as `ok` in the full run.
  </acceptance_criteria>
  <done>Docs, help and README describe the eight-tab layout, the folded todo is closed, and every gate is green apart from the known local witness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keypress / `opened_on` → sub-view | landing on the wrong Docs sub-tab silently skips milestone discovery |
| archive directory contents → cells | milestone versions and file names (third-party, `Untrusted`) |
| background archive tasks → UI state | the debug-fixed cache keyed by alias then milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-22 | Tampering (integrity) | `opened_on` / digits / `m` routing after Archive shares Docs' index | medium | mitigate | `switch_to_sub_view` is the single arrival rule; `opened_on` bypasses the index; `opened_on_archive_lands_on_docs_milestones`; round-trip test with the same-index assertion |
| T-24-23 | Information Disclosure / Integrity | regression of the archive cache fix (one project's milestone shown under another) | high | mitigate | the fix's code is not modified (prohibition); its five regression tests keep every assertion and pass through the new path, checked by exact name with a `5 passed` count |
| T-24-24 | Tampering | milestone names in the strip / breadcrumb | low | mitigate | the strip is static text; milestone names keep flowing through the existing `Untrusted::shown()` paths the escape probe covers |
| T-24-25 | Denial of Service | `m` swallowing typed text in a Docs input mode | low | mitigate | guarded arm only on Browse/Archive views; executor confirms no text-input mode exists there before adding it |
| T-24-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- The five archive regression tests pass by exact name (`5 passed`), with unchanged assertions.
- `rtk proxy cargo test --no-fail-fast`: only the known git-version witness fails; 49 suites ran.
- `rtk proxy cargo clippy -- -D warnings` clean; `--keep-going --all-targets` shows only the eleven pre-existing locations.
- The final stale-literal sweep prints nothing.
</verification>

<success_criteria>
- SC-5 complete: PhaseList gone (24-03), Phases at 2 sharing the selection (24-05), Archive lives in Docs as Milestones, and every tab-index consumer — round-trip tests, help, escape guard, README, in-memory view state — agrees with `1:Roadmap 2:Phases 3:Backlog 4:Git 5:Queue 6:Sess 7:Cfg 8:Docs` + Driver.
- The debug fix's behaviour and tests survive the move.
</success_criteria>

<output>
Create `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-07-SUMMARY.md` when done
</output>
