---
phase: 24-roadmap-tab-redesign-and-detail-tab-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.rs
  - src/ui/screens/detail.rs
  - src/ui/screens/render_escape_guard.rs
autonomous: true
requirements: [D-B01, D-B02, D-B03, D-B05, D-B07, D-B09, D-B10, D-B11]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The detail view's first eight tabs are, in order, `1:Roadmap 2:Phases 3:Backlog 4:Git 5:Queue 6:Sess 7:Cfg 8:Docs` (compact `1:Rd 2:Ph 3:Bk 4:Gt 5:Qu 6:Ss 7:Cf 8:Dc`), with the Driver tab last behind the experimental flag — the final D-B01/D-B11 positions. Archive sits at an INTERIM ninth position (`9:Arch` / `9:Ar`) until plan 24-07 folds it into Docs; this keeps `DetailScreen::opened_on(.., Archive, ..)` and the five debug-fix regression tests in `src/app.rs` passing untouched until that plan."
    - "The PhaseList tab no longer exists: `DetailSubView` has no `PhaseList` variant, `render_phase_list` and both of its dispatch arms are gone, and `phase_list_label` (still used by the Phases/Pipeline tab) survives (D-B02)."
    - "The Roadmap tab is the default: `#[default]` is `RoadmapViz`, and it is the fallback of `sub_view_from_index` (out-of-range, and the Driver index with the experimental flag off) and of `effective_sub_view` (D-B07)."
    - "The Pipeline tab is labelled `Phases` at position 2 — tab label and its block titles — while the `DetailSubView::Pipeline` variant name is kept (D-B03, RESEARCH A13)."
    - "Digit `0` is inert in the detail view (it falls through to the no-op arm); digits `1`-`9` map to indices 0-8 (D-B10; `9` becomes inert in 24-07)."
    - "Every tab-index consumer agrees: `tab_index`/`sub_view_from_index` round-trip over every index, the four tab-bar widths equal the label arrays' own `Σ(len + 2) + (n − 1)` arithmetic, the footer digit-range prefix names the new range, and the escape-guard probe iterates exactly the remaining sub-views (D-B05, D-B11)."
    - "Per-project view state needs no migration: `detail_sub_view_per_project` stores the enum in memory only, so removing a variant changes only the code that constructs or matches it (D-B09)."
  artifacts:
    - path: "src/app.rs"
      provides: "DetailSubView without PhaseList, #[default] RoadmapViz, updated Driver index doc and index test"
      contains: "#[default]\n    RoadmapViz"
    - path: "src/ui/screens/detail.rs"
      provides: "new label arrays, TAB_COUNT 10, DRIVER_TAB_INDEX 9, re-derived widths, mappings, digits, footer prefix, Phases titles"
      contains: "\"1:Roadmap\""
    - path: "src/ui/screens/render_escape_guard.rs"
      provides: "ALL_SUB_VIEWS without PhaseList, DETAIL_TAB_ARRIVAL and sub_view_label in agreement"
      contains: "const ALL_SUB_VIEWS: [crate::app::DetailSubView; 10]"
  key_links:
    - from: "src/ui/screens/detail.rs tab_index"
      to: "src/ui/screens/detail.rs sub_view_from_index"
      via: "every_tab_index_round_trips_through_its_sub_view"
      pattern: "fn every_tab_index_round_trips_through_its_sub_view"
    - from: "TAB_LABELS_FULL / TAB_LABELS_COMPACT"
      to: "TAB_BAR_* width constants"
      via: "the_tab_bar_widths_are_the_label_arrays_own_arithmetic"
      pattern: "fn the_tab_bar_widths_are_the_label_arrays_own_arithmetic"
  prohibitions:
    - "`DetailSubView::Archive`'s behaviour, key arms, render, arrival logic and the five archive regression tests in `src/app.rs` are not touched (D-B06: the Archive move is plan 24-07's, last)."
    - "No hand-written tab index at a call site: handoffs use `tab_index(&view)`."
---

<objective>
Consolidate the detail tabs' order and labels to the locked layout — Roadmap first, Pipeline
renamed Phases at position 2, PhaseList removed — and update every index consumer, leaving
Archive at an interim ninth position so the Archive-to-Docs move (and its debug-fix regression
tests) can land last, in plan 24-07, exactly as D-B06 requires.

Purpose: the renumber is the wide, mechanical, silent-wrong-tab-risk part of success criterion 5;
doing it now, in parallel with the parser and engine, leaves the final plan focused on Archive.
Why an interim ninth Archive tab rather than folding it now: folding Archive into Docs IS the
Archive→Docs move (it forces `opened_on` to change and the five archive tests to be ported), which
the user's ordering puts in the last wave. The interim costs a second, small edit of the tab
count and widths in 24-07; the tests updated here are made count-agnostic where their meaning
allows so that edit stays small. [INFERRED — audit]

Dependency: none — wave 1. Files (`src/app.rs`, `src/ui/screens/detail.rs`,
`src/ui/screens/render_escape_guard.rs`) are disjoint from 24-01 and 24-02.

Output: the new tab order, PhaseList gone, all tab tests green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 24 plan; newly-created names are not drift).

**New files:** `tests/fixtures/roadmaps/README.md`, `tests/fixtures/roadmaps/{daily-vow,sentriq,ttbook}-{ROADMAP,STATE}.md` (24-01), `src/ui/roadmap_view.rs` (24-04).
**Parser functions (24-01):** `parse_phase_goals`, `parse_planned_build_phases`, `shipped_milestones`, `declared_phase_count`; `ProjectState.{phase_goals, planned_phases, milestone_name}`.
**Engine types and functions (24-02):** `ListNode`, `BandInput`, `ListInput`, `RoadmapModel`, `ListRow`, `PhaseFacts`, `BandFacts`, `PhaseStatus`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, `layout_list`, `is_folded`, `lane_text`, the `RoadmapModel` navigation methods, the glyph consts.
**Widget (24-04):** `RoadmapView`, `RoadmapViewState`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`, `LANE_CAP_NARROW`, `LANE_CAP_WIDE`, `LANE_OVERFLOW`, `panes`, `keep_visible`.
**View state (24-05):** `ProjectViewCache.{roadmap_cursor, roadmap_fold_toggles, roadmap_edge_walk}`; `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`; `roadmap_model_for`.
**Detail screen (24-06/24-07):** `roadmap_summary_line`, `switch_to_sub_view`, `docs_sub_tab_strip`; test helpers `render_detail_to_text_at`, `roadmap_list_pane`.
**Keys:** Roadmap `j`/`k`/`Up`/`Down`/`PageUp`/`PageDown`/`g`/`G`/`h`/`l`/`[`/`]`/`Space`/`Enter` (24-05); Docs `m` (24-07).
**Constants changed:** `TAB_COUNT` 11 → 10 → 9, `DRIVER_TAB_INDEX` 10 → 9 → 8, `TAB_BAR_*` re-derived, label arrays reordered (24-03, 24-07).
**Removed:** `DetailSubView` variant `PhaseList`, `render_phase_list` (24-03); the old graph layout and `RoadmapGraphWidget` (24-06); stale help row for `r` (24-05); detail-view digits `9`/`0` (24-03/24-07).
</artifacts_this_phase_produces>

<!-- planner-discipline-allow: 1:Phases -->
<!-- planner-discipline-allow: 5:Pipe -->
<!-- planner-discipline-allow: 0:Docs -->
<!-- planner-discipline-allow: DetailSubView::PhaseList -->

<tasks>

<task type="tracer">
  <name>Task 1: New tab order end to end — enum, labels, mappings, digits, widths, PhaseList removed, Phases titles</name>
  <reversibility rating="costly">D-B01 is rated costly in CONTEXT: undoing a tab order touches every index consumer (label arrays, `tab_index`/`sub_view_from_index`, digits, round-trip tests, help, escape-guard tests, README). Locked and user-approved, so no decision gate; flagged only.</reversibility>
  <files>src/app.rs, src/ui/screens/detail.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - `src/app.rs` — `DetailSubView` (~15-40, the Driver doc paragraph) and `the_driver_sub_view_is_index_ten_in_both_directions` (~4248-4261). Do NOT edit the archive regression block (~4725-5040).
    - `src/ui/screens/detail.rs` — tab constants and docs (~347-453), `opened_on` (~698-730, leave as is), `tab_index` / `sub_view_from_index` / `effective_sub_view` (~778-847), the `adjudicate_screen!` prose (~1451-1488), the digit and arrow arms (~2219-2260), both render dispatches (`render` ~3463-3485 and `render_main_only` ~5074-5097), `render_phase_list` (~3607-3792), `render_pipeline_tab` titles (~4301-4370), `phase_list_label` (~5689, used by Pipeline), `driver_footer_spans` (~6093-6140) and `footer_spans` (~6145-6244). Re-read: line numbers drift.
    - `src/ui/screens/render_escape_guard.rs` — `ALL_SUB_VIEWS` (~1355), `DETAIL_TAB_ARRIVAL` (~1382-1519), `sub_view_label` (~1558-1573) and the prose that counts tabs/states (~110-128, ~838, ~1012, ~1240-1270, ~1590, ~1617).
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md` — § Tab-Index Consumer Inventory, § Pattern 6, § Pitfall 5.
  </read_first>
  <action>
**Enum (`src/app.rs`).** Remove the PhaseList variant; put `#[default]` on `RoadmapViz` (D-B07);
update the Driver doc to "Index 9 … `TAB_COUNT` is 10" and add one line on `Archive` saying it sits
at an interim ninth tab until the Docs fold (plan 24-07).

**Constants (`detail.rs`).** `TAB_COUNT = 10`. `TAB_LABELS_FULL` =
`["1:Roadmap", "2:Phases", "3:Backlog", "4:Git", "5:Queue", "6:Sess", "7:Cfg", "8:Docs", "9:Arch", "D:Drive"]`;
`TAB_LABELS_COMPACT` = `["1:Rd", "2:Ph", "3:Bk", "4:Gt", "5:Qu", "6:Ss", "7:Cf", "8:Dc", "9:Ar", "D:Dr"]`
(D-B01, D-B11). `DRIVER_TAB_INDEX = 9`. Re-derive the four widths from the arrays with the
documented `Σ(len + 2) + (n − 1)` (+1 marker cell with Driver) arithmetic and rewrite each doc's
worked sum — expected full 98, compact 70, full-no-driver 87, compact-no-driver 62; the anti-drift
test is the authority, never hand-edit its `derive`. Update `visible_tab_count`'s doc (10 / 9).

**Mappings.** `tab_index`: RoadmapViz 0, Pipeline 1, Backlog 2, GitHistory 3, Queue 4, Sessions 5,
Defaults 6, Browse 7, Archive 8, Driver `DRIVER_TAB_INDEX` — exhaustive, no wildcard.
`sub_view_from_index` mirrors it; its two fallbacks and `effective_sub_view`'s fallback become
`RoadmapViz`. Update the surrounding comments (index 9 = Driver).

**Digits (D-B10).** `'1'..='9'` → indices 0..8 through `switch_to_tab`; delete the arm for the
zero key so it falls through to the existing no-op `_` arm. Update the `Shift+D` and `Right`
comments ("last tab is index 8 with the flag off").

**Footer.** Both digit-range prefixes (Driver footer and `footer_spans`) become `[1-9/D]` /
`[1-9]` (same width as before, so the measured Driver footer widths hold — re-check them).

**PhaseList removal (D-B02).** Delete `render_phase_list` and the PhaseList arm in BOTH render
dispatches (a change applied to one path only is the hazard the comment near `render` records).
Keep `phase_list_label` (Pipeline uses it) and any helper still referenced. The Roadmap header
(`render_roadmap`) already draws Path, Status + Milestone, the unreadable/recovered lines, the
Paused line and the change-tracker banner — confirm by reading it, and state in the SUMMARY
which PhaseList-only content remains to land in 24-06 (the per-phase `[stage]` badge and the
empty-state wording, D-B08).

**Phases label (D-B03).** In `render_pipeline_tab`, the `" Pipeline "` block titles become
`" Phases "` (the left list is already `" Phases "`); keep the variant name `Pipeline`.

**Escape guard (compile + honesty).** `ALL_SUB_VIEWS` becomes `[_; 10]` without PhaseList;
drop the `"PhaseList tab"` row from `DETAIL_TAB_ARRIVAL` and the PhaseList arm from
`sub_view_label`; update the tab/state counts in the prose to the new numbers. If any probe
assertion shows a third-party field whose ONLY render site was PhaseList, do not fake an arrival:
record it in the SUMMARY (the Roadmap redesign in 24-06 must re-cover it).

**Compile-fix the tests** that name the removed variant (`detail.rs` ~9790, ~9823, ~10061,
~10076, ~11395, ~14508 and `app.rs` ~4260): use `RoadmapViz` where the test means "the default
tab" and `Backlog` where it means "some other tab" (e.g. `roadmap_graph_tab_v_is_inert_on_other_tabs`).

**New end-to-end test** `a_fresh_detail_screen_opens_on_the_roadmap_and_two_reaches_phases`: a
`DetailScreen::new` with no stored view (a `test_ctx()` project state inserted) reports
`effective_sub_view` = `RoadmapViz` and its `render_detail_to_text` contains the Roadmap header's
`Path:` line; pressing `2` stores `DetailSubView::Pipeline` and the render contains ` Phases `;
pressing the zero key afterwards leaves the stored view `Pipeline`.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::every_tab_index_round_trips_through_its_sub_view</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed" (the test was renamed or filtered away)</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::a_fresh_detail_screen_opens_on_the_roadmap_and_two_reaches_phases</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `rtk proxy cargo build` succeeds.
    - `rtk proxy git grep -n -F -e 'DetailSubView::PhaseList' -e 'fn render_phase_list' -- src tests` prints nothing.
    - `rtk proxy git grep -n -F -e '"1:Phases"' -e '"5:Pipe"' -e '"8:Arch"' -e '"0:Docs"' -- src` prints nothing.
    - `rtk proxy git grep -n "KeyCode::Char('0') =>" -- src/ui/screens/detail.rs` prints nothing.
    - `git diff $PLAN_BASE -- src/app.rs` shows no line changed between the `The Archive tab's drill-in view across refreshes` banner and the end of the file (`$PLAN_BASE` is the commit this plan's worktree started from; diffing against it covers committed and uncommitted work).
  </acceptance_criteria>
  <done>The detail view opens on Roadmap, `2` reaches Phases, `0` is inert, PhaseList is gone, and the crate builds.</done>
</task>

<task type="auto">
  <name>Task 2: Every tab test and probe agrees with the new order (count-agnostic where possible)</name>
  <files>src/ui/screens/detail.rs, src/app.rs, src/ui/screens/render_escape_guard.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` tests — the footer and tab block (~9745-10360): `test_other_footers_unchanged_by_browse_edit_hint`, `the_tabs_hint_names_shift_d_on_every_tab`, `the_tabs_hint_drops_shift_d_on_every_tab_when_experimental_is_off`, `the_driver_footer_has_three_measured_width_forms`, `each_driver_footer_form_fits_the_width_that_selects_it`, `every_tab_index_round_trips_through_its_sub_view`, `the_visible_tab_count_drops_the_eleventh_tab_when_experimental_is_off`, `no_width_tier_emits_a_driver_label_when_experimental_is_off`, `the_whole_bar_tiers_still_offer_the_driver_label_when_experimental_is_on`, `the_ten_tab_bar_renders_whole_at_its_own_re_derived_widths`, `the_tab_bar_widths_are_the_label_arrays_own_arithmetic`, `index_ten_is_not_a_tab_when_experimental_is_off`, `a_stored_driver_sub_view_reads_as_the_default_tab_when_experimental_is_off`, `right_from_the_last_visible_tab_stays_put_when_experimental_is_off`, `right_from_tab_nine_reaches_the_driver_tab_when_experimental_is_on`, `the_full_tier_renders_eleven_labels_at_its_measured_width`, `the_compact_tier_renders_eleven_labels_at_its_measured_width`, `the_windowed_tier_always_contains_the_active_tab`, `the_active_tab_label_is_always_present_in_the_rendered_bar`, `shift_d_and_right_both_reach_the_driver_tab`.
    - `src/app.rs` — `the_driver_sub_view_is_index_ten_in_both_directions`.
    - `src/ui/screens/render_escape_guard.rs` — `DETAIL_TAB_ARRIVAL`'s remaining rows and the set-equality assertion that compares them to `ALL_SUB_VIEWS` + `DETAIL_SUB_STATES`.
  </read_first>
  <action>
Update each listed test to the new order, preferring expressions over literals so plan 24-07's
second renumber touches as few lines as possible: counts via `visible_tab_count(..)` /
`TAB_COUNT`, the last pre-Driver index via `DRIVER_TAB_INDEX - 1`, labels via the arrays. Rename
tests whose names state a count to count-agnostic names (for example "…_drops_the_driver_tab_…",
"…_renders_every_label_…", "…_bar_renders_whole_…", "an_index_past_the_visible_tabs_is_not_a_tab…",
"right_from_the_last_pre_driver_tab_reaches_the_driver_tab…"), and record old → new names in the
SUMMARY. Specifics: the round-trip test's out-of-range fallback expects `RoadmapViz`; the
active-label test's first pair becomes the Roadmap full/compact labels; the stored-Driver test
expects `RoadmapViz`; `shift_d_and_right_both_reach_the_driver_tab` starts from the last pre-Driver
tab (`sub_view_from_index(DRIVER_TAB_INDEX - 1, true)`, Archive in this interim) rather than from
Browse; the no-width-tier test's active indices stay inside `0..visible_tab_count(false)`; the
footer-string tests expect the new digit-range prefix; the anti-drift width test is unchanged and
must pass. In `src/app.rs`, rename `the_driver_sub_view_is_index_ten_in_both_directions` to
`the_driver_sub_view_is_the_last_tab_index_in_both_directions` and assert the literal `9`, `10 →
RoadmapViz` (the point of the test is the literal index). In `render_escape_guard.rs`, make the
arrival table and prose agree with the ten probed sub-views; the set-equality test must pass.

Historical prose that quotes old label strings (for example the width docs' account of which
tabs used to fall off an 80-column bar) is reworded to describe those tabs without their old
label strings, so the sweep below can stay strict. Finish with the Pitfall-5 sweep (see
acceptance) and the full suite.
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
    <automated>rtk proxy cargo test --lib app::tests</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or a FAILED line naming `archive_milestone_view_keeps_its_content_across_the_periodic_prune` or any other `app::tests::` test</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer `test result:` lines than the 49-suite baseline recorded in 24-RESEARCH.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `rtk proxy git grep -n -F -e '[1-0' -e '1:Phases' -e '5:Pipe' -e '8:Arch' -e '0:Docs' -e 'DetailSubView::PhaseList' -- src tests` prints nothing.
    - The five archive regression tests (`archive_milestone_view_keeps_its_content_across_the_periodic_prune`, `the_same_milestone_version_in_two_projects_does_not_share_a_cache_entry`, `archive_milestone_view_reloads_in_place_when_planning_files_change`, `the_archive_milestone_list_picks_up_a_newly_archived_milestone_in_place`, `an_in_place_reload_that_shrinks_the_listing_keeps_the_cursor_on_a_row`) appear as `ok` with their source unchanged.
    - `rtk proxy cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Every tab-index consumer in code and tests agrees with Roadmap · Phases · Backlog · Git · Queue · Sess · Cfg · Docs (+ interim Archive, + Driver), and the full suite is green apart from the known witness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keypress → tab index → sub-view | a mismatched mapping silently lands the user on the wrong tab (and can run a tab's arrival work, e.g. scans, unasked) |
| third-party text → cells (escape-guard probe) | removing a tab must not silently drop the only render site the probe proves escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-09 | Tampering (integrity) | `tab_index` / `sub_view_from_index` / digit arms | medium | mitigate | exhaustive wildcard-free `tab_index`; round-trip test over every index; stale-literal git-grep sweep; the zero key deleted rather than remapped |
| T-24-10 | Repudiation / Information Disclosure | escape-guard coverage after PhaseList removal | medium | mitigate | `ALL_SUB_VIEWS` and `DETAIL_TAB_ARRIVAL` kept in set equality; any field whose only render site disappears is recorded, not faked |
| T-24-11 | Denial of Service | a stored Driver view with the flag off | low | mitigate | `effective_sub_view` fallback to `RoadmapViz` keeps the existing coercion at all three read sites |
| T-24-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `rtk proxy cargo test --no-fail-fast`: only the known git-version witness fails; 49 suites ran.
- `rtk proxy cargo clippy -- -D warnings` clean.
- The Pitfall-5 git-grep sweep prints nothing.
- The five `app::tests` archive regression tests pass with unchanged source.
</verification>

<success_criteria>
- Tabs 1-8 are in the locked order with the locked labels; PhaseList is gone; Pipeline shows as Phases; Roadmap is the default; `0` is inert.
- All tab tests, the escape-guard probe and the archive regression tests pass.
</success_criteria>

<output>
Create `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-03-SUMMARY.md` when done
</output>
