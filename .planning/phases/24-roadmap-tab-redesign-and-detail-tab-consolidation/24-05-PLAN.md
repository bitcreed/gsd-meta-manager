---
phase: 24-roadmap-tab-redesign-and-detail-tab-consolidation
plan: 05
type: execute
wave: 2
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - src/ui/screens/detail.rs
  - src/ui/screens/mod.rs
  - src/ui/screens/help.rs
autonomous: true
requirements: [D-A05, D-A07, D-A10, D-A11, D-A12, D-B03, D-B08, D-B12]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One adapter, `roadmap_model_for`, turns a `ProjectState` into the 24-02 `RoadmapModel`: `state.phases` then `state.planned_phases` (deduped by `phase_key`, GSD phase wins) as nodes, each band from `roadmap_md::milestone_index_of`, shipped flags from `shipped_milestones`, a synthetic band named `{state.milestone} {milestone_name}` for phases no milestone holds when no roadmap milestone is active, markers from `state.phase_marker(phase)` (D-B12), plan counts from `phase_plan_counts`, goals from `phase_goals`, and the `[stage]` badge from `disk_suffix_spans` (D-B08)."
    - "Build phases are selectable Roadmap phases: the cursor reaches ttbook's 14-18 with `j`/`k`/`G`/`h`/`l`/`[`/`]` like any other phase; `Enter` on one returns a status message saying it is a planned placeholder with no Phases entry (D-A12, SC-4)."
    - "On the Roadmap tab (graph view), `j`/`Down` and `k`/`Up` move the cursor over phase, band and shipped-summary rows; `PageDown`/`PageUp` move it by the last rendered list height; `g`/`G` go to the first/last row; `h` jumps to a dependency and repeated `h` cycles the ORIGIN phase's needs then implied deps; `l` does the same over what it unblocks; `[`/`]` step through phases in the same wave with wrap; `Space` folds/unfolds the milestone under the cursor (a phase's own band); `←`/`→` still switch tabs; `v` still toggles the box view, where the old scroll keys keep scrolling (D-A10)."
    - "`Enter` on a Roadmap phase opens the Phases tab on that phase (`pipeline_selected` = its index in `state.phases`, switched with `tab_index(&DetailSubView::Pipeline)`), and moving the Phases selection writes the Roadmap cursor back — one shared selected phase (D-B03)."
    - "`Enter` on the collapsed shipped-milestones row goes to the Archive view (`tab_index(&DetailSubView::Archive)` — plan 24-07 re-routes it to Docs › Milestones); `Enter` on a milestone band folds it like `Space` (D-A07, D-B04 prep)."
    - "The cursor is stored as a `CursorTarget` (phase_key or band key), never an index, so it survives ROADMAP reloads and folds; a fold that hides the cursor's phase leaves the cursor on that band row; any key other than `h`/`l` ends an edge walk (D-A05)."
    - "No mouse handling is added anywhere (D-A11: deferred)."
    - "The help popup documents the Roadmap keys as whole rows and no longer carries the stale `r` row; the `v` row is byte-identical; the Roadmap footer advertises `h/l` and `Space` and keeps `[v]` and `[e]`."
  artifacts:
    - path: "src/ui/screens/mod.rs"
      provides: "ProjectViewCache.roadmap_cursor / roadmap_fold_toggles / roadmap_edge_walk"
      contains: "pub roadmap_cursor: Option<crate::ui::roadmap_graph::CursorTarget>"
    - path: "src/ui/screens/detail.rs"
      provides: "roadmap_model_for adapter, Roadmap key arms, Pipeline→Roadmap selection sync, DetailScreen roadmap_list_offset / roadmap_list_viewport cells, Roadmap footer hints, fixture_state test helper"
      contains: "fn roadmap_model_for"
    - path: "src/ui/screens/help.rs"
      provides: "Roadmap key rows replacing the stale `r` row"
      contains: "Roadmap tab: fold / unfold a milestone (detail view)"
  key_links:
    - from: "src/ui/screens/detail.rs (RoadmapViz Enter arm)"
      to: "src/ui/screens/detail.rs switch_to_tab"
      via: "tab_index(&DetailSubView::Pipeline) after writing pipeline_selected"
      pattern: "tab_index\\(&DetailSubView::Pipeline\\)"
    - from: "src/ui/screens/detail.rs roadmap_model_for"
      to: "src/ui/roadmap_graph.rs layout_list"
      via: "ListInput built from ProjectState"
      pattern: "roadmap_graph::layout_list"
    - from: "Pipeline j/k/PageUp/PageDown arms"
      to: "ProjectViewCache.roadmap_cursor"
      via: "phase_key of the newly selected phase"
      pattern: "roadmap_cursor = Some"
  prohibitions:
    - "No literal tab index at a handoff site."
    - "No mouse event handling (D-A11)."
    - "`render_roadmap`'s drawing is not changed here (plan 24-06 owns the render; this plan only adds state, keys and the footer/help text)."
---

<objective>
Give the Roadmap tab its cursor: the ProjectState → model adapter, the per-project cursor/fold
state, every Roadmap key of D-A10, and the shared selected phase with the Phases tab (D-B03).

Purpose: success criterion 2's key half, and the handoff half of criterion 5, testable through
`handle_key` against the real fixtures before the new drawing lands (24-06 renders exactly the
cursor this plan stores).

Dependency: `depends_on: [24-01, 24-02, 24-03]`. **Semantic** on 24-01 (the adapter reads
`planned_phases`, `phase_goals`, `milestone_name`, `shipped_milestones` and the vendored
fixtures) and on 24-02 (`layout_list`, `CursorTarget`, `EdgeWalk` and the navigation methods).
**Shared file** with 24-03 (`src/ui/screens/detail.rs`, whose tab order this plan's tests assume).
Files are disjoint from its wave-2 sibling 24-04 (`src/ui/roadmap_view.rs`, `src/ui/mod.rs`).

Output: cursor state, key arms, adapter, footer and help rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-PATTERNS.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-01-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-02-SUMMARY.md
@.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-03-SUMMARY.md
</context>

<interfaces>
From 24-02 (`crate::ui::roadmap_graph`): `ListNode`, `BandInput`, `ListInput`, `layout_list(&ListInput, &HashSet<BandKey>) -> RoadmapModel`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, and `RoadmapModel::{visible_targets, resolve_cursor, row_of, step, first_target, last_target, phase_index, edge_jump, wave_step, unfold_for}`.
From 24-01 (`crate::state_reader`): `ProjectState.{planned_phases, phase_goals, milestone_name}`, `roadmap_md::{shipped_milestones, declared_phase_count, milestone_index_of, active_milestone_index}`, `phase_plan_counts`, `ProjectState::phase_marker`.
If a SUMMARY records a deviation from these names, the merged code wins.

Created here:
- `ProjectViewCache { pub roadmap_cursor: Option<CursorTarget>, pub roadmap_fold_toggles: HashSet<BandKey>, pub roadmap_edge_walk: Option<EdgeWalk>, .. }`
- `DetailScreen { roadmap_list_offset: Cell<usize>, roadmap_list_viewport: Cell<u16>, .. }` (24-06's render writes both)
- `pub(crate) fn roadmap_model_for(state: &ProjectState, cache: Option<&ProjectViewCache>, show_badges: bool) -> RoadmapModel`
- test helper `fixture_state(name: &str) -> ProjectState` (writes `tests/fixtures/roadmaps/{name}-ROADMAP.md` / `-STATE.md` from `include_str!` into a tempdir `.planning/` and calls `parse_project_state`)
</interfaces>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-wide inventory (identical in every Phase 24 plan; newly-created names are not drift).

**New files:** `tests/fixtures/roadmaps/README.md`, `tests/fixtures/roadmaps/{daily-vow,sentriq,ttbook}-{ROADMAP,STATE}.md` (24-01), `src/ui/roadmap_view.rs` (24-04).
**Parser functions (24-01):** `parse_phase_goals`, `parse_planned_build_phases`, `shipped_milestones`, `declared_phase_count`; `ProjectState.{phase_goals, planned_phases, milestone_name}`.
**Engine types and functions (24-02):** `ListNode`, `BandInput`, `ListInput`, `RoadmapModel`, `ListRow`, `PhaseFacts`, `BandFacts`, `PhaseStatus`, `BandKey`, `CursorTarget`, `EdgeDir`, `EdgeWalk`, `layout_list`, `is_folded`, `lane_text`, the `RoadmapModel` navigation methods, the glyph consts.
**Widget (24-04):** `RoadmapView`, `RoadmapViewState`, `ROADMAP_SIDE_BY_SIDE_MIN_COLS`, `LANE_CAP_NARROW`, `LANE_CAP_WIDE`, `LANE_OVERFLOW`, `panes`, `keep_visible`.
**View state (24-05):** `ProjectViewCache.{roadmap_cursor, roadmap_fold_toggles, roadmap_edge_walk}`; `DetailScreen.{roadmap_list_offset, roadmap_list_viewport}`; `roadmap_model_for`.
**Detail screen (24-06/24-07):** `roadmap_summary_line`, `switch_to_sub_view`, `docs_sub_tab_strip`; test helpers `render_detail_to_text_at`, `roadmap_list_pane`.
**Keys:** Roadmap `j`/`k`/`Up`/`Down`/`PageUp`/`PageDown`/`g`/`G`/`h`/`l`/`[`/`]`/`Space`/`Enter` (24-05); Docs `m` (24-07).
**Constants changed:** `TAB_COUNT` 11 → 10 → 9, `DRIVER_TAB_INDEX` 10 → 9 → 8, `TAB_BAR_*` re-derived, label arrays reordered (24-03, 24-07).
**Removed:** `DetailSubView` variant `PhaseList`, `render_phase_list` (24-03); the old graph layout and `RoadmapGraphWidget` (24-06); stale help row for `r` (24-05); detail-view digits `9`/`0` (24-03/24-07).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Enter on a Roadmap phase opens it in Phases — adapter, cursor state, shared selection</name>
  <reversibility rating="reversible">Additive `ProjectViewCache` fields (`#[derive(Default)]`, zero constructor churn) and new key arms; the view state is in memory only (D-B09).</reversibility>
  <files>src/ui/screens/mod.rs, src/ui/screens/detail.rs</files>
  <read_first>
    - `src/ui/screens/mod.rs` — `ProjectViewCache` (~906-1040): `pipeline_selected`, `roadmap_box_view` doc style, the "additive with zero constructor churn" comment (~1019-1024).
    - `src/ui/screens/detail.rs` — re-read after 24-03 merged: `DetailScreen` fields and `new` (~640-700, the `Cell` viewports), `tab_index` / `switch_to_tab` (confirm it does not reset `pipeline_selected` on arrival), the `Enter | Char(' ')` arm's per-view match (~2262), the Pipeline `j`/`k`/`PageDown`/`PageUp` arms (~1674, ~1828, ~1954, ~2118), `disk_suffix_spans` (~1206-1280), `render_pipeline_tab`'s clamp of `pipeline_selected` (~4328), and the test helpers `TEST_ALIAS`, `test_ctx`, `press` (~10415-10526).
    - `src/ui/roadmap_graph.rs` — the 24-02 contract (interfaces block above).
    - `src/state_reader/roadmap_md.rs` / `src/state_reader/mod.rs` — the 24-01 additions.
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-RESEARCH.md` — § Pattern 4 (selection model, shared selection), § Real Fixtures (sentriq synthetic band).
  </read_first>
  <action>
**Cache.** Add to `ProjectViewCache`, with docs in the `roadmap_box_view` register:
`roadmap_cursor: Option<CursorTarget>` (a reader-generated key, not third-party prose; `None`
means "resolve to the default"), `roadmap_fold_toggles: HashSet<BandKey>` (bands whose fold state
differs from the default — shipped summary folded, others open), `roadmap_edge_walk:
Option<EdgeWalk>` (the `h`/`l` cycle). Add `roadmap_list_offset: Cell<usize>` and
`roadmap_list_viewport: Cell<u16>` to `DetailScreen` (documented like `git_commit_viewport`;
initialised in `new`).

**Adapter `roadmap_model_for`** (the interfaces block): nodes = `state.phases` in order, then
`state.planned_phases` whose `phase_key` is not already present (`planned: true`); for each node
`band = milestone_index_of(&state.milestones, &number)`, else — when
`active_milestone_index(&state.milestones, &state.milestone)` is `Some(a)` — `a`, else a synthetic
band appended after the real ones and labelled `Untrusted::from_untrusted_source(format!("{}
{}", state.milestone, name))` using `milestone_name`'s raw text (just `state.milestone` when there
is no name), else `None` when `state.milestone` is empty [INFERRED — audit]; `bands` = every
`state.milestones` entry as `BandInput { label: m.label.clone(), shipped: shipped_milestones(..)[i],
declared_phases: declared_phase_count(m) }` plus the synthetic one; `marker =
state.phase_marker(phase)` (D-B12, the folded todo's single source of truth); `plans =
phase_plan_counts(phase, &state.phase_disk_statuses)`; `goal = state.phase_goals.get(&phase_key(..))`;
`badge` = the concatenated, trimmed text of `disk_suffix_spans(&number,
&state.phase_disk_statuses, show_badges)` (`None` when empty). Fold toggles from the cache (empty
when none). No I/O — in-memory state only (CLAUDE.md: never block the render loop).

**Enter (tracer slice).** In the `Enter | Char(' ')` arm add a `DetailSubView::RoadmapViz` branch,
active only while `roadmap_box_view` is false: build the model, resolve the stored cursor; on a
`CursorTarget::Phase(key)` whose node is a GSD phase, set `pipeline_selected` to its index in
`state.phases` (matched with `phase_key`), store the resolved cursor, and return
`switch_to_tab(&self.alias, tab_index(&DetailSubView::Pipeline), &mut self.scroll_offset, ctx)`.
(Space, bands, the summary and planned phases arrive in Task 2.)

**Shared selection back.** In the four Pipeline arms (`j`/`Down`, `k`/`Up`, `PageDown`,
`PageUp`), after `pipeline_selected` moves, set `roadmap_cursor =
Some(CursorTarget::Phase(phase_key(&state.phases[pipeline_selected].number)))`.

**Tests** (new `fixture_state` helper per the interfaces block; paths
`include_str!("../../../tests/fixtures/roadmaps/…")`):
`roadmap_model_for_daily_vow_has_one_row_for_phase_20` (exactly one `ListRow::Phase` with id `20`;
`lane_text` contains no line that repeats an id), `roadmap_model_for_sentriq_uses_a_synthetic_band`
(one band row labelled `v0.12 Actuation Routines`, shipped summary present from `v0.11`),
`roadmap_model_for_ttbook_lists_build_phases_as_phases` (ids 8-18 each on one Phase row, 14-18
`planned`), `roadmap_enter_opens_the_selected_phase_in_phases` (daily-vow, RoadmapViz stored,
cursor `Phase("22")`: after `Enter` the stored view is `Pipeline` and `pipeline_selected` is the
index of `22` in `state.phases`), `phases_selection_is_shared_back_to_the_roadmap` (on Pipeline,
`j` sets `roadmap_cursor` to the newly selected phase's key).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::roadmap</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests::phases_selection_is_shared_back_to_the_roadmap</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - The five named tests appear as `ok`.
    - `rtk proxy git grep -n 'tab_index(&DetailSubView::Pipeline)' -- src/ui/screens/detail.rs` prints at least one line inside the RoadmapViz Enter branch.
    - `rtk proxy cargo build` succeeds.
  </acceptance_criteria>
  <done>Pressing Enter on a Roadmap phase lands on the Phases tab with that phase selected, and the Phases selection flows back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Every Roadmap key — move, jump, cycle, wave-step, fold, Enter on bands and build phases</name>
  <files>src/ui/screens/detail.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — the top-level arms for `j`/`Down`, `k`/`Up`, `PageDown`, `PageUp` and their `_ =>` generic-scroll fallbacks (~1799-1808, ~1910-1918, ~2077-2086, ~2207-2215), the guarded single-view arms `g` on Browse (~2721) and `G` on Driver (~3106) as patterns, `v` on RoadmapViz (~3398), and the `_ => ScreenAction::None` fallback. Re-read — lines drift.
    - `src/ui/roadmap_graph.rs` — `edge_jump`, `wave_step`, `unfold_for`, `resolve_cursor` semantics (24-02 Task 3).
    - `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-CONTEXT.md` — D-A10.
  </read_first>
  <behavior>
    - daily-vow, cursor unset: resolves to `23`; `k` → `22`; `j` → `23`; `j` at the last row stays; `g` → the shipped summary; `G` → `23`.
    - daily-vow from `23`: `h` → `21`, `h` → `20`, `h` → `21` (cycling 23's needs then implied); `l` from `21` → `22`, `l` → `23`; `]` from `22` → `23`, `]` → `22`.
    - daily-vow: `g` then `Space` unfolds the shipped summary (five more band rows become targets); `Space` again folds it; `Space` on phase `19` folds `v1.5` and leaves the cursor on the `v1.5` band row.
    - An `h` whose target lies in a folded band unfolds that band.
    - `Enter` on the shipped summary stores `DetailSubView::Archive` as the current view; `Enter` on a band toggles its fold.
    - ttbook: `G` → `18`; `Enter` there returns `ScreenAction::SetStatusMessage` whose text contains `planned placeholder` and the stored view stays `RoadmapViz`.
    - With `roadmap_box_view` on, `j` changes `scroll_offset` and not `roadmap_cursor`; `g`/`h`/`l`/`[`/`]`/`Space`/`Enter` do nothing.
    - Any key other than `h`/`l` clears `roadmap_edge_walk`.
  </behavior>
  <action>
Add a private helper on `DetailScreen` that builds the model with `roadmap_model_for`, resolves
the stored cursor, applies one navigation operation, calls `unfold_for` when the new target is
hidden (`row_of` is `None`), stores the new cursor (and walk, for `h`/`l`), and sets
`needs_redraw` — so every arm is one line and the resolution rule exists once.

Arms, all on `current_view == DetailSubView::RoadmapViz` with the box view off: explicit
`RoadmapViz` branches inside the `j`/`Down`, `k`/`Up`, `PageDown`, `PageUp` matches (page size =
`roadmap_list_viewport` minus one, at least 1; the box view keeps the existing generic scroll);
guarded arms for `g`, `G`, `h`, `l`, `[`, `]` placed before the catch-all; in the `Enter | Space`
branch: `Space` toggles the fold of the band under the cursor or of the cursor phase's band;
`Enter` on a band toggles its fold, on the shipped summary returns
`switch_to_tab(&self.alias, tab_index(&DetailSubView::Archive), ..)` (plan 24-07 re-routes this
to Docs › Milestones), on a planned phase returns `ScreenAction::SetStatusMessage(format!("Build
phase {id} is a planned placeholder, not a GSD phase — it has no Phases entry"))` (escape the id
with `shown`), on a GSD phase does Task 1's handoff. Clear `roadmap_edge_walk` in every Roadmap
arm except `h`/`l`. Keep `v`, `Left`, `Right`, `e` and the digits unchanged. Add no mouse
handling (D-A11).

Write one test per behaviour group: `roadmap_j_k_g_capital_g_move_the_cursor`,
`roadmap_h_cycles_needs_then_implied`, `roadmap_l_and_brackets_follow_edges_and_waves`,
`roadmap_space_folds_and_the_cursor_moves_to_the_band`, `roadmap_jump_into_a_folded_band_unfolds_it`,
`roadmap_enter_on_the_shipped_row_opens_the_archive_view`,
`roadmap_enter_on_a_band_toggles_its_fold`, `roadmap_enter_on_a_build_phase_explains_itself`,
`roadmap_box_view_keeps_generic_scroll`, `other_roadmap_keys_end_the_edge_walk`,
`roadmap_keys_are_inert_without_project_state`. Update any pre-existing test that pressed
`j`/`k` on the Roadmap graph expecting a scroll (rewrite it to assert the cursor).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::detail::tests</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
  </verify>
  <acceptance_criteria>
    - The eleven tests named in this task appear as `ok`.
    - `git diff $PLAN_BASE -- src/ui/screens/` adds no line containing `Mouse` (D-A11; `$PLAN_BASE` is the commit this plan's worktree started from; diffing against it covers committed and uncommitted work).
    - `roadmap_space_folds_and_the_cursor_moves_to_the_band` asserts the resolved cursor is the `v1.5` band target after `Space` on phase `19`.
  </acceptance_criteria>
  <done>Every D-A10 key does what the locked decision says on real fixture data, including build phases and folds.</done>
</task>

<task type="auto">
  <name>Task 3: Footer hints and help rows for the Roadmap keys</name>
  <files>src/ui/screens/detail.rs, src/ui/screens/help.rs</files>
  <read_first>
    - `src/ui/screens/detail.rs` — `footer_spans`' `RoadmapViz` arm (~6227) and `roadmap_graph_tab_footer_advertises_v` (~14513).
    - `src/ui/screens/help.rs` — `help_lines` (~200-234), `the_flag_off_help_body_is_shorter_but_still_documents_the_other_keys` (~843-868), `the_body_is_long_enough_that_the_scroll_work_was_required` (~870), `the_roadmap_graph_toggle_key_is_documented` (~884-896), and the comment about byte-identical rows near `row("b", …)`.
  </read_first>
  <action>
**Footer.** In `footer_spans`' `RoadmapViz` arm, before the existing `[v]iew` and `[e]nqueue`
hints, add `[h/l]` + `edge  ` and `[Space]` + `fold  ` in the arm's existing bold-key style.

**Help.** Replace the stale `r` row (bound only on the Config tab) with five rows, each unique
across the body: `row("j/k g/G", "Roadmap tab: move the phase cursor / top / bottom (detail view)")`,
`row("h / l", "Roadmap tab: jump to a dependency / to a phase it unblocks (detail view)")`,
`row("[ / ]", "Roadmap tab: previous / next phase in the same wave (detail view)")`,
`row("Space", "Roadmap tab: fold / unfold a milestone (detail view)")`,
`row("Enter", "Roadmap tab: open the phase in Phases (detail view)")`. Keep the `v` row
byte-identical. In `the_flag_off_help_body_is_shorter_but_still_documents_the_other_keys`, swap
the `r` entry for the `Space` row. Add `the_roadmap_cursor_keys_are_documented_as_whole_rows`
(each of the five rows present as a whole line) and `the_stale_roadmap_toggle_row_is_gone`.
Add `roadmap_footer_advertises_edges_and_fold` in `detail.rs` (contains `[h/l]`, `[Space]`, `[v]`,
`[e]`).
  </action>
  <verify>
    <automated>rtk proxy cargo test --lib ui::screens::help</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed", or any FAILED line</fails_when>
    <automated>rtk proxy cargo test --no-fail-fast</automated>
    <fails_when>any FAILED test other than `envelope::policy::tests::the_config_section_constants_record_the_git_version_they_were_derived_against`, or fewer `test result:` lines than the 49-suite baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - `the_roadmap_cursor_keys_are_documented_as_whole_rows`, `the_stale_roadmap_toggle_row_is_gone`, `the_roadmap_graph_toggle_key_is_documented` and `roadmap_footer_advertises_edges_and_fold` appear as `ok`.
    - `help_lines(true)` contains no row whose key cell is `r` and whose description mentions the Roadmap (asserted by `the_stale_roadmap_toggle_row_is_gone`).
    - `rtk proxy cargo clippy -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The Roadmap keys are discoverable from the footer and the help popup, and the stale row is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keypress → tab handoff | a wrong index lands on the wrong tab and runs its arrival work |
| third-party phase id → status message | a build-phase id is echoed into the status line |
| stored cursor → state after a ROADMAP reload | an index into a changed list could panic or select the wrong phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-15 | Tampering (integrity) | Roadmap → Phases / Archive handoff | medium | mitigate | `tab_index(&view)` only; `pipeline_selected` computed by `phase_key` match; handoff tests on fixtures |
| T-24-16 | Denial of Service | cursor or `pipeline_selected` out of range after reload | medium | mitigate | cursor stored as a key and re-resolved every use (`resolve_cursor` falls back); `render_pipeline_tab` already clamps `pipeline_selected` |
| T-24-17 | Tampering | build-phase id in the status message | medium | mitigate | id passed through `shown()` before formatting |
| T-24-18 | Elevation of Privilege | a key arm on the Roadmap reaching the driver | low | accept | no arm added here starts, stops or injects into a run; Driver arms stay guarded by `current_view == Driver` |
| T-24-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `rtk proxy cargo test --no-fail-fast`: only the known git-version witness fails; 49 suites ran.
- `rtk proxy cargo clippy -- -D warnings` clean.
- The five `app::tests` archive regression tests still pass unchanged.
</verification>

<success_criteria>
- The cursor visits every phase (GSD and build) and band; h/l/[/]/Space/Enter behave per D-A10.
- Roadmap and Phases share the selected phase in both directions.
- Help and footer document the keys.
</success_criteria>

<output>
Create `.planning/phases/24-roadmap-tab-redesign-and-detail-tab-consolidation/24-05-SUMMARY.md` when done
</output>
