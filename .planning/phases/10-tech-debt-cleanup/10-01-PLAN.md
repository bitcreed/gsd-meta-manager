---
phase: 10-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/change_tracker.rs
  - src/state_reader/mod.rs
  - src/state_reader/state_md.rs
  - src/state_reader/roadmap_md.rs
  - src/state_reader/backlog.rs
  - src/state_reader/config_json.rs
  - src/ui/screens/mod.rs
  - src/session_detector.rs
  - src/config.rs
  - src/action.rs
  - src/app.rs
  - tests/state_reader_test.rs
autonomous: true
requirements: [DEBT-01, DEBT-02]

must_haves:
  truths:
    - "`cargo build` completes with zero warnings and no `#[allow(dead_code)]` or `#[allow(unused)]` suppressions"
    - "`cargo clippy` produces zero warnings"
    - "`cargo fmt --check` reports no formatting differences"
    - "`cargo test` passes all 20 tests including the end_to_end_add_then_list_via_cli integration test"
  artifacts:
    - path: "src/change_tracker.rs"
      provides: "Change tracker without dead_code suppression"
      contains: "struct ChangeTracker"
    - path: "src/state_reader/mod.rs"
      provides: "State reader module without dead_code suppression on config_json"
    - path: "src/ui/screens/mod.rs"
      provides: "Screen trait and ScreenAction without allow(unused)/allow(dead_code)"
  key_links:
    - from: "src/state_reader/mod.rs"
      to: "src/state_reader/config_json.rs"
      via: "module declaration"
      pattern: "pub mod config_json"
    - from: "tests/state_reader_test.rs"
      to: "src/state_reader/config_json.rs"
      via: "test imports"
      pattern: "use gsd_meta_manager::state_reader::config_json"
    - from: "src/change_tracker.rs"
      to: "src/state_reader/mod.rs"
      via: "ProjectState import"
      pattern: "use crate::state_reader::ProjectState"
---

<objective>
Resolve all compiler warnings, clippy lints, and formatting issues to establish a clean baseline for v1.2 feature work.

Purpose: Eliminate 8 `#[allow(dead_code)]`/`#[allow(unused)]` suppressions, fix 4 clippy warnings, and correct formatting so that CI-quality checks pass cleanly. This prevents the archive browser (Phase 12) from needing to reintroduce suppressions.
Output: Zero-warning build with all 20 tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tech-debt-cleanup/10-CONTEXT.md
@.planning/research/PITFALLS.md (Pitfall 3: Tech debt cleanup breaks existing tests)
@.planning/research/ARCHITECTURE.md (Phase 12 integration points — which structs are needed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and resolve all dead code annotations</name>
  <files>
    src/change_tracker.rs,
    src/state_reader/mod.rs,
    src/state_reader/state_md.rs,
    src/state_reader/roadmap_md.rs,
    src/state_reader/backlog.rs,
    src/ui/screens/mod.rs,
    tests/state_reader_test.rs
  </files>
  <read_first>
    src/change_tracker.rs,
    src/state_reader/mod.rs,
    src/state_reader/state_md.rs,
    src/state_reader/roadmap_md.rs,
    src/state_reader/backlog.rs,
    src/state_reader/config_json.rs,
    src/ui/screens/mod.rs,
    src/app.rs,
    tests/state_reader_test.rs,
    tests/registry_test.rs
  </read_first>
  <action>
    Resolve each of the 8 `#[allow(dead_code)]`/`#[allow(unused)]` annotations one at a time. Run `cargo test` after each removal to catch regressions immediately (per Pitfall 3 from PITFALLS.md).

    **1. `src/change_tracker.rs:14` — `ProjectSnapshot` struct (per D-04)**
    The `initial_snapshots` HashMap is populated via `record_initial()` but never read anywhere. The struct `ProjectSnapshot` and `from_state()` are genuinely dead. Options:
    - REMOVE `ProjectSnapshot` struct, `from_state()` impl, and `initial_snapshots` field from `ChangeTracker`. Keep `record_initial()` as a no-op or remove it too. If removing `record_initial()`, also remove the call site in `src/app.rs` (around line 153).
    - Run `cargo test` after this change.

    **2. `src/state_reader/state_md.rs:4` — `StateFrontmatter` struct (per D-02)**
    This struct IS used: `parse_state_md()` returns `Option<StateFrontmatter>` and its fields are read in `parse_project_state()` in `mod.rs`. The `#[allow(dead_code)]` suppresses warnings about fields like `gsd_state_version`, `milestone_name`, `last_updated`, `last_activity` which are deserialized but not consumed by `parse_project_state()`. These fields WILL be needed by the archive browser (Phase 12) for milestone metadata display.
    - KEEP the struct as-is but remove the `#[allow(dead_code)]` annotation.
    - Add `#[cfg(test)]` assertions in the existing test `test_parse_real_state_md` that read the currently-unused fields (`gsd_state_version`, `milestone_name`, `last_updated`) to make them "used". The test already asserts on `gsd_state_version` and `milestone_name` — check if `last_updated` and `last_activity` need assertions too.
    - Alternatively, if the compiler does NOT warn about individual fields on a Deserialize struct (only about the struct itself being unused), simply removing the annotation may be sufficient. Try removing it first and check.
    - Run `cargo test` after.

    **3. `src/state_reader/state_md.rs:25` — `ProgressInfo` struct (per D-02)**
    Same situation as StateFrontmatter. The `percent` field is deserialized but not consumed downstream. The test `test_parse_real_state_md` already asserts `fm.progress.percent == 0`.
    - REMOVE the `#[allow(dead_code)]` annotation.
    - Verify no new warnings appear (Deserialize structs with all pub fields may not trigger dead_code).
    - Run `cargo test` after.

    **4. `src/state_reader/roadmap_md.rs:4` — `RoadmapPhase` struct (per D-02)**
    This struct IS actively used: `parse_roadmap_phases()` creates them, `ProjectState.phases` stores them, `change_tracker.rs` reads `p.number`, `p.name`, `p.completed`. The `description` field is set during parsing but only consumed in tests (and will be needed by archive browser).
    - REMOVE the `#[allow(dead_code)]` annotation.
    - Verify: if `description` field triggers a warning, add a test assertion for it in the existing `test_parse_roadmap_phases_real` test (it already checks `phases[0].description` via `contains("Async TUI foundation")`... but only in `state_reader_test.rs`, not the unit test). The integration test `test_parse_real_roadmap` checks `phases[0].description.contains("Async TUI foundation")` — this should suffice.
    - Also check `total_plans` and `completed_plans` — the tests exercise these. Should be fine.
    - Run `cargo test` after.

    **5. `src/state_reader/backlog.rs:7` — `BacklogItem.slug` field (per D-03)**
    The `slug` field is populated during parsing in `parse_backlog_items()` but never read downstream. Check all consumers:
    - `src/ui/screens/detail.rs` uses `BacklogItem.description`, `.dir_name`, `.content`, `.number` — grep for `.slug` usage.
    - If `.slug` has zero consumers: REMOVE the field from `BacklogItem`, remove it from the struct literal in `parse_backlog_items()`, and remove the `#[allow(dead_code)]` annotation. The `parse_backlog_dir_name()` function returns `(number, slug)` — keep it returning the tuple but just don't store slug in the struct (use `_` for the slug in the destructuring, or keep the function as-is since it's tested).
    - Run `cargo test` after.

    **6. `src/state_reader/mod.rs:3` — `#[allow(dead_code)]` on `config_json` module (per D-04)**
    The `config_json` module defines `GsdConfig` and `parse_gsd_config()`. These are NOT used by `parse_project_state()` — the module is dead from the library's production code perspective. HOWEVER, `tests/state_reader_test.rs` imports and tests `parse_gsd_config`. Decision:
    - KEEP the module (it has tests exercising it and the archive browser may use it).
    - REMOVE the `#[allow(dead_code)]` on the module.
    - The module's public items are tested in integration tests — this should suppress dead_code warnings since they're reachable from `pub` exports. If warnings persist, the module's public API surface makes it "used" from a library perspective (integration tests link against the lib).
    - Run `cargo test` after.

    **7. `src/ui/screens/mod.rs:26` — `#[allow(unused)]` on `Screen` trait**
    The `Screen` trait is implemented by 8 screens and used as `Box<dyn Screen>` in the screen stack. It is definitely used. The `#[allow(unused)]` was likely added to suppress warnings about trait methods not being called directly (they're called via dynamic dispatch).
    - REMOVE the `#[allow(unused)]` annotation.
    - If the `name()` method triggers an unused warning (it's defined on the trait but may not be called), add a `#[allow(unused)]` specifically on the `name()` method, or verify it IS called somewhere.
    - Run `cargo test` after.

    **8. `src/ui/screens/mod.rs:33` — `#[allow(dead_code)]` on `ScreenAction` enum (per D-04)**
    All 6 variants are matched in `app.rs` (line 351-369). However, `DispatchAction` is matched but never constructed — it's handled but never produced. Per D-02, the archive browser (Phase 12) will likely need `DispatchAction` for sending `Action::ArchivesLoaded` from within a screen's key handler.
    - REMOVE the `#[allow(dead_code)]` annotation.
    - If `DispatchAction` variant triggers a "never constructed" warning: keep it with a targeted `#[allow(dead_code)]` comment explaining it's reserved for Phase 12 archive browser. Example:
      ```rust
      /// Used by archive browser (Phase 12) to dispatch async load actions.
      #[allow(dead_code)]
      DispatchAction(Action),
      ```
    - Run `cargo test` after.
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "allow(dead_code)" src/` returns at most 1 result (the targeted DispatchAction annotation if needed)
    - `grep -rn "allow(unused)" src/` returns 0 results
    - `cargo test` reports "20 passed; 0 failed"
  </acceptance_criteria>
  <done>All `#[allow(dead_code)]` and `#[allow(unused)]` annotations removed (except one targeted annotation on DispatchAction if compiler requires it). All 20 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix clippy warnings and formatting, verify clean baseline</name>
  <files>
    src/change_tracker.rs,
    src/config.rs,
    src/session_detector.rs,
    src/state_reader/state_md.rs,
    src/action.rs,
    src/app.rs
  </files>
  <read_first>
    src/change_tracker.rs,
    src/config.rs,
    src/session_detector.rs,
    src/state_reader/state_md.rs,
    src/action.rs,
    src/app.rs
  </read_first>
  <action>
    Fix all clippy warnings and formatting issues identified by `cargo clippy` and `cargo fmt --check`. Per D-05 and D-06.

    **Clippy fix 1: `src/change_tracker.rs` — `new_without_default`**
    Add a `Default` implementation for `ChangeTracker`:
    ```rust
    impl Default for ChangeTracker {
        fn default() -> Self {
            Self::new()
        }
    }
    ```
    Note: if Task 1 removed `initial_snapshots`, the `new()` method may have changed. Adjust accordingly.

    **Clippy fix 2: `src/config.rs` — `new_without_default`**
    Add a `Default` implementation for `Config`. The struct already has fields that can default:
    ```rust
    impl Default for Config {
        fn default() -> Self {
            Self::new()
        }
    }
    ```

    **Clippy fix 3: `src/session_detector.rs:24` — `redundant_closure`**
    Replace `.filter_map(|pid| build_session(pid))` with `.filter_map(build_session)`.

    **Clippy fix 4: `src/state_reader/state_md.rs:48` — `manual_pattern_char_comparison`**
    Replace `.trim_start_matches(|c: char| c == '\r' || c == '\n')` with `.trim_start_matches(['\r', '\n'])`.

    **Formatting fixes (`cargo fmt`)**
    Run `cargo fmt` to fix all formatting issues. The `cargo fmt --check` output showed diffs in:
    - `src/action.rs:1` — import ordering
    - `src/app.rs:4` — import ordering
    - Possibly more (the output was truncated)

    Simply run `cargo fmt` to auto-fix all of these.

    **Final verification sequence:**
    1. Run `cargo fmt` (fixes all formatting)
    2. Run `cargo clippy 2>&1` (should produce zero warnings)
    3. Run `cargo build 2>&1` (should produce zero warnings)
    4. Run `cargo test` (all 20 tests must pass, including the `end_to_end_add_then_list_via_cli` integration test)
  </action>
  <verify>
    <automated>cd /home/blk/projects/rust/gsd-manager && cargo fmt --check 2>&1 && cargo clippy 2>&1 | grep -c "warning:" | grep "^0$" && cargo build 2>&1 | grep -c "warning" | grep "^0$" && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` exits with code 0 (no formatting differences)
    - `cargo clippy 2>&1 | grep "warning:"` returns empty (zero warnings)
    - `cargo build 2>&1 | grep "warning"` returns empty (zero warnings)
    - `cargo test` reports "20 passed; 0 failed"
    - `grep -rn "new_without_default\|redundant_closure\|manual_pattern_char" src/` returns 0 results
  </acceptance_criteria>
  <done>Zero clippy warnings, zero build warnings, perfect formatting, all 20 tests passing. Clean baseline established for v1.2 feature work.</done>
</task>

</tasks>

<verification>
Run the full verification sequence to confirm Phase 10 success criteria:

```bash
# 1. No dead_code/unused suppressions (except targeted DispatchAction)
grep -rn "allow(dead_code)\|allow(unused)" src/ | grep -v "DispatchAction"
# Expected: empty output

# 2. Zero build warnings
cargo build 2>&1 | grep "warning"
# Expected: empty output (or only "Compiling"/"Finished" lines)

# 3. Zero clippy warnings
cargo clippy 2>&1 | grep "warning:"
# Expected: empty output

# 4. Clean formatting
cargo fmt --check
# Expected: exit code 0

# 5. All tests pass
cargo test
# Expected: "test result: ok. 20 passed; 0 failed"
```
</verification>

<success_criteria>
1. `cargo build` completes with zero warnings (no `#[allow(dead_code)]` suppressions except the one targeted annotation on DispatchAction if compiler requires it)
2. `cargo clippy` produces zero warnings
3. `cargo fmt --check` passes with no diffs
4. `cargo test` passes all 20 tests including `end_to_end_add_then_list_via_cli`
</success_criteria>

<output>
After completion, create `.planning/phases/10-tech-debt-cleanup/10-01-SUMMARY.md`
</output>
